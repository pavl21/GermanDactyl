From 39d27b5cdf665d696b795a0946fb033cc1c315d3 Mon Sep 17 00:00:00 2001
From: Pavl21 <schwarzpaul123@gmail.com>
Date: Mon, 20 Mar 2023 13:26:46 +0100
Subject: [PATCH] Translations v1.11.3

---
 app/Exceptions/Handler.php                    |     4 +-
 .../Connection/DaemonConnectionException.php  |     4 +
 .../Http/Server/FileSizeTooLargeException.php |     2 +-
 .../Server/ServerStateConflictException.php   |    12 +-
 .../Http/TwoFactorAuthRequiredException.php   |     2 +-
 .../Model/DataValidationException.php         |     2 +-
 .../AutoAllocationNotEnabledException.php     |     2 +-
 ...oAutoAllocationSpaceAvailableException.php |     2 +-
 .../Service/Backup/BackupLockedException.php  |     2 +-
 .../Backup/TooManyBackupsException.php        |     2 +-
 ...tabaseClientFeatureNotEnabledException.php |     2 +-
 .../NoSuitableDatabaseHostException.php       |     2 +-
 .../Database/TooManyDatabasesException.php    |     2 +-
 .../TwoFactorAuthenticationTokenInvalid.php   |     2 +-
 app/Http/Controllers/Admin/ApiController.php  |     2 +-
 .../Controllers/Admin/DatabaseController.php  |    10 +-
 .../Controllers/Admin/LocationController.php  |     4 +-
 .../Controllers/Admin/MountController.php     |     8 +-
 .../Admin/NodeAutoDeployController.php        |     2 +-
 .../Admin/Servers/ServerViewController.php    |     2 +-
 .../Controllers/Admin/ServersController.php   |     4 +-
 .../Admin/Settings/AdvancedController.php     |     2 +-
 .../Admin/Settings/IndexController.php        |     2 +-
 .../Admin/Settings/MailController.php         |     2 +-
 .../Api/Client/ApiKeyController.php           |     2 +-
 .../Api/Client/Servers/BackupController.php   |     6 +-
 .../Api/Client/Servers/CommandController.php  |     2 +-
 .../Servers/NetworkAllocationController.php   |     4 +
 .../Api/Client/Servers/ScheduleController.php |     2 +-
 .../Client/Servers/ScheduleTaskController.php |     8 +-
 .../Api/Client/Servers/SettingsController.php |     2 +-
 .../Api/Client/Servers/StartupController.php  |     4 +-
 .../Client/Servers/WebsocketController.php    |     4 +-
 .../Api/Client/TwoFactorController.php        |     6 +-
 .../Backups/BackupRemoteUploadController.php  |     6 +-
 .../Remote/Backups/BackupStatusController.php |     4 +-
 .../Remote/SftpAuthenticationController.php   |     8 +-
 .../Auth/LoginCheckpointController.php        |     2 +-
 app/Models/Permission.php                     |   102 +-
 app/Notifications/AccountCreated.php          |    10 +-
 app/Notifications/AddedToServer.php           |     6 +-
 app/Notifications/MailTested.php              |     6 +-
 app/Notifications/RemovedFromServer.php       |     6 +-
 app/Notifications/SendPasswordReset.php       |     8 +-
 app/Notifications/ServerInstalled.php         |     6 +-
 .../Backups/InitiateBackupService.php         |     4 +-
 patches/v1.11.3.patch                         | 11053 ----------------
 public/themes/pterodactyl/css/pterodactyl.css |     2 +-
 .../themes/pterodactyl/js/admin/new-server.js |    18 +-
 .../pterodactyl/js/admin/server/transfer.js   |    10 +-
 resources/lang/en/activity.php                |   160 +-
 resources/lang/en/admin/nests.php             |    24 +-
 resources/lang/en/admin/node.php              |    16 +-
 resources/lang/en/admin/server.php            |    38 +-
 resources/lang/en/admin/user.php              |     6 +-
 resources/lang/en/auth.php                    |     2 +-
 resources/lang/en/command/messages.php        |    80 +-
 resources/lang/en/exceptions.php              |    58 +-
 resources/lang/en/pagination.php              |     4 +-
 resources/lang/en/passwords.php               |    10 +-
 resources/lang/en/strings.php                 |   146 +-
 resources/lang/en/validation.php              |   132 +-
 .../scripts/api/server/files/compressFiles.ts |     4 +-
 .../api/server/files/decompressFiles.ts       |     2 +-
 .../scripts/components/NavigationBar.tsx      |     6 +-
 .../auth/ForgotPasswordContainer.tsx          |    20 +-
 .../auth/LoginCheckpointContainer.tsx         |    14 +-
 .../components/auth/LoginContainer.tsx        |    14 +-
 .../components/auth/LoginFormContainer.tsx    |    11 +-
 .../auth/ResetPasswordContainer.tsx           |    22 +-
 .../dashboard/AccountApiContainer.tsx         |    19 +-
 .../dashboard/AccountOverviewContainer.tsx    |    12 +-
 .../components/dashboard/ApiKeyModal.tsx      |     7 +-
 .../dashboard/DashboardContainer.tsx          |     6 +-
 .../components/dashboard/ServerRow.tsx        |    22 +-
 .../activity/ActivityLogContainer.tsx         |     2 +-
 .../forms/ConfigureTwoFactorForm.tsx          |     8 +-
 .../dashboard/forms/CreateApiKeyForm.tsx      |    10 +-
 .../dashboard/forms/DisableTOTPDialog.tsx     |    12 +-
 .../dashboard/forms/RecoveryTokensDialog.tsx  |     8 +-
 .../dashboard/forms/SetupTOTPDialog.tsx       |    19 +-
 .../forms/UpdateEmailAddressForm.tsx          |    10 +-
 .../dashboard/forms/UpdatePasswordForm.tsx    |    16 +-
 .../dashboard/search/SearchContainer.tsx      |     2 +-
 .../dashboard/search/SearchModal.tsx          |     6 +-
 .../dashboard/ssh/AccountSSHContainer.tsx     |    13 +-
 .../dashboard/ssh/CreateSSHKeyForm.tsx        |     8 +-
 .../dashboard/ssh/DeleteSSHKeyButton.tsx      |     6 +-
 .../components/elements/ConfirmationModal.tsx |     2 +-
 .../components/elements/CopyOnClick.tsx       |     4 +-
 .../components/elements/ErrorBoundary.tsx     |     2 +-
 .../components/elements/PageContentBlock.tsx  |    11 +-
 .../components/elements/PermissionRoute.tsx   |     2 +-
 .../components/elements/ScreenBlock.tsx       |     4 +-
 .../elements/activity/ActivityLogEntry.tsx    |    11 +-
 .../activity/ActivityLogMetaButton.tsx        |     6 +-
 .../elements/dialog/ConfirmationDialog.tsx    |     2 +-
 .../elements/table/PaginationFooter.tsx       |     8 +-
 .../server/ConflictStateRenderer.tsx          |    18 +-
 .../server/ServerActivityLogContainer.tsx     |     6 +-
 .../components/server/UptimeDuration.tsx      |     4 +-
 .../components/server/WebsocketHandler.tsx    |     4 +-
 .../server/backups/BackupContainer.tsx        |     8 +-
 .../server/backups/BackupContextMenu.tsx      |    29 +-
 .../components/server/backups/BackupRow.tsx   |     9 +-
 .../server/backups/CreateBackupButton.tsx     |    24 +-
 .../components/server/console/Console.tsx     |    10 +-
 .../server/console/PowerButtons.tsx           |    12 +-
 .../server/console/ServerConsoleContainer.tsx |     8 +-
 .../server/console/ServerDetailsBlock.tsx     |    16 +-
 .../components/server/console/StatGraphs.tsx  |    16 +-
 .../server/databases/CreateDatabaseButton.tsx |    26 +-
 .../server/databases/DatabaseRow.tsx          |    38 +-
 .../server/databases/DatabasesContainer.tsx   |     9 +-
 .../server/databases/RotatePasswordButton.tsx |     2 +-
 .../server/features/GSLTokenModalFeature.tsx  |    12 +-
 .../features/JavaVersionModalFeature.tsx      |     6 +-
 .../server/features/PIDLimitModalFeature.tsx  |    22 +-
 .../server/features/SteamDiskSpaceFeature.tsx |    20 +-
 .../server/features/eula/EulaModalFeature.tsx |     8 +-
 .../server/files/ChmodFileModal.tsx           |     4 +-
 .../server/files/FileDropdownMenu.tsx         |    24 +-
 .../server/files/FileEditContainer.tsx        |    12 +-
 .../server/files/FileManagerContainer.tsx     |     7 +-
 .../server/files/FileManagerStatus.tsx        |    10 +-
 .../components/server/files/FileNameModal.tsx |     6 +-
 .../components/server/files/FileObjectRow.tsx |     5 +-
 .../server/files/MassActionsBar.tsx           |    22 +-
 .../server/files/NewDirectoryButton.tsx       |    12 +-
 .../server/files/RenameFileModal.tsx          |     8 +-
 .../components/server/files/UploadButton.tsx  |     6 +-
 .../server/network/AllocationRow.tsx          |     6 +-
 .../server/network/DeleteAllocationButton.tsx |     6 +-
 .../server/network/NetworkContainer.tsx       |     7 +-
 .../server/schedules/DeleteScheduleButton.tsx |     8 +-
 .../server/schedules/EditScheduleModal.tsx    |    32 +-
 .../server/schedules/NewTaskButton.tsx        |     2 +-
 .../server/schedules/RunScheduleButton.tsx    |     2 +-
 .../schedules/ScheduleCheatsheetCards.tsx     |    22 +-
 .../server/schedules/ScheduleContainer.tsx    |     6 +-
 .../server/schedules/ScheduleCronRow.tsx      |     8 +-
 .../schedules/ScheduleEditContainer.tsx       |    25 +-
 .../server/schedules/ScheduleRow.tsx          |     7 +-
 .../server/schedules/ScheduleTaskRow.tsx      |    24 +-
 .../server/schedules/TaskDetailsModal.tsx     |    48 +-
 .../server/settings/ReinstallServerBox.tsx    |    20 +-
 .../server/settings/RenameServerBox.tsx       |     6 +-
 .../server/settings/SettingsContainer.tsx     |    14 +-
 .../server/startup/StartupContainer.tsx       |    16 +-
 .../components/server/startup/VariableBox.tsx |     2 +-
 .../server/users/AddSubuserButton.tsx         |     2 +-
 .../server/users/EditSubuserModal.tsx         |    22 +-
 .../server/users/RemoveSubuserButton.tsx      |    10 +-
 .../components/server/users/UserRow.tsx       |     4 +-
 .../server/users/UsersContainer.tsx           |     4 +-
 .../scripts/hoc/RequireServerPermission.tsx   |     2 +-
 resources/scripts/routers/routes.ts           |    26 +-
 resources/scripts/state/flashes.ts            |     4 +-
 resources/views/admin/api/index.blade.php     |    31 +-
 resources/views/admin/api/new.blade.php       |    22 +-
 .../views/admin/databases/index.blade.php     |    42 +-
 .../views/admin/databases/view.blade.php      |    46 +-
 resources/views/admin/eggs/new.blade.php      |    64 +-
 resources/views/admin/eggs/scripts.blade.php  |    36 +-
 .../views/admin/eggs/variables.blade.php      |    60 +-
 resources/views/admin/eggs/view.blade.php     |    78 +-
 resources/views/admin/index.blade.php         |    16 +-
 .../views/admin/locations/index.blade.php     |    34 +-
 .../views/admin/locations/view.blade.php      |    14 +-
 resources/views/admin/mounts/index.blade.php  |    52 +-
 resources/views/admin/mounts/view.blade.php   |    54 +-
 resources/views/admin/nests/index.blade.php   |    28 +-
 resources/views/admin/nests/new.blade.php     |    14 +-
 resources/views/admin/nests/view.blade.php    |    24 +-
 resources/views/admin/nodes/index.blade.php   |    24 +-
 resources/views/admin/nodes/new.blade.php     |    68 +-
 .../admin/nodes/view/allocation.blade.php     |    70 +-
 .../admin/nodes/view/configuration.blade.php  |    35 +-
 .../views/admin/nodes/view/index.blade.php    |    40 +-
 .../views/admin/nodes/view/servers.blade.php  |    22 +-
 .../views/admin/nodes/view/settings.blade.php |    96 +-
 resources/views/admin/servers/index.blade.php |    24 +-
 resources/views/admin/servers/new.blade.php   |   106 +-
 .../servers/partials/navigation.blade.php     |    16 +-
 .../views/admin/servers/view/build.blade.php  |    76 +-
 .../admin/servers/view/database.blade.php     |    59 +-
 .../views/admin/servers/view/delete.blade.php |    34 +-
 .../admin/servers/view/details.blade.php      |    22 +-
 .../views/admin/servers/view/index.blade.php  |    56 +-
 .../views/admin/servers/view/manage.blade.php |    70 +-
 .../views/admin/servers/view/mounts.blade.php |    18 +-
 .../admin/servers/view/startup.blade.php      |    46 +-
 .../views/admin/settings/advanced.blade.php   |    44 +-
 .../views/admin/settings/index.blade.php      |    30 +-
 resources/views/admin/settings/mail.blade.php |    59 +-
 resources/views/admin/users/index.blade.php   |    24 +-
 resources/views/admin/users/new.blade.php     |    38 +-
 resources/views/admin/users/view.blade.php    |    38 +-
 resources/views/layouts/admin.blade.php       |    36 +-
 .../partials/admin/settings/nav.blade.php     |     6 +-
 .../partials/admin/settings/notice.blade.php  |     2 +-
 .../notifications/email-plain.blade.php       |     4 +-
 .../vendor/notifications/email.blade.php      |    12 +-
 203 files changed, 1953 insertions(+), 12973 deletions(-)
 delete mode 100644 patches/v1.11.3.patch

diff --git a/app/Exceptions/Handler.php b/app/Exceptions/Handler.php
index bcc636b..46b7cac 100644
--- a/app/Exceptions/Handler.php
+++ b/app/Exceptions/Handler.php
@@ -200,14 +200,14 @@ class Handler extends ExceptionHandler
                 : strval($match ?? '500'),
             'detail' => $e instanceof HttpExceptionInterface || !is_null($match)
                 ? $e->getMessage()
-                : 'An unexpected error was encountered while processing this request, please try again.',
+                : 'Es trat ein unbekannter Fehler beim laufenen Prozess auf. Bitte versuche es erneut.',
         ];
 
         if ($e instanceof ModelNotFoundException || $e->getPrevious() instanceof ModelNotFoundException) {
             // Show a nicer error message compared to the standard "No query results for model"
             // response that is normally returned. If we are in debug mode this will get overwritten
             // with a more specific error message to help narrow down things.
-            $error['detail'] = 'The requested resource could not be found on the server.';
+            $error['detail'] = 'Die gesuchten Daten konnten nicht gefunden werden';
         }
 
         if (config('app.debug')) {
diff --git a/app/Exceptions/Http/Connection/DaemonConnectionException.php b/app/Exceptions/Http/Connection/DaemonConnectionException.php
index 8c3d474..54abc61 100644
--- a/app/Exceptions/Http/Connection/DaemonConnectionException.php
+++ b/app/Exceptions/Http/Connection/DaemonConnectionException.php
@@ -46,8 +46,10 @@ class DaemonConnectionException extends DisplayException
 
         if (is_null($response)) {
             $message = 'Could not establish a connection to the machine running this server. Please try again.';
+            $message = 'Es konnte keine Verbindung zum Server hergestellt werden. Bitte versuche es nochmal.';
         } else {
             $message = sprintf('There was an error while communicating with the machine running this server. This error has been logged, please try again. (code: %s) (request_id: %s)', $response->getStatusCode(), $this->requestId ?? '<nil>');
+            $message = sprintf('Es ist ein Fehler bei der Verbindung zum Server aufgetreten. (Code: %s) (request_id: %s)', $response->getStatusCode(), $this->requestId ?? '<nil>');
         }
 
         // Attempt to pull the actual error message off the response and return that if it is not
@@ -55,6 +57,7 @@ class DaemonConnectionException extends DisplayException
         if ($this->statusCode < 500 && !is_null($response)) {
             $body = json_decode($response->getBody()->__toString(), true);
             $message = sprintf('An error occurred on the remote host: %s. (request id: %s)', $body['error'] ?? $message, $this->requestId ?? '<nil>');
+            $message = sprintf('Es trat ein Fehler beim Remote Host auf: %s. (request id: %s)', $body['error'] ?? $message, $this->requestId ?? '<nil>');
         }
 
         $level = $this->statusCode >= 500 && $this->statusCode !== 504
@@ -88,3 +91,4 @@ class DaemonConnectionException extends DisplayException
         return $this->requestId;
     }
 }
+
diff --git a/app/Exceptions/Http/Server/FileSizeTooLargeException.php b/app/Exceptions/Http/Server/FileSizeTooLargeException.php
index 59a53d3..e516e93 100644
--- a/app/Exceptions/Http/Server/FileSizeTooLargeException.php
+++ b/app/Exceptions/Http/Server/FileSizeTooLargeException.php
@@ -11,6 +11,6 @@ class FileSizeTooLargeException extends DisplayException
      */
     public function __construct()
     {
-        parent::__construct('The file you are attempting to open is too large to view in the file editor.');
+        parent::__construct('Die Datei ist zu groß, um sie zu öffnen.');
     }
 }
diff --git a/app/Exceptions/Http/Server/ServerStateConflictException.php b/app/Exceptions/Http/Server/ServerStateConflictException.php
index ea6a60a..63803ed 100644
--- a/app/Exceptions/Http/Server/ServerStateConflictException.php
+++ b/app/Exceptions/Http/Server/ServerStateConflictException.php
@@ -14,17 +14,17 @@ class ServerStateConflictException extends ConflictHttpException
      */
     public function __construct(Server $server, Throwable $previous = null)
     {
-        $message = 'This server is currently in an unsupported state, please try again later.';
+        $message = 'Der Server ist aktuell in einem unbekannten Status, bitte versuche es erneut.';
         if ($server->isSuspended()) {
-            $message = 'This server is currently suspended and the functionality requested is unavailable.';
+            $message = 'Der Server ist aktuell gesperrt und steht nicht zur Verfügung.';
         } elseif ($server->node->isUnderMaintenance()) {
-            $message = 'The node of this server is currently under maintenance and the functionality requested is unavailable.';
+            $message = 'Der Node des Servers ist derzeit in Wartungsarbeiten. In der Zeit ist dein Server nicht erreichbar.';
         } elseif (!$server->isInstalled()) {
-            $message = 'This server has not yet completed its installation process, please try again later.';
+            $message = 'Der Server hat noch einen aktiven Installationsprozess.';
         } elseif ($server->status === Server::STATUS_RESTORING_BACKUP) {
-            $message = 'This server is currently restoring from a backup, please try again later.';
+            $message = 'Der Server wird aktuell wiederhergestellt.';
         } elseif (!is_null($server->transfer)) {
-            $message = 'This server is currently being transferred to a new machine, please try again later.';
+            $message = 'Der Server wird gerade zu einem anderen Node übertragen. Das kann etwas dauern.';
         }
 
         parent::__construct($message, $previous);
diff --git a/app/Exceptions/Http/TwoFactorAuthRequiredException.php b/app/Exceptions/Http/TwoFactorAuthRequiredException.php
index cecb646..f032d47 100644
--- a/app/Exceptions/Http/TwoFactorAuthRequiredException.php
+++ b/app/Exceptions/Http/TwoFactorAuthRequiredException.php
@@ -14,6 +14,6 @@ class TwoFactorAuthRequiredException extends HttpException implements HttpExcept
      */
     public function __construct(Throwable $previous = null)
     {
-        parent::__construct(Response::HTTP_BAD_REQUEST, 'Two-factor authentication is required on this account in order to access this endpoint.', $previous);
+        parent::__construct(Response::HTTP_BAD_REQUEST, 'Es wird hier eine 2-Fakor Authentifizierung benötigt.', $previous);
     }
 }
diff --git a/app/Exceptions/Model/DataValidationException.php b/app/Exceptions/Model/DataValidationException.php
index 6ceb6e9..63b4cac 100644
--- a/app/Exceptions/Model/DataValidationException.php
+++ b/app/Exceptions/Model/DataValidationException.php
@@ -17,7 +17,7 @@ class DataValidationException extends PterodactylException implements HttpExcept
     public function __construct(protected Validator $validator, protected Model $model)
     {
         $message = sprintf(
-            'Could not save %s[%s]: failed to validate data: %s',
+            'Konnte %s[%s] nicht speichern: fehlschlag beim validieren der Daten: %s',
             get_class($model),
             $model->getKey(),
             $validator->errors()->toJson()
diff --git a/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php b/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php
index a593347..eb80e25 100644
--- a/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php
+++ b/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php
@@ -12,7 +12,7 @@ class AutoAllocationNotEnabledException extends DisplayException
     public function __construct()
     {
         parent::__construct(
-            'Server auto-allocation is not enabled for this instance.'
+            'Auf diesem Server ist die automatische Portfreischaltung nicht erlaubt.'
         );
     }
 }
diff --git a/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php b/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php
index 8e3c9b0..2283ca7 100644
--- a/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php
+++ b/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php
@@ -12,7 +12,7 @@ class NoAutoAllocationSpaceAvailableException extends DisplayException
     public function __construct()
     {
         parent::__construct(
-            'Cannot assign additional allocation: no more space available on node.'
+            'Dies ist zurzeit leider nicht möglich: Node ist überlastet und muss erweitert werden.'
         );
     }
 }
diff --git a/app/Exceptions/Service/Backup/BackupLockedException.php b/app/Exceptions/Service/Backup/BackupLockedException.php
index 3c9dbcf..3ec969f 100644
--- a/app/Exceptions/Service/Backup/BackupLockedException.php
+++ b/app/Exceptions/Service/Backup/BackupLockedException.php
@@ -11,6 +11,6 @@ class BackupLockedException extends DisplayException
      */
     public function __construct()
     {
-        parent::__construct('Cannot delete a backup that is marked as locked.');
+        parent::__construct('Backup kann nicht gelöscht werden, es wird derzeit geschützt.');
     }
 }
diff --git a/app/Exceptions/Service/Backup/TooManyBackupsException.php b/app/Exceptions/Service/Backup/TooManyBackupsException.php
index 8ccbaf7..0dd2eb3 100644
--- a/app/Exceptions/Service/Backup/TooManyBackupsException.php
+++ b/app/Exceptions/Service/Backup/TooManyBackupsException.php
@@ -12,7 +12,7 @@ class TooManyBackupsException extends DisplayException
     public function __construct(int $backupLimit)
     {
         parent::__construct(
-            sprintf('Cannot create a new backup, this server has reached its limit of %d backups.', $backupLimit)
+            sprintf('Du darfst maximal %d Backups besitzen.', $backupLimit)
         );
     }
 }
diff --git a/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php b/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php
index 809cb4f..f4a1325 100644
--- a/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php
+++ b/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php
@@ -8,6 +8,6 @@ class DatabaseClientFeatureNotEnabledException extends PterodactylException
 {
     public function __construct()
     {
-        parent::__construct('Client database creation is not enabled in this Panel.');
+        parent::__construct('Die Erstellung von Datenbanken ist deaktiveirt.');
     }
 }
diff --git a/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php b/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php
index 72f8dca..afbaac2 100644
--- a/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php
+++ b/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php
@@ -11,6 +11,6 @@ class NoSuitableDatabaseHostException extends DisplayException
      */
     public function __construct()
     {
-        parent::__construct('No database host was found that meets the requirements for this server.');
+        parent::__construct('Dieses System stellt aktuell keinen Host für Datenbanken zur Verfügung. Dies kann vom Betreiber eingerichtet werden.');
     }
 }
diff --git a/app/Exceptions/Service/Database/TooManyDatabasesException.php b/app/Exceptions/Service/Database/TooManyDatabasesException.php
index fe0e305..5090e13 100644
--- a/app/Exceptions/Service/Database/TooManyDatabasesException.php
+++ b/app/Exceptions/Service/Database/TooManyDatabasesException.php
@@ -8,6 +8,6 @@ class TooManyDatabasesException extends DisplayException
 {
     public function __construct()
     {
-        parent::__construct('Operation aborted: creating a new database would put this server over the defined limit.');
+        parent::__construct('Nicht möglich: Du hast bereits das Limit an Datenbanken für diesen Server erreicht.');
     }
 }
diff --git a/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php b/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php
index b7b819b..08cae08 100644
--- a/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php
+++ b/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php
@@ -11,6 +11,6 @@ class TwoFactorAuthenticationTokenInvalid extends DisplayException
      */
     public function __construct()
     {
-        parent::__construct('The provided two-factor authentication token was not valid.');
+        parent::__construct('Der 6-stellige Code ist falsch. Bitte erneut versuchen.');
     }
 }
diff --git a/app/Http/Controllers/Admin/ApiController.php b/app/Http/Controllers/Admin/ApiController.php
index 02ad6e5..e30660b 100644
--- a/app/Http/Controllers/Admin/ApiController.php
+++ b/app/Http/Controllers/Admin/ApiController.php
@@ -70,7 +70,7 @@ class ApiController extends Controller
             'user_id' => $request->user()->id,
         ], $request->getKeyPermissions());
 
-        $this->alert->success('A new application API key has been generated for your account.')->flash();
+        $this->alert->success('Es wurde ein neuer API-Schlüssel aktiviert und steht jetzt zur Verfügung.')->flash();
 
         return redirect()->route('admin.api.index');
     }
diff --git a/app/Http/Controllers/Admin/DatabaseController.php b/app/Http/Controllers/Admin/DatabaseController.php
index e0dc0dc..889ccf8 100644
--- a/app/Http/Controllers/Admin/DatabaseController.php
+++ b/app/Http/Controllers/Admin/DatabaseController.php
@@ -72,7 +72,7 @@ class DatabaseController extends Controller
         } catch (Exception $exception) {
             if ($exception instanceof PDOException || $exception->getPrevious() instanceof PDOException) {
                 $this->alert->danger(
-                    sprintf('There was an error while trying to connect to the host or while executing a query: "%s"', $exception->getMessage())
+                    sprintf('Es ist ein Fehler beim Verbindungsaufbau des Datenbank Hosts aufgetreten: "%s"', $exception->getMessage())
                 )->flash();
 
                 return redirect()->route('admin.databases')->withInput($request->validated());
@@ -81,7 +81,7 @@ class DatabaseController extends Controller
             }
         }
 
-        $this->alert->success('Successfully created a new database host on the system.')->flash();
+        $this->alert->success('Das Datenbank Host-System wurde erfolgreich erstellt.')->flash();
 
         return redirect()->route('admin.databases.view', $host->id);
     }
@@ -97,13 +97,13 @@ class DatabaseController extends Controller
 
         try {
             $this->updateService->handle($host->id, $request->normalize());
-            $this->alert->success('Database host was updated successfully.')->flash();
+            $this->alert->success('Datenbank-Host wurde aktualisiert.')->flash();
         } catch (Exception $exception) {
             // Catch any SQL related exceptions and display them back to the user, otherwise just
             // throw the exception like normal and move on with it.
             if ($exception instanceof PDOException || $exception->getPrevious() instanceof PDOException) {
                 $this->alert->danger(
-                    sprintf('There was an error while trying to connect to the host or while executing a query: "%s"', $exception->getMessage())
+                    sprintf('Es ist ein Fehler beim Verbindungsaufbau des Datenbank Hosts aufgetreten: "%s"', $exception->getMessage())
                 )->flash();
 
                 return $redirect->withInput($request->normalize());
@@ -123,7 +123,7 @@ class DatabaseController extends Controller
     public function delete(int $host): RedirectResponse
     {
         $this->deletionService->handle($host);
-        $this->alert->success('The requested database host has been deleted from the system.')->flash();
+        $this->alert->success('Das Datenbank Host-System wurde erfolgreich entfernt.')->flash();
 
         return redirect()->route('admin.databases');
     }
diff --git a/app/Http/Controllers/Admin/LocationController.php b/app/Http/Controllers/Admin/LocationController.php
index ea01cba..4f35cc8 100644
--- a/app/Http/Controllers/Admin/LocationController.php
+++ b/app/Http/Controllers/Admin/LocationController.php
@@ -60,7 +60,7 @@ class LocationController extends Controller
     public function create(LocationFormRequest $request): RedirectResponse
     {
         $location = $this->creationService->handle($request->normalize());
-        $this->alert->success('Location was created successfully.')->flash();
+        $this->alert->success('Standort wurde erfolgreich erstellt.')->flash();
 
         return redirect()->route('admin.locations.view', $location->id);
     }
@@ -77,7 +77,7 @@ class LocationController extends Controller
         }
 
         $this->updateService->handle($location->id, $request->normalize());
-        $this->alert->success('Location was updated successfully.')->flash();
+        $this->alert->success('Der Standort wurde erfolgreich aktualisiert.')->flash();
 
         return redirect()->route('admin.locations.view', $location->id);
     }
diff --git a/app/Http/Controllers/Admin/MountController.php b/app/Http/Controllers/Admin/MountController.php
index 097ad66..70c598c 100644
--- a/app/Http/Controllers/Admin/MountController.php
+++ b/app/Http/Controllers/Admin/MountController.php
@@ -72,7 +72,7 @@ class MountController extends Controller
         $model->saveOrFail();
         $mount = $model->fresh();
 
-        $this->alert->success('Mount was created successfully.')->flash();
+        $this->alert->success('Mount wurde erfolgreich erstellt.')->flash();
 
         return redirect()->route('admin.mounts.view', $mount->id);
     }
@@ -90,7 +90,7 @@ class MountController extends Controller
 
         $mount->forceFill($request->validated())->save();
 
-        $this->alert->success('Mount was updated successfully.')->flash();
+        $this->alert->success('Mount wurde erfolgreich aktualisiert.')->flash();
 
         return redirect()->route('admin.mounts.view', $mount->id);
     }
@@ -121,7 +121,7 @@ class MountController extends Controller
             $mount->eggs()->attach($eggs);
         }
 
-        $this->alert->success('Mount was updated successfully.')->flash();
+        $this->alert->success('Mount wurde erfolgreich aktualisiert.')->flash();
 
         return redirect()->route('admin.mounts.view', $mount->id);
     }
@@ -138,7 +138,7 @@ class MountController extends Controller
             $mount->nodes()->attach($nodes);
         }
 
-        $this->alert->success('Mount was updated successfully.')->flash();
+        $this->alert->success('Mount wurde erfolgreich aktualisiert.')->flash();
 
         return redirect()->route('admin.mounts.view', $mount->id);
     }
diff --git a/app/Http/Controllers/Admin/NodeAutoDeployController.php b/app/Http/Controllers/Admin/NodeAutoDeployController.php
index ac0684a..06f5a76 100644
--- a/app/Http/Controllers/Admin/NodeAutoDeployController.php
+++ b/app/Http/Controllers/Admin/NodeAutoDeployController.php
@@ -49,7 +49,7 @@ class NodeAutoDeployController extends Controller
         if (!$key) {
             $key = $this->keyCreationService->setKeyType(ApiKey::TYPE_APPLICATION)->handle([
                 'user_id' => $request->user()->id,
-                'memo' => 'Automatically generated node deployment key.',
+                'memo' => 'Es wurde ein neuer Entwickler-Schlüssel (Dev-Key) generiert.',
                 'allowed_ips' => [],
             ], ['r_nodes' => 1]);
         }
diff --git a/app/Http/Controllers/Admin/Servers/ServerViewController.php b/app/Http/Controllers/Admin/Servers/ServerViewController.php
index 7cf64a2..f49ac67 100644
--- a/app/Http/Controllers/Admin/Servers/ServerViewController.php
+++ b/app/Http/Controllers/Admin/Servers/ServerViewController.php
@@ -124,7 +124,7 @@ class ServerViewController extends Controller
     public function manage(Request $request, Server $server): View
     {
         if ($server->status === Server::STATUS_INSTALL_FAILED) {
-            throw new DisplayException('This server is in a failed install state and cannot be recovered. Please delete and re-create the server.');
+            throw new DisplayException('Dieser Server kann nicht aufgesetzt werden, da ein unterwarteter Fehler aufgetreten ist. Der gesamte Server ist betroffen, bitte setze ihn neu auf.');
         }
 
         // Check if the panel doesn't have at least 2 nodes configured.
diff --git a/app/Http/Controllers/Admin/ServersController.php b/app/Http/Controllers/Admin/ServersController.php
index 3d2c55a..639cbd9 100644
--- a/app/Http/Controllers/Admin/ServersController.php
+++ b/app/Http/Controllers/Admin/ServersController.php
@@ -254,7 +254,7 @@ class ServersController extends Controller
 
         $mountServer->saveOrFail();
 
-        $this->alert->success('Mount was added successfully.')->flash();
+        $this->alert->success('Mount wurde erfolgreich hinzugefügt.')->flash();
 
         return redirect()->route('admin.servers.view.mounts', $server->id);
     }
@@ -266,7 +266,7 @@ class ServersController extends Controller
     {
         MountServer::where('mount_id', $mount->id)->where('server_id', $server->id)->delete();
 
-        $this->alert->success('Mount was removed successfully.')->flash();
+        $this->alert->success('Mount wurde erfolgreich entfernt.')->flash();
 
         return redirect()->route('admin.servers.view.mounts', $server->id);
     }
diff --git a/app/Http/Controllers/Admin/Settings/AdvancedController.php b/app/Http/Controllers/Admin/Settings/AdvancedController.php
index bf68832..085d589 100644
--- a/app/Http/Controllers/Admin/Settings/AdvancedController.php
+++ b/app/Http/Controllers/Admin/Settings/AdvancedController.php
@@ -55,7 +55,7 @@ class AdvancedController extends Controller
         }
 
         $this->kernel->call('queue:restart');
-        $this->alert->success('Advanced settings have been updated successfully and the queue worker was restarted to apply these changes.')->flash();
+        $this->alert->success('Die Einstellungen wurden gespeichert und das reCAPTCHA Service wurde (bei Bedarf) neu gestartet')->flash();
 
         return redirect()->route('admin.settings.advanced');
     }
diff --git a/app/Http/Controllers/Admin/Settings/IndexController.php b/app/Http/Controllers/Admin/Settings/IndexController.php
index eabede9..49a5e47 100644
--- a/app/Http/Controllers/Admin/Settings/IndexController.php
+++ b/app/Http/Controllers/Admin/Settings/IndexController.php
@@ -53,7 +53,7 @@ class IndexController extends Controller
         }
 
         $this->kernel->call('queue:restart');
-        $this->alert->success('Panel settings have been updated successfully and the queue worker was restarted to apply these changes.')->flash();
+        $this->alert->success('Die Änderungen wurden gespeichert und werden in Kürze angewendet.')->flash();
 
         return redirect()->route('admin.settings');
     }
diff --git a/app/Http/Controllers/Admin/Settings/MailController.php b/app/Http/Controllers/Admin/Settings/MailController.php
index 050990d..037d718 100644
--- a/app/Http/Controllers/Admin/Settings/MailController.php
+++ b/app/Http/Controllers/Admin/Settings/MailController.php
@@ -53,7 +53,7 @@ class MailController extends Controller
     public function update(MailSettingsFormRequest $request): Response
     {
         if ($this->config->get('mail.default') !== 'smtp') {
-            throw new DisplayException('This feature is only available if SMTP is the selected email driver for the Panel.');
+            throw new DisplayException('Das Mail-System ist noch nicht eingereichtet.');
         }
 
         $values = $request->normalize();
diff --git a/app/Http/Controllers/Api/Client/ApiKeyController.php b/app/Http/Controllers/Api/Client/ApiKeyController.php
index ac00a4d..f2349a2 100644
--- a/app/Http/Controllers/Api/Client/ApiKeyController.php
+++ b/app/Http/Controllers/Api/Client/ApiKeyController.php
@@ -30,7 +30,7 @@ class ApiKeyController extends ClientApiController
     public function store(StoreApiKeyRequest $request): array
     {
         if ($request->user()->apiKeys->count() >= 25) {
-            throw new DisplayException('You have reached the account limit for number of API keys.');
+            throw new DisplayException('Du hast das Limit an API-Schlüssel erreicht.');
         }
 
         $token = $request->user()->createToken(
diff --git a/app/Http/Controllers/Api/Client/Servers/BackupController.php b/app/Http/Controllers/Api/Client/Servers/BackupController.php
index 7a35341..24ceb38 100644
--- a/app/Http/Controllers/Api/Client/Servers/BackupController.php
+++ b/app/Http/Controllers/Api/Client/Servers/BackupController.php
@@ -164,7 +164,7 @@ class BackupController extends ClientApiController
         }
 
         if ($backup->disk !== Backup::ADAPTER_AWS_S3 && $backup->disk !== Backup::ADAPTER_WINGS) {
-            throw new BadRequestHttpException('The backup requested references an unknown disk driver type and cannot be downloaded.');
+            throw new BadRequestHttpException('Die Quelle des Backups ist derzeit unbekannt und kann nicht heruntergeladen werden.');
         }
 
         $url = $this->downloadLinkService->handle($backup, $request->user());
@@ -197,11 +197,11 @@ class BackupController extends ClientApiController
         // Cannot restore a backup unless a server is fully installed and not currently
         // processing a different backup restoration request.
         if (!is_null($server->status)) {
-            throw new BadRequestHttpException('This server is not currently in a state that allows for a backup to be restored.');
+            throw new BadRequestHttpException('Aktuell ist der Server in einem Status, wo kein Backup angewendet werden kann.');
         }
 
         if (!$backup->is_successful && is_null($backup->completed_at)) {
-            throw new BadRequestHttpException('This backup cannot be restored at this time: not completed or failed.');
+            throw new BadRequestHttpException('Dieses Backup kann zurzeit nicht verwendet werden: Unvollständig oder Fehlerhaft.');
         }
 
         $log = Activity::event('server:backup.restore')
diff --git a/app/Http/Controllers/Api/Client/Servers/CommandController.php b/app/Http/Controllers/Api/Client/Servers/CommandController.php
index 6cc50de..44eac18 100644
--- a/app/Http/Controllers/Api/Client/Servers/CommandController.php
+++ b/app/Http/Controllers/Api/Client/Servers/CommandController.php
@@ -40,7 +40,7 @@ class CommandController extends ClientApiController
                     $previous->getResponse() instanceof ResponseInterface
                     && $previous->getResponse()->getStatusCode() === Response::HTTP_BAD_GATEWAY
                 ) {
-                    throw new HttpException(Response::HTTP_BAD_GATEWAY, 'Server must be online in order to send commands.', $exception);
+                    throw new HttpException(Response::HTTP_BAD_GATEWAY, 'Der Server muss online sein, damit du Befehle senden kannst.', $exception);
                 }
             }
 
diff --git a/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php b/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php
index 4e3c5f9..e9ae9d0 100644
--- a/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php
+++ b/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php
@@ -94,6 +94,7 @@ class NetworkAllocationController extends ClientApiController
     {
         if ($server->allocations()->count() >= $server->allocation_limit) {
             throw new DisplayException('Cannot assign additional allocations to this server: limit has been reached.');
+            throw new DisplayException('Der angeforderte Port konnte nicht dem Server zugewiesen werden, du hast das Limit bereits erreicht.');
         }
 
         $allocation = $this->assignableAllocationService->handle($server);
@@ -119,10 +120,12 @@ class NetworkAllocationController extends ClientApiController
         // allocation limit set.
         if (empty($server->allocation_limit)) {
             throw new DisplayException('You cannot delete allocations for this server: no allocation limit is set.');
+            throw new DisplayException('Du kannst Ports für diesen Server nicht löschen: Es ist kein Port-Limit festgelegt.');
         }
 
         if ($allocation->id === $server->allocation_id) {
             throw new DisplayException('You cannot delete the primary allocation for this server.');
+            throw new DisplayException('Du kannst den primären Port für diesen Server nicht löschen.');
         }
 
         Allocation::query()->where('id', $allocation->id)->update([
@@ -138,3 +141,4 @@ class NetworkAllocationController extends ClientApiController
         return new JsonResponse([], JsonResponse::HTTP_NO_CONTENT);
     }
 }
+
diff --git a/app/Http/Controllers/Api/Client/Servers/ScheduleController.php b/app/Http/Controllers/Api/Client/Servers/ScheduleController.php
index 50c04c7..3b23933 100644
--- a/app/Http/Controllers/Api/Client/Servers/ScheduleController.php
+++ b/app/Http/Controllers/Api/Client/Servers/ScheduleController.php
@@ -179,7 +179,7 @@ class ScheduleController extends ClientApiController
                 $request->input('day_of_week')
             );
         } catch (Exception $exception) {
-            throw new DisplayException('The cron data provided does not evaluate to a valid expression.');
+            throw new DisplayException('Die Angaben entsprechen nicht den Regeln von Cron.');
         }
     }
 }
diff --git a/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php b/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php
index e2e4389..21665d5 100644
--- a/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php
+++ b/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php
@@ -41,11 +41,11 @@ class ScheduleTaskController extends ClientApiController
     {
         $limit = config('pterodactyl.client_features.schedules.per_schedule_task_limit', 10);
         if ($schedule->tasks()->count() >= $limit) {
-            throw new ServiceLimitExceededException("Schedules may not have more than $limit tasks associated with them. Creating this task would put this schedule over the limit.");
+            throw new ServiceLimitExceededException("Zeitpläne können maximal $limit Aufgaben beinhalten.");
         }
 
         if ($server->backup_limit === 0 && $request->action === 'backup') {
-            throw new HttpForbiddenException("A backup task cannot be created when the server's backup limit is set to 0.");
+            throw new HttpForbiddenException("Die Aufgabe, Backups zu erstellen ist nicht möglich. Dieser Server darf momentan keine Backups erstellen.");
         }
 
         /** @var \Pterodactyl\Models\Task|null $lastTask */
@@ -105,7 +105,7 @@ class ScheduleTaskController extends ClientApiController
         }
 
         if ($server->backup_limit === 0 && $request->action === 'backup') {
-            throw new HttpForbiddenException("A backup task cannot be created when the server's backup limit is set to 0.");
+            throw new HttpForbiddenException("Eine Aufgabe mit einem Backup-Befehl kann nicht ausgeführt werden. Dieser Server darf aktuell keine Backups erstellen.");
         }
 
         $this->connection->transaction(function () use ($request, $schedule, $task) {
@@ -160,7 +160,7 @@ class ScheduleTaskController extends ClientApiController
         }
 
         if (!$request->user()->can(Permission::ACTION_SCHEDULE_UPDATE, $server)) {
-            throw new HttpForbiddenException('You do not have permission to perform this action.');
+            throw new HttpForbiddenException('Du hast dafür ungenügende Rechte.');
         }
 
         $schedule->tasks()
diff --git a/app/Http/Controllers/Api/Client/Servers/SettingsController.php b/app/Http/Controllers/Api/Client/Servers/SettingsController.php
index e84c159..a8a3695 100644
--- a/app/Http/Controllers/Api/Client/Servers/SettingsController.php
+++ b/app/Http/Controllers/Api/Client/Servers/SettingsController.php
@@ -78,7 +78,7 @@ class SettingsController extends ClientApiController
     public function dockerImage(SetDockerImageRequest $request, Server $server): JsonResponse
     {
         if (!in_array($server->image, array_values($server->egg->docker_images))) {
-            throw new BadRequestHttpException('This server\'s Docker image has been manually set by an administrator and cannot be updated.');
+            throw new BadRequestHttpException('Dieses Docker Image wurde vom Betreiber hinzugefügt und kann nicht verändert werden.');
         }
 
         $original = $server->image;
diff --git a/app/Http/Controllers/Api/Client/Servers/StartupController.php b/app/Http/Controllers/Api/Client/Servers/StartupController.php
index 9548be2..f7a6148 100644
--- a/app/Http/Controllers/Api/Client/Servers/StartupController.php
+++ b/app/Http/Controllers/Api/Client/Servers/StartupController.php
@@ -57,9 +57,9 @@ class StartupController extends ClientApiController
         $original = $variable->server_value;
 
         if (is_null($variable) || !$variable->user_viewable) {
-            throw new BadRequestHttpException('The environment variable you are trying to edit does not exist.');
+            throw new BadRequestHttpException('Die Variable, die du nutzen möchtest existiert nicht.');
         } elseif (!$variable->user_editable) {
-            throw new BadRequestHttpException('The environment variable you are trying to edit is read-only.');
+            throw new BadRequestHttpException('Die Variable, die du bearbeiten möchtest, darf nicht verändert werden.');
         }
 
         // Revalidate the variable value using the egg variable specific validation rules for it.
diff --git a/app/Http/Controllers/Api/Client/Servers/WebsocketController.php b/app/Http/Controllers/Api/Client/Servers/WebsocketController.php
index 59b6f75..0e7d682 100644
--- a/app/Http/Controllers/Api/Client/Servers/WebsocketController.php
+++ b/app/Http/Controllers/Api/Client/Servers/WebsocketController.php
@@ -34,7 +34,7 @@ class WebsocketController extends ClientApiController
     {
         $user = $request->user();
         if ($user->cannot(Permission::ACTION_WEBSOCKET_CONNECT, $server)) {
-            throw new HttpForbiddenException('You do not have permission to connect to this server\'s websocket.');
+            throw new HttpForbiddenException('Du hast nicht die Berechtigung, um dich mit dem Websocket Server zu verbinden.');
         }
 
         $permissions = $this->permissionsService->handle($server, $user);
@@ -43,7 +43,7 @@ class WebsocketController extends ClientApiController
         if (!is_null($server->transfer)) {
             // Check if the user has permissions to receive transfer logs.
             if (!in_array('admin.websocket.transfer', $permissions)) {
-                throw new HttpForbiddenException('You do not have permission to view server transfer logs.');
+                throw new HttpForbiddenException('Du hast nicht die Berechtigung, um die Transfer-Logs zu sehen.');
             }
 
             // Redirect the websocket request to the new node if the server has been archived.
diff --git a/app/Http/Controllers/Api/Client/TwoFactorController.php b/app/Http/Controllers/Api/Client/TwoFactorController.php
index 8cfa09b..7d6715e 100644
--- a/app/Http/Controllers/Api/Client/TwoFactorController.php
+++ b/app/Http/Controllers/Api/Client/TwoFactorController.php
@@ -36,7 +36,7 @@ class TwoFactorController extends ClientApiController
     public function index(Request $request): JsonResponse
     {
         if ($request->user()->use_totp) {
-            throw new BadRequestHttpException('Two-factor authentication is already enabled on this account.');
+            throw new BadRequestHttpException('2-Fakor Authentifizierung ist bereits aktiv..');
         }
 
         return new JsonResponse([
@@ -59,7 +59,7 @@ class TwoFactorController extends ClientApiController
 
         $data = $validator->validate();
         if (!password_verify($data['password'], $request->user()->password)) {
-            throw new BadRequestHttpException('The password provided was not valid.');
+            throw new BadRequestHttpException('Das Passwort ist falsch.');
         }
 
         $tokens = $this->toggleTwoFactorService->handle($request->user(), $data['code'], true);
@@ -83,7 +83,7 @@ class TwoFactorController extends ClientApiController
     public function delete(Request $request): JsonResponse
     {
         if (!password_verify($request->input('password') ?? '', $request->user()->password)) {
-            throw new BadRequestHttpException('The password provided was not valid.');
+            throw new BadRequestHttpException('Das Passwort ist falsch.');
         }
 
         /** @var \Pterodactyl\Models\User $user */
diff --git a/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php b/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php
index 7d92e0b..549a7bb 100644
--- a/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php
+++ b/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php
@@ -35,7 +35,7 @@ class BackupRemoteUploadController extends Controller
         // Get the size query parameter.
         $size = (int) $request->query('size');
         if (empty($size)) {
-            throw new BadRequestHttpException('A non-empty "size" query parameter must be provided.');
+            throw new BadRequestHttpException('Es muss ein nicht leerer Abfrageparameter "Größe" angegeben werden.');
         }
 
         /** @var \Pterodactyl\Models\Backup $backup */
@@ -44,13 +44,13 @@ class BackupRemoteUploadController extends Controller
         // Prevent backups that have already been completed from trying to
         // be uploaded again.
         if (!is_null($backup->completed_at)) {
-            throw new ConflictHttpException('This backup is already in a completed state.');
+            throw new ConflictHttpException('Das Backup ist bereits fertig.');
         }
 
         // Ensure we are using the S3 adapter.
         $adapter = $this->backupManager->adapter();
         if (!$adapter instanceof S3Filesystem) {
-            throw new BadRequestHttpException('The configured backup adapter is not an S3 compatible adapter.');
+            throw new BadRequestHttpException('Der konfigurierte Backup-Adapter ist kein S3-kompatibler Adapter.');
         }
 
         // The path where backup will be uploaded to
diff --git a/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php b/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php
index f9c2a79..8ea4b3e 100644
--- a/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php
+++ b/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php
@@ -34,7 +34,7 @@ class BackupStatusController extends Controller
         $model = Backup::query()->where('uuid', $backup)->firstOrFail();
 
         if ($model->is_successful) {
-            throw new BadRequestHttpException('Cannot update the status of a backup that is already marked as completed.');
+            throw new BadRequestHttpException('Der Status des Backups konnte nicht geändert werden, es ist bereits als fertig markiert..');
         }
 
         $action = $request->boolean('successful') ? 'server:backup.complete' : 'server:backup.fail';
@@ -109,7 +109,7 @@ class BackupStatusController extends Controller
                 return;
             }
 
-            throw new DisplayException('Cannot complete backup request: no upload_id present on model.');
+            throw new DisplayException('Backup-Anfrage konnte nicht ausgeführt werden: upload_id am Modell vorhanden.');
         }
 
         $params = [
diff --git a/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php b/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php
index a360d1d..0d0ac6e 100644
--- a/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php
+++ b/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php
@@ -35,13 +35,13 @@ class SftpAuthenticationController extends Controller
     {
         $connection = $this->parseUsername($request->input('username'));
         if (empty($connection['server'])) {
-            throw new BadRequestHttpException('No valid server identifier was included in the request.');
+            throw new BadRequestHttpException('Keine gültige Serverkennung in der Anfrage enthalten.');
         }
 
         if ($this->hasTooManyLoginAttempts($request)) {
             $seconds = $this->limiter()->availableIn($this->throttleKey($request));
 
-            throw new TooManyRequestsHttpException($seconds, "Too many login attempts for this account, please try again in $seconds seconds.");
+            throw new TooManyRequestsHttpException($seconds, "Zu viele Login-Anfragen. Bitte warte $seconds Sekunden.");
         }
 
         $user = $this->getUser($request, $connection['username']);
@@ -132,7 +132,7 @@ class SftpAuthenticationController extends Controller
             $this->incrementLoginAttempts($request);
         }
 
-        throw new HttpForbiddenException('Authorization credentials were not correct, please try again.');
+        throw new HttpForbiddenException('Die Zugangsdaten sind falsch. Bitte schau nochmal rüber.');
     }
 
     /**
@@ -146,7 +146,7 @@ class SftpAuthenticationController extends Controller
             if (!in_array(Permission::ACTION_FILE_SFTP, $permissions)) {
                 Activity::event('server:sftp.denied')->actor($user)->subject($server)->log();
 
-                throw new HttpForbiddenException('You do not have permission to access SFTP for this server.');
+                throw new HttpForbiddenException('Du hast nicht die Berechtigung, um den SFTP-Zugang zu verwenden.');
             }
         }
 
diff --git a/app/Http/Controllers/Auth/LoginCheckpointController.php b/app/Http/Controllers/Auth/LoginCheckpointController.php
index af05c55..aff275d 100644
--- a/app/Http/Controllers/Auth/LoginCheckpointController.php
+++ b/app/Http/Controllers/Auth/LoginCheckpointController.php
@@ -16,7 +16,7 @@ use Illuminate\Contracts\Validation\Factory as ValidationFactory;
 
 class LoginCheckpointController extends AbstractLoginController
 {
-    private const TOKEN_EXPIRED_MESSAGE = 'The authentication token provided has expired, please refresh the page and try again.';
+    private const TOKEN_EXPIRED_MESSAGE = 'Der Login-Schlüssel ist abgelaufen, bitte melde dich erneut an.';
 
     /**
      * LoginCheckpointController constructor.
diff --git a/app/Models/Permission.php b/app/Models/Permission.php
index b2e0007..dd084b0 100644
--- a/app/Models/Permission.php
+++ b/app/Models/Permission.php
@@ -100,110 +100,110 @@ class Permission extends Model
      */
     protected static array $permissions = [
         'websocket' => [
-            'description' => 'Allows the user to connect to the server websocket, giving them access to view console output and realtime server stats.',
+            'description' => 'Ermöglicht dem Benutzer, sich mit dem Server-Websocket zu verbinden, wodurch er Zugriff auf die Konsolenausgabe und Serverstatistiken in Echtzeit erhält.',
             'keys' => [
-                'connect' => 'Allows a user to connect to the websocket instance for a server to stream the console.',
+                'connect' => 'Ermöglicht dem Benutzer, sich mit der Websocket-Instanz für einen Server zu verbinden, um die Konsole zu streamen.',
             ],
         ],
 
         'control' => [
-            'description' => 'Permissions that control a user\'s ability to control the power state of a server, or send commands.',
+            'description' => 'Diese Berechtigungen richten sich an die Konsole des Servers. Dazu gehört die Realtime-Konsole und das Starten oder Stoppen des Servers.',
             'keys' => [
-                'console' => 'Allows a user to send commands to the server instance via the console.',
-                'start' => 'Allows a user to start the server if it is stopped.',
-                'stop' => 'Allows a user to stop a server if it is running.',
-                'restart' => 'Allows a user to perform a server restart. This allows them to start the server if it is offline, but not put the server in a completely stopped state.',
+                'console' => 'Erlaubt den Zugang zur Konsole, wo Befehle an den Server gesendet werden können.',
+                'start' => 'Erlaubt den Server zu starten, falls der aus ist.',
+                'stop' => 'Erlaubt den Server zu stoppen, falls der gerade läuft.',
+                'restart' => 'Erlaubt den Server neu zu starten.',
             ],
         ],
 
         'user' => [
-            'description' => 'Permissions that allow a user to manage other subusers on a server. They will never be able to edit their own account, or assign permissions they do not have themselves.',
+            'description' => 'Diese Berechtigungen richten sich nach den Benutzern, die diesen Server verwalten. Der Benutzer wird nicht in der Lage sein, die eigenen Berechtigungen zu verändern.',
             'keys' => [
-                'create' => 'Allows a user to create new subusers for the server.',
-                'read' => 'Allows the user to view subusers and their permissions for the server.',
-                'update' => 'Allows a user to modify other subusers.',
-                'delete' => 'Allows a user to delete a subuser from the server.',
+                'create' => 'Erlaubt es, neue Benutzer in diesen Server zu integrieren',
+                'read' => 'Erlaubt es, die zugeteilten Berechtigungen anderer zu sehen.',
+                'update' => 'Erlaubt es die Berechtigungen von anderen Benutzern zu ändern.',
+                'delete' => 'Erlaubt es, die Benutzer vom Server komplett auszuschließen.',
             ],
         ],
 
         'file' => [
-            'description' => 'Permissions that control a user\'s ability to modify the filesystem for this server.',
+            'description' => 'Diese Berechtigungen richten sich nach dem Datei-Manager von diesem Server. ',
             'keys' => [
-                'create' => 'Allows a user to create additional files and folders via the Panel or direct upload.',
-                'read' => 'Allows a user to view the contents of a directory, but not view the contents of or download files.',
-                'read-content' => 'Allows a user to view the contents of a given file. This will also allow the user to download files.',
-                'update' => 'Allows a user to update the contents of an existing file or directory.',
-                'delete' => 'Allows a user to delete files or directories.',
-                'archive' => 'Allows a user to archive the contents of a directory as well as decompress existing archives on the system.',
-                'sftp' => 'Allows a user to connect to SFTP and manage server files using the other assigned file permissions.',
+                'create' => 'Erlaubt es, Dateien oder Ordner zu erstellen oder hochzuladen',
+                'read' => 'Erlaubt es, Dateien oder Ordner zu öffnen.',
+                'read-content' => 'Erlaubt es, den Inhalt von Dateien oder Ordnern zu lesen',
+                'update' => 'Erlaubt es, Inhalte in Dateien oder Ordnern zu verändern.',
+                'delete' => 'Erlaubt es, Inhalte oder Ordner zu löschen.',
+                'archive' => 'Erlaubt es, Inhalte zu archivieren oder zu entpacken.',
+                'sftp' => 'Erlaubt es, auf die Inhalte des Servers außerhalb des Panels zugzugreifen. Mithilfe von SFTP, dort werden dieselben Rechte wie hier zugeteilt.',
             ],
         ],
 
         'backup' => [
-            'description' => 'Permissions that control a user\'s ability to generate and manage server backups.',
+            'description' => 'Diese Berechtigungen richten sich nach der Verwaltung der Backups des Servers.',
             'keys' => [
-                'create' => 'Allows a user to create new backups for this server.',
-                'read' => 'Allows a user to view all backups that exist for this server.',
-                'delete' => 'Allows a user to remove backups from the system.',
-                'download' => 'Allows a user to download a backup for the server. Danger: this allows a user to access all files for the server in the backup.',
-                'restore' => 'Allows a user to restore a backup for the server. Danger: this allows the user to delete all of the server files in the process.',
+                'create' => 'Erlaubt es, ein Backup zu erstellen.',
+                'read' => 'Erlaubt es, die verfügbaren Backups zu sehen.',
+                'delete' => 'Erlaubt es, Backups zu löschen.',
+                'download' => 'Erlaubt es, Backups herunterzuladen. ACHTUNG: Das Backup enthält ALLE INHALTE des Servers!',
+                'restore' => 'Erlaubt es, den Server mit einem Backup wiederherzustellen. ACHTUNG: Dies kann den Server in einen früheren Zustand versetzen.',
             ],
         ],
 
         // Controls permissions for editing or viewing a server's allocations.
         'allocation' => [
-            'description' => 'Permissions that control a user\'s ability to modify the port allocations for this server.',
+            'description' => 'Diese Berechtigungen richten sich nach den Portfreigaben des Servers',
             'keys' => [
-                'read' => 'Allows a user to view all allocations currently assigned to this server. Users with any level of access to this server can always view the primary allocation.',
-                'create' => 'Allows a user to assign additional allocations to the server.',
-                'update' => 'Allows a user to change the primary server allocation and attach notes to each allocation.',
-                'delete' => 'Allows a user to delete an allocation from the server.',
+                'read' => 'Erlaubt es, alle zugewiesenen Ports für diesen Server einzusehen. Der primäre Port ist für alle Benutzer immer sichtbar.',
+                'create' => 'Erlaubt es, neue Ports für diesen Server zuzuweisen.',
+                'update' => 'Erlaubt es, den primären Port zu ändern und die Vermerke zu anderen Ports zu bearbeiten.',
+                'delete' => 'Erlaubt es, zugewiesene Ports vom Server zu entfernen.',
             ],
         ],
 
         // Controls permissions for editing or viewing a server's startup parameters.
         'startup' => [
-            'description' => 'Permissions that control a user\'s ability to view this server\'s startup parameters.',
+            'description' => 'Diese Berechtigungen richten sich nach den Variablen des Servers und allgemeines zum Startbefehl',
             'keys' => [
-                'read' => 'Allows a user to view the startup variables for a server.',
-                'update' => 'Allows a user to modify the startup variables for the server.',
-                'docker-image' => 'Allows a user to modify the Docker image used when running the server.',
+                'read' => 'Erlaubt es, den Startbefehl zu sehen.',
+                'update' => 'Erlaubt es, Variablen des Startbefehls zu ändern.',
+                'docker-image' => 'Erlaubt es, den Docker-Image des Servers zu ändern.',
             ],
         ],
 
         'database' => [
-            'description' => 'Permissions that control a user\'s access to the database management for this server.',
+            'description' => 'Diese Berechtigungen richten sich nach der Verwaltung der Datenbanken.',
             'keys' => [
-                'create' => 'Allows a user to create a new database for this server.',
-                'read' => 'Allows a user to view the database associated with this server.',
-                'update' => 'Allows a user to rotate the password on a database instance. If the user does not have the view_password permission they will not see the updated password.',
-                'delete' => 'Allows a user to remove a database instance from this server.',
-                'view_password' => 'Allows a user to view the password associated with a database instance for this server.',
+                'create' => 'Erlaubt es, neue Datenbanken für den Server zu erstellen.',
+                'read' => 'Erlaubt es, die Übersicht aller Datenbanken des Servers zu sehen.',
+                'update' => 'Erlaubt es, das Passwort der Datenbank zurückzusetzen. Solange die unterste Berechtigung nicht genehmigt wird, wird der Benutzer das neue Passwort nicht sehen',
+                'delete' => 'Erlaubt es, Datenbanken für den Server zu löschen.',
+                'view_password' => 'Erlaubt die Ansicht des aktuellen Passwortes für Datenbanken',
             ],
         ],
 
         'schedule' => [
-            'description' => 'Permissions that control a user\'s access to the schedule management for this server.',
+            'description' => 'Diese Berechtigungen richten sich nach den Zeitplänen des Servers.',
             'keys' => [
-                'create' => 'Allows a user to create new schedules for this server.', // task.create-schedule
-                'read' => 'Allows a user to view schedules and the tasks associated with them for this server.', // task.view-schedule, task.list-schedules
-                'update' => 'Allows a user to update schedules and schedule tasks for this server.', // task.edit-schedule, task.queue-schedule, task.toggle-schedule
-                'delete' => 'Allows a user to delete schedules for this server.', // task.delete-schedule
+                'create' => 'Erlaubt es, neue Aufgaben zu erstellen.', // task.create-schedule
+                'read' => 'Erlaubt es, die geplanten Aufgaben zu sehen', // task.view-schedule, task.list-schedules
+                'update' => 'Erlaubt es, die Zeitpläne zu bearbeiten.', // task.edit-schedule, task.queue-schedule, task.toggle-schedule
+                'delete' => 'Erlaubt es, Zeitpläne zu entfernen', // task.delete-schedule
             ],
         ],
 
         'settings' => [
-            'description' => 'Permissions that control a user\'s access to the settings for this server.',
+            'description' => 'Diese Berechtigungen richten sich nach den allgemeinen Einstellungen des Servers',
             'keys' => [
-                'rename' => 'Allows a user to rename this server and change the description of it.',
-                'reinstall' => 'Allows a user to trigger a reinstall of this server.',
+                'rename' => 'Erlaubt es, den Namen oder die Kurzbeschreibung des Servers zu ändern.',
+                'reinstall' => 'Erlaubt es, den Server neu aufzusetzen und die Software des Servers zu aktualisieren (Datenverlust möglich).',
             ],
         ],
 
         'activity' => [
-            'description' => 'Permissions that control a user\'s access to the server activity logs.',
+            'description' => 'Diese Berechtigung richtet sich nach allen Aktivitäten des Servers von allen Benutzern.',
             'keys' => [
-                'read' => 'Allows a user to view the activity logs for the server.',
+                'read' => 'Erlaubt es, alle Aktivitäten des Servers zu sehen',
             ],
         ],
     ];
diff --git a/app/Notifications/AccountCreated.php b/app/Notifications/AccountCreated.php
index fb13380..1037a59 100644
--- a/app/Notifications/AccountCreated.php
+++ b/app/Notifications/AccountCreated.php
@@ -33,13 +33,13 @@ class AccountCreated extends Notification implements ShouldQueue
     public function toMail(): MailMessage
     {
         $message = (new MailMessage())
-            ->greeting('Hello ' . $this->user->name . '!')
-            ->line('You are receiving this email because an account has been created for you on ' . config('app.name') . '.')
-            ->line('Username: ' . $this->user->username)
-            ->line('Email: ' . $this->user->email);
+            ->greeting('Willkommen, ' . $this->user->name . '!')
+            ->line('Es wurde gerade ein Account für dich auf' . config('app.name') . ' erstellt. Melde dich an, um Zugriff auf deine Server zu erhalten.')
+            ->line('Benutzername: ' . $this->user->username)
+            ->line('Deine E-Mail: ' . $this->user->email);
 
         if (!is_null($this->token)) {
-            return $message->action('Setup Your Account', url('/auth/password/reset/' . $this->token . '?email=' . urlencode($this->user->email)));
+            return $message->action('Account verwalten', url('/auth/password/reset/' . $this->token . '?email=' . urlencode($this->user->email)));
         }
 
         return $message;
diff --git a/app/Notifications/AddedToServer.php b/app/Notifications/AddedToServer.php
index c78c7db..40656c8 100644
--- a/app/Notifications/AddedToServer.php
+++ b/app/Notifications/AddedToServer.php
@@ -35,9 +35,9 @@ class AddedToServer extends Notification implements ShouldQueue
     public function toMail(): MailMessage
     {
         return (new MailMessage())
-            ->greeting('Hello ' . $this->server->user . '!')
-            ->line('You have been added as a subuser for the following server, allowing you certain control over the server.')
+            ->greeting('Hallo ' . $this->server->user . '!')
+            ->line('Gute Neuigkeiten! Du wurdest eingeladen, an einem Server mitzuwirken.')
             ->line('Server Name: ' . $this->server->name)
-            ->action('Visit Server', url('/server/' . $this->server->uuidShort));
+            ->action('Server aufrufen', url('/server/' . $this->server->uuidShort));
     }
 }
diff --git a/app/Notifications/MailTested.php b/app/Notifications/MailTested.php
index 892ff7b..0da27ac 100644
--- a/app/Notifications/MailTested.php
+++ b/app/Notifications/MailTested.php
@@ -20,8 +20,8 @@ class MailTested extends Notification
     public function toMail(): MailMessage
     {
         return (new MailMessage())
-            ->subject('Pterodactyl Test Message')
-            ->greeting('Hello ' . $this->user->name . '!')
-            ->line('This is a test of the Pterodactyl mail system. You\'re good to go!');
+            ->subject('Test bestanden!')
+            ->greeting('Hey ' . $this->user->name . '!')
+            ->line('Das ist eine Test-Mail, die über dein Panel ausgelöst wurde. Passt alles, du kannst loslegen.');
     }
 }
diff --git a/app/Notifications/RemovedFromServer.php b/app/Notifications/RemovedFromServer.php
index 492dc60..8c3f15d 100644
--- a/app/Notifications/RemovedFromServer.php
+++ b/app/Notifications/RemovedFromServer.php
@@ -36,9 +36,9 @@ class RemovedFromServer extends Notification implements ShouldQueue
     {
         return (new MailMessage())
             ->error()
-            ->greeting('Hello ' . $this->server->user . '.')
-            ->line('You have been removed as a subuser for the following server.')
+            ->greeting('Hallo ' . $this->server->user . '.')
+            ->line('Du wurdest als Mitwirkender aus einem Server entfernt.')
             ->line('Server Name: ' . $this->server->name)
-            ->action('Visit Panel', route('index'));
+            ->action('Server-Übersicht öffnen', route('index'));
     }
 }
diff --git a/app/Notifications/SendPasswordReset.php b/app/Notifications/SendPasswordReset.php
index b424c82..240cc3c 100644
--- a/app/Notifications/SendPasswordReset.php
+++ b/app/Notifications/SendPasswordReset.php
@@ -32,9 +32,9 @@ class SendPasswordReset extends Notification implements ShouldQueue
     public function toMail(mixed $notifiable): MailMessage
     {
         return (new MailMessage())
-            ->subject('Reset Password')
-            ->line('You are receiving this email because we received a password reset request for your account.')
-            ->action('Reset Password', url('/auth/password/reset/' . $this->token . '?email=' . urlencode($notifiable->email)))
-            ->line('If you did not request a password reset, no further action is required.');
+            ->subject('Passwort zurücksetzen')
+            ->line('Wir haben eine Anfragen, das du dein Passwort zurücksetzen möchtest. Klicke unten auf den Button, um fortzufahren.')
+            ->action('Passwort ändern', url('/auth/password/reset/' . $this->token . '?email=' . urlencode($notifiable->email)))
+            ->line('Du warst das nicht? Dann hat jemand anders versucht, dein Passwort zu ändern. Ändere es nicht für jemand anderen, denke an die Rechte deiner Server!');
     }
 }
diff --git a/app/Notifications/ServerInstalled.php b/app/Notifications/ServerInstalled.php
index 46f38ff..ec80266 100644
--- a/app/Notifications/ServerInstalled.php
+++ b/app/Notifications/ServerInstalled.php
@@ -52,9 +52,9 @@ class ServerInstalled extends Notification implements ShouldQueue, ReceivesEvent
     public function toMail(): MailMessage
     {
         return (new MailMessage())
-            ->greeting('Hello ' . $this->user->username . '.')
-            ->line('Your server has finished installing and is now ready for you to use.')
+            ->greeting('Hallo ' . $this->user->username . '.')
+            ->line('Gute Neuigkeiten: Dein Server ist einsatzbereit! Du kannst jetzt loslegen.')
             ->line('Server Name: ' . $this->server->name)
-            ->action('Login and Begin Using', route('index'));
+            ->action('Panel öffnen', route('index'));
     }
 }
diff --git a/app/Services/Backups/InitiateBackupService.php b/app/Services/Backups/InitiateBackupService.php
index be8f966..8419d73 100644
--- a/app/Services/Backups/InitiateBackupService.php
+++ b/app/Services/Backups/InitiateBackupService.php
@@ -80,7 +80,7 @@ class InitiateBackupService
         if ($period > 0) {
             $previous = $this->repository->getBackupsGeneratedDuringTimespan($server->id, $period);
             if ($previous->count() >= $limit) {
-                $message = sprintf('Only %d backups may be generated within a %d second span of time.', $limit, $period);
+                $message = sprintf('Nur %d Backups können innerhalb von %d Sekunden erstellt werden.', $limit, $period);
 
                 throw new TooManyRequestsHttpException(CarbonImmutable::now()->diffInSeconds($previous->last()->created_at->addSeconds($period)), $message);
             }
@@ -112,7 +112,7 @@ class InitiateBackupService
             $backup = $this->repository->create([
                 'server_id' => $server->id,
                 'uuid' => Uuid::uuid4()->toString(),
-                'name' => trim($name) ?: sprintf('Backup at %s', CarbonImmutable::now()->toDateTimeString()),
+                'name' => trim($name) ?: sprintf('Backup vom %s', CarbonImmutable::now()->toDateTimeString()),
                 'ignored_files' => array_values($this->ignoredFiles ?? []),
                 'disk' => $this->backupManager->getDefaultAdapter(),
                 'is_locked' => $this->isLocked,
diff --git a/patches/v1.11.3.patch b/patches/v1.11.3.patch
deleted file mode 100644
index 750b288..0000000
--- a/patches/v1.11.3.patch
+++ /dev/null
@@ -1,11053 +0,0 @@
-From 1f412594c41fc6b0a28bf4b83ca16ce37f5f0738 Mon Sep 17 00:00:00 2001
-From: Pavl21 <schwarzpaul123@gmail.com>
-Date: Sat, 4 Feb 2023 21:46:09 +0100
-Subject: [PATCH] Translations v1.11.3
-
----
- app/Exceptions/Handler.php                    |   4 +-
- .../Connection/DaemonConnectionException.php  |   4 +
- .../Http/Server/FileSizeTooLargeException.php |   2 +-
- .../Server/ServerStateConflictException.php   |  12 +-
- .../Http/TwoFactorAuthRequiredException.php   |   2 +-
- .../Model/DataValidationException.php         |   2 +-
- .../AutoAllocationNotEnabledException.php     |   2 +-
- ...oAutoAllocationSpaceAvailableException.php |   2 +-
- .../Service/Backup/BackupLockedException.php  |   2 +-
- .../Backup/TooManyBackupsException.php        |   2 +-
- ...tabaseClientFeatureNotEnabledException.php |   2 +-
- .../NoSuitableDatabaseHostException.php       |   2 +-
- .../Database/TooManyDatabasesException.php    |   2 +-
- .../TwoFactorAuthenticationTokenInvalid.php   |   2 +-
- app/Http/Controllers/Admin/ApiController.php  |   2 +-
- .../Controllers/Admin/DatabaseController.php  |  10 +-
- .../Controllers/Admin/LocationController.php  |   4 +-
- .../Controllers/Admin/MountController.php     |   8 +-
- .../Admin/NodeAutoDeployController.php        |   2 +-
- .../Admin/Servers/ServerViewController.php    |   2 +-
- .../Controllers/Admin/ServersController.php   |   4 +-
- .../Admin/Settings/AdvancedController.php     |   2 +-
- .../Admin/Settings/IndexController.php        |   2 +-
- .../Admin/Settings/MailController.php         |   2 +-
- .../Api/Client/ApiKeyController.php           |   2 +-
- .../Api/Client/Servers/BackupController.php   |   6 +-
- .../Api/Client/Servers/CommandController.php  |   2 +-
- .../Servers/NetworkAllocationController.php   |   4 +
- .../Api/Client/Servers/ScheduleController.php |   2 +-
- .../Client/Servers/ScheduleTaskController.php |   8 +-
- .../Api/Client/Servers/SettingsController.php |   2 +-
- .../Api/Client/Servers/StartupController.php  |   4 +-
- .../Client/Servers/WebsocketController.php    |   4 +-
- .../Api/Client/TwoFactorController.php        |   6 +-
- .../Backups/BackupRemoteUploadController.php  |   6 +-
- .../Remote/Backups/BackupStatusController.php |   4 +-
- .../Remote/SftpAuthenticationController.php   |   8 +-
- .../Auth/LoginCheckpointController.php        |   2 +-
- app/Models/Permission.php                     | 102 +++++------
- .../Backups/InitiateBackupService.php         |   4 +-
- public/themes/pterodactyl/css/pterodactyl.css |   2 +-
- .../themes/pterodactyl/js/admin/new-server.js |  18 +-
- .../pterodactyl/js/admin/server/transfer.js   |  10 +-
- resources/lang/en/activity.php                | 160 +++++++++---------
- resources/lang/en/admin/nests.php             |  24 +--
- resources/lang/en/admin/node.php              |  16 +-
- resources/lang/en/admin/server.php            |  38 ++---
- resources/lang/en/admin/user.php              |   6 +-
- resources/lang/en/auth.php                    |   2 +-
- resources/lang/en/command/messages.php        |  80 ++++-----
- resources/lang/en/exceptions.php              |  58 +++----
- resources/lang/en/pagination.php              |   4 +-
- resources/lang/en/passwords.php               |  10 +-
- resources/lang/en/strings.php                 | 146 ++++++++--------
- resources/lang/en/validation.php              | 132 +++++++--------
- .../scripts/api/server/files/compressFiles.ts |   4 +-
- .../api/server/files/decompressFiles.ts       |   2 +-
- .../scripts/components/NavigationBar.tsx      |   6 +-
- .../auth/ForgotPasswordContainer.tsx          |  20 +--
- .../auth/LoginCheckpointContainer.tsx         |  14 +-
- .../components/auth/LoginContainer.tsx        |  14 +-
- .../components/auth/LoginFormContainer.tsx    |  11 +-
- .../auth/ResetPasswordContainer.tsx           |  22 +--
- .../dashboard/AccountApiContainer.tsx         |  19 ++-
- .../dashboard/AccountOverviewContainer.tsx    |  12 +-
- .../components/dashboard/ApiKeyModal.tsx      |   7 +-
- .../dashboard/DashboardContainer.tsx          |   6 +-
- .../components/dashboard/ServerRow.tsx        |  22 +--
- .../activity/ActivityLogContainer.tsx         |   2 +-
- .../forms/ConfigureTwoFactorForm.tsx          |   8 +-
- .../dashboard/forms/CreateApiKeyForm.tsx      |  10 +-
- .../dashboard/forms/DisableTOTPDialog.tsx     |  12 +-
- .../dashboard/forms/RecoveryTokensDialog.tsx  |   8 +-
- .../dashboard/forms/SetupTOTPDialog.tsx       |  19 +--
- .../forms/UpdateEmailAddressForm.tsx          |  10 +-
- .../dashboard/forms/UpdatePasswordForm.tsx    |  16 +-
- .../dashboard/search/SearchContainer.tsx      |   2 +-
- .../dashboard/search/SearchModal.tsx          |   6 +-
- .../dashboard/ssh/AccountSSHContainer.tsx     |  13 +-
- .../dashboard/ssh/CreateSSHKeyForm.tsx        |   8 +-
- .../dashboard/ssh/DeleteSSHKeyButton.tsx      |   6 +-
- .../components/elements/ConfirmationModal.tsx |   2 +-
- .../components/elements/CopyOnClick.tsx       |   4 +-
- .../components/elements/ErrorBoundary.tsx     |   2 +-
- .../components/elements/PageContentBlock.tsx  |  11 +-
- .../components/elements/PermissionRoute.tsx   |   2 +-
- .../components/elements/ScreenBlock.tsx       |   4 +-
- .../elements/activity/ActivityLogEntry.tsx    |  11 +-
- .../activity/ActivityLogMetaButton.tsx        |   6 +-
- .../elements/dialog/ConfirmationDialog.tsx    |   2 +-
- .../elements/table/PaginationFooter.tsx       |   8 +-
- .../server/ConflictStateRenderer.tsx          |  18 +-
- .../server/ServerActivityLogContainer.tsx     |   6 +-
- .../components/server/UptimeDuration.tsx      |   4 +-
- .../components/server/WebsocketHandler.tsx    |   4 +-
- .../server/backups/BackupContainer.tsx        |   8 +-
- .../server/backups/BackupContextMenu.tsx      |  29 ++--
- .../components/server/backups/BackupRow.tsx   |   9 +-
- .../server/backups/CreateBackupButton.tsx     |  24 +--
- .../components/server/console/Console.tsx     |  10 +-
- .../server/console/PowerButtons.tsx           |  12 +-
- .../server/console/ServerConsoleContainer.tsx |   8 +-
- .../server/console/ServerDetailsBlock.tsx     |  16 +-
- .../components/server/console/StatGraphs.tsx  |  16 +-
- .../server/databases/CreateDatabaseButton.tsx |  26 +--
- .../server/databases/DatabaseRow.tsx          |  38 ++---
- .../server/databases/DatabasesContainer.tsx   |   9 +-
- .../server/databases/RotatePasswordButton.tsx |   2 +-
- .../server/features/GSLTokenModalFeature.tsx  |  12 +-
- .../features/JavaVersionModalFeature.tsx      |   6 +-
- .../server/features/PIDLimitModalFeature.tsx  |  22 ++-
- .../server/features/SteamDiskSpaceFeature.tsx |  20 +--
- .../server/features/eula/EulaModalFeature.tsx |   8 +-
- .../server/files/ChmodFileModal.tsx           |   4 +-
- .../server/files/FileDropdownMenu.tsx         |  24 +--
- .../server/files/FileEditContainer.tsx        |  12 +-
- .../server/files/FileManagerContainer.tsx     |   7 +-
- .../server/files/FileManagerStatus.tsx        |  10 +-
- .../components/server/files/FileNameModal.tsx |   6 +-
- .../components/server/files/FileObjectRow.tsx |   5 +-
- .../server/files/MassActionsBar.tsx           |  22 +--
- .../server/files/NewDirectoryButton.tsx       |  12 +-
- .../server/files/RenameFileModal.tsx          |   8 +-
- .../components/server/files/UploadButton.tsx  |   6 +-
- .../server/network/AllocationRow.tsx          |   6 +-
- .../server/network/DeleteAllocationButton.tsx |   6 +-
- .../server/network/NetworkContainer.tsx       |   7 +-
- .../server/schedules/DeleteScheduleButton.tsx |   8 +-
- .../server/schedules/EditScheduleModal.tsx    |  32 ++--
- .../server/schedules/NewTaskButton.tsx        |   2 +-
- .../server/schedules/RunScheduleButton.tsx    |   2 +-
- .../schedules/ScheduleCheatsheetCards.tsx     |  22 +--
- .../server/schedules/ScheduleContainer.tsx    |   6 +-
- .../server/schedules/ScheduleCronRow.tsx      |   8 +-
- .../schedules/ScheduleEditContainer.tsx       |  25 +--
- .../server/schedules/ScheduleRow.tsx          |   7 +-
- .../server/schedules/ScheduleTaskRow.tsx      |  24 +--
- .../server/schedules/TaskDetailsModal.tsx     |  48 +++---
- .../server/settings/ReinstallServerBox.tsx    |  20 +--
- .../server/settings/RenameServerBox.tsx       |   6 +-
- .../server/settings/SettingsContainer.tsx     |  14 +-
- .../server/startup/StartupContainer.tsx       |  16 +-
- .../components/server/startup/VariableBox.tsx |   2 +-
- .../server/users/AddSubuserButton.tsx         |   2 +-
- .../server/users/EditSubuserModal.tsx         |  22 +--
- .../server/users/RemoveSubuserButton.tsx      |  10 +-
- .../components/server/users/UserRow.tsx       |   4 +-
- .../server/users/UsersContainer.tsx           |   4 +-
- .../scripts/hoc/RequireServerPermission.tsx   |   2 +-
- resources/scripts/routers/routes.ts           |  26 +--
- resources/scripts/state/flashes.ts            |   4 +-
- resources/views/admin/api/index.blade.php     |  31 ++--
- resources/views/admin/api/new.blade.php       |  22 +--
- .../views/admin/databases/index.blade.php     |  42 ++---
- .../views/admin/databases/view.blade.php      |  46 ++---
- resources/views/admin/eggs/new.blade.php      |  64 +++----
- resources/views/admin/eggs/scripts.blade.php  |  36 ++--
- .../views/admin/eggs/variables.blade.php      |  60 +++----
- resources/views/admin/eggs/view.blade.php     |  78 ++++-----
- resources/views/admin/index.blade.php         |  16 +-
- .../views/admin/locations/index.blade.php     |  34 ++--
- .../views/admin/locations/view.blade.php      |  14 +-
- resources/views/admin/mounts/index.blade.php  |  52 +++---
- resources/views/admin/mounts/view.blade.php   |  54 +++---
- resources/views/admin/nests/index.blade.php   |  28 +--
- resources/views/admin/nests/new.blade.php     |  14 +-
- resources/views/admin/nests/view.blade.php    |  24 +--
- resources/views/admin/nodes/index.blade.php   |  22 +--
- resources/views/admin/nodes/new.blade.php     |  68 ++++----
- .../admin/nodes/view/allocation.blade.php     |  70 ++++----
- .../admin/nodes/view/configuration.blade.php  |  35 ++--
- .../views/admin/nodes/view/index.blade.php    |  40 ++---
- .../views/admin/nodes/view/servers.blade.php  |  22 +--
- .../views/admin/nodes/view/settings.blade.php |  94 +++++-----
- resources/views/admin/servers/index.blade.php |  24 +--
- resources/views/admin/servers/new.blade.php   | 112 ++++++------
- .../servers/partials/navigation.blade.php     |  16 +-
- .../views/admin/servers/view/build.blade.php  |  76 ++++-----
- .../admin/servers/view/database.blade.php     |  57 ++++---
- .../views/admin/servers/view/delete.blade.php |  32 ++--
- .../admin/servers/view/details.blade.php      |  22 +--
- .../views/admin/servers/view/index.blade.php  |  52 +++---
- .../views/admin/servers/view/manage.blade.php |  70 ++++----
- .../views/admin/servers/view/mounts.blade.php |  18 +-
- .../admin/servers/view/startup.blade.php      |  46 ++---
- .../views/admin/settings/advanced.blade.php   |  44 ++---
- .../views/admin/settings/index.blade.php      |  30 ++--
- resources/views/admin/settings/mail.blade.php |  59 +++----
- resources/views/admin/users/index.blade.php   |  24 +--
- resources/views/admin/users/new.blade.php     |  38 ++---
- resources/views/admin/users/view.blade.php    |  38 ++---
- resources/views/layouts/admin.blade.php       |  36 ++--
- .../partials/admin/settings/nav.blade.php     |   6 +-
- .../partials/admin/settings/notice.blade.php  |   2 +-
- 194 files changed, 1921 insertions(+), 1888 deletions(-)
-
-diff --git a/app/Exceptions/Handler.php b/app/Exceptions/Handler.php
-index bcc636b..46b7cac 100644
---- a/app/Exceptions/Handler.php
-+++ b/app/Exceptions/Handler.php
-@@ -200,14 +200,14 @@ class Handler extends ExceptionHandler
-                 : strval($match ?? '500'),
-             'detail' => $e instanceof HttpExceptionInterface || !is_null($match)
-                 ? $e->getMessage()
--                : 'An unexpected error was encountered while processing this request, please try again.',
-+                : 'Es trat ein unbekannter Fehler beim laufenen Prozess auf. Bitte versuche es erneut.',
-         ];
- 
-         if ($e instanceof ModelNotFoundException || $e->getPrevious() instanceof ModelNotFoundException) {
-             // Show a nicer error message compared to the standard "No query results for model"
-             // response that is normally returned. If we are in debug mode this will get overwritten
-             // with a more specific error message to help narrow down things.
--            $error['detail'] = 'The requested resource could not be found on the server.';
-+            $error['detail'] = 'Die gesuchten Daten konnten nicht gefunden werden';
-         }
- 
-         if (config('app.debug')) {
-diff --git a/app/Exceptions/Http/Connection/DaemonConnectionException.php b/app/Exceptions/Http/Connection/DaemonConnectionException.php
-index 8c3d474..54abc61 100644
---- a/app/Exceptions/Http/Connection/DaemonConnectionException.php
-+++ b/app/Exceptions/Http/Connection/DaemonConnectionException.php
-@@ -46,8 +46,10 @@ class DaemonConnectionException extends DisplayException
- 
-         if (is_null($response)) {
-             $message = 'Could not establish a connection to the machine running this server. Please try again.';
-+            $message = 'Es konnte keine Verbindung zum Server hergestellt werden. Bitte versuche es nochmal.';
-         } else {
-             $message = sprintf('There was an error while communicating with the machine running this server. This error has been logged, please try again. (code: %s) (request_id: %s)', $response->getStatusCode(), $this->requestId ?? '<nil>');
-+            $message = sprintf('Es ist ein Fehler bei der Verbindung zum Server aufgetreten. (Code: %s) (request_id: %s)', $response->getStatusCode(), $this->requestId ?? '<nil>');
-         }
- 
-         // Attempt to pull the actual error message off the response and return that if it is not
-@@ -55,6 +57,7 @@ class DaemonConnectionException extends DisplayException
-         if ($this->statusCode < 500 && !is_null($response)) {
-             $body = json_decode($response->getBody()->__toString(), true);
-             $message = sprintf('An error occurred on the remote host: %s. (request id: %s)', $body['error'] ?? $message, $this->requestId ?? '<nil>');
-+            $message = sprintf('Es trat ein Fehler beim Remote Host auf: %s. (request id: %s)', $body['error'] ?? $message, $this->requestId ?? '<nil>');
-         }
- 
-         $level = $this->statusCode >= 500 && $this->statusCode !== 504
-@@ -88,3 +91,4 @@ class DaemonConnectionException extends DisplayException
-         return $this->requestId;
-     }
- }
-+
-diff --git a/app/Exceptions/Http/Server/FileSizeTooLargeException.php b/app/Exceptions/Http/Server/FileSizeTooLargeException.php
-index 59a53d3..e516e93 100644
---- a/app/Exceptions/Http/Server/FileSizeTooLargeException.php
-+++ b/app/Exceptions/Http/Server/FileSizeTooLargeException.php
-@@ -11,6 +11,6 @@ class FileSizeTooLargeException extends DisplayException
-      */
-     public function __construct()
-     {
--        parent::__construct('The file you are attempting to open is too large to view in the file editor.');
-+        parent::__construct('Die Datei ist zu groß, um sie zu öffnen.');
-     }
- }
-diff --git a/app/Exceptions/Http/Server/ServerStateConflictException.php b/app/Exceptions/Http/Server/ServerStateConflictException.php
-index ea6a60a..63803ed 100644
---- a/app/Exceptions/Http/Server/ServerStateConflictException.php
-+++ b/app/Exceptions/Http/Server/ServerStateConflictException.php
-@@ -14,17 +14,17 @@ class ServerStateConflictException extends ConflictHttpException
-      */
-     public function __construct(Server $server, Throwable $previous = null)
-     {
--        $message = 'This server is currently in an unsupported state, please try again later.';
-+        $message = 'Der Server ist aktuell in einem unbekannten Status, bitte versuche es erneut.';
-         if ($server->isSuspended()) {
--            $message = 'This server is currently suspended and the functionality requested is unavailable.';
-+            $message = 'Der Server ist aktuell gesperrt und steht nicht zur Verfügung.';
-         } elseif ($server->node->isUnderMaintenance()) {
--            $message = 'The node of this server is currently under maintenance and the functionality requested is unavailable.';
-+            $message = 'Der Node des Servers ist derzeit in Wartungsarbeiten. In der Zeit ist dein Server nicht erreichbar.';
-         } elseif (!$server->isInstalled()) {
--            $message = 'This server has not yet completed its installation process, please try again later.';
-+            $message = 'Der Server hat noch einen aktiven Installationsprozess.';
-         } elseif ($server->status === Server::STATUS_RESTORING_BACKUP) {
--            $message = 'This server is currently restoring from a backup, please try again later.';
-+            $message = 'Der Server wird aktuell wiederhergestellt.';
-         } elseif (!is_null($server->transfer)) {
--            $message = 'This server is currently being transferred to a new machine, please try again later.';
-+            $message = 'Der Server wird gerade zu einem anderen Node übertragen. Das kann etwas dauern.';
-         }
- 
-         parent::__construct($message, $previous);
-diff --git a/app/Exceptions/Http/TwoFactorAuthRequiredException.php b/app/Exceptions/Http/TwoFactorAuthRequiredException.php
-index cecb646..f032d47 100644
---- a/app/Exceptions/Http/TwoFactorAuthRequiredException.php
-+++ b/app/Exceptions/Http/TwoFactorAuthRequiredException.php
-@@ -14,6 +14,6 @@ class TwoFactorAuthRequiredException extends HttpException implements HttpExcept
-      */
-     public function __construct(Throwable $previous = null)
-     {
--        parent::__construct(Response::HTTP_BAD_REQUEST, 'Two-factor authentication is required on this account in order to access this endpoint.', $previous);
-+        parent::__construct(Response::HTTP_BAD_REQUEST, 'Es wird hier eine 2-Fakor Authentifizierung benötigt.', $previous);
-     }
- }
-diff --git a/app/Exceptions/Model/DataValidationException.php b/app/Exceptions/Model/DataValidationException.php
-index 6ceb6e9..63b4cac 100644
---- a/app/Exceptions/Model/DataValidationException.php
-+++ b/app/Exceptions/Model/DataValidationException.php
-@@ -17,7 +17,7 @@ class DataValidationException extends PterodactylException implements HttpExcept
-     public function __construct(protected Validator $validator, protected Model $model)
-     {
-         $message = sprintf(
--            'Could not save %s[%s]: failed to validate data: %s',
-+            'Konnte %s[%s] nicht speichern: fehlschlag beim validieren der Daten: %s',
-             get_class($model),
-             $model->getKey(),
-             $validator->errors()->toJson()
-diff --git a/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php b/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php
-index a593347..eb80e25 100644
---- a/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php
-+++ b/app/Exceptions/Service/Allocation/AutoAllocationNotEnabledException.php
-@@ -12,7 +12,7 @@ class AutoAllocationNotEnabledException extends DisplayException
-     public function __construct()
-     {
-         parent::__construct(
--            'Server auto-allocation is not enabled for this instance.'
-+            'Auf diesem Server ist die automatische Portfreischaltung nicht erlaubt.'
-         );
-     }
- }
-diff --git a/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php b/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php
-index 8e3c9b0..2283ca7 100644
---- a/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php
-+++ b/app/Exceptions/Service/Allocation/NoAutoAllocationSpaceAvailableException.php
-@@ -12,7 +12,7 @@ class NoAutoAllocationSpaceAvailableException extends DisplayException
-     public function __construct()
-     {
-         parent::__construct(
--            'Cannot assign additional allocation: no more space available on node.'
-+            'Dies ist zurzeit leider nicht möglich: Node ist überlastet und muss erweitert werden.'
-         );
-     }
- }
-diff --git a/app/Exceptions/Service/Backup/BackupLockedException.php b/app/Exceptions/Service/Backup/BackupLockedException.php
-index 3c9dbcf..3ec969f 100644
---- a/app/Exceptions/Service/Backup/BackupLockedException.php
-+++ b/app/Exceptions/Service/Backup/BackupLockedException.php
-@@ -11,6 +11,6 @@ class BackupLockedException extends DisplayException
-      */
-     public function __construct()
-     {
--        parent::__construct('Cannot delete a backup that is marked as locked.');
-+        parent::__construct('Backup kann nicht gelöscht werden, es wird derzeit geschützt.');
-     }
- }
-diff --git a/app/Exceptions/Service/Backup/TooManyBackupsException.php b/app/Exceptions/Service/Backup/TooManyBackupsException.php
-index 8ccbaf7..0dd2eb3 100644
---- a/app/Exceptions/Service/Backup/TooManyBackupsException.php
-+++ b/app/Exceptions/Service/Backup/TooManyBackupsException.php
-@@ -12,7 +12,7 @@ class TooManyBackupsException extends DisplayException
-     public function __construct(int $backupLimit)
-     {
-         parent::__construct(
--            sprintf('Cannot create a new backup, this server has reached its limit of %d backups.', $backupLimit)
-+            sprintf('Du darfst maximal %d Backups besitzen.', $backupLimit)
-         );
-     }
- }
-diff --git a/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php b/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php
-index 809cb4f..f4a1325 100644
---- a/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php
-+++ b/app/Exceptions/Service/Database/DatabaseClientFeatureNotEnabledException.php
-@@ -8,6 +8,6 @@ class DatabaseClientFeatureNotEnabledException extends PterodactylException
- {
-     public function __construct()
-     {
--        parent::__construct('Client database creation is not enabled in this Panel.');
-+        parent::__construct('Die Erstellung von Datenbanken ist deaktiveirt.');
-     }
- }
-diff --git a/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php b/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php
-index 72f8dca..afbaac2 100644
---- a/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php
-+++ b/app/Exceptions/Service/Database/NoSuitableDatabaseHostException.php
-@@ -11,6 +11,6 @@ class NoSuitableDatabaseHostException extends DisplayException
-      */
-     public function __construct()
-     {
--        parent::__construct('No database host was found that meets the requirements for this server.');
-+        parent::__construct('Dieses System stellt aktuell keinen Host für Datenbanken zur Verfügung. Dies kann vom Betreiber eingerichtet werden.');
-     }
- }
-diff --git a/app/Exceptions/Service/Database/TooManyDatabasesException.php b/app/Exceptions/Service/Database/TooManyDatabasesException.php
-index fe0e305..5090e13 100644
---- a/app/Exceptions/Service/Database/TooManyDatabasesException.php
-+++ b/app/Exceptions/Service/Database/TooManyDatabasesException.php
-@@ -8,6 +8,6 @@ class TooManyDatabasesException extends DisplayException
- {
-     public function __construct()
-     {
--        parent::__construct('Operation aborted: creating a new database would put this server over the defined limit.');
-+        parent::__construct('Nicht möglich: Du hast bereits das Limit an Datenbanken für diesen Server erreicht.');
-     }
- }
-diff --git a/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php b/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php
-index b7b819b..08cae08 100644
---- a/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php
-+++ b/app/Exceptions/Service/User/TwoFactorAuthenticationTokenInvalid.php
-@@ -11,6 +11,6 @@ class TwoFactorAuthenticationTokenInvalid extends DisplayException
-      */
-     public function __construct()
-     {
--        parent::__construct('The provided two-factor authentication token was not valid.');
-+        parent::__construct('Der 6-stellige Code ist falsch. Bitte erneut versuchen.');
-     }
- }
-diff --git a/app/Http/Controllers/Admin/ApiController.php b/app/Http/Controllers/Admin/ApiController.php
-index 02ad6e5..e30660b 100644
---- a/app/Http/Controllers/Admin/ApiController.php
-+++ b/app/Http/Controllers/Admin/ApiController.php
-@@ -70,7 +70,7 @@ class ApiController extends Controller
-             'user_id' => $request->user()->id,
-         ], $request->getKeyPermissions());
- 
--        $this->alert->success('A new application API key has been generated for your account.')->flash();
-+        $this->alert->success('Es wurde ein neuer API-Schlüssel aktiviert und steht jetzt zur Verfügung.')->flash();
- 
-         return redirect()->route('admin.api.index');
-     }
-diff --git a/app/Http/Controllers/Admin/DatabaseController.php b/app/Http/Controllers/Admin/DatabaseController.php
-index e0dc0dc..889ccf8 100644
---- a/app/Http/Controllers/Admin/DatabaseController.php
-+++ b/app/Http/Controllers/Admin/DatabaseController.php
-@@ -72,7 +72,7 @@ class DatabaseController extends Controller
-         } catch (Exception $exception) {
-             if ($exception instanceof PDOException || $exception->getPrevious() instanceof PDOException) {
-                 $this->alert->danger(
--                    sprintf('There was an error while trying to connect to the host or while executing a query: "%s"', $exception->getMessage())
-+                    sprintf('Es ist ein Fehler beim Verbindungsaufbau des Datenbank Hosts aufgetreten: "%s"', $exception->getMessage())
-                 )->flash();
- 
-                 return redirect()->route('admin.databases')->withInput($request->validated());
-@@ -81,7 +81,7 @@ class DatabaseController extends Controller
-             }
-         }
- 
--        $this->alert->success('Successfully created a new database host on the system.')->flash();
-+        $this->alert->success('Das Datenbank Host-System wurde erfolgreich erstellt.')->flash();
- 
-         return redirect()->route('admin.databases.view', $host->id);
-     }
-@@ -97,13 +97,13 @@ class DatabaseController extends Controller
- 
-         try {
-             $this->updateService->handle($host->id, $request->normalize());
--            $this->alert->success('Database host was updated successfully.')->flash();
-+            $this->alert->success('Datenbank-Host wurde aktualisiert.')->flash();
-         } catch (Exception $exception) {
-             // Catch any SQL related exceptions and display them back to the user, otherwise just
-             // throw the exception like normal and move on with it.
-             if ($exception instanceof PDOException || $exception->getPrevious() instanceof PDOException) {
-                 $this->alert->danger(
--                    sprintf('There was an error while trying to connect to the host or while executing a query: "%s"', $exception->getMessage())
-+                    sprintf('Es ist ein Fehler beim Verbindungsaufbau des Datenbank Hosts aufgetreten: "%s"', $exception->getMessage())
-                 )->flash();
- 
-                 return $redirect->withInput($request->normalize());
-@@ -123,7 +123,7 @@ class DatabaseController extends Controller
-     public function delete(int $host): RedirectResponse
-     {
-         $this->deletionService->handle($host);
--        $this->alert->success('The requested database host has been deleted from the system.')->flash();
-+        $this->alert->success('Das Datenbank Host-System wurde erfolgreich entfernt.')->flash();
- 
-         return redirect()->route('admin.databases');
-     }
-diff --git a/app/Http/Controllers/Admin/LocationController.php b/app/Http/Controllers/Admin/LocationController.php
-index ea01cba..4f35cc8 100644
---- a/app/Http/Controllers/Admin/LocationController.php
-+++ b/app/Http/Controllers/Admin/LocationController.php
-@@ -60,7 +60,7 @@ class LocationController extends Controller
-     public function create(LocationFormRequest $request): RedirectResponse
-     {
-         $location = $this->creationService->handle($request->normalize());
--        $this->alert->success('Location was created successfully.')->flash();
-+        $this->alert->success('Standort wurde erfolgreich erstellt.')->flash();
- 
-         return redirect()->route('admin.locations.view', $location->id);
-     }
-@@ -77,7 +77,7 @@ class LocationController extends Controller
-         }
- 
-         $this->updateService->handle($location->id, $request->normalize());
--        $this->alert->success('Location was updated successfully.')->flash();
-+        $this->alert->success('Der Standort wurde erfolgreich aktualisiert.')->flash();
- 
-         return redirect()->route('admin.locations.view', $location->id);
-     }
-diff --git a/app/Http/Controllers/Admin/MountController.php b/app/Http/Controllers/Admin/MountController.php
-index 097ad66..70c598c 100644
---- a/app/Http/Controllers/Admin/MountController.php
-+++ b/app/Http/Controllers/Admin/MountController.php
-@@ -72,7 +72,7 @@ class MountController extends Controller
-         $model->saveOrFail();
-         $mount = $model->fresh();
- 
--        $this->alert->success('Mount was created successfully.')->flash();
-+        $this->alert->success('Mount wurde erfolgreich erstellt.')->flash();
- 
-         return redirect()->route('admin.mounts.view', $mount->id);
-     }
-@@ -90,7 +90,7 @@ class MountController extends Controller
- 
-         $mount->forceFill($request->validated())->save();
- 
--        $this->alert->success('Mount was updated successfully.')->flash();
-+        $this->alert->success('Mount wurde erfolgreich aktualisiert.')->flash();
- 
-         return redirect()->route('admin.mounts.view', $mount->id);
-     }
-@@ -121,7 +121,7 @@ class MountController extends Controller
-             $mount->eggs()->attach($eggs);
-         }
- 
--        $this->alert->success('Mount was updated successfully.')->flash();
-+        $this->alert->success('Mount wurde erfolgreich aktualisiert.')->flash();
- 
-         return redirect()->route('admin.mounts.view', $mount->id);
-     }
-@@ -138,7 +138,7 @@ class MountController extends Controller
-             $mount->nodes()->attach($nodes);
-         }
- 
--        $this->alert->success('Mount was updated successfully.')->flash();
-+        $this->alert->success('Mount wurde erfolgreich aktualisiert.')->flash();
- 
-         return redirect()->route('admin.mounts.view', $mount->id);
-     }
-diff --git a/app/Http/Controllers/Admin/NodeAutoDeployController.php b/app/Http/Controllers/Admin/NodeAutoDeployController.php
-index ac0684a..06f5a76 100644
---- a/app/Http/Controllers/Admin/NodeAutoDeployController.php
-+++ b/app/Http/Controllers/Admin/NodeAutoDeployController.php
-@@ -49,7 +49,7 @@ class NodeAutoDeployController extends Controller
-         if (!$key) {
-             $key = $this->keyCreationService->setKeyType(ApiKey::TYPE_APPLICATION)->handle([
-                 'user_id' => $request->user()->id,
--                'memo' => 'Automatically generated node deployment key.',
-+                'memo' => 'Es wurde ein neuer Entwickler-Schlüssel (Dev-Key) generiert.',
-                 'allowed_ips' => [],
-             ], ['r_nodes' => 1]);
-         }
-diff --git a/app/Http/Controllers/Admin/Servers/ServerViewController.php b/app/Http/Controllers/Admin/Servers/ServerViewController.php
-index 7cf64a2..f49ac67 100644
---- a/app/Http/Controllers/Admin/Servers/ServerViewController.php
-+++ b/app/Http/Controllers/Admin/Servers/ServerViewController.php
-@@ -124,7 +124,7 @@ class ServerViewController extends Controller
-     public function manage(Request $request, Server $server): View
-     {
-         if ($server->status === Server::STATUS_INSTALL_FAILED) {
--            throw new DisplayException('This server is in a failed install state and cannot be recovered. Please delete and re-create the server.');
-+            throw new DisplayException('Dieser Server kann nicht aufgesetzt werden, da ein unterwarteter Fehler aufgetreten ist. Der gesamte Server ist betroffen, bitte setze ihn neu auf.');
-         }
- 
-         // Check if the panel doesn't have at least 2 nodes configured.
-diff --git a/app/Http/Controllers/Admin/ServersController.php b/app/Http/Controllers/Admin/ServersController.php
-index 3d2c55a..639cbd9 100644
---- a/app/Http/Controllers/Admin/ServersController.php
-+++ b/app/Http/Controllers/Admin/ServersController.php
-@@ -254,7 +254,7 @@ class ServersController extends Controller
- 
-         $mountServer->saveOrFail();
- 
--        $this->alert->success('Mount was added successfully.')->flash();
-+        $this->alert->success('Mount wurde erfolgreich hinzugefügt.')->flash();
- 
-         return redirect()->route('admin.servers.view.mounts', $server->id);
-     }
-@@ -266,7 +266,7 @@ class ServersController extends Controller
-     {
-         MountServer::where('mount_id', $mount->id)->where('server_id', $server->id)->delete();
- 
--        $this->alert->success('Mount was removed successfully.')->flash();
-+        $this->alert->success('Mount wurde erfolgreich entfernt.')->flash();
- 
-         return redirect()->route('admin.servers.view.mounts', $server->id);
-     }
-diff --git a/app/Http/Controllers/Admin/Settings/AdvancedController.php b/app/Http/Controllers/Admin/Settings/AdvancedController.php
-index bf68832..085d589 100644
---- a/app/Http/Controllers/Admin/Settings/AdvancedController.php
-+++ b/app/Http/Controllers/Admin/Settings/AdvancedController.php
-@@ -55,7 +55,7 @@ class AdvancedController extends Controller
-         }
- 
-         $this->kernel->call('queue:restart');
--        $this->alert->success('Advanced settings have been updated successfully and the queue worker was restarted to apply these changes.')->flash();
-+        $this->alert->success('Die Einstellungen wurden gespeichert und das reCAPTCHA Service wurde (bei Bedarf) neu gestartet')->flash();
- 
-         return redirect()->route('admin.settings.advanced');
-     }
-diff --git a/app/Http/Controllers/Admin/Settings/IndexController.php b/app/Http/Controllers/Admin/Settings/IndexController.php
-index eabede9..49a5e47 100644
---- a/app/Http/Controllers/Admin/Settings/IndexController.php
-+++ b/app/Http/Controllers/Admin/Settings/IndexController.php
-@@ -53,7 +53,7 @@ class IndexController extends Controller
-         }
- 
-         $this->kernel->call('queue:restart');
--        $this->alert->success('Panel settings have been updated successfully and the queue worker was restarted to apply these changes.')->flash();
-+        $this->alert->success('Die Änderungen wurden gespeichert und werden in Kürze angewendet.')->flash();
- 
-         return redirect()->route('admin.settings');
-     }
-diff --git a/app/Http/Controllers/Admin/Settings/MailController.php b/app/Http/Controllers/Admin/Settings/MailController.php
-index 050990d..037d718 100644
---- a/app/Http/Controllers/Admin/Settings/MailController.php
-+++ b/app/Http/Controllers/Admin/Settings/MailController.php
-@@ -53,7 +53,7 @@ class MailController extends Controller
-     public function update(MailSettingsFormRequest $request): Response
-     {
-         if ($this->config->get('mail.default') !== 'smtp') {
--            throw new DisplayException('This feature is only available if SMTP is the selected email driver for the Panel.');
-+            throw new DisplayException('Das Mail-System ist noch nicht eingereichtet.');
-         }
- 
-         $values = $request->normalize();
-diff --git a/app/Http/Controllers/Api/Client/ApiKeyController.php b/app/Http/Controllers/Api/Client/ApiKeyController.php
-index ac00a4d..f2349a2 100644
---- a/app/Http/Controllers/Api/Client/ApiKeyController.php
-+++ b/app/Http/Controllers/Api/Client/ApiKeyController.php
-@@ -30,7 +30,7 @@ class ApiKeyController extends ClientApiController
-     public function store(StoreApiKeyRequest $request): array
-     {
-         if ($request->user()->apiKeys->count() >= 25) {
--            throw new DisplayException('You have reached the account limit for number of API keys.');
-+            throw new DisplayException('Du hast das Limit an API-Schlüssel erreicht.');
-         }
- 
-         $token = $request->user()->createToken(
-diff --git a/app/Http/Controllers/Api/Client/Servers/BackupController.php b/app/Http/Controllers/Api/Client/Servers/BackupController.php
-index 7a35341..24ceb38 100644
---- a/app/Http/Controllers/Api/Client/Servers/BackupController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/BackupController.php
-@@ -164,7 +164,7 @@ class BackupController extends ClientApiController
-         }
- 
-         if ($backup->disk !== Backup::ADAPTER_AWS_S3 && $backup->disk !== Backup::ADAPTER_WINGS) {
--            throw new BadRequestHttpException('The backup requested references an unknown disk driver type and cannot be downloaded.');
-+            throw new BadRequestHttpException('Die Quelle des Backups ist derzeit unbekannt und kann nicht heruntergeladen werden.');
-         }
- 
-         $url = $this->downloadLinkService->handle($backup, $request->user());
-@@ -197,11 +197,11 @@ class BackupController extends ClientApiController
-         // Cannot restore a backup unless a server is fully installed and not currently
-         // processing a different backup restoration request.
-         if (!is_null($server->status)) {
--            throw new BadRequestHttpException('This server is not currently in a state that allows for a backup to be restored.');
-+            throw new BadRequestHttpException('Aktuell ist der Server in einem Status, wo kein Backup angewendet werden kann.');
-         }
- 
-         if (!$backup->is_successful && is_null($backup->completed_at)) {
--            throw new BadRequestHttpException('This backup cannot be restored at this time: not completed or failed.');
-+            throw new BadRequestHttpException('Dieses Backup kann zurzeit nicht verwendet werden: Unvollständig oder Fehlerhaft.');
-         }
- 
-         $log = Activity::event('server:backup.restore')
-diff --git a/app/Http/Controllers/Api/Client/Servers/CommandController.php b/app/Http/Controllers/Api/Client/Servers/CommandController.php
-index 6cc50de..44eac18 100644
---- a/app/Http/Controllers/Api/Client/Servers/CommandController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/CommandController.php
-@@ -40,7 +40,7 @@ class CommandController extends ClientApiController
-                     $previous->getResponse() instanceof ResponseInterface
-                     && $previous->getResponse()->getStatusCode() === Response::HTTP_BAD_GATEWAY
-                 ) {
--                    throw new HttpException(Response::HTTP_BAD_GATEWAY, 'Server must be online in order to send commands.', $exception);
-+                    throw new HttpException(Response::HTTP_BAD_GATEWAY, 'Der Server muss online sein, damit du Befehle senden kannst.', $exception);
-                 }
-             }
- 
-diff --git a/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php b/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php
-index 4e3c5f9..e9ae9d0 100644
---- a/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/NetworkAllocationController.php
-@@ -94,6 +94,7 @@ class NetworkAllocationController extends ClientApiController
-     {
-         if ($server->allocations()->count() >= $server->allocation_limit) {
-             throw new DisplayException('Cannot assign additional allocations to this server: limit has been reached.');
-+            throw new DisplayException('Der angeforderte Port konnte nicht dem Server zugewiesen werden, du hast das Limit bereits erreicht.');
-         }
- 
-         $allocation = $this->assignableAllocationService->handle($server);
-@@ -119,10 +120,12 @@ class NetworkAllocationController extends ClientApiController
-         // allocation limit set.
-         if (empty($server->allocation_limit)) {
-             throw new DisplayException('You cannot delete allocations for this server: no allocation limit is set.');
-+            throw new DisplayException('Du kannst Ports für diesen Server nicht löschen: Es ist kein Port-Limit festgelegt.');
-         }
- 
-         if ($allocation->id === $server->allocation_id) {
-             throw new DisplayException('You cannot delete the primary allocation for this server.');
-+            throw new DisplayException('Du kannst den primären Port für diesen Server nicht löschen.');
-         }
- 
-         Allocation::query()->where('id', $allocation->id)->update([
-@@ -138,3 +141,4 @@ class NetworkAllocationController extends ClientApiController
-         return new JsonResponse([], JsonResponse::HTTP_NO_CONTENT);
-     }
- }
-+
-diff --git a/app/Http/Controllers/Api/Client/Servers/ScheduleController.php b/app/Http/Controllers/Api/Client/Servers/ScheduleController.php
-index 50c04c7..3b23933 100644
---- a/app/Http/Controllers/Api/Client/Servers/ScheduleController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/ScheduleController.php
-@@ -179,7 +179,7 @@ class ScheduleController extends ClientApiController
-                 $request->input('day_of_week')
-             );
-         } catch (Exception $exception) {
--            throw new DisplayException('The cron data provided does not evaluate to a valid expression.');
-+            throw new DisplayException('Die Angaben entsprechen nicht den Regeln von Cron.');
-         }
-     }
- }
-diff --git a/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php b/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php
-index e2e4389..21665d5 100644
---- a/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/ScheduleTaskController.php
-@@ -41,11 +41,11 @@ class ScheduleTaskController extends ClientApiController
-     {
-         $limit = config('pterodactyl.client_features.schedules.per_schedule_task_limit', 10);
-         if ($schedule->tasks()->count() >= $limit) {
--            throw new ServiceLimitExceededException("Schedules may not have more than $limit tasks associated with them. Creating this task would put this schedule over the limit.");
-+            throw new ServiceLimitExceededException("Zeitpläne können maximal $limit Aufgaben beinhalten.");
-         }
- 
-         if ($server->backup_limit === 0 && $request->action === 'backup') {
--            throw new HttpForbiddenException("A backup task cannot be created when the server's backup limit is set to 0.");
-+            throw new HttpForbiddenException("Die Aufgabe, Backups zu erstellen ist nicht möglich. Dieser Server darf momentan keine Backups erstellen.");
-         }
- 
-         /** @var \Pterodactyl\Models\Task|null $lastTask */
-@@ -105,7 +105,7 @@ class ScheduleTaskController extends ClientApiController
-         }
- 
-         if ($server->backup_limit === 0 && $request->action === 'backup') {
--            throw new HttpForbiddenException("A backup task cannot be created when the server's backup limit is set to 0.");
-+            throw new HttpForbiddenException("Eine Aufgabe mit einem Backup-Befehl kann nicht ausgeführt werden. Dieser Server darf aktuell keine Backups erstellen.");
-         }
- 
-         $this->connection->transaction(function () use ($request, $schedule, $task) {
-@@ -160,7 +160,7 @@ class ScheduleTaskController extends ClientApiController
-         }
- 
-         if (!$request->user()->can(Permission::ACTION_SCHEDULE_UPDATE, $server)) {
--            throw new HttpForbiddenException('You do not have permission to perform this action.');
-+            throw new HttpForbiddenException('Du hast dafür ungenügende Rechte.');
-         }
- 
-         $schedule->tasks()
-diff --git a/app/Http/Controllers/Api/Client/Servers/SettingsController.php b/app/Http/Controllers/Api/Client/Servers/SettingsController.php
-index e84c159..a8a3695 100644
---- a/app/Http/Controllers/Api/Client/Servers/SettingsController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/SettingsController.php
-@@ -78,7 +78,7 @@ class SettingsController extends ClientApiController
-     public function dockerImage(SetDockerImageRequest $request, Server $server): JsonResponse
-     {
-         if (!in_array($server->image, array_values($server->egg->docker_images))) {
--            throw new BadRequestHttpException('This server\'s Docker image has been manually set by an administrator and cannot be updated.');
-+            throw new BadRequestHttpException('Dieses Docker Image wurde vom Betreiber hinzugefügt und kann nicht verändert werden.');
-         }
- 
-         $original = $server->image;
-diff --git a/app/Http/Controllers/Api/Client/Servers/StartupController.php b/app/Http/Controllers/Api/Client/Servers/StartupController.php
-index 9548be2..f7a6148 100644
---- a/app/Http/Controllers/Api/Client/Servers/StartupController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/StartupController.php
-@@ -57,9 +57,9 @@ class StartupController extends ClientApiController
-         $original = $variable->server_value;
- 
-         if (is_null($variable) || !$variable->user_viewable) {
--            throw new BadRequestHttpException('The environment variable you are trying to edit does not exist.');
-+            throw new BadRequestHttpException('Die Variable, die du nutzen möchtest existiert nicht.');
-         } elseif (!$variable->user_editable) {
--            throw new BadRequestHttpException('The environment variable you are trying to edit is read-only.');
-+            throw new BadRequestHttpException('Die Variable, die du bearbeiten möchtest, darf nicht verändert werden.');
-         }
- 
-         // Revalidate the variable value using the egg variable specific validation rules for it.
-diff --git a/app/Http/Controllers/Api/Client/Servers/WebsocketController.php b/app/Http/Controllers/Api/Client/Servers/WebsocketController.php
-index 59b6f75..0e7d682 100644
---- a/app/Http/Controllers/Api/Client/Servers/WebsocketController.php
-+++ b/app/Http/Controllers/Api/Client/Servers/WebsocketController.php
-@@ -34,7 +34,7 @@ class WebsocketController extends ClientApiController
-     {
-         $user = $request->user();
-         if ($user->cannot(Permission::ACTION_WEBSOCKET_CONNECT, $server)) {
--            throw new HttpForbiddenException('You do not have permission to connect to this server\'s websocket.');
-+            throw new HttpForbiddenException('Du hast nicht die Berechtigung, um dich mit dem Websocket Server zu verbinden.');
-         }
- 
-         $permissions = $this->permissionsService->handle($server, $user);
-@@ -43,7 +43,7 @@ class WebsocketController extends ClientApiController
-         if (!is_null($server->transfer)) {
-             // Check if the user has permissions to receive transfer logs.
-             if (!in_array('admin.websocket.transfer', $permissions)) {
--                throw new HttpForbiddenException('You do not have permission to view server transfer logs.');
-+                throw new HttpForbiddenException('Du hast nicht die Berechtigung, um die Transfer-Logs zu sehen.');
-             }
- 
-             // Redirect the websocket request to the new node if the server has been archived.
-diff --git a/app/Http/Controllers/Api/Client/TwoFactorController.php b/app/Http/Controllers/Api/Client/TwoFactorController.php
-index 8cfa09b..7d6715e 100644
---- a/app/Http/Controllers/Api/Client/TwoFactorController.php
-+++ b/app/Http/Controllers/Api/Client/TwoFactorController.php
-@@ -36,7 +36,7 @@ class TwoFactorController extends ClientApiController
-     public function index(Request $request): JsonResponse
-     {
-         if ($request->user()->use_totp) {
--            throw new BadRequestHttpException('Two-factor authentication is already enabled on this account.');
-+            throw new BadRequestHttpException('2-Fakor Authentifizierung ist bereits aktiv..');
-         }
- 
-         return new JsonResponse([
-@@ -59,7 +59,7 @@ class TwoFactorController extends ClientApiController
- 
-         $data = $validator->validate();
-         if (!password_verify($data['password'], $request->user()->password)) {
--            throw new BadRequestHttpException('The password provided was not valid.');
-+            throw new BadRequestHttpException('Das Passwort ist falsch.');
-         }
- 
-         $tokens = $this->toggleTwoFactorService->handle($request->user(), $data['code'], true);
-@@ -83,7 +83,7 @@ class TwoFactorController extends ClientApiController
-     public function delete(Request $request): JsonResponse
-     {
-         if (!password_verify($request->input('password') ?? '', $request->user()->password)) {
--            throw new BadRequestHttpException('The password provided was not valid.');
-+            throw new BadRequestHttpException('Das Passwort ist falsch.');
-         }
- 
-         /** @var \Pterodactyl\Models\User $user */
-diff --git a/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php b/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php
-index 7d92e0b..549a7bb 100644
---- a/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php
-+++ b/app/Http/Controllers/Api/Remote/Backups/BackupRemoteUploadController.php
-@@ -35,7 +35,7 @@ class BackupRemoteUploadController extends Controller
-         // Get the size query parameter.
-         $size = (int) $request->query('size');
-         if (empty($size)) {
--            throw new BadRequestHttpException('A non-empty "size" query parameter must be provided.');
-+            throw new BadRequestHttpException('Es muss ein nicht leerer Abfrageparameter "Größe" angegeben werden.');
-         }
- 
-         /** @var \Pterodactyl\Models\Backup $backup */
-@@ -44,13 +44,13 @@ class BackupRemoteUploadController extends Controller
-         // Prevent backups that have already been completed from trying to
-         // be uploaded again.
-         if (!is_null($backup->completed_at)) {
--            throw new ConflictHttpException('This backup is already in a completed state.');
-+            throw new ConflictHttpException('Das Backup ist bereits fertig.');
-         }
- 
-         // Ensure we are using the S3 adapter.
-         $adapter = $this->backupManager->adapter();
-         if (!$adapter instanceof S3Filesystem) {
--            throw new BadRequestHttpException('The configured backup adapter is not an S3 compatible adapter.');
-+            throw new BadRequestHttpException('Der konfigurierte Backup-Adapter ist kein S3-kompatibler Adapter.');
-         }
- 
-         // The path where backup will be uploaded to
-diff --git a/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php b/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php
-index f9c2a79..8ea4b3e 100644
---- a/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php
-+++ b/app/Http/Controllers/Api/Remote/Backups/BackupStatusController.php
-@@ -34,7 +34,7 @@ class BackupStatusController extends Controller
-         $model = Backup::query()->where('uuid', $backup)->firstOrFail();
- 
-         if ($model->is_successful) {
--            throw new BadRequestHttpException('Cannot update the status of a backup that is already marked as completed.');
-+            throw new BadRequestHttpException('Der Status des Backups konnte nicht geändert werden, es ist bereits als fertig markiert..');
-         }
- 
-         $action = $request->boolean('successful') ? 'server:backup.complete' : 'server:backup.fail';
-@@ -109,7 +109,7 @@ class BackupStatusController extends Controller
-                 return;
-             }
- 
--            throw new DisplayException('Cannot complete backup request: no upload_id present on model.');
-+            throw new DisplayException('Backup-Anfrage konnte nicht ausgeführt werden: upload_id am Modell vorhanden.');
-         }
- 
-         $params = [
-diff --git a/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php b/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php
-index a360d1d..0d0ac6e 100644
---- a/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php
-+++ b/app/Http/Controllers/Api/Remote/SftpAuthenticationController.php
-@@ -35,13 +35,13 @@ class SftpAuthenticationController extends Controller
-     {
-         $connection = $this->parseUsername($request->input('username'));
-         if (empty($connection['server'])) {
--            throw new BadRequestHttpException('No valid server identifier was included in the request.');
-+            throw new BadRequestHttpException('Keine gültige Serverkennung in der Anfrage enthalten.');
-         }
- 
-         if ($this->hasTooManyLoginAttempts($request)) {
-             $seconds = $this->limiter()->availableIn($this->throttleKey($request));
- 
--            throw new TooManyRequestsHttpException($seconds, "Too many login attempts for this account, please try again in $seconds seconds.");
-+            throw new TooManyRequestsHttpException($seconds, "Zu viele Login-Anfragen. Bitte warte $seconds Sekunden.");
-         }
- 
-         $user = $this->getUser($request, $connection['username']);
-@@ -132,7 +132,7 @@ class SftpAuthenticationController extends Controller
-             $this->incrementLoginAttempts($request);
-         }
- 
--        throw new HttpForbiddenException('Authorization credentials were not correct, please try again.');
-+        throw new HttpForbiddenException('Die Zugangsdaten sind falsch. Bitte schau nochmal rüber.');
-     }
- 
-     /**
-@@ -146,7 +146,7 @@ class SftpAuthenticationController extends Controller
-             if (!in_array(Permission::ACTION_FILE_SFTP, $permissions)) {
-                 Activity::event('server:sftp.denied')->actor($user)->subject($server)->log();
- 
--                throw new HttpForbiddenException('You do not have permission to access SFTP for this server.');
-+                throw new HttpForbiddenException('Du hast nicht die Berechtigung, um den SFTP-Zugang zu verwenden.');
-             }
-         }
- 
-diff --git a/app/Http/Controllers/Auth/LoginCheckpointController.php b/app/Http/Controllers/Auth/LoginCheckpointController.php
-index af05c55..aff275d 100644
---- a/app/Http/Controllers/Auth/LoginCheckpointController.php
-+++ b/app/Http/Controllers/Auth/LoginCheckpointController.php
-@@ -16,7 +16,7 @@ use Illuminate\Contracts\Validation\Factory as ValidationFactory;
- 
- class LoginCheckpointController extends AbstractLoginController
- {
--    private const TOKEN_EXPIRED_MESSAGE = 'The authentication token provided has expired, please refresh the page and try again.';
-+    private const TOKEN_EXPIRED_MESSAGE = 'Der Login-Schlüssel ist abgelaufen, bitte melde dich erneut an.';
- 
-     /**
-      * LoginCheckpointController constructor.
-diff --git a/app/Models/Permission.php b/app/Models/Permission.php
-index b2e0007..dd084b0 100644
---- a/app/Models/Permission.php
-+++ b/app/Models/Permission.php
-@@ -100,110 +100,110 @@ class Permission extends Model
-      */
-     protected static array $permissions = [
-         'websocket' => [
--            'description' => 'Allows the user to connect to the server websocket, giving them access to view console output and realtime server stats.',
-+            'description' => 'Ermöglicht dem Benutzer, sich mit dem Server-Websocket zu verbinden, wodurch er Zugriff auf die Konsolenausgabe und Serverstatistiken in Echtzeit erhält.',
-             'keys' => [
--                'connect' => 'Allows a user to connect to the websocket instance for a server to stream the console.',
-+                'connect' => 'Ermöglicht dem Benutzer, sich mit der Websocket-Instanz für einen Server zu verbinden, um die Konsole zu streamen.',
-             ],
-         ],
- 
-         'control' => [
--            'description' => 'Permissions that control a user\'s ability to control the power state of a server, or send commands.',
-+            'description' => 'Diese Berechtigungen richten sich an die Konsole des Servers. Dazu gehört die Realtime-Konsole und das Starten oder Stoppen des Servers.',
-             'keys' => [
--                'console' => 'Allows a user to send commands to the server instance via the console.',
--                'start' => 'Allows a user to start the server if it is stopped.',
--                'stop' => 'Allows a user to stop a server if it is running.',
--                'restart' => 'Allows a user to perform a server restart. This allows them to start the server if it is offline, but not put the server in a completely stopped state.',
-+                'console' => 'Erlaubt den Zugang zur Konsole, wo Befehle an den Server gesendet werden können.',
-+                'start' => 'Erlaubt den Server zu starten, falls der aus ist.',
-+                'stop' => 'Erlaubt den Server zu stoppen, falls der gerade läuft.',
-+                'restart' => 'Erlaubt den Server neu zu starten.',
-             ],
-         ],
- 
-         'user' => [
--            'description' => 'Permissions that allow a user to manage other subusers on a server. They will never be able to edit their own account, or assign permissions they do not have themselves.',
-+            'description' => 'Diese Berechtigungen richten sich nach den Benutzern, die diesen Server verwalten. Der Benutzer wird nicht in der Lage sein, die eigenen Berechtigungen zu verändern.',
-             'keys' => [
--                'create' => 'Allows a user to create new subusers for the server.',
--                'read' => 'Allows the user to view subusers and their permissions for the server.',
--                'update' => 'Allows a user to modify other subusers.',
--                'delete' => 'Allows a user to delete a subuser from the server.',
-+                'create' => 'Erlaubt es, neue Benutzer in diesen Server zu integrieren',
-+                'read' => 'Erlaubt es, die zugeteilten Berechtigungen anderer zu sehen.',
-+                'update' => 'Erlaubt es die Berechtigungen von anderen Benutzern zu ändern.',
-+                'delete' => 'Erlaubt es, die Benutzer vom Server komplett auszuschließen.',
-             ],
-         ],
- 
-         'file' => [
--            'description' => 'Permissions that control a user\'s ability to modify the filesystem for this server.',
-+            'description' => 'Diese Berechtigungen richten sich nach dem Datei-Manager von diesem Server. ',
-             'keys' => [
--                'create' => 'Allows a user to create additional files and folders via the Panel or direct upload.',
--                'read' => 'Allows a user to view the contents of a directory, but not view the contents of or download files.',
--                'read-content' => 'Allows a user to view the contents of a given file. This will also allow the user to download files.',
--                'update' => 'Allows a user to update the contents of an existing file or directory.',
--                'delete' => 'Allows a user to delete files or directories.',
--                'archive' => 'Allows a user to archive the contents of a directory as well as decompress existing archives on the system.',
--                'sftp' => 'Allows a user to connect to SFTP and manage server files using the other assigned file permissions.',
-+                'create' => 'Erlaubt es, Dateien oder Ordner zu erstellen oder hochzuladen',
-+                'read' => 'Erlaubt es, Dateien oder Ordner zu öffnen.',
-+                'read-content' => 'Erlaubt es, den Inhalt von Dateien oder Ordnern zu lesen',
-+                'update' => 'Erlaubt es, Inhalte in Dateien oder Ordnern zu verändern.',
-+                'delete' => 'Erlaubt es, Inhalte oder Ordner zu löschen.',
-+                'archive' => 'Erlaubt es, Inhalte zu archivieren oder zu entpacken.',
-+                'sftp' => 'Erlaubt es, auf die Inhalte des Servers außerhalb des Panels zugzugreifen. Mithilfe von SFTP, dort werden dieselben Rechte wie hier zugeteilt.',
-             ],
-         ],
- 
-         'backup' => [
--            'description' => 'Permissions that control a user\'s ability to generate and manage server backups.',
-+            'description' => 'Diese Berechtigungen richten sich nach der Verwaltung der Backups des Servers.',
-             'keys' => [
--                'create' => 'Allows a user to create new backups for this server.',
--                'read' => 'Allows a user to view all backups that exist for this server.',
--                'delete' => 'Allows a user to remove backups from the system.',
--                'download' => 'Allows a user to download a backup for the server. Danger: this allows a user to access all files for the server in the backup.',
--                'restore' => 'Allows a user to restore a backup for the server. Danger: this allows the user to delete all of the server files in the process.',
-+                'create' => 'Erlaubt es, ein Backup zu erstellen.',
-+                'read' => 'Erlaubt es, die verfügbaren Backups zu sehen.',
-+                'delete' => 'Erlaubt es, Backups zu löschen.',
-+                'download' => 'Erlaubt es, Backups herunterzuladen. ACHTUNG: Das Backup enthält ALLE INHALTE des Servers!',
-+                'restore' => 'Erlaubt es, den Server mit einem Backup wiederherzustellen. ACHTUNG: Dies kann den Server in einen früheren Zustand versetzen.',
-             ],
-         ],
- 
-         // Controls permissions for editing or viewing a server's allocations.
-         'allocation' => [
--            'description' => 'Permissions that control a user\'s ability to modify the port allocations for this server.',
-+            'description' => 'Diese Berechtigungen richten sich nach den Portfreigaben des Servers',
-             'keys' => [
--                'read' => 'Allows a user to view all allocations currently assigned to this server. Users with any level of access to this server can always view the primary allocation.',
--                'create' => 'Allows a user to assign additional allocations to the server.',
--                'update' => 'Allows a user to change the primary server allocation and attach notes to each allocation.',
--                'delete' => 'Allows a user to delete an allocation from the server.',
-+                'read' => 'Erlaubt es, alle zugewiesenen Ports für diesen Server einzusehen. Der primäre Port ist für alle Benutzer immer sichtbar.',
-+                'create' => 'Erlaubt es, neue Ports für diesen Server zuzuweisen.',
-+                'update' => 'Erlaubt es, den primären Port zu ändern und die Vermerke zu anderen Ports zu bearbeiten.',
-+                'delete' => 'Erlaubt es, zugewiesene Ports vom Server zu entfernen.',
-             ],
-         ],
- 
-         // Controls permissions for editing or viewing a server's startup parameters.
-         'startup' => [
--            'description' => 'Permissions that control a user\'s ability to view this server\'s startup parameters.',
-+            'description' => 'Diese Berechtigungen richten sich nach den Variablen des Servers und allgemeines zum Startbefehl',
-             'keys' => [
--                'read' => 'Allows a user to view the startup variables for a server.',
--                'update' => 'Allows a user to modify the startup variables for the server.',
--                'docker-image' => 'Allows a user to modify the Docker image used when running the server.',
-+                'read' => 'Erlaubt es, den Startbefehl zu sehen.',
-+                'update' => 'Erlaubt es, Variablen des Startbefehls zu ändern.',
-+                'docker-image' => 'Erlaubt es, den Docker-Image des Servers zu ändern.',
-             ],
-         ],
- 
-         'database' => [
--            'description' => 'Permissions that control a user\'s access to the database management for this server.',
-+            'description' => 'Diese Berechtigungen richten sich nach der Verwaltung der Datenbanken.',
-             'keys' => [
--                'create' => 'Allows a user to create a new database for this server.',
--                'read' => 'Allows a user to view the database associated with this server.',
--                'update' => 'Allows a user to rotate the password on a database instance. If the user does not have the view_password permission they will not see the updated password.',
--                'delete' => 'Allows a user to remove a database instance from this server.',
--                'view_password' => 'Allows a user to view the password associated with a database instance for this server.',
-+                'create' => 'Erlaubt es, neue Datenbanken für den Server zu erstellen.',
-+                'read' => 'Erlaubt es, die Übersicht aller Datenbanken des Servers zu sehen.',
-+                'update' => 'Erlaubt es, das Passwort der Datenbank zurückzusetzen. Solange die unterste Berechtigung nicht genehmigt wird, wird der Benutzer das neue Passwort nicht sehen',
-+                'delete' => 'Erlaubt es, Datenbanken für den Server zu löschen.',
-+                'view_password' => 'Erlaubt die Ansicht des aktuellen Passwortes für Datenbanken',
-             ],
-         ],
- 
-         'schedule' => [
--            'description' => 'Permissions that control a user\'s access to the schedule management for this server.',
-+            'description' => 'Diese Berechtigungen richten sich nach den Zeitplänen des Servers.',
-             'keys' => [
--                'create' => 'Allows a user to create new schedules for this server.', // task.create-schedule
--                'read' => 'Allows a user to view schedules and the tasks associated with them for this server.', // task.view-schedule, task.list-schedules
--                'update' => 'Allows a user to update schedules and schedule tasks for this server.', // task.edit-schedule, task.queue-schedule, task.toggle-schedule
--                'delete' => 'Allows a user to delete schedules for this server.', // task.delete-schedule
-+                'create' => 'Erlaubt es, neue Aufgaben zu erstellen.', // task.create-schedule
-+                'read' => 'Erlaubt es, die geplanten Aufgaben zu sehen', // task.view-schedule, task.list-schedules
-+                'update' => 'Erlaubt es, die Zeitpläne zu bearbeiten.', // task.edit-schedule, task.queue-schedule, task.toggle-schedule
-+                'delete' => 'Erlaubt es, Zeitpläne zu entfernen', // task.delete-schedule
-             ],
-         ],
- 
-         'settings' => [
--            'description' => 'Permissions that control a user\'s access to the settings for this server.',
-+            'description' => 'Diese Berechtigungen richten sich nach den allgemeinen Einstellungen des Servers',
-             'keys' => [
--                'rename' => 'Allows a user to rename this server and change the description of it.',
--                'reinstall' => 'Allows a user to trigger a reinstall of this server.',
-+                'rename' => 'Erlaubt es, den Namen oder die Kurzbeschreibung des Servers zu ändern.',
-+                'reinstall' => 'Erlaubt es, den Server neu aufzusetzen und die Software des Servers zu aktualisieren (Datenverlust möglich).',
-             ],
-         ],
- 
-         'activity' => [
--            'description' => 'Permissions that control a user\'s access to the server activity logs.',
-+            'description' => 'Diese Berechtigung richtet sich nach allen Aktivitäten des Servers von allen Benutzern.',
-             'keys' => [
--                'read' => 'Allows a user to view the activity logs for the server.',
-+                'read' => 'Erlaubt es, alle Aktivitäten des Servers zu sehen',
-             ],
-         ],
-     ];
-diff --git a/app/Services/Backups/InitiateBackupService.php b/app/Services/Backups/InitiateBackupService.php
-index be8f966..8419d73 100644
---- a/app/Services/Backups/InitiateBackupService.php
-+++ b/app/Services/Backups/InitiateBackupService.php
-@@ -80,7 +80,7 @@ class InitiateBackupService
-         if ($period > 0) {
-             $previous = $this->repository->getBackupsGeneratedDuringTimespan($server->id, $period);
-             if ($previous->count() >= $limit) {
--                $message = sprintf('Only %d backups may be generated within a %d second span of time.', $limit, $period);
-+                $message = sprintf('Nur %d Backups können innerhalb von %d Sekunden erstellt werden.', $limit, $period);
- 
-                 throw new TooManyRequestsHttpException(CarbonImmutable::now()->diffInSeconds($previous->last()->created_at->addSeconds($period)), $message);
-             }
-@@ -112,7 +112,7 @@ class InitiateBackupService
-             $backup = $this->repository->create([
-                 'server_id' => $server->id,
-                 'uuid' => Uuid::uuid4()->toString(),
--                'name' => trim($name) ?: sprintf('Backup at %s', CarbonImmutable::now()->toDateTimeString()),
-+                'name' => trim($name) ?: sprintf('Backup vom %s', CarbonImmutable::now()->toDateTimeString()),
-                 'ignored_files' => array_values($this->ignoredFiles ?? []),
-                 'disk' => $this->backupManager->getDefaultAdapter(),
-                 'is_locked' => $this->isLocked,
-diff --git a/public/themes/pterodactyl/css/pterodactyl.css b/public/themes/pterodactyl/css/pterodactyl.css
-index 8e3580f..c18f538 100644
---- a/public/themes/pterodactyl/css/pterodactyl.css
-+++ b/public/themes/pterodactyl/css/pterodactyl.css
-@@ -462,7 +462,7 @@ label.control-label > span {
- }
- 
- label.control-label > span.field-required:before {
--    content: "required";
-+    content: "Pflichtfeld";
-     color: #dd4b39;
- }
- 
-diff --git a/public/themes/pterodactyl/js/admin/new-server.js b/public/themes/pterodactyl/js/admin/new-server.js
-index db138cb..3e72f36 100644
---- a/public/themes/pterodactyl/js/admin/new-server.js
-+++ b/public/themes/pterodactyl/js/admin/new-server.js
-@@ -19,27 +19,27 @@
- // SOFTWARE.
- $(document).ready(function() {
-     $('#pNestId').select2({
--        placeholder: 'Select a Nest',
-+        placeholder: 'Wähle ein Nest aus',
-     }).change();
- 
-     $('#pEggId').select2({
--        placeholder: 'Select a Nest Egg',
-+        placeholder: 'Wähle ein Nest Egg aus',
-     });
- 
-     $('#pPackId').select2({
--        placeholder: 'Select a Service Pack',
-+        placeholder: 'Wähle ein Service Pack aus',
-     });
- 
-     $('#pNodeId').select2({
--        placeholder: 'Select a Node',
-+        placeholder: 'Wähle eine Node aus',
-     }).change();
- 
-     $('#pAllocation').select2({
--        placeholder: 'Select a Default Allocation',
-+        placeholder: 'Wähle eine Standardzuordnung aus',
-     });
- 
-     $('#pAllocationAdditional').select2({
--        placeholder: 'Select Additional Allocations',
-+        placeholder: 'Füge weitere Ports hinzu',
-     });
- });
- 
-@@ -59,7 +59,7 @@ $('#pNodeId').on('change', function () {
-         if (v.id == currentNode) {
-             $('#pAllocation').html('').select2({
-                 data: v.allocations,
--                placeholder: 'Select a Default Allocation',
-+                placeholder: 'Wähle eine Standardzuordnung aus',
-             });
- 
-             updateAdditionalAllocations();
-@@ -114,7 +114,7 @@ $('#pEggId').on('change', function (event) {
-     $.each(_.get(objectChain, 'variables', []), function (i, item) {
-         variableIds[item.env_variable] = 'var_ref_' + item.id;
- 
--        let isRequired = (item.required === 1) ? '<span class="label label-danger">Required</span> ' : '';
-+        let isRequired = (item.required === 1) ? '<span class="label label-danger">Pflichtfeld</span> ' : '';
-         let dataAppend = ' \
-             <div class="form-group col-sm-6"> \
-                 <label for="var_ref_' + item.id + '" class="control-label">' + isRequired + item.name + '</label> \
-@@ -154,7 +154,7 @@ function updateAdditionalAllocations() {
- 
-             $('#pAllocationAdditional').html('').select2({
-                 data: allocations,
--                placeholder: 'Select Additional Allocations',
-+                placeholder: 'Füge weitere Ports hinzu',
-             });
-         }
-     });
-diff --git a/public/themes/pterodactyl/js/admin/server/transfer.js b/public/themes/pterodactyl/js/admin/server/transfer.js
-index 5c2664a..2145670 100644
---- a/public/themes/pterodactyl/js/admin/server/transfer.js
-+++ b/public/themes/pterodactyl/js/admin/server/transfer.js
-@@ -1,14 +1,14 @@
- $(document).ready(function () {
-     $('#pNodeId').select2({
--        placeholder: 'Select a Node',
-+        placeholder: 'Node auswählen',
-     }).change();
- 
-     $('#pAllocation').select2({
--        placeholder: 'Select a Default Allocation',
-+        placeholder: 'Wähle eine Standardzuordnung aus',
-     });
- 
-     $('#pAllocationAdditional').select2({
--        placeholder: 'Select Additional Allocations',
-+        placeholder: 'Füge weitere Ports hinzu',
-     });
- });
- 
-@@ -19,7 +19,7 @@ $('#pNodeId').on('change', function () {
-         if (v.id == currentNode) {
-             $('#pAllocation').html('').select2({
-                 data: v.allocations,
--                placeholder: 'Select a Default Allocation',
-+                placeholder: 'Wähle eine Standardzuordnung aus',
-             });
- 
-             updateAdditionalAllocations();
-@@ -49,7 +49,7 @@ function updateAdditionalAllocations() {
- 
-             $('#pAllocationAdditional').html('').select2({
-                 data: allocations,
--                placeholder: 'Select Additional Allocations',
-+                placeholder: 'Füge weitere Ports hinzu',
-             });
-         }
-     });
-diff --git a/resources/lang/en/activity.php b/resources/lang/en/activity.php
-index 501a1dc..54514c2 100644
---- a/resources/lang/en/activity.php
-+++ b/resources/lang/en/activity.php
-@@ -8,123 +8,123 @@
-  */
- return [
-     'auth' => [
--        'fail' => 'Failed log in',
--        'success' => 'Logged in',
--        'password-reset' => 'Password reset',
--        'reset-password' => 'Requested password reset',
--        'checkpoint' => 'Two-factor authentication requested',
--        'recovery-token' => 'Used two-factor recovery token',
--        'token' => 'Solved two-factor challenge',
--        'ip-blocked' => 'Blocked request from unlisted IP address for :identifier',
-+        'fail' => 'Login fehlgeschlagen',
-+        'success' => 'Eingeloggt',
-+        'password-reset' => 'Passwort zurückgesetzt',
-+        'reset-password' => 'Passwort-Anfrage gesendet',
-+        'checkpoint' => '2-FA Anfrage gesendet',
-+        'recovery-token' => 'Hat ein Backup-Code für 2-FA verwendet',
-+        'token' => '2-FA wurde genehmigt',
-+        'ip-blocked' => 'Anfrage von nicht gelisteter IP-Adresse geblockt: :identifier',
-         'sftp' => [
--            'fail' => 'Failed SFTP log in',
-+            'fail' => 'SFTP Login fehlgeschlagen',
-         ],
-     ],
-     'user' => [
-         'account' => [
--            'email-changed' => 'Changed email from :old to :new',
--            'password-changed' => 'Changed password',
-+            'email-changed' => 'E-Mail von :old zu :new geändert',
-+            'password-changed' => 'Passwort geändert',
-         ],
-         'api-key' => [
--            'create' => 'Created new API key :identifier',
--            'delete' => 'Deleted API key :identifier',
-+            'create' => 'Neuen API-Schlüssel :identifier erstellt',
-+            'delete' => 'Hat API-Schlüssel :identifier gelöscht',
-         ],
-         'ssh-key' => [
--            'create' => 'Added SSH key :fingerprint to account',
--            'delete' => 'Removed SSH key :fingerprint from account',
-+            'create' => 'Hat SSH-Schlüssel :fingerprint hinzugefügt',
-+            'delete' => 'Hat SSH-Schlüssel :fingerprint entfernt',
-         ],
-         'two-factor' => [
--            'create' => 'Enabled two-factor auth',
--            'delete' => 'Disabled two-factor auth',
-+            'create' => 'Hat 2-FA aktiviert',
-+            'delete' => 'Hat 2-FA deaktiviert',
-         ],
-     ],
-     'server' => [
--        'reinstall' => 'Reinstalled server',
-+        'reinstall' => 'Hat den Server neu aufgesetzt',
-         'console' => [
--            'command' => 'Executed ":command" on the server',
-+            'command' => 'Führte den Befehl ":command" auf dem Server aus',
-         ],
-         'power' => [
--            'start' => 'Started the server',
--            'stop' => 'Stopped the server',
--            'restart' => 'Restarted the server',
--            'kill' => 'Killed the server process',
-+            'start' => 'Hat den Server gestartet',
-+            'stop' => 'Hat den Server gestoppt',
-+            'restart' => 'Hat den Server neu gestartet',
-+            'kill' => 'Hat den Server gekillt',
-         ],
-         'backup' => [
--            'download' => 'Downloaded the :name backup',
--            'delete' => 'Deleted the :name backup',
--            'restore' => 'Restored the :name backup (deleted files: :truncate)',
--            'restore-complete' => 'Completed restoration of the :name backup',
--            'restore-failed' => 'Failed to complete restoration of the :name backup',
--            'start' => 'Started a new backup :name',
--            'complete' => 'Marked the :name backup as complete',
--            'fail' => 'Marked the :name backup as failed',
--            'lock' => 'Locked the :name backup',
--            'unlock' => 'Unlocked the :name backup',
-+            'download' => 'Hat das Backup :name heruntergeladen',
-+            'delete' => 'Hat das Backup :name gelöscht',
-+            'restore' => 'Stellte das Backup :name wieder her (Gelöschte Dateien: :truncate)',
-+            'restore-complete' => 'Komplette Wiederherstellung des Backups :name',
-+            'restore-failed' => 'Fehlgeschlagene Wiederherstellung des Backups :name',
-+            'start' => 'Erstellt das Backup :name',
-+            'complete' => 'Markiert das Backup :name als erfolgreich',
-+            'fail' => 'Markiert das Backup :name als fehlgeschlagen',
-+            'lock' => 'Hat das Backup :name gesperrt',
-+            'unlock' => 'Hat das Backup :name entsperrt',
-         ],
-         'database' => [
--            'create' => 'Created new database :name',
--            'rotate-password' => 'Password rotated for database :name',
--            'delete' => 'Deleted database :name',
-+            'create' => 'Hat die Datenbank :name erstellt',
-+            'rotate-password' => 'Neues Passwort für die Datenbank :name erstellt',
-+            'delete' => 'Hat die Datenbank :name gelöscht',
-         ],
-         'file' => [
--            'compress_one' => 'Compressed :directory:file',
--            'compress_other' => 'Compressed :count files in :directory',
--            'read' => 'Viewed the contents of :file',
--            'copy' => 'Created a copy of :file',
--            'create-directory' => 'Created directory :directory:name',
--            'decompress' => 'Decompressed :files in :directory',
--            'delete_one' => 'Deleted :directory:files.0',
--            'delete_other' => 'Deleted :count files in :directory',
--            'download' => 'Downloaded :file',
--            'pull' => 'Downloaded a remote file from :url to :directory',
--            'rename_one' => 'Renamed :directory:files.0.from to :directory:files.0.to',
--            'rename_other' => 'Renamed :count files in :directory',
--            'write' => 'Wrote new content to :file',
--            'upload' => 'Began a file upload',
--            'uploaded' => 'Uploaded :directory:file',
-+            'compress_one' => 'Hat :directory:file komprimiert',
-+            'compress_other' => 'Hat :count Dateien in :directory komprimiert',
-+            'read' => 'Hat sich :file angeschaut',
-+            'copy' => 'Hat eine Kopie von :file erstellt',
-+            'create-directory' => 'Hat den Ordner :directory:name erstellt',
-+            'decompress' => 'Hat :files Dateien in :directory entpackt',
-+            'delete_one' => 'Hat die Datei :directory:files.0 gelöscht',
-+            'delete_other' => 'Hat :count Dateien in :directory gelöscht',
-+            'download' => 'Hat :file heruntergeladen',
-+            'pull' => 'Hat die Remote-Datei :url in :directory abgelegt',
-+            'rename_one' => 'Hat den Namen :directory:files.0.from in :directory:files.0.to geändert',
-+            'rename_other' => 'Hat :count Dateien in :directory geändert',
-+            'write' => 'Hat etwas in :file verändert',
-+            'upload' => 'Hat einen Upload gestartet',
-+            'uploaded' => 'Hat :directory:file hochgeladen',
-         ],
-         'sftp' => [
--            'denied' => 'Blocked SFTP access due to permissions',
--            'create_one' => 'Created :files.0',
--            'create_other' => 'Created :count new files',
--            'write_one' => 'Modified the contents of :files.0',
--            'write_other' => 'Modified the contents of :count files',
--            'delete_one' => 'Deleted :files.0',
--            'delete_other' => 'Deleted :count files',
--            'create-directory_one' => 'Created the :files.0 directory',
--            'create-directory_other' => 'Created :count directories',
--            'rename_one' => 'Renamed :files.0.from to :files.0.to',
--            'rename_other' => 'Renamed or moved :count files',
-+            'denied' => 'Hat den SFTP-Zugang aufgrund ungenügender Rechte verweigert',
-+            'create_one' => 'Hat die Datei :files.0 erstellt',
-+            'create_other' => 'Hat :count neue Dateien erstellt',
-+            'write_one' => 'Hat etwas in :files.0 verändert',
-+            'write_other' => 'Hat etwas in :count Dateien verändert',
-+            'delete_one' => 'Hat :files.0 gelöscht',
-+            'delete_other' => 'Hat :count Dateien gelöscht',
-+            'create-directory_one' => 'Hat den Ordner :files.0 erstellt',
-+            'create-directory_other' => 'Hat :count Ordner erstellt',
-+            'rename_one' => 'Hat die Datei :files.0.from in :files.0.to unbenannt',
-+            'rename_other' => 'Hat :count Dateien verschoben oder umbenannt',
-         ],
-         'allocation' => [
--            'create' => 'Added :allocation to the server',
--            'notes' => 'Updated the notes for :allocation from ":old" to ":new"',
--            'primary' => 'Set :allocation as the primary server allocation',
--            'delete' => 'Deleted the :allocation allocation',
-+            'create' => 'Hat den Port :allocation zu diesem Server zugeteilt',
-+            'notes' => 'Hat den Vermerk vom Port :allocation von ":old" zu ":new" geändert',
-+            'primary' => 'Hat den Port :allocation als primären Server-Zugang festgelegt',
-+            'delete' => 'Hat den Port :allocation entfernt',
-         ],
-         'schedule' => [
--            'create' => 'Created the :name schedule',
--            'update' => 'Updated the :name schedule',
--            'execute' => 'Manually executed the :name schedule',
--            'delete' => 'Deleted the :name schedule',
-+            'create' => 'Hat den Zeitplan :name erstellt',
-+            'update' => 'Hat den Zeitplan :name verändert',
-+            'execute' => 'Hat den Zeitplan :name manuell gestartet',
-+            'delete' => 'Hat den Zeitplan :name gelöscht',
-         ],
-         'task' => [
--            'create' => 'Created a new ":action" task for the :name schedule',
--            'update' => 'Updated the ":action" task for the :name schedule',
--            'delete' => 'Deleted a task for the :name schedule',
-+            'create' => 'Hat die Aufgabe ":action" in :name erstellt',
-+            'update' => 'Hat die Aufgabe ":action" in :name verändert',
-+            'delete' => 'Hat eine Aufgabe in im Zeitplan :name gelöscht',
-         ],
-         'settings' => [
--            'rename' => 'Renamed the server from :old to :new',
--            'description' => 'Changed the server description from :old to :new',
-+            'rename' => 'Hat den Server von :old zu :new umbenannt',
-+            'description' => 'Hat die Beschreibung des Servers von :old zu :new unbenannt',
-         ],
-         'startup' => [
--            'edit' => 'Changed the :variable variable from ":old" to ":new"',
--            'image' => 'Updated the Docker Image for the server from :old to :new',
-+            'edit' => 'Hat die Variable :variable von ":old" zu ":new" verändert',
-+            'image' => 'Hat das Docker Image von :old zu :new aktualisiert',
-         ],
-         'subuser' => [
--            'create' => 'Added :email as a subuser',
--            'update' => 'Updated the subuser permissions for :email',
--            'delete' => 'Removed :email as a subuser',
-+            'create' => 'Hat :email als Benutzer hinzugefügt',
-+            'update' => 'Hat die Berechtigungen von :email verändert',
-+            'delete' => 'Hat :email vom Server entfernt',
-         ],
-     ],
- ];
-diff --git a/resources/lang/en/admin/nests.php b/resources/lang/en/admin/nests.php
-index 2a6f377..88d4e29 100644
---- a/resources/lang/en/admin/nests.php
-+++ b/resources/lang/en/admin/nests.php
-@@ -2,25 +2,25 @@
- 
- return [
-     'notices' => [
--        'created' => 'A new nest, :name, has been successfully created.',
--        'deleted' => 'Successfully deleted the requested nest from the Panel.',
--        'updated' => 'Successfully updated the nest configuration options.',
-+        'created' => 'Das neue Nest :name wurde erfolgreich erstellt.',
-+        'deleted' => 'Das Nest wurde erfolgreich vom Panel entfernt.',
-+        'updated' => 'Die Konfigurationsoptionen des Nests wurden erfolgreich aktualisiert.',
-     ],
-     'eggs' => [
-         'notices' => [
--            'imported' => 'Successfully imported this Egg and its associated variables.',
--            'updated_via_import' => 'This Egg has been updated using the file provided.',
--            'deleted' => 'Successfully deleted the requested egg from the Panel.',
--            'updated' => 'Egg configuration has been updated successfully.',
--            'script_updated' => 'Egg install script has been updated and will run whenever servers are installed.',
--            'egg_created' => 'A new egg was laid successfully. You will need to restart any running daemons to apply this new egg.',
-+            'imported' => 'Dieses Egg wurde erfolgreich wurde erfolgreich mit seinen zugehörigen Variablen importiert.',
-+            'updated_via_import' => 'Dieses Egg wurde erfolgreich mit der angegebenen Datei aktualisiert.',
-+            'deleted' => 'Das angefragte Egg wurde Erfolgreich vom Panel entfernt.',
-+            'updated' => 'Die Egg-Konfiguration wurde erfolgreich aktualisiert.',
-+            'script_updated' => 'Das Egg-Installationsskript wurde aktualisiert und wird nun bei jeder Serverinstallation ausgeführt.',
-+            'egg_created' => 'Das Egg wurde erfolgreich erstellt. Du musst alle laufenden Daemons neu starten, um dieses Egg anzuwenden.',
-         ],
-     ],
-     'variables' => [
-         'notices' => [
--            'variable_deleted' => 'The variable ":variable" has been deleted and will no longer be available to servers once rebuilt.',
--            'variable_updated' => 'The variable ":variable" has been updated. You will need to rebuild any servers using this variable in order to apply changes.',
--            'variable_created' => 'New variable has successfully been created and assigned to this egg.',
-+            'variable_deleted' => 'Die Variable ":variable" wurde gelöscht und wird nach Rebuild nicht länger für Server verfügbar sein.',
-+            'variable_updated' => 'Die Variable ":variable" wurde updated. Du musst alle Server, die diese Variable verwenden, neu erstellen, um die Änderungen zu übernehmen.',
-+            'variable_created' => 'Die neue Variable wurde erfolgreich erstellt und diesem Egg zugeteilt.',
-         ],
-     ],
- ];
-diff --git a/resources/lang/en/admin/node.php b/resources/lang/en/admin/node.php
-index cffd4d7..fdd1cb9 100644
---- a/resources/lang/en/admin/node.php
-+++ b/resources/lang/en/admin/node.php
-@@ -2,15 +2,15 @@
- 
- return [
-     'validation' => [
--        'fqdn_not_resolvable' => 'The FQDN or IP address provided does not resolve to a valid IP address.',
--        'fqdn_required_for_ssl' => 'A fully qualified domain name that resolves to a public IP address is required in order to use SSL for this node.',
-+        'fqdn_not_resolvable' => 'Der bereitgestellte FQDN oder die angegebene IP-Adresse wird nicht in eine gültige IP-Adresse aufgelöst.',
-+        'fqdn_required_for_ssl' => 'Ein vollständig qualifizierter Domänenname, der in eine öffentliche IP-Adresse aufgelöst wird, ist erforderlich, um SSL für diesen Node zu verwenden.',
-     ],
-     'notices' => [
--        'allocations_added' => 'Allocations have successfully been added to this node.',
--        'node_deleted' => 'Node has been successfully removed from the panel.',
--        'location_required' => 'You must have at least one location configured before you can add a node to this panel.',
--        'node_created' => 'Successfully created new node. You can automatically configure the daemon on this machine by visiting the \'Configuration\' tab. <strong>Before you can add any servers you must first allocate at least one IP address and port.</strong>',
--        'node_updated' => 'Node information has been updated. If any daemon settings were changed you will need to reboot it for those changes to take effect.',
--        'unallocated_deleted' => 'Deleted all un-allocated ports for <code>:ip</code>.',
-+        'allocations_added' => 'Die Ports wurden erfolgreich freigegeben.',
-+        'node_deleted' => 'Node wurde erfolgreich entfernt.',
-+        'location_required' => 'Du musst zuerst ein Standort hinzufügen, bevor du ein Node erstellen kannst.',
-+        'node_created' => 'Neuer Node erfolgreich erstellt. Du kannst den Daemon auf diesem Computer automatisch konfigurieren, indem du die Registerkarte \'Wings-Integration\' öffnest. <strong>Bevor du Server hinzufügen kannst, musst du zuerst mindestens eine IP-Adresse und einen Port zuweisen.</strong>',
-+        'node_updated' => 'Die Informationen zum Node wurden gespeichert. Möglicherweise ist ein Neustart notwendig.',
-+        'unallocated_deleted' => 'Alle nicht zugewiesene Ports für <code>:ip</code> wurden entfernt.',
-     ],
- ];
-diff --git a/resources/lang/en/admin/server.php b/resources/lang/en/admin/server.php
-index 29cb72d..6e1bd2f 100644
---- a/resources/lang/en/admin/server.php
-+++ b/resources/lang/en/admin/server.php
-@@ -2,26 +2,26 @@
- 
- return [
-     'exceptions' => [
--        'no_new_default_allocation' => 'You are attempting to delete the default allocation for this server but there is no fallback allocation to use.',
--        'marked_as_failed' => 'This server was marked as having failed a previous installation. Current status cannot be toggled in this state.',
--        'bad_variable' => 'There was a validation error with the :name variable.',
--        'daemon_exception' => 'There was an exception while attempting to communicate with the daemon resulting in a HTTP/:code response code. This exception has been logged. (request id: :request_id)',
--        'default_allocation_not_found' => 'The requested default allocation was not found in this server\'s allocations.',
-+        'no_new_default_allocation' => 'Du versuchst, die Standardzuweisung für diesen Server zu löschen, aber es gibt keine Ersatzzuweisung, die du verwenden kannst.',
-+        'marked_as_failed' => 'Dieser Server wurde bei einer früheren Installation als fehlgeschlagen markiert. Der aktuelle Status kann in diesem Zustand nicht umgeschaltet werden.',
-+        'bad_variable' => 'Es gab einen Validierungsfehler mit der Variable :name.',
-+        'daemon_exception' => 'Beim Versuch, mit dem Daemon zu kommunizieren, ist eine Ausnahme aufgetreten, die zu einem HTTP/:code Antwortcode führte. Diese Ausnahme wurde protokolliert (Anfrage-ID: :request_id).',
-+        'default_allocation_not_found' => 'Die angeforderte Standardzuweisung wurde in den Zuweisungen dieses Servers nicht gefunden.',
-     ],
-     'alerts' => [
--        'startup_changed' => 'The startup configuration for this server has been updated. If this server\'s nest or egg was changed a reinstall will be occurring now.',
--        'server_deleted' => 'Server has successfully been deleted from the system.',
--        'server_created' => 'Server was successfully created on the panel. Please allow the daemon a few minutes to completely install this server.',
--        'build_updated' => 'The build details for this server have been updated. Some changes may require a restart to take effect.',
--        'suspension_toggled' => 'Server suspension status has been changed to :status.',
--        'rebuild_on_boot' => 'This server has been marked as requiring a Docker Container rebuild. This will happen the next time the server is started.',
--        'install_toggled' => 'The installation status for this server has been toggled.',
--        'server_reinstalled' => 'This server has been queued for a reinstallation beginning now.',
--        'details_updated' => 'Server details have been successfully updated.',
--        'docker_image_updated' => 'Successfully changed the default Docker image to use for this server. A reboot is required to apply this change.',
--        'node_required' => 'You must have at least one node configured before you can add a server to this panel.',
--        'transfer_nodes_required' => 'You must have at least two nodes configured before you can transfer servers.',
--        'transfer_started' => 'Server transfer has been started.',
--        'transfer_not_viable' => 'The node you selected does not have the required disk space or memory available to accommodate this server.',
-+        'startup_changed' => 'Die Startkonfiguration für diesen Server wurde aktualisiert. Wenn das Nest oder Egg dieses Servers geändert wurde, wird nun eine Neuinstallation durchgeführt.',
-+        'server_deleted' => 'Der Server wurde erfolgreich aus dem System gelöscht.',
-+        'server_created' => 'Der Server wurde erfolgreich im Panel erstellt. Bitte gib dem Daemon ein paar Minuten Zeit, um diesen Server vollständig zu installieren.',
-+        'build_updated' => 'Die Build-Details für diesen Server wurden aktualisiert. Einige Änderungen erfordern möglicherweise einen Neustart, um wirksam zu werden.',
-+        'suspension_toggled' => 'Der Status der Serversperre wurde in :status geändert.',
-+        'rebuild_on_boot' => 'Dieser Server wurde so markiert, dass ein Docker-Container-Rebuild erforderlich ist. Dies geschieht, wenn der Server das nächste Mal gestartet wird.',
-+        'install_toggled' => 'Der Installationsstatus für diesen Server wurde umgeschaltet.',
-+        'server_reinstalled' => 'Dieser Server steht ab sofort für eine Neuinstallation in der Warteschlange.',
-+        'details_updated' => 'Die Serverdetails wurden erfolgreich aktualisiert.',
-+        'docker_image_updated' => 'Das für diesen Server zu verwendende Standard-Docker-Image wurde erfolgreich geändert. Ein Neustart ist erforderlich, um diese Änderung anzuwenden.',
-+        'node_required' => 'Du musst mindestens eine Node konfiguriert haben, bevor du einen Server zu diesem Panel hinzufügen kannst.',
-+        'transfer_nodes_required' => 'Du musst mindestens 2 Nodes konfiguriert haben bevor du Server transferieren kannst.',
-+        'transfer_started' => 'Servertransfer wurde gestartet',
-+        'transfer_not_viable' => 'Die von dir ausgewählte Node verfügt nicht über den erforderlichen Festplatten- oder Arbeitsspeicher, um diesen Server aufzunehmen.',
-     ],
- ];
-diff --git a/resources/lang/en/admin/user.php b/resources/lang/en/admin/user.php
-index 65e2278..0fa646d 100644
---- a/resources/lang/en/admin/user.php
-+++ b/resources/lang/en/admin/user.php
-@@ -2,10 +2,10 @@
- 
- return [
-     'exceptions' => [
--        'user_has_servers' => 'Cannot delete a user with active servers attached to their account. Please delete their servers before continuing.',
-+        'user_has_servers' => 'Ein Benutzer mit aktiven Servern in seinem Konto kann nicht gelöscht werden. Bitte lösche seine Server, bevor du fortfährst.',
-     ],
-     'notices' => [
--        'account_created' => 'Account has been created successfully.',
--        'account_updated' => 'Account has been successfully updated.',
-+        'account_created' => 'Das Konto wurde erfolgreich angelegt. ',
-+        'account_updated' => 'Das Konto wurde erfolgreich aktualisiert.',
-     ],
- ];
-diff --git a/resources/lang/en/auth.php b/resources/lang/en/auth.php
-index 2a3a452..6510364 100644
---- a/resources/lang/en/auth.php
-+++ b/resources/lang/en/auth.php
-@@ -3,7 +3,7 @@
- return [
-     'sign_in' => 'Sign In',
-     'go_to_login' => 'Go to Login',
--    'failed' => 'No account matching those credentials could be found.',
-+    'failed' => 'Dieser Account existiert nicht. Bitte überprüfe deine Zugangsdaten.',
- 
-     'forgot_password' => [
-         'label' => 'Forgot Password?',
-diff --git a/resources/lang/en/command/messages.php b/resources/lang/en/command/messages.php
-index a4a3aaf..bfe0073 100644
---- a/resources/lang/en/command/messages.php
-+++ b/resources/lang/en/command/messages.php
-@@ -2,65 +2,65 @@
- 
- return [
-     'location' => [
--        'no_location_found' => 'Could not locate a record matching the provided short code.',
--        'ask_short' => 'Location Short Code',
--        'ask_long' => 'Location Description',
--        'created' => 'Successfully created a new location (:name) with an ID of :id.',
--        'deleted' => 'Successfully deleted the requested location.',
-+        'no_location_found' => 'Es konnte kein Datensatz gefunden werden, der mit dem angegebenen Kurzcode übereinstimmt.',
-+        'ask_short' => 'Standort Kurzcode',
-+        'ask_long' => 'Standortbeschreibung',
-+        'created' => 'Erfolgreich den neuen Ort :name mit der ID :id erstellt.',
-+        'deleted' => 'Der angeforderte Speicherort wurde erfolgreich gelöscht.',
-     ],
-     'user' => [
--        'search_users' => 'Enter a Username, User ID, or Email Address',
--        'select_search_user' => 'ID of user to delete (Enter \'0\' to re-search)',
--        'deleted' => 'User successfully deleted from the Panel.',
--        'confirm_delete' => 'Are you sure you want to delete this user from the Panel?',
--        'no_users_found' => 'No users were found for the search term provided.',
--        'multiple_found' => 'Multiple accounts were found for the user provided, unable to delete a user because of the --no-interaction flag.',
--        'ask_admin' => 'Is this user an administrator?',
--        'ask_email' => 'Email Address',
--        'ask_username' => 'Username',
--        'ask_name_first' => 'First Name',
--        'ask_name_last' => 'Last Name',
--        'ask_password' => 'Password',
--        'ask_password_tip' => 'If you would like to create an account with a random password emailed to the user, re-run this command (CTRL+C) and pass the `--no-password` flag.',
--        'ask_password_help' => 'Passwords must be at least 8 characters in length and contain at least one capital letter and number.',
-+        'search_users' => 'Gib einen Benutzernamen, eine Benutzer-ID oder eine E-Mail-Adresse ein.',
-+        'select_search_user' => 'ID des zu löschenden Benutzers (Gib \'0\' ein, um erneut zu suchen)',
-+        'deleted' => 'Benutzer erfolgreich vom Panel entfernt.',
-+        'confirm_delete' => 'Bist du dir sicher, dass du diesen Nutzer vom Panel entfernen möchtest?',
-+        'no_users_found' => 'Mit den eingegebenen Suchbegriff wurden keine Benutzer gefunden.',
-+        'multiple_found' => 'Es wurden mehrere Konten für den angegebenen Benutzer gefunden, der aufgrund des Flags --no-interaction nicht gelöscht werden kann.',
-+        'ask_admin' => 'Ist dieser Nutzer ein Administrator?',
-+        'ask_email' => 'Email-Adresse',
-+        'ask_username' => 'Benutzername',
-+        'ask_name_first' => 'Vorname',
-+        'ask_name_last' => 'Nachname',
-+        'ask_password' => 'Passwort',
-+        'ask_password_tip' => 'Wenn du ein Konto mit einem zufälligen Kennwort erstellen möchtest, das dem Benutzer per E-Mail zugesandt wird, führe diesen Befehl erneut aus (STRG+C) und übergeben das Flag "no-password".',
-+        'ask_password_help' => 'Die Passwörter müssen mindestens 8 Zeichen lang sein und mindestens einen Großbuchstaben und eine Zahl enthalten.',
-         '2fa_help_text' => [
--            'This command will disable 2-factor authentication for a user\'s account if it is enabled. This should only be used as an account recovery command if the user is locked out of their account.',
--            'If this is not what you wanted to do, press CTRL+C to exit this process.',
-+            'Mit diesem Befehl wird die 2-Faktor-Authentifizierung für das Konto eines Benutzers deaktiviert, wenn sie aktiviert ist. Er sollte nur als Befehl zur Wiederherstellung des Kontos verwendet werden, wenn der Benutzer aus seinem Konto ausgesperrt wurde.',
-+            'Wenn du das nicht willst, drückst du STRG+C, um diesen Vorgang zu beenden.',
-         ],
--        '2fa_disabled' => '2-Factor authentication has been disabled for :email.',
-+        '2fa_disabled' => '2-Faktor Authentifizierung wurde für :email. deaktiviert.',
-     ],
-     'schedule' => [
--        'output_line' => 'Dispatching job for first task in `:schedule` (:hash).',
-+        'output_line' => 'Versenden des Auftrags für die erste Aufgabe in `:schedule` (:hash).',
-     ],
-     'maintenance' => [
--        'deleting_service_backup' => 'Deleting service backup file :file.',
-+        'deleting_service_backup' => 'Löschen der Service-Backup-Datei :file.',
-     ],
-     'server' => [
--        'rebuild_failed' => 'Rebuild request for ":name" (#:id) on node ":node" failed with error: :message',
-+        'rebuild_failed' => 'Rebuild-Anfrage für ":name" (#:id) auf Node ":node" fehlgeschlagen mit Fehler: :message',
-         'reinstall' => [
--            'failed' => 'Reinstall request for ":name" (#:id) on node ":node" failed with error: :message',
--            'confirm' => 'You are about to reinstall against a group of servers. Do you wish to continue?',
-+            'failed' => 'Neuinstallationsanforderung für ":name" (#:id) auf Knoten ":node" fehlgeschlagen mit Fehler: :message',
-+            'confirm' => 'Du bist dabei, eine Neuinstallation für eine Gruppe von Servern durchzuführen. Willst du fortfahren?',
-         ],
-         'power' => [
--            'confirm' => 'You are about to perform a :action against :count servers. Do you wish to continue?',
--            'action_failed' => 'Power action request for ":name" (#:id) on node ":node" failed with error: :message',
-+            'confirm' => 'Du bist dabei, eine :Aktion gegen :count-Server durchzuführen. Willst du fortfahren?',
-+            'action_failed' => 'Power Action-Anfrage für ":name" (#:id) auf Knoten ":node" fehlgeschlagen mit Fehler: :message',
-         ],
-     ],
-     'environment' => [
-         'mail' => [
--            'ask_smtp_host' => 'SMTP Host (e.g. smtp.gmail.com)',
-+            'ask_smtp_host' => 'SMTP Host (z.B. smtp.gmail.com)',
-             'ask_smtp_port' => 'SMTP Port',
--            'ask_smtp_username' => 'SMTP Username',
--            'ask_smtp_password' => 'SMTP Password',
-+            'ask_smtp_username' => 'SMTP Nutzername',
-+            'ask_smtp_password' => 'SMTP Passwort',
-             'ask_mailgun_domain' => 'Mailgun Domain',
--            'ask_mailgun_endpoint' => 'Mailgun Endpoint',
--            'ask_mailgun_secret' => 'Mailgun Secret',
--            'ask_mandrill_secret' => 'Mandrill Secret',
--            'ask_postmark_username' => 'Postmark API Key',
--            'ask_driver' => 'Which driver should be used for sending emails?',
--            'ask_mail_from' => 'Email address emails should originate from',
--            'ask_mail_name' => 'Name that emails should appear from',
--            'ask_encryption' => 'Encryption method to use',
-+            'ask_mailgun_endpoint' => 'Mailgun Endpunkt',
-+            'ask_mailgun_secret' => 'Mailgun Geheimschlüssel',
-+            'ask_mandrill_secret' => 'Mandrill Geheimschlüssel',
-+            'ask_postmark_username' => 'Postmark API-Schlüssel',
-+            'ask_driver' => 'Welcher Treiber sollte für den Versand von E-Mails verwendet werden?',
-+            'ask_mail_from' => 'E-Mail-Adresse, von der die E-Mails stammen sollten',
-+            'ask_mail_name' => 'Name, unter dem die E-Mails erscheinen sollen',
-+            'ask_encryption' => 'Zu verwendende Verschlüsselungsmethode',
-         ],
-     ],
- ];
-diff --git a/resources/lang/en/exceptions.php b/resources/lang/en/exceptions.php
-index 4d44c4f..0919d9f 100644
---- a/resources/lang/en/exceptions.php
-+++ b/resources/lang/en/exceptions.php
-@@ -1,59 +1,59 @@
- <?php
- 
- return [
--    'daemon_connection_failed' => 'There was an exception while attempting to communicate with the daemon resulting in a HTTP/:code response code. This exception has been logged.',
-+    'daemon_connection_failed' => 'Beim Versuch, mit dem Daemon zu kommunizieren, ist eine Ausnahme aufgetreten, die zu einem HTTP/:code-Antwortcode führte. Diese Ausnahme wurde protokolliert.',
-     'node' => [
--        'servers_attached' => 'A node must have no servers linked to it in order to be deleted.',
--        'daemon_off_config_updated' => 'The daemon configuration <strong>has been updated</strong>, however there was an error encountered while attempting to automatically update the configuration file on the Daemon. You will need to manually update the configuration file (config.yml) for the daemon to apply these changes.',
-+        'servers_attached' => 'Dieser Node darf keine aktiven Server beinhalten, die darüber laufen.',
-+        'daemon_off_config_updated' => 'Die Daemon-Config <strong>wurde aktualisiert</strong>, aber stehen noch nicht bereit! Du musst die Config.yml auf den neusten bringen und Wings neu starten.',
-     ],
-     'allocations' => [
--        'server_using' => 'A server is currently assigned to this allocation. An allocation can only be deleted if no server is currently assigned.',
--        'too_many_ports' => 'Adding more than 1000 ports in a single range at once is not supported.',
--        'invalid_mapping' => 'The mapping provided for :port was invalid and could not be processed.',
--        'cidr_out_of_range' => 'CIDR notation only allows masks between /25 and /32.',
--        'port_out_of_range' => 'Ports in an allocation must be greater than 1024 and less than or equal to 65535.',
-+        'server_using' => 'Der Port wurde bereits einem Server zugewiesen. Entferne erst den zugewiesenen Port vom jeweiligen Server.',
-+        'too_many_ports' => 'Ein Server kann nicht mehr als 1000 Ports zugeteilt bekommen (Wofür braucht man so viele?).',
-+        'invalid_mapping' => 'Die Kartierung für :port war ungültig und kann nicht übernommen werden.',
-+        'cidr_out_of_range' => 'Die CIDR-Notation erlaubt nur Masken zwischen /25 und /32.',
-+        'port_out_of_range' => 'Die freigegebenen Ports müssen größer als 1024 und kleiner als 65535 sein.',
-     ],
-     'nest' => [
--        'delete_has_servers' => 'A Nest with active servers attached to it cannot be deleted from the Panel.',
-+        'delete_has_servers' => 'Ein Nest mit aktiven Servern kann nicht entfernt werden.',
-         'egg' => [
--            'delete_has_servers' => 'An Egg with active servers attached to it cannot be deleted from the Panel.',
--            'invalid_copy_id' => 'The Egg selected for copying a script from either does not exist, or is copying a script itself.',
--            'must_be_child' => 'The "Copy Settings From" directive for this Egg must be a child option for the selected Nest.',
--            'has_children' => 'This Egg is a parent to one or more other Eggs. Please delete those Eggs before deleting this Egg.',
-+            'delete_has_servers' => 'Ein Nest mit aktiven Servern kann nicht entfernt werden',
-+            'invalid_copy_id' => 'Das zum Kopieren eines Skripts ausgewählte Ei existiert entweder nicht oder kopiert selbst ein Skript.',
-+            'must_be_child' => 'TDie Anweisung "Einstellungen kopieren von" für dieses Egg muss eine untergeordnete Option für das ausgewählte Nest sein.',
-+            'has_children' => 'Dieses Egg ist mit anderen Eggs verbunden. Entferne erst die anderen Eggs, die dazu gehören.',
-         ],
-         'variables' => [
--            'env_not_unique' => 'The environment variable :name must be unique to this Egg.',
--            'reserved_name' => 'The environment variable :name is protected and cannot be assigned to a variable.',
--            'bad_validation_rule' => 'The validation rule ":rule" is not a valid rule for this application.',
-+            'env_not_unique' => 'Die Umgebungsvariable :name muss für dieses Ei eindeutig sein.',
-+            'reserved_name' => 'Die Umgebungsvariable :name ist geschützt und kann keiner Variablen zugewiesen werden.',
-+            'bad_validation_rule' => 'Die Validierungsregel ":rule" ist keine gültige Regel für diese Anwendung.',
-         ],
-         'importer' => [
--            'json_error' => 'There was an error while attempting to parse the JSON file: :error.',
--            'file_error' => 'The JSON file provided was not valid.',
--            'invalid_json_provided' => 'The JSON file provided is not in a format that can be recognized.',
-+            'json_error' => 'Beim Analysieren der JSON-Datei ist ein Fehler aufgetreten: :error.',
-+            'file_error' => 'Die bereitgestellte JSON-Datei war ungültig.',
-+            'invalid_json_provided' => 'Die bereitgestellte JSON-Datei hat kein erkennbares Format.',
-         ],
-     ],
-     'subusers' => [
--        'editing_self' => 'Editing your own subuser account is not permitted.',
--        'user_is_owner' => 'You cannot add the server owner as a subuser for this server.',
--        'subuser_exists' => 'A user with that email address is already assigned as a subuser for this server.',
-+        'editing_self' => 'Du kannst deine eigenen Berechtigungen nicht verändern.',
-+        'user_is_owner' => 'Der Benutzer ist bereits der Admin des Servers',
-+        'subuser_exists' => 'Dieser Benutzer ist bereits ein Teil des Servers.',
-     ],
-     'databases' => [
--        'delete_has_databases' => 'Cannot delete a database host server that has active databases linked to it.',
-+        'delete_has_databases' => 'Ein Datenbankhostserver, mit dem aktive Datenbanken verknüpft sind, kann nicht gelöscht werden.',
-     ],
-     'tasks' => [
--        'chain_interval_too_long' => 'The maximum interval time for a chained task is 15 minutes.',
-+        'chain_interval_too_long' => 'Die maximale Intervallzeit für eine verkettete Aufgabe beträgt 15 Minuten.',
-     ],
-     'locations' => [
--        'has_nodes' => 'Cannot delete a location that has active nodes attached to it.',
-+        'has_nodes' => 'Der Standort kann bei einem aktiven zugeteilten Node nicht entfernt werden.',
-     ],
-     'users' => [
--        'node_revocation_failed' => 'Failed to revoke keys on <a href=":link">Node #:node</a>. :error',
-+        'node_revocation_failed' => 'Schlüssel konnten nicht widerrufen werden in <a href=":link">Node #:node</a>. :error',
-     ],
-     'deployment' => [
--        'no_viable_nodes' => 'No nodes satisfying the requirements specified for automatic deployment could be found.',
--        'no_viable_allocations' => 'No allocations satisfying the requirements for automatic deployment were found.',
-+        'no_viable_nodes' => 'Es konnten keine Nodes gefunden werden, die die für die automatische Bereitstellung angegebenen Anforderungen erfüllen.',
-+        'no_viable_allocations' => 'Es wurden keine Portfreigaben gefunden, die die Anforderungen für die automatische Bereitstellung erfüllen.',
-     ],
-     'api' => [
--        'resource_not_found' => 'The requested resource does not exist on this server.',
-+        'resource_not_found' => 'Die angeforderte Ressource ist auf diesem Server nicht vorhanden.',
-     ],
- ];
-diff --git a/resources/lang/en/pagination.php b/resources/lang/en/pagination.php
-index ecac3aa..25253b1 100644
---- a/resources/lang/en/pagination.php
-+++ b/resources/lang/en/pagination.php
-@@ -12,6 +12,6 @@ return [
-     |
-     */
- 
--    'previous' => '&laquo; Previous',
--    'next' => 'Next &raquo;',
-+    'previous' => '&laquo; Zurück',
-+    'next' => 'Weiter &raquo;',
- ];
-diff --git a/resources/lang/en/passwords.php b/resources/lang/en/passwords.php
-index 76a6959..a743f3e 100644
---- a/resources/lang/en/passwords.php
-+++ b/resources/lang/en/passwords.php
-@@ -11,9 +11,9 @@ return [
-     | has failed, such as for an invalid token or invalid new password.
-     |
-     */
--    'password' => 'Passwords must be at least six characters and match the confirmation.',
--    'reset' => 'Your password has been reset!',
--    'sent' => 'We have e-mailed your password reset link!',
--    'token' => 'This password reset token is invalid.',
--    'user' => "We can't find a user with that e-mail address.",
-+    'password' => 'Passwörter müssen mindestens sechs Zeichen lang sein und der Bestätigung entsprechen.',
-+    'reset' => 'Dein Passwort wurde zurückgesetzt!',
-+    'sent' => 'Wir haben dir einen Link zum Zurücksetzen deines Passworts per E-Mail geschickt!',
-+    'token' => 'Der Token zum Zurücksetzen des Passworts ist ungültig.',
-+    'user' => "Wir können keinen Benutzer mit dieser E-Mail-Adresse finden.",
- ];
-diff --git a/resources/lang/en/strings.php b/resources/lang/en/strings.php
-index ddade2c..98532bd 100644
---- a/resources/lang/en/strings.php
-+++ b/resources/lang/en/strings.php
-@@ -1,95 +1,95 @@
- <?php
- 
- return [
--    'email' => 'Email',
--    'email_address' => 'Email address',
--    'user_identifier' => 'Username or Email',
--    'password' => 'Password',
--    'new_password' => 'New password',
--    'confirm_password' => 'Confirm new password',
--    'login' => 'Login',
--    'home' => 'Home',
--    'servers' => 'Servers',
-+    'email' => 'E-Mail',
-+    'email_address' => 'Email-Addresse',
-+    'user_identifier' => 'Benutzername oder E-Mail',
-+    'password' => 'Passwort',
-+    'new_password' => 'Neues Passwort',
-+    'confirm_password' => 'Neues Passwort bestätigen',
-+    'login' => 'Anmelden',
-+    'home' => 'Zuhause',
-+    'servers' => 'Server',
-     'id' => 'ID',
-     'name' => 'Name',
-     'node' => 'Node',
--    'connection' => 'Connection',
--    'memory' => 'Memory',
-+    'connection' => 'Verbindung',
-+    'memory' => 'Arbeitsspeicher',
-     'cpu' => 'CPU',
--    'disk' => 'Disk',
-+    'disk' => 'Speicher',
-     'status' => 'Status',
--    'search' => 'Search',
--    'suspended' => 'Suspended',
-+    'search' => 'Suche',
-+    'suspended' => 'Gesperrt',
-     'account' => 'Account',
--    'security' => 'Security',
--    'ip' => 'IP Address',
--    'last_activity' => 'Last Activity',
--    'revoke' => 'Revoke',
--    '2fa_token' => 'Authentication Token',
--    'submit' => 'Submit',
--    'close' => 'Close',
--    'settings' => 'Settings',
--    'configuration' => 'Configuration',
-+    'security' => 'Sicherheit',
-+    'ip' => 'IP-Adresse',
-+    'last_activity' => 'Letzte Aktivität',
-+    'revoke' => 'Entziehen',
-+    '2fa_token' => 'Authentifizierungstoken',
-+    'submit' => 'Absenden',
-+    'close' => 'Schließen',
-+    'settings' => 'Einstellungen',
-+    'configuration' => 'Konfiguration',
-     'sftp' => 'SFTP',
--    'databases' => 'Databases',
-+    'databases' => 'Datenbanken',
-     'memo' => 'Memo',
--    'created' => 'Created',
--    'expires' => 'Expires',
-+    'created' => 'Erstellt',
-+    'expires' => 'Läuft ab',
-     'public_key' => 'Token',
--    'api_access' => 'Api Access',
--    'never' => 'never',
--    'sign_out' => 'Sign out',
--    'admin_control' => 'Admin Control',
--    'required' => 'Required',
-+    'api_access' => 'API-Zugriff',
-+    'never' => 'Niemals',
-+    'sign_out' => 'Ausloggen',
-+    'admin_control' => 'Admin-Steuerung',
-+    'required' => 'Erforderlich',
-     'port' => 'Port',
--    'username' => 'Username',
--    'database' => 'Database',
--    'new' => 'New',
--    'danger' => 'Danger',
--    'create' => 'Create',
--    'select_all' => 'Select All',
--    'select_none' => 'Select None',
-+    'username' => 'Benutzername',
-+    'database' => 'Datenbank',
-+    'new' => 'Neu',
-+    'danger' => 'Gefahr',
-+    'create' => 'Erstellen',
-+    'select_all' => 'Alle wählen',
-+    'select_none' => 'Nichts auswählen',
-     'alias' => 'Alias',
--    'primary' => 'Primary',
--    'make_primary' => 'Make Primary',
--    'none' => 'None',
--    'cancel' => 'Cancel',
--    'created_at' => 'Created At',
--    'action' => 'Action',
--    'data' => 'Data',
--    'queued' => 'Queued',
--    'last_run' => 'Last Run',
--    'next_run' => 'Next Run',
--    'not_run_yet' => 'Not Run Yet',
--    'yes' => 'Yes',
--    'no' => 'No',
--    'delete' => 'Delete',
-+    'primary' => 'Primär',
-+    'make_primary' => 'Primär machen',
-+    'none' => 'Keiner',
-+    'cancel' => 'Abbrechen',
-+    'created_at' => 'Erstellt am',
-+    'action' => 'Aktion',
-+    'data' => 'Daten',
-+    'queued' => 'In Warteschlange',
-+    'last_run' => 'Letzter Lauf',
-+    'next_run' => 'Nächster Lauf',
-+    'not_run_yet' => 'Noch nicht ausgeführt',
-+    'yes' => 'Ja',
-+    'no' => 'Nein',
-+    'delete' => 'Löschen',
-     '2fa' => '2FA',
--    'logout' => 'Logout',
--    'admin_cp' => 'Admin Control Panel',
-+    'logout' => 'Ausloggen',
-+    'admin_cp' => 'Admin-Systemsteuerung',
-     'optional' => 'Optional',
--    'read_only' => 'Read Only',
-+    'read_only' => 'Nur lesen',
-     'relation' => 'Relation',
--    'owner' => 'Owner',
-+    'owner' => 'Inhaber',
-     'admin' => 'Admin',
--    'subuser' => 'Subuser',
--    'captcha_invalid' => 'The provided captcha is invalid.',
--    'tasks' => 'Tasks',
--    'seconds' => 'Seconds',
--    'minutes' => 'Minutes',
--    'under_maintenance' => 'Under Maintenance',
-+    'subuser' => 'Unterbenutzer',
-+    'captcha_invalid' => 'Das bereitgestellte Captcha ist ungültig.',
-+    'tasks' => 'Aufgaben',
-+    'seconds' => 'Sekunden',
-+    'minutes' => 'Minuten',
-+    'under_maintenance' => 'Wird gewartet',
-     'days' => [
--        'sun' => 'Sunday',
--        'mon' => 'Monday',
--        'tues' => 'Tuesday',
--        'wed' => 'Wednesday',
--        'thurs' => 'Thursday',
--        'fri' => 'Friday',
--        'sat' => 'Saturday',
-+        'sun' => 'Sonntag',
-+        'mon' => 'Montag',
-+        'tues' => 'Dienstag',
-+        'wed' => 'Mittwoch',
-+        'thurs' => 'Donnerstag',
-+        'fri' => 'Freitag',
-+        'sat' => 'Samstag',
-     ],
--    'last_used' => 'Last Used',
--    'enable' => 'Enable',
--    'disable' => 'Disable',
--    'save' => 'Save',
-+    'last_used' => 'Zuletzt verwendet',
-+    'enable' => 'Aktivieren',
-+    'disable' => 'Deaktivieren',
-+    'save' => 'Speichern',
-     'copyright' => '&copy; 2015 - :year Pterodactyl Software',
- ];
-diff --git a/resources/lang/en/validation.php b/resources/lang/en/validation.php
-index a82aaa1..6b7b426 100644
---- a/resources/lang/en/validation.php
-+++ b/resources/lang/en/validation.php
-@@ -12,78 +12,78 @@ return [
-     |
-     */
- 
--    'accepted' => 'The :attribute must be accepted.',
--    'active_url' => 'The :attribute is not a valid URL.',
--    'after' => 'The :attribute must be a date after :date.',
--    'after_or_equal' => 'The :attribute must be a date after or equal to :date.',
--    'alpha' => 'The :attribute may only contain letters.',
--    'alpha_dash' => 'The :attribute may only contain letters, numbers, and dashes.',
--    'alpha_num' => 'The :attribute may only contain letters and numbers.',
--    'array' => 'The :attribute must be an array.',
--    'before' => 'The :attribute must be a date before :date.',
--    'before_or_equal' => 'The :attribute must be a date before or equal to :date.',
-+    'accepted' => ':attribute muss akzeptiert werden',
-+    'active_url' => ':attribute ist keine validate URL.',
-+    'after' => ':attribute muss ein Datum nach :date sein.',
-+    'after_or_equal' => ':attribute muss ein Datum nach oder gleich :date sein.',
-+    'alpha' => ':attribute darf nur Buchstaben enthalten.',
-+    'alpha_dash' => ':attribute darf nur Buchstaben, Zahlen und Bindestriche enthalten.',
-+    'alpha_num' => ':attribute darf nur Buchstaben und Zahlen enthalten.',
-+    'array' => ':attribute muss ein Array sein.',
-+    'before' => ':attribute muss ein Datum vor :date sein.',
-+    'before_or_equal' => ':attribute muss ein Datum vor oder gleich :date sein.',
-     'between' => [
--        'numeric' => 'The :attribute must be between :min and :max.',
--        'file' => 'The :attribute must be between :min and :max kilobytes.',
--        'string' => 'The :attribute must be between :min and :max characters.',
--        'array' => 'The :attribute must have between :min and :max items.',
-+        'numeric' => ':attribute muss zwischen :min und :max liegen.',
-+        'file' => ':attribute muss zwischen :min und :max Kilobyte liegen.',
-+        'string' => ':attribute muss zwischen :min und :max Zeichen liegen.',
-+        'array' => ':attribute muss zwischen :min und :max Elemente haben.',
-     ],
--    'boolean' => 'The :attribute field must be true or false.',
--    'confirmed' => 'The :attribute confirmation does not match.',
--    'date' => 'The :attribute is not a valid date.',
--    'date_format' => 'The :attribute does not match the format :format.',
--    'different' => 'The :attribute and :other must be different.',
--    'digits' => 'The :attribute must be :digits digits.',
--    'digits_between' => 'The :attribute must be between :min and :max digits.',
--    'dimensions' => 'The :attribute has invalid image dimensions.',
--    'distinct' => 'The :attribute field has a duplicate value.',
--    'email' => 'The :attribute must be a valid email address.',
--    'exists' => 'The selected :attribute is invalid.',
--    'file' => 'The :attribute must be a file.',
--    'filled' => 'The :attribute field is required.',
--    'image' => 'The :attribute must be an image.',
--    'in' => 'The selected :attribute is invalid.',
--    'in_array' => 'The :attribute field does not exist in :other.',
--    'integer' => 'The :attribute must be an integer.',
--    'ip' => 'The :attribute must be a valid IP address.',
--    'json' => 'The :attribute must be a valid JSON string.',
-+    'boolean' => ':attribute muss wahr oder falsch sein.',
-+    'confirmed' => ':attribute Bestätigung stimmt nicht überein.',
-+    'date' => ':attribute ist kein gültiges Datum.',
-+    'date_format' => ':attribute passt nicht zum Format :format.',
-+    'different' => ':attribute und :other müssen unterschiedlich sein.',
-+    'digits' => ':attribute muss :digits Ziffern sein.',
-+    'digits_between' => ':attribute muss zwischen :min und :max Ziffern liegen.',
-+    'dimensions' => ':attribute hat ungültige Bildabmessungen.',
-+    'distinct' => 'Das :attribute Feld hat einen doppelten Wert.',
-+    'email' => ':attribute muss eine gültige E-Mail-Adresse sein.',
-+    'exists' => 'Das ausgewählte Attribut :attribute ist ungültig.',
-+    'file' => ':attribute muss eine Datei sein.',
-+    'filled' => 'Das :attribute Feld wird für die Fortsetzung benötigt.',
-+    'image' => ':attribute muss ein Bild sein.',
-+    'in' => 'Das ausgewählte Attribut :attribute ist ungültig.',
-+    'in_array' => 'Das :attribute Feld existiert nicht in :other.',
-+    'integer' => ':attribute muss eine ganze Zahl sein.',
-+    'ip' => ':attribute muss eine gültige IP-Adresse sein.',
-+    'json' => ':attribute muss eine gültige JSON-Zeichenfolge sein.',
-     'max' => [
--        'numeric' => 'The :attribute may not be greater than :max.',
--        'file' => 'The :attribute may not be greater than :max kilobytes.',
--        'string' => 'The :attribute may not be greater than :max characters.',
--        'array' => 'The :attribute may not have more than :max items.',
-+        'numeric' => ':attribute darf nicht größer als :max sein.',
-+        'file' => ':attribute darf nicht größer als :max kilobytes sein.',
-+        'string' => ':attribute darf nicht größer als :max Zeichen sein.',
-+        'array' => ':attribute darf nicht mehr als :max Elemente haben.',
-     ],
--    'mimes' => 'The :attribute must be a file of type: :values.',
--    'mimetypes' => 'The :attribute must be a file of type: :values.',
-+    'mimes' => ':attribute muss eine Datei des Typs :values sein.',
-+    'mimetypes' => ':attribute muss eine Datei des Typs :values sein.',
-     'min' => [
--        'numeric' => 'The :attribute must be at least :min.',
--        'file' => 'The :attribute must be at least :min kilobytes.',
--        'string' => 'The :attribute must be at least :min characters.',
--        'array' => 'The :attribute must have at least :min items.',
-+        'numeric' => ':attribute muss mindestens :min sein.',
-+        'file' => ':attribute muss mindestens :min Kilobyte groß sein.',
-+        'string' => ':attribute muss mindestens :min Zeichen lang sein.',
-+        'array' => ':attribute muss mindestens :min Elemente haben.',
-     ],
--    'not_in' => 'The selected :attribute is invalid.',
--    'numeric' => 'The :attribute must be a number.',
--    'present' => 'The :attribute field must be present.',
--    'regex' => 'The :attribute format is invalid.',
--    'required' => 'The :attribute field is required.',
--    'required_if' => 'The :attribute field is required when :other is :value.',
--    'required_unless' => 'The :attribute field is required unless :other is in :values.',
--    'required_with' => 'The :attribute field is required when :values is present.',
--    'required_with_all' => 'The :attribute field is required when :values is present.',
--    'required_without' => 'The :attribute field is required when :values is not present.',
--    'required_without_all' => 'The :attribute field is required when none of :values are present.',
--    'same' => 'The :attribute and :other must match.',
-+    'not_in' => 'Das ausgewählte Attribut :attribute ist ungültig.',
-+    'numeric' => ':attribute muss eine Nummer sein.',
-+    'present' => 'Das :attribute Feld muss vorhanden sein.',
-+    'regex' => 'Das :attribute Format ist ungültig.',
-+    'required' => 'Das :attribute Feld ist erforderlich.',
-+    'required_if' => 'Das :attribute Feld ist erforderlich, wenn :other :value ist.',
-+    'required_unless' => 'Das :attribute Feld ist erforderlich, es sei denn, :other ist :values.',
-+    'required_with' => 'Das :attribute Feld ist erforderlich, wenn :values vorhanden ist.',
-+    'required_with_all' => 'Das :attribute Feld ist erforderlich, wenn :values vorhanden ist.',
-+    'required_without' => 'Das Feld :attribute ist erforderlich, wenn :values nicht vorhanden ist.',
-+    'required_without_all' => 'Das :attribute Feld ist erforderlich, wenn keiner der :values vorhanden ist.',
-+    'same' => ':attribute und :other müssen übereinstimmen.',
-     'size' => [
--        'numeric' => 'The :attribute must be :size.',
--        'file' => 'The :attribute must be :size kilobytes.',
--        'string' => 'The :attribute must be :size characters.',
--        'array' => 'The :attribute must contain :size items.',
-+        'numeric' => ':attribute muss :size sein.',
-+        'file' => ':attribute muss :size Kilobyte sein.',
-+        'string' => ':attribute muss :size Zeichen sein.',
-+        'array' => ':attribute muss :size Elemente enthalten.',
-     ],
--    'string' => 'The :attribute must be a string.',
--    'timezone' => 'The :attribute must be a valid zone.',
--    'unique' => 'The :attribute has already been taken.',
--    'uploaded' => 'The :attribute failed to upload.',
--    'url' => 'The :attribute format is invalid.',
-+    'string' => ':attribute muss eine Zeichenfolge sein.',
-+    'timezone' => ':attribute muss eine gültige Zone sein.',
-+    'unique' => ':attribute wurde bereits vergeben.',
-+    'uploaded' => ':attribute konnte nicht hochgeladen werden.',
-+    'url' => 'Das Format von :attribute ist ungültig.',
- 
-     /*
-     |--------------------------------------------------------------------------
-@@ -100,7 +100,7 @@ return [
- 
-     // Internal validation logic for Pterodactyl
-     'internal' => [
--        'variable_value' => ':env variable',
--        'invalid_password' => 'The password provided was invalid for this account.',
-+        'variable_value' => ':env Variable',
-+        'invalid_password' => 'Das angegebene Passwort war für dieses Konto ungültig.',
-     ],
- ];
-diff --git a/resources/scripts/api/server/files/compressFiles.ts b/resources/scripts/api/server/files/compressFiles.ts
-index b4c0a25..6eef1df 100644
---- a/resources/scripts/api/server/files/compressFiles.ts
-+++ b/resources/scripts/api/server/files/compressFiles.ts
-@@ -7,9 +7,9 @@ export default async (uuid: string, directory: string, files: string[]): Promise
-         `/api/client/servers/${uuid}/files/compress`,
-         { root: directory, files },
-         {
--            timeout: 60000,
-+            timeout: 30000,
-             timeoutErrorMessage:
--                'It looks like this archive is taking a long time to generate. It will appear once completed.',
-+                'Die Archivierung hat zu lange gedauert. Der Prozess wurde abgebrochen.',
-         }
-     );
- 
-diff --git a/resources/scripts/api/server/files/decompressFiles.ts b/resources/scripts/api/server/files/decompressFiles.ts
-index 37557a6..b058238 100644
---- a/resources/scripts/api/server/files/decompressFiles.ts
-+++ b/resources/scripts/api/server/files/decompressFiles.ts
-@@ -7,7 +7,7 @@ export default async (uuid: string, directory: string, file: string): Promise<vo
-         {
-             timeout: 300000,
-             timeoutErrorMessage:
--                'It looks like this archive is taking a long time to be unarchived. Once completed the unarchived files will appear.',
-+                'Das Entpacken dauert länger als erwartet. Bitte diese Seite nicht schließen, das Popup schließt sich nach fertigstellung selbst. ',
-         }
-     );
- };
-diff --git a/resources/scripts/components/NavigationBar.tsx b/resources/scripts/components/NavigationBar.tsx
-index 89180b1..055bc2b 100644
---- a/resources/scripts/components/NavigationBar.tsx
-+++ b/resources/scripts/components/NavigationBar.tsx
-@@ -67,20 +67,20 @@ export default () => {
-                         </NavLink>
-                     </Tooltip>
-                     {rootAdmin && (
--                        <Tooltip placement={'bottom'} content={'Admin'}>
-+                        <Tooltip placement={'bottom'} content={'Admin-Panel'}>
-                             <a href={'/admin'} rel={'noreferrer'}>
-                                 <FontAwesomeIcon icon={faCogs} />
-                             </a>
-                         </Tooltip>
-                     )}
--                    <Tooltip placement={'bottom'} content={'Account Settings'}>
-+                    <Tooltip placement={'bottom'} content={'Mein Account'}>
-                         <NavLink to={'/account'}>
-                             <span className={'flex items-center w-5 h-5'}>
-                                 <Avatar.User />
-                             </span>
-                         </NavLink>
-                     </Tooltip>
--                    <Tooltip placement={'bottom'} content={'Sign Out'}>
-+                    <Tooltip placement={'bottom'} content={'Abmelden'}>
-                         <button onClick={onTriggerLogout}>
-                             <FontAwesomeIcon icon={faSignOutAlt} />
-                         </button>
-diff --git a/resources/scripts/components/auth/ForgotPasswordContainer.tsx b/resources/scripts/components/auth/ForgotPasswordContainer.tsx
-index 76ddf19..fbe0ebc 100644
---- a/resources/scripts/components/auth/ForgotPasswordContainer.tsx
-+++ b/resources/scripts/components/auth/ForgotPasswordContainer.tsx
-@@ -38,7 +38,7 @@ export default () => {
-                 console.error(error);
- 
-                 setSubmitting(false);
--                addFlash({ type: 'error', title: 'Error', message: httpErrorToHuman(error) });
-+                addFlash({ type: 'error', title: 'Fehler', message: httpErrorToHuman(error) });
-             });
- 
-             return;
-@@ -47,11 +47,11 @@ export default () => {
-         requestPasswordResetEmail(email, token)
-             .then((response) => {
-                 resetForm();
--                addFlash({ type: 'success', title: 'Success', message: response });
-+                addFlash({ type: 'success', title: 'Erfolgreich', message: response });
-             })
-             .catch((error) => {
-                 console.error(error);
--                addFlash({ type: 'error', title: 'Error', message: httpErrorToHuman(error) });
-+                addFlash({ type: 'error', title: 'Fehler', message: httpErrorToHuman(error) });
-             })
-             .then(() => {
-                 setToken('');
-@@ -67,24 +67,24 @@ export default () => {
-             initialValues={{ email: '' }}
-             validationSchema={object().shape({
-                 email: string()
--                    .email('A valid email address must be provided to continue.')
--                    .required('A valid email address must be provided to continue.'),
-+                    .email('Gib eine gültige E-Mail Adresse an')
-+                    .required('Gib eine gültige E-Mail Adresse an'),
-             })}
-         >
-             {({ isSubmitting, setSubmitting, submitForm }) => (
--                <LoginFormContainer title={'Request Password Reset'} css={tw`w-full flex`}>
-+                <LoginFormContainer title={'Passwort zurücksetzen'} css={tw`w-full flex`}>
-                     <Field
-                         light
--                        label={'Email'}
-+                        label={'E-Mail'}
-                         description={
--                            'Enter your account email address to receive instructions on resetting your password.'
-+                            'Gib deine E-Mail Adresse an, um dein Passwort zurückzusetzen. Du bekommst dann eine E-Mail mit einer Anleitung.'
-                         }
-                         name={'email'}
-                         type={'email'}
-                     />
-                     <div css={tw`mt-6`}>
-                         <Button type={'submit'} size={'xlarge'} disabled={isSubmitting} isLoading={isSubmitting}>
--                            Send Email
-+                            E-Mail senden
-                         </Button>
-                     </div>
-                     {recaptchaEnabled && (
-@@ -107,7 +107,7 @@ export default () => {
-                             to={'/auth/login'}
-                             css={tw`text-xs text-neutral-500 tracking-wide uppercase no-underline hover:text-neutral-700`}
-                         >
--                            Return to Login
-+                            Zum Login
-                         </Link>
-                     </div>
-                 </LoginFormContainer>
-diff --git a/resources/scripts/components/auth/LoginCheckpointContainer.tsx b/resources/scripts/components/auth/LoginCheckpointContainer.tsx
-index 66a6fb8..d3d8b6b 100644
---- a/resources/scripts/components/auth/LoginCheckpointContainer.tsx
-+++ b/resources/scripts/components/auth/LoginCheckpointContainer.tsx
-@@ -27,16 +27,16 @@ const LoginCheckpointContainer = () => {
-     const [isMissingDevice, setIsMissingDevice] = useState(false);
- 
-     return (
--        <LoginFormContainer title={'Device Checkpoint'} css={tw`w-full flex`}>
-+        <LoginFormContainer title={'2-FA Authentifizierung'} css={tw`w-full flex`}>
-             <div css={tw`mt-6`}>
-                 <Field
-                     light
-                     name={isMissingDevice ? 'recoveryCode' : 'code'}
--                    title={isMissingDevice ? 'Recovery Code' : 'Authentication Code'}
-+                    title={isMissingDevice ? 'Wiederherstellungscode' : '2-FA Authentifizierung'}
-                     description={
-                         isMissingDevice
--                            ? 'Enter one of the recovery codes generated when you setup 2-Factor authentication on this account in order to continue.'
--                            : 'Enter the two-factor token generated by your device.'
-+                            ? 'Beim Einrichten der 2-Faktor Authentifizierung wurden dir Backup-Codes bereitgestellt. Nutze eine dieser Codes, um fortzufahren.'
-+                            : 'Gib hier den 6-stelligen Code ein'
-                     }
-                     type={'text'}
-                     autoComplete={'one-time-code'}
-@@ -45,7 +45,7 @@ const LoginCheckpointContainer = () => {
-             </div>
-             <div css={tw`mt-6`}>
-                 <Button size={'xlarge'} type={'submit'} disabled={isSubmitting} isLoading={isSubmitting}>
--                    Continue
-+                    Weiter
-                 </Button>
-             </div>
-             <div css={tw`mt-6 text-center`}>
-@@ -57,7 +57,7 @@ const LoginCheckpointContainer = () => {
-                     }}
-                     css={tw`cursor-pointer text-xs text-neutral-500 tracking-wide uppercase no-underline hover:text-neutral-700`}
-                 >
--                    {!isMissingDevice ? "I've Lost My Device" : 'I Have My Device'}
-+                    {!isMissingDevice ? "Ich habe mein Gerät verloren" : 'Ich hab mein Gerät vor mir'}
-                 </span>
-             </div>
-             <div css={tw`mt-6 text-center`}>
-@@ -65,7 +65,7 @@ const LoginCheckpointContainer = () => {
-                     to={'/auth/login'}
-                     css={tw`text-xs text-neutral-500 tracking-wide uppercase no-underline hover:text-neutral-700`}
-                 >
--                    Return to Login
-+                    Zum Login zurück
-                 </Link>
-             </div>
-         </LoginFormContainer>
-diff --git a/resources/scripts/components/auth/LoginContainer.tsx b/resources/scripts/components/auth/LoginContainer.tsx
-index 453a27e..29a9311 100644
---- a/resources/scripts/components/auth/LoginContainer.tsx
-+++ b/resources/scripts/components/auth/LoginContainer.tsx
-@@ -69,19 +69,19 @@ const LoginContainer = ({ history }: RouteComponentProps) => {
-             onSubmit={onSubmit}
-             initialValues={{ username: '', password: '' }}
-             validationSchema={object().shape({
--                username: string().required('A username or email must be provided.'),
--                password: string().required('Please enter your account password.'),
-+                username: string().required('Gib deinen Benutzernamen oder deine E-Mail ein'),
-+                password: string().required('Gib dein Passwort ein'),
-             })}
-         >
-             {({ isSubmitting, setSubmitting, submitForm }) => (
--                <LoginFormContainer title={'Login to Continue'} css={tw`w-full flex`}>
--                    <Field light type={'text'} label={'Username or Email'} name={'username'} disabled={isSubmitting} />
-+                <LoginFormContainer title={'Anmelden'} css={tw`w-full flex`}>
-+                    <Field light type={'text'} label={'Benutzername oder E-Mail'} name={'username'} disabled={isSubmitting} />
-                     <div css={tw`mt-6`}>
--                        <Field light type={'password'} label={'Password'} name={'password'} disabled={isSubmitting} />
-+                        <Field light type={'password'} label={'Passwort'} name={'password'} disabled={isSubmitting} />
-                     </div>
-                     <div css={tw`mt-6`}>
-                         <Button type={'submit'} size={'xlarge'} isLoading={isSubmitting} disabled={isSubmitting}>
--                            Login
-+                            Anmelden
-                         </Button>
-                     </div>
-                     {recaptchaEnabled && (
-@@ -104,7 +104,7 @@ const LoginContainer = ({ history }: RouteComponentProps) => {
-                             to={'/auth/password'}
-                             css={tw`text-xs text-neutral-500 tracking-wide no-underline uppercase hover:text-neutral-600`}
-                         >
--                            Forgot password?
-+                            Passwort vergessen?
-                         </Link>
-                     </div>
-                 </LoginFormContainer>
-diff --git a/resources/scripts/components/auth/LoginFormContainer.tsx b/resources/scripts/components/auth/LoginFormContainer.tsx
-index 6274981..6b8f1d0 100644
---- a/resources/scripts/components/auth/LoginFormContainer.tsx
-+++ b/resources/scripts/components/auth/LoginFormContainer.tsx
-@@ -48,7 +48,16 @@ export default forwardRef<HTMLFormElement, Props>(({ title, ...props }, ref) =>
-                 target={'_blank'}
-                 css={tw`no-underline text-neutral-500 hover:text-neutral-300`}
-             >
--                Pterodactyl Software
-+                Pterodactyl Software&nbsp;
-+            </a>
-+            | Übersetzt von&nbsp;
-+            <a
-+                rel={'noopener nofollow noreferrer'}
-+                href={'https://germandactyl.de'}
-+                target={'_blank'}
-+                css={tw`no-underline text-neutral-500 hover:text-neutral-300`}
-+            >
-+                Pavl21
-             </a>
-         </p>
-     </Container>
-diff --git a/resources/scripts/components/auth/ResetPasswordContainer.tsx b/resources/scripts/components/auth/ResetPasswordContainer.tsx
-index 86f95ab..4bdd0e6 100644
---- a/resources/scripts/components/auth/ResetPasswordContainer.tsx
-+++ b/resources/scripts/components/auth/ResetPasswordContainer.tsx
-@@ -39,7 +39,7 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
-                 console.error(error);
- 
-                 setSubmitting(false);
--                addFlash({ type: 'error', title: 'Error', message: httpErrorToHuman(error) });
-+                addFlash({ type: 'error', title: 'Fehler', message: httpErrorToHuman(error) });
-             });
-     };
- 
-@@ -52,16 +52,16 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
-             }}
-             validationSchema={object().shape({
-                 password: string()
--                    .required('A new password is required.')
--                    .min(8, 'Your new password should be at least 8 characters in length.'),
-+                    .required('Gib dein neues Passwort ein')
-+                    .min(8, 'Dein Passwort sollte mehr als 8 Zeichen haben.'),
-                 passwordConfirmation: string()
--                    .required('Your new password does not match.')
-+                    .required('Beide Passwörter stimmen nicht überein.')
-                     // @ts-expect-error this is valid
--                    .oneOf([ref('password'), null], 'Your new password does not match.'),
-+                    .oneOf([ref('password'), null], 'Beide Passwörter stimmen nicht überein.'),
-             })}
-         >
-             {({ isSubmitting }) => (
--                <LoginFormContainer title={'Reset Password'} css={tw`w-full flex`}>
-+                <LoginFormContainer title={'Passwort zurücksetzen'} css={tw`w-full flex`}>
-                     <div>
-                         <label>Email</label>
-                         <Input value={email} isLight disabled />
-@@ -69,18 +69,18 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
-                     <div css={tw`mt-6`}>
-                         <Field
-                             light
--                            label={'New Password'}
-+                            label={'Neues Passwort'}
-                             name={'password'}
-                             type={'password'}
--                            description={'Passwords must be at least 8 characters in length.'}
-+                            description={'Das Passwort muss länger als 8 Zeichen sein.'}
-                         />
-                     </div>
-                     <div css={tw`mt-6`}>
--                        <Field light label={'Confirm New Password'} name={'passwordConfirmation'} type={'password'} />
-+                        <Field light label={'Jetzt zurücksetzen'} name={'passwordConfirmation'} type={'password'} />
-                     </div>
-                     <div css={tw`mt-6`}>
-                         <Button size={'xlarge'} type={'submit'} disabled={isSubmitting} isLoading={isSubmitting}>
--                            Reset Password
-+                            Jetzt zurücksetzen
-                         </Button>
-                     </div>
-                     <div css={tw`mt-6 text-center`}>
-@@ -88,7 +88,7 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
-                             to={'/auth/login'}
-                             css={tw`text-xs text-neutral-500 tracking-wide no-underline uppercase hover:text-neutral-600`}
-                         >
--                            Return to Login
-+                            Zum Login zurück
-                         </Link>
-                     </div>
-                 </LoginFormContainer>
-diff --git a/resources/scripts/components/dashboard/AccountApiContainer.tsx b/resources/scripts/components/dashboard/AccountApiContainer.tsx
-index 282f190..710dc11 100644
---- a/resources/scripts/components/dashboard/AccountApiContainer.tsx
-+++ b/resources/scripts/components/dashboard/AccountApiContainer.tsx
-@@ -14,6 +14,7 @@ import GreyRowBox from '@/components/elements/GreyRowBox';
- import { Dialog } from '@/components/elements/dialog';
- import { useFlashKey } from '@/plugins/useFlash';
- import Code from '@/components/elements/Code';
-+import { de } from "date-fns/locale";
- 
- export default () => {
-     const [deleteIdentifier, setDeleteIdentifier] = useState('');
-@@ -42,26 +43,26 @@ export default () => {
-     };
- 
-     return (
--        <PageContentBlock title={'Account API'}>
-+        <PageContentBlock title={'API-Zugangsdaten'}>
-             <FlashMessageRender byKey={'account'} />
-             <div css={tw`md:flex flex-nowrap my-10`}>
--                <ContentBox title={'Create API Key'} css={tw`flex-none w-full md:w-1/2`}>
-+                <ContentBox title={'API-Schlüssel erstellen'} css={tw`flex-none w-full md:w-1/2`}>
-                     <CreateApiKeyForm onKeyCreated={(key) => setKeys((s) => [...s!, key])} />
-                 </ContentBox>
--                <ContentBox title={'API Keys'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
-+                <ContentBox title={'Alle API-Schlüssel'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
-                     <SpinnerOverlay visible={loading} />
-                     <Dialog.Confirm
--                        title={'Delete API Key'}
--                        confirm={'Delete Key'}
-+                        title={'API-Schlüssel löschen'}
-+                        confirm={'Schlüssel löschen'}
-                         open={!!deleteIdentifier}
-                         onClose={() => setDeleteIdentifier('')}
-                         onConfirmed={() => doDeletion(deleteIdentifier)}
-                     >
--                        All requests using the <Code>{deleteIdentifier}</Code> key will be invalidated.
-+                        Alle Anfragen, die an <Code>{deleteIdentifier}</Code> gesendet werden, ignoriert dann dein Server.
-                     </Dialog.Confirm>
-                     {keys.length === 0 ? (
-                         <p css={tw`text-center text-sm`}>
--                            {loading ? 'Loading...' : 'No API keys exist for this account.'}
-+                            {loading ? 'Einen Moment bitte...' : 'Du hast keine API-Schlüssel aktiv.'}
-                         </p>
-                     ) : (
-                         keys.map((key, index) => (
-@@ -73,8 +74,8 @@ export default () => {
-                                 <div css={tw`ml-4 flex-1 overflow-hidden`}>
-                                     <p css={tw`text-sm break-words`}>{key.description}</p>
-                                     <p css={tw`text-2xs text-neutral-300 uppercase`}>
--                                        Last used:&nbsp;
--                                        {key.lastUsedAt ? format(key.lastUsedAt, 'MMM do, yyyy HH:mm') : 'Never'}
-+                                        Zuletzt angefragt:&nbsp;
-+                                        {key.lastUsedAt ? format(key.lastUsedAt, 'do MMM yyyy, HH:mm', {locale: de}) : 'Noch nie'}
-                                     </p>
-                                 </div>
-                                 <p css={tw`text-sm ml-4 hidden md:block`}>
-diff --git a/resources/scripts/components/dashboard/AccountOverviewContainer.tsx b/resources/scripts/components/dashboard/AccountOverviewContainer.tsx
-index 4f64aec..2b83276 100644
---- a/resources/scripts/components/dashboard/AccountOverviewContainer.tsx
-+++ b/resources/scripts/components/dashboard/AccountOverviewContainer.tsx
-@@ -30,21 +30,21 @@ export default () => {
-     const { state } = useLocation<undefined | { twoFactorRedirect?: boolean }>();
- 
-     return (
--        <PageContentBlock title={'Account Overview'}>
-+        <PageContentBlock title={'Mein Account'}>
-             {state?.twoFactorRedirect && (
--                <MessageBox title={'2-Factor Required'} type={'error'}>
--                    Your account must have two-factor authentication enabled in order to continue.
-+                <MessageBox title={'2-Faktor benötigt'} type={'error'}>
-+                    Die 2-Faktor Authentifizierung wird benötigt. Richte es erst ein.
-                 </MessageBox>
-             )}
- 
-             <Container css={[tw`lg:grid lg:grid-cols-3 mb-10`, state?.twoFactorRedirect ? tw`mt-4` : tw`mt-10`]}>
--                <ContentBox title={'Update Password'} showFlashes={'account:password'}>
-+                <ContentBox title={'Passwort ändern'} showFlashes={'account:password'}>
-                     <UpdatePasswordForm />
-                 </ContentBox>
--                <ContentBox css={tw`mt-8 sm:mt-0 sm:ml-8`} title={'Update Email Address'} showFlashes={'account:email'}>
-+                <ContentBox css={tw`mt-8 sm:mt-0 sm:ml-8`} title={'E-Mail Adresse ändern'} showFlashes={'account:email'}>
-                     <UpdateEmailAddressForm />
-                 </ContentBox>
--                <ContentBox css={tw`md:ml-8 mt-8 md:mt-0`} title={'Two-Step Verification'}>
-+                <ContentBox css={tw`md:ml-8 mt-8 md:mt-0`} title={'2-Faktor Authentifizierung'}>
-                     <ConfigureTwoFactorForm />
-                 </ContentBox>
-             </Container>
-diff --git a/resources/scripts/components/dashboard/ApiKeyModal.tsx b/resources/scripts/components/dashboard/ApiKeyModal.tsx
-index 6d01029..047b398 100644
---- a/resources/scripts/components/dashboard/ApiKeyModal.tsx
-+++ b/resources/scripts/components/dashboard/ApiKeyModal.tsx
-@@ -14,10 +14,9 @@ const ApiKeyModal = ({ apiKey }: Props) => {
- 
-     return (
-         <>
--            <h3 css={tw`mb-6 text-2xl`}>Your API Key</h3>
-+            <h3 css={tw`mb-6 text-2xl`}>Dein API-Schlüssel</h3>
-             <p css={tw`text-sm mb-6`}>
--                The API key you have requested is shown below. Please store this in a safe location, it will not be
--                shown again.
-+                Dein API-Schlüssel steht jetzt bereit. Gib es nur an Clients oder Personen weiter, denen du vertraust. Sie haben dieselben Berechtigungen wie du!
-             </p>
-             <pre css={tw`text-sm bg-neutral-900 rounded py-2 px-4 font-mono`}>
-                 <CopyOnClick text={apiKey}>
-@@ -26,7 +25,7 @@ const ApiKeyModal = ({ apiKey }: Props) => {
-             </pre>
-             <div css={tw`flex justify-end mt-6`}>
-                 <Button type={'button'} onClick={() => dismiss()}>
--                    Close
-+                    Schliessen
-                 </Button>
-             </div>
-         </>
-diff --git a/resources/scripts/components/dashboard/DashboardContainer.tsx b/resources/scripts/components/dashboard/DashboardContainer.tsx
-index 25e0e4d..8895906 100644
---- a/resources/scripts/components/dashboard/DashboardContainer.tsx
-+++ b/resources/scripts/components/dashboard/DashboardContainer.tsx
-@@ -53,7 +53,7 @@ export default () => {
-             {rootAdmin && (
-                 <div css={tw`mb-2 flex justify-end items-center`}>
-                     <p css={tw`uppercase text-xs text-neutral-400 mr-2`}>
--                        {showOnlyAdmin ? "Showing others' servers" : 'Showing your servers'}
-+                        {showOnlyAdmin ? "Andere Server" : 'Eigene Server'}
-                     </p>
-                     <Switch
-                         name={'show_all_servers'}
-@@ -74,8 +74,8 @@ export default () => {
-                         ) : (
-                             <p css={tw`text-center text-sm text-neutral-400`}>
-                                 {showOnlyAdmin
--                                    ? 'There are no other servers to display.'
--                                    : 'There are no servers associated with your account.'}
-+                                    ? 'Derzeit gibt es keine Server.'
-+                                    : 'Aktuell ist kein Server mit deinem Account verbunden.'}
-                             </p>
-                         )
-                     }
-diff --git a/resources/scripts/components/dashboard/ServerRow.tsx b/resources/scripts/components/dashboard/ServerRow.tsx
-index 08637bf..8290dfc 100644
---- a/resources/scripts/components/dashboard/ServerRow.tsx
-+++ b/resources/scripts/components/dashboard/ServerRow.tsx
-@@ -84,9 +84,9 @@ export default ({ server, className }: { server: Server; className?: string }) =
-         alarms.disk = server.limits.disk === 0 ? false : isAlarmState(stats.diskUsageInBytes, server.limits.disk);
-     }
- 
--    const diskLimit = server.limits.disk !== 0 ? bytesToString(mbToBytes(server.limits.disk)) : 'Unlimited';
--    const memoryLimit = server.limits.memory !== 0 ? bytesToString(mbToBytes(server.limits.memory)) : 'Unlimited';
--    const cpuLimit = server.limits.cpu !== 0 ? server.limits.cpu + ' %' : 'Unlimited';
-+    const diskLimit = server.limits.disk !== 0 ? bytesToString(mbToBytes(server.limits.disk)) : 'Kein Limit';
-+    const memoryLimit = server.limits.memory !== 0 ? bytesToString(mbToBytes(server.limits.memory)) : 'Kein Limit';
-+    const cpuLimit = server.limits.cpu !== 0 ? server.limits.cpu + ' %' : 'Kein Limit';
- 
-     return (
-         <StatusIndicatorBox as={Link} to={`/server/${server.id}`} className={className} $status={stats?.status}>
-@@ -120,19 +120,19 @@ export default ({ server, className }: { server: Server; className?: string }) =
-                     isSuspended ? (
-                         <div css={tw`flex-1 text-center`}>
-                             <span css={tw`bg-red-500 rounded px-2 py-1 text-red-100 text-xs`}>
--                                {server.status === 'suspended' ? 'Suspended' : 'Connection Error'}
-+                                {server.status === 'suspended' ? 'Gesperrt' : 'Verbindungsfehler'}
-                             </span>
-                         </div>
-                     ) : server.isTransferring || server.status ? (
-                         <div css={tw`flex-1 text-center`}>
-                             <span css={tw`bg-neutral-500 rounded px-2 py-1 text-neutral-100 text-xs`}>
-                                 {server.isTransferring
--                                    ? 'Transferring'
-+                                    ? 'Transfer läuft...'
-                                     : server.status === 'installing'
--                                    ? 'Installing'
-+                                    ? 'Installation läuft...'
-                                     : server.status === 'restoring_backup'
--                                    ? 'Restoring Backup'
--                                    : 'Unavailable'}
-+                                    ? 'Backup wiederherstellen...'
-+                                    : 'Nicht verfügbar'}
-                             </span>
-                         </div>
-                     ) : (
-@@ -147,7 +147,7 @@ export default ({ server, className }: { server: Server; className?: string }) =
-                                     {stats.cpuUsagePercent.toFixed(2)} %
-                                 </IconDescription>
-                             </div>
--                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>of {cpuLimit}</p>
-+                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>von {cpuLimit}</p>
-                         </div>
-                         <div css={tw`flex-1 ml-4 sm:block hidden`}>
-                             <div css={tw`flex justify-center`}>
-@@ -156,7 +156,7 @@ export default ({ server, className }: { server: Server; className?: string }) =
-                                     {bytesToString(stats.memoryUsageInBytes)}
-                                 </IconDescription>
-                             </div>
--                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>of {memoryLimit}</p>
-+                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>von {memoryLimit}</p>
-                         </div>
-                         <div css={tw`flex-1 ml-4 sm:block hidden`}>
-                             <div css={tw`flex justify-center`}>
-@@ -165,7 +165,7 @@ export default ({ server, className }: { server: Server; className?: string }) =
-                                     {bytesToString(stats.diskUsageInBytes)}
-                                 </IconDescription>
-                             </div>
--                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>of {diskLimit}</p>
-+                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>von {diskLimit}</p>
-                         </div>
-                     </React.Fragment>
-                 )}
-diff --git a/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx b/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx
-index f10701f..666d5f6 100644
---- a/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx
-+++ b/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx
-@@ -31,7 +31,7 @@ export default () => {
-     }, [error]);
- 
-     return (
--        <PageContentBlock title={'Account Activity Log'}>
-+        <PageContentBlock title={'Alle Account-Aktivitäten'}>
-             <FlashMessageRender byKey={'account'} />
-             {(filters.filters?.event || filters.filters?.ip) && (
-                 <div className={'flex justify-end mb-2'}>
-diff --git a/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx b/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx
-index a59977f..eac4b37 100644
---- a/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx
-+++ b/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx
-@@ -32,14 +32,14 @@ export default () => {
-             <DisableTOTPDialog open={visible === 'disable'} onClose={() => setVisible(null)} />
-             <p css={tw`text-sm`}>
-                 {isEnabled
--                    ? 'Two-step verification is currently enabled on your account.'
--                    : 'You do not currently have two-step verification enabled on your account. Click the button below to begin configuring it.'}
-+                    ? 'Die 2-Faktor Authentifizierung ist aktuell aktiv.'
-+                    : '2-Faktor ist aktuell nicht eingerichtet, du kannst es jederzeit einrichten.'}
-             </p>
-             <div css={tw`mt-6`}>
-                 {isEnabled ? (
--                    <Button.Danger onClick={() => setVisible('disable')}>Disable Two-Step</Button.Danger>
-+                    <Button.Danger onClick={() => setVisible('disable')}>2-Faktor deaktivieren</Button.Danger>
-                 ) : (
--                    <Button onClick={() => setVisible('enable')}>Enable Two-Step</Button>
-+                    <Button onClick={() => setVisible('enable')}>2-Faktor einrichten</Button>
-                 )}
-             </div>
-         </div>
-diff --git a/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx b/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx
-index 9c27304..9e46ff1 100644
---- a/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx
-+++ b/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx
-@@ -59,24 +59,24 @@ export default ({ onKeyCreated }: { onKeyCreated: (key: ApiKey) => void }) => {
-                     <Form>
-                         <SpinnerOverlay visible={isSubmitting} />
-                         <FormikFieldWrapper
--                            label={'Description'}
-+                            label={'Beschreibung'}
-                             name={'description'}
--                            description={'A description of this API key.'}
-+                            description={'Für was ist der API-Schlüssel gedacht?'}
-                             css={tw`mb-6`}
-                         >
-                             <Field name={'description'} as={Input} />
-                         </FormikFieldWrapper>
-                         <FormikFieldWrapper
--                            label={'Allowed IPs'}
-+                            label={'Erlaubte IPs'}
-                             name={'allowedIps'}
-                             description={
--                                'Leave blank to allow any IP address to use this API key, otherwise provide each IP address on a new line.'
-+                                'Erlaube hier nur Anfragen, die von zugelassenen IPs stammen, lasse es leer wenn es theoretisch alle dürfen.'
-                             }
-                         >
-                             <Field name={'allowedIps'} as={CustomTextarea} />
-                         </FormikFieldWrapper>
-                         <div css={tw`flex justify-end mt-6`}>
--                            <Button>Create</Button>
-+                            <Button>Erstellen</Button>
-                         </div>
-                     </Form>
-                 )}
-diff --git a/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx b/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx
-index 125376e..f186910 100644
---- a/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx
-+++ b/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx
-@@ -41,7 +41,7 @@ const DisableTOTPDialog = () => {
-         <form id={'disable-totp-form'} className={'mt-6'} onSubmit={submit}>
-             <FlashMessageRender byKey={'account:two-step'} className={'-mt-2 mb-6'} />
-             <label className={'block pb-1'} htmlFor={'totp-password'}>
--                Password
-+                Passwort
-             </label>
-             <Input.Text
-                 id={'totp-password'}
-@@ -51,14 +51,14 @@ const DisableTOTPDialog = () => {
-                 onChange={(e) => setPassword(e.currentTarget.value)}
-             />
-             <Dialog.Footer>
--                <Button.Text onClick={close}>Cancel</Button.Text>
-+                <Button.Text onClick={close}>Abbrechen</Button.Text>
-                 <Tooltip
-                     delay={100}
-                     disabled={password.length > 0}
--                    content={'You must enter your account password to continue.'}
-+                    content={'Gib dein Passwort ein, um fortzufahren.'}
-                 >
-                     <Button.Danger type={'submit'} form={'disable-totp-form'} disabled={submitting || !password.length}>
--                        Disable
-+                        Deaktivieren
-                     </Button.Danger>
-                 </Tooltip>
-             </Dialog.Footer>
-@@ -67,6 +67,6 @@ const DisableTOTPDialog = () => {
- };
- 
- export default asDialog({
--    title: 'Disable Two-Step Verification',
--    description: 'Disabling two-step verification will make your account less secure.',
-+    title: '2-Faktor deaktivieren',
-+    description: 'Das Deaktivieren verringert die Sicherheit deines Accounts.',
- })(DisableTOTPDialog);
-diff --git a/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx b/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx
-index 3e2d0ba..7e95f23 100644
---- a/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx
-+++ b/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx
-@@ -20,9 +20,9 @@ export default ({ tokens, open, onClose }: RecoveryTokenDialogProps) => {
-         <Dialog
-             open={open}
-             onClose={onClose}
--            title={'Two-Step Authentication Enabled'}
-+            title={'2-Faktor Authentifizierung ist jetzt aktiv.'}
-             description={
--                'Store the codes below somewhere safe. If you lose access to your phone you can use these backup codes to sign in.'
-+                'Speicher die Backup-Codes dort ab, wo sie sicher und gut auffindbar sind. Das ist sehr wichtig, damit du bei Verlust deines Gerätes die 2FA neu einrichten kannst.'
-             }
-             hideCloseIcon
-             preventExternalClose
-@@ -41,10 +41,10 @@ export default ({ tokens, open, onClose }: RecoveryTokenDialogProps) => {
-                 </pre>
-             </CopyOnClick>
-             <Alert type={'danger'} className={'mt-3'}>
--                These codes will not be shown again.
-+                Diese Codes werden nur dieses mal gezeigt.
-             </Alert>
-             <Dialog.Footer>
--                <Button.Text onClick={onClose}>Done</Button.Text>
-+                <Button.Text onClick={onClose}>Ich habs gespeichert</Button.Text>
-             </Dialog.Footer>
-         </Dialog>
-     );
-diff --git a/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx b/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx
-index 836b2ef..41c2c10 100644
---- a/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx
-+++ b/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx
-@@ -70,12 +70,11 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
-             </div>
-             <CopyOnClick text={token?.secret}>
-                 <p className={'font-mono text-sm text-gray-100 text-center mt-2'}>
--                    {token?.secret.match(/.{1,4}/g)!.join(' ') || 'Loading...'}
-+                    {token?.secret.match(/.{1,4}/g)!.join(' ') || 'Wird geladen...'}
-                 </p>
-             </CopyOnClick>
-             <p id={'totp-code-description'} className={'mt-6'}>
--                Scan the QR code above using the two-step authentication app of your choice. Then, enter the 6-digit
--                code generated into the field below.
-+                Scanne den QR-Code mit einer Authentificator-App deiner Wahl (z.B: Microsoft Authentificator) und gib dann den 6-stelligen Code ein.
-             </p>
-             <Input.Text
-                 aria-labelledby={'totp-code-description'}
-@@ -90,7 +89,7 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
-                 pattern={'\\d{6}'}
-             />
-             <label htmlFor={'totp-password'} className={'block mt-3'}>
--                Account Password
-+                Passwort deines Accounts
-             </label>
-             <Input.Text
-                 variant={Input.Text.Variants.Loose}
-@@ -100,13 +99,13 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
-                 onChange={(e) => setPassword(e.currentTarget.value)}
-             />
-             <Dialog.Footer>
--                <Button.Text onClick={close}>Cancel</Button.Text>
-+                <Button.Text onClick={close}>Abbrechen</Button.Text>
-                 <Tooltip
-                     disabled={password.length > 0 && value.length === 6}
-                     content={
-                         !token
--                            ? 'Waiting for QR code to load...'
--                            : 'You must enter the 6-digit code and your password to continue.'
-+                            ? 'Dein QR-Code wird geladen...'
-+                            : 'Gib den 6-stelligen Code und dein Passwort ein, um fortzufahren.'
-                     }
-                     delay={100}
-                 >
-@@ -115,7 +114,7 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
-                         type={'submit'}
-                         form={'enable-totp-form'}
-                     >
--                        Enable
-+                        Aktivieren
-                     </Button>
-                 </Tooltip>
-             </Dialog.Footer>
-@@ -124,7 +123,7 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
- };
- 
- export default asDialog({
--    title: 'Enable Two-Step Verification',
-+    title: '2-Faktor Authentifizierung einrichten',
-     description:
--        "Help protect your account from unauthorized access. You'll be prompted for a verification code each time you sign in.",
-+        'Schütze dich vor ungebetenen Benutzern in deinem eigenen Account und richte hier dein Schutzschild ein.',
- })(ConfigureTwoFactorForm);
-diff --git a/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx b/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx
-index 462a37f..b721880 100644
---- a/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx
-+++ b/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx
-@@ -16,7 +16,7 @@ interface Values {
- 
- const schema = Yup.object().shape({
-     email: Yup.string().email().required(),
--    password: Yup.string().required('You must provide your current account password.'),
-+    password: Yup.string().required('Gib dein aktuelles Passwort für deinen Account ein.'),
- });
- 
- export default () => {
-@@ -33,14 +33,14 @@ export default () => {
-                 addFlash({
-                     type: 'success',
-                     key: 'account:email',
--                    message: 'Your primary email has been updated.',
-+                    message: 'Deine aktuelle E-Mail Adresse wurde durch die neue ersetzt.',
-                 })
-             )
-             .catch((error) =>
-                 addFlash({
-                     type: 'error',
-                     key: 'account:email',
--                    title: 'Error',
-+                    title: 'Fehler',
-                     message: httpErrorToHuman(error),
-                 })
-             )
-@@ -62,11 +62,11 @@ export default () => {
-                                 id={'confirm_password'}
-                                 type={'password'}
-                                 name={'password'}
--                                label={'Confirm Password'}
-+                                label={'Passwort bestäigen'}
-                             />
-                         </div>
-                         <div css={tw`mt-6`}>
--                            <Button disabled={isSubmitting || !isValid}>Update Email</Button>
-+                            <Button disabled={isSubmitting || !isValid}>E-Mail speichern</Button>
-                         </div>
-                     </Form>
-                 </React.Fragment>
-diff --git a/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx b/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx
-index 707ee72..fcc38e9 100644
---- a/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx
-+++ b/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx
-@@ -17,11 +17,11 @@ interface Values {
- }
- 
- const schema = Yup.object().shape({
--    current: Yup.string().min(1).required('You must provide your current password.'),
-+    current: Yup.string().min(1).required('Gib dein aktuelles Passwort ein'),
-     password: Yup.string().min(8).required(),
-     confirmPassword: Yup.string().test(
-         'password',
--        'Password confirmation does not match the password you entered.',
-+        'Die Passwortbestätigung stimmt nicht mit dem eingegebenen Passwort überein.',
-         function (value) {
-             return value === this.parent.password;
-         }
-@@ -47,7 +47,7 @@ export default () => {
-                 addFlash({
-                     key: 'account:password',
-                     type: 'error',
--                    title: 'Error',
-+                    title: 'Fehler',
-                     message: httpErrorToHuman(error),
-                 })
-             )
-@@ -69,16 +69,16 @@ export default () => {
-                                 id={'current_password'}
-                                 type={'password'}
-                                 name={'current'}
--                                label={'Current Password'}
-+                                label={'Aktuelles Passwort'}
-                             />
-                             <div css={tw`mt-6`}>
-                                 <Field
-                                     id={'new_password'}
-                                     type={'password'}
-                                     name={'password'}
--                                    label={'New Password'}
-+                                    label={'Neues Passwort'}
-                                     description={
--                                        'Your new password should be at least 8 characters in length and unique to this website.'
-+                                        'Dein neues Passwort sollte mindestens 8 Zeichen lang sein, das verbessert die Sicherheit deines Accounts.'
-                                     }
-                                 />
-                             </div>
-@@ -87,11 +87,11 @@ export default () => {
-                                     id={'confirm_new_password'}
-                                     type={'password'}
-                                     name={'confirmPassword'}
--                                    label={'Confirm New Password'}
-+                                    label={'Bestätige das neue Passwort'}
-                                 />
-                             </div>
-                             <div css={tw`mt-6`}>
--                                <Button disabled={isSubmitting || !isValid}>Update Password</Button>
-+                                <Button disabled={isSubmitting || !isValid}>Neues Passwort speichern</Button>
-                             </div>
-                         </Form>
-                     </React.Fragment>
-diff --git a/resources/scripts/components/dashboard/search/SearchContainer.tsx b/resources/scripts/components/dashboard/search/SearchContainer.tsx
-index 4da0f3a..94ed25d 100644
---- a/resources/scripts/components/dashboard/search/SearchContainer.tsx
-+++ b/resources/scripts/components/dashboard/search/SearchContainer.tsx
-@@ -19,7 +19,7 @@ export default () => {
-     return (
-         <>
-             {visible && <SearchModal appear visible={visible} onDismissed={() => setVisible(false)} />}
--            <Tooltip placement={'bottom'} content={'Search'}>
-+            <Tooltip placement={'bottom'} content={'Suchen'}>
-                 <div className={'navigation-link'} onClick={() => setVisible(true)}>
-                     <FontAwesomeIcon icon={faSearch} />
-                 </div>
-diff --git a/resources/scripts/components/dashboard/search/SearchModal.tsx b/resources/scripts/components/dashboard/search/SearchModal.tsx
-index 1335cff..e45596c 100644
---- a/resources/scripts/components/dashboard/search/SearchModal.tsx
-+++ b/resources/scripts/components/dashboard/search/SearchModal.tsx
-@@ -80,7 +80,7 @@ export default ({ ...props }: Props) => {
-         <Formik
-             onSubmit={search}
-             validationSchema={object().shape({
--                term: string().min(3, 'Please enter at least three characters to begin searching.'),
-+                term: string().min(3, 'Gib mindetens 3 Zeichen ein, um die Suche zu starten'),
-             })}
-             initialValues={{ term: '' } as Values}
-         >
-@@ -89,8 +89,8 @@ export default ({ ...props }: Props) => {
-                     <Form>
-                         <FormikFieldWrapper
-                             name={'term'}
--                            label={'Search term'}
--                            description={'Enter a server name, uuid, or allocation to begin searching.'}
-+                            label={'Serversuche'}
-+                            description={'Gib den Servernamen, die UUID oder den zugewiesenen Port ein.'}
-                         >
-                             <SearchWatcher />
-                             <InputSpinner visible={isSubmitting}>
-diff --git a/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx b/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx
-index 0308b39..df22f9a 100644
---- a/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx
-+++ b/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx
-@@ -12,6 +12,7 @@ import { faKey } from '@fortawesome/free-solid-svg-icons';
- import { format } from 'date-fns';
- import CreateSSHKeyForm from '@/components/dashboard/ssh/CreateSSHKeyForm';
- import DeleteSSHKeyButton from '@/components/dashboard/ssh/DeleteSSHKeyButton';
-+import { de } from "date-fns/locale";
- 
- export default () => {
-     const { clearAndAddHttpError } = useFlashKey('account');
-@@ -25,17 +26,17 @@ export default () => {
-     }, [error]);
- 
-     return (
--        <PageContentBlock title={'SSH Keys'}>
-+        <PageContentBlock title={'SSH-Schlüssel'}>
-             <FlashMessageRender byKey={'account'} />
-             <div css={tw`md:flex flex-nowrap my-10`}>
--                <ContentBox title={'Add SSH Key'} css={tw`flex-none w-full md:w-1/2`}>
-+                <ContentBox title={'SSH-Schlüssel hinzufügen'} css={tw`flex-none w-full md:w-1/2`}>
-                     <CreateSSHKeyForm />
-                 </ContentBox>
--                <ContentBox title={'SSH Keys'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
-+                <ContentBox title={'SSH Schlüssel'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
-                     <SpinnerOverlay visible={!data && isValidating} />
-                     {!data || !data.length ? (
-                         <p css={tw`text-center text-sm`}>
--                            {!data ? 'Loading...' : 'No SSH Keys exist for this account.'}
-+                            {!data ? 'Einen Moment...' : 'Du hast keine SSH-Schlüssel eingerichtet.'}
-                         </p>
-                     ) : (
-                         data.map((key, index) => (
-@@ -48,8 +49,8 @@ export default () => {
-                                     <p css={tw`text-sm break-words font-medium`}>{key.name}</p>
-                                     <p css={tw`text-xs mt-1 font-mono truncate`}>SHA256:{key.fingerprint}</p>
-                                     <p css={tw`text-xs mt-1 text-neutral-300 uppercase`}>
--                                        Added on:&nbsp;
--                                        {format(key.createdAt, 'MMM do, yyyy HH:mm')}
-+                                        Erstellt am:&nbsp;
-+                                        {format(key.createdAt, 'do MMM yyyy, HH:mm', { locale: de })}
-                                     </p>
-                                 </div>
-                                 <DeleteSSHKeyButton name={key.name} fingerprint={key.fingerprint} />
-diff --git a/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx b/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx
-index 4b4f39c..f0094e2 100644
---- a/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx
-+++ b/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx
-@@ -48,18 +48,18 @@ export default () => {
-                 {({ isSubmitting }) => (
-                     <Form>
-                         <SpinnerOverlay visible={isSubmitting} />
--                        <FormikFieldWrapper label={'SSH Key Name'} name={'name'} css={tw`mb-6`}>
-+                        <FormikFieldWrapper label={'SSH-Schlüssel Name'} name={'name'} css={tw`mb-6`}>
-                             <Field name={'name'} as={Input} />
-                         </FormikFieldWrapper>
-                         <FormikFieldWrapper
--                            label={'Public Key'}
-+                            label={'Öffentlicher Schlüssel'}
-                             name={'publicKey'}
--                            description={'Enter your public SSH key.'}
-+                            description={'Gib deinen SSH-Key ein'}
-                         >
-                             <Field name={'publicKey'} as={CustomTextarea} />
-                         </FormikFieldWrapper>
-                         <div css={tw`flex justify-end mt-6`}>
--                            <Button>Save</Button>
-+                            <Button>Speichern</Button>
-                         </div>
-                     </Form>
-                 )}
-diff --git a/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx b/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx
-index bd9aaf0..1f2610a 100644
---- a/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx
-+++ b/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx
-@@ -28,12 +28,12 @@ export default ({ name, fingerprint }: { name: string; fingerprint: string }) =>
-         <>
-             <Dialog.Confirm
-                 open={visible}
--                title={'Delete SSH Key'}
--                confirm={'Delete Key'}
-+                title={'SSH-Schlüssel löschen'}
-+                confirm={'Schlüssel löschen'}
-                 onConfirmed={onClick}
-                 onClose={() => setVisible(false)}
-             >
--                Removing the <Code>{name}</Code> SSH key will invalidate its usage across the Panel.
-+                Entfernt <Code>{name}</Code> vom Panel, der Schlüssel wird dann ungültig.
-             </Dialog.Confirm>
-             <button css={tw`ml-4 p-2 text-sm`} onClick={() => setVisible(true)}>
-                 <FontAwesomeIcon
-diff --git a/resources/scripts/components/elements/ConfirmationModal.tsx b/resources/scripts/components/elements/ConfirmationModal.tsx
-index 52f9f9e..1a3a6df 100644
---- a/resources/scripts/components/elements/ConfirmationModal.tsx
-+++ b/resources/scripts/components/elements/ConfirmationModal.tsx
-@@ -20,7 +20,7 @@ const ConfirmationModal: React.FC<Props> = ({ title, children, buttonText, onCon
-             <div css={tw`text-neutral-300`}>{children}</div>
-             <div css={tw`flex flex-wrap items-center justify-end mt-8`}>
-                 <Button isSecondary onClick={() => dismiss()} css={tw`w-full sm:w-auto border-transparent`}>
--                    Cancel
-+                    Abbrechen
-                 </Button>
-                 <Button color={'red'} css={tw`w-full sm:w-auto mt-4 sm:mt-0 sm:ml-4`} onClick={() => onConfirmed()}>
-                     {buttonText}
-diff --git a/resources/scripts/components/elements/CopyOnClick.tsx b/resources/scripts/components/elements/CopyOnClick.tsx
-index 431e9c8..0fecf91 100644
---- a/resources/scripts/components/elements/CopyOnClick.tsx
-+++ b/resources/scripts/components/elements/CopyOnClick.tsx
-@@ -51,8 +51,8 @@ const CopyOnClick = ({ text, showInNotification = true, children }: CopyOnClickP
-                             <div className={'rounded-md py-3 px-4 text-gray-200 bg-neutral-600/95 shadow'}>
-                                 <p>
-                                     {showInNotification
--                                        ? `Copied "${String(text)}" to clipboard.`
--                                        : 'Copied text to clipboard.'}
-+                                        ? `"${String(text)}" wurde in die Zwischenablage kopiert.`
-+                                        : 'Der Text wurde in die Zwischenablage kopiert.'}
-                                 </p>
-                             </div>
-                         </div>
-diff --git a/resources/scripts/components/elements/ErrorBoundary.tsx b/resources/scripts/components/elements/ErrorBoundary.tsx
-index c92d952..1da8ac7 100644
---- a/resources/scripts/components/elements/ErrorBoundary.tsx
-+++ b/resources/scripts/components/elements/ErrorBoundary.tsx
-@@ -27,7 +27,7 @@ class ErrorBoundary extends React.Component<{}, State> {
-                 <div css={tw`flex items-center bg-neutral-900 rounded p-3 text-red-500`}>
-                     <Icon icon={faExclamationTriangle} css={tw`h-4 w-auto mr-2`} />
-                     <p css={tw`text-sm text-neutral-100`}>
--                        An error was encountered by the application while rendering this view. Try refreshing the page.
-+                        Beim verarbeiten dieser Seite trat ein Fehler auf. Bitte lade die Seite neu.
-                     </p>
-                 </div>
-             </div>
-diff --git a/resources/scripts/components/elements/PageContentBlock.tsx b/resources/scripts/components/elements/PageContentBlock.tsx
-index a4df195..cfa78f9 100644
---- a/resources/scripts/components/elements/PageContentBlock.tsx
-+++ b/resources/scripts/components/elements/PageContentBlock.tsx
-@@ -34,7 +34,16 @@ const PageContentBlock: React.FC<PageContentBlockProps> = ({ title, showFlashKey
-                         >
-                             Pterodactyl&reg;
-                         </a>
--                        &nbsp;&copy; 2015 - {new Date().getFullYear()}
-+                        &nbsp;&copy; 2015 - {new Date().getFullYear()} | Übersetzt von&nbsp;
-+                        <a
-+                            rel={'noopener nofollow noreferrer'}
-+                            href={'https://germandactyl.de'}
-+                            target={'_blank'}
-+                            css={tw`no-underline text-neutral-500 hover:text-neutral-300`}
-+                        >
-+                            Pavl21
-+                        </a>
-+
-                     </p>
-                 </ContentContainer>
-             </>
-diff --git a/resources/scripts/components/elements/PermissionRoute.tsx b/resources/scripts/components/elements/PermissionRoute.tsx
-index e7a46e5..82aede9 100644
---- a/resources/scripts/components/elements/PermissionRoute.tsx
-+++ b/resources/scripts/components/elements/PermissionRoute.tsx
-@@ -18,7 +18,7 @@ export default ({ permission, children, ...props }: Props) => (
-                 matchAny
-                 action={permission}
-                 renderOnError={
--                    <ServerError title={'Access Denied'} message={'You do not have permission to access this page.'} />
-+                    <ServerError title={'Ungenügende Rechte'} message={'Der Zugang zu dieser Seite wurde bei dir gesperrt.'} />
-                 }
-             >
-                 {children}
-diff --git a/resources/scripts/components/elements/ScreenBlock.tsx b/resources/scripts/components/elements/ScreenBlock.tsx
-index 0b57e92..5c10ce3 100644
---- a/resources/scripts/components/elements/ScreenBlock.tsx
-+++ b/resources/scripts/components/elements/ScreenBlock.tsx
-@@ -69,14 +69,14 @@ type ServerErrorProps = (Omit<PropsWithBack, 'image' | 'title'> | Omit<PropsWith
- };
- 
- const ServerError = ({ title, ...props }: ServerErrorProps) => (
--    <ScreenBlock title={title || 'Something went wrong'} image={ServerErrorSvg} {...props} />
-+    <ScreenBlock title={title || 'Etwas ist schief gelaufen'} image={ServerErrorSvg} {...props} />
- );
- 
- const NotFound = ({ title, message, onBack }: Partial<Pick<ScreenBlockProps, 'title' | 'message' | 'onBack'>>) => (
-     <ScreenBlock
-         title={title || '404'}
-         image={NotFoundSvg}
--        message={message || 'The requested resource was not found.'}
-+        message={message || 'Die angeforderte Seite wurde nicht gefunden.'}
-         onBack={onBack}
-     />
- );
-diff --git a/resources/scripts/components/elements/activity/ActivityLogEntry.tsx b/resources/scripts/components/elements/activity/ActivityLogEntry.tsx
-index 183b2ab..58e015b 100644
---- a/resources/scripts/components/elements/activity/ActivityLogEntry.tsx
-+++ b/resources/scripts/components/elements/activity/ActivityLogEntry.tsx
-@@ -11,6 +11,7 @@ import style from './style.module.css';
- import Avatar from '@/components/Avatar';
- import useLocationHash from '@/plugins/useLocationHash';
- import { getObjectKeys, isObject } from '@/lib/objects';
-+import { de } from 'date-fns/locale';
- 
- interface Props {
-     activity: ActivityLog;
-@@ -53,7 +54,7 @@ export default ({ activity, children }: Props) => {
-             <div className={'col-span-10 sm:col-span-9 flex'}>
-                 <div className={'flex-1 px-4 sm:px-0'}>
-                     <div className={'flex items-center text-gray-50'}>
--                        <Tooltip placement={'top'} content={actor?.email || 'System User'}>
-+                        <Tooltip placement={'top'} content={actor?.email || 'System Nutzer'}>
-                             <span>{actor?.username || 'System'}</span>
-                         </Tooltip>
-                         <span className={'text-gray-400'}>&nbsp;&mdash;&nbsp;</span>
-@@ -65,12 +66,12 @@ export default ({ activity, children }: Props) => {
-                         </Link>
-                         <div className={classNames(style.icons, 'group-hover:text-gray-300')}>
-                             {activity.isApi && (
--                                <Tooltip placement={'top'} content={'Using API Key'}>
-+                                <Tooltip placement={'top'} content={'API-Schlüssel verwendet'}>
-                                     <TerminalIcon />
-                                 </Tooltip>
-                             )}
-                             {activity.event.startsWith('server:sftp.') && (
--                                <Tooltip placement={'top'} content={'Using SFTP'}>
-+                                <Tooltip placement={'top'} content={'SFTP verwendet'}>
-                                     <FolderOpenIcon />
-                                 </Tooltip>
-                             )}
-@@ -87,8 +88,8 @@ export default ({ activity, children }: Props) => {
-                                 <span className={'text-gray-400'}>&nbsp;|&nbsp;</span>
-                             </span>
-                         )}
--                        <Tooltip placement={'right'} content={format(activity.timestamp, 'MMM do, yyyy H:mm:ss')}>
--                            <span>{formatDistanceToNowStrict(activity.timestamp, { addSuffix: true })}</span>
-+                        <Tooltip placement={'right'} content={format(activity.timestamp, 'do MMM yyyy, H:mm:ss', {locale: de})}>
-+                            <span>{formatDistanceToNowStrict(activity.timestamp, { locale: de, addSuffix: true })}</span>
-                         </Tooltip>
-                     </div>
-                 </div>
-diff --git a/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx b/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx
-index f1460ac..1c15e53 100644
---- a/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx
-+++ b/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx
-@@ -8,7 +8,7 @@ export default ({ meta }: { meta: Record<string, unknown> }) => {
- 
-     return (
-         <div className={'self-center md:px-4'}>
--            <Dialog open={open} onClose={() => setOpen(false)} hideCloseIcon title={'Metadata'}>
-+            <Dialog open={open} onClose={() => setOpen(false)} hideCloseIcon title={'Metadaten'}>
-                 <pre
-                     className={
-                         'bg-gray-900 rounded p-2 font-mono text-sm leading-relaxed overflow-x-scroll whitespace-pre-wrap'
-@@ -17,11 +17,11 @@ export default ({ meta }: { meta: Record<string, unknown> }) => {
-                     {JSON.stringify(meta, null, 2)}
-                 </pre>
-                 <Dialog.Footer>
--                    <Button.Text onClick={() => setOpen(false)}>Close</Button.Text>
-+                    <Button.Text onClick={() => setOpen(false)}>Schliessen</Button.Text>
-                 </Dialog.Footer>
-             </Dialog>
-             <button
--                aria-describedby={'View additional event metadata'}
-+                aria-describedby={'Weitere Daten anzeigen'}
-                 className={
-                     'p-2 transition-colors duration-100 text-gray-400 group-hover:text-gray-300 group-hover:hover:text-gray-50'
-                 }
-diff --git a/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx b/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx
-index bedfe4e..c7bd28b 100644
---- a/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx
-+++ b/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx
-@@ -13,7 +13,7 @@ export default ({ confirm = 'Okay', children, onConfirmed, ...props }: Confirmat
-         <Dialog {...props} description={typeof children === 'string' ? children : undefined}>
-             {typeof children !== 'string' && children}
-             <Dialog.Footer>
--                <Button.Text onClick={props.onClose}>Cancel</Button.Text>
-+                <Button.Text onClick={props.onClose}>Abbrechen</Button.Text>
-                 <Button.Danger onClick={onConfirmed}>{confirm}</Button.Danger>
-             </Dialog.Footer>
-         </Dialog>
-diff --git a/resources/scripts/components/elements/table/PaginationFooter.tsx b/resources/scripts/components/elements/table/PaginationFooter.tsx
-index 0484065..6defb0a 100644
---- a/resources/scripts/components/elements/table/PaginationFooter.tsx
-+++ b/resources/scripts/components/elements/table/PaginationFooter.tsx
-@@ -40,13 +40,13 @@ const PaginationFooter = ({ pagination, className, onPageSelect }: Props) => {
-     return (
-         <div className={classNames('flex items-center justify-between my-2', className)}>
-             <p className={'text-sm text-neutral-500'}>
--                Showing&nbsp;
-+                Zeige&nbsp;
-                 <span className={'font-semibold text-neutral-400'}>
-                     {Math.max(start, Math.min(pagination.total, 1))}
-                 </span>
--                &nbsp;to&nbsp;
--                <span className={'font-semibold text-neutral-400'}>{end}</span> of&nbsp;
--                <span className={'font-semibold text-neutral-400'}>{pagination.total}</span> results.
-+                &nbsp;von&nbsp;
-+                <span className={'font-semibold text-neutral-400'}>{end}</span> mit insgesamt&nbsp;
-+                <span className={'font-semibold text-neutral-400'}>{pagination.total}</span> Ergebnissen.
-             </p>
-             {pagination.totalPages > 1 && (
-                 <div className={'flex space-x-1'}>
-diff --git a/resources/scripts/components/server/ConflictStateRenderer.tsx b/resources/scripts/components/server/ConflictStateRenderer.tsx
-index d090b73..8adc74e 100644
---- a/resources/scripts/components/server/ConflictStateRenderer.tsx
-+++ b/resources/scripts/components/server/ConflictStateRenderer.tsx
-@@ -14,30 +14,30 @@ export default () => {
- 
-     return status === 'installing' || status === 'install_failed' || status === 'reinstall_failed' ? (
-         <ScreenBlock
--            title={'Running Installer'}
-+            title={'Installation läuft'}
-             image={ServerInstallSvg}
--            message={'Your server should be ready soon, please try again in a few minutes.'}
-+            message={'Dein Server wird gerade aufgesetzt, das kann einige Minuten dauern. Hol dir ne tüte Popcorn und warte auf den First Start :)'}
-         />
-     ) : status === 'suspended' ? (
-         <ScreenBlock
--            title={'Server Suspended'}
-+            title={'Server gesperrt'}
-             image={ServerErrorSvg}
--            message={'This server is suspended and cannot be accessed.'}
-+            message={'Der Zugang zum Server ist aktuell gesperrt. Melde dich beim Betreiber, um das Problem zu lösen.'}
-         />
-     ) : isNodeUnderMaintenance ? (
-         <ScreenBlock
--            title={'Node under Maintenance'}
-+            title={'Node ist offline'}
-             image={ServerErrorSvg}
--            message={'The node of this server is currently under maintenance.'}
-+            message={'Der Node ist aktuell in Wartungsarbeiten, dein Server steht daher gerade nicht zur Verfügung.'}
-         />
-     ) : (
-         <ScreenBlock
--            title={isTransferring ? 'Transferring' : 'Restoring from Backup'}
-+            title={isTransferring ? 'Transfer läuft...' : 'Backup wiederherstellen...'}
-             image={ServerRestoreSvg}
-             message={
-                 isTransferring
--                    ? 'Your server is being transferred to a new node, please check back later.'
--                    : 'Your server is currently being restored from a backup, please check back in a few minutes.'
-+                    ? 'Dein Server wird aktuell auf den neuen Node übertragen, das kann etwas dauern.'
-+                    : 'Dein Server wird aktuell mit einem Backup wiederhergestellt. Das kann je nach Größe des Servers etwas dauern.'
-             }
-         />
-     );
-diff --git a/resources/scripts/components/server/ServerActivityLogContainer.tsx b/resources/scripts/components/server/ServerActivityLogContainer.tsx
-index 5319454..f7eaac1 100644
---- a/resources/scripts/components/server/ServerActivityLogContainer.tsx
-+++ b/resources/scripts/components/server/ServerActivityLogContainer.tsx
-@@ -32,7 +32,7 @@ export default () => {
-     }, [error]);
- 
-     return (
--        <ServerContentBlock title={'Activity Log'}>
-+        <ServerContentBlock title={'Alle Aktivitäten'}>
-             <FlashMessageRender byKey={'server:activity'} />
-             {(filters.filters?.event || filters.filters?.ip) && (
-                 <div className={'flex justify-end mb-2'}>
-@@ -41,14 +41,14 @@ export default () => {
-                         className={classNames(btnStyles.button, btnStyles.text, 'w-full sm:w-auto')}
-                         onClick={() => setFilters((value) => ({ ...value, filters: {} }))}
-                     >
--                        Clear Filters <XCircleIcon className={'w-4 h-4 ml-2'} />
-+                        Filter lösen <XCircleIcon className={'w-4 h-4 ml-2'} />
-                     </Link>
-                 </div>
-             )}
-             {!data && isValidating ? (
-                 <Spinner centered />
-             ) : !data?.items.length ? (
--                <p className={'text-sm text-center text-gray-400'}>No activity logs available for this server.</p>
-+                <p className={'text-sm text-center text-gray-400'}>Hier werden alle Aktivitäten angezeigt.</p>
-             ) : (
-                 <div className={'bg-gray-700'}>
-                     {data?.items.map((activity) => (
-diff --git a/resources/scripts/components/server/UptimeDuration.tsx b/resources/scripts/components/server/UptimeDuration.tsx
-index 6623de0..d54717e 100644
---- a/resources/scripts/components/server/UptimeDuration.tsx
-+++ b/resources/scripts/components/server/UptimeDuration.tsx
-@@ -10,14 +10,14 @@ export default ({ uptime }: { uptime: number }) => {
-     if (days > 0) {
-         return (
-             <>
--                {days}d {hours}h {minutes}m
-+                {days}T {hours}Std. {minutes}min.
-             </>
-         );
-     }
- 
-     return (
-         <>
--            {hours}h {minutes}m {seconds}s
-+            {hours}Std. {minutes}min. {seconds}sek.
-         </>
-     );
- };
-diff --git a/resources/scripts/components/server/WebsocketHandler.tsx b/resources/scripts/components/server/WebsocketHandler.tsx
-index 2772013..47a14f7 100644
---- a/resources/scripts/components/server/WebsocketHandler.tsx
-+++ b/resources/scripts/components/server/WebsocketHandler.tsx
-@@ -54,7 +54,7 @@ export default () => {
-                 updateToken(uuid, socket);
-             } else {
-                 setError(
--                    'There was an error validating the credentials provided for the websocket. Please refresh the page.'
-+                    'Beim Anmelden ist ein Fehler aufgetreten. Bitte lade die Seite neu.'
-                 );
-             }
-         });
-@@ -112,7 +112,7 @@ export default () => {
-                         <>
-                             <Spinner size={'small'} />
-                             <p css={tw`ml-2 text-sm text-red-100`}>
--                                We&apos;re having some trouble connecting to your server, please wait...
-+                                Aktuell gibt es Probleme, den Node-Server zu erreichen. Wir versuchen es weiter...
-                             </p>
-                         </>
-                     ) : (
-diff --git a/resources/scripts/components/server/backups/BackupContainer.tsx b/resources/scripts/components/server/backups/BackupContainer.tsx
-index 0134142..8d74854 100644
---- a/resources/scripts/components/server/backups/BackupContainer.tsx
-+++ b/resources/scripts/components/server/backups/BackupContainer.tsx
-@@ -43,8 +43,8 @@ const BackupContainer = () => {
-                         !backupLimit ? null : (
-                             <p css={tw`text-center text-sm text-neutral-300`}>
-                                 {page > 1
--                                    ? "Looks like we've run out of backups to show you, try going back a page."
--                                    : 'It looks like there are no backups currently stored for this server.'}
-+                                    ? "Anscheinend gibt es gerade keine Backups, die verfügbar sind. Versuch es später nochmal."
-+                                    : 'Aktuell gibt es keine Backups für diesen Server'}
-                             </p>
-                         )
-                     ) : (
-@@ -56,14 +56,14 @@ const BackupContainer = () => {
-             </Pagination>
-             {backupLimit === 0 && (
-                 <p css={tw`text-center text-sm text-neutral-300`}>
--                    Backups cannot be created for this server because the backup limit is set to 0.
-+                    Aktuell wird nicht erlaubt, das Backups für diesen Server erstellt werden dürfen.
-                 </p>
-             )}
-             <Can action={'backup.create'}>
-                 <div css={tw`mt-6 sm:flex items-center justify-end`}>
-                     {backupLimit > 0 && backups.backupCount > 0 && (
-                         <p css={tw`text-sm text-neutral-300 mb-4 sm:mr-6 sm:mb-0`}>
--                            {backups.backupCount} of {backupLimit} backups have been created for this server.
-+                            {backups.backupCount} Backups sind verfügbar ({backupLimit} ist Maximum)
-                         </p>
-                     )}
-                     {backupLimit > 0 && backupLimit > backups.backupCount && (
-diff --git a/resources/scripts/components/server/backups/BackupContextMenu.tsx b/resources/scripts/components/server/backups/BackupContextMenu.tsx
-index 43c8b35..374cc85 100644
---- a/resources/scripts/components/server/backups/BackupContextMenu.tsx
-+++ b/resources/scripts/components/server/backups/BackupContextMenu.tsx
-@@ -122,21 +122,22 @@ export default ({ backup }: Props) => {
-             <Dialog.Confirm
-                 open={modal === 'unlock'}
-                 onClose={() => setModal('')}
--                title={`Unlock "${backup.name}"`}
-+                title={`Wirklich "${backup.name}" entsperren?`}
-                 onConfirmed={onLockToggle}
-             >
--                This backup will no longer be protected from automated or accidental deletions.
-+                Wenn du dies freigibst, wird das Backup nicht mehr vor versehntlicher Löschung geschützt.
-             </Dialog.Confirm>
-             <Dialog.Confirm
-                 open={modal === 'restore'}
-                 onClose={() => setModal('')}
--                confirm={'Restore'}
--                title={`Restore "${backup.name}"`}
-+                confirm={'Wiederherstellen'}
-+                title={`Backup "${backup.name}" anwenden?`}
-                 onConfirmed={() => doRestorationAction()}
-             >
-                 <p>
--                    Your server will be stopped. You will not be able to control the power state, access the file
--                    manager, or create additional backups until completed.
-+                    Dein Server wird dabei gestoppt! Im laufenden Prozess hast du keine Rechte, 
-+					um etwas am Server zu tun. Wenn du auf Nummer sicher gehen möchtest, erstelle vorher ein Backup, 
-+					falls diese Wiederherstellung schief geht.
-                 </p>
-                 <p css={tw`mt-4 -mb-2 bg-gray-700 p-3 rounded`}>
-                     <label htmlFor={'restore_truncate'} css={tw`text-base flex items-center cursor-pointer`}>
-@@ -148,18 +149,18 @@ export default ({ backup }: Props) => {
-                             checked={truncate}
-                             onChange={() => setTruncate((s) => !s)}
-                         />
--                        Delete all files before restoring backup.
-+                        Alle Daten vor dem Backup löschen
-                     </label>
-                 </p>
-             </Dialog.Confirm>
-             <Dialog.Confirm
--                title={`Delete "${backup.name}"`}
--                confirm={'Continue'}
-+                title={`Backup "${backup.name}" löschen?`}
-+                confirm={'Bestätigen'}
-                 open={modal === 'delete'}
-                 onClose={() => setModal('')}
-                 onConfirmed={doDeletion}
-             >
--                This is a permanent operation. The backup cannot be recovered once deleted.
-+                Das Backup kann danach nicht wiederhergestellt werden. Tue dies mit bedacht!
-             </Dialog.Confirm>
-             <SpinnerOverlay visible={loading} fixed />
-             {backup.isSuccessful ? (
-@@ -177,13 +178,13 @@ export default ({ backup }: Props) => {
-                         <Can action={'backup.download'}>
-                             <DropdownButtonRow onClick={doDownload}>
-                                 <FontAwesomeIcon fixedWidth icon={faCloudDownloadAlt} css={tw`text-xs`} />
--                                <span css={tw`ml-2`}>Download</span>
-+                                <span css={tw`ml-2`}>Herunterladen</span>
-                             </DropdownButtonRow>
-                         </Can>
-                         <Can action={'backup.restore'}>
-                             <DropdownButtonRow onClick={() => setModal('restore')}>
-                                 <FontAwesomeIcon fixedWidth icon={faBoxOpen} css={tw`text-xs`} />
--                                <span css={tw`ml-2`}>Restore</span>
-+                                <span css={tw`ml-2`}>Wiederherstellen</span>
-                             </DropdownButtonRow>
-                         </Can>
-                         <Can action={'backup.delete'}>
-@@ -194,12 +195,12 @@ export default ({ backup }: Props) => {
-                                         icon={backup.isLocked ? faUnlock : faLock}
-                                         css={tw`text-xs mr-2`}
-                                     />
--                                    {backup.isLocked ? 'Unlock' : 'Lock'}
-+                                    {backup.isLocked ? 'Entsperren' : 'Sperren'}
-                                 </DropdownButtonRow>
-                                 {!backup.isLocked && (
-                                     <DropdownButtonRow danger onClick={() => setModal('delete')}>
-                                         <FontAwesomeIcon fixedWidth icon={faTrashAlt} css={tw`text-xs`} />
--                                        <span css={tw`ml-2`}>Delete</span>
-+                                        <span css={tw`ml-2`}>Löschen</span>
-                                     </DropdownButtonRow>
-                                 )}
-                             </>
-diff --git a/resources/scripts/components/server/backups/BackupRow.tsx b/resources/scripts/components/server/backups/BackupRow.tsx
-index f59f472..12aea65 100644
---- a/resources/scripts/components/server/backups/BackupRow.tsx
-+++ b/resources/scripts/components/server/backups/BackupRow.tsx
-@@ -12,6 +12,7 @@ import GreyRowBox from '@/components/elements/GreyRowBox';
- import getServerBackups from '@/api/swr/getServerBackups';
- import { ServerBackup } from '@/api/server/types';
- import { SocketEvent } from '@/components/server/events';
-+import { de } from "date-fns/locale";
- 
- interface Props {
-     backup: ServerBackup;
-@@ -67,7 +68,7 @@ export default ({ backup, className }: Props) => {
-                             <span
-                                 css={tw`bg-red-500 py-px px-2 rounded-full text-white text-xs uppercase border border-red-600 mr-2`}
-                             >
--                                Failed
-+                                FEHLER
-                             </span>
-                         )}
-                         <p css={tw`break-words truncate`}>{backup.name}</p>
-@@ -81,10 +82,10 @@ export default ({ backup, className }: Props) => {
-                 </div>
-             </div>
-             <div css={tw`flex-1 md:flex-none md:w-48 mt-4 md:mt-0 md:ml-8 md:text-center`}>
--                <p title={format(backup.createdAt, 'ddd, MMMM do, yyyy HH:mm:ss')} css={tw`text-sm`}>
--                    {formatDistanceToNow(backup.createdAt, { includeSeconds: true, addSuffix: true })}
-+                <p title={format(backup.createdAt, 'do MMMM yyyy ,HH:mm:ss', { locale: de })} css={tw`text-sm`}>
-+                    {formatDistanceToNow(backup.createdAt, { locale: de, includeSeconds: true, addSuffix: true })}
-                 </p>
--                <p css={tw`text-2xs text-neutral-500 uppercase mt-1`}>Created</p>
-+                <p css={tw`text-2xs text-neutral-500 uppercase mt-1`}>Erstellt</p>
-             </div>
-             <Can action={['backup.download', 'backup.restore', 'backup.delete']} matchAny>
-                 <div css={tw`mt-4 md:mt-0 ml-6`} style={{ marginRight: '-0.5rem' }}>
-diff --git a/resources/scripts/components/server/backups/CreateBackupButton.tsx b/resources/scripts/components/server/backups/CreateBackupButton.tsx
-index fef0150..6884837 100644
---- a/resources/scripts/components/server/backups/CreateBackupButton.tsx
-+++ b/resources/scripts/components/server/backups/CreateBackupButton.tsx
-@@ -28,21 +28,21 @@ const ModalContent = ({ ...props }: RequiredModalProps) => {
-         <Modal {...props} showSpinnerOverlay={isSubmitting}>
-             <Form>
-                 <FlashMessageRender byKey={'backups:create'} css={tw`mb-4`} />
--                <h2 css={tw`text-2xl mb-6`}>Create server backup</h2>
-+                <h2 css={tw`text-2xl mb-6`}>Backup erstellen</h2>
-                 <Field
-                     name={'name'}
--                    label={'Backup name'}
--                    description={'If provided, the name that should be used to reference this backup.'}
-+                    label={'Name des Backups'}
-+                    description={'Wenn du es leer lässt, wird ein Zeitstempel als Name genutzt'}
-                 />
-                 <div css={tw`mt-6`}>
-                     <FormikFieldWrapper
-                         name={'ignored'}
--                        label={'Ignored Files & Directories'}
-+                        label={'Dateien & Ordner ingorieren'}
-                         description={`
--                            Enter the files or folders to ignore while generating this backup. Leave blank to use
--                            the contents of the .pteroignore file in the root of the server directory if present.
--                            Wildcard matching of files and folders is supported in addition to negating a rule by
--                            prefixing the path with an exclamation point.
-+                            Gib die Dateien oder Ordner ein, die beim Erstellen dieser Sicherung ignoriert werden sollen. Kein Pflichtfeld.
-+                            den Inhalt der Datei .pteroignore im Stammverzeichnis des Serververzeichnisses, falls vorhanden.
-+                            Der Wildcard-Abgleich von Dateien und Ordnern wird zusätzlich zum Negieren einer Regel durch unterstützt
-+                            dem Pfad ein Ausrufezeichen voranstellen.
-                         `}
-                     >
-                         <FormikField as={Textarea} name={'ignored'} rows={6} />
-@@ -52,14 +52,14 @@ const ModalContent = ({ ...props }: RequiredModalProps) => {
-                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
-                         <FormikSwitch
-                             name={'isLocked'}
--                            label={'Locked'}
--                            description={'Prevents this backup from being deleted until explicitly unlocked.'}
-+                            label={'Backup schützen'}
-+                            description={'Schützt das Backup vor versehentlichen Löschen, dies kann später deaktiviert werden.'}
-                         />
-                     </div>
-                 </Can>
-                 <div css={tw`flex justify-end mt-6`}>
-                     <Button type={'submit'} disabled={isSubmitting}>
--                        Start backup
-+                        Backup erstellen
-                     </Button>
-                 </div>
-             </Form>
-@@ -109,7 +109,7 @@ export default () => {
-                 </Formik>
-             )}
-             <Button css={tw`w-full sm:w-auto`} onClick={() => setVisible(true)}>
--                Create backup
-+                Backup erstellen
-             </Button>
-         </>
-     );
-diff --git a/resources/scripts/components/server/console/Console.tsx b/resources/scripts/components/server/console/Console.tsx
-index 25f0743..ef815ba 100644
---- a/resources/scripts/components/server/console/Console.tsx
-+++ b/resources/scripts/components/server/console/Console.tsx
-@@ -52,7 +52,7 @@ const terminalProps: ITerminalOptions = {
- };
- 
- export default () => {
--    const TERMINAL_PRELUDE = '\u001b[1m\u001b[33mcontainer@pterodactyl~ \u001b[0m';
-+    const TERMINAL_PRELUDE = '\u001b[1m\u001b[33mcontainer@server-verwaltung~ \u001b[0m';
-     const ref = useRef<HTMLDivElement>(null);
-     const terminal = useMemo(() => new Terminal({ ...terminalProps }), []);
-     const fitAddon = new FitAddon();
-@@ -79,7 +79,7 @@ export default () => {
-         switch (status) {
-             // Sent by either the source or target node if a failure occurs.
-             case 'failure':
--                terminal.writeln(TERMINAL_PRELUDE + 'Transfer has failed.\u001b[0m');
-+                terminal.writeln(TERMINAL_PRELUDE + 'Transfer ist fehlgeschlagen.\u001b[0m');
-                 return;
-         }
-     };
-@@ -90,7 +90,7 @@ export default () => {
-         );
- 
-     const handlePowerChangeEvent = (state: string) =>
--        terminal.writeln(TERMINAL_PRELUDE + 'Server marked as ' + state + '...\u001b[0m');
-+        terminal.writeln(TERMINAL_PRELUDE + 'Server meldet den Status ' + state + '...\u001b[0m');
- 
-     const handleCommandKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
-         if (e.key === 'ArrowUp') {
-@@ -206,8 +206,8 @@ export default () => {
-                     <input
-                         className={classNames('peer', styles.command_input)}
-                         type={'text'}
--                        placeholder={'Type a command...'}
--                        aria-label={'Console command input.'}
-+                        placeholder={'Befehl eingeben...'}
-+                        aria-label={'Die Macht ist so fern, und doch so nah...'}
-                         disabled={!instance || !connected}
-                         onKeyDown={handleCommandKeyDown}
-                         autoCorrect={'off'}
-diff --git a/resources/scripts/components/server/console/PowerButtons.tsx b/resources/scripts/components/server/console/PowerButtons.tsx
-index f5f8189..bed4591 100644
---- a/resources/scripts/components/server/console/PowerButtons.tsx
-+++ b/resources/scripts/components/server/console/PowerButtons.tsx
-@@ -42,11 +42,11 @@ export default ({ className }: PowerButtonProps) => {
-                 open={open}
-                 hideCloseIcon
-                 onClose={() => setOpen(false)}
--                title={'Forcibly Stop Process'}
--                confirm={'Continue'}
-+                title={'WARNUNG'}
-+                confirm={'Fortfahren'}
-                 onConfirmed={onButtonClick.bind(this, 'kill-confirmed')}
-             >
--                Forcibly stopping a server can lead to data corruption.
-+                Das erzwingen kann Daten zerstören, da in diesem Vorgang im Grunde der Stecker gezogen wird, wo der Server überhaupt nicht mit rechnet! Es können Daten verloren gehen.
-             </Dialog.Confirm>
-             <Can action={'control.start'}>
-                 <Button
-@@ -54,12 +54,12 @@ export default ({ className }: PowerButtonProps) => {
-                     disabled={status !== 'offline'}
-                     onClick={onButtonClick.bind(this, 'start')}
-                 >
--                    Start
-+                    Starten
-                 </Button>
-             </Can>
-             <Can action={'control.restart'}>
-                 <Button.Text className={'flex-1'} disabled={!status} onClick={onButtonClick.bind(this, 'restart')}>
--                    Restart
-+                    Neustart
-                 </Button.Text>
-             </Can>
-             <Can action={'control.stop'}>
-@@ -68,7 +68,7 @@ export default ({ className }: PowerButtonProps) => {
-                     disabled={status === 'offline'}
-                     onClick={onButtonClick.bind(this, killable ? 'kill' : 'stop')}
-                 >
--                    {killable ? 'Kill' : 'Stop'}
-+                    {killable ? 'Stop erzwingen' : 'Stoppen'}
-                 </Button.Danger>
-             </Can>
-         </div>
-diff --git a/resources/scripts/components/server/console/ServerConsoleContainer.tsx b/resources/scripts/components/server/console/ServerConsoleContainer.tsx
-index c3af78f..ea82c34 100644
---- a/resources/scripts/components/server/console/ServerConsoleContainer.tsx
-+++ b/resources/scripts/components/server/console/ServerConsoleContainer.tsx
-@@ -22,14 +22,14 @@ const ServerConsoleContainer = () => {
-     const isNodeUnderMaintenance = ServerContext.useStoreState((state) => state.server.data!.isNodeUnderMaintenance);
- 
-     return (
--        <ServerContentBlock title={'Console'}>
-+        <ServerContentBlock title={'Konsole'}>
-             {(isNodeUnderMaintenance || isInstalling || isTransferring) && (
-                 <Alert type={'warning'} className={'mb-4'}>
-                     {isNodeUnderMaintenance
--                        ? 'The node of this server is currently under maintenance and all actions are unavailable.'
-+                        ? 'Der Node, mit dem dein Server verbunden ist, befindet sich aktuell in Wartung. Dein Server bleibt solange offline.'
-                         : isInstalling
--                        ? 'This server is currently running its installation process and most actions are unavailable.'
--                        : 'This server is currently being transferred to another node and all actions are unavailable.'}
-+                        ? 'Dein Server hat gerade einen aktiven Installationsprozess. In der Zeit sind einige Aktionen nicht verfügbar.'
-+                        : 'Dein Server wird gerade auf einen anderen Node übertragen, das kann etwas dauern. In der Zeit kannst du nichts tun.'}
-                 </Alert>
-             )}
-             <div className={'grid grid-cols-4 gap-4 mb-4'}>
-diff --git a/resources/scripts/components/server/console/ServerDetailsBlock.tsx b/resources/scripts/components/server/console/ServerDetailsBlock.tsx
-index a3a0096..872208e 100644
---- a/resources/scripts/components/server/console/ServerDetailsBlock.tsx
-+++ b/resources/scripts/components/server/console/ServerDetailsBlock.tsx
-@@ -90,23 +90,25 @@ const ServerDetailsBlock = ({ className }: { className?: string }) => {
- 
-     return (
-         <div className={classNames('grid grid-cols-6 gap-2 md:gap-4', className)}>
--            <StatBlock icon={faWifi} title={'Address'} copyOnClick={allocation}>
-+            <StatBlock icon={faWifi} title={'Server-Zugang'} copyOnClick={allocation}>
-                 {allocation}
-             </StatBlock>
-             <StatBlock
-                 icon={faClock}
--                title={'Uptime'}
-+                title={'Laufzeit'}
-                 color={getBackgroundColor(status === 'running' ? 0 : status !== 'offline' ? 9 : 10, 10)}
-             >
-                 {status === null ? (
-                     'Offline'
-                 ) : stats.uptime > 0 ? (
-                     <UptimeDuration uptime={stats.uptime / 1000} />
-+                ) : status === 'starting' ? (
-+                    'Startet'
-                 ) : (
-                     capitalize(status)
-                 )}
-             </StatBlock>
--            <StatBlock icon={faMicrochip} title={'CPU Load'} color={getBackgroundColor(stats.cpu, limits.cpu)}>
-+            <StatBlock icon={faMicrochip} title={'CPU-Auslastung'} color={getBackgroundColor(stats.cpu, limits.cpu)}>
-                 {status === 'offline' ? (
-                     <span className={'text-gray-400'}>Offline</span>
-                 ) : (
-@@ -115,7 +117,7 @@ const ServerDetailsBlock = ({ className }: { className?: string }) => {
-             </StatBlock>
-             <StatBlock
-                 icon={faMemory}
--                title={'Memory'}
-+                title={'Arbeitsspeicher'}
-                 color={getBackgroundColor(stats.memory / 1024, limits.memory * 1024)}
-             >
-                 {status === 'offline' ? (
-@@ -124,13 +126,13 @@ const ServerDetailsBlock = ({ className }: { className?: string }) => {
-                     <Limit limit={textLimits.memory}>{bytesToString(stats.memory)}</Limit>
-                 )}
-             </StatBlock>
--            <StatBlock icon={faHdd} title={'Disk'} color={getBackgroundColor(stats.disk / 1024, limits.disk * 1024)}>
-+            <StatBlock icon={faHdd} title={'Speicher'} color={getBackgroundColor(stats.disk / 1024, limits.disk * 1024)}>
-                 <Limit limit={textLimits.disk}>{bytesToString(stats.disk)}</Limit>
-             </StatBlock>
--            <StatBlock icon={faCloudDownloadAlt} title={'Network (Inbound)'}>
-+            <StatBlock icon={faCloudDownloadAlt} title={'Traffic (Eingehend)'}>
-                 {status === 'offline' ? <span className={'text-gray-400'}>Offline</span> : bytesToString(stats.rx)}
-             </StatBlock>
--            <StatBlock icon={faCloudUploadAlt} title={'Network (Outbound)'}>
-+            <StatBlock icon={faCloudUploadAlt} title={'Traffic (Ausgehend)'}>
-                 {status === 'offline' ? <span className={'text-gray-400'}>Offline</span> : bytesToString(stats.tx)}
-             </StatBlock>
-         </div>
-diff --git a/resources/scripts/components/server/console/StatGraphs.tsx b/resources/scripts/components/server/console/StatGraphs.tsx
-index 9af405b..237fc6f 100644
---- a/resources/scripts/components/server/console/StatGraphs.tsx
-+++ b/resources/scripts/components/server/console/StatGraphs.tsx
-@@ -17,8 +17,8 @@ export default () => {
-     const previous = useRef<Record<'tx' | 'rx', number>>({ tx: -1, rx: -1 });
- 
-     const cpu = useChartTickLabel('CPU', limits.cpu, '%', 2);
--    const memory = useChartTickLabel('Memory', limits.memory, 'MiB');
--    const network = useChart('Network', {
-+    const memory = useChartTickLabel('Arbeitsspeicher', limits.memory, 'MiB');
-+    const network = useChart('Traffic', {
-         sets: 2,
-         options: {
-             scales: {
-@@ -34,7 +34,7 @@ export default () => {
-         callback(opts, index) {
-             return {
-                 ...opts,
--                label: !index ? 'Network In' : 'Network Out',
-+                label: !index ? 'Traffic (Eingehend)' : 'Traffic (Ausgehend)',
-                 borderColor: !index ? theme('colors.cyan.400') : theme('colors.yellow.400'),
-                 backgroundColor: hexToRgba(!index ? theme('colors.cyan.700') : theme('colors.yellow.700'), 0.5),
-             };
-@@ -68,20 +68,20 @@ export default () => {
- 
-     return (
-         <>
--            <ChartBlock title={'CPU Load'}>
-+            <ChartBlock title={'CPU-Auslastung'}>
-                 <Line {...cpu.props} />
-             </ChartBlock>
--            <ChartBlock title={'Memory'}>
-+            <ChartBlock title={'Arbeitsspeicher'}>
-                 <Line {...memory.props} />
-             </ChartBlock>
-             <ChartBlock
--                title={'Network'}
-+                title={'Traffic'}
-                 legend={
-                     <>
--                        <Tooltip arrow content={'Inbound'}>
-+                        <Tooltip arrow content={'Eingehend'}>
-                             <CloudDownloadIcon className={'mr-2 w-4 h-4 text-yellow-400'} />
-                         </Tooltip>
--                        <Tooltip arrow content={'Outbound'}>
-+                        <Tooltip arrow content={'Ausgehend'}>
-                             <CloudUploadIcon className={'w-4 h-4 text-cyan-400'} />
-                         </Tooltip>
-                     </>
-diff --git a/resources/scripts/components/server/databases/CreateDatabaseButton.tsx b/resources/scripts/components/server/databases/CreateDatabaseButton.tsx
-index 24c19e0..02f59a7 100644
---- a/resources/scripts/components/server/databases/CreateDatabaseButton.tsx
-+++ b/resources/scripts/components/server/databases/CreateDatabaseButton.tsx
-@@ -18,14 +18,14 @@ interface Values {
- 
- const schema = object().shape({
-     databaseName: string()
--        .required('A database name must be provided.')
--        .min(3, 'Database name must be at least 3 characters.')
--        .max(48, 'Database name must not exceed 48 characters.')
-+        .required('Die Erstellung der Datenbank erfordert einen Namen.')
-+        .min(3, 'Es werden mindestens 3 Zeichen benötigt')
-+        .max(48, 'Es sind maximal 48 Zeichen erlaubt.')
-         .matches(
-             /^[\w\-.]{3,48}$/,
--            'Database name should only contain alphanumeric characters, underscores, dashes, and/or periods.'
-+            'Der Name darf nur alphanummerische Zeichen, Unterstriche, Bindestriche oder Punkte enthalten.'
-         ),
--    connectionsFrom: string().matches(/^[\w\-/.%:]+$/, 'A valid host address must be provided.'),
-+    connectionsFrom: string().matches(/^[\w\-/.%:]+$/, 'Es muss eine gültige Hostadresse angegeben werden.'),
- });
- 
- export default () => {
-@@ -69,23 +69,23 @@ export default () => {
-                         }}
-                     >
-                         <FlashMessageRender byKey={'database:create'} css={tw`mb-6`} />
--                        <h2 css={tw`text-2xl mb-6`}>Create new database</h2>
-+                        <h2 css={tw`text-2xl mb-6`}>Datenbank erstellen</h2>
-                         <Form css={tw`m-0`}>
-                             <Field
-                                 type={'string'}
-                                 id={'database_name'}
-                                 name={'databaseName'}
--                                label={'Database Name'}
--                                description={'A descriptive name for your database instance.'}
-+                                label={'Name der Datenbank'}
-+                                description={'Eine kurze Beschreibung in ein bis zwei Wörtern (am besten), für was die Datenbank gedacht ist.'}
-                             />
-                             <div css={tw`mt-6`}>
-                                 <Field
-                                     type={'string'}
-                                     id={'connections_from'}
-                                     name={'connectionsFrom'}
--                                    label={'Connections From'}
-+                                    label={'Verbindungen beschränkt erlauben'}
-                                     description={
--                                        'Where connections should be allowed from. Leave blank to allow connections from anywhere.'
-+                                        'Gibt vor, von wo Verbindungen zugelassen werden dürfen. Lasse das Feld leer, um alle Verbindungen zuzulassen.'
-                                     }
-                                 />
-                             </div>
-@@ -96,17 +96,17 @@ export default () => {
-                                     css={tw`w-full sm:w-auto sm:mr-2`}
-                                     onClick={() => setVisible(false)}
-                                 >
--                                    Cancel
-+                                    Abbrechen
-                                 </Button>
-                                 <Button css={tw`w-full mt-4 sm:w-auto sm:mt-0`} type={'submit'}>
--                                    Create Database
-+                                    Datenbank erstellen
-                                 </Button>
-                             </div>
-                         </Form>
-                     </Modal>
-                 )}
-             </Formik>
--            <Button onClick={() => setVisible(true)}>New Database</Button>
-+            <Button onClick={() => setVisible(true)}>Neue Datenbank</Button>
-         </>
-     );
- };
-diff --git a/resources/scripts/components/server/databases/DatabaseRow.tsx b/resources/scripts/components/server/databases/DatabaseRow.tsx
-index 9d2c05a..807eccb 100644
---- a/resources/scripts/components/server/databases/DatabaseRow.tsx
-+++ b/resources/scripts/components/server/databases/DatabaseRow.tsx
-@@ -40,8 +40,8 @@ export default ({ database, className }: Props) => {
- 
-     const schema = object().shape({
-         confirm: string()
--            .required('The database name must be provided.')
--            .oneOf([database.name.split('_', 2)[1], database.name], 'The database name must be provided.'),
-+            .required('Gib einen Namen für die Datenbank ein.')
-+            .oneOf([database.name.split('_', 2)[1], database.name], 'Name der Datenbank fehlt.'),
-     });
- 
-     const submit = (values: { confirm: string }, { setSubmitting }: FormikHelpers<{ confirm: string }>) => {
-@@ -72,25 +72,25 @@ export default ({ database, className }: Props) => {
-                         }}
-                     >
-                         <FlashMessageRender byKey={'database:delete'} css={tw`mb-6`} />
--                        <h2 css={tw`text-2xl mb-6`}>Confirm database deletion</h2>
-+                        <h2 css={tw`text-2xl mb-6`}>Datenbank löschen?</h2>
-                         <p css={tw`text-sm`}>
--                            Deleting a database is a permanent action, it cannot be undone. This will permanently delete
--                            the <strong>{database.name}</strong> database and remove all associated data.
-+                            Dadurch wird die Datenbank  <strong>{database.name}</strong>  unwiderruflich gelöscht. Bitte beachte, welche Plugins etc. mit der Datenbank verbunden sind!
-+							Trenne vor dem Löschen der Datenbank alle Verknüpfungen.
-                         </p>
-                         <Form css={tw`m-0 mt-6`}>
-                             <Field
-                                 type={'text'}
-                                 id={'confirm_name'}
-                                 name={'confirm'}
--                                label={'Confirm Database Name'}
--                                description={'Enter the database name to confirm deletion.'}
-+                                label={'Bestätige den Namen'}
-+                                description={'Gib den Namen der Datenbank ein, um die Löschung zu bestätigen'}
-                             />
-                             <div css={tw`mt-6 text-right`}>
-                                 <Button type={'button'} isSecondary css={tw`mr-2`} onClick={() => setVisible(false)}>
--                                    Cancel
-+                                    Abbrechen
-                                 </Button>
-                                 <Button type={'submit'} color={'red'} disabled={!isValid}>
--                                    Delete Database
-+                                    Datenbank löschen
-                                 </Button>
-                             </div>
-                         </Form>
-@@ -99,33 +99,33 @@ export default ({ database, className }: Props) => {
-             </Formik>
-             <Modal visible={connectionVisible} onDismissed={() => setConnectionVisible(false)}>
-                 <FlashMessageRender byKey={'database-connection-modal'} css={tw`mb-6`} />
--                <h3 css={tw`mb-6 text-2xl`}>Database connection details</h3>
-+                <h3 css={tw`mb-6 text-2xl`}>Details der Datenbank</h3>
-                 <div>
--                    <Label>Endpoint</Label>
-+                    <Label>Zugang</Label>
-                     <CopyOnClick text={database.connectionString}>
-                         <Input type={'text'} readOnly value={database.connectionString} />
-                     </CopyOnClick>
-                 </div>
-                 <div css={tw`mt-6`}>
--                    <Label>Connections from</Label>
-+                    <Label>Verbindungen von</Label>
-                     <Input type={'text'} readOnly value={database.allowConnectionsFrom} />
-                 </div>
-                 <div css={tw`mt-6`}>
--                    <Label>Username</Label>
-+                    <Label>Benutzername</Label>
-                     <CopyOnClick text={database.username}>
-                         <Input type={'text'} readOnly value={database.username} />
-                     </CopyOnClick>
-                 </div>
-                 <Can action={'database.view_password'}>
-                     <div css={tw`mt-6`}>
--                        <Label>Password</Label>
-+                        <Label>Passwort</Label>
-                         <CopyOnClick text={database.password}>
-                             <Input type={'text'} readOnly value={database.password} />
-                         </CopyOnClick>
-                     </div>
-                 </Can>
-                 <div css={tw`mt-6`}>
--                    <Label>JDBC Connection String</Label>
-+                    <Label>JDBC Verbindungen</Label>
-                     <CopyOnClick text={jdbcConnectionString}>
-                         <Input type={'text'} readOnly value={jdbcConnectionString} />
-                     </CopyOnClick>
-@@ -135,7 +135,7 @@ export default ({ database, className }: Props) => {
-                         <RotatePasswordButton databaseId={database.id} onUpdate={appendDatabase} />
-                     </Can>
-                     <Button isSecondary onClick={() => setConnectionVisible(false)}>
--                        Close
-+                        Schliessen
-                     </Button>
-                 </div>
-             </Modal>
-@@ -152,17 +152,17 @@ export default ({ database, className }: Props) => {
-                     <CopyOnClick text={database.connectionString}>
-                         <p css={tw`text-sm`}>{database.connectionString}</p>
-                     </CopyOnClick>
--                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Endpoint</p>
-+                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Endpunkt</p>
-                 </div>
-                 <div css={tw`ml-8 text-center hidden md:block`}>
-                     <p css={tw`text-sm`}>{database.allowConnectionsFrom}</p>
--                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Connections from</p>
-+                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Verbindungen von</p>
-                 </div>
-                 <div css={tw`ml-8 text-center hidden md:block`}>
-                     <CopyOnClick text={database.username}>
-                         <p css={tw`text-sm`}>{database.username}</p>
-                     </CopyOnClick>
--                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Username</p>
-+                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Benutzername</p>
-                 </div>
-                 <div css={tw`ml-8`}>
-                     <Button isSecondary css={tw`mr-2`} onClick={() => setConnectionVisible(true)}>
-diff --git a/resources/scripts/components/server/databases/DatabasesContainer.tsx b/resources/scripts/components/server/databases/DatabasesContainer.tsx
-index 16b6900..dfddf2f 100644
---- a/resources/scripts/components/server/databases/DatabasesContainer.tsx
-+++ b/resources/scripts/components/server/databases/DatabasesContainer.tsx
-@@ -37,7 +37,7 @@ export default () => {
-     }, []);
- 
-     return (
--        <ServerContentBlock title={'Databases'}>
-+        <ServerContentBlock title={'Datenbanken'}>
-             <FlashMessageRender byKey={'databases'} css={tw`mb-4`} />
-             {!databases.length && loading ? (
-                 <Spinner size={'large'} centered />
-@@ -55,16 +55,15 @@ export default () => {
-                         ) : (
-                             <p css={tw`text-center text-sm text-neutral-300`}>
-                                 {databaseLimit > 0
--                                    ? 'It looks like you have no databases.'
--                                    : 'Databases cannot be created for this server.'}
-+                                    ? 'Aktuell gibt es keine Datenbanken für deinen Server.'
-+                                    : 'Aktuell ist es auf deinem Server nicht erlaubt, Datenbanken zu erstellen.'}
-                             </p>
-                         )}
-                         <Can action={'database.create'}>
-                             <div css={tw`mt-6 flex items-center justify-end`}>
-                                 {databaseLimit > 0 && databases.length > 0 && (
-                                     <p css={tw`text-sm text-neutral-300 mb-4 sm:mr-6 sm:mb-0`}>
--                                        {databases.length} of {databaseLimit} databases have been allocated to this
--                                        server.
-+                                        {databases.length} Datenbanken sind aktiv ({databaseLimit} ist Maximum)
-                                     </p>
-                                 )}
-                                 {databaseLimit > 0 && databaseLimit !== databases.length && (
-diff --git a/resources/scripts/components/server/databases/RotatePasswordButton.tsx b/resources/scripts/components/server/databases/RotatePasswordButton.tsx
-index 0c687d7..28572fd 100644
---- a/resources/scripts/components/server/databases/RotatePasswordButton.tsx
-+++ b/resources/scripts/components/server/databases/RotatePasswordButton.tsx
-@@ -37,7 +37,7 @@ export default ({ databaseId, onUpdate }: { databaseId: string; onUpdate: (datab
- 
-     return (
-         <Button isSecondary color={'primary'} css={tw`mr-2`} onClick={rotate} isLoading={loading}>
--            Rotate Password
-+            Neues Passwort erstellen
-         </Button>
-     );
- };
-diff --git a/resources/scripts/components/server/features/GSLTokenModalFeature.tsx b/resources/scripts/components/server/features/GSLTokenModalFeature.tsx
-index f56ae7c..9017053 100644
---- a/resources/scripts/components/server/features/GSLTokenModalFeature.tsx
-+++ b/resources/scripts/components/server/features/GSLTokenModalFeature.tsx
-@@ -75,25 +75,25 @@ const GSLTokenModalFeature = () => {
-             >
-                 <FlashMessageRender key={'feature:gslToken'} css={tw`mb-4`} />
-                 <Form>
--                    <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Invalid GSL token!</h2>
-+                    <h2 css={tw`text-2xl mb-4 text-neutral-100`}>GSL Token ist ungültig!</h2>
-                     <p css={tw`mt-4`}>
--                        It seems like your Gameserver Login Token (GSL token) is invalid or has expired.
-+                        Es schaut so aus, als wäre dein GSL-Token entweder nicht gültig oder abgelaufen.
-                     </p>
-                     <p css={tw`mt-4`}>
--                        You can either generate a new one and enter it below or leave the field blank to remove it
--                        completely.
-+                        Du hast einen neuen generieren lassen, oder du lässt das Feld leer.
-+                        Demnach wird es dann entfernt.
-                     </p>
-                     <div css={tw`sm:flex items-center mt-4`}>
-                         <Field
-                             name={'gslToken'}
-                             label={'GSL Token'}
--                            description={'Visit https://steamcommunity.com/dev/managegameservers to generate a token.'}
-+                            description={'Besuche https://steamcommunity.com/dev/managegameservers um einen Token zu generieren.'}
-                             autoFocus
-                         />
-                     </div>
-                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
-                         <Button type={'submit'} css={tw`mt-4 sm:mt-0 sm:ml-4 w-full sm:w-auto`}>
--                            Update GSL Token
-+                            GSL-Token speichern
-                         </Button>
-                     </div>
-                 </Form>
-diff --git a/resources/scripts/components/server/features/JavaVersionModalFeature.tsx b/resources/scripts/components/server/features/JavaVersionModalFeature.tsx
-index 91902f0..e811355 100644
---- a/resources/scripts/components/server/features/JavaVersionModalFeature.tsx
-+++ b/resources/scripts/components/server/features/JavaVersionModalFeature.tsx
-@@ -76,11 +76,11 @@ const JavaVersionModalFeature = () => {
-             showSpinnerOverlay={loading}
-         >
-             <FlashMessageRender key={'feature:javaVersion'} css={tw`mb-4`} />
--            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Unsupported Java Version</h2>
-+            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Java-Version wird nicht unterstützt</h2>
-             <p css={tw`mt-4`}>
--                This server is currently running an unsupported version of Java and cannot be started.
-+                Dein Server läuft aktuell mit einer Java-Version, die nicht kompatibel ist.
-                 <Can action={'startup.docker-image'}>
--                    &nbsp;Please select a supported version from the list below to continue starting the server.
-+                    &nbsp;Wähle eine vefügbare Version, und starte dann erneut den Server.
-                 </Can>
-             </p>
-             <Can action={'startup.docker-image'}>
-diff --git a/resources/scripts/components/server/features/PIDLimitModalFeature.tsx b/resources/scripts/components/server/features/PIDLimitModalFeature.tsx
-index f4061bc..b3baf8e 100644
---- a/resources/scripts/components/server/features/PIDLimitModalFeature.tsx
-+++ b/resources/scripts/components/server/features/PIDLimitModalFeature.tsx
-@@ -60,20 +60,19 @@ const PIDLimitModalFeature = () => {
-                 <>
-                     <div css={tw`mt-4 sm:flex items-center`}>
-                         <FontAwesomeIcon css={tw`pr-4`} icon={faExclamationTriangle} color={'orange'} size={'4x'} />
--                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>Memory or process limit reached...</h2>
-+                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>RAM oder Prozess-Limit erreicht!</h2>
-                     </div>
--                    <p css={tw`mt-4`}>This server has reached the maximum process or memory limit.</p>
-+                    <p css={tw`mt-4`}>Dein Server hat den Limit des Servers erreicht, prüfe die Nutzung der Ressourcen oder führe ein Upgrade durch</p>
-                     <p css={tw`mt-4`}>
--                        Increasing <code css={tw`font-mono bg-neutral-900`}>container_pid_limit</code> in the wings
--                        configuration, <code css={tw`font-mono bg-neutral-900`}>config.yml</code>, might help resolve
--                        this issue.
-+                        Füge <code css={tw`font-mono bg-neutral-900`}>container_pid_limit</code> in die 
-+						in die Wings-Config (<code css={tw`font-mono bg-neutral-900`}>config.yml</code>), das kann das Problem lösen
-                     </p>
-                     <p css={tw`mt-4`}>
--                        <b>Note: Wings must be restarted for the configuration file changes to take effect</b>
-+                        <b>Info: Wings muss neu gestartet werden. Dies ist mit <code css={tw`font-mono bg-neutral-900`}>systemctl restart wings</code> möglich.</b>
-                     </p>
-                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
-                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
--                            Close
-+                            Schliessen
-                         </Button>
-                     </div>
-                 </>
-@@ -81,20 +80,19 @@ const PIDLimitModalFeature = () => {
-                 <>
-                     <div css={tw`mt-4 sm:flex items-center`}>
-                         <FontAwesomeIcon css={tw`pr-4`} icon={faExclamationTriangle} color={'orange'} size={'4x'} />
--                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Possible resource limit reached...</h2>
-+                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Limit erreicht!</h2>
-                     </div>
-                     <p css={tw`mt-4`}>
--                        This server is attempting to use more resources than allocated. Please contact the administrator
--                        and give them the error below.
-+                        Dieser Server hat mehr Ressourcen zugeteilt bekommen als verfügbar. Bitte kontaktiere den Administrator.
-                     </p>
-                     <p css={tw`mt-4`}>
-                         <code css={tw`font-mono bg-neutral-900`}>
--                            pthread_create failed, Possibly out of memory or process/resource limits reached
-+                            pthread_create failed, wahrscheinlich ist das Limit des RAMs oder der Prozesse erreicht worden.
-                         </code>
-                     </p>
-                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
-                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
--                            Close
-+                            Schliessen
-                         </Button>
-                     </div>
-                 </>
-diff --git a/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx b/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx
-index 81acba5..869c3e2 100644
---- a/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx
-+++ b/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx
-@@ -50,35 +50,33 @@ const SteamDiskSpaceFeature = () => {
-             {isAdmin ? (
-                 <>
-                     <div css={tw`mt-4 sm:flex items-center`}>
--                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>Out of available disk space...</h2>
-+                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>Der Speicher ist voll!</h2>
-                     </div>
-                     <p css={tw`mt-4`}>
--                        This server has run out of available disk space and cannot complete the install or update
--                        process.
-+                        Der Server hat den Speicherlimit erreicht. Erweitere deinen Speicherplatz oder entferne unnötige Dateien.
-                     </p>
-                     <p css={tw`mt-4`}>
--                        Ensure the machine has enough disk space by typing{' '}
--                        <code css={tw`font-mono bg-neutral-900 rounded py-1 px-2`}>df -h</code> on the machine hosting
--                        this server. Delete files or increase the available disk space to resolve the issue.
-+                        Sei versichert, das auf dem Server genug Speicherplatz beim ausführen verfügbar ist, das kannst du mit dem Befehl {' '}
-+                        <code css={tw`font-mono bg-neutral-900 rounded py-1 px-2`}>df -h</code> prüfen. Entferne unnötige Dateien oder Ordner, um Speicherplatz zu schaffen und das Problem zu beheben.
-                     </p>
-                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
-                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
--                            Close
-+                            Okay
-                         </Button>
-                     </div>
-                 </>
-             ) : (
-                 <>
-                     <div css={tw`mt-4 sm:flex items-center`}>
--                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Out of available disk space...</h2>
-+                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Der Speicher ist voll!</h2>
-                     </div>
-                     <p css={tw`mt-4`}>
--                        This server has run out of available disk space and cannot complete the install or update
--                        process. Please get in touch with the administrator(s) and inform them of disk space issues.
-+                        Dein Server hat den Speicherlimit erreicht,  es kann somit keine Installation oder Upgrade durchgeführt werden.
-+                        Bitte kontaktiere den Support des Systems, um das Problem zu beheben.
-                     </p>
-                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
-                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
--                            Close
-+                            Okay
-                         </Button>
-                     </div>
-                 </>
-diff --git a/resources/scripts/components/server/features/eula/EulaModalFeature.tsx b/resources/scripts/components/server/features/eula/EulaModalFeature.tsx
-index fd7afe1..fb9fe19 100644
---- a/resources/scripts/components/server/features/eula/EulaModalFeature.tsx
-+++ b/resources/scripts/components/server/features/eula/EulaModalFeature.tsx
-@@ -65,9 +65,9 @@ const EulaModalFeature = () => {
-             showSpinnerOverlay={loading}
-         >
-             <FlashMessageRender key={'feature:eula'} css={tw`mb-4`} />
--            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Accept Minecraft&reg; EULA</h2>
-+            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Akzeptiere die Minecraft&reg; EULA</h2>
-             <p css={tw`text-neutral-200`}>
--                By pressing {'"I Accept"'} below you are indicating your agreement to the&nbsp;
-+                Wenn du {'"Ja, ich will"'} anklickst, akzeptierst du die &nbsp;
-                 <a
-                     target={'_blank'}
-                     css={tw`text-primary-300 underline transition-colors duration-150 hover:text-primary-400`}
-@@ -80,10 +80,10 @@ const EulaModalFeature = () => {
-             </p>
-             <div css={tw`mt-8 sm:flex items-center justify-end`}>
-                 <Button isSecondary onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
--                    Cancel
-+                    Abbrechen
-                 </Button>
-                 <Button onClick={onAcceptEULA} css={tw`mt-4 sm:mt-0 sm:ml-4 w-full sm:w-auto`}>
--                    I Accept
-+                    Ja, ich will
-                 </Button>
-             </div>
-         </Modal>
-diff --git a/resources/scripts/components/server/files/ChmodFileModal.tsx b/resources/scripts/components/server/files/ChmodFileModal.tsx
-index 27a474d..e8f49f4 100644
---- a/resources/scripts/components/server/files/ChmodFileModal.tsx
-+++ b/resources/scripts/components/server/files/ChmodFileModal.tsx
-@@ -59,10 +59,10 @@ const ChmodFileModal = ({ files, ...props }: OwnProps) => {
-                     <Form css={tw`m-0`}>
-                         <div css={tw`flex flex-wrap items-end`}>
-                             <div css={tw`w-full sm:flex-1 sm:mr-4`}>
--                                <Field type={'string'} id={'file_mode'} name={'mode'} label={'File Mode'} autoFocus />
-+                                <Field type={'string'} id={'file_mode'} name={'mode'} label={'Rechte-Modus'} autoFocus />
-                             </div>
-                             <div css={tw`w-full sm:w-auto mt-4 sm:mt-0`}>
--                                <Button css={tw`w-full`}>Update</Button>
-+                                <Button css={tw`w-full`}>Speichern</Button>
-                             </div>
-                         </div>
-                     </Form>
-diff --git a/resources/scripts/components/server/files/FileDropdownMenu.tsx b/resources/scripts/components/server/files/FileDropdownMenu.tsx
-index fd89295..ff86a49 100644
---- a/resources/scripts/components/server/files/FileDropdownMenu.tsx
-+++ b/resources/scripts/components/server/files/FileDropdownMenu.tsx
-@@ -132,12 +132,12 @@ const FileDropdownMenu = ({ file }: { file: FileObject }) => {
-             <Dialog.Confirm
-                 open={showConfirmation}
-                 onClose={() => setShowConfirmation(false)}
--                title={`Delete ${file.isFile ? 'File' : 'Directory'}`}
--                confirm={'Delete'}
-+                title={`${file.isFile ? 'Datei' : 'Ordner'} löschen?`}
-+                confirm={'Löschen'}
-                 onConfirmed={doDeletion}
-             >
--                You will not be able to recover the contents of&nbsp;
--                <span className={'font-semibold text-gray-50'}>{file.name}</span> once deleted.
-+                Dadurch löschst du unwiderruflich of&nbsp;
-+                <span className={'font-semibold text-gray-50'}>{file.name}</span>. Bist du dir sicher?
-             </Dialog.Confirm>
-             <DropdownMenu
-                 ref={onClickRef}
-@@ -167,27 +167,27 @@ const FileDropdownMenu = ({ file }: { file: FileObject }) => {
-                 )}
-             >
-                 <Can action={'file.update'}>
--                    <Row onClick={() => setModal('rename')} icon={faPencilAlt} title={'Rename'} />
--                    <Row onClick={() => setModal('move')} icon={faLevelUpAlt} title={'Move'} />
--                    <Row onClick={() => setModal('chmod')} icon={faFileCode} title={'Permissions'} />
-+                    <Row onClick={() => setModal('rename')} icon={faPencilAlt} title={'Umbenennen'} />
-+                    <Row onClick={() => setModal('move')} icon={faLevelUpAlt} title={'Verschieben'} />
-+                    <Row onClick={() => setModal('chmod')} icon={faFileCode} title={'Berechtigungen'} />
-                 </Can>
-                 {file.isFile && (
-                     <Can action={'file.create'}>
--                        <Row onClick={doCopy} icon={faCopy} title={'Copy'} />
-+                        <Row onClick={doCopy} icon={faCopy} title={'Kopieren'} />
-                     </Can>
-                 )}
-                 {file.isArchiveType() ? (
-                     <Can action={'file.create'}>
--                        <Row onClick={doUnarchive} icon={faBoxOpen} title={'Unarchive'} />
-+                        <Row onClick={doUnarchive} icon={faBoxOpen} title={'Entpacken'} />
-                     </Can>
-                 ) : (
-                     <Can action={'file.archive'}>
--                        <Row onClick={doArchive} icon={faFileArchive} title={'Archive'} />
-+                        <Row onClick={doArchive} icon={faFileArchive} title={'Archivieren'} />
-                     </Can>
-                 )}
--                {file.isFile && <Row onClick={doDownload} icon={faFileDownload} title={'Download'} />}
-+                {file.isFile && <Row onClick={doDownload} icon={faFileDownload} title={'Herunterladen'} />}
-                 <Can action={'file.delete'}>
--                    <Row onClick={() => setShowConfirmation(true)} icon={faTrashAlt} title={'Delete'} $danger />
-+                    <Row onClick={() => setShowConfirmation(true)} icon={faTrashAlt} title={'Löschen'} $danger />
-                 </Can>
-             </DropdownMenu>
-         </>
-diff --git a/resources/scripts/components/server/files/FileEditContainer.tsx b/resources/scripts/components/server/files/FileEditContainer.tsx
-index 28ba91d..e2e94e4 100644
---- a/resources/scripts/components/server/files/FileEditContainer.tsx
-+++ b/resources/scripts/components/server/files/FileEditContainer.tsx
-@@ -94,10 +94,10 @@ export default () => {
-             {hash.replace(/^#/, '').endsWith('.pteroignore') && (
-                 <div css={tw`mb-4 p-4 border-l-4 bg-neutral-900 rounded border-cyan-400`}>
-                     <p css={tw`text-neutral-300 text-sm`}>
--                        You&apos;re editing a <code css={tw`font-mono bg-black rounded py-px px-1`}>.pteroignore</code>{' '}
--                        file. Any files or directories listed in here will be excluded from backups. Wildcards are
--                        supported by using an asterisk (<code css={tw`font-mono bg-black rounded py-px px-1`}>*</code>).
--                        You can negate a prior rule by prepending an exclamation point (
-+                        Du bearbeitest eine <code css={tw`font-mono bg-black rounded py-px px-1`}>.pteroignore</code>{' '}
-+                        Datei. Diese Datei zeigt dem Backup-System, das dieses Verzeichnis ausgenmommen werden soll. Wildcards sind
-+                        unterstützt durch die Verwendung eines Sternchens (<code css={tw`font-mono bg-black rounded py-px px-1`}>*</code>).
-+                        Du kannst eine vorherige Regel negieren, indem Sie ein Ausrufezeichen voranstellen (
-                         <code css={tw`font-mono bg-black rounded py-px px-1`}>!</code>).
-                     </p>
-                 </div>
-@@ -142,13 +142,13 @@ export default () => {
-                 {action === 'edit' ? (
-                     <Can action={'file.update'}>
-                         <Button css={tw`flex-1 sm:flex-none`} onClick={() => save()}>
--                            Save Content
-+                            Änderungen speichern
-                         </Button>
-                     </Can>
-                 ) : (
-                     <Can action={'file.create'}>
-                         <Button css={tw`flex-1 sm:flex-none`} onClick={() => setModalVisible(true)}>
--                            Create File
-+                            Datei erstellen
-                         </Button>
-                     </Can>
-                 )}
-diff --git a/resources/scripts/components/server/files/FileManagerContainer.tsx b/resources/scripts/components/server/files/FileManagerContainer.tsx
-index 3bebd20..d77e76e 100644
---- a/resources/scripts/components/server/files/FileManagerContainer.tsx
-+++ b/resources/scripts/components/server/files/FileManagerContainer.tsx
-@@ -79,7 +79,7 @@ export default () => {
-                             <NewDirectoryButton />
-                             <UploadButton />
-                             <NavLink to={`/server/${id}/files/new${window.location.hash}`}>
--                                <Button>New File</Button>
-+                                <Button>Neue Datei</Button>
-                             </NavLink>
-                         </div>
-                     </Can>
-@@ -90,15 +90,14 @@ export default () => {
-             ) : (
-                 <>
-                     {!files.length ? (
--                        <p css={tw`text-sm text-neutral-400 text-center`}>This directory seems to be empty.</p>
-+                        <p css={tw`text-sm text-neutral-400 text-center`}>Dieser Ordner ist leer.</p>
-                     ) : (
-                         <CSSTransition classNames={'fade'} timeout={150} appear in>
-                             <div>
-                                 {files.length > 250 && (
-                                     <div css={tw`rounded bg-yellow-400 mb-px p-3`}>
-                                         <p css={tw`text-yellow-900 text-sm text-center`}>
--                                            This directory is too large to display in the browser, limiting the output
--                                            to the first 250 files.
-+                                            Dieser Ordner ist zu groß, um alles anzuzeigen. Die Ansicht wurde auf 250 Dateien begrenzt.
-                                         </p>
-                                     </div>
-                                 )}
-diff --git a/resources/scripts/components/server/files/FileManagerStatus.tsx b/resources/scripts/components/server/files/FileManagerStatus.tsx
-index 3e1c2df..ad14075 100644
---- a/resources/scripts/components/server/files/FileManagerStatus.tsx
-+++ b/resources/scripts/components/server/files/FileManagerStatus.tsx
-@@ -58,17 +58,17 @@ const FileUploadList = () => {
-             ))}
-             <Dialog.Footer>
-                 <Button.Danger variant={Button.Variants.Secondary} onClick={() => clearFileUploads()}>
--                    Cancel Uploads
-+                    Upload abbrechen
-                 </Button.Danger>
--                <Button.Text onClick={close}>Close</Button.Text>
-+                <Button.Text onClick={close}>Schliessen</Button.Text>
-             </Dialog.Footer>
-         </div>
-     );
- };
- 
- const FileUploadListDialog = asDialog({
--    title: 'File Uploads',
--    description: 'The following files are being uploaded to your server.',
-+    title: 'Wird hochgeladen',
-+    description: 'Folgende Dateien werden gerade hochgeladen...',
- })(FileUploadList);
- 
- export default () => {
-@@ -89,7 +89,7 @@ export default () => {
-     return (
-         <>
-             {count > 0 && (
--                <Tooltip content={`${count} files are uploading, click to view`}>
-+                <Tooltip content={`${count} Dateien werden hochgeladen, zum Anzeigen klicken`}>
-                     <button
-                         className={'flex items-center justify-center w-10 h-10'}
-                         onClick={() => (open.value = true)}
-diff --git a/resources/scripts/components/server/files/FileNameModal.tsx b/resources/scripts/components/server/files/FileNameModal.tsx
-index 04781ba..2cfbc72 100644
---- a/resources/scripts/components/server/files/FileNameModal.tsx
-+++ b/resources/scripts/components/server/files/FileNameModal.tsx
-@@ -44,12 +44,12 @@ export default ({ onFileNamed, onDismissed, ...props }: Props) => {
-                         <Field
-                             id={'fileName'}
-                             name={'fileName'}
--                            label={'File Name'}
--                            description={'Enter the name that this file should be saved as.'}
-+                            label={'Datei erstellen'}
-+                            description={'Gib ein, wie die Datei heißen soll.'}
-                             autoFocus
-                         />
-                         <div css={tw`mt-6 text-right`}>
--                            <Button>Create File</Button>
-+                            <Button>Erstellen</Button>
-                         </div>
-                     </Form>
-                 </Modal>
-diff --git a/resources/scripts/components/server/files/FileObjectRow.tsx b/resources/scripts/components/server/files/FileObjectRow.tsx
-index 8032dab..882af81 100644
---- a/resources/scripts/components/server/files/FileObjectRow.tsx
-+++ b/resources/scripts/components/server/files/FileObjectRow.tsx
-@@ -14,6 +14,7 @@ import { usePermissions } from '@/plugins/usePermissions';
- import { join } from 'path';
- import { bytesToString } from '@/lib/formatters';
- import styles from './style.module.css';
-+import { de } from "date-fns/locale";
- 
- const Clickable: React.FC<{ file: FileObject }> = memo(({ file, children }) => {
-     const [canReadContents] = usePermissions(['file.read-content']);
-@@ -57,8 +58,8 @@ const FileObjectRow = ({ file }: { file: FileObject }) => (
-             {file.isFile && <div css={tw`w-1/6 text-right mr-4 hidden sm:block`}>{bytesToString(file.size)}</div>}
-             <div css={tw`w-1/5 text-right mr-4 hidden md:block`} title={file.modifiedAt.toString()}>
-                 {Math.abs(differenceInHours(file.modifiedAt, new Date())) > 48
--                    ? format(file.modifiedAt, 'MMM do, yyyy h:mma')
--                    : formatDistanceToNow(file.modifiedAt, { addSuffix: true })}
-+                    ? format(file.modifiedAt, 'do MMM yyyy hh:mm', { locale: de })
-+                    : formatDistanceToNow(file.modifiedAt, { locale: de, addSuffix: true })}
-             </div>
-         </Clickable>
-         <FileDropdownMenu file={file} />
-diff --git a/resources/scripts/components/server/files/MassActionsBar.tsx b/resources/scripts/components/server/files/MassActionsBar.tsx
-index 44230f2..9271d55 100644
---- a/resources/scripts/components/server/files/MassActionsBar.tsx
-+++ b/resources/scripts/components/server/files/MassActionsBar.tsx
-@@ -33,7 +33,7 @@ const MassActionsBar = () => {
-     const onClickCompress = () => {
-         setLoading(true);
-         clearFlashes('files');
--        setLoadingMessage('Archiving files...');
-+        setLoadingMessage('Dateien archivieren...');
- 
-         compressFiles(uuid, directory, selectedFiles)
-             .then(() => mutate())
-@@ -46,7 +46,7 @@ const MassActionsBar = () => {
-         setLoading(true);
-         setShowConfirm(false);
-         clearFlashes('files');
--        setLoadingMessage('Deleting files...');
-+        setLoadingMessage('Löschvorgang läuft...');
- 
-         deleteFiles(uuid, directory, selectedFiles)
-             .then(() => {
-@@ -67,21 +67,21 @@ const MassActionsBar = () => {
-                     {loadingMessage}
-                 </SpinnerOverlay>
-                 <Dialog.Confirm
--                    title={'Delete Files'}
-+                    title={'Dateien löschen'}
-                     open={showConfirm}
--                    confirm={'Delete'}
-+                    confirm={'Löschen'}
-                     onClose={() => setShowConfirm(false)}
-                     onConfirmed={onClickConfirmDeletion}
-                 >
-                     <p className={'mb-2'}>
--                        Are you sure you want to delete&nbsp;
--                        <span className={'font-semibold text-gray-50'}>{selectedFiles.length} files</span>? This is a
--                        permanent action and the files cannot be recovered.
-+                        Bist du sicher, das du&nbsp;
-+                        <span className={'font-semibold text-gray-50'}>{selectedFiles.length} Dateien löschen möchtest</span>? Das ist eine
-+                        Aktion, die man nicht rückgängig machen kann!
-                     </p>
-                     {selectedFiles.slice(0, 15).map((file) => (
-                         <li key={file}>{file}</li>
-                     ))}
--                    {selectedFiles.length > 15 && <li>and {selectedFiles.length - 15} others</li>}
-+                    {selectedFiles.length > 15 && <li>und {selectedFiles.length - 15} weitere</li>}
-                 </Dialog.Confirm>
-                 {showMove && (
-                     <RenameFileModal
-@@ -96,10 +96,10 @@ const MassActionsBar = () => {
-                     <div className={'fixed bottom-0 mb-6 flex justify-center w-full z-50'}>
-                         <Fade timeout={75} in={selectedFiles.length > 0} unmountOnExit>
-                             <div css={tw`flex items-center space-x-4 pointer-events-auto rounded p-4 bg-black/50`}>
--                                <Button onClick={() => setShowMove(true)}>Move</Button>
--                                <Button onClick={onClickCompress}>Archive</Button>
-+                                <Button onClick={() => setShowMove(true)}>Verschieben</Button>
-+                                <Button onClick={onClickCompress}>Archivieren</Button>
-                                 <Button.Danger variant={Button.Variants.Secondary} onClick={() => setShowConfirm(true)}>
--                                    Delete
-+                                    Löschen
-                                 </Button.Danger>
-                             </div>
-                         </Fade>
-diff --git a/resources/scripts/components/server/files/NewDirectoryButton.tsx b/resources/scripts/components/server/files/NewDirectoryButton.tsx
-index ff74357..fd8737e 100644
---- a/resources/scripts/components/server/files/NewDirectoryButton.tsx
-+++ b/resources/scripts/components/server/files/NewDirectoryButton.tsx
-@@ -21,7 +21,7 @@ interface Values {
- }
- 
- const schema = object().shape({
--    directoryName: string().required('A valid directory name must be provided.'),
-+    directoryName: string().required('Gib einen gültigen Namen ein.'),
- });
- 
- const generateDirectoryData = (name: string): FileObject => ({
-@@ -40,7 +40,7 @@ const generateDirectoryData = (name: string): FileObject => ({
- });
- 
- const NewDirectoryDialog = asDialog({
--    title: 'Create Directory',
-+    title: 'Ordner erstellen',
- })(() => {
-     const uuid = ServerContext.useStoreState((state) => state.server.data!.uuid);
-     const directory = ServerContext.useStoreState((state) => state.files.directory);
-@@ -73,7 +73,7 @@ const NewDirectoryDialog = asDialog({
-                     <Form css={tw`m-0`}>
-                         <Field autoFocus id={'directoryName'} name={'directoryName'} label={'Name'} />
-                         <p css={tw`mt-2 text-sm md:text-base break-all`}>
--                            <span css={tw`text-neutral-200`}>This directory will be created as&nbsp;</span>
-+                            <span css={tw`text-neutral-200`}>Der Ordner wird erstellt in &nbsp;</span>
-                             <Code>
-                                 /home/container/
-                                 <span css={tw`text-cyan-200`}>
-@@ -84,10 +84,10 @@ const NewDirectoryDialog = asDialog({
-                     </Form>
-                     <Dialog.Footer>
-                         <Button.Text className={'w-full sm:w-auto'} onClick={close}>
--                            Cancel
-+                            Abbrechen
-                         </Button.Text>
-                         <Button className={'w-full sm:w-auto'} onClick={submitForm}>
--                            Create
-+                            Erstellen
-                         </Button>
-                     </Dialog.Footer>
-                 </>
-@@ -103,7 +103,7 @@ export default ({ className }: WithClassname) => {
-         <>
-             <NewDirectoryDialog open={open} onClose={setOpen.bind(this, false)} />
-             <Button.Text onClick={setOpen.bind(this, true)} className={className}>
--                Create Directory
-+                Ordner erstellen
-             </Button.Text>
-         </>
-     );
-diff --git a/resources/scripts/components/server/files/RenameFileModal.tsx b/resources/scripts/components/server/files/RenameFileModal.tsx
-index 4f67533..8a15287 100644
---- a/resources/scripts/components/server/files/RenameFileModal.tsx
-+++ b/resources/scripts/components/server/files/RenameFileModal.tsx
-@@ -66,22 +66,22 @@ const RenameFileModal = ({ files, useMoveTerminology, ...props }: OwnProps) => {
-                                     type={'string'}
-                                     id={'file_name'}
-                                     name={'name'}
--                                    label={'File Name'}
-+                                    label={'Umbenennen'}
-                                     description={
-                                         useMoveTerminology
--                                            ? 'Enter the new name and directory of this file or folder, relative to the current directory.'
-+                                            ? 'Vergebe einen neuen Namen für diese Datei (oder Ordner).'
-                                             : undefined
-                                     }
-                                     autoFocus
-                                 />
-                             </div>
-                             <div css={tw`w-full sm:w-auto mt-4 sm:mt-0`}>
--                                <Button css={tw`w-full`}>{useMoveTerminology ? 'Move' : 'Rename'}</Button>
-+                                <Button css={tw`w-full`}>{useMoveTerminology ? 'Verschieben' : 'Unbenennen'}</Button>
-                             </div>
-                         </div>
-                         {useMoveTerminology && (
-                             <p css={tw`text-xs mt-2 text-neutral-400`}>
--                                <strong css={tw`text-neutral-200`}>New location:</strong>
-+                                <strong css={tw`text-neutral-200`}>Neuer Ort:</strong>
-                                 &nbsp;/home/container/{join(directory, values.name).replace(/^(\.\.\/|\/)+/, '')}
-                             </p>
-                         )}
-diff --git a/resources/scripts/components/server/files/UploadButton.tsx b/resources/scripts/components/server/files/UploadButton.tsx
-index 9ab889b..1cfc5e0 100644
---- a/resources/scripts/components/server/files/UploadButton.tsx
-+++ b/resources/scripts/components/server/files/UploadButton.tsx
-@@ -65,7 +65,7 @@ export default ({ className }: WithClassname) => {
-         clearAndAddHttpError();
-         const list = Array.from(files);
-         if (list.some((file) => !file.size || (!file.type && file.size === 4096))) {
--            return addError('Folder uploads are not supported at this time.', 'Error');
-+            return addError('Ordner können nicht hochgeladen werden.', 'Fehler');
-         }
- 
-         const uploads = list.map((file) => {
-@@ -125,7 +125,7 @@ export default ({ className }: WithClassname) => {
-                             >
-                                 <CloudUploadIcon className={'w-10 h-10 flex-shrink-0'} />
-                                 <p className={'font-header flex-1 text-lg text-neutral-100 text-center'}>
--                                    Drag and drop files to upload.
-+                                    Drag&Drop um etwas hochzuladen
-                                 </p>
-                             </div>
-                         </div>
-@@ -147,7 +147,7 @@ export default ({ className }: WithClassname) => {
-                 multiple
-             />
-             <Button className={className} onClick={() => fileUploadInput.current && fileUploadInput.current.click()}>
--                Upload
-+                Hochladen
-             </Button>
-         </>
-     );
-diff --git a/resources/scripts/components/server/network/AllocationRow.tsx b/resources/scripts/components/server/network/AllocationRow.tsx
-index e68bc33..ca0a5d6 100644
---- a/resources/scripts/components/server/network/AllocationRow.tsx
-+++ b/resources/scripts/components/server/network/AllocationRow.tsx
-@@ -88,7 +88,7 @@ const AllocationRow = ({ allocation }: Props) => {
-                 <InputSpinner visible={loading}>
-                     <Textarea
-                         className={'bg-neutral-800 hover:border-neutral-600 border-transparent'}
--                        placeholder={'Notes'}
-+                        placeholder={'Vermerk zum Port'}
-                         defaultValue={allocation.notes || undefined}
-                         onChange={(e) => setAllocationNotes(e.currentTarget.value)}
-                     />
-@@ -97,7 +97,7 @@ const AllocationRow = ({ allocation }: Props) => {
-             <div className={'flex justify-end space-x-4 mt-4 w-full md:mt-0 md:w-48'}>
-                 {allocation.isDefault ? (
-                     <Button size={Button.Sizes.Small} className={'!text-gray-50 !bg-blue-600'} disabled>
--                        Primary
-+                        Primär
-                     </Button>
-                 ) : (
-                     <>
-@@ -106,7 +106,7 @@ const AllocationRow = ({ allocation }: Props) => {
-                         </Can>
-                         <Can action={'allocation.update'}>
-                             <Button.Text size={Button.Sizes.Small} onClick={setPrimaryAllocation}>
--                                Make Primary
-+                                Primär machen
-                             </Button.Text>
-                         </Can>
-                     </>
-diff --git a/resources/scripts/components/server/network/DeleteAllocationButton.tsx b/resources/scripts/components/server/network/DeleteAllocationButton.tsx
-index c680448..f3ee3f4 100644
---- a/resources/scripts/components/server/network/DeleteAllocationButton.tsx
-+++ b/resources/scripts/components/server/network/DeleteAllocationButton.tsx
-@@ -39,11 +39,11 @@ const DeleteAllocationButton = ({ allocation }: Props) => {
-             <Dialog.Confirm
-                 open={confirm}
-                 onClose={() => setConfirm(false)}
--                title={'Remove Allocation'}
--                confirm={'Delete'}
-+                title={'Port rauswerfen'}
-+                confirm={'Entfernen'}
-                 onConfirmed={deleteAllocation}
-             >
--                This allocation will be immediately removed from your server.
-+                Dadurch wird der Port nicht mehr für deinen Server reserviert und für andere freigegeben.
-             </Dialog.Confirm>
-             <Button.Danger
-                 variant={Button.Variants.Secondary}
-diff --git a/resources/scripts/components/server/network/NetworkContainer.tsx b/resources/scripts/components/server/network/NetworkContainer.tsx
-index 182beaa..d59c661 100644
---- a/resources/scripts/components/server/network/NetworkContainer.tsx
-+++ b/resources/scripts/components/server/network/NetworkContainer.tsx
-@@ -51,7 +51,7 @@ const NetworkContainer = () => {
-     };
- 
-     return (
--        <ServerContentBlock showFlashKey={'server:network'} title={'Network'}>
-+        <ServerContentBlock showFlashKey={'server:network'} title={'Netzwerk'}>
-             {!data ? (
-                 <Spinner size={'large'} centered />
-             ) : (
-@@ -64,12 +64,11 @@ const NetworkContainer = () => {
-                             <SpinnerOverlay visible={loading} />
-                             <div css={tw`mt-6 sm:flex items-center justify-end`}>
-                                 <p css={tw`text-sm text-neutral-300 mb-4 sm:mr-6 sm:mb-0`}>
--                                    You are currently using {data.length} of {allocationLimit} allowed allocations for
--                                    this server.
-+                                    {data.length} Ports stehen für diesen Server bereit ({allocationLimit} dürfen es Maximal sein)
-                                 </p>
-                                 {allocationLimit > data.length && (
-                                     <Button css={tw`w-full sm:w-auto`} color={'primary'} onClick={onCreateAllocation}>
--                                        Create Allocation
-+                                        Port zuteilen
-                                     </Button>
-                                 )}
-                             </div>
-diff --git a/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx b/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx
-index 1710ad0..aa29c1d 100644
---- a/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx
-+++ b/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx
-@@ -41,19 +41,19 @@ export default ({ scheduleId, onDeleted }: Props) => {
-             <Dialog.Confirm
-                 open={visible}
-                 onClose={() => setVisible(false)}
--                title={'Delete Schedule'}
--                confirm={'Delete'}
-+                title={'Zeitplan löschen'}
-+                confirm={'Bestätigen'}
-                 onConfirmed={onDelete}
-             >
-                 <SpinnerOverlay visible={isLoading} />
--                All tasks will be removed and any running processes will be terminated.
-+                Alle geplanten Aufgaben werden hierbei gelöscht und bereits laufende werden abgebrochen.
-             </Dialog.Confirm>
-             <Button.Danger
-                 variant={Button.Variants.Secondary}
-                 className={'flex-1 sm:flex-none mr-4 border-transparent'}
-                 onClick={() => setVisible(true)}
-             >
--                Delete
-+                Löschen
-             </Button.Danger>
-         </>
-     );
-diff --git a/resources/scripts/components/server/schedules/EditScheduleModal.tsx b/resources/scripts/components/server/schedules/EditScheduleModal.tsx
-index 6a89cb3..47f705e 100644
---- a/resources/scripts/components/server/schedules/EditScheduleModal.tsx
-+++ b/resources/scripts/components/server/schedules/EditScheduleModal.tsx
-@@ -90,29 +90,29 @@ const EditScheduleModal = ({ schedule }: Props) => {
-         >
-             {({ isSubmitting }) => (
-                 <Form>
--                    <h3 css={tw`text-2xl mb-6`}>{schedule ? 'Edit schedule' : 'Create new schedule'}</h3>
-+                    <h3 css={tw`text-2xl mb-6`}>{schedule ? 'Zeitplan bearbeiten' : 'Zeitplan erstellen'}</h3>
-                     <FlashMessageRender byKey={'schedule:edit'} css={tw`mb-6`} />
-                     <Field
-                         name={'name'}
--                        label={'Schedule name'}
--                        description={'A human readable identifier for this schedule.'}
-+                        label={'Name des Zeitplans'}
-+                        description={'Gib dem Zeitplan einen guten Namen, damit du weißt, wofür dies gedacht ist.'}
-                     />
-                     <div css={tw`grid grid-cols-2 sm:grid-cols-5 gap-4 mt-6`}>
-                         <Field name={'minute'} label={'Minute'} />
--                        <Field name={'hour'} label={'Hour'} />
--                        <Field name={'dayOfMonth'} label={'Day of month'} />
--                        <Field name={'month'} label={'Month'} />
--                        <Field name={'dayOfWeek'} label={'Day of week'} />
-+                        <Field name={'hour'} label={'Stunde'} />
-+                        <Field name={'dayOfMonth'} label={'Tag des Monats'} />
-+                        <Field name={'month'} label={'Monat'} />
-+                        <Field name={'dayOfWeek'} label={'Tag der Woche'} />
-                     </div>
-                     <p css={tw`text-neutral-400 text-xs mt-2`}>
--                        The schedule system supports the use of Cronjob syntax when defining when tasks should begin
--                        running. Use the fields above to specify when these tasks should begin running.
-+                        Das Zeitplansystem unterstützt die Verwendung der Cronjob-Syntax beim Definieren, wann Aufgaben ausgeführt werden sollen.
-+                        Verwende die Felder oben, um anzugeben, wann diese Tasks ausgeführt werden sollen.
-                     </p>
-                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
-                         <Switch
-                             name={'show_cheatsheet'}
--                            description={'Show the cron cheatsheet for some examples.'}
--                            label={'Show Cheatsheet'}
-+                            description={'Wenn du Hilfe beim definieren brauchst, kann das nützlich sein.'}
-+                            label={'Beispiele anzeigen'}
-                             defaultChecked={showCheatsheet}
-                             onChange={() => setShowCheetsheet((s) => !s)}
-                         />
-@@ -125,20 +125,20 @@ const EditScheduleModal = ({ schedule }: Props) => {
-                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
-                         <FormikSwitch
-                             name={'onlyWhenOnline'}
--                            description={'Only execute this schedule when the server is in a running state.'}
--                            label={'Only When Server Is Online'}
-+                            description={'Führe den Zeitplan nur aus, wenn der Server in dem Zeitpunkt online ist.'}
-+                            label={'Nur wenn der Server online ist'}
-                         />
-                     </div>
-                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
-                         <FormikSwitch
-                             name={'enabled'}
--                            description={'This schedule will be executed automatically if enabled.'}
--                            label={'Schedule Enabled'}
-+                            description={'Der Zeitplan wird automatisch gestartet, wenn der Zeitpunkt erreicht wird.'}
-+                            label={'Zeitplan aktiv'}
-                         />
-                     </div>
-                     <div css={tw`mt-6 text-right`}>
-                         <Button className={'w-full sm:w-auto'} type={'submit'} disabled={isSubmitting}>
--                            {schedule ? 'Save changes' : 'Create schedule'}
-+                            {schedule ? 'Änderungen speichern' : 'Zeitplan erstellen'}
-                         </Button>
-                     </div>
-                 </Form>
-diff --git a/resources/scripts/components/server/schedules/NewTaskButton.tsx b/resources/scripts/components/server/schedules/NewTaskButton.tsx
-index 7b48016..5f2a815 100644
---- a/resources/scripts/components/server/schedules/NewTaskButton.tsx
-+++ b/resources/scripts/components/server/schedules/NewTaskButton.tsx
-@@ -14,7 +14,7 @@ export default ({ schedule }: Props) => {
-         <>
-             <TaskDetailsModal schedule={schedule} visible={visible} onModalDismissed={() => setVisible(false)} />
-             <Button onClick={() => setVisible(true)} className={'flex-1'}>
--                New Task
-+                Neue Aufgabe
-             </Button>
-         </>
-     );
-diff --git a/resources/scripts/components/server/schedules/RunScheduleButton.tsx b/resources/scripts/components/server/schedules/RunScheduleButton.tsx
-index 750b544..5c56255 100644
---- a/resources/scripts/components/server/schedules/RunScheduleButton.tsx
-+++ b/resources/scripts/components/server/schedules/RunScheduleButton.tsx
-@@ -37,7 +37,7 @@ const RunScheduleButton = ({ schedule }: { schedule: Schedule }) => {
-                 disabled={schedule.isProcessing}
-                 onClick={onTriggerExecute}
-             >
--                Run Now
-+                Jetzt starten
-             </Button>
-         </>
-     );
-diff --git a/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx b/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx
-index 0eeed15..321f2f0 100644
---- a/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx
-+++ b/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx
-@@ -6,47 +6,47 @@ export default () => {
-         <>
-             <div css={tw`md:w-1/2 h-full bg-neutral-600`}>
-                 <div css={tw`flex flex-col`}>
--                    <h2 css={tw`py-4 px-6 font-bold`}>Examples</h2>
-+                    <h2 css={tw`py-4 px-6 font-bold`}>Beispiele</h2>
-                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
-                         <div css={tw`w-1/2`}>*/5 * * * *</div>
--                        <div css={tw`w-1/2`}>every 5 minutes</div>
-+                        <div css={tw`w-1/2`}>Alle 5 Minuten</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6`}>
-                         <div css={tw`w-1/2`}>0 */1 * * *</div>
--                        <div css={tw`w-1/2`}>every hour</div>
-+                        <div css={tw`w-1/2`}>Stündlich</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
-                         <div css={tw`w-1/2`}>0 8-12 * * *</div>
--                        <div css={tw`w-1/2`}>hour range</div>
-+                        <div css={tw`w-1/2`}>Stundenbereich</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6`}>
-                         <div css={tw`w-1/2`}>0 0 * * *</div>
--                        <div css={tw`w-1/2`}>once a day</div>
-+                        <div css={tw`w-1/2`}>Einmal am Tag</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
-                         <div css={tw`w-1/2`}>0 0 * * MON</div>
--                        <div css={tw`w-1/2`}>every Monday</div>
-+                        <div css={tw`w-1/2`}>Immer Montags</div>
-                     </div>
-                 </div>
-             </div>
-             <div css={tw`md:w-1/2 h-full bg-neutral-600`}>
--                <h2 css={tw`py-4 px-6 font-bold`}>Special Characters</h2>
-+                <h2 css={tw`py-4 px-6 font-bold`}>Spezielle Aktionen</h2>
-                 <div css={tw`flex flex-col`}>
-                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
-                         <div css={tw`w-1/2`}>*</div>
--                        <div css={tw`w-1/2`}>any value</div>
-+                        <div css={tw`w-1/2`}>Jeder Wert</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6`}>
-                         <div css={tw`w-1/2`}>,</div>
--                        <div css={tw`w-1/2`}>value list separator</div>
-+                        <div css={tw`w-1/2`}>Wertelisten-Trennzeichen</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
-                         <div css={tw`w-1/2`}>-</div>
--                        <div css={tw`w-1/2`}>range values</div>
-+                        <div css={tw`w-1/2`}>Bereichswerte</div>
-                     </div>
-                     <div css={tw`flex py-4 px-6`}>
-                         <div css={tw`w-1/2`}>/</div>
--                        <div css={tw`w-1/2`}>step values</div>
-+                        <div css={tw`w-1/2`}>Stufenwerte</div>
-                     </div>
-                 </div>
-             </div>
-diff --git a/resources/scripts/components/server/schedules/ScheduleContainer.tsx b/resources/scripts/components/server/schedules/ScheduleContainer.tsx
-index 1722314..1ac89a4 100644
---- a/resources/scripts/components/server/schedules/ScheduleContainer.tsx
-+++ b/resources/scripts/components/server/schedules/ScheduleContainer.tsx
-@@ -38,7 +38,7 @@ export default () => {
-     }, []);
- 
-     return (
--        <ServerContentBlock title={'Schedules'}>
-+        <ServerContentBlock title={'Zeitpläne'}>
-             <FlashMessageRender byKey={'schedules'} css={tw`mb-4`} />
-             {!schedules.length && loading ? (
-                 <Spinner size={'large'} centered />
-@@ -46,7 +46,7 @@ export default () => {
-                 <>
-                     {schedules.length === 0 ? (
-                         <p css={tw`text-sm text-center text-neutral-300`}>
--                            There are no schedules configured for this server.
-+                            Es gibt aktuell keine Zeitpläne für diesen Server
-                         </p>
-                     ) : (
-                         schedules.map((schedule) => (
-@@ -68,7 +68,7 @@ export default () => {
-                         <div css={tw`mt-8 flex justify-end`}>
-                             <EditScheduleModal visible={visible} onModalDismissed={() => setVisible(false)} />
-                             <Button type={'button'} onClick={() => setVisible(true)}>
--                                Create schedule
-+                                Zeitplan erstellen
-                             </Button>
-                         </div>
-                     </Can>
-diff --git a/resources/scripts/components/server/schedules/ScheduleCronRow.tsx b/resources/scripts/components/server/schedules/ScheduleCronRow.tsx
-index 187478f..4b372e2 100644
---- a/resources/scripts/components/server/schedules/ScheduleCronRow.tsx
-+++ b/resources/scripts/components/server/schedules/ScheduleCronRow.tsx
-@@ -15,19 +15,19 @@ const ScheduleCronRow = ({ cron, className }: Props) => (
-         </div>
-         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
-             <p className={'font-medium'}>{cron.hour}</p>
--            <p className={'text-2xs text-neutral-500 uppercase'}>Hour</p>
-+            <p className={'text-2xs text-neutral-500 uppercase'}>Stunde</p>
-         </div>
-         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
-             <p className={'font-medium'}>{cron.dayOfMonth}</p>
--            <p className={'text-2xs text-neutral-500 uppercase'}>Day (Month)</p>
-+            <p className={'text-2xs text-neutral-500 uppercase'}>Tag des Monats</p>
-         </div>
-         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
-             <p className={'font-medium'}>{cron.month}</p>
--            <p className={'text-2xs text-neutral-500 uppercase'}>Month</p>
-+            <p className={'text-2xs text-neutral-500 uppercase'}>Monat</p>
-         </div>
-         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
-             <p className={'font-medium'}>{cron.dayOfWeek}</p>
--            <p className={'text-2xs text-neutral-500 uppercase'}>Day (Week)</p>
-+            <p className={'text-2xs text-neutral-500 uppercase'}>Tag der Woche</p>
-         </div>
-     </div>
- );
-diff --git a/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx b/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx
-index bacef24..b85da88 100644
---- a/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx
-+++ b/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx
-@@ -17,6 +17,7 @@ import isEqual from 'react-fast-compare';
- import { format } from 'date-fns';
- import ScheduleCronRow from '@/components/server/schedules/ScheduleCronRow';
- import RunScheduleButton from '@/components/server/schedules/RunScheduleButton';
-+import { de } from "date-fns/locale";
- 
- interface Params {
-     id: string;
-@@ -36,7 +37,7 @@ const ActivePill = ({ active }: { active: boolean }) => (
-             active ? tw`bg-green-600 text-green-100` : tw`bg-red-600 text-red-100`,
-         ]}
-     >
--        {active ? 'Active' : 'Inactive'}
-+        {active ? 'Aktiv' : 'Inaktiv'}
-     </span>
- );
- 
-@@ -78,7 +79,7 @@ export default () => {
-     }, []);
- 
-     return (
--        <PageContentBlock title={'Schedules'}>
-+        <PageContentBlock title={'Zeitpläne'}>
-             <FlashMessageRender byKey={'schedules'} css={tw`mb-4`} />
-             {!schedule || isLoading ? (
-                 <Spinner size={'large'} centered />
-@@ -97,23 +98,23 @@ export default () => {
-                                             css={tw`flex items-center rounded-full px-2 py-px text-xs ml-4 uppercase bg-neutral-600 text-white`}
-                                         >
-                                             <Spinner css={tw`w-3! h-3! mr-2`} />
--                                            Processing
-+                                            Wird ausgeführt...
-                                         </span>
-                                     ) : (
-                                         <ActivePill active={schedule.isActive} />
-                                     )}
-                                 </h3>
-                                 <p css={tw`mt-1 text-sm text-neutral-200`}>
--                                    Last run at:&nbsp;
-+                                    Zuletzt ausgeführt:&nbsp;
-                                     {schedule.lastRunAt ? (
--                                        format(schedule.lastRunAt, "MMM do 'at' h:mma")
-+                                        format(schedule.lastRunAt, "do MMM 'um' hh:mm", { locale: de })
-                                     ) : (
-                                         <span css={tw`text-neutral-300`}>n/a</span>
-                                     )}
-                                     <span css={tw`ml-4 pl-4 border-l-4 border-neutral-600 py-px`}>
--                                        Next run at:&nbsp;
-+                                        Nächster Start:&nbsp;
-                                         {schedule.nextRunAt ? (
--                                            format(schedule.nextRunAt, "MMM do 'at' h:mma")
-+                                            format(schedule.nextRunAt, "do MMM 'um' hh:mm", { locale: de })
-                                         ) : (
-                                             <span css={tw`text-neutral-300`}>n/a</span>
-                                         )}
-@@ -123,7 +124,7 @@ export default () => {
-                             <div css={tw`flex sm:block mt-3 sm:mt-0`}>
-                                 <Can action={'schedule.update'}>
-                                     <Button.Text className={'flex-1 mr-4'} onClick={toggleEditModal}>
--                                        Edit
-+                                        Bearbeiten
-                                     </Button.Text>
-                                     <NewTaskButton schedule={schedule} />
-                                 </Can>
-@@ -131,10 +132,10 @@ export default () => {
-                         </div>
-                         <div css={tw`hidden sm:grid grid-cols-5 md:grid-cols-5 gap-4 mb-4 mt-4`}>
-                             <CronBox title={'Minute'} value={schedule.cron.minute} />
--                            <CronBox title={'Hour'} value={schedule.cron.hour} />
--                            <CronBox title={'Day (Month)'} value={schedule.cron.dayOfMonth} />
--                            <CronBox title={'Month'} value={schedule.cron.month} />
--                            <CronBox title={'Day (Week)'} value={schedule.cron.dayOfWeek} />
-+                            <CronBox title={'Stunde'} value={schedule.cron.hour} />
-+                            <CronBox title={'Tag des Monats'} value={schedule.cron.dayOfMonth} />
-+                            <CronBox title={'Monat'} value={schedule.cron.month} />
-+                            <CronBox title={'Tag der Woche'} value={schedule.cron.dayOfWeek} />
-                         </div>
-                         <div css={tw`bg-neutral-700 rounded-b`}>
-                             {schedule.tasks.length > 0
-diff --git a/resources/scripts/components/server/schedules/ScheduleRow.tsx b/resources/scripts/components/server/schedules/ScheduleRow.tsx
-index 1a7da64..5a3d4c6 100644
---- a/resources/scripts/components/server/schedules/ScheduleRow.tsx
-+++ b/resources/scripts/components/server/schedules/ScheduleRow.tsx
-@@ -5,6 +5,7 @@ import { faCalendarAlt } from '@fortawesome/free-solid-svg-icons';
- import { format } from 'date-fns';
- import tw from 'twin.macro';
- import ScheduleCronRow from '@/components/server/schedules/ScheduleCronRow';
-+import { de } from "date-fns/locale";
- 
- export default ({ schedule }: { schedule: Schedule }) => (
-     <>
-@@ -14,7 +15,7 @@ export default ({ schedule }: { schedule: Schedule }) => (
-         <div css={tw`flex-1 md:ml-4`}>
-             <p>{schedule.name}</p>
-             <p css={tw`text-xs text-neutral-400`}>
--                Last run at: {schedule.lastRunAt ? format(schedule.lastRunAt, "MMM do 'at' h:mma") : 'never'}
-+                Letzter Start: {schedule.lastRunAt ? format(schedule.lastRunAt, "do MMM 'um' hh:mm", {locale: de}) : 'Noch nie'}
-             </p>
-         </div>
-         <div>
-@@ -24,7 +25,7 @@ export default ({ schedule }: { schedule: Schedule }) => (
-                     schedule.isActive ? tw`bg-green-600` : tw`bg-neutral-400`,
-                 ]}
-             >
--                {schedule.isActive ? 'Active' : 'Inactive'}
-+                {schedule.isActive ? 'Aktiv' : 'Inaktiv'}
-             </p>
-         </div>
-         <ScheduleCronRow cron={schedule.cron} css={tw`mx-auto sm:mx-8 w-full sm:w-auto mt-4 sm:mt-0`} />
-@@ -35,7 +36,7 @@ export default ({ schedule }: { schedule: Schedule }) => (
-                     schedule.isActive && !schedule.isProcessing ? tw`bg-green-600` : tw`bg-neutral-400`,
-                 ]}
-             >
--                {schedule.isProcessing ? 'Processing' : schedule.isActive ? 'Active' : 'Inactive'}
-+                {schedule.isProcessing ? 'Wird ausgeführt' : schedule.isActive ? 'Aktiv' : 'Inaktiv'}
-             </p>
-         </div>
-     </>
-diff --git a/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx b/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx
-index f950b94..dc0c44d 100644
---- a/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx
-+++ b/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx
-@@ -29,13 +29,13 @@ interface Props {
- const getActionDetails = (action: string): [string, any] => {
-     switch (action) {
-         case 'command':
--            return ['Send Command', faCode];
-+            return ['Befehl senden', faCode];
-         case 'power':
--            return ['Send Power Action', faToggleOn];
-+            return ['Status des Servers ändern', faToggleOn];
-         case 'backup':
--            return ['Create Backup', faFileArchive];
-+            return ['Backup erstellen', faFileArchive];
-         default:
--            return ['Unknown Action', faCode];
-+            return ['Unbekannte Aufgabe', faCode];
-     }
- };
- 
-@@ -76,13 +76,13 @@ export default ({ schedule, task }: Props) => {
-                 onModalDismissed={() => setIsEditing(false)}
-             />
-             <ConfirmationModal
--                title={'Confirm task deletion'}
--                buttonText={'Delete Task'}
-+                title={'Löschen bestätigen'}
-+                buttonText={'Aufgabe löschen'}
-                 onConfirmed={onConfirmDeletion}
-                 visible={visible}
-                 onModalDismissed={() => setVisible(false)}
-             >
--                Are you sure you want to delete this task? This action cannot be undone.
-+                Möchtest du wirklich diese Aufgabe löschen, dies kannst du nicht rückgängig machen.
-             </ConfirmationModal>
-             <FontAwesomeIcon icon={icon} css={tw`text-lg text-white hidden md:block`} />
-             <div css={tw`flex-none sm:flex-1 w-full sm:w-auto overflow-x-auto`}>
-@@ -90,7 +90,7 @@ export default ({ schedule, task }: Props) => {
-                 {task.payload && (
-                     <div css={tw`md:ml-6 mt-2`}>
-                         {task.action === 'backup' && (
--                            <p css={tw`text-xs uppercase text-neutral-400 mb-1`}>Ignoring files & folders:</p>
-+                            <p css={tw`text-xs uppercase text-neutral-400 mb-1`}>Dateien und Ordner ignorieren:</p>
-                         )}
-                         <div
-                             css={tw`font-mono bg-neutral-800 rounded py-1 px-2 text-sm w-auto inline-block whitespace-pre-wrap break-all`}
-@@ -105,7 +105,7 @@ export default ({ schedule, task }: Props) => {
-                     <div css={tw`mr-6`}>
-                         <div css={tw`flex items-center px-2 py-1 bg-yellow-500 text-yellow-800 text-sm rounded-full`}>
-                             <Icon icon={faArrowCircleDown} css={tw`w-3 h-3 mr-2`} />
--                            Continues on Failure
-+                            Fortsetzen, wenn es fehlschlägt
-                         </div>
-                     </div>
-                 )}
-@@ -113,14 +113,14 @@ export default ({ schedule, task }: Props) => {
-                     <div css={tw`mr-6`}>
-                         <div css={tw`flex items-center px-2 py-1 bg-neutral-500 text-sm rounded-full`}>
-                             <Icon icon={faClock} css={tw`w-3 h-3 mr-2`} />
--                            {task.timeOffset}s later
-+                            {task.timeOffset}s später
-                         </div>
-                     </div>
-                 )}
-                 <Can action={'schedule.update'}>
-                     <button
-                         type={'button'}
--                        aria-label={'Edit scheduled task'}
-+                        aria-label={'Geplante Aufgabe bearbeiten'}
-                         css={tw`block text-sm p-2 text-neutral-500 hover:text-neutral-100 transition-colors duration-150 mr-4 ml-auto sm:ml-0`}
-                         onClick={() => setIsEditing(true)}
-                     >
-@@ -130,7 +130,7 @@ export default ({ schedule, task }: Props) => {
-                 <Can action={'schedule.update'}>
-                     <button
-                         type={'button'}
--                        aria-label={'Delete scheduled task'}
-+                        aria-label={'Geplante Aufgabe löschen'}
-                         css={tw`block text-sm p-2 text-neutral-500 hover:text-red-600 transition-colors duration-150`}
-                         onClick={() => setVisible(true)}
-                     >
-diff --git a/resources/scripts/components/server/schedules/TaskDetailsModal.tsx b/resources/scripts/components/server/schedules/TaskDetailsModal.tsx
-index f54c0a2..7fd15fb 100644
---- a/resources/scripts/components/server/schedules/TaskDetailsModal.tsx
-+++ b/resources/scripts/components/server/schedules/TaskDetailsModal.tsx
-@@ -36,15 +36,15 @@ const schema = object().shape({
-     action: string().required().oneOf(['command', 'power', 'backup']),
-     payload: string().when('action', {
-         is: (v) => v !== 'backup',
--        then: string().required('A task payload must be provided.'),
-+        then: string().required('Eine Aufgabe muss bereitgestellt werden.'),
-         otherwise: string(),
-     }),
-     continueOnFailure: boolean(),
-     timeOffset: number()
--        .typeError('The time offset must be a valid number between 0 and 900.')
--        .required('A time offset value must be provided.')
--        .min(0, 'The time offset must be at least 0 seconds.')
--        .max(900, 'The time offset must be less than 900 seconds.'),
-+        .typeError('Die Zeit muss zwischen 0 und 900 Sekunden sein')
-+        .required('Es wurde keine Zeit festgelegt.')
-+        .min(0, 'Der Zeitversatz muss mindestens 0 Sekunden betragen.')
-+        .max(900, 'Die Zeit muss unter 900 Sekunden sein.'),
- });
- 
- const ActionListener = () => {
-@@ -83,7 +83,7 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
-         if (backupLimit === 0 && values.action === 'backup') {
-             setSubmitting(false);
-             addError({
--                message: "A backup task cannot be created when the server's backup limit is set to 0.",
-+                message: "Aktuell darf für diesen Server kein Backup erstellt werden.",
-                 key: 'schedule:task',
-             });
-         } else {
-@@ -119,25 +119,25 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
-             {({ isSubmitting, values }) => (
-                 <Form css={tw`m-0`}>
-                     <FlashMessageRender byKey={'schedule:task'} css={tw`mb-4`} />
--                    <h2 css={tw`text-2xl mb-6`}>{task ? 'Edit Task' : 'Create Task'}</h2>
-+                    <h2 css={tw`text-2xl mb-6`}>{task ? 'Aufgabe bearbeiten' : 'Aufgabe erstellen'}</h2>
-                     <div css={tw`flex`}>
-                         <div css={tw`mr-2 w-1/3`}>
--                            <Label>Action</Label>
-+                            <Label>Kernaufgabe</Label>
-                             <ActionListener />
-                             <FormikFieldWrapper name={'action'}>
-                                 <FormikField as={Select} name={'action'}>
--                                    <option value={'command'}>Send command</option>
--                                    <option value={'power'}>Send power action</option>
--                                    <option value={'backup'}>Create backup</option>
-+                                    <option value={'command'}>Befehl senden</option>
-+                                    <option value={'power'}>Serverstatus ändern</option>
-+                                    <option value={'backup'}>Backup erstellen</option>
-                                 </FormikField>
-                             </FormikFieldWrapper>
-                         </div>
-                         <div css={tw`flex-1 ml-6`}>
-                             <Field
-                                 name={'timeOffset'}
--                                label={'Time offset (in seconds)'}
-+                                label={'Wartezeit bis zur Fortsetzung (in sek.)'}
-                                 description={
--                                    'The amount of time to wait after the previous task executes before running this one. If this is the first task on a schedule this will not be applied.'
-+                                    'Zwischen den Aufgaben kannst du festlegen, wie lange gewartet werden soll. Dies gilt für alle Aufgaben in diesem Zeitplan.'
-                                 }
-                             />
-                         </div>
-@@ -145,30 +145,30 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
-                     <div css={tw`mt-6`}>
-                         {values.action === 'command' ? (
-                             <div>
--                                <Label>Payload</Label>
-+                                <Label>Aufgabe</Label>
-                                 <FormikFieldWrapper name={'payload'}>
-                                     <FormikField as={Textarea} name={'payload'} rows={6} />
-                                 </FormikFieldWrapper>
-                             </div>
-                         ) : values.action === 'power' ? (
-                             <div>
--                                <Label>Payload</Label>
-+                                <Label>Aufgabe</Label>
-                                 <FormikFieldWrapper name={'payload'}>
-                                     <FormikField as={Select} name={'payload'}>
--                                        <option value={'start'}>Start the server</option>
--                                        <option value={'restart'}>Restart the server</option>
--                                        <option value={'stop'}>Stop the server</option>
--                                        <option value={'kill'}>Terminate the server</option>
-+                                        <option value={'start'}>Server starten</option>
-+                                        <option value={'restart'}>Server neustarten</option>
-+                                        <option value={'stop'}>Server stoppen</option>
-+                                        <option value={'kill'}>Server sofort stoppen (kann zu Datenverlust führen)</option>
-                                     </FormikField>
-                                 </FormikFieldWrapper>
-                             </div>
-                         ) : (
-                             <div>
--                                <Label>Ignored Files</Label>
-+                                <Label>Dateien ignorieren</Label>
-                                 <FormikFieldWrapper
-                                     name={'payload'}
-                                     description={
--                                        'Optional. Include the files and folders to be excluded in this backup. By default, the contents of your .pteroignore file will be used. If you have reached your backup limit, the oldest backup will be rotated.'
-+                                        'Optional. Du musst das nicht machen, wenn du bereits in den Verzeichnissen die Datei .ptignore angelegt hast.'
-                                     }
-                                 >
-                                     <FormikField as={Textarea} name={'payload'} rows={6} />
-@@ -179,13 +179,13 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
-                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
-                         <FormikSwitch
-                             name={'continueOnFailure'}
--                            description={'Future tasks will be run when this task fails.'}
--                            label={'Continue on Failure'}
-+                            description={'Andere Aufgaben werden fortgesetzt, wenn der davor fehlschlägt.'}
-+                            label={'Bei Fehler fortsetzen'}
-                         />
-                     </div>
-                     <div css={tw`flex justify-end mt-6`}>
-                         <Button type={'submit'} disabled={isSubmitting}>
--                            {task ? 'Save Changes' : 'Create Task'}
-+                            {task ? 'Speichern' : 'Aufgabe erstellen'}
-                         </Button>
-                     </div>
-                 </Form>
-diff --git a/resources/scripts/components/server/settings/ReinstallServerBox.tsx b/resources/scripts/components/server/settings/ReinstallServerBox.tsx
-index 3a5a8da..8a0c96d 100644
---- a/resources/scripts/components/server/settings/ReinstallServerBox.tsx
-+++ b/resources/scripts/components/server/settings/ReinstallServerBox.tsx
-@@ -21,7 +21,7 @@ export default () => {
-                 addFlash({
-                     key: 'settings',
-                     type: 'success',
--                    message: 'Your server has begun the reinstallation process.',
-+                    message: 'Dein Server wird jetzt neu aufgesetzt...',
-                 });
-             })
-             .catch((error) => {
-@@ -37,28 +37,26 @@ export default () => {
-     }, []);
- 
-     return (
--        <TitledGreyBox title={'Reinstall Server'} css={tw`relative`}>
-+        <TitledGreyBox title={'Server neu aufsetzen'} css={tw`relative`}>
-             <Dialog.Confirm
-                 open={modalVisible}
--                title={'Confirm server reinstallation'}
--                confirm={'Yes, reinstall server'}
-+                title={'WARNUNG'}
-+                confirm={'Server jetzt neu aufsetzen'}
-                 onClose={() => setModalVisible(false)}
-                 onConfirmed={reinstall}
-             >
--                Your server will be stopped and some files may be deleted or modified during this process, are you sure
--                you wish to continue?
-+                Dein Server wird hierbei heruntergefahren und mit dem neuesten Update auf den aktuellen Stand gebracht.
-+                Einige Dateien werden auch auf die Standard-Optionen zurückgesetzt. Mach dir am besten einmal ein Backup.
-             </Dialog.Confirm>
-             <p css={tw`text-sm`}>
--                Reinstalling your server will stop it, and then re-run the installation script that initially set it
--                up.&nbsp;
-+                Das neu Aufsetzen des Servers wird den Server stoppen und einige Dateien modifizieren oder löschen.&nbsp;
-                 <strong css={tw`font-medium`}>
--                    Some files may be deleted or modified during this process, please back up your data before
--                    continuing.
-+                    Mache vorher ein Backup, bevor du etwas bereust.
-                 </strong>
-             </p>
-             <div css={tw`mt-6 text-right`}>
-                 <Button.Danger variant={Button.Variants.Secondary} onClick={() => setModalVisible(true)}>
--                    Reinstall Server
-+                    Prozess starten
-                 </Button.Danger>
-             </div>
-         </TitledGreyBox>
-diff --git a/resources/scripts/components/server/settings/RenameServerBox.tsx b/resources/scripts/components/server/settings/RenameServerBox.tsx
-index 9cb290a..8ec58ae 100644
---- a/resources/scripts/components/server/settings/RenameServerBox.tsx
-+++ b/resources/scripts/components/server/settings/RenameServerBox.tsx
-@@ -24,18 +24,18 @@ const RenameServerBox = () => {
-     const { isSubmitting } = useFormikContext<Values>();
- 
-     return (
--        <TitledGreyBox title={'Change Server Details'} css={tw`relative`}>
-+        <TitledGreyBox title={'Details des Servers'} css={tw`relative`}>
-             <SpinnerOverlay visible={isSubmitting} />
-             <Form css={tw`mb-0`}>
-                 <Field id={'name'} name={'name'} label={'Server Name'} type={'text'} />
-                 <div css={tw`mt-6`}>
--                    <Label>Server Description</Label>
-+                    <Label>Beschreibung des Servers</Label>
-                     <FormikFieldWrapper name={'description'}>
-                         <FormikField as={Textarea} name={'description'} rows={3} />
-                     </FormikFieldWrapper>
-                 </div>
-                 <div css={tw`mt-6 text-right`}>
--                    <Button type={'submit'}>Save</Button>
-+                    <Button type={'submit'}>Speichern</Button>
-                 </div>
-             </Form>
-         </TitledGreyBox>
-diff --git a/resources/scripts/components/server/settings/SettingsContainer.tsx b/resources/scripts/components/server/settings/SettingsContainer.tsx
-index bcdc80b..c3d2314 100644
---- a/resources/scripts/components/server/settings/SettingsContainer.tsx
-+++ b/resources/scripts/components/server/settings/SettingsContainer.tsx
-@@ -23,20 +23,20 @@ export default () => {
-     const sftp = ServerContext.useStoreState((state) => state.server.data!.sftpDetails, isEqual);
- 
-     return (
--        <ServerContentBlock title={'Settings'}>
-+        <ServerContentBlock title={'Einstellungen'}>
-             <FlashMessageRender byKey={'settings'} css={tw`mb-4`} />
-             <div css={tw`md:flex`}>
-                 <div css={tw`w-full md:flex-1 md:mr-10`}>
-                     <Can action={'file.sftp'}>
--                        <TitledGreyBox title={'SFTP Details'} css={tw`mb-6 md:mb-10`}>
-+                        <TitledGreyBox title={'SFTP Zugangsdaten'} css={tw`mb-6 md:mb-10`}>
-                             <div>
--                                <Label>Server Address</Label>
-+                                <Label>Adresse</Label>
-                                 <CopyOnClick text={`sftp://${ip(sftp.ip)}:${sftp.port}`}>
-                                     <Input type={'text'} value={`sftp://${ip(sftp.ip)}:${sftp.port}`} readOnly />
-                                 </CopyOnClick>
-                             </div>
-                             <div css={tw`mt-6`}>
--                                <Label>Username</Label>
-+                                <Label>Benutzer</Label>
-                                 <CopyOnClick text={`${username}.${id}`}>
-                                     <Input type={'text'} value={`${username}.${id}`} readOnly />
-                                 </CopyOnClick>
-@@ -45,19 +45,19 @@ export default () => {
-                                 <div css={tw`flex-1`}>
-                                     <div css={tw`border-l-4 border-cyan-500 p-3`}>
-                                         <p css={tw`text-xs text-neutral-200`}>
--                                            Your SFTP password is the same as the password you use to access this panel.
-+                                            Das SFTP Passwort ist dasselbe, wie von deinem Login.
-                                         </p>
-                                     </div>
-                                 </div>
-                                 <div css={tw`ml-4`}>
-                                     <a href={`sftp://${username}.${id}@${ip(sftp.ip)}:${sftp.port}`}>
--                                        <Button.Text variant={Button.Variants.Secondary}>Launch SFTP</Button.Text>
-+                                        <Button.Text variant={Button.Variants.Secondary}>In Client öffnen</Button.Text>
-                                     </a>
-                                 </div>
-                             </div>
-                         </TitledGreyBox>
-                     </Can>
--                    <TitledGreyBox title={'Debug Information'} css={tw`mb-6 md:mb-10`}>
-+                    <TitledGreyBox title={'Standort des Servers'} css={tw`mb-6 md:mb-10`}>
-                         <div css={tw`flex items-center justify-between text-sm`}>
-                             <p>Node</p>
-                             <code css={tw`font-mono bg-neutral-900 rounded py-1 px-2`}>{node}</code>
-diff --git a/resources/scripts/components/server/startup/StartupContainer.tsx b/resources/scripts/components/server/startup/StartupContainer.tsx
-index 35bb9a6..8cfbd1a 100644
---- a/resources/scripts/components/server/startup/StartupContainer.tsx
-+++ b/resources/scripts/components/server/startup/StartupContainer.tsx
-@@ -80,12 +80,12 @@ const StartupContainer = () => {
-         !error || (error && isValidating) ? (
-             <Spinner centered size={Spinner.Size.LARGE} />
-         ) : (
--            <ServerError title={'Oops!'} message={httpErrorToHuman(error)} onRetry={() => mutate()} />
-+            <ServerError title={'Hups!'} message={httpErrorToHuman(error)} onRetry={() => mutate()} />
-         )
-     ) : (
--        <ServerContentBlock title={'Startup Settings'} showFlashKey={'startup:image'}>
-+        <ServerContentBlock title={'Einstellungen'} showFlashKey={'startup:image'}>
-             <div css={tw`md:flex`}>
--                <TitledGreyBox title={'Startup Command'} css={tw`flex-1`}>
-+                <TitledGreyBox title={'Startbefehl'} css={tw`flex-1`}>
-                     <div css={tw`px-1 py-2`}>
-                         <p css={tw`font-mono bg-neutral-900 rounded py-2 px-4`}>{data.invocation}</p>
-                     </div>
-@@ -107,8 +107,8 @@ const StartupContainer = () => {
-                                 </Select>
-                             </InputSpinner>
-                             <p css={tw`text-xs text-neutral-300 mt-2`}>
--                                This is an advanced feature allowing you to select a Docker image to use when running
--                                this server instance.
-+                                Das ist eine erweiterte Funktion, mit der man die Docker Version auswählen kann.
-+                                Hier ist aber normalerweise kein Handlungsbedarf notwendig.
-                             </p>
-                         </>
-                     ) : (
-@@ -116,15 +116,15 @@ const StartupContainer = () => {
-                             <Input disabled readOnly value={variables.dockerImage} />
-                             {isCustomImage && (
-                                 <p css={tw`text-xs text-neutral-300 mt-2`}>
--                                    This {"server's"} Docker image has been manually set by an administrator and cannot
--                                    be changed through this UI.
-+                                    Dieser {"server's"} Docker Image wurde vom Betreiber fest eingestellt und
-+                                    kann nicht verändert werden.
-                                 </p>
-                             )}
-                         </>
-                     )}
-                 </TitledGreyBox>
-             </div>
--            <h3 css={tw`mt-8 mb-2 text-2xl`}>Variables</h3>
-+            <h3 css={tw`mt-8 mb-2 text-2xl`}>Variablen</h3>
-             <div css={tw`grid gap-8 md:grid-cols-2`}>
-                 {data.variables.map((variable) => (
-                     <VariableBox key={variable.envVariable} variable={variable} />
-diff --git a/resources/scripts/components/server/startup/VariableBox.tsx b/resources/scripts/components/server/startup/VariableBox.tsx
-index ad73615..f2ed381 100644
---- a/resources/scripts/components/server/startup/VariableBox.tsx
-+++ b/resources/scripts/components/server/startup/VariableBox.tsx
-@@ -63,7 +63,7 @@ const VariableBox = ({ variable }: Props) => {
-             title={
-                 <p css={tw`text-sm uppercase`}>
-                     {!variable.isEditable && (
--                        <span css={tw`bg-neutral-700 text-xs py-1 px-2 rounded-full mr-2 mb-1`}>Read Only</span>
-+                        <span css={tw`bg-neutral-700 text-xs py-1 px-2 rounded-full mr-2 mb-1`}>Nur lesen</span>
-                     )}
-                     {variable.name}
-                 </p>
-diff --git a/resources/scripts/components/server/users/AddSubuserButton.tsx b/resources/scripts/components/server/users/AddSubuserButton.tsx
-index d4847ce..cf9c34a 100644
---- a/resources/scripts/components/server/users/AddSubuserButton.tsx
-+++ b/resources/scripts/components/server/users/AddSubuserButton.tsx
-@@ -8,7 +8,7 @@ export default () => {
-     return (
-         <>
-             <EditSubuserModal visible={visible} onModalDismissed={() => setVisible(false)} />
--            <Button onClick={() => setVisible(true)}>New User</Button>
-+            <Button onClick={() => setVisible(true)}>Neuer Benutzer</Button>
-         </>
-     );
- };
-diff --git a/resources/scripts/components/server/users/EditSubuserModal.tsx b/resources/scripts/components/server/users/EditSubuserModal.tsx
-index c28d56f..51dabaf 100644
---- a/resources/scripts/components/server/users/EditSubuserModal.tsx
-+++ b/resources/scripts/components/server/users/EditSubuserModal.tsx
-@@ -96,9 +96,9 @@ const EditSubuserModal = ({ subuser }: Props) => {
-             }
-             validationSchema={object().shape({
-                 email: string()
--                    .max(191, 'Email addresses must not exceed 191 characters.')
--                    .email('A valid email address must be provided.')
--                    .required('A valid email address must be provided.'),
-+                    .max(191, 'Die E-Mail Adresse darf 191 Zeichen nicht überschreiten.')
-+                    .email('Gib eine gültige E-Mail Adresse ein.')
-+                    .required('Schau bitte nochmal drauf, die E-Mail stimmt nicht ganz.'),
-                 permissions: array().of(string()),
-             })}
-         >
-@@ -106,12 +106,12 @@ const EditSubuserModal = ({ subuser }: Props) => {
-                 <div css={tw`flex justify-between`}>
-                     <h2 css={tw`text-2xl`} ref={ref}>
-                         {subuser
--                            ? `${canEditUser ? 'Modify' : 'View'} permissions for ${subuser.email}`
--                            : 'Create new subuser'}
-+                            ? `${canEditUser ? 'Bearbeite' : 'Zeige'} Berechtigungen von ${subuser.email}`
-+                            : 'Neuen Benutzer erstellen'}
-                     </h2>
-                     <div>
-                         <Button type={'submit'} css={tw`w-full sm:w-auto`}>
--                            {subuser ? 'Save' : 'Invite User'}
-+                            {subuser ? 'Speichern' : 'Benutzer einladen'}
-                         </Button>
-                     </div>
-                 </div>
-@@ -119,8 +119,7 @@ const EditSubuserModal = ({ subuser }: Props) => {
-                 {!isRootAdmin && loggedInPermissions[0] !== '*' && (
-                     <div css={tw`mt-4 pl-4 py-2 border-l-4 border-cyan-400`}>
-                         <p css={tw`text-sm text-neutral-300`}>
--                            Only permissions which your account is currently assigned may be selected when creating or
--                            modifying other users.
-+                            Der Benutzer kann nur die Berechtigungen nutzen, die hier genehmigt werden.
-                         </p>
-                     </div>
-                 )}
-@@ -128,9 +127,10 @@ const EditSubuserModal = ({ subuser }: Props) => {
-                     <div css={tw`mt-6`}>
-                         <Field
-                             name={'email'}
--                            label={'User Email'}
-+                            label={'E-Mail des Benutzers'}
-                             description={
--                                'Enter the email address of the user you wish to invite as a subuser for this server.'
-+                                'Gib die E-Mail Adresse ein, die dem Benutzer gehört.' +
-+                                'Diese E-Mail Adresse muss einen Account besitzen.'
-                             }
-                         />
-                     </div>
-@@ -160,7 +160,7 @@ const EditSubuserModal = ({ subuser }: Props) => {
-                 <Can action={subuser ? 'user.update' : 'user.create'}>
-                     <div css={tw`pb-6 flex justify-end`}>
-                         <Button type={'submit'} css={tw`w-full sm:w-auto`}>
--                            {subuser ? 'Save' : 'Invite User'}
-+                            {subuser ? 'Speichern' : 'Einladen'}
-                         </Button>
-                     </div>
-                 </Can>
-diff --git a/resources/scripts/components/server/users/RemoveSubuserButton.tsx b/resources/scripts/components/server/users/RemoveSubuserButton.tsx
-index defc6b4..6ceab1f 100644
---- a/resources/scripts/components/server/users/RemoveSubuserButton.tsx
-+++ b/resources/scripts/components/server/users/RemoveSubuserButton.tsx
-@@ -36,19 +36,19 @@ export default ({ subuser }: { subuser: Subuser }) => {
-     return (
-         <>
-             <ConfirmationModal
--                title={'Delete this subuser?'}
--                buttonText={'Yes, remove subuser'}
-+                title={'Benutzer entfernen?'}
-+                buttonText={'Rechte entfernen'}
-                 visible={showConfirmation}
-                 showSpinnerOverlay={loading}
-                 onConfirmed={() => doDeletion()}
-                 onModalDismissed={() => setShowConfirmation(false)}
-             >
--                Are you sure you wish to remove this subuser? They will have all access to this server revoked
--                immediately.
-+                Bist du dir sicher? Hierbei entfernst du alle Rechte, die der Benutzer zu diesem Server bekommen hat.
-+                Alternativ kannst du auch die Rechte einschränken.
-             </ConfirmationModal>
-             <button
-                 type={'button'}
--                aria-label={'Delete subuser'}
-+                aria-label={'Benutzer entfernen'}
-                 css={tw`block text-sm p-2 text-neutral-500 hover:text-red-600 transition-colors duration-150`}
-                 onClick={() => setShowConfirmation(true)}
-             >
-diff --git a/resources/scripts/components/server/users/UserRow.tsx b/resources/scripts/components/server/users/UserRow.tsx
-index 45693c5..99c60a3 100644
---- a/resources/scripts/components/server/users/UserRow.tsx
-+++ b/resources/scripts/components/server/users/UserRow.tsx
-@@ -36,13 +36,13 @@ export default ({ subuser }: Props) => {
-                     />
-                     &nbsp;
-                 </p>
--                <p css={tw`text-2xs text-neutral-500 uppercase hidden md:block`}>2FA Enabled</p>
-+                <p css={tw`text-2xs text-neutral-500 uppercase hidden md:block`}>2-FA aktiviert</p>
-             </div>
-             <div css={tw`ml-4 hidden md:block`}>
-                 <p css={tw`font-medium text-center`}>
-                     {subuser.permissions.filter((permission) => permission !== 'websocket.connect').length}
-                 </p>
--                <p css={tw`text-2xs text-neutral-500 uppercase`}>Permissions</p>
-+                <p css={tw`text-2xs text-neutral-500 uppercase`}>Berechtigungen</p>
-             </div>
-             {subuser.uuid !== uuid && (
-                 <>
-diff --git a/resources/scripts/components/server/users/UsersContainer.tsx b/resources/scripts/components/server/users/UsersContainer.tsx
-index a5d505f..c2e5828 100644
---- a/resources/scripts/components/server/users/UsersContainer.tsx
-+++ b/resources/scripts/components/server/users/UsersContainer.tsx
-@@ -48,10 +48,10 @@ export default () => {
-     }
- 
-     return (
--        <ServerContentBlock title={'Users'}>
-+        <ServerContentBlock title={'Benutzer'}>
-             <FlashMessageRender byKey={'users'} css={tw`mb-4`} />
-             {!subusers.length ? (
--                <p css={tw`text-center text-sm text-neutral-300`}>It looks like you don&apos;t have any subusers.</p>
-+                <p css={tw`text-center text-sm text-neutral-300`}>Aktuell arbeitest du alleine am Server</p>
-             ) : (
-                 subusers.map((subuser) => <UserRow key={subuser.uuid} subuser={subuser} />)
-             )}
-diff --git a/resources/scripts/hoc/RequireServerPermission.tsx b/resources/scripts/hoc/RequireServerPermission.tsx
-index cb7da2b..1ff586f 100644
---- a/resources/scripts/hoc/RequireServerPermission.tsx
-+++ b/resources/scripts/hoc/RequireServerPermission.tsx
-@@ -11,7 +11,7 @@ const RequireServerPermission: React.FC<RequireServerPermissionProps> = ({ child
-         <Can
-             action={permissions}
-             renderOnError={
--                <ServerError title={'Access Denied'} message={'You do not have permission to access this page.'} />
-+                <ServerError title={'Ungenügende Rechte'} message={'Der Zugang zu dieser Seite wurde für dich gesperrt.'} />
-             }
-         >
-             {children}
-diff --git a/resources/scripts/routers/routes.ts b/resources/scripts/routers/routes.ts
-index 8fba642..af71083 100644
---- a/resources/scripts/routers/routes.ts
-+++ b/resources/scripts/routers/routes.ts
-@@ -46,23 +46,23 @@ export default {
-     account: [
-         {
-             path: '/',
--            name: 'Account',
-+            name: 'Mein Account',
-             component: AccountOverviewContainer,
-             exact: true,
-         },
-         {
-             path: '/api',
--            name: 'API Credentials',
-+            name: 'API-Zugangsdaten',
-             component: AccountApiContainer,
-         },
-         {
-             path: '/ssh',
--            name: 'SSH Keys',
-+            name: 'SSH-Schlüssel',
-             component: AccountSSHContainer,
-         },
-         {
-             path: '/activity',
--            name: 'Activity',
-+            name: 'Aktivitäten',
-             component: ActivityLogContainer,
-         },
-     ],
-@@ -70,14 +70,14 @@ export default {
-         {
-             path: '/',
-             permission: null,
--            name: 'Console',
-+            name: 'Konsole',
-             component: ServerConsole,
-             exact: true,
-         },
-         {
-             path: '/files',
-             permission: 'file.*',
--            name: 'Files',
-+            name: 'Dateien',
-             component: FileManagerContainer,
-         },
-         {
-@@ -89,13 +89,13 @@ export default {
-         {
-             path: '/databases',
-             permission: 'database.*',
--            name: 'Databases',
-+            name: 'Datenbanken',
-             component: DatabasesContainer,
-         },
-         {
-             path: '/schedules',
-             permission: 'schedule.*',
--            name: 'Schedules',
-+            name: 'Zeitpläne',
-             component: ScheduleContainer,
-         },
-         {
-@@ -107,7 +107,7 @@ export default {
-         {
-             path: '/users',
-             permission: 'user.*',
--            name: 'Users',
-+            name: 'Benutzer',
-             component: UsersContainer,
-         },
-         {
-@@ -119,25 +119,25 @@ export default {
-         {
-             path: '/network',
-             permission: 'allocation.*',
--            name: 'Network',
-+            name: 'Netzwerk',
-             component: NetworkContainer,
-         },
-         {
-             path: '/startup',
-             permission: 'startup.*',
--            name: 'Startup',
-+            name: 'Startbefehl',
-             component: StartupContainer,
-         },
-         {
-             path: '/settings',
-             permission: ['settings.*', 'file.sftp'],
--            name: 'Settings',
-+            name: 'Einstellungen',
-             component: SettingsContainer,
-         },
-         {
-             path: '/activity',
-             permission: 'activity.*',
--            name: 'Activity',
-+            name: 'Aktivitäten',
-             component: ServerActivityLogContainer,
-         },
-     ],
-diff --git a/resources/scripts/state/flashes.ts b/resources/scripts/state/flashes.ts
-index 088aca2..caac16e 100644
---- a/resources/scripts/state/flashes.ts
-+++ b/resources/scripts/state/flashes.ts
-@@ -26,7 +26,7 @@ const flashes: FlashStore = {
-     }),
- 
-     addError: action((state, payload) => {
--        state.items.push({ type: 'error', title: 'Error', ...payload });
-+        state.items.push({ type: 'error', title: 'Fehler', ...payload });
-     }),
- 
-     clearAndAddHttpError: action((state, payload) => {
-@@ -38,7 +38,7 @@ const flashes: FlashStore = {
-             state.items = [
-                 {
-                     type: 'error',
--                    title: 'Error',
-+                    title: 'Fehler',
-                     key: payload.key,
-                     message: httpErrorToHuman(payload.error),
-                 },
-diff --git a/resources/views/admin/api/index.blade.php b/resources/views/admin/api/index.blade.php
-index d863c57..e6e3205 100644
---- a/resources/views/admin/api/index.blade.php
-+++ b/resources/views/admin/api/index.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Application API
-+    Anwendungs-API
- @endsection
- 
- @section('content-header')
--    <h1>Application API<small>Control access credentials for managing this Panel via the API.</small></h1>
-+    <h1>Anwendungs-API<small>Verwalte das Panel über die Application-API</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Application API</li>
-+        <li class="active">Anwendungs-API</li>
-     </ol>
- @endsection
- 
-@@ -17,18 +17,18 @@
-         <div class="col-xs-12">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Credentials List</h3>
-+                    <h3 class="box-title">Aktive Schlüssel</h3>
-                     <div class="box-tools">
--                        <a href="{{ route('admin.api.new') }}" class="btn btn-sm btn-primary">Create New</a>
-+                        <a href="{{ route('admin.api.new') }}" class="btn btn-sm btn-primary">Schlüssel erstellen</a>
-                     </div>
-                 </div>
-                 <div class="box-body table-responsive no-padding">
-                     <table class="table table-hover">
-                         <tr>
--                            <th>Key</th>
--                            <th>Memo</th>
--                            <th>Last Used</th>
--                            <th>Created</th>
-+                            <th>API-Schlüssel</th>
-+                            <th>Notiz</th>
-+                            <th>Zuletzt genutzt</th>
-+                            <th>Erstellt</th>
-                             <th></th>
-                         </tr>
-                         @foreach($keys as $key)
-@@ -66,12 +66,13 @@
-                 event.preventDefault();
-                 swal({
-                     type: 'error',
--                    title: 'Revoke API Key',
--                    text: 'Once this API key is revoked any applications currently using it will stop working.',
-+                    title: 'API-Schlüssel ungültig machen',
-+                    text: 'Alle Verknüpfungen mit diesem Schlüssel werden nach Bestätigung unbenutzbar.',
-                     showCancelButton: true,
-+                    cancelButtonText: 'Abbrechen',
-                     allowOutsideClick: true,
-                     closeOnConfirm: false,
--                    confirmButtonText: 'Revoke',
-+                    confirmButtonText: 'Ungültig machen',
-                     confirmButtonColor: '#d9534f',
-                     showLoaderOnConfirm: true
-                 }, function () {
-@@ -85,15 +86,15 @@
-                         swal({
-                             type: 'success',
-                             title: '',
--                            text: 'API Key has been revoked.'
-+                            text: 'API-Schlüssel ist jetzt nicht mehr aktiv.'
-                         });
-                         self.parent().parent().slideUp();
-                     }).fail(function (jqXHR) {
-                         console.error(jqXHR);
-                         swal({
-                             type: 'error',
--                            title: 'Whoops!',
--                            text: 'An error occurred while attempting to revoke this key.'
-+                            title: 'Hups!',
-+                            text: 'Beim verändern der Gültigkeit trat ein Fehler auf.'
-                         });
-                     });
-                 });
-diff --git a/resources/views/admin/api/new.blade.php b/resources/views/admin/api/new.blade.php
-index b5876ee..be91f38 100644
---- a/resources/views/admin/api/new.blade.php
-+++ b/resources/views/admin/api/new.blade.php
-@@ -1,15 +1,15 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Application API
-+    Anwendungs-API
- @endsection
- 
- @section('content-header')
--    <h1>Application API<small>Create a new application API key.</small></h1>
-+    <h1>Anwendungs-API<small>Erstelle einen neuen API-Schlüssel</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.api.index') }}">Application API</a></li>
--        <li class="active">New Credentials</li>
-+        <li><a href="{{ route('admin.api.index') }}">Anwendungs-API</a></li>
-+        <li class="active">Neuer API-Schlüssel</li>
-     </ol>
- @endsection
- 
-@@ -19,7 +19,7 @@
-             <div class="col-sm-8 col-xs-12">
-                 <div class="box box-primary">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Select Permissions</h3>
-+                        <h3 class="box-title">Berechtigungen festlegen</h3>
-                     </div>
-                     <div class="box-body table-responsive no-padding">
-                         <table class="table table-hover">
-@@ -28,15 +28,15 @@
-                                     <td class="col-sm-3 strong">{{ str_replace('_', ' ', title_case($resource)) }}</td>
-                                     <td class="col-sm-3 radio radio-primary text-center">
-                                         <input type="radio" id="r_{{ $resource }}" name="r_{{ $resource }}" value="{{ $permissions['r'] }}">
--                                        <label for="r_{{ $resource }}">Read</label>
-+                                        <label for="r_{{ $resource }}">Lesen</label>
-                                     </td>
-                                     <td class="col-sm-3 radio radio-primary text-center">
-                                         <input type="radio" id="rw_{{ $resource }}" name="r_{{ $resource }}" value="{{ $permissions['rw'] }}">
--                                        <label for="rw_{{ $resource }}">Read &amp; Write</label>
-+                                        <label for="rw_{{ $resource }}">Lesen &amp; Schreiben</label>
-                                     </td>
-                                     <td class="col-sm-3 radio text-center">
-                                         <input type="radio" id="n_{{ $resource }}" name="r_{{ $resource }}" value="{{ $permissions['n'] }}" checked>
--                                        <label for="n_{{ $resource }}">None</label>
-+                                        <label for="n_{{ $resource }}">Keine Rechte</label>
-                                     </td>
-                                 </tr>
-                             @endforeach
-@@ -48,14 +48,14 @@
-                 <div class="box box-primary">
-                     <div class="box-body">
-                         <div class="form-group">
--                            <label class="control-label" for="memoField">Description <span class="field-required"></span></label>
-+                            <label class="control-label" for="memoField">Beschreibung <span class="field-required"></span></label>
-                             <input id="memoField" type="text" name="memo" class="form-control">
-                         </div>
--                        <p class="text-muted">Once you have assigned permissions and created this set of credentials you will be unable to come back and edit it. If you need to make changes down the road you will need to create a new set of credentials.</p>
-+                        <p class="text-muted">Nachdem du Berechtigungen zugewiesen und diesen Satz von Anmeldeinformationen erstellt hast, kannst du ihn nicht mehr bearbeiten. Wenn Sie später Änderungen vornehmen müssen, musst du einen neuen Satz von Anmeldeinformationen erstellen.</p>
-                     </div>
-                     <div class="box-footer">
-                         {{ csrf_field() }}
--                        <button type="submit" class="btn btn-success btn-sm pull-right">Create Credentials</button>
-+                        <button type="submit" class="btn btn-success btn-sm pull-right">API-Schlüssel aktivieren</button>
-                     </div>
-                 </div>
-             </div>
-diff --git a/resources/views/admin/databases/index.blade.php b/resources/views/admin/databases/index.blade.php
-index e4c69c5..40d026c 100644
---- a/resources/views/admin/databases/index.blade.php
-+++ b/resources/views/admin/databases/index.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Database Hosts
-+    Datenbank Hosts
- @endsection
- 
- @section('content-header')
--    <h1>Database Hosts<small>Database hosts that servers can have databases created on.</small></h1>
-+    <h1>Datenbanken Hosts<small>Erstelle hier einen zentralen Host-Punkt für weitere Datenbanken.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Database Hosts</li>
-+        <li class="active">Datenbank-Hosts</li>
-     </ol>
- @endsection
- 
-@@ -17,9 +17,9 @@
-     <div class="col-xs-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Host List</h3>
-+                <h3 class="box-title">Host Liste</h3>
-                 <div class="box-tools">
--                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newHostModal">Create New</button>
-+                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newHostModal">Erstellen</button>
-                 </div>
-             </div>
-             <div class="box-body table-responsive no-padding">
-@@ -30,8 +30,8 @@
-                             <th>Name</th>
-                             <th>Host</th>
-                             <th>Port</th>
--                            <th>Username</th>
--                            <th class="text-center">Databases</th>
-+                            <th>Benutzername</th>
-+                            <th class="text-center">Datenbanken</th>
-                             <th class="text-center">Node</th>
-                         </tr>
-                         @foreach ($hosts as $host)
-@@ -46,7 +46,7 @@
-                                     @if(! is_null($host->node))
-                                         <a href="{{ route('admin.nodes.view', $host->node->id) }}">{{ $host->node->name }}</a>
-                                     @else
--                                        <span class="label label-default">None</span>
-+                                        <span class="label label-default">Keine</span>
-                                     @endif
-                                 </td>
-                             </tr>
-@@ -63,42 +63,42 @@
-             <form action="{{ route('admin.databases') }}" method="POST">
-                 <div class="modal-header">
-                     <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
--                    <h4 class="modal-title">Create New Database Host</h4>
-+                    <h4 class="modal-title">Database Host hinzufügen</h4>
-                 </div>
-                 <div class="modal-body">
-                     <div class="form-group">
-                         <label for="pName" class="form-label">Name</label>
-                         <input type="text" name="name" id="pName" class="form-control" />
--                        <p class="text-muted small">A short identifier used to distinguish this location from others. Must be between 1 and 60 characters, for example, <code>us.nyc.lvl3</code>.</p>
-+                        <p class="text-muted small">Eine kurze Beschreibung zum Standort, wo der Server steht. Zum Beispiel:<code>de.srv1.hl4</code>.</p>
-                     </div>
-                     <div class="row">
-                         <div class="col-md-6">
-                             <label for="pHost" class="form-label">Host</label>
-                             <input type="text" name="host" id="pHost" class="form-control" />
--                            <p class="text-muted small">The IP address or FQDN that should be used when attempting to connect to this MySQL host <em>from the panel</em> to add new databases.</p>
-+                            <p class="text-muted small">Die IP-Adresse oder Domain, worüber die Datenbank und <em>das Panel läuft.</em></p>
-                         </div>
-                         <div class="col-md-6">
-                             <label for="pPort" class="form-label">Port</label>
-                             <input type="text" name="port" id="pPort" class="form-control" value="3306"/>
--                            <p class="text-muted small">The port that MySQL is running on for this host.</p>
-+                            <p class="text-muted small">Der Port, worüber alle Datenbanken laufen.</p>
-                         </div>
-                     </div>
-                     <div class="row">
-                         <div class="col-md-6">
--                            <label for="pUsername" class="form-label">Username</label>
-+                            <label for="pUsername" class="form-label">Benutzername</label>
-                             <input type="text" name="username" id="pUsername" class="form-control" />
--                            <p class="text-muted small">The username of an account that has enough permissions to create new users and databases on the system.</p>
-+                            <p class="text-muted small">Der Benutzername eines Kontos, das über ausreichende Berechtigungen verfügt, um neue Benutzer und Datenbanken auf dem System zu erstellen.</p>
-                         </div>
-                         <div class="col-md-6">
--                            <label for="pPassword" class="form-label">Password</label>
-+                            <label for="pPassword" class="form-label">Passwort</label>
-                             <input type="password" name="password" id="pPassword" class="form-control" />
--                            <p class="text-muted small">The password to the account defined.</p>
-+                            <p class="text-muted small">Das Passwort für das definierte Konto.</p>
-                         </div>
-                     </div>
-                     <div class="form-group">
-                         <label for="pNodeId" class="form-label">Linked Node</label>
-                         <select name="node_id" id="pNodeId" class="form-control">
--                            <option value="">None</option>
-+                            <option value="">Keine</option>
-                             @foreach($locations as $location)
-                                 <optgroup label="{{ $location->short }}">
-                                     @foreach($location->nodes as $node)
-@@ -107,14 +107,14 @@
-                                 </optgroup>
-                             @endforeach
-                         </select>
--                        <p class="text-muted small">This setting does nothing other than default to this database host when adding a database to a server on the selected node.</p>
-+                        <p class="text-muted small">Diese Einstellung bewirkt nichts anderes als standardmäßig diesen Datenbankhost, wenn eine Datenbank zu einem Server auf dem ausgewählten Knoten hinzugefügt wird.</p>
-                     </div>
-                 </div>
-                 <div class="modal-footer">
--                    <p class="text-danger small text-left">The account defined for this database host <strong>must</strong> have the <code>WITH GRANT OPTION</code> permission. If the defined account does not have this permission requests to create databases <em>will</em> fail. <strong>Do not use the same account details for MySQL that you have defined for this panel.</strong></p>
-+                    <p class="text-danger small text-left">Der Account der die Datenbank verwaltet <strong>muss</strong> die <code>WITH GRANT OPTION</code> Berechtigung haben. Wenn das nicht der Fall ist, dann wird das Erstellen einer Datenbank<em>fehlschlagen</em><strong>. Nutze nicht denselben Account für das Panel und MySQL. Zwei verschiedene sind wichtig!</strong></p>
-                     {!! csrf_field() !!}
--                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
--                    <button type="submit" class="btn btn-success btn-sm">Create</button>
-+                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
-+                    <button type="submit" class="btn btn-success btn-sm">Erstellen</button>
-                 </div>
-             </form>
-         </div>
-diff --git a/resources/views/admin/databases/view.blade.php b/resources/views/admin/databases/view.blade.php
-index e105751..e4b6ee1 100644
---- a/resources/views/admin/databases/view.blade.php
-+++ b/resources/views/admin/databases/view.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Database Hosts &rarr; View &rarr; {{ $host->name }}
-+    Datenbank-Hosts &rarr; Betrachte &rarr; {{ $host->name }}
- @endsection
- 
- @section('content-header')
--    <h1>{{ $host->name }}<small>Viewing associated databases and details for this database host.</small></h1>
-+    <h1>{{ $host->name }}<small>Anzeigen der zugehörigen Datenbanken und Details für diesen Datenbankhost.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.databases') }}">Database Hosts</a></li>
-+        <li><a href="{{ route('admin.databases') }}">Datenbank-Hosts</a></li>
-         <li class="active">{{ $host->name }}</li>
-     </ol>
- @endsection
-@@ -19,7 +19,7 @@
-         <div class="col-sm-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Host Details</h3>
-+                    <h3 class="box-title">Host-Details</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
-@@ -29,17 +29,17 @@
-                     <div class="form-group">
-                         <label for="pHost" class="form-label">Host</label>
-                         <input type="text" id="pHost" name="host" class="form-control" value="{{ old('host', $host->host) }}" />
--                        <p class="text-muted small">The IP address or FQDN that should be used when attempting to connect to this MySQL host <em>from the panel</em> to add new databases.</p>
-+                        <p class="text-muted small">Die IP-Adresse oder der FQDN, der verwendet werden soll, wenn versucht wird, eine Verbindung zu diesem MySQL-Host <em>vom Panel</em> aus herzustellen, um neue Datenbanken hinzuzufügen.</p>
-                     </div>
-                     <div class="form-group">
-                         <label for="pPort" class="form-label">Port</label>
-                         <input type="text" id="pPort" name="port" class="form-control" value="{{ old('port', $host->port) }}" />
--                        <p class="text-muted small">The port that MySQL is running on for this host.</p>
-+                        <p class="text-muted small">Der Port, auf dem MySQL-Server für diesen Host läuft.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="pNodeId" class="form-label">Linked Node</label>
-+                        <label for="pNodeId" class="form-label">Verlinkte Node</label>
-                         <select name="node_id" id="pNodeId" class="form-control">
--                            <option value="">None</option>
-+                            <option value="">Keine</option>
-                             @foreach($locations as $location)
-                                 <optgroup label="{{ $location->short }}">
-                                     @foreach($location->nodes as $node)
-@@ -48,7 +48,7 @@
-                                 </optgroup>
-                             @endforeach
-                         </select>
--                        <p class="text-muted small">This setting does nothing other than default to this database host when adding a database to a server on the selected node.</p>
-+                        <p class="text-muted small">Diese Einstellung bewirkt nichts anderes, als dass beim Hinzufügen einer Datenbank zu einem Server auf der ausgewählten Node standardmäßig dieser Datenbankhost verwendet wird.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -56,25 +56,25 @@
-         <div class="col-sm-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">User Details</h3>
-+                    <h3 class="box-title">Benutzer-Details</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
--                        <label for="pUsername" class="form-label">Username</label>
-+                        <label for="pUsername" class="form-label">Benutzername</label>
-                         <input type="text" name="username" id="pUsername" class="form-control" value="{{ old('username', $host->username) }}" />
--                        <p class="text-muted small">The username of an account that has enough permissions to create new users and databases on the system.</p>
-+                        <p class="text-muted small">Der Benutzername eines Kontos, das über ausreichende Berechtigungen verfügt, um neue Benutzer und Datenbanken im System zu erstellen.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="pPassword" class="form-label">Password</label>
-+                        <label for="pPassword" class="form-label">Passwort</label>
-                         <input type="password" name="password" id="pPassword" class="form-control" />
--                        <p class="text-muted small">The password to the account defined. Leave blank to continue using the assigned password.</p>
-+                        <p class="text-muted small">Das Passwort für das definierte Konto. Leer lassen, um weiterhin das zugewiesene Passwort zu verwenden.</p>
-                     </div>
-                     <hr />
--                    <p class="text-danger small text-left">The account defined for this database host <strong>must</strong> have the <code>WITH GRANT OPTION</code> permission. If the defined account does not have this permission requests to create databases <em>will</em> fail. <strong>Do not use the same account details for MySQL that you have defined for this panel.</strong></p>
-+                    <p class="text-danger small text-left">Der Account der die Datenbank verwaltet <strong>muss</strong> die <code>WITH GRANT OPTION</code> Berechtigung haben. Wenn das nicht der Fall ist, dann wird das Erstellen einer Datenbankfehlschlagen. <strong>Nutze nicht denselben Account für das Panel und MySQL. Zwei verschiedene sind wichtig!</strong></p>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Save</button>
-+                    <button name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Speichern</button>
-                     <button name="_method" value="DELETE" class="btn btn-sm btn-danger pull-left muted muted-hover"><i class="fa fa-trash-o"></i></button>
-                 </div>
-             </div>
-@@ -85,16 +85,16 @@
-     <div class="col-xs-12">
-         <div class="box">
-             <div class="box-header with-border">
--                <h3 class="box-title">Databases</h3>
-+                <h3 class="box-title">Datenbank</h3>
-             </div>
-             <div class="box-body table-responsive no-padding">
-                 <table class="table table-hover">
-                     <tr>
-                         <th>Server</th>
--                        <th>Database Name</th>
--                        <th>Username</th>
--                        <th>Connections From</th>
--                        <th>Max Connections</th>
-+                        <th>Datenbankname</th>
-+                        <th>Benutzername</th>
-+                        <th>Verbindungen von</th>
-+                        <th>Maximale Verbindungen</th>
-                         <th></th>
-                     </tr>
-                     @foreach($databases as $database)
-@@ -106,11 +106,11 @@
-                             @if($database->max_connections != null)
-                                 <td class="middle">{{ $database->max_connections }}</td>
-                             @else
--                                <td class="middle">Unlimited</td>
-+                                <td class="middle">Kein Limit</td>
-                             @endif
-                             <td class="text-center">
-                                 <a href="{{ route('admin.servers.view.database', $database->getRelation('server')->id) }}">
--                                    <button class="btn btn-xs btn-primary">Manage</button>
-+                                    <button class="btn btn-xs btn-primary">Verwalten</button>
-                                 </a>
-                             </td>
-                         </tr>
-diff --git a/resources/views/admin/eggs/new.blade.php b/resources/views/admin/eggs/new.blade.php
-index 9a0a078..2159965 100644
---- a/resources/views/admin/eggs/new.blade.php
-+++ b/resources/views/admin/eggs/new.blade.php
-@@ -1,15 +1,15 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Nests &rarr; New Egg
-+    Nests &rarr; Neues Egg
- @endsection
- 
- @section('content-header')
--    <h1>New Egg<small>Create a new Egg to assign to servers.</small></h1>
-+    <h1>Neues Egg<small>Erstellen Sie ein neues Egg, um es Servern zuzuweisen.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
--        <li class="active">New Egg</li>
-+        <li class="active">Neues Egg</li>
-     </ol>
- @endsection
- 
-@@ -19,43 +19,43 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Configuration</h3>
-+                    <h3 class="box-title">Konfiguration</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="row">
-                         <div class="col-sm-6">
-                             <div class="form-group">
--                                <label for="pNestId" class="form-label">Associated Nest</label>
-+                                <label for="pNestId" class="form-label">Zugehöriges Nest</label>
-                                 <div>
-                                     <select name="nest_id" id="pNestId">
-                                         @foreach($nests as $nest)
-                                             <option value="{{ $nest->id }}" {{ old('nest_id') != $nest->id ?: 'selected' }}>{{ $nest->name }} &lt;{{ $nest->author }}&gt;</option>
-                                         @endforeach
-                                     </select>
--                                    <p class="text-muted small">Think of a Nest as a category. You can put multiple Eggs in a nest, but consider putting only Eggs that are related to each other in each Nest.</p>
-+                                    <p class="text-muted small">Stellen Sie sich ein Nest als eine Kategorie vor. Du kannst mehrere Eggs in ein Nest legen, aber überlege dir, nur Eggs in jedes Nest zu legen, die miteinander verwandt sind.</p>
-                                 </div>
-                             </div>
-                             <div class="form-group">
-                                 <label for="pName" class="form-label">Name</label>
-                                 <input type="text" id="pName" name="name" value="{{ old('name') }}" class="form-control" />
--                                <p class="text-muted small">A simple, human-readable name to use as an identifier for this Egg. This is what users will see as their game server type.</p>
-+                                <p class="text-muted small">Ein einfacher, für Menschen lesbarer Name, der als Kennung für dieses Egg verwendet werden kann. Dies wird den Benutzern als ihr Gameserver-Typ angezeigt.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pDescription" class="form-label">Description</label>
-+                                <label for="pDescription" class="form-label">Beschreibung</label>
-                                 <textarea id="pDescription" name="description" class="form-control" rows="8">{{ old('description') }}</textarea>
--                                <p class="text-muted small">A description of this Egg.</p>
-+                                <p class="text-muted small">Eine Beschreibung dieses Eggs.</p>
-                             </div>
-                             <div class="form-group">
-                                 <div class="checkbox checkbox-primary no-margin-bottom">
-                                     <input id="pForceOutgoingIp" name="force_outgoing_ip" type="checkbox" value="1" {{ \Pterodactyl\Helpers\Utilities::checked('force_outgoing_ip', 0) }} />
--                                    <label for="pForceOutgoingIp" class="strong">Force Outgoing IP</label>
-+                                    <label for="pForceOutgoingIp" class="strong">Ausgehende IP erzwingen</label>
-                                     <p class="text-muted small">
--                                        Forces all outgoing network traffic to have its Source IP NATed to the IP of the server's primary allocation IP.
--                                        Required for certain games to work properly when the Node has multiple public IP addresses.
-+                                        Erzwingt, dass der gesamte ausgehende Netzwerkverkehr seine Quell-IP mit der IP der primären Zuordnungs-IP des Servers NATiert.
-+                                        Erforderlich, damit bestimmte Spiele ordnungsgemäß funktionieren, wenn die Node mehrere öffentliche IP-Adressen hat.
-                                         <br>
-                                         <strong>
--                                            Enabling this option will disable internal networking for any servers using this egg,
--                                            causing them to be unable to internally access other servers on the same node.
-+                                            Das Aktivieren dieser Option deaktiviert das interne Netzwerk für alle Server,
-+                                            die dieses Egg verwenden, wodurch sie nicht mehr intern auf andere Server auf dieselbe Node zugreifen können.
-                                         </strong>
-                                     </p>
-                                 </div>
-@@ -65,12 +65,12 @@
-                             <div class="form-group">
-                                 <label for="pDockerImage" class="control-label">Docker Images</label>
-                                 <textarea id="pDockerImages" name="docker_images" rows="4" placeholder="quay.io/pterodactyl/service" class="form-control">{{ old('docker_images') }}</textarea>
--                                <p class="text-muted small">The docker images available to servers using this egg. Enter one per line. Users will be able to select from this list of images if more than one value is provided.</p>
-+                                <p class="text-muted small">Die Docker-Images, die für Server, die dieses Egg verwenden, verfügbar sind. Gib einen Wert pro Zeile ein. Wenn mehr als ein Wert angegeben wird, können die Nutzer aus dieser Liste von Images auswählen.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pStartup" class="control-label">Startup Command</label>
-+                                <label for="pStartup" class="control-label">Startbefehl</label>
-                                 <textarea id="pStartup" name="startup" class="form-control" rows="10">{{ old('startup') }}</textarea>
--                                <p class="text-muted small">The default startup command that should be used for new servers created with this Egg. You can change this per-server as needed.</p>
-+                                <p class="text-muted small">Der standardmäßige Startbefehl, der für neue Server verwendet werden sollte, die mit diesem Egg erstellt wurden. Du kannst dies für jeden Server nach Bedarf ändern.</p>
-                             </div>
-                         </div>
-                     </div>
-@@ -80,51 +80,51 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Process Management</h3>
-+                    <h3 class="box-title">Prozessmanagement</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="row">
-                         <div class="col-xs-12">
-                             <div class="alert alert-warning">
--                                <p>All fields are required unless you select a separate option from the 'Copy Settings From' dropdown, in which case fields may be left blank to use the values from that option.</p>
-+                                <p>Alle Felder müssen ausgefüllt werden, es sei denn, du wählst eine andere Option aus dem Dropdown-Menü "Einstellungen kopieren von". In diesem Fall können die Felder leer gelassen werden, um die Werte aus dieser Option zu verwenden.</p>
-                             </div>
-                         </div>
-                         <div class="col-sm-6">
-                             <div class="form-group">
--                                <label for="pConfigFrom" class="form-label">Copy Settings From</label>
-+                                <label for="pConfigFrom" class="form-label">Einstellungen kopieren von</label>
-                                 <select name="config_from" id="pConfigFrom" class="form-control">
--                                    <option value="">None</option>
-+                                    <option value="">Keinem</option>
-                                 </select>
--                                <p class="text-muted small">If you would like to default to settings from another Egg select it from the dropdown above.</p>
-+                                <p class="text-muted small">Wenn du die Einstellungen eines anderen Eggs verwenden möchtest, wähle es aus dem Dropdown-Menü oben aus.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pConfigStop" class="form-label">Stop Command</label>
-+                                <label for="pConfigStop" class="form-label">Stop-Befehl</label>
-                                 <input type="text" id="pConfigStop" name="config_stop" class="form-control" value="{{ old('config_stop') }}" />
--                                <p class="text-muted small">The command that should be sent to server processes to stop them gracefully. If you need to send a <code>SIGINT</code> you should enter <code>^C</code> here.</p>
-+                                <p class="text-muted small">Der Befehl, der an Serverprozesse gesendet werden sollte, um sie ordnungsgemäß zu stoppen. Wenn du einen <code>SIGINT</code> senden musst, gib hier <code>^C</code> ein.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pConfigLogs" class="form-label">Log Configuration</label>
-+                                <label for="pConfigLogs" class="form-label">Log-Konfiguration</label>
-                                 <textarea data-action="handle-tabs" id="pConfigLogs" name="config_logs" class="form-control" rows="6">{{ old('config_logs') }}</textarea>
--                                <p class="text-muted small">This should be a JSON representation of where log files are stored, and whether or not the daemon should be creating custom logs.</p>
-+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation sein, wo Log-Dateien gespeichert werden und ob der Daemon benutzerdefinierte Protokolle erstellen soll oder nicht.</p>
-                             </div>
-                         </div>
-                         <div class="col-sm-6">
-                             <div class="form-group">
--                                <label for="pConfigFiles" class="form-label">Configuration Files</label>
-+                                <label for="pConfigFiles" class="form-label">Konfigurationsdateien</label>
-                                 <textarea data-action="handle-tabs" id="pConfigFiles" name="config_files" class="form-control" rows="6">{{ old('config_files') }}</textarea>
--                                <p class="text-muted small">This should be a JSON representation of configuration files to modify and what parts should be changed.</p>
-+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation der zu ändernden Konfigurationsdateien und der zu ändernden Teile sein.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pConfigStartup" class="form-label">Start Configuration</label>
-+                                <label for="pConfigStartup" class="form-label">Startkonfiguration</label>
-                                 <textarea data-action="handle-tabs" id="pConfigStartup" name="config_startup" class="form-control" rows="6">{{ old('config_startup') }}</textarea>
--                                <p class="text-muted small">This should be a JSON representation of what values the daemon should be looking for when booting a server to determine completion.</p>
-+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation dessen sein, nach welchen Werten der Daemon beim Booten eines Servers suchen sollte, um den Abschluss zu bestimmen.</p>
-                             </div>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" class="btn btn-success btn-sm pull-right">Create</button>
-+                    <button type="submit" class="btn btn-success btn-sm pull-right">Erstellen</button>
-                 </div>
-             </div>
-         </div>
-@@ -141,7 +141,7 @@
-         $('#pConfigFrom').select2();
-     });
-     $('#pNestId').on('change', function (event) {
--        $('#pConfigFrom').html('<option value="">None</option>').select2({
-+        $('#pConfigFrom').html('<option value="">Keinem</option>').select2({
-             data: $.map(_.get(Pterodactyl.nests, $(this).val() + '.eggs', []), function (item) {
-                 return {
-                     id: item.id,
-diff --git a/resources/views/admin/eggs/scripts.blade.php b/resources/views/admin/eggs/scripts.blade.php
-index 5bbc9ee..c78a73c 100644
---- a/resources/views/admin/eggs/scripts.blade.php
-+++ b/resources/views/admin/eggs/scripts.blade.php
-@@ -1,17 +1,17 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Nests &rarr; Egg: {{ $egg->name }} &rarr; Install Script
-+    Nests &rarr; Egg: {{ $egg->name }} &rarr; Installationsskript
- @endsection
- 
- @section('content-header')
--    <h1>{{ $egg->name }}<small>Manage the install script for this Egg.</small></h1>
-+    <h1>{{ $egg->name }}<small>Verwalten des Installationsskripts für dieses Egg.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
-         <li><a href="{{ route('admin.nests.view', $egg->nest->id) }}">{{ $egg->nest->name }}</a></li>
-         <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">{{ $egg->name }}</a></li>
--        <li class="active">{{ $egg->name }}</li>
-+        <li class="active">Installationsskript</li>
-     </ol>
- @endsection
- 
-@@ -20,9 +20,9 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Configuration</a></li>
--                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variables</a></li>
--                <li class="active"><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Install Script</a></li>
-+                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Konfiguration</a></li>
-+                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variablen</a></li>
-+                <li class="active"><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Installationsskript</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -32,12 +32,12 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Install Script</h3>
-+                    <h3 class="box-title">Installationsskript</h3>
-                 </div>
-                 @if(! is_null($egg->copyFrom))
-                     <div class="box-body">
-                         <div class="callout callout-warning no-margin">
--                            This service option is copying installation scripts and container options from <a href="{{ route('admin.nests.egg.view', $egg->copyFrom->id) }}">{{ $egg->copyFrom->name }}</a>. Any changes you make to this script will not apply unless you select "None" from the dropdown box below.
-+                            Diese Dienstoption kopiert Installationsskripts und Containeroptionen von <a href="{{ route('admin.nests.egg.view', $egg->copyFrom->id) }}">{{ $egg->copyFrom->name }}</a>. Alle Änderungen, die du an diesem Skript vornimmst, werden nicht übernommen, es sei denn, du wählst in der Dropdown-Box unten "Keinem" aus.
-                         </div>
-                     </div>
-                 @endif
-@@ -47,29 +47,29 @@
-                 <div class="box-body">
-                     <div class="row">
-                         <div class="form-group col-sm-4">
--                            <label class="control-label">Copy Script From</label>
-+                            <label class="control-label">Skript kopieren von</label>
-                             <select id="pCopyScriptFrom" name="copy_script_from">
--                                <option value="">None</option>
-+                                <option value="">Keinem</option>
-                                 @foreach($copyFromOptions as $opt)
-                                     <option value="{{ $opt->id }}" {{ $egg->copy_script_from !== $opt->id ?: 'selected' }}>{{ $opt->name }}</option>
-                                 @endforeach
-                             </select>
--                            <p class="text-muted small">If selected, script above will be ignored and script from selected option will be used in place.</p>
-+                            <p class="text-muted small">Wenn diese Option ausgewählt ist, wird das obige Skript ignoriert und stattdessen das Skript der ausgewählten Option verwendet.</p>
-                         </div>
-                         <div class="form-group col-sm-4">
--                            <label class="control-label">Script Container</label>
-+                            <label class="control-label">Skript-Container</label>
-                             <input type="text" name="script_container" class="form-control" value="{{ $egg->script_container }}" />
--                            <p class="text-muted small">Docker container to use when running this script for the server.</p>
-+                            <p class="text-muted small">Docker-Container, der beim Ausführen dieses Skripts für den Server verwendet werden soll.</p>
-                         </div>
-                         <div class="form-group col-sm-4">
--                            <label class="control-label">Script Entrypoint Command</label>
-+                            <label class="control-label">Skript-Startbefehl</label>
-                             <input type="text" name="script_entry" class="form-control" value="{{ $egg->script_entry }}" />
--                            <p class="text-muted small">The entrypoint command to use for this script.</p>
-+                            <p class="text-muted small">Der Startbefehl, der für dieses Skript verwendet werden soll.</p>
-                         </div>
-                     </div>
-                     <div class="row">
-                         <div class="col-xs-12 text-muted">
--                            The following service options rely on this script:
-+                            Die folgenden Dienstoptionen basieren auf diesem Skript:
-                             @if(count($relyOnScript) > 0)
-                                 @foreach($relyOnScript as $rely)
-                                     <a href="{{ route('admin.nests.egg.view', $rely->id) }}">
-@@ -77,7 +77,7 @@
-                                     </a>
-                                 @endforeach
-                             @else
--                                <em>none</em>
-+                                <em>Keine</em>
-                             @endif
-                         </div>
-                     </div>
-@@ -85,7 +85,7 @@
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
-                     <textarea name="script_install" class="hidden"></textarea>
--                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Save</button>
-+                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Speichern</button>
-                 </div>
-             </div>
-         </div>
-diff --git a/resources/views/admin/eggs/variables.blade.php b/resources/views/admin/eggs/variables.blade.php
-index fbc1478..a7b8dbc 100644
---- a/resources/views/admin/eggs/variables.blade.php
-+++ b/resources/views/admin/eggs/variables.blade.php
-@@ -1,17 +1,17 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Egg &rarr; {{ $egg->name }} &rarr; Variables
-+    Egg &rarr; {{ $egg->name }} &rarr; Variablen
- @endsection
- 
- @section('content-header')
--    <h1>{{ $egg->name }}<small>Managing variables for this Egg.</small></h1>
-+    <h1>{{ $egg->name }}<small>Verwalten von Variablen für dieses Egg.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
-         <li><a href="{{ route('admin.nests.view', $egg->nest->id) }}">{{ $egg->nest->name }}</a></li>
-         <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">{{ $egg->name }}</a></li>
--        <li class="active">Variables</li>
-+        <li class="active">Variablen</li>
-     </ol>
- @endsection
- 
-@@ -20,9 +20,9 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Configuration</a></li>
--                <li class="active"><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variables</a></li>
--                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Install Script</a></li>
-+                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Konfiguration</a></li>
-+                <li class="active"><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variablen</a></li>
-+                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Installationsskript</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -31,7 +31,7 @@
-     <div class="col-xs-12">
-         <div class="box no-border">
-             <div class="box-body">
--                <a href="#" class="btn btn-sm btn-success pull-right" data-toggle="modal" data-target="#newVariableModal">Create New Variable</a>
-+                <a href="#" class="btn btn-sm btn-success pull-right" data-toggle="modal" data-target="#newVariableModal">Neue Variable erstellen</a>
-             </div>
-         </div>
-     </div>
-@@ -50,38 +50,38 @@
-                             <input type="text" name="name" value="{{ $variable->name }}" class="form-control" />
-                         </div>
-                         <div class="form-group">
--                            <label class="form-label">Description</label>
-+                            <label class="form-label">Beschreibung</label>
-                             <textarea name="description" class="form-control" rows="3">{{ $variable->description }}</textarea>
-                         </div>
-                         <div class="row">
-                             <div class="form-group col-md-6">
--                                <label class="form-label">Environment Variable</label>
-+                                <label class="form-label">Umgebungsvariable</label>
-                                 <input type="text" name="env_variable" value="{{ $variable->env_variable }}" class="form-control" />
-                             </div>
-                             <div class="form-group col-md-6">
--                                <label class="form-label">Default Value</label>
-+                                <label class="form-label">Standardwert</label>
-                                 <input type="text" name="default_value" value="{{ $variable->default_value }}" class="form-control" />
-                             </div>
-                             <div class="col-xs-12">
--                                <p class="text-muted small">This variable can be accessed in the startup command by using <code>{{ $variable->env_variable }}</code>.</p>
-+                                <p class="text-muted small">Auf diese Variable kann im Startbefehl mit <code>{{ $variable->env_variable }}</code> zugegriffen werden.</p>
-                             </div>
-                         </div>
-                         <div class="form-group">
--                            <label class="form-label">Permissions</label>
-+                            <label class="form-label">Berechtigungen</label>
-                             <select name="options[]" class="pOptions form-control" multiple>
--                                <option value="user_viewable" {{ (! $variable->user_viewable) ?: 'selected' }}>Users Can View</option>
--                                <option value="user_editable" {{ (! $variable->user_editable) ?: 'selected' }}>Users Can Edit</option>
-+                                <option value="user_viewable" {{ (! $variable->user_viewable) ?: 'selected' }}>Nutzer können ansehen</option>
-+                                <option value="user_editable" {{ (! $variable->user_editable) ?: 'selected' }}>Nutzer können bearbeiten</option>
-                             </select>
-                         </div>
-                         <div class="form-group">
--                            <label class="form-label">Input Rules</label>
-+                            <label class="form-label">Eingaberegeln</label>
-                             <input type="text" name="rules" class="form-control" value="{{ $variable->rules }}" />
--                            <p class="text-muted small">These rules are defined using standard <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework validation rules</a>.</p>
-+                            <p class="text-muted small">Diese Regeln werden mithilfe von standardmäßigen <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework-Validierungsregeln</a> definiert.</p>
-                         </div>
-                     </div>
-                     <div class="box-footer">
-                         {!! csrf_field() !!}
--                        <button class="btn btn-sm btn-primary pull-right" name="_method" value="PATCH" type="submit">Save</button>
-+                        <button class="btn btn-sm btn-primary pull-right" name="_method" value="PATCH" type="submit">Speichern</button>
-                         <button class="btn btn-sm btn-danger pull-left muted muted-hover" data-action="delete" name="_method" value="DELETE" type="submit"><i class="fa fa-trash-o"></i></button>
-                     </div>
-                 </form>
-@@ -94,7 +94,7 @@
-         <div class="modal-content">
-             <div class="modal-header">
-                 <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
--                <h4 class="modal-title">Create New Egg Variable</h4>
-+                <h4 class="modal-title">Neue Egg-Variable erstellen</h4>
-             </div>
-             <form action="{{ route('admin.nests.egg.variables', $egg->id) }}" method="POST">
-                 <div class="modal-body">
-@@ -103,39 +103,39 @@
-                         <input type="text" name="name" class="form-control" value="{{ old('name') }}"/>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Description</label>
-+                        <label class="control-label">Beschreibung</label>
-                         <textarea name="description" class="form-control" rows="3">{{ old('description') }}</textarea>
-                     </div>
-                     <div class="row">
-                         <div class="form-group col-md-6">
--                            <label class="control-label">Environment Variable <span class="field-required"></span></label>
-+                            <label class="control-label">Umgebungsvariable <span class="field-required"></span></label>
-                             <input type="text" name="env_variable" class="form-control" value="{{ old('env_variable') }}" />
-                         </div>
-                         <div class="form-group col-md-6">
--                            <label class="control-label">Default Value</label>
-+                            <label class="control-label">Standardwert</label>
-                             <input type="text" name="default_value" class="form-control" value="{{ old('default_value') }}" />
-                         </div>
-                         <div class="col-xs-12">
--                            <p class="text-muted small">This variable can be accessed in the startup command by entering <code>@{{environment variable value}}</code>.</p>
-+                            <p class="text-muted small">Auf diese Variable kann im Startbefehl durch Eingabe von <code>@{{environment variable value}}</code> zugegriffen werden.</p>
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Permissions</label>
-+                        <label class="control-label">Berechtigungen</label>
-                         <select name="options[]" class="pOptions form-control" multiple>
--                            <option value="user_viewable">Users Can View</option>
--                            <option value="user_editable">Users Can Edit</option>
-+                            <option value="user_viewable">Nutzer können ansehen</option>
-+                            <option value="user_editable">Nutzer können bearbeiten</option>
-                         </select>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Input Rules <span class="field-required"></span></label>
-+                        <label class="control-label">Eingaberegeln <span class="field-required"></span></label>
-                         <input type="text" name="rules" class="form-control" value="{{ old('rules', 'required|string|max:20') }}" placeholder="required|string|max:20" />
--                        <p class="text-muted small">These rules are defined using standard <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework validation rules</a>.</p>
-+                            <p class="text-muted small">Diese Regeln werden mithilfe von standardmäßigen <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework-Validierungsregeln</a> definiert.</p>
-                     </div>
-                 </div>
-                 <div class="modal-footer">
-                     {!! csrf_field() !!}
--                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Close</button>
--                    <button type="submit" class="btn btn-primary">Create Variable</button>
-+                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Schließen</button>
-+                    <button type="submit" class="btn btn-primary">Variable erstellen</button>
-                 </div>
-             </form>
-         </div>
-@@ -148,7 +148,7 @@
-     <script>
-         $('.pOptions').select2();
-         $('[data-action="delete"]').on('mouseenter', function (event) {
--            $(this).find('i').html(' Delete Variable');
-+            $(this).find('i').html(' Variable löschen');
-         }).on('mouseleave', function (event) {
-             $(this).find('i').html('');
-         });
-diff --git a/resources/views/admin/eggs/view.blade.php b/resources/views/admin/eggs/view.blade.php
-index b999d2a..2106519 100644
---- a/resources/views/admin/eggs/view.blade.php
-+++ b/resources/views/admin/eggs/view.blade.php
-@@ -19,9 +19,9 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li class="active"><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Configuration</a></li>
--                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variables</a></li>
--                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Install Script</a></li>
-+                <li class="active"><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Konfiguration</a></li>
-+                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variablen</a></li>
-+                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Installationsskript</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -34,16 +34,16 @@
-                     <div class="row">
-                         <div class="col-xs-8">
-                             <div class="form-group no-margin-bottom">
--                                <label for="pName" class="control-label">Egg File</label>
-+                                <label for="pName" class="control-label">Egg-Datei</label>
-                                 <div>
-                                     <input type="file" name="import_file" class="form-control" style="border: 0;margin-left:-10px;" />
--                                    <p class="text-muted small no-margin-bottom">If you would like to replace settings for this Egg by uploading a new JSON file, simply select it here and press "Update Egg". This will not change any existing startup strings or Docker images for existing servers.</p>
-+                                    <p class="text-muted small no-margin-bottom">Wenn du die Einstellungen für dieses Egg ersetzen möchtest, indem du eine neue JSON-Datei hochlädst, wähle sie einfach hier aus und klicke auf "Egg aktualisieren". Dadurch werden keine bestehenden Startstrings oder Docker-Images für bestehende Server geändert.</p>
-                                 </div>
-                             </div>
-                         </div>
-                         <div class="col-xs-4">
-                             {!! csrf_field() !!}
--                            <button type="submit" name="_method" value="PUT" class="btn btn-sm btn-danger pull-right">Update Egg</button>
-+                            <button type="submit" name="_method" value="PUT" class="btn btn-sm btn-danger pull-right">Egg aktualisieren</button>
-                         </div>
-                     </div>
-                 </div>
-@@ -56,7 +56,7 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Configuration</h3>
-+                    <h3 class="box-title">Konfiguration</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="row">
-@@ -64,7 +64,7 @@
-                             <div class="form-group">
-                                 <label for="pName" class="control-label">Name <span class="field-required"></span></label>
-                                 <input type="text" id="pName" name="name" value="{{ $egg->name }}" class="form-control" />
--                                <p class="text-muted small">A simple, human-readable name to use as an identifier for this Egg.</p>
-+                                <p class="text-muted small">Ein einfacher, für Menschen lesbarer Name, der als Kennung für dieses Egg verwendet werden kann. Dies wird den Benutzern als ihr Gameserver-Typ angezeigt.</p>
-                             </div>
-                             <div class="form-group">
-                                 <label for="pUuid" class="control-label">UUID</label>
-@@ -74,29 +74,29 @@
-                             <div class="form-group">
-                                 <label for="pAuthor" class="control-label">Author</label>
-                                 <input type="text" id="pAuthor" readonly value="{{ $egg->author }}" class="form-control" />
--                                <p class="text-muted small">The author of this version of the Egg. Uploading a new Egg configuration from a different author will change this.</p>
-+                                <p class="text-muted small">Der Autor dieser Version des Egg. Das Hochladen einer neuen Egg-Konfiguration von einem anderen Autor ändert dies.</p>
-                             </div>
-                             <div class="form-group">
-                                 <label for="pDockerImage" class="control-label">Docker Images <span class="field-required"></span></label>
-                                 <textarea id="pDockerImages" name="docker_images" class="form-control" rows="4">{{ implode(PHP_EOL, $images) }}</textarea>
-                                 <p class="text-muted small">
--                                    The docker images available to servers using this egg. Enter one per line. Users
--                                    will be able to select from this list of images if more than one value is provided.
--                                    Optionally, a display name may be provided by prefixing the image with the name
--                                    followed by a pipe character, and then the image URL. Example: <code>Display Name|ghcr.io/my/egg</code>
-+                                    Die Docker-Images, die für Server, die dieses Egg verwenden, verfügbar sind. Gib einen Wert pro Zeile ein.
-+                                    Wenn mehr als ein Wert angegeben wird, können die Nutzer aus dieser Liste von Images auswählen
-+                                    Optional kann ein Anzeigename bereitgestellt werden, indem dem Bild der Name vorangestellt wird,
-+                                    gefolgt von einem Pipe-Zeichen und dann der Bild-URL. Beispiel: <code>Anzeigename|ghcr.io/my/egg</code>
-                                 </p>
-                             </div>
-                             <div class="form-group">
-                                 <div class="checkbox checkbox-primary no-margin-bottom">
-                                     <input id="pForceOutgoingIp" name="force_outgoing_ip" type="checkbox" value="1" @if($egg->force_outgoing_ip) checked @endif />
--                                    <label for="pForceOutgoingIp" class="strong">Force Outgoing IP</label>
-+                                    <label for="pForceOutgoingIp" class="strong">Ausgehende IP erzwingen</label>
-                                     <p class="text-muted small">
--                                        Forces all outgoing network traffic to have its Source IP NATed to the IP of the server's primary allocation IP.
--                                        Required for certain games to work properly when the Node has multiple public IP addresses.
-+                                        Erzwingt, dass der gesamte ausgehende Netzwerkverkehr seine Quell-IP mit der IP der primären Zuordnungs-IP des Servers NATiert.
-+                                        Erforderlich, damit bestimmte Spiele ordnungsgemäß funktionieren, wenn die Node mehrere öffentliche IP-Adressen hat.
-                                         <br>
-                                         <strong>
--                                            Enabling this option will disable internal networking for any servers using this egg,
--                                            causing them to be unable to internally access other servers on the same node.
-+                                            Das Aktivieren dieser Option deaktiviert das interne Netzwerk für alle Server,
-+                                            die dieses Egg verwenden, wodurch sie nicht mehr intern auf andere Server auf dieselbe Node zugreifen können.
-                                         </strong>
-                                     </p>
-                                 </div>
-@@ -105,14 +105,14 @@
-                         </div>
-                         <div class="col-sm-6">
-                             <div class="form-group">
--                                <label for="pDescription" class="control-label">Description</label>
-+                                <label for="pDescription" class="control-label">Beschreibung</label>
-                                 <textarea id="pDescription" name="description" class="form-control" rows="8">{{ $egg->description }}</textarea>
--                                <p class="text-muted small">A description of this Egg that will be displayed throughout the Panel as needed.</p>
-+                                <p class="text-muted small">Eine Beschreibung dieses Eggs, die bei Bedarf im gesamten Panel angezeigt wird.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pStartup" class="control-label">Startup Command <span class="field-required"></span></label>
-+                                <label for="pStartup" class="control-label">Startbefehl <span class="field-required"></span></label>
-                                 <textarea id="pStartup" name="startup" class="form-control" rows="8">{{ $egg->startup }}</textarea>
--                                <p class="text-muted small">The default startup command that should be used for new servers using this Egg.</p>
-+                                <p class="text-muted small">Der standardmäßige Startbefehl, der für neue Server verwendet werden sollte, die dieses Eggs verwenden.</p>
-                             </div>
-                         </div>
-                     </div>
-@@ -122,56 +122,56 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Process Management</h3>
-+                    <h3 class="box-title">Prozessmanagement</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="row">
-                         <div class="col-xs-12">
-                             <div class="alert alert-warning">
--                                <p>The following configuration options should not be edited unless you understand how this system works. If wrongly modified it is possible for the daemon to break.</p>
--                                <p>All fields are required unless you select a separate option from the 'Copy Settings From' dropdown, in which case fields may be left blank to use the values from that Egg.</p>
-+                                <p>Die folgenden Konfigurationsoptionen sollten nicht bearbeitet werden, es sei denn, Sie verstehen, wie dieses System funktioniert. Bei falscher Änderung kann der Daemon kaputt gehen.</p>
-+                                <p>Alle Felder sind erforderlich, es sei denn, Sie wählen eine separate Option aus der Dropdown-Liste „Einstellungen kopieren von“. In diesem Fall können die Felder leer gelassen werden, um die Werte aus diesem Egg zu verwenden.</p>
-                             </div>
-                         </div>
-                         <div class="col-sm-6">
-                             <div class="form-group">
--                                <label for="pConfigFrom" class="form-label">Copy Settings From</label>
-+                                <label for="pConfigFrom" class="form-label">Einstellungen kopieren von</label>
-                                 <select name="config_from" id="pConfigFrom" class="form-control">
--                                    <option value="">None</option>
-+                                    <option value="">Keinem</option>
-                                     @foreach($egg->nest->eggs as $o)
-                                         <option value="{{ $o->id }}" {{ ($egg->config_from !== $o->id) ?: 'selected' }}>{{ $o->name }} &lt;{{ $o->author }}&gt;</option>
-                                     @endforeach
-                                 </select>
--                                <p class="text-muted small">If you would like to default to settings from another Egg select it from the menu above.</p>
-+                                <p class="text-muted small">Wenn du die Einstellungen eines anderen Eggs verwenden möchtest, wähle es im Menü oben aus.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pConfigStop" class="form-label">Stop Command</label>
-+                                <label for="pConfigStop" class="form-label">Stop-Befehl</label>
-                                 <input type="text" id="pConfigStop" name="config_stop" class="form-control" value="{{ $egg->config_stop }}" />
--                                <p class="text-muted small">The command that should be sent to server processes to stop them gracefully. If you need to send a <code>SIGINT</code> you should enter <code>^C</code> here.</p>
-+                                <p class="text-muted small">Der Befehl, der an Serverprozesse gesendet werden sollte, um sie ordnungsgemäß zu stoppen. Wenn du einen <code>SIGINT</code> senden musst, gib hier <code>^C</code> ein.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pConfigLogs" class="form-label">Log Configuration</label>
-+                                <label for="pConfigLogs" class="form-label">Log-Konfiguration</label>
-                                 <textarea data-action="handle-tabs" id="pConfigLogs" name="config_logs" class="form-control" rows="6">{{ ! is_null($egg->config_logs) ? json_encode(json_decode($egg->config_logs), JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) : '' }}</textarea>
--                                <p class="text-muted small">This should be a JSON representation of where log files are stored, and whether or not the daemon should be creating custom logs.</p>
-+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation sein, wo Log-Dateien gespeichert werden und ob der Daemon benutzerdefinierte Protokolle erstellen soll oder nicht.</p>
-                             </div>
-                         </div>
-                         <div class="col-sm-6">
-                             <div class="form-group">
--                                <label for="pConfigFiles" class="form-label">Configuration Files</label>
-+                                <label for="pConfigFiles" class="form-label">Konfigurationsdateien</label>
-                                 <textarea data-action="handle-tabs" id="pConfigFiles" name="config_files" class="form-control" rows="6">{{ ! is_null($egg->config_files) ? json_encode(json_decode($egg->config_files), JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) : '' }}</textarea>
--                                <p class="text-muted small">This should be a JSON representation of configuration files to modify and what parts should be changed.</p>
-+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation der zu ändernden Konfigurationsdateien und der zu ändernden Teile sein.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pConfigStartup" class="form-label">Start Configuration</label>
-+                                <label for="pConfigStartup" class="form-label">Startkonfiguration</label>
-                                 <textarea data-action="handle-tabs" id="pConfigStartup" name="config_startup" class="form-control" rows="6">{{ ! is_null($egg->config_startup) ? json_encode(json_decode($egg->config_startup), JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) : '' }}</textarea>
--                                <p class="text-muted small">This should be a JSON representation of what values the daemon should be looking for when booting a server to determine completion.</p>
-+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation dessen sein, nach welchen Werten der Daemon beim Booten eines Servers suchen sollte, um den Abschluss zu bestimmen.</p>
-                             </div>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Save</button>
--                    <a href="{{ route('admin.nests.egg.export', $egg->id) }}" class="btn btn-sm btn-info pull-right" style="margin-right:10px;">Export</a>
-+                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Speichern</button>
-+                    <a href="{{ route('admin.nests.egg.export', $egg->id) }}" class="btn btn-sm btn-info pull-right" style="margin-right:10px;">Exportieren</a>
-                     <button id="deleteButton" type="submit" name="_method" value="DELETE" class="btn btn-danger btn-sm muted muted-hover">
-                         <i class="fa fa-trash-o"></i>
-                     </button>
-@@ -187,7 +187,7 @@
-     <script>
-     $('#pConfigFrom').select2();
-     $('#deleteButton').on('mouseenter', function (event) {
--        $(this).find('i').html(' Delete Egg');
-+        $(this).find('i').html(' Egg löschen');
-     }).on('mouseleave', function (event) {
-         $(this).find('i').html('');
-     });
-diff --git a/resources/views/admin/index.blade.php b/resources/views/admin/index.blade.php
-index ed26a92..7476068 100644
---- a/resources/views/admin/index.blade.php
-+++ b/resources/views/admin/index.blade.php
-@@ -5,10 +5,10 @@
- @endsection
- 
- @section('content-header')
--    <h1>Administrative Overview<small>A quick glance at your system.</small></h1>
-+    <h1>Admin Dashboard<small>Hier ist deine Übersicht vom System.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Index</li>
-+        <li class="active">Infoseite</li>
-     </ol>
- @endsection
- 
-@@ -23,13 +23,13 @@
-             @endif
-         ">
-             <div class="box-header with-border">
--                <h3 class="box-title">System Information</h3>
-+                <h3 class="box-title">Status des Systems</h3>
-             </div>
-             <div class="box-body">
-                 @if ($version->isLatestPanel())
--                    You are running Pterodactyl Panel version <code>{{ config('app.version') }}</code>. Your panel is up-to-date!
-+                    Auf diesem Server läuft derzeit die Version <code>{{ config('app.version') }}</code>. Du bist auf dem neuesten Stand!
-                 @else
--                    Your panel is <strong>not up-to-date!</strong> The latest version is <a href="https://github.com/Pterodactyl/Panel/releases/v{{ $version->getPanel() }}" target="_blank"><code>{{ $version->getPanel() }}</code></a> and you are currently running version <code>{{ config('app.version') }}</code>.
-+                    Das Panel ist <strong>nicht aktuell!</strong> Die letzte Version ist <a href="https://github.com/Pterodactyl/Panel/releases/v{{ $version->getPanel() }}" target="_blank"><code>{{ $version->getPanel() }}</code></a> und du nutzt derzeit <code>{{ config('app.version') }}</code> auf diesem Server. WARNUNG: Du nutzt GermanDactyl für die Übersetzung, es stehen möglicherweise noch keine Updates zur Verfügung!
-                 @endif
-             </div>
-         </div>
-@@ -37,17 +37,17 @@
- </div>
- <div class="row">
-     <div class="col-xs-6 col-sm-3 text-center">
--        <a href="{{ $version->getDiscord() }}"><button class="btn btn-warning" style="width:100%;"><i class="fa fa-fw fa-support"></i> Get Help <small>(via Discord)</small></button></a>
-+        <a href="{{ $version->getDiscord() }}"><button class="btn btn-warning" style="width:100%;"><i class="fa fa-fw fa-support"></i> Hilfe erhalten <small>(Englisch)</small></button></a>
-     </div>
-     <div class="col-xs-6 col-sm-3 text-center">
--        <a href="https://pterodactyl.io"><button class="btn btn-primary" style="width:100%;"><i class="fa fa-fw fa-link"></i> Documentation</button></a>
-+        <a href="https://pterodactyl.io"><button class="btn btn-primary" style="width:100%;"><i class="fa fa-fw fa-link"></i> Wiki-Page</button></a>
-     </div>
-     <div class="clearfix visible-xs-block">&nbsp;</div>
-     <div class="col-xs-6 col-sm-3 text-center">
-         <a href="https://github.com/pterodactyl/panel"><button class="btn btn-primary" style="width:100%;"><i class="fa fa-fw fa-support"></i> Github</button></a>
-     </div>
-     <div class="col-xs-6 col-sm-3 text-center">
--        <a href="{{ $version->getDonations() }}"><button class="btn btn-success" style="width:100%;"><i class="fa fa-fw fa-money"></i> Support the Project</button></a>
-+        <a href="{{ $version->getDonations() }}"><button class="btn btn-success" style="width:100%;"><i class="fa fa-fw fa-money"></i> Unterstütze Pterodactyl</button></a>
-     </div>
- </div>
- @endsection
-diff --git a/resources/views/admin/locations/index.blade.php b/resources/views/admin/locations/index.blade.php
-index c54ffe0..e245a76 100644
---- a/resources/views/admin/locations/index.blade.php
-+++ b/resources/views/admin/locations/index.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Locations
-+    Standorte
- @endsection
- 
- @section('content-header')
--    <h1>Locations<small>All locations that nodes can be assigned to for easier categorization.</small></h1>
-+    <h1>Standorte<small>Alle Standorte, wo Rechenzentren für deine Server stehen.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Locations</li>
-+        <li class="active">Standorte</li>
-     </ol>
- @endsection
- 
-@@ -17,9 +17,9 @@
-     <div class="col-xs-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Location List</h3>
-+                <h3 class="box-title">Alle Standorte</h3>
-                 <div class="box-tools">
--                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newLocationModal">Create New</button>
-+                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newLocationModal">Hinzufügen</button>
-                 </div>
-             </div>
-             <div class="box-body table-responsive no-padding">
-@@ -27,10 +27,10 @@
-                     <tbody>
-                         <tr>
-                             <th>ID</th>
--                            <th>Short Code</th>
--                            <th>Description</th>
--                            <th class="text-center">Nodes</th>
--                            <th class="text-center">Servers</th>
-+                            <th>Kürzel</th>
-+                            <th>Beschreibung</th>
-+                            <th class="text-center">Laufende Nodes</th>
-+                            <th class="text-center">Aktive Server</th>
-                         </tr>
-                         @foreach ($locations as $location)
-                             <tr>
-@@ -52,27 +52,27 @@
-         <div class="modal-content">
-             <form action="{{ route('admin.locations') }}" method="POST">
-                 <div class="modal-header">
--                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
--                    <h4 class="modal-title">Create Location</h4>
-+                    <button type="button" class="close" data-dismiss="modal" aria-label="Schließen"><span aria-hidden="true">&times;</span></button>
-+                    <h4 class="modal-title">Standort hinzufügen</h4>
-                 </div>
-                 <div class="modal-body">
-                     <div class="row">
-                         <div class="col-md-12">
--                            <label for="pShortModal" class="form-label">Short Code</label>
-+                            <label for="pShortModal" class="form-label">Kürzel</label>
-                             <input type="text" name="short" id="pShortModal" class="form-control" />
--                            <p class="text-muted small">A short identifier used to distinguish this location from others. Must be between 1 and 60 characters, for example, <code>us.nyc.lvl3</code>.</p>
-+                            <p class="text-muted small">Ein Kürzel für den Standort des Rechenzentrums, du kannst deine eigene Struktur nutzen, zum Beispiel <code>rz-de-srv01</code></p>
-                         </div>
-                         <div class="col-md-12">
--                            <label for="pLongModal" class="form-label">Description</label>
-+                            <label for="pLongModal" class="form-label">Beschreibung</label>
-                             <textarea name="long" id="pLongModal" class="form-control" rows="4"></textarea>
--                            <p class="text-muted small">A longer description of this location. Must be less than 191 characters.</p>
-+                            <p class="text-muted small">Eine Beschreibung des Standortes, zum Beispiel die Anschrift oder Kontaktdaten.</p>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="modal-footer">
-                     {!! csrf_field() !!}
--                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
--                    <button type="submit" class="btn btn-success btn-sm">Create</button>
-+                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
-+                    <button type="submit" class="btn btn-success btn-sm">Erstellen</button>
-                 </div>
-             </form>
-         </div>
-diff --git a/resources/views/admin/locations/view.blade.php b/resources/views/admin/locations/view.blade.php
-index f34dcf3..b7d57f0 100644
---- a/resources/views/admin/locations/view.blade.php
-+++ b/resources/views/admin/locations/view.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Locations &rarr; View &rarr; {{ $location->short }}
-+    Standorte &rarr; Ansehen &rarr; {{ $location->short }}
- @endsection
- 
- @section('content-header')
-     <h1>{{ $location->short }}<small>{{ str_limit($location->long, 75) }}</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.locations') }}">Locations</a></li>
-+        <li><a href="{{ route('admin.locations') }}">Standorte</a></li>
-         <li class="active">{{ $location->short }}</li>
-     </ol>
- @endsection
-@@ -18,23 +18,23 @@
-     <div class="col-sm-6">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Location Details</h3>
-+                <h3 class="box-title">Details des Standortes</h3>
-             </div>
-             <form action="{{ route('admin.locations.view', $location->id) }}" method="POST">
-                 <div class="box-body">
-                     <div class="form-group">
--                        <label for="pShort" class="form-label">Short Code</label>
-+                        <label for="pShort" class="form-label">Kürzel</label>
-                         <input type="text" id="pShort" name="short" class="form-control" value="{{ $location->short }}" />
-                     </div>
-                     <div class="form-group">
--                        <label for="pLong" class="form-label">Description</label>
-+                        <label for="pLong" class="form-label">Beschreibung</label>
-                         <textarea id="pLong" name="long" class="form-control" rows="4">{{ $location->long }}</textarea>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
-                     {!! method_field('PATCH') !!}
--                    <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Save</button>
-+                    <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Speichern</button>
-                     <button name="action" value="delete" class="btn btn-sm btn-danger pull-left muted muted-hover"><i class="fa fa-trash-o"></i></button>
-                 </div>
-             </form>
-@@ -51,7 +51,7 @@
-                         <th>ID</th>
-                         <th>Name</th>
-                         <th>FQDN</th>
--                        <th>Servers</th>
-+                        <th>Alle Server</th>
-                     </tr>
-                     @foreach($location->nodes as $node)
-                         <tr>
-diff --git a/resources/views/admin/mounts/index.blade.php b/resources/views/admin/mounts/index.blade.php
-index a3b9892..491bc4c 100644
---- a/resources/views/admin/mounts/index.blade.php
-+++ b/resources/views/admin/mounts/index.blade.php
-@@ -2,14 +2,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Mounts
-+    Integrationen
- @endsection
- 
- @section('content-header')
--    <h1>Mounts<small>Configure and manage additional mount points for servers.</small></h1>
-+    <h1>Integrationen<small>Konfiguriere und verwalte die Integrationen.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Mounts</li>
-+        <li class="active">Integrationen</li>
-     </ol>
- @endsection
- 
-@@ -18,10 +18,10 @@
-         <div class="col-xs-12">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Mount List</h3>
-+                    <h3 class="box-title">Alle Integrationen</h3>
- 
-                     <div class="box-tools">
--                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newMountModal">Create New</button>
-+                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newMountModal">Erstellen</button>
-                     </div>
-                 </div>
- 
-@@ -31,11 +31,11 @@
-                             <tr>
-                                 <th>ID</th>
-                                 <th>Name</th>
--                                <th>Source</th>
--                                <th>Target</th>
-+                                <th>Quelle</th>
-+                                <th>Ziel</th>
-                                 <th class="text-center">Eggs</th>
-                                 <th class="text-center">Nodes</th>
--                                <th class="text-center">Servers</th>
-+                                <th class="text-center">Anzahl Server</th>
-                             </tr>
- 
-                             @foreach ($mounts as $mount)
-@@ -65,7 +65,7 @@
-                             <span aria-hidden="true" style="color: #FFFFFF">&times;</span>
-                         </button>
- 
--                        <h4 class="modal-title">Create Mount</h4>
-+                        <h4 class="modal-title">Integration erstellen</h4>
-                     </div>
- 
-                     <div class="modal-body">
-@@ -73,69 +73,69 @@
-                             <div class="col-md-12">
-                                 <label for="pName" class="form-label">Name</label>
-                                 <input type="text" id="pName" name="name" class="form-control" />
--                                <p class="text-muted small">Unique name used to separate this mount from another.</p>
-+                                <p class="text-muted small">Ein Name, womit du den mit anderen unterscheiden kannst.</p>
-                             </div>
- 
-                             <div class="col-md-12">
--                                <label for="pDescription" class="form-label">Description</label>
-+                                <label for="pDescription" class="form-label">Beschreibung</label>
-                                 <textarea id="pDescription" name="description" class="form-control" rows="4"></textarea>
--                                <p class="text-muted small">A longer description for this mount, must be less than 191 characters.</p>
-+                                <p class="text-muted small">Eine Beschreibung, wofür dies gedacht ist (max. 191 Zeichen)</p>
-                             </div>
- 
-                             <div class="col-md-6">
--                                <label for="pSource" class="form-label">Source</label>
-+                                <label for="pSource" class="form-label">Quelle</label>
-                                 <input type="text" id="pSource" name="source" class="form-control" />
--                                <p class="text-muted small">File path on the host system to mount to a container.</p>
-+                                <p class="text-muted small">Die Verzeichnisquelle im Host-System</p>
-                             </div>
- 
-                             <div class="col-md-6">
--                                <label for="pTarget" class="form-label">Target</label>
-+                                <label for="pTarget" class="form-label">Ziel</label>
-                                 <input type="text" id="pTarget" name="target" class="form-control" />
--                                <p class="text-muted small">Where the mount will be accessible inside a container.</p>
-+                                <p class="text-muted small">Wo dies in einem Container zugänglich sein wird.</p>
-                             </div>
- 
-                             <div class="col-md-6">
--                                <label class="form-label">Read Only</label>
-+                                <label class="form-label">Nur Lesezugriff</label>
- 
-                                 <div>
-                                     <div class="radio radio-success radio-inline">
-                                         <input type="radio" id="pReadOnlyFalse" name="read_only" value="0" checked>
--                                        <label for="pReadOnlyFalse">False</label>
-+                                        <label for="pReadOnlyFalse">Nein</label>
-                                     </div>
- 
-                                     <div class="radio radio-warning radio-inline">
-                                         <input type="radio" id="pReadOnly" name="read_only" value="1">
--                                        <label for="pReadOnly">True</label>
-+                                        <label for="pReadOnly">Ja</label>
-                                     </div>
-                                 </div>
- 
--                                <p class="text-muted small">Is the mount read only inside the container?</p>
-+                                <p class="text-muted small">Ist es im Container schreibgeschützt?</p>
-                             </div>
- 
-                             <div class="col-md-6">
--                                <label class="form-label">User Mountable</label>
-+                                <label class="form-label">An Benutzer verknüpfbar</label>
- 
-                                 <div>
-                                     <div class="radio radio-success radio-inline">
-                                         <input type="radio" id="pUserMountableFalse" name="user_mountable" value="0" checked>
--                                        <label for="pUserMountableFalse">False</label>
-+                                        <label for="pUserMountableFalse">Nein</label>
-                                     </div>
- 
-                                     <div class="radio radio-warning radio-inline">
-                                         <input type="radio" id="pUserMountable" name="user_mountable" value="1">
--                                        <label for="pUserMountable">True</label>
-+                                        <label for="pUserMountable">Ja</label>
-                                     </div>
-                                 </div>
- 
--                                <p class="text-muted small">Should users be able to mount this themselves?</p>
-+                                <p class="text-muted small">Sollten Benutzer in der Lage sein, dies selbst zu integrieren?</p>
-                             </div>
-                         </div>
-                     </div>
- 
-                     <div class="modal-footer">
-                         {!! csrf_field() !!}
--                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
--                        <button type="submit" class="btn btn-success btn-sm">Create</button>
-+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
-+                        <button type="submit" class="btn btn-success btn-sm">Erstellen</button>
-                     </div>
-                 </form>
-             </div>
-diff --git a/resources/views/admin/mounts/view.blade.php b/resources/views/admin/mounts/view.blade.php
-index 96a156d..409713b 100644
---- a/resources/views/admin/mounts/view.blade.php
-+++ b/resources/views/admin/mounts/view.blade.php
-@@ -2,7 +2,7 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Mounts &rarr; View &rarr; {{ $mount->id }}
-+    Mounts &rarr; Ansehen &rarr; {{ $mount->id }}
- @endsection
- 
- @section('content-header')
-@@ -19,13 +19,13 @@
-         <div class="col-sm-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Mount Details</h3>
-+                    <h3 class="box-title">Mount-Details</h3>
-                 </div>
- 
-                 <form action="{{ route('admin.mounts.view', $mount->id) }}" method="POST">
-                     <div class="box-body">
-                         <div class="form-group">
--                            <label for="PUniqueID" class="form-label">Unique ID</label>
-+                            <label for="PUniqueID" class="form-label">Eindeutige ID</label>
-                             <input type="text" id="PUniqueID" class="form-control" value="{{ $mount->uuid }}" disabled />
-                         </div>
- 
-@@ -35,51 +35,51 @@
-                         </div>
- 
-                         <div class="form-group">
--                            <label for="pDescription" class="form-label">Description</label>
-+                            <label for="pDescription" class="form-label">Beschreibung</label>
-                             <textarea id="pDescription" name="description" class="form-control" rows="4">{{ $mount->description }}</textarea>
-                         </div>
- 
-                         <div class="row">
-                             <div class="form-group col-md-6">
--                                <label for="pSource" class="form-label">Source</label>
-+                                <label for="pSource" class="form-label">Quelle</label>
-                                 <input type="text" id="pSource" name="source" class="form-control" value="{{ $mount->source }}" />
-                             </div>
- 
-                             <div class="form-group col-md-6">
--                                <label for="pTarget" class="form-label">Target</label>
-+                                <label for="pTarget" class="form-label">Ziel</label>
-                                 <input type="text" id="pTarget" name="target" class="form-control" value="{{ $mount->target }}" />
-                             </div>
-                         </div>
- 
-                         <div class="row">
-                             <div class="form-group col-md-6">
--                                <label class="form-label">Read Only</label>
-+                                <label class="form-label">Nur lesen</label>
- 
-                                 <div>
-                                     <div class="radio radio-success radio-inline">
-                                         <input type="radio" id="pReadOnlyFalse" name="read_only" value="0" @if(!$mount->read_only) checked @endif>
--                                        <label for="pReadOnlyFalse">False</label>
-+                                        <label for="pReadOnlyFalse">Nein</label>
-                                     </div>
- 
-                                     <div class="radio radio-warning radio-inline">
-                                         <input type="radio" id="pReadOnly" name="read_only" value="1" @if($mount->read_only) checked @endif>
--                                        <label for="pReadOnly">True</label>
-+                                        <label for="pReadOnly">Ja</label>
-                                     </div>
-                                 </div>
-                             </div>
- 
-                             <div class="form-group col-md-6">
--                                <label class="form-label">User Mountable</label>
-+                                <label class="form-label">Vom Benutzer montierbar</label>
- 
-                                 <div>
-                                     <div class="radio radio-success radio-inline">
-                                         <input type="radio" id="pUserMountableFalse" name="user_mountable" value="0" @if(!$mount->user_mountable) checked @endif>
--                                        <label for="pUserMountableFalse">False</label>
-+                                        <label for="pUserMountableFalse">Nein</label>
-                                     </div>
- 
-                                     <div class="radio radio-warning radio-inline">
-                                         <input type="radio" id="pUserMountable" name="user_mountable" value="1" @if($mount->user_mountable) checked @endif>
--                                        <label for="pUserMountable">True</label>
-+                                        <label for="pUserMountable">Ja</label>
-                                     </div>
-                                 </div>
-                             </div>
-@@ -90,7 +90,7 @@
-                         {!! csrf_field() !!}
-                         {!! method_field('PATCH') !!}
- 
--                        <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Save</button>
-+                        <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Speichern</button>
-                         <button name="action" value="delete" class="btn btn-sm btn-danger pull-left muted muted-hover"><i class="fa fa-trash-o"></i></button>
-                     </div>
-                 </form>
-@@ -103,7 +103,7 @@
-                     <h3 class="box-title">Eggs</h3>
- 
-                     <div class="box-tools">
--                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addEggsModal">Add Eggs</button>
-+                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addEggsModal">Eggs hinzufügen</button>
-                     </div>
-                 </div>
- 
-@@ -133,7 +133,7 @@
-                     <h3 class="box-title">Nodes</h3>
- 
-                     <div class="box-tools">
--                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addNodesModal">Add Nodes</button>
-+                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addNodesModal">Nodes hinzufügen</button>
-                     </div>
-                 </div>
- 
-@@ -171,7 +171,7 @@
-                             <span aria-hidden="true" style="color: #FFFFFF">&times;</span>
-                         </button>
- 
--                        <h4 class="modal-title">Add Eggs</h4>
-+                        <h4 class="modal-title">Eggs hinzufügen</h4>
-                     </div>
- 
-                     <div class="modal-body">
-@@ -198,8 +198,8 @@
-                     <div class="modal-footer">
-                         {!! csrf_field() !!}
- 
--                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
--                        <button type="submit" class="btn btn-primary btn-sm">Add</button>
-+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
-+                        <button type="submit" class="btn btn-primary btn-sm">Hinzufügen</button>
-                     </div>
-                 </form>
-             </div>
-@@ -215,7 +215,7 @@
-                             <span aria-hidden="true" style="color: #FFFFFF">&times;</span>
-                         </button>
- 
--                        <h4 class="modal-title">Add Nodes</h4>
-+                        <h4 class="modal-title">Nodes hinzufügen</h4>
-                     </div>
- 
-                     <div class="modal-body">
-@@ -242,8 +242,8 @@
-                     <div class="modal-footer">
-                         {!! csrf_field() !!}
- 
--                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
--                        <button type="submit" class="btn btn-primary btn-sm">Add</button>
-+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
-+                        <button type="submit" class="btn btn-primary btn-sm">Hinzufügen</button>
-                     </div>
-                 </form>
-             </div>
-@@ -257,11 +257,11 @@
-     <script>
-         $(document).ready(function() {
-             $('#pEggs').select2({
--                placeholder: 'Select eggs..',
-+                placeholder: 'Eggs auswählen..',
-             });
- 
-             $('#pNodes').select2({
--                placeholder: 'Select nodes..',
-+                placeholder: 'Nodes auswählen..',
-             });
- 
-             $('button[data-action="detach-egg"]').click(function (event) {
-@@ -276,11 +276,11 @@
-                     headers: { 'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content') },
-                 }).done(function () {
-                     element.parent().parent().addClass('warning').delay(100).fadeOut();
--                    swal({ type: 'success', title: 'Egg detached.' });
-+                    swal({ type: 'success', title: 'Egg getrennt.' });
-                 }).fail(function (jqXHR) {
-                     console.error(jqXHR);
-                     swal({
--                        title: 'Whoops!',
-+                        title: 'Hoppla!',
-                         text: jqXHR.responseJSON.error,
-                         type: 'error'
-                     });
-@@ -299,11 +299,11 @@
-                     headers: { 'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content') },
-                 }).done(function () {
-                     element.parent().parent().addClass('warning').delay(100).fadeOut();
--                    swal({ type: 'success', title: 'Node detached.' });
-+                    swal({ type: 'success', title: 'Node getrennt.' });
-                 }).fail(function (jqXHR) {
-                     console.error(jqXHR);
-                     swal({
--                        title: 'Whoops!',
-+                        title: 'Hoppla!',
-                         text: jqXHR.responseJSON.error,
-                         type: 'error'
-                     });
-diff --git a/resources/views/admin/nests/index.blade.php b/resources/views/admin/nests/index.blade.php
-index 4bb6ae7..406c7a0 100644
---- a/resources/views/admin/nests/index.blade.php
-+++ b/resources/views/admin/nests/index.blade.php
-@@ -5,7 +5,7 @@
- @endsection
- 
- @section('content-header')
--    <h1>Nests<small>All nests currently available on this system.</small></h1>
-+    <h1>Nests<small>Alle verfügbaren Nests im System</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li class="active">Nests</li>
-@@ -16,7 +16,7 @@
- <div class="row">
-     <div class="col-xs-12">
-         <div class="alert alert-danger">
--            Eggs are a powerful feature of Pterodactyl Panel that allow for extreme flexibility and configuration. Please note that while powerful, modifying an egg wrongly can very easily brick your servers and cause more problems. Please avoid editing our default eggs — those provided by <code>support@pterodactyl.io</code> — unless you are absolutely sure of what you are doing.
-+            Eggs sind ein leistungsstarkes Merkmal von Pterodactyl Panel, das extreme Flexibilität und Konfiguration ermöglicht. Bitte beachte, dass ein falsches Modifizieren eines Eggs zwar leistungsfähig ist, aber sehr leicht deine Server zerstören und mehr Probleme verursachen kann. Bitte vermeide es, unsere Standard-Eggs – die von <code>support@pterodactyl.io</code> zu bearbeiten, es sei denn, du bist absolut sicher, was du tust.
-         </div>
-     </div>
- </div>
-@@ -24,10 +24,10 @@
-     <div class="col-xs-12">
-         <div class="box">
-             <div class="box-header with-border">
--                <h3 class="box-title">Configured Nests</h3>
-+                <h3 class="box-title">Verfügbare Nests</h3>
-                 <div class="box-tools">
--                    <a href="#" class="btn btn-sm btn-success" data-toggle="modal" data-target="#importServiceOptionModal" role="button"><i class="fa fa-upload"></i> Import Egg</a>
--                    <a href="{{ route('admin.nests.new') }}" class="btn btn-primary btn-sm">Create New</a>
-+                    <a href="#" class="btn btn-sm btn-success" data-toggle="modal" data-target="#importServiceOptionModal" role="button"><i class="fa fa-upload"></i> Egg importieren</a>
-+                    <a href="{{ route('admin.nests.new') }}" class="btn btn-primary btn-sm">Hinzufügen</a>
-                 </div>
-             </div>
-             <div class="box-body table-responsive no-padding">
-@@ -35,9 +35,9 @@
-                     <tr>
-                         <th>ID</th>
-                         <th>Name</th>
--                        <th>Description</th>
-+                        <th>Beschreibung</th>
-                         <th class="text-center">Eggs</th>
--                        <th class="text-center">Servers</th>
-+                        <th class="text-center">Server-Anzahl</th>
-                     </tr>
-                     @foreach($nests as $nest)
-                         <tr>
-@@ -58,33 +58,33 @@
-         <div class="modal-content">
-             <div class="modal-header">
-                 <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
--                <h4 class="modal-title">Import an Egg</h4>
-+                <h4 class="modal-title">Egg importieren</h4>
-             </div>
-             <form action="{{ route('admin.nests.egg.import') }}" enctype="multipart/form-data" method="POST">
-                 <div class="modal-body">
-                     <div class="form-group">
--                        <label class="control-label" for="pImportFile">Egg File <span class="field-required"></span></label>
-+                        <label class="control-label" for="pImportFile">Egg-Datei<span class="field-required"></span></label>
-                         <div>
-                             <input id="pImportFile" type="file" name="import_file" class="form-control" accept="application/json" />
--                            <p class="small text-muted">Select the <code>.json</code> file for the new egg that you wish to import.</p>
-+                            <p class="small text-muted">Wähle die <code>.json</code> Datei, welche du als Egg importieren möchtest.</p>
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label" for="pImportToNest">Associated Nest <span class="field-required"></span></label>
-+                        <label class="control-label" for="pImportToNest">Passender Nest <span class="field-required"></span></label>
-                         <div>
-                             <select id="pImportToNest" name="import_to_nest">
-                                 @foreach($nests as $nest)
-                                    <option value="{{ $nest->id }}">{{ $nest->name }} &lt;{{ $nest->author }}&gt;</option>
-                                 @endforeach
-                             </select>
--                            <p class="small text-muted">Select the nest that this egg will be associated with from the dropdown. If you wish to associate it with a new nest you will need to create that nest before continuing.</p>
-+                            <p class="small text-muted">Wähle das Nest, dem dieses Egg zugeordnet werden soll, aus der Dropdown-Liste aus. Wenn du es mit einer neuen Verschachtelung verknüpfen möchtest, müssen Sie diese Verschachtelung erstellen, bevor du fortfährst.</p>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="modal-footer">
-                     {{ csrf_field() }}
--                    <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
--                    <button type="submit" class="btn btn-primary">Import</button>
-+                    <button type="button" class="btn btn-default" data-dismiss="modal">Abbrechen</button>
-+                    <button type="submit" class="btn btn-primary">Importieren</button>
-                 </div>
-             </form>
-         </div>
-diff --git a/resources/views/admin/nests/new.blade.php b/resources/views/admin/nests/new.blade.php
-index 9a1cb0e..3d502a4 100644
---- a/resources/views/admin/nests/new.blade.php
-+++ b/resources/views/admin/nests/new.blade.php
-@@ -1,15 +1,15 @@
- @extends('layouts.admin')
- 
- @section('title')
--    New Nest
-+    Neues Nest
- @endsection
- 
- @section('content-header')
--    <h1>New Nest<small>Configure a new nest to deploy to all nodes.</small></h1>
-+    <h1>Neues Nest<small>Richte ein Nest ein, womit du Nodes integrieren kannst.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
--        <li class="active">New</li>
-+        <li class="active">Erstellen</li>
-     </ol>
- @endsection
- 
-@@ -19,18 +19,18 @@
-         <div class="col-md-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">New Nest</h3>
-+                    <h3 class="box-title">Neues Nest</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
-                         <label class="control-label">Name</label>
-                         <div>
-                             <input type="text" name="name" class="form-control" value="{{ old('name') }}" />
--                            <p class="text-muted"><small>This should be a descriptive category name that encompasses all of the eggs within the nest.</small></p>
-+                            <p class="text-muted"><small>Dies sollte ein Name sein, der nicht leicht zu vertauschen ist.</small></p>
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Description</label>
-+                        <label class="control-label">Beschreibung</label>
-                         <div>
-                             <textarea name="description" class="form-control" rows="6">{{ old('description') }}</textarea>
-                         </div>
-@@ -38,7 +38,7 @@
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" class="btn btn-primary pull-right">Save</button>
-+                    <button type="submit" class="btn btn-primary pull-right">Speichern</button>
-                 </div>
-             </div>
-         </div>
-diff --git a/resources/views/admin/nests/view.blade.php b/resources/views/admin/nests/view.blade.php
-index 9a67307..46f1c88 100644
---- a/resources/views/admin/nests/view.blade.php
-+++ b/resources/views/admin/nests/view.blade.php
-@@ -23,11 +23,11 @@
-                         <label class="control-label">Name <span class="field-required"></span></label>
-                         <div>
-                             <input type="text" name="name" class="form-control" value="{{ $nest->name }}" />
--                            <p class="text-muted"><small>This should be a descriptive category name that encompasses all of the options within the service.</small></p>
-+                            <p class="text-muted"><small>Dies sollte ein beschreibender Kategoriename sein, der alle Optionen innerhalb des Dienstes umfasst.</small></p>
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Description</label>
-+                        <label class="control-label">Beschreibung</label>
-                         <div>
-                             <textarea name="description" class="form-control" rows="7">{{ $nest->description }}</textarea>
-                         </div>
-@@ -35,7 +35,7 @@
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Save</button>
-+                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Speichern</button>
-                     <button id="deleteButton" type="submit" name="_method" value="DELETE" class="btn btn-sm btn-danger muted muted-hover"><i class="fa fa-trash-o"></i></button>
-                 </div>
-             </div>
-@@ -45,24 +45,24 @@
-         <div class="box">
-             <div class="box-body">
-                 <div class="form-group">
--                    <label class="control-label">Nest ID</label>
-+                    <label class="control-label">Nest-ID</label>
-                     <div>
-                         <input type="text" readonly class="form-control" value="{{ $nest->id }}" />
--                        <p class="text-muted small">A unique ID used for identification of this nest internally and through the API.</p>
-+                        <p class="text-muted small">Eine eindeutige ID, die zur Identifizierung dieses Nests intern und über die API verwendet wird.</p>
-                     </div>
-                 </div>
-                 <div class="form-group">
--                    <label class="control-label">Author</label>
-+                    <label class="control-label">Autor</label>
-                     <div>
-                         <input type="text" readonly class="form-control" value="{{ $nest->author }}" />
--                        <p class="text-muted small">The author of this service option. Please direct questions and issues to them unless this is an official option authored by <code>support@pterodactyl.io</code>.</p>
-+                        <p class="text-muted small">Der Autor dieser Serviceoption. Bitte richten Sie Fragen und Probleme an sie, es sei denn, dies ist eine offizielle Option, die von <code>support@pterodactyl.io</code> verfasst wurde.</p>
-                     </div>
-                 </div>
-                 <div class="form-group">
-                     <label class="control-label">UUID</label>
-                     <div>
-                         <input type="text" readonly class="form-control" value="{{ $nest->uuid }}" />
--                        <p class="text-muted small">A UUID that all servers using this option are assigned for identification purposes.</p>
-+                        <p class="text-muted small">Eine UUID, die allen Servern, die diese Option verwenden, zu Identifikationszwecken zugewiesen wird.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -80,8 +80,8 @@
-                     <tr>
-                         <th>ID</th>
-                         <th>Name</th>
--                        <th>Description</th>
--                        <th class="text-center">Servers</th>
-+                        <th>Beschreibung</th>
-+                        <th class="text-center">Server</th>
-                         <th class="text-center"></th>
-                     </tr>
-                     @foreach($nest->eggs as $egg)
-@@ -98,7 +98,7 @@
-                 </table>
-             </div>
-             <div class="box-footer">
--                <a href="{{ route('admin.nests.egg.new') }}"><button class="btn btn-success btn-sm pull-right">New Egg</button></a>
-+                <a href="{{ route('admin.nests.egg.new') }}"><button class="btn btn-success btn-sm pull-right">Neues Egg</button></a>
-             </div>
-         </div>
-     </div>
-@@ -109,7 +109,7 @@
-     @parent
-     <script>
-         $('#deleteButton').on('mouseenter', function (event) {
--            $(this).find('i').html(' Delete Nest');
-+            $(this).find('i').html(' Nest löschen');
-         }).on('mouseleave', function (event) {
-             $(this).find('i').html('');
-         });
-diff --git a/resources/views/admin/nodes/index.blade.php b/resources/views/admin/nodes/index.blade.php
-index 3b3a3fa..5ee61bb 100644
---- a/resources/views/admin/nodes/index.blade.php
-+++ b/resources/views/admin/nodes/index.blade.php
-@@ -1,7 +1,7 @@
- @extends('layouts.admin')
- 
- @section('title')
--    List Nodes
-+    Alle Nodes
- @endsection
- 
- @section('scripts')
-@@ -10,7 +10,7 @@
- @endsection
- 
- @section('content-header')
--    <h1>Nodes<small>All nodes available on the system.</small></h1>
-+    <h1>Nodes<small>Alle Nodes in deinem System.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li class="active">Nodes</li>
-@@ -22,14 +22,14 @@
-     <div class="col-xs-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Node List</h3>
-+                <h3 class="box-title">Node-Übersicht</h3>
-                 <div class="box-tools search01">
-                     <form action="{{ route('admin.nodes') }}" method="GET">
-                         <div class="input-group input-group-sm">
--                            <input type="text" name="filter[name]" class="form-control pull-right" value="{{ request()->input('filter.name') }}" placeholder="Search Nodes">
-+                            <input type="text" name="filter[name]" class="form-control pull-right" value="{{ request()->input('filter.name') }}" placeholder="Node suchen...">
-                             <div class="input-group-btn">
-                                 <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
--                                <a href="{{ route('admin.nodes.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Create New</button></a>
-+                                <a href="{{ route('admin.nodes.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Hinzufügen</button></a>
-                             </div>
-                         </div>
-                     </form>
-@@ -41,12 +41,12 @@
-                         <tr>
-                             <th></th>
-                             <th>Name</th>
--                            <th>Location</th>
--                            <th>Memory</th>
--                            <th>Disk</th>
--                            <th class="text-center">Servers</th>
-+                            <th>Standort</th>
-+                            <th>Arbeitsspeicher</th>
-+                            <th>Speicherplatz</th>
-+                            <th class="text-center">Server</th>
-                             <th class="text-center">SSL</th>
--                            <th class="text-center">Public</th>
-+                            <th class="text-center">Öffentlich</th>
-                         </tr>
-                         @foreach ($nodes as $node)
-                             <tr>
-@@ -91,7 +91,7 @@
-                 });
-                 $(element).removeClass('text-muted').find('i').removeClass().addClass('fa fa-fw fa-heartbeat faa-pulse animated').css('color', '#50af51');
-             }).fail(function (error) {
--                var errorText = 'Error connecting to node! Check browser console for details.';
-+                var errorText = 'Verbindung zum Node nicht verfügbar. Prüfe die Konsole für Details..';
-                 try {
-                     errorText = error.responseJSON.errors[0].detail || errorText;
-                 } catch (ex) {}
-diff --git a/resources/views/admin/nodes/new.blade.php b/resources/views/admin/nodes/new.blade.php
-index b10d08a..e90a03c 100644
---- a/resources/views/admin/nodes/new.blade.php
-+++ b/resources/views/admin/nodes/new.blade.php
-@@ -1,15 +1,15 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Nodes &rarr; New
-+    Node anlegen
- @endsection
- 
- @section('content-header')
--    <h1>New Node<small>Create a new local or remote node for servers to be installed to.</small></h1>
-+    <h1>Neuer Node<small>Erstelle einen lokalen externen Node-Server</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
--        <li class="active">New</li>
-+        <li class="active">Node erstellen</li>
-     </ol>
- @endsection
- 
-@@ -19,20 +19,20 @@
-         <div class="col-sm-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Basic Details</h3>
-+                    <h3 class="box-title">Infos zur Node</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
-                         <label for="pName" class="form-label">Name</label>
-                         <input type="text" name="name" id="pName" class="form-control" value="{{ old('name') }}"/>
--                        <p class="text-muted small">Character limits: <code>a-zA-Z0-9_.-</code> and <code>[Space]</code> (min 1, max 100 characters).</p>
-+                        <p class="text-muted small">Erlaubte Zeichen: <code>a-zA-Z0-9_.-</code> und <code>[Leerzeichen]</code> (mind. 1, max. 100).</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="pDescription" class="form-label">Description</label>
-+                        <label for="pDescription" class="form-label">Beschreibung</label>
-                         <textarea name="description" id="pDescription" rows="4" class="form-control">{{ old('description') }}</textarea>
-                     </div>
-                     <div class="form-group">
--                        <label for="pLocationId" class="form-label">Location</label>
-+                        <label for="pLocationId" class="form-label">Standort</label>
-                         <select name="location_id" id="pLocationId">
-                             @foreach($locations as $location)
-                                 <option value="{{ $location->id }}" {{ $location->id != old('location_id') ?: 'selected' }}>{{ $location->short }}</option>
-@@ -40,56 +40,56 @@
-                         </select>
-                     </div>
-                     <div class="form-group">
--                        <label class="form-label">Node Visibility</label>
-+                        <label class="form-label">Sichtbarkeit</label>
-                         <div>
-                             <div class="radio radio-success radio-inline">
- 
-                                 <input type="radio" id="pPublicTrue" value="1" name="public" checked>
--                                <label for="pPublicTrue"> Public </label>
-+                                <label for="pPublicTrue"> Öffentlich </label>
-                             </div>
-                             <div class="radio radio-danger radio-inline">
-                                 <input type="radio" id="pPublicFalse" value="0" name="public">
--                                <label for="pPublicFalse"> Private </label>
-+                                <label for="pPublicFalse"> Privat </label>
-                             </div>
-                         </div>
--                        <p class="text-muted small">By setting a node to <code>private</code> you will be denying the ability to auto-deploy to this node.
-+                        <p class="text-muted small">Wenn du dein Node auf <code>Privat</code> einstellst, blockst du die Fähigkeit zur automatischen Bereitstellung auf diesem Node.
-                     </div>
-                     <div class="form-group">
-                         <label for="pFQDN" class="form-label">FQDN</label>
-                         <input type="text" name="fqdn" id="pFQDN" class="form-control" value="{{ old('fqdn') }}"/>
--                        <p class="text-muted small">Please enter domain name (e.g <code>node.example.com</code>) to be used for connecting to the daemon. An IP address may be used <em>only</em> if you are not using SSL for this node.</p>
-+                        <p class="text-muted small">Gib eine Domain für die Node ein, (Bsp.: <code>node.example.com</code>) um sich mit dem Daemon verbinden zu können. Eine IP-Adresse kann <em>nur genutzt werden</em>, wenn du kein SSL verwendest.</p>
-                     </div>
-                     <div class="form-group">
--                        <label class="form-label">Communicate Over SSL</label>
-+                        <label class="form-label">Über SSL kommunizieren</label>
-                         <div>
-                             <div class="radio radio-success radio-inline">
-                                 <input type="radio" id="pSSLTrue" value="https" name="scheme" checked>
--                                <label for="pSSLTrue"> Use SSL Connection</label>
-+                                <label for="pSSLTrue"> SSL verwenden</label>
-                             </div>
-                             <div class="radio radio-danger radio-inline">
-                                 <input type="radio" id="pSSLFalse" value="http" name="scheme" @if(request()->isSecure()) disabled @endif>
--                                <label for="pSSLFalse"> Use HTTP Connection</label>
-+                                <label for="pSSLFalse"> HTTP verwenden</label>
-                             </div>
-                         </div>
-                         @if(request()->isSecure())
--                            <p class="text-danger small">Your Panel is currently configured to use a secure connection. In order for browsers to connect to your node it <strong>must</strong> use a SSL connection.</p>
-+                            <p class="text-danger small">Dein Panel ist derzeit für die Verwendung einer sicheren Verbindung konfiguriert. Damit sich Browser mit Ihrem Node verbinden können, <strong>muss</strong> eine SSL-Verbindung verwendet werden.</p>
-                         @else
--                            <p class="text-muted small">In most cases you should select to use a SSL connection. If using an IP Address or you do not wish to use SSL at all, select a HTTP connection.</p>
-+                            <p class="text-muted small">In den meisten Fällen ist eine SSL-Verbindung zu empfehlen. Wenn du eine IP-Adresse verwendest oder überhaupt kein SSL verwenden möchtest, wähle eine HTTP-Verbindung.</p>
-                         @endif
-                     </div>
-                     <div class="form-group">
--                        <label class="form-label">Behind Proxy</label>
-+                        <label class="form-label">Proxy nutzen</label>
-                         <div>
-                             <div class="radio radio-success radio-inline">
-                                 <input type="radio" id="pProxyFalse" value="0" name="behind_proxy" checked>
--                                <label for="pProxyFalse"> Not Behind Proxy </label>
-+                                <label for="pProxyFalse"> Nein </label>
-                             </div>
-                             <div class="radio radio-info radio-inline">
-                                 <input type="radio" id="pProxyTrue" value="1" name="behind_proxy">
--                                <label for="pProxyTrue"> Behind Proxy </label>
-+                                <label for="pProxyTrue"> Ja </label>
-                             </div>
-                         </div>
--                        <p class="text-muted small">If you are running the daemon behind a proxy such as Cloudflare, select this to have the daemon skip looking for certificates on boot.</p>
-+                        <p class="text-muted small">Wenn du den Daemon hinter einem Proxy wie Cloudflare ausführst, wähle diese Option aus, damit der Daemon die Suche nach Zertifikaten beim Booten überspringt.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -97,50 +97,50 @@
-         <div class="col-sm-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Configuration</h3>
-+                    <h3 class="box-title">System-Bereitstellung</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="row">
-                         <div class="form-group col-md-6">
--                            <label for="pDaemonBase" class="form-label">Daemon Server File Directory</label>
-+                            <label for="pDaemonBase" class="form-label">Daemon Server - Verzeichnis</label>
-                             <input type="text" name="daemonBase" id="pDaemonBase" class="form-control" value="/var/lib/pterodactyl/volumes" />
--                            <p class="text-muted small">Enter the directory where server files should be stored. <strong>If you use OVH you should check your partition scheme. You may need to use <code>/home/daemon-data</code> to have enough space.</strong></p>
-+                            <p class="text-muted small">Gebe das Verzeichnis ein, in dem Serverdateien gespeichert werden sollen. <strong>Wenn du OVH verwendest, solltest du das Partitionsschema überprüfen. Möglicherweise musst du <code>/home/daemon-data</code> verwenden, um genügend Speicherplatz zu haben.</strong></p>
-                         </div>
-                         <div class="form-group col-md-6">
--                            <label for="pMemory" class="form-label">Total Memory</label>
-+                            <label for="pMemory" class="form-label">Verfügbarer RAM</label>
-                             <div class="input-group">
-                                 <input type="text" name="memory" data-multiplicator="true" class="form-control" id="pMemory" value="{{ old('memory') }}"/>
--                                <span class="input-group-addon">MiB</span>
-+                                <span class="input-group-addon">MB</span>
-                             </div>
-                         </div>
-                         <div class="form-group col-md-6">
--                            <label for="pMemoryOverallocate" class="form-label">Memory Over-Allocation</label>
-+                            <label for="pMemoryOverallocate" class="form-label">RAM-Toleranz über dem Limit</label>
-                             <div class="input-group">
-                                 <input type="text" name="memory_overallocate" class="form-control" id="pMemoryOverallocate" value="{{ old('memory_overallocate') }}"/>
-                                 <span class="input-group-addon">%</span>
-                             </div>
-                         </div>
-                         <div class="col-md-12">
--                            <p class="text-muted small">Enter the total amount of memory available for new servers. If you would like to allow overallocation of memory enter the percentage that you want to allow. To disable checking for overallocation enter <code>-1</code> into the field. Entering <code>0</code> will prevent creating new servers if it would put the node over the limit.</p>
-+                            <p class="text-muted small">Gebe die Gesamtmenge an verfügbarem Arbeitsspeicher für neue Server ein. Wenn du eine Überbelegung des Arbeitsspeichers zulassen möchtest, gebe den Prozentsatz ein, den du zulassen möchtest. Um die Überprüfung auf Überbelegung zu deaktivieren, gebe <code>-1</code> in das Feld ein. Die Eingabe von <code>0</code> verhindert das Erstellen neuer Server, wenn der Knoten dadurch das Limit überschreiten würde.</p>
-                         </div>
-                     </div>
-                     <div class="row">
-                         <div class="form-group col-md-6">
--                            <label for="pDisk" class="form-label">Total Disk Space</label>
-+                            <label for="pDisk" class="form-label">Verfügbarer Speicher</label>
-                             <div class="input-group">
-                                 <input type="text" name="disk" data-multiplicator="true" class="form-control" id="pDisk" value="{{ old('disk') }}"/>
--                                <span class="input-group-addon">MiB</span>
-+                                <span class="input-group-addon">MB</span>
-                             </div>
-                         </div>
-                         <div class="form-group col-md-6">
--                            <label for="pDiskOverallocate" class="form-label">Disk Over-Allocation</label>
-+                            <label for="pDiskOverallocate" class="form-label">Speicher-Toleranz über den Limit</label>
-                             <div class="input-group">
-                                 <input type="text" name="disk_overallocate" class="form-control" id="pDiskOverallocate" value="{{ old('disk_overallocate') }}"/>
-                                 <span class="input-group-addon">%</span>
-                             </div>
-                         </div>
-                         <div class="col-md-12">
--                            <p class="text-muted small">Enter the total amount of disk space available for new servers. If you would like to allow overallocation of disk space enter the percentage that you want to allow. To disable checking for overallocation enter <code>-1</code> into the field. Entering <code>0</code> will prevent creating new servers if it would put the node over the limit.</p>
-+                            <p class="text-muted small">Gebe die Gesamtmenge an Speicherplatz ein, die für neue Server verfügbar ist. Wenn du eine Überbelegung des Speicherplatzes zulassen möchtest, gebe den Prozentsatz ein, den Sie zulassen möchtest. Um die Überprüfung auf Überbelegung zu deaktivieren, gebe <code>-1</code> in das Feld ein. Die Eingabe von <code>0</code> verhindert das Erstellen neuer Server, wenn der Knoten dadurch das Limit überschreiten würde.</p>
-                         </div>
-                     </div>
-                     <div class="row">
-@@ -153,13 +153,13 @@
-                             <input type="text" name="daemonSFTP" class="form-control" id="pDaemonSFTP" value="2022" />
-                         </div>
-                         <div class="col-md-12">
--                            <p class="text-muted small">The daemon runs its own SFTP management container and does not use the SSHd process on the main physical server. <Strong>Do not use the same port that you have assigned for your physical server's SSH process.</strong> If you will be running the daemon behind CloudFlare&reg; you should set the daemon port to <code>8443</code> to allow websocket proxying over SSL.</p>
-+                            <p class="text-muted small">Der Daemon führt seinen eigenen SFTP-Verwaltungscontainer aus und verwendet nicht den SSHd-Prozess auf dem physischen Hauptserver. <Strong>Verwende nicht denselben Port, den du für den SSH-Prozess Ihres physischen Servers zugewiesen hast.</strong> Wenn du den Daemon hinter CloudFlare&reg; nutzt, solltest du den Daemon-Port auf <code>8443</code> setzen, um Websocket-Proxy über SSL zuzulassen.</p>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" class="btn btn-success pull-right">Create Node</button>
-+                    <button type="submit" class="btn btn-success pull-right">Node erstellen</button>
-                 </div>
-             </div>
-         </div>
-diff --git a/resources/views/admin/nodes/view/allocation.blade.php b/resources/views/admin/nodes/view/allocation.blade.php
-index 396428b..462e76a 100644
---- a/resources/views/admin/nodes/view/allocation.blade.php
-+++ b/resources/views/admin/nodes/view/allocation.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    {{ $node->name }}: Allocations
-+    {{ $node->name }}: Freigegebene Ports
- @endsection
- 
- @section('content-header')
--    <h1>{{ $node->name }}<small>Control allocations available for servers on this node.</small></h1>
-+    <h1>{{ $node->name }}<small>Verwalte die zugewiesenen Ports</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
-         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
--        <li class="active">Allocations</li>
-+        <li class="active">Ports</li>
-     </ol>
- @endsection
- 
-@@ -19,11 +19,11 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
--                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
--                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
--                <li class="active"><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
--                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
-+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Info</a></li>
-+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
-+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
-+                <li class="active"><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
-+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -32,7 +32,7 @@
-     <div class="col-sm-8">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Existing Allocations</h3>
-+                <h3 class="box-title">Zugeteilte Ports</h3>
-             </div>
-             <div class="box-body table-responsive no-padding" style="overflow-x: visible">
-                 <table class="table table-hover" style="margin-bottom:0;">
-@@ -40,17 +40,17 @@
-                         <th>
-                             <input type="checkbox" class="select-all-files hidden-xs" data-action="selectAll">
-                         </th>
--                        <th>IP Address <i class="fa fa-fw fa-minus-square" style="font-weight:normal;color:#d9534f;cursor:pointer;" data-toggle="modal" data-target="#allocationModal"></i></th>
--                        <th>IP Alias</th>
-+                        <th>IP Addresse<i class="fa fa-fw fa-minus-square" style="font-weight:normal;color:#d9534f;cursor:pointer;" data-toggle="modal" data-target="#allocationModal"></i></th>
-+                        <th>Vermerk zur IP</th>
-                         <th>Port</th>
--                        <th>Assigned To</th>
-+                        <th>Zugeteilt an</th>
-                         <th>
-                             <div class="btn-group hidden-xs">
-                                 <button type="button" id="mass_actions" class="btn btn-sm btn-default dropdown-toggle disabled"
--                                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mass Actions <span class="caret"></span>
-+                                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mehrfachauswahl <span class="caret"></span>
-                                 </button>
-                                 <ul class="dropdown-menu dropdown-massactions">
--                                    <li><a href="#" id="selective-deletion" data-action="selective-deletion">Delete <i class="fa fa-fw fa-trash-o"></i></a></li>
-+                                    <li><a href="#" id="selective-deletion" data-action="selective-deletion">Löschen <i class="fa fa-fw fa-trash-o"></i></a></li>
-                                 </ul>
-                             </div>
-                         </th>
-@@ -95,38 +95,38 @@
-         <form action="{{ route('admin.nodes.view.allocation', $node->id) }}" method="POST">
-             <div class="box box-success">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Assign New Allocations</h3>
-+                    <h3 class="box-title">Manuelle Portfreigabe</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
--                        <label for="pAllocationIP" class="control-label">IP Address</label>
-+                        <label for="pAllocationIP" class="control-label">IP Addresse</label>
-                         <div>
-                             <select class="form-control" name="allocation_ip" id="pAllocationIP" multiple>
-                                 @foreach($allocations as $allocation)
-                                     <option value="{{ $allocation->ip }}">{{ $allocation->ip }}</option>
-                                 @endforeach
-                             </select>
--                            <p class="text-muted small">Enter an IP address to assign ports to here.</p>
-+                            <p class="text-muted small">Gib die IP-Adresse ein, die verwendet werden soll.</p>
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="pAllocationIP" class="control-label">IP Alias</label>
-+                        <label for="pAllocationIP" class="control-label">Vermerk zur IP</label>
-                         <div>
-                             <input type="text" id="pAllocationAlias" class="form-control" name="allocation_alias" placeholder="alias" />
--                            <p class="text-muted small">If you would like to assign a default alias to these allocations enter it here.</p>
-+                            <p class="text-muted small">Wenn diese IP mit entsprechenden Ports mit einer Info vermerrkt werden soll, kannst du das hier tun. Bsp: Minecraft Ports.</p>
-                         </div>
-                     </div>
-                     <div class="form-group">
-                         <label for="pAllocationPorts" class="control-label">Ports</label>
-                         <div>
-                             <select class="form-control" name="allocation_ports[]" id="pAllocationPorts" multiple></select>
--                            <p class="text-muted small">Enter individual ports or port ranges here separated by commas or spaces.</p>
-+                            <p class="text-muted small">Gib alle Ports ein, die freigegeben werden sollen. Du kannst mit Kommas oder Bindestrichen mehrere Ports auf einmal freigeben.</p>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" class="btn btn-success btn-sm pull-right">Submit</button>
-+                    <button type="submit" class="btn btn-success btn-sm pull-right">Übernehmen</button>
-                 </div>
-             </div>
-         </form>
-@@ -137,7 +137,7 @@
-         <div class="modal-content">
-             <div class="modal-header">
-                 <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
--                <h4 class="modal-title">Delete Allocations for IP Block</h4>
-+                <h4 class="modal-title">Ports für den IP-Block entfernen</h4>
-             </div>
-             <form action="{{ route('admin.nodes.view.allocation.removeBlock', $node->id) }}" method="POST">
-                 <div class="modal-body">
-@@ -153,8 +153,8 @@
-                 </div>
-                 <div class="modal-footer">
-                     {{{ csrf_field() }}}
--                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
--                    <button type="submit" class="btn btn-danger">Delete Allocations</button>
-+                    <button type="button" class="btn btn-default" data-dismiss="modal">Schliessen</button>
-+                    <button type="submit" class="btn btn-danger">Ports entfernen</button>
-                 </div>
-             </form>
-         </div>
-@@ -200,12 +200,13 @@
-         var allocation = $(this).data('id');
-         swal({
-             title: '',
--            text: 'Are you sure you want to delete this allocation?',
-+            text: 'Bist du sicher, das du diesen Port entfernen möchtest?',
-             type: 'warning',
-             showCancelButton: true,
-+            cancelButtonText: 'Abbrechen',
-             allowOutsideClick: true,
-             closeOnConfirm: false,
--            confirmButtonText: 'Delete',
-+            confirmButtonText: 'Ja, entfernen',
-             confirmButtonColor: '#d9534f',
-             showLoaderOnConfirm: true
-         }, function () {
-@@ -215,11 +216,11 @@
-                 headers: { 'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content') },
-             }).done(function (data) {
-                 element.parent().parent().addClass('warning').delay(100).fadeOut();
--                swal({ type: 'success', title: 'Port Deleted!' });
-+                swal({ type: 'success', title: 'Port entfernt' });
-             }).fail(function (jqXHR) {
-                 console.error(jqXHR);
-                 swal({
--                    title: 'Whoops!',
-+                    title: 'Hups!',
-                     text: jqXHR.responseJSON.error,
-                     type: 'error'
-                 });
-@@ -299,15 +300,16 @@
- 
-             formattedItems = formattedItems.slice(0, -2);
-             if (selectedItems.length > 5) {
--                formattedItems += ', and ' + (selectedItems.length - 5) + ' other(s)';
-+                formattedItems += ', und ' + (selectedItems.length - 5) + ' weitere';
-             }
- 
-             swal({
-                 type: 'warning',
-                 title: '',
--                text: 'Are you sure you want to delete the following allocations: ' + formattedItems + '?',
-+                text: 'Möchtest du wirklich folgende Ports entfernen: ' + formattedItems + '?',
-                 html: true,
-                 showCancelButton: true,
-+                cancelButtonText: 'Abbrechen',
-                 showConfirmButton: true,
-                 closeOnConfirm: false,
-                 showLoaderOnConfirm: true
-@@ -332,15 +334,15 @@
- 
-                     swal({
-                         type: 'success',
--                        title: 'Allocations Deleted'
-+                        title: 'Ports entfernt'
-                     });
-                 }).fail(function (jqXHR) {
-                     console.error(jqXHR);
-                     swal({
-                         type: 'error',
--                        title: 'Whoops!',
-+                        title: 'Hups!',
-                         html: true,
--                        text: 'An error occurred while attempting to delete these allocations. Please try again.',
-+                        text: 'Es ist ein Fehler beim entfernen der Ports aufgetreten. Versuch es später nochmal.',
-                     });
-                 });
-             });
-@@ -348,7 +350,7 @@
-             swal({
-                 type: 'warning',
-                 title: '',
--                text: 'Please select allocation(s) to delete.',
-+                text: 'Du hast keine Ports ausgewählt, die entfernt werden sollen.',
-             });
-         }
-     }
-diff --git a/resources/views/admin/nodes/view/configuration.blade.php b/resources/views/admin/nodes/view/configuration.blade.php
-index 1bbf15f..218731c 100644
---- a/resources/views/admin/nodes/view/configuration.blade.php
-+++ b/resources/views/admin/nodes/view/configuration.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    {{ $node->name }}: Configuration
-+    {{ $node->name }}: Wings-Integration
- @endsection
- 
- @section('content-header')
--    <h1>{{ $node->name }}<small>Your daemon configuration file.</small></h1>
-+    <h1>{{ $node->name }}<small>Die Konfguationsdatei.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
-         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
--        <li class="active">Configuration</li>
-+        <li class="active">Wings-Integration</li>
-     </ol>
- @endsection
- 
-@@ -19,11 +19,11 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
--                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
--                <li class="active"><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
--                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
--                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
-+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Info</a></li>
-+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
-+                <li class="active"><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
-+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
-+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -32,29 +32,28 @@
-     <div class="col-sm-8">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Configuration File</h3>
-+                <h3 class="box-title">Generierte Config.yml</h3>
-             </div>
-             <div class="box-body">
-                 <pre class="no-margin">{{ $node->getYamlConfiguration() }}</pre>
-             </div>
-             <div class="box-footer">
--                <p class="no-margin">This file should be placed in your daemon's root directory (usually <code>/etc/pterodactyl</code>) in a file called <code>config.yml</code>.</p>
-+                <p class="no-margin">Diese Textzeilen müssen in der <code>config.yml</code> eingesetzt werden. Du findest die Datei im Pfad <code>/etc/pterodactyl</code>.</p>
-             </div>
-         </div>
-     </div>
-     <div class="col-sm-4">
-         <div class="box box-success">
-             <div class="box-header with-border">
--                <h3 class="box-title">Auto-Deploy</h3>
-+                <h3 class="box-title">Automatisch Einrichten</h3>
-             </div>
-             <div class="box-body">
-                 <p class="text-muted small">
--                    Use the button below to generate a custom deployment command that can be used to configure
--                    wings on the target server with a single command.
-+                    Du kannst auch die einfache Variante nutzen, indem du einen Befehl in deinen Linux Server sendest, der generiert wird.
-                 </p>
-             </div>
-             <div class="box-footer">
--                <button type="button" id="configTokenBtn" class="btn btn-sm btn-default" style="width:100%;">Generate Token</button>
-+                <button type="button" id="configTokenBtn" class="btn btn-sm btn-default" style="width:100%;">Befehl generieren</button>
-             </div>
-         </div>
-     </div>
-@@ -72,14 +71,14 @@
-         }).done(function (data) {
-             swal({
-                 type: 'success',
--                title: 'Token created.',
--                text: '<p>To auto-configure your node run the following command:<br /><small><pre>cd /etc/pterodactyl && sudo wings configure --panel-url {{ config('app.url') }} --token ' + data.token + ' --node ' + data.node + '{{ config('app.debug') ? ' --allow-insecure' : '' }}</pre></small></p>',
-+                title: 'Befehl erstellt.',
-+                text: '<p>Führe folgenden Befehl aus, um dies automatisch zu übernehmen:<br /><small><pre>cd /etc/pterodactyl && sudo wings configure --panel-url {{ config('app.url') }} --token ' + data.token + ' --node ' + data.node + '{{ config('app.debug') ? ' --allow-insecure' : '' }}</pre></small></p>',
-                 html: true
-             })
-         }).fail(function () {
-             swal({
--                title: 'Error',
--                text: 'Something went wrong creating your token.',
-+                title: 'Fehler bei der Erstellung',
-+                text: 'Irgendwas hat nicht geklappt, versuch es gleich nochmal.',
-                 type: 'error'
-             });
-         });
-diff --git a/resources/views/admin/nodes/view/index.blade.php b/resources/views/admin/nodes/view/index.blade.php
-index 2d0bb32..c24992f 100644
---- a/resources/views/admin/nodes/view/index.blade.php
-+++ b/resources/views/admin/nodes/view/index.blade.php
-@@ -5,7 +5,7 @@
- @endsection
- 
- @section('content-header')
--    <h1>{{ $node->name }}<small>A quick overview of your node.</small></h1>
-+    <h1>{{ $node->name }}<small>Eine kleine Übersicht zum Node</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
-@@ -18,11 +18,11 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li class="active"><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
--                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
--                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
--                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
--                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
-+                <li class="active"><a href="{{ route('admin.nodes.view', $node->id) }}">Infos</a></li>
-+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
-+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
-+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
-+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Verbundene Server</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -39,14 +39,14 @@
-                         <table class="table table-hover">
-                             <tr>
-                                 <td>Daemon Version</td>
--                                <td><code data-attr="info-version"><i class="fa fa-refresh fa-fw fa-spin"></i></code> (Latest: <code>{{ $version->getDaemon() }}</code>)</td>
-+                                <td><code data-attr="info-version"><i class="fa fa-refresh fa-fw fa-spin"></i></code> (Neuste: <code>{{ $version->getDaemon() }}</code>)</td>
-                             </tr>
-                             <tr>
--                                <td>System Information</td>
-+                                <td>System-Information</td>
-                                 <td data-attr="info-system"><i class="fa fa-refresh fa-fw fa-spin"></i></td>
-                             </tr>
-                             <tr>
--                                <td>Total CPU Threads</td>
-+                                <td>Anzahl an CPU-Kerne</td>
-                                 <td data-attr="info-cpus"><i class="fa fa-refresh fa-fw fa-spin"></i></td>
-                             </tr>
-                         </table>
-@@ -57,7 +57,7 @@
-                 <div class="col-xs-12">
-                     <div class="box box-default">
-                         <div class="box-header with-border">
--                            Description
-+                            Beschreibung
-                         </div>
-                         <div class="box-body table-responsive">
-                             <pre>{{ $node->description }}</pre>
-@@ -68,16 +68,16 @@
-             <div class="col-xs-12">
-                 <div class="box box-danger">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Delete Node</h3>
-+                        <h3 class="box-title">Node löschen</h3>
-                     </div>
-                     <div class="box-body">
--                        <p class="no-margin">Deleting a node is a irreversible action and will immediately remove this node from the panel. There must be no servers associated with this node in order to continue.</p>
-+                        <p class="no-margin">Dies kann nicht rückgängig gemacht, aber neu hinzugefügt und eingerichtet werden. Diesem Node dürfen keine Server zugeordnet sein, um fortzufahren.</p>
-                     </div>
-                     <div class="box-footer">
-                         <form action="{{ route('admin.nodes.view.delete', $node->id) }}" method="POST">
-                             {!! csrf_field() !!}
-                             {!! method_field('DELETE') !!}
--                            <button type="submit" class="btn btn-danger btn-sm pull-right" {{ ($node->servers_count < 1) ?: 'disabled' }}>Yes, Delete This Node</button>
-+                            <button type="submit" class="btn btn-danger btn-sm pull-right" {{ ($node->servers_count < 1) ?: 'disabled' }}>Ja, diese Node löschen</button>
-                         </form>
-                     </div>
-                 </div>
-@@ -87,7 +87,7 @@
-     <div class="col-sm-4">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">At-a-Glance</h3>
-+                <h3 class="box-title">Verfügbare Ressourcen</h3>
-             </div>
-             <div class="box-body">
-                 <div class="row">
-@@ -96,8 +96,8 @@
-                         <div class="info-box bg-orange">
-                             <span class="info-box-icon"><i class="ion ion-wrench"></i></span>
-                             <div class="info-box-content" style="padding: 23px 10px 0;">
--                                <span class="info-box-text">This node is under</span>
--                                <span class="info-box-number">Maintenance</span>
-+                                <span class="info-box-text">Diese Node ist in</span>
-+                                <span class="info-box-number">Wartung</span>
-                             </div>
-                         </div>
-                     </div>
-@@ -106,8 +106,8 @@
-                         <div class="info-box bg-{{ $stats['disk']['css'] }}">
-                             <span class="info-box-icon"><i class="ion ion-ios-folder-outline"></i></span>
-                             <div class="info-box-content" style="padding: 15px 10px 0;">
--                                <span class="info-box-text">Disk Space Allocated</span>
--                                <span class="info-box-number">{{ $stats['disk']['value'] }} / {{ $stats['disk']['max'] }} MiB</span>
-+                                <span class="info-box-text">Speicherplatz</span>
-+                                <span class="info-box-number">{{ $stats['disk']['value'] }} / {{ $stats['disk']['max'] }} MB</span>
-                                 <div class="progress">
-                                     <div class="progress-bar" style="width: {{ $stats['disk']['percent'] }}%"></div>
-                                 </div>
-@@ -118,7 +118,7 @@
-                         <div class="info-box bg-{{ $stats['memory']['css'] }}">
-                             <span class="info-box-icon"><i class="ion ion-ios-barcode-outline"></i></span>
-                             <div class="info-box-content" style="padding: 15px 10px 0;">
--                                <span class="info-box-text">Memory Allocated</span>
-+                                <span class="info-box-text">Arbeitsspeicher</span>
-                                 <span class="info-box-number">{{ $stats['memory']['value'] }} / {{ $stats['memory']['max'] }} MiB</span>
-                                 <div class="progress">
-                                     <div class="progress-bar" style="width: {{ $stats['memory']['percent'] }}%"></div>
-@@ -130,7 +130,7 @@
-                         <div class="info-box bg-blue">
-                             <span class="info-box-icon"><i class="ion ion-social-buffer-outline"></i></span>
-                             <div class="info-box-content" style="padding: 23px 10px 0;">
--                                <span class="info-box-text">Total Servers</span>
-+                                <span class="info-box-text">Server verbunden</span>
-                                 <span class="info-box-number">{{ $node->servers_count }}</span>
-                             </div>
-                         </div>
-diff --git a/resources/views/admin/nodes/view/servers.blade.php b/resources/views/admin/nodes/view/servers.blade.php
-index b8b9ee5..5913b2f 100644
---- a/resources/views/admin/nodes/view/servers.blade.php
-+++ b/resources/views/admin/nodes/view/servers.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    {{ $node->name }}: Servers
-+    {{ $node->name }}: Alle Server
- @endsection
- 
- @section('content-header')
--    <h1>{{ $node->name }}<small>All servers currently assigned to this node.</small></h1>
-+    <h1>{{ $node->name }}<small>Alle Server, die mit diesem Node verbunden sind.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
-         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
--        <li class="active">Servers</li>
-+        <li class="active">Alle Server</li>
-     </ol>
- @endsection
- 
-@@ -19,11 +19,11 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
--                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
--                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
--                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
--                <li class="active"><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
-+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Infos</a></li>
-+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
-+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
-+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
-+                <li class="active"><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -32,14 +32,14 @@
-     <div class="col-sm-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Process Manager</h3>
-+                <h3 class="box-title">Laufende Server</h3>
-             </div>
-             <div class="box-body table-responsive no-padding">
-                 <table class="table table-hover">
-                     <tr>
-                         <th>ID</th>
--                        <th>Server Name</th>
--                        <th>Owner</th>
-+                        <th>Projektname</th>
-+                        <th>Admin</th>
-                         <th>Service</th>
-                     </tr>
-                     @foreach($servers as $server)
-diff --git a/resources/views/admin/nodes/view/settings.blade.php b/resources/views/admin/nodes/view/settings.blade.php
-index bfc9d8c..ec0f256 100644
---- a/resources/views/admin/nodes/view/settings.blade.php
-+++ b/resources/views/admin/nodes/view/settings.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    {{ $node->name }}: Settings
-+    {{ $node->name }}: Einstellungen
- @endsection
- 
- @section('content-header')
--    <h1>{{ $node->name }}<small>Configure your node settings.</small></h1>
-+    <h1>{{ $node->name }}<small>Node-Einstellungen ändern.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
-         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
--        <li class="active">Settings</li>
-+        <li class="active">Einstellungen</li>
-     </ol>
- @endsection
- 
-@@ -19,11 +19,11 @@
-     <div class="col-xs-12">
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
--                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
--                <li class="active"><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
--                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
--                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
--                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
-+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Info</a></li>
-+                <li class="active"><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
-+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
-+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
-+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
-             </ul>
-         </div>
-     </div>
-@@ -33,24 +33,24 @@
-         <div class="col-sm-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Settings</h3>
-+                    <h3 class="box-title">Einstellungen</h3>
-                 </div>
-                 <div class="box-body row">
-                     <div class="form-group col-xs-12">
-                         <label for="name" class="control-label">Node Name</label>
-                         <div>
-                             <input type="text" autocomplete="off" name="name" class="form-control" value="{{ old('name', $node->name) }}" />
--                            <p class="text-muted"><small>Character limits: <code>a-zA-Z0-9_.-</code> and <code>[Space]</code> (min 1, max 100 characters).</small></p>
-+                            <p class="text-muted"><small>Mögliche Zeichen: <code>a-zA-Z0-9_.-</code> and <code>[Leerzeichen]</code> (min 1, max 100 Zeichen).</small></p>
-                         </div>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label for="description" class="control-label">Description</label>
-+                        <label for="description" class="control-label">Beschreibung</label>
-                         <div>
-                             <textarea name="description" id="description" rows="4" class="form-control">{{ $node->description }}</textarea>
-                         </div>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label for="name" class="control-label">Location</label>
-+                        <label for="name" class="control-label">Standort</label>
-                         <div>
-                             <select name="location_id" class="form-control">
-                                 @foreach($locations as $location)
-@@ -60,62 +60,62 @@
-                         </div>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label for="public" class="control-label">Allow Automatic Allocation <sup><a data-toggle="tooltip" data-placement="top" title="Allow automatic allocation to this Node?">?</a></sup></label>
-+                        <label for="public" class="control-label">Automatische Portzuweisung erlauben <sup><a data-toggle="tooltip" data-placement="top" title="Allow automatic allocation to this Node?">?</a></sup></label>
-                         <div>
--                            <input type="radio" name="public" value="1" {{ (old('public', $node->public)) ? 'checked' : '' }} id="public_1" checked> <label for="public_1" style="padding-left:5px;">Yes</label><br />
--                            <input type="radio" name="public" value="0" {{ (old('public', $node->public)) ? '' : 'checked' }} id="public_0"> <label for="public_0" style="padding-left:5px;">No</label>
-+                            <input type="radio" name="public" value="1" {{ (old('public', $node->public)) ? 'checked' : '' }} id="public_1" checked> <label for="public_1" style="padding-left:5px;">Ja</label><br />
-+                            <input type="radio" name="public" value="0" {{ (old('public', $node->public)) ? '' : 'checked' }} id="public_0"> <label for="public_0" style="padding-left:5px;">Nein</label>
-                         </div>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label for="fqdn" class="control-label">Fully Qualified Domain Name</label>
-+                        <label for="fqdn" class="control-label">Domain für diesen Node</label>
-                         <div>
-                             <input type="text" autocomplete="off" name="fqdn" class="form-control" value="{{ old('fqdn', $node->fqdn) }}" />
-                         </div>
--                        <p class="text-muted"><small>Please enter domain name (e.g <code>node.example.com</code>) to be used for connecting to the daemon. An IP address may only be used if you are not using SSL for this node.
-+                        <p class="text-muted"><small>Bitte gib den Domainnamen (z. B. <code>node.example.com</code>) ein, der für die Verbindung zum Daemon verwendet werden soll. Eine IP-Adresse darf nur verwendet werden, wenn du für diesen Node kein SSL verwendest.
-                                 <a tabindex="0" data-toggle="popover" data-trigger="focus" title="Why do I need a FQDN?" data-content="In order to secure communications between your server and this node we use SSL. We cannot generate a SSL certificate for IP Addresses, and as such you will need to provide a FQDN.">Why?</a>
-                             </small></p>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Communicate Over SSL</label>
-+                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Über SSL kommunizieren</label>
-                         <div>
-                             <div class="radio radio-success radio-inline">
-                                 <input type="radio" id="pSSLTrue" value="https" name="scheme" {{ (old('scheme', $node->scheme) === 'https') ? 'checked' : '' }}>
--                                <label for="pSSLTrue"> Use SSL Connection</label>
-+                                <label for="pSSLTrue"> SSL verwenden</label>
-                             </div>
-                             <div class="radio radio-danger radio-inline">
-                                 <input type="radio" id="pSSLFalse" value="http" name="scheme" {{ (old('scheme', $node->scheme) !== 'https') ? 'checked' : '' }}>
--                                <label for="pSSLFalse"> Use HTTP Connection</label>
-+                                <label for="pSSLFalse"> HTTP verwenden</label>
-                             </div>
-                         </div>
--                        <p class="text-muted small">In most cases you should select to use a SSL connection. If using an IP Address or you do not wish to use SSL at all, select a HTTP connection.</p>
-+                        <p class="text-muted small">In den meisten Fällen solltest du dich für eine SSL-Verbindung entscheiden. Wenn du eine IP-Adresse verwendest oder überhaupt kein SSL verwenden möchtest, wähle eine HTTP-Verbindung.</p>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Behind Proxy</label>
-+                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Proxy verwenden</label>
-                         <div>
-                             <div class="radio radio-success radio-inline">
-                                 <input type="radio" id="pProxyFalse" value="0" name="behind_proxy" {{ (old('behind_proxy', $node->behind_proxy) == false) ? 'checked' : '' }}>
--                                <label for="pProxyFalse"> Not Behind Proxy </label>
-+                                <label for="pProxyFalse"> Ja </label>
-                             </div>
-                             <div class="radio radio-info radio-inline">
-                                 <input type="radio" id="pProxyTrue" value="1" name="behind_proxy" {{ (old('behind_proxy', $node->behind_proxy) == true) ? 'checked' : '' }}>
--                                <label for="pProxyTrue"> Behind Proxy </label>
-+                                <label for="pProxyTrue"> Nein </label>
-                             </div>
-                         </div>
--                        <p class="text-muted small">If you are running the daemon behind a proxy such as Cloudflare, select this to have the daemon skip looking for certificates on boot.</p>
-+                        <p class="text-muted small">Wenn du den Daemon hinter einem Proxy wie Cloudflare ausführst, wähle diese Option aus, damit der Daemon die Suche nach Zertifikaten beim Booten überspringt.</p>
-                     </div>
-                     <div class="form-group col-xs-12">
--                        <label class="form-label"><span class="label label-warning"><i class="fa fa-wrench"></i></span> Maintenance Mode</label>
-+                        <label class="form-label"><span class="label label-warning"><i class="fa fa-wrench"></i></span> Wartungsmodus</label>
-                         <div>
-                             <div class="radio radio-success radio-inline">
-                                 <input type="radio" id="pMaintenanceFalse" value="0" name="maintenance_mode" {{ (old('behind_proxy', $node->maintenance_mode) == false) ? 'checked' : '' }}>
--                                <label for="pMaintenanceFalse"> Disabled</label>
-+                                <label for="pMaintenanceFalse"> Deaktiviert</label>
-                             </div>
-                             <div class="radio radio-warning radio-inline">
-                                 <input type="radio" id="pMaintenanceTrue" value="1" name="maintenance_mode" {{ (old('behind_proxy', $node->maintenance_mode) == true) ? 'checked' : '' }}>
--                                <label for="pMaintenanceTrue"> Enabled</label>
-+                                <label for="pMaintenanceTrue"> Aktiviert</label>
-                             </div>
-                         </div>
--                        <p class="text-muted small">If the node is marked as 'Under Maintenance' users won't be able to access servers that are on this node.</p>
-+                        <p class="text-muted small">Wenn der Wartungsmodus an ist, können Benutzer, die unter diesem Node einen Server haben, währenddessen nicht den Server starten.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -123,46 +123,46 @@
-         <div class="col-sm-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Allocation Limits</h3>
-+                    <h3 class="box-title">Ressoucen-Verwaltung</h3>
-                 </div>
-                 <div class="box-body row">
-                     <div class="col-xs-12">
-                         <div class="row">
-                             <div class="form-group col-xs-6">
--                                <label for="memory" class="control-label">Total Memory</label>
-+                                <label for="memory" class="control-label">Verfügbarer RAM</label>
-                                 <div class="input-group">
-                                     <input type="text" name="memory" class="form-control" data-multiplicator="true" value="{{ old('memory', $node->memory) }}"/>
--                                    <span class="input-group-addon">MiB</span>
-+                                    <span class="input-group-addon">MB</span>
-                                 </div>
-                             </div>
-                             <div class="form-group col-xs-6">
--                                <label for="memory_overallocate" class="control-label">Overallocate</label>
-+                                <label for="memory_overallocate" class="control-label">RAM-Toleranz über dem Limit</label>
-                                 <div class="input-group">
-                                     <input type="text" name="memory_overallocate" class="form-control" value="{{ old('memory_overallocate', $node->memory_overallocate) }}"/>
-                                     <span class="input-group-addon">%</span>
-                                 </div>
-                             </div>
-                         </div>
--                        <p class="text-muted small">Enter the total amount of memory available on this node for allocation to servers. You may also provide a percentage that can allow allocation of more than the defined memory.</p>
-+                        <p class="text-muted small">Gib die Gesamtmenge an Arbeitsspeicher ein, die auf diesem Node für die Zuordnung zu Servern verfügbar ist. Du kannst auch einen Prozentsatz angeben, der die Zuweisung von mehr als dem definierten Speicher zulassen kann.</p>
-                     </div>
-                     <div class="col-xs-12">
-                         <div class="row">
-                             <div class="form-group col-xs-6">
--                                <label for="disk" class="control-label">Disk Space</label>
-+                                <label for="disk" class="control-label">Speicherplatz</label>
-                                 <div class="input-group">
-                                     <input type="text" name="disk" class="form-control" data-multiplicator="true" value="{{ old('disk', $node->disk) }}"/>
--                                    <span class="input-group-addon">MiB</span>
-+                                    <span class="input-group-addon">MB</span>
-                                 </div>
-                             </div>
-                             <div class="form-group col-xs-6">
--                                <label for="disk_overallocate" class="control-label">Overallocate</label>
-+                                <label for="disk_overallocate" class="control-label">Speicher-Toleranz über dem Limit</label>
-                                 <div class="input-group">
-                                     <input type="text" name="disk_overallocate" class="form-control" value="{{ old('disk_overallocate', $node->disk_overallocate) }}"/>
-                                     <span class="input-group-addon">%</span>
-                                 </div>
-                             </div>
-                         </div>
--                        <p class="text-muted small">Enter the total amount of disk space available on this node for server allocation. You may also provide a percentage that will determine the amount of disk space over the set limit to allow.</p>
-+                        <p class="text-muted small">Gib den Gesamtspeicherplatz ein, der auf diesem Node für die Serverzuweisung verfügbar ist. Du kannst auch einen Prozentsatz angeben, der bestimmt, wie viel Speicherplatz über dem festgelegten Limit zulässig ist.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -170,16 +170,16 @@
-         <div class="col-sm-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">General Configuration</h3>
-+                    <h3 class="box-title">Allgemeine Einstellungen</h3>
-                 </div>
-                 <div class="box-body row">
-                     <div class="form-group col-xs-12">
--                        <label for="disk_overallocate" class="control-label">Maximum Web Upload Filesize</label>
-+                        <label for="disk_overallocate" class="control-label">Maximale Upload-Größe</label>
-                         <div class="input-group">
-                             <input type="text" name="upload_size" class="form-control" value="{{ old('upload_size', $node->upload_size) }}"/>
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
--                        <p class="text-muted"><small>Enter the maximum size of files that can be uploaded through the web-based file manager.</small></p>
-+                        <p class="text-muted"><small>Gib das Maximum an, wie groß eine Datei sein darf, den man hochladen kann.</small></p>
-                     </div>
-                     <div class="col-xs-12">
-                         <div class="row">
-@@ -198,7 +198,7 @@
-                         </div>
-                         <div class="row">
-                             <div class="col-md-12">
--                                <p class="text-muted"><small>The daemon runs its own SFTP management container and does not use the SSHd process on the main physical server. <Strong>Do not use the same port that you have assigned for your physical server's SSH process.</strong></small></p>
-+                                <p class="text-muted"><small>Der Daemon führt seinen eigenen SFTP-Verwaltungscontainer aus und verwendet nicht den SSHd-Prozess auf dem physischen Hauptserver. <Strong>Verwende nicht denselben Port, den du für den SSH-Prozess Ihres physischen Servers zugewiesen hast.</strong></small></p>
-                             </div>
-                         </div>
-                     </div>
-@@ -208,20 +208,20 @@
-         <div class="col-xs-12">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Save Settings</h3>
-+                    <h3 class="box-title">Änderungen speichern</h3>
-                 </div>
-                 <div class="box-body row">
-                     <div class="form-group col-sm-6">
-                         <div>
--                            <input type="checkbox" name="reset_secret" id="reset_secret" /> <label for="reset_secret" class="control-label">Reset Daemon Master Key</label>
-+                            <input type="checkbox" name="reset_secret" id="reset_secret" /> <label for="reset_secret" class="control-label">Daemon Master Schlüssel zurücksetzen</label>
-                         </div>
--                        <p class="text-muted"><small>Resetting the daemon master key will void any request coming from the old key. This key is used for all sensitive operations on the daemon including server creation and deletion. We suggest changing this key regularly for security.</small></p>
-+                        <p class="text-muted"><small>Das Zurücksetzen des Daemon-Hauptschlüssels macht alle Anfragen ungültig, die vom alten Schlüssel stammen. Dieser Schlüssel wird für alle sensiblen Vorgänge auf dem Daemon verwendet, einschließlich Servererstellung und -löschung. Wir empfehlen, diesen Schlüssel aus Sicherheitsgründen regelmäßig zu ändern.</small></p>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! method_field('PATCH') !!}
-                     {!! csrf_field() !!}
--                    <button type="submit" class="btn btn-primary pull-right">Save Changes</button>
-+                    <button type="submit" class="btn btn-primary pull-right">Änderungen speichern</button>
-                 </div>
-             </div>
-         </div>
-diff --git a/resources/views/admin/servers/index.blade.php b/resources/views/admin/servers/index.blade.php
-index 4d5ebcd..5c1e381 100644
---- a/resources/views/admin/servers/index.blade.php
-+++ b/resources/views/admin/servers/index.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    List Servers
-+    Alle Server
- @endsection
- 
- @section('content-header')
--    <h1>Servers<small>All servers available on the system.</small></h1>
-+    <h1>Server-Übersicht<small>Hier werden alle Server vom System angezeigt.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Servers</li>
-+        <li class="active">Alle Server</li>
-     </ol>
- @endsection
- 
-@@ -17,14 +17,14 @@
-     <div class="col-xs-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Server List</h3>
-+                <h3 class="box-title">Server-Liste</h3>
-                 <div class="box-tools search01">
-                     <form action="{{ route('admin.servers') }}" method="GET">
-                         <div class="input-group input-group-sm">
--                            <input type="text" name="filter[*]" class="form-control pull-right" value="{{ request()->input()['filter']['*'] ?? '' }}" placeholder="Search Servers">
-+                            <input type="text" name="filter[*]" class="form-control pull-right" value="{{ request()->input()['filter']['*'] ?? '' }}" placeholder="Server suchen">
-                             <div class="input-group-btn">
-                                 <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
--                                <a href="{{ route('admin.servers.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Create New</button></a>
-+                                <a href="{{ route('admin.servers.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Server erstellen</button></a>
-                             </div>
-                         </div>
-                     </form>
-@@ -34,11 +34,11 @@
-                 <table class="table table-hover">
-                     <tbody>
-                         <tr>
--                            <th>Server Name</th>
-+                            <th>Projektname</th>
-                             <th>UUID</th>
--                            <th>Owner</th>
-+                            <th>Admin</th>
-                             <th>Node</th>
--                            <th>Connection</th>
-+                            <th>Zugang</th>
-                             <th></th>
-                             <th></th>
-                         </tr>
-@@ -53,11 +53,11 @@
-                                 </td>
-                                 <td class="text-center">
-                                     @if($server->isSuspended())
--                                        <span class="label bg-maroon">Suspended</span>
-+                                        <span class="label bg-maroon">Gesperrt</span>
-                                     @elseif(! $server->isInstalled())
--                                        <span class="label label-warning">Installing</span>
-+                                        <span class="label label-warning">Installiere...</span>
-                                     @else
--                                        <span class="label label-success">Active</span>
-+                                        <span class="label label-success">Aktiv</span>
-                                     @endif
-                                 </td>
-                                 <td class="text-center">
-diff --git a/resources/views/admin/servers/new.blade.php b/resources/views/admin/servers/new.blade.php
-index 4198634..91812dc 100644
---- a/resources/views/admin/servers/new.blade.php
-+++ b/resources/views/admin/servers/new.blade.php
-@@ -1,15 +1,15 @@
- @extends('layouts.admin')
- 
- @section('title')
--    New Server
-+    Server erstellen
- @endsection
- 
- @section('content-header')
--    <h1>Create Server<small>Add a new server to the panel.</small></h1>
-+    <h1>Server erstellen<small>Erstelle einen neuen Server in Pterodactyl</small></h1>
-     <ol class="breadcrumb">
--        <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
--        <li class="active">Create Server</li>
-+        <li><a href="{{ route('admin.index') }}">Server-Übersicht</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Server-Verwaltung</a></li>
-+        <li class="active">Server erstellen</li>
-     </ol>
- @endsection
- 
-@@ -19,35 +19,35 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Core Details</h3>
-+                    <h3 class="box-title">Kern-Details</h3>
-                 </div>
- 
-                 <div class="box-body row">
-                     <div class="col-md-6">
-                         <div class="form-group">
--                            <label for="pName">Server Name</label>
-+                            <label for="pName">Name des Servers</label>
-                             <input type="text" class="form-control" id="pName" name="name" value="{{ old('name') }}" placeholder="Server Name">
--                            <p class="small text-muted no-margin">Character limits: <code>a-z A-Z 0-9 _ - .</code> and <code>[Space]</code>.</p>
-+                            <p class="small text-muted no-margin">Darf genutzt werden: <code>a-z A-Z 0-9 _ - .</code> und <code>[Leerzeichen]</code>.</p>
-                         </div>
- 
-                         <div class="form-group">
--                            <label for="pUserId">Server Owner</label>
-+                            <label for="pUserId">Admin des Servers</label>
-                             <select id="pUserId" name="owner_id" class="form-control" style="padding-left:0;"></select>
--                            <p class="small text-muted no-margin">Email address of the Server Owner.</p>
-+                            <p class="small text-muted no-margin">E-Mail Adresse des Owners (Muss als Benutzer registriert sein).</p>
-                         </div>
-                     </div>
- 
-                     <div class="col-md-6">
-                         <div class="form-group">
--                            <label for="pDescription" class="control-label">Server Description</label>
-+                            <label for="pDescription" class="control-label">Kurzbeschreibung</label>
-                             <textarea id="pDescription" name="description" rows="3" class="form-control">{{ old('description') }}</textarea>
--                            <p class="text-muted small">A brief description of this server.</p>
-+                            <p class="text-muted small">Eine Beschreibung, was der Server genau ist.</p>
-                         </div>
- 
-                         <div class="form-group">
-                             <div class="checkbox checkbox-primary no-margin-bottom">
-                                 <input id="pStartOnCreation" name="start_on_completion" type="checkbox" {{ \Pterodactyl\Helpers\Utilities::checked('start_on_completion', 1) }} />
--                                <label for="pStartOnCreation" class="strong">Start Server when Installed</label>
-+                                <label for="pStartOnCreation" class="strong">Hochfahren, wenn der Server bereit ist</label>
-                             </div>
-                         </div>
-                     </div>
-@@ -61,7 +61,7 @@
-             <div class="box">
-                 <div class="overlay" id="allocationLoader" style="display:none;"><i class="fa fa-refresh fa-spin"></i></div>
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Allocation Management</h3>
-+                    <h3 class="box-title">Portzuweisung</h3>
-                 </div>
- 
-                 <div class="box-body row">
-@@ -81,19 +81,19 @@
-                             @endforeach
-                         </select>
- 
--                        <p class="small text-muted no-margin">The node which this server will be deployed to.</p>
-+                        <p class="small text-muted no-margin">Der Node, worüber der Server betrieben wird.</p>
-                     </div>
- 
-                     <div class="form-group col-sm-4">
--                        <label for="pAllocation">Default Allocation</label>
-+                        <label for="pAllocation">Primärer Port</label>
-                         <select id="pAllocation" name="allocation_id" class="form-control"></select>
--                        <p class="small text-muted no-margin">The main allocation that will be assigned to this server.</p>
-+                        <p class="small text-muted no-margin">Der Port, der primär für diesen Server gelten soll.</p>
-                     </div>
- 
-                     <div class="form-group col-sm-4">
--                        <label for="pAllocationAdditional">Additional Allocation(s)</label>
-+                        <label for="pAllocationAdditional">Weitere Portreservierungen</label>
-                         <select id="pAllocationAdditional" name="allocation_additional[]" class="form-control" multiple></select>
--                        <p class="small text-muted no-margin">Additional allocations to assign to this server on creation.</p>
-+                        <p class="small text-muted no-margin">Alle Ports, die für diesen Server direkt mit reserviert werden sollen.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -105,30 +105,30 @@
-             <div class="box">
-                 <div class="overlay" id="allocationLoader" style="display:none;"><i class="fa fa-refresh fa-spin"></i></div>
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Application Feature Limits</h3>
-+                    <h3 class="box-title">Limits des Servers</h3>
-                 </div>
- 
-                 <div class="box-body row">
-                     <div class="form-group col-xs-6">
--                        <label for="pDatabaseLimit" class="control-label">Database Limit</label>
-+                        <label for="pDatabaseLimit" class="control-label">Anzahl an Datenbanken</label>
-                         <div>
-                             <input type="text" id="pDatabaseLimit" name="database_limit" class="form-control" value="{{ old('database_limit', 0) }}"/>
-                         </div>
--                        <p class="text-muted small">The total number of databases a user is allowed to create for this server.</p>
-+                        <p class="text-muted small">Das Maximum an Datenbanken, was dieser Server betreiben darf.</p>
-                     </div>
-                     <div class="form-group col-xs-6">
--                        <label for="pAllocationLimit" class="control-label">Allocation Limit</label>
-+                        <label for="pAllocationLimit" class="control-label">Anzahl an Portfreigaben</label>
-                         <div>
-                             <input type="text" id="pAllocationLimit" name="allocation_limit" class="form-control" value="{{ old('allocation_limit', 0) }}"/>
-                         </div>
--                        <p class="text-muted small">The total number of allocations a user is allowed to create for this server.</p>
-+                        <p class="text-muted small">Das Maximum an Ports, die zugewiesen werden dürfen.</p>
-                     </div>
-                     <div class="form-group col-xs-6">
--                        <label for="pBackupLimit" class="control-label">Backup Limit</label>
-+                        <label for="pBackupLimit" class="control-label">Anzahl an Backups</label>
-                         <div>
-                             <input type="text" id="pBackupLimit" name="backup_limit" class="form-control" value="{{ old('backup_limit', 0) }}"/>
-                         </div>
--                        <p class="text-muted small">The total number of backups that can be created for this server.</p>
-+                        <p class="text-muted small">Das Maximum an Backups, die gleichzeitig bereit stehen düfen.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -138,7 +138,7 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Resource Management</h3>
-+                    <h3 class="box-title">Ressourcen für diesen Server</h3>
-                 </div>
- 
-                 <div class="box-body row">
-@@ -150,72 +150,72 @@
-                             <span class="input-group-addon">%</span>
-                         </div>
- 
--                        <p class="text-muted small">If you do not want to limit CPU usage, set the value to <code>0</code>. To determine a value, take the number of threads and multiply it by 100. For example, on a quad core system without hyperthreading <code>(4 * 100 = 400)</code> there is <code>400%</code> available. To limit a server to using half of a single thread, you would set the value to <code>50</code>. To allow a server to use up to two threads, set the value to <code>200</code>.<p>
-+                        <p class="text-muted small">Wenn du das Limit auf <code>0</code> einstellst, wird kein Limit festgelegt. 100% bedeutet 1 Kern, wenn du also 4 Kerne zuweisen möchtest, dann musst du <code>400%</code> angeben: <code>(4 * 100 = 400)</code>. Wenn du nur einen halben Kern zuweisen möchtest, dann gib <code>50</code> an. Du kannst auch ganze und halbe Kerne gleichzeitig angeben, 2 1/2 Kerne wären <code>250</code>.<p>
-                     </div>
- 
-                     <div class="form-group col-xs-6">
--                        <label for="pThreads">CPU Pinning</label>
-+                        <label for="pThreads">Direkte CPU-Kern Reservierung</label>
- 
-                         <div>
-                             <input type="text" id="pThreads" name="threads" class="form-control" value="{{ old('threads') }}" />
-                         </div>
- 
--                        <p class="text-muted small"><strong>Advanced:</strong> Enter the specific CPU threads that this process can run on, or leave blank to allow all threads. This can be a single number, or a comma separated list. Example: <code>0</code>, <code>0-1,3</code>, or <code>0,1,3,4</code>.</p>
-+                        <p class="text-muted small"><strong>Erweitert:</strong> Gib die spezifischen CPU-Threads ein, auf denen dieser Prozess ausgeführt werden kann, oder lasse das Feld leer, um alle Threads zuzulassen. Dies kann eine einzelne Zahl oder eine durch Kommas getrennte Liste sein. Beispiel: <code>0</code>, <code>0-1,3</code> oder <code>0,1,3,4</code>.</p>
-                     </div>
-                 </div>
- 
-                 <div class="box-body row">
-                     <div class="form-group col-xs-6">
--                        <label for="pMemory">Memory</label>
-+                        <label for="pMemory">Arbeitsspeicher</label>
- 
-                         <div class="input-group">
-                             <input type="text" id="pMemory" name="memory" class="form-control" value="{{ old('memory') }}" />
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
- 
--                        <p class="text-muted small">The maximum amount of memory allowed for this container. Setting this to <code>0</code> will allow unlimited memory in a container.</p>
-+                        <p class="text-muted small">Die maximal zulässige RAM-Menge für diesen Container. Wenn du es auf <code>0</code> setzt, wird kein Limit festgelegt.</p>
-                     </div>
- 
-                     <div class="form-group col-xs-6">
--                        <label for="pSwap">Swap</label>
-+                        <label for="pSwap">Auslagerungsbereich / Swap</label>
- 
-                         <div class="input-group">
-                             <input type="text" id="pSwap" name="swap" class="form-control" value="{{ old('swap', 0) }}" />
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
- 
--                        <p class="text-muted small">Setting this to <code>0</code> will disable swap space on this server. Setting to <code>-1</code> will allow unlimited swap.</p>
-+                        <p class="text-muted small">Wenn es auf <code>0</code> festgelegt ist, wird der Auslagerungsbereich auf diesem Server deaktiviert. Die Einstellung auf <code>-1</code> erlaubt unbegrenzten Austausch.</p>
-                     </div>
-                 </div>
- 
-                 <div class="box-body row">
-                     <div class="form-group col-xs-6">
--                        <label for="pDisk">Disk Space</label>
-+                        <label for="pDisk">Festplattenspeicher</label>
- 
-                         <div class="input-group">
-                             <input type="text" id="pDisk" name="disk" class="form-control" value="{{ old('disk') }}" />
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
- 
--                        <p class="text-muted small">This server will not be allowed to boot if it is using more than this amount of space. If a server goes over this limit while running it will be safely stopped and locked until enough space is available. Set to <code>0</code> to allow unlimited disk usage.</p>
-+                        <p class="text-muted small">Dieser Server darf nicht gestartet werden, wenn er mehr als diese Menge an Speicherplatz verwendet. Wenn ein Server während des Betriebs dieses Limit überschreitet, wird er sicher gestoppt und gesperrt, bis genügend Speicherplatz verfügbar ist. Auf <code>0</code> setzen, um unbegrenzte Festplattennutzung zuzulassen.</p>
-                     </div>
- 
-                     <div class="form-group col-xs-6">
--                        <label for="pIO">Block IO Weight</label>
-+                        <label for="pIO">Block-IO Gewicht</label>
- 
-                         <div>
-                             <input type="text" id="pIO" name="io" class="form-control" value="{{ old('io', 500) }}" />
-                         </div>
- 
--                        <p class="text-muted small"><strong>Advanced</strong>: The IO performance of this server relative to other <em>running</em> containers on the system. Value should be between <code>10</code> and <code>1000</code>. Please see <a href="https://docs.docker.com/engine/reference/run/#block-io-bandwidth-blkio-constraint" target="_blank">this documentation</a> for more information about it.</p>
-+                        <p class="text-muted small"><strong>Erweitert</strong>: Die IO-Leistung dieses Servers im Vergleich zu anderen <em>ausgeführten</em> Containern auf dem System. Der Wert sollte zwischen <code>10</code> und <code>1000</code> liegen. Weitere Informationen findest du in <a href="https://docs.docker.com/engine/reference/run/#block-io-bandwidth-blkio-constraint" target="_blank">dieser Wiki-Page</a>.</p>
-                     </div>
-                     <div class="form-group col-xs-12">
-                         <div class="checkbox checkbox-primary no-margin-bottom">
-                             <input type="checkbox" id="pOomDisabled" name="oom_disabled" value="0" {{ \Pterodactyl\Helpers\Utilities::checked('oom_disabled', 0) }} />
--                            <label for="pOomDisabled" class="strong">Enable OOM Killer</label>
-+                            <label for="pOomDisabled" class="strong">RAM-Killer</label>
-                         </div>
- 
--                        <p class="small text-muted no-margin">Terminates the server if it breaches the memory limits. Enabling OOM killer may cause server processes to exit unexpectedly.</p>
-+                        <p class="small text-muted no-margin">Beendet den Server, wenn er die Speichergrenzen überschreitet. Das Aktivieren von OOM Killer kann dazu führen, dass Serverprozesse unerwartet beendet werden, Datenverlust ist hierbei möglich!</p>
-                     </div>
-                 </div>
-             </div>
-@@ -226,7 +226,7 @@
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Nest Configuration</h3>
-+                    <h3 class="box-title">Nest - Einstellungen</h3>
-                 </div>
- 
-                 <div class="box-body row">
-@@ -243,21 +243,21 @@
-                             @endforeach
-                         </select>
- 
--                        <p class="small text-muted no-margin">Select the Nest that this server will be grouped under.</p>
-+                        <p class="small text-muted no-margin">Wähle den Nest, der die Grundlagen des Servers festlegen soll.</p>
-                     </div>
- 
-                     <div class="form-group col-xs-12">
-                         <label for="pEggId">Egg</label>
-                         <select id="pEggId" name="egg_id" class="form-control"></select>
--                        <p class="small text-muted no-margin">Select the Egg that will define how this server should operate.</p>
-+                        <p class="small text-muted no-margin">Wähle den Egg aus, den du nutzen möchtest.</p>
-                     </div>
-                     <div class="form-group col-xs-12">
-                         <div class="checkbox checkbox-primary no-margin-bottom">
-                             <input type="checkbox" id="pSkipScripting" name="skip_scripts" value="1" {{ \Pterodactyl\Helpers\Utilities::checked('skip_scripts', 0) }} />
--                            <label for="pSkipScripting" class="strong">Skip Egg Install Script</label>
-+                            <label for="pSkipScripting" class="strong">Egg-Installation überpringen</label>
-                         </div>
- 
--                        <p class="small text-muted no-margin">If the selected Egg has an install script attached to it, the script will run during the install. If you would like to skip this step, check this box.</p>
-+                        <p class="small text-muted no-margin">Wenn du diesen Abschnitt überspringen und/oder selbst machen möchtest, dann setze einen Häkchen.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -266,15 +266,15 @@
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Docker Configuration</h3>
-+                    <h3 class="box-title">Docker - Einstellungen</h3>
-                 </div>
- 
-                 <div class="box-body row">
-                     <div class="form-group col-xs-12">
-                         <label for="pDefaultContainer">Docker Image</label>
-                         <select id="pDefaultContainer" name="image" class="form-control"></select>
--                        <input id="pDefaultContainerCustom" name="custom_image" value="{{ old('custom_image') }}" class="form-control" placeholder="Or enter a custom image..." style="margin-top:1rem"/>
--                        <p class="small text-muted no-margin">This is the default Docker image that will be used to run this server. Select an image from the dropdown above, or enter a custom image in the text field above.</p>
-+                        <input id="pDefaultContainerCustom" name="custom_image" value="{{ old('custom_image') }}" class="form-control" placeholder="Oder füge ein eigenes Image hinzu..." style="margin-top:1rem"/>
-+                        <p class="small text-muted no-margin">Dies ist das Standard-Docker-Image, das zum Ausführen dieses Servers verwendet wird. Wähle ein Bild aus der Dropdown-Liste oben aus oder gebe ein benutzerdefiniertes Bild in das Textfeld oben ein.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -285,26 +285,26 @@
-         <div class="col-md-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Startup Configuration</h3>
-+                    <h3 class="box-title">Startbefehl - Einstellungen</h3>
-                 </div>
- 
-                 <div class="box-body row">
-                     <div class="form-group col-xs-12">
--                        <label for="pStartup">Startup Command</label>
-+                        <label for="pStartup">Der Befehl zum starten des Servers</label>
-                         <input type="text" id="pStartup" name="startup" value="{{ old('startup') }}" class="form-control" />
--                        <p class="small text-muted no-margin">The following data substitutes are available for the startup command: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, and <code>@{{SERVER_PORT}}</code>. They will be replaced with the allocated memory, server IP, and server port respectively.</p>
-+                        <p class="small text-muted no-margin">Die folgenden Variablen können in diesen Befehl integriert werden: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, und <code>@{{SERVER_PORT}}</code>. Sie werden automatisch mit den Einstellungen befüllt, die festgelegt wurden. Zukünfigte Änderungen an den Limits oder den Ports werden bei Nutzung automatisch verändert.</p>
-                     </div>
-                 </div>
- 
-                 <div class="box-header with-border" style="margin-top:-10px;">
--                    <h3 class="box-title">Service Variables</h3>
-+                    <h3 class="box-title">Serversoftware - Einstellungen</h3>
-                 </div>
- 
-                 <div class="box-body row" id="appendVariablesTo"></div>
- 
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <input type="submit" class="btn btn-success pull-right" value="Create Server" />
-+                    <input type="submit" class="btn btn-success pull-right" value="Server erstellen" />
-                 </div>
-             </div>
-         </div>
-diff --git a/resources/views/admin/servers/partials/navigation.blade.php b/resources/views/admin/servers/partials/navigation.blade.php
-index 964eac8..1117b05 100644
---- a/resources/views/admin/servers/partials/navigation.blade.php
-+++ b/resources/views/admin/servers/partials/navigation.blade.php
-@@ -7,29 +7,29 @@
-         <div class="nav-tabs-custom nav-tabs-floating">
-             <ul class="nav nav-tabs">
-                 <li class="{{ $router->currentRouteNamed('admin.servers.view') ? 'active' : '' }}">
--                    <a href="{{ route('admin.servers.view', $server->id) }}">About</a></li>
-+                    <a href="{{ route('admin.servers.view', $server->id) }}">Über den Server</a></li>
-                 @if($server->isInstalled())
-                     <li class="{{ $router->currentRouteNamed('admin.servers.view.details') ? 'active' : '' }}">
--                        <a href="{{ route('admin.servers.view.details', $server->id) }}">Details</a>
-+                        <a href="{{ route('admin.servers.view.details', $server->id) }}">Infos</a>
-                     </li>
-                     <li class="{{ $router->currentRouteNamed('admin.servers.view.build') ? 'active' : '' }}">
--                        <a href="{{ route('admin.servers.view.build', $server->id) }}">Build Configuration</a>
-+                        <a href="{{ route('admin.servers.view.build', $server->id) }}">Einstellungen</a>
-                     </li>
-                     <li class="{{ $router->currentRouteNamed('admin.servers.view.startup') ? 'active' : '' }}">
--                        <a href="{{ route('admin.servers.view.startup', $server->id) }}">Startup</a>
-+                        <a href="{{ route('admin.servers.view.startup', $server->id) }}">Startbefehl</a>
-                     </li>
-                     <li class="{{ $router->currentRouteNamed('admin.servers.view.database') ? 'active' : '' }}">
--                        <a href="{{ route('admin.servers.view.database', $server->id) }}">Database</a>
-+                        <a href="{{ route('admin.servers.view.database', $server->id) }}">Datenbanken</a>
-                     </li>
-                     <li class="{{ $router->currentRouteNamed('admin.servers.view.mounts') ? 'active' : '' }}">
--                        <a href="{{ route('admin.servers.view.mounts', $server->id) }}">Mounts</a>
-+                        <a href="{{ route('admin.servers.view.mounts', $server->id) }}">Integrationen</a>
-                     </li>
-                 @endif
-                 <li class="{{ $router->currentRouteNamed('admin.servers.view.manage') ? 'active' : '' }}">
--                    <a href="{{ route('admin.servers.view.manage', $server->id) }}">Manage</a>
-+                    <a href="{{ route('admin.servers.view.manage', $server->id) }}">Verwaltung</a>
-                 </li>
-                 <li class="tab-danger {{ $router->currentRouteNamed('admin.servers.view.delete') ? 'active' : '' }}">
--                    <a href="{{ route('admin.servers.view.delete', $server->id) }}">Delete</a>
-+                    <a href="{{ route('admin.servers.view.delete', $server->id) }}">Löschen</a>
-                 </li>
-                 <li class="tab-success">
-                     <a href="/server/{{ $server->uuidShort }}" target="_blank"><i class="fa fa-external-link"></i></a>
-diff --git a/resources/views/admin/servers/view/build.blade.php b/resources/views/admin/servers/view/build.blade.php
-index 655ea36..987d39a 100644
---- a/resources/views/admin/servers/view/build.blade.php
-+++ b/resources/views/admin/servers/view/build.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Server — {{ $server->name }}: Build Details
-+    Server — {{ $server->name }}: Kontrollzentrum
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Control allocations and system resources for this server.</small></h1>
-+    <h1>{{ $server->name }}<small>Verwalte hier die Ressourcen des Servers.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
--        <li class="active">Build Configuration</li>
-+        <li class="active">Kontrollzentrum</li>
-     </ol>
- @endsection
- 
-@@ -21,7 +21,7 @@
-         <div class="col-sm-5">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Resource Management</h3>
-+                    <h3 class="box-title">Zugewiesene Ressourcen</h3>
-                 </div>
-                 <div class="box-body">
-                 <div class="form-group">
-@@ -30,59 +30,59 @@
-                             <input type="text" name="cpu" class="form-control" value="{{ old('cpu', $server->cpu) }}"/>
-                             <span class="input-group-addon">%</span>
-                         </div>
--                        <p class="text-muted small">Each <em>virtual</em> core (thread) on the system is considered to be <code>100%</code>. Setting this value to <code>0</code> will allow a server to use CPU time without restrictions.</p>
-+                        <p class="text-muted small">Jeder <em>virtuelle</em> ganze Kern bedeutet hier: <code>100%</code>. Setze es auf <code>0</code>, wenn du kein Limit festlegen möchtest.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="threads" class="control-label">CPU Pinning</label>
-+                        <label for="threads" class="control-label">Direkte CPU-Kern Reservierung</label>
-                         <div>
-                             <input type="text" name="threads" class="form-control" value="{{ old('threads', $server->threads) }}"/>
-                         </div>
--                        <p class="text-muted small"><strong>Advanced:</strong> Enter the specific CPU cores that this process can run on, or leave blank to allow all cores. This can be a single number, or a comma seperated list. Example: <code>0</code>, <code>0-1,3</code>, or <code>0,1,3,4</code>.</p>
-+                        <p class="text-muted small"><strong>Erweitert:</strong> Gib die spezifischen CPU-Threads ein, auf denen dieser Prozess ausgeführt werden kann, oder lasse das Feld leer, um alle Threads zuzulassen. Dies kann eine einzelne Zahl oder eine durch Kommas getrennte Liste sein. Beispiel: <code>0</code>, <code>0-1,3</code>, or <code>0,1,3,4</code>.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="memory" class="control-label">Allocated Memory</label>
-+                        <label for="memory" class="control-label">Arbeitsspeicher</label>
-                         <div class="input-group">
-                             <input type="text" name="memory" data-multiplicator="true" class="form-control" value="{{ old('memory', $server->memory) }}"/>
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
--                        <p class="text-muted small">The maximum amount of memory allowed for this container. Setting this to <code>0</code> will allow unlimited memory in a container.</p>
-+                        <p class="text-muted small">Die maximal zulässige RAM-Menge für diesen Container. Wenn du es auf <code>0</code> setzt, wird kein Limit festgelegt.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="swap" class="control-label">Allocated Swap</label>
-+                        <label for="swap" class="control-label">Auslagerungsbereich / Swap</label>
-                         <div class="input-group">
-                             <input type="text" name="swap" data-multiplicator="true" class="form-control" value="{{ old('swap', $server->swap) }}"/>
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
--                        <p class="text-muted small">Setting this to <code>0</code> will disable swap space on this server. Setting to <code>-1</code> will allow unlimited swap.</p>
-+                        <p class="text-muted small">Setze es auf <code>0</code>, wenn du es nicht nutzen möchtest. Du kannst auch mit <code>-1</code> einen unbegrenzten Bereich festlegen.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="cpu" class="control-label">Disk Space Limit</label>
-+                        <label for="cpu" class="control-label">Festplattenspeicher</label>
-                         <div class="input-group">
-                             <input type="text" name="disk" class="form-control" value="{{ old('disk', $server->disk) }}"/>
--                            <span class="input-group-addon">MiB</span>
-+                            <span class="input-group-addon">MB</span>
-                         </div>
--                        <p class="text-muted small">This server will not be allowed to boot if it is using more than this amount of space. If a server goes over this limit while running it will be safely stopped and locked until enough space is available. Set to <code>0</code> to allow unlimited disk usage.</p>
-+                        <p class="text-muted small">Dieser Server darf nicht gestartet werden, wenn er mehr als diese Menge an Speicherplatz verwendet. Wenn ein Server während des Betriebs dieses Limit überschreitet, wird er sicher gestoppt und gesperrt, bis genügend Speicherplatz verfügbar ist. Auf <code>0</code> setzen, um unbegrenzte Festplattennutzung zuzulassen.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="io" class="control-label">Block IO Proportion</label>
-+                        <label for="io" class="control-label">Block IO Gewicht</label>
-                         <div>
-                             <input type="text" name="io" class="form-control" value="{{ old('io', $server->io) }}"/>
-                         </div>
--                        <p class="text-muted small"><strong>Advanced</strong>: The IO performance of this server relative to other <em>running</em> containers on the system. Value should be between <code>10</code> and <code>1000</code>.</code></p>
-+                        <p class="text-muted small"><strong>Erweitert</strong>: Die IO-Leistung dieses Servers im Vergleich zu anderen <em>ausgeführten</em> Containern auf dem System. Der Wert sollte zwischen <code>10</code> und <code>1000</code> liegen.</code></p>
-                     </div>
-                     <div class="form-group">
--                        <label for="cpu" class="control-label">OOM Killer</label>
-+                        <label for="cpu" class="control-label">RAM-Killer</label>
-                         <div>
-                             <div class="radio radio-danger radio-inline">
-                                 <input type="radio" id="pOomKillerEnabled" value="0" name="oom_disabled" @if(!$server->oom_disabled)checked @endif>
--                                <label for="pOomKillerEnabled">Enabled</label>
-+                                <label for="pOomKillerEnabled">An</label>
-                             </div>
-                             <div class="radio radio-success radio-inline">
-                                 <input type="radio" id="pOomKillerDisabled" value="1" name="oom_disabled" @if($server->oom_disabled)checked @endif>
--                                <label for="pOomKillerDisabled">Disabled</label>
-+                                <label for="pOomKillerDisabled">Aus</label>
-                             </div>
-                             <p class="text-muted small">
--                                Enabling OOM killer may cause server processes to exit unexpectedly.
-+                                Beendet den Server, wenn er die Speichergrenzen überschreitet. Das Aktivieren von OOM Killer kann dazu führen, dass Serverprozesse unerwartet beendet werden, Datenverlust ist hierbei möglich!
-                             </p>
-                         </div>
-                     </div>
-@@ -94,30 +94,30 @@
-                 <div class="col-xs-12">
-                     <div class="box">
-                         <div class="box-header with-border">
--                            <h3 class="box-title">Application Feature Limits</h3>
-+                            <h3 class="box-title">Weitere Limits</h3>
-                         </div>
-                         <div class="box-body">
-                             <div class="row">
-                                 <div class="form-group col-xs-6">
--                                    <label for="database_limit" class="control-label">Database Limit</label>
-+                                    <label for="database_limit" class="control-label">Maximum an Datenbanken</label>
-                                     <div>
-                                         <input type="text" name="database_limit" class="form-control" value="{{ old('database_limit', $server->database_limit) }}"/>
-                                     </div>
--                                    <p class="text-muted small">The total number of databases a user is allowed to create for this server.</p>
-+                                    <p class="text-muted small">Die maximale Anzahl an Datenbanken, die der Server gleichzeitig betreiben darf.</p>
-                                 </div>
-                                 <div class="form-group col-xs-6">
--                                    <label for="allocation_limit" class="control-label">Allocation Limit</label>
-+                                    <label for="allocation_limit" class="control-label">Maximum an Portfreigaben</label>
-                                     <div>
-                                         <input type="text" name="allocation_limit" class="form-control" value="{{ old('allocation_limit', $server->allocation_limit) }}"/>
-                                     </div>
--                                    <p class="text-muted small">The total number of allocations a user is allowed to create for this server.</p>
-+                                    <p class="text-muted small">Die maximale Anzahl an Portfreigaben, die der Server maximal haben darf.</p>
-                                 </div>
-                                 <div class="form-group col-xs-6">
--                                    <label for="backup_limit" class="control-label">Backup Limit</label>
-+                                    <label for="backup_limit" class="control-label">Maximum an Backups</label>
-                                     <div>
-                                         <input type="text" name="backup_limit" class="form-control" value="{{ old('backup_limit', $server->backup_limit) }}"/>
-                                     </div>
--                                    <p class="text-muted small">The total number of backups that can be created for this server.</p>
-+                                    <p class="text-muted small">Die maximale Anzahl an Backups, die gleichzeitig zur Verfügung stehen dürfen.</p>
-                                 </div>
-                             </div>
-                         </div>
-@@ -126,11 +126,11 @@
-                 <div class="col-xs-12">
-                     <div class="box">
-                         <div class="box-header with-border">
--                            <h3 class="box-title">Allocation Management</h3>
-+                            <h3 class="box-title">Portzuteilung - Einstellungen</h3>
-                         </div>
-                         <div class="box-body">
-                             <div class="form-group">
--                                <label for="pAllocation" class="control-label">Game Port</label>
-+                                <label for="pAllocation" class="control-label">Primärer Port</label>
-                                 <select id="pAllocation" name="allocation_id" class="form-control">
-                                     @foreach ($assigned as $assignment)
-                                         <option value="{{ $assignment->id }}"
-@@ -140,10 +140,10 @@
-                                         >{{ $assignment->alias }}:{{ $assignment->port }}</option>
-                                     @endforeach
-                                 </select>
--                                <p class="text-muted small">The default connection address that will be used for this game server.</p>
-+                                <p class="text-muted small">Das ist der primäre Port für diesen Server.</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pAddAllocations" class="control-label">Assign Additional Ports</label>
-+                                <label for="pAddAllocations" class="control-label">Ports zuweisen</label>
-                                 <div>
-                                     <select name="add_allocations[]" class="form-control" multiple id="pAddAllocations">
-                                         @foreach ($unassigned as $assignment)
-@@ -151,10 +151,10 @@
-                                         @endforeach
-                                     </select>
-                                 </div>
--                                <p class="text-muted small">Please note that due to software limitations you cannot assign identical ports on different IPs to the same server.</p>
-+                                <p class="text-muted small">Du kannst jeden Port nur einmal verwenden!</p>
-                             </div>
-                             <div class="form-group">
--                                <label for="pRemoveAllocations" class="control-label">Remove Additional Ports</label>
-+                                <label for="pRemoveAllocations" class="control-label">Ports entfernen</label>
-                                 <div>
-                                     <select name="remove_allocations[]" class="form-control" multiple id="pRemoveAllocations">
-                                         @foreach ($assigned as $assignment)
-@@ -162,12 +162,12 @@
-                                         @endforeach
-                                     </select>
-                                 </div>
--                                <p class="text-muted small">Simply select which ports you would like to remove from the list above. If you want to assign a port on a different IP that is already in use you can select it from the left and delete it here.</p>
-+                                <p class="text-muted small">Hier kannst du Ports wieder für andere Server freigeben.</p>
-                             </div>
-                         </div>
-                         <div class="box-footer">
-                             {!! csrf_field() !!}
--                            <button type="submit" class="btn btn-primary pull-right">Update Build Configuration</button>
-+                            <button type="submit" class="btn btn-primary pull-right">Änderungen speichern</button>
-                         </div>
-                     </div>
-                 </div>
-diff --git a/resources/views/admin/servers/view/database.blade.php b/resources/views/admin/servers/view/database.blade.php
-index 385ce0f..89be468 100644
---- a/resources/views/admin/servers/view/database.blade.php
-+++ b/resources/views/admin/servers/view/database.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Server — {{ $server->name }}: Databases
-+    Server — {{ $server->name }}: Datenbanken
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Manage server databases.</small></h1>
-+    <h1>{{ $server->name }}<small>Verwalte die Datenbanken des Servers.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
--        <li class="active">Databases</li>
-+        <li class="active">Datenbanken</li>
-     </ol>
- @endsection
- 
-@@ -19,20 +19,20 @@
- <div class="row">
-     <div class="col-sm-7">
-         <div class="alert alert-info">
--            Database passwords can be viewed when <a href="/server/{{ $server->uuidShort }}/databases">visiting this server</a> on the front-end.
-+            Die Passwörter der Datenbanken können <a href="/server/{{ $server->uuidShort }}/databases">im Server-Panel</a> eingesehen werden.
-         </div>
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Active Databases</h3>
-+                <h3 class="box-title">Aktive Datenbanken</h3>
-             </div>
-             <div class="box-body table-responsible no-padding">
-                 <table class="table table-hover">
-                     <tr>
--                        <th>Database</th>
--                        <th>Username</th>
--                        <th>Connections From</th>
-+                        <th>Datenbank</th>
-+                        <th>Benutzername</th>
-+                        <th>Verbindungen von</th>
-                         <th>Host</th>
--                        <th>Max Connections</th>
-+                        <th>Max. Verbindungen</th>
-                         <th></th>
-                     </tr>
-                     @foreach($server->databases as $database)
-@@ -44,7 +44,7 @@
-                             @if($database->max_connections != null)
-                                 <td>{{ $database->max_connections }}</td>
-                             @else
--                                <td>Unlimited</td>
-+                                <td>Kein Limit</td>
-                             @endif
-                             <td class="text-center">
-                                 <button data-action="reset-password" data-id="{{ $database->id }}" class="btn btn-xs btn-primary"><i class="fa fa-refresh"></i></button>
-@@ -59,41 +59,41 @@
-     <div class="col-sm-5">
-         <div class="box box-success">
-             <div class="box-header with-border">
--                <h3 class="box-title">Create New Database</h3>
-+                <h3 class="box-title">Datenbank erstellen</h3>
-             </div>
-             <form action="{{ route('admin.servers.view.database', $server->id) }}" method="POST">
-                 <div class="box-body">
-                     <div class="form-group">
--                        <label for="pDatabaseHostId" class="control-label">Database Host</label>
-+                        <label for="pDatabaseHostId" class="control-label">Datenbank Host</label>
-                         <select id="pDatabaseHostId" name="database_host_id" class="form-control">
-                             @foreach($hosts as $host)
-                                 <option value="{{ $host->id }}">{{ $host->name }}</option>
-                             @endforeach
-                         </select>
--                        <p class="text-muted small">Select the host database server that this database should be created on.</p>
-+                        <p class="text-muted small">Wähle den Datenbank Host aus, der für das erstellen von Datenbanken für diesen Server verwendet werden soll.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="pDatabaseName" class="control-label">Database</label>
-+                        <label for="pDatabaseName" class="control-label">Datenbank</label>
-                         <div class="input-group">
-                             <span class="input-group-addon">s{{ $server->id }}_</span>
-                             <input id="pDatabaseName" type="text" name="database" class="form-control" placeholder="database" />
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="pRemote" class="control-label">Connections</label>
-+                        <label for="pRemote" class="control-label">Verbindungen</label>
-                         <input id="pRemote" type="text" name="remote" class="form-control" value="%" />
--                        <p class="text-muted small">This should reflect the IP address that connections are allowed from. Uses standard MySQL notation. If unsure leave as <code>%</code>.</p>
-+                        <p class="text-muted small">Dies sollte die IP-Adresse widerspiegeln, von der aus Verbindungen erlaubt sind. Verwendet die Standard-MySQL-Notation. Wenn du dir nicht sicher bist, lasse es auf <code>%</code>.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="pmax_connections" class="control-label">Concurrent Connections</label>
-+                        <label for="pmax_connections" class="control-label">Gleichzeitige Verbindungen</label>
-                         <input id="pmax_connections" type="text" name="max_connections" class="form-control"/>
--                        <p class="text-muted small">This should reflect the max number of concurrent connections from this user to the database. Leave empty for unlimited.</p>
-+                        <p class="text-muted small">Hier kannst du entscheiden, wie viele aktuelle Sitzungen gleichzeitig in dieser Datenbank laufen dürfen. Wenn du kein Limit setzen möchtest, lasse es leer.</p>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <p class="text-muted small no-margin">A username and password for this database will be randomly generated after form submission.</p>
--                    <input type="submit" class="btn btn-sm btn-success pull-right" value="Create Database" />
-+                    <p class="text-muted small no-margin">Die Zugangsdaten werden zufällig erstellt und orientiert sich nicht an Merkmalen des Servers.</p>
-+                    <input type="submit" class="btn btn-sm btn-success pull-right" value="Datenbank erstellen" />
-                 </div>
-             </form>
-         </div>
-@@ -111,9 +111,10 @@
-         swal({
-             title: '',
-             type: 'warning',
--            text: 'Are you sure that you want to delete this database? There is no going back, all data will immediately be removed.',
-+            text: 'Möchtest du wirklich die Datenbank entfernen? Dieser Vorgang kann nicht rückgängig gemacht werden.',
-             showCancelButton: true,
--            confirmButtonText: 'Delete',
-+            cancelButtonText: 'Abbrechen',
-+            confirmButtonText: 'Ja, entfernen',
-             confirmButtonColor: '#d9534f',
-             closeOnConfirm: false,
-             showLoaderOnConfirm: true,
-@@ -129,8 +130,8 @@
-                 console.error(jqXHR);
-                 swal({
-                     type: 'error',
--                    title: 'Whoops!',
--                    text: (typeof jqXHR.responseJSON.error !== 'undefined') ? jqXHR.responseJSON.error : 'An error occurred while processing this request.'
-+                    title: 'Hups!',
-+                    text: (typeof jqXHR.responseJSON.error !== 'undefined') ? jqXHR.responseJSON.error : 'Beim verarbeiten der Aufgabe ist ein Fehler aufgetreten.'
-                 });
-             });
-         });
-@@ -148,17 +149,17 @@
-             swal({
-                 type: 'success',
-                 title: '',
--                text: 'The password for this database has been reset.',
-+                text: 'Das Passwort für die Datenbank wurde ersetzt.',
-             });
-         }).fail(function(jqXHR, textStatus, errorThrown) {
-             console.error(jqXHR);
--            var error = 'An error occurred while trying to process this request.';
-+            var error = 'Beim verarbeiten der Aufgabe ist ein Fehler aufgetreten.';
-             if (typeof jqXHR.responseJSON !== 'undefined' && typeof jqXHR.responseJSON.error !== 'undefined') {
-                 error = jqXHR.responseJSON.error;
-             }
-             swal({
-                 type: 'error',
--                title: 'Whoops!',
-+                title: 'Hups!',
-                 text: error
-             });
-         }).always(function () {
-diff --git a/resources/views/admin/servers/view/delete.blade.php b/resources/views/admin/servers/view/delete.blade.php
-index c237096..bd0b528 100644
---- a/resources/views/admin/servers/view/delete.blade.php
-+++ b/resources/views/admin/servers/view/delete.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Server — {{ $server->name }}: Delete
-+    Server — {{ $server->name }}: Löschen
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Delete this server from the panel.</small></h1>
-+    <h1>{{ $server->name }}<small>Lösche diesen Server vom System.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-         <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
--        <li class="active">Delete</li>
-+        <li class="active">Löschen</li>
-     </ol>
- @endsection
- 
-@@ -20,16 +20,16 @@
-     <div class="col-md-6">
-         <div class="box">
-             <div class="box-header with-border">
--                <h3 class="box-title">Safely Delete Server</h3>
-+                <h3 class="box-title">Server sicher löschen</h3>
-             </div>
-             <div class="box-body">
--                <p>This action will attempt to delete the server from both the panel and daemon. If either one reports an error the action will be cancelled.</p>
--                <p class="text-danger small">Deleting a server is an irreversible action. <strong>All server data</strong> (including files and users) will be removed from the system.</p>
-+                <p>Dies löscht den Server vollständig vom System, auch Backups werden entfernt. Sollten Probleme auftreten, wird der Vorgang abgebrochen.</p>
-+                <p class="text-danger small">Das löschen des Servers kann nicht rückgängig gemacht werden. Bedenke, das <strong>Alle Serverdaten</strong> gelöscht werden.</p>
-             </div>
-             <div class="box-footer">
-                 <form id="deleteform" action="{{ route('admin.servers.view.delete', $server->id) }}" method="POST">
-                     {!! csrf_field() !!}
--                    <button id="deletebtn" class="btn btn-danger">Safely Delete This Server</button>
-+                    <button id="deletebtn" class="btn btn-danger">Server sicher löschen</button>
-                 </form>
-             </div>
-         </div>
-@@ -37,17 +37,17 @@
-     <div class="col-md-6">
-         <div class="box box-danger">
-             <div class="box-header with-border">
--                <h3 class="box-title">Force Delete Server</h3>
-+                <h3 class="box-title">Server unsicher löschen</h3>
-             </div>
-             <div class="box-body">
--                <p>This action will attempt to delete the server from both the panel and daemon. If the daemon does not respond, or reports an error the deletion will continue.</p>
--                <p class="text-danger small">Deleting a server is an irreversible action. <strong>All server data</strong> (including files and users) will be removed from the system. This method may leave dangling files on your daemon if it reports an error.</p>
-+                <p>Dies löscht den Server vollständig vom System, auch Backups werden entfernt. Sollten Probleme auftreten, wird der Vorgang nicht abgebrochen.</p>
-+                <p class="text-danger small">Das löschen des Servers kann nicht rückgängig gemacht werden. Bedenke, das <strong>Alle Serverdaten</strong> gelöscht werden. Diese Methode kann das System schädigen, wenn viele Verknüpfungen mit dem Server vorhanden und nicht entfernt worden sind.</p>
-             </div>
-             <div class="box-footer">
-                 <form id="forcedeleteform" action="{{ route('admin.servers.view.delete', $server->id) }}" method="POST">
-                     {!! csrf_field() !!}
-                     <input type="hidden" name="force_delete" value="1" />
--                    <button id="forcedeletebtn"" class="btn btn-danger">Forcibly Delete This Server</button>
-+                    <button id="forcedeletebtn" class="btn btn-danger">Server unsicher löschen</button>
-                 </form>
-             </div>
-         </div>
-@@ -63,9 +63,10 @@
-         swal({
-             title: '',
-             type: 'warning',
--            text: 'Are you sure that you want to delete this server? There is no going back, all data will immediately be removed.',
-+            text: 'Sind Sie sicher, dass Sie diesen Server löschen wollen? Es gibt kein Zurück mehr, alle Daten werden sofort gelöscht.',
-             showCancelButton: true,
--            confirmButtonText: 'Delete',
-+            cancelButtonText: 'Abbrechen',
-+            confirmButtonText: 'Löschen',
-             confirmButtonColor: '#d9534f',
-             closeOnConfirm: false
-         }, function () {
-@@ -78,9 +79,10 @@
-         swal({
-             title: '',
-             type: 'warning',
--            text: 'Are you sure that you want to delete this server? There is no going back, all data will immediately be removed.',
-+            text: 'Sind Sie sicher, dass Sie diesen Server löschen wollen? Es gibt kein Zurück mehr, alle Daten werden sofort gelöscht.',
-             showCancelButton: true,
--            confirmButtonText: 'Delete',
-+            cancelButtonText: 'Abbrechen',
-+            confirmButtonText: 'Löschen',
-             confirmButtonColor: '#d9534f',
-             closeOnConfirm: false
-         }, function () {
-diff --git a/resources/views/admin/servers/view/details.blade.php b/resources/views/admin/servers/view/details.blade.php
-index 9b4464b..6aef80d 100644
---- a/resources/views/admin/servers/view/details.blade.php
-+++ b/resources/views/admin/servers/view/details.blade.php
-@@ -5,10 +5,10 @@
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Edit details for this server including owner and container.</small></h1>
-+    <h1>{{ $server->name }}<small>Details zu diesem Server bearbeiten, einschließlich Eigentümer und Container.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}"> Alle Server</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-         <li class="active">Details</li>
-     </ol>
-@@ -20,37 +20,37 @@
-     <div class="col-xs-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">Base Information</h3>
-+                <h3 class="box-title">Allgemeines zum Server</h3>
-             </div>
-             <form action="{{ route('admin.servers.view.details', $server->id) }}" method="POST">
-                 <div class="box-body">
-                     <div class="form-group">
-                         <label for="name" class="control-label">Server Name <span class="field-required"></span></label>
-                         <input type="text" name="name" value="{{ old('name', $server->name) }}" class="form-control" />
--                        <p class="text-muted small">Character limits: <code>a-zA-Z0-9_-</code> and <code>[Space]</code>.</p>
-+                        <p class="text-muted small">Darf verwendet werden: <code>a-zA-Z0-9_-</code> und <code>[Leerzeichen]</code>.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="external_id" class="control-label">External Identifier</label>
-+                        <label for="external_id" class="control-label">Externe Kennung</label>
-                         <input type="text" name="external_id" value="{{ old('external_id', $server->external_id) }}" class="form-control" />
--                        <p class="text-muted small">Leave empty to not assign an external identifier for this server. The external ID should be unique to this server and not be in use by any other servers.</p>
-+                        <p class="text-muted small">Lasse dieses Feld leer, um diesem Server keine externe Kennung zuzuweisen. Die externe ID sollte für diesen Server eindeutig sein und nicht von anderen Servern verwendet werden.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="pUserId" class="control-label">Server Owner <span class="field-required"></span></label>
-+                        <label for="pUserId" class="control-label">Inhaber des Servers <span class="field-required"></span></label>
-                         <select name="owner_id" class="form-control" id="pUserId">
-                             <option value="{{ $server->owner_id }}" selected>{{ $server->user->email }}</option>
-                         </select>
--                        <p class="text-muted small">You can change the owner of this server by changing this field to an email matching another use on this system. If you do this a new daemon security token will be generated automatically.</p>
-+                        <p class="text-muted small">Du kannst hier den Inhaber des Servers ändern Die Voraussetung ist, das der neue Benutzer bereits registriert ist.</p>
-                     </div>
-                     <div class="form-group">
--                        <label for="description" class="control-label">Server Description</label>
-+                        <label for="description" class="control-label">Beschreibung des Servers</label>
-                         <textarea name="description" rows="3" class="form-control">{{ old('description', $server->description) }}</textarea>
--                        <p class="text-muted small">A brief description of this server.</p>
-+                        <p class="text-muted small">Eine Beschreibung, was genau dieser Server ist.</p>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
-                     {!! method_field('PATCH') !!}
--                    <input type="submit" class="btn btn-sm btn-primary" value="Update Details" />
-+                    <input type="submit" class="btn btn-sm btn-primary" value="Änderungen speichern" />
-                 </div>
-             </form>
-         </div>
-diff --git a/resources/views/admin/servers/view/index.blade.php b/resources/views/admin/servers/view/index.blade.php
-index f94c6d4..a134b60 100644
---- a/resources/views/admin/servers/view/index.blade.php
-+++ b/resources/views/admin/servers/view/index.blade.php
-@@ -8,7 +8,7 @@
-     <h1>{{ $server->name }}<small>{{ str_limit($server->description) }}</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
-         <li class="active">{{ $server->name }}</li>
-     </ol>
- @endsection
-@@ -26,13 +26,13 @@
-                     <div class="box-body table-responsive no-padding">
-                         <table class="table table-hover">
-                             <tr>
--                                <td>Internal Identifier</td>
-+                                <td>Interne Kennung</td>
-                                 <td><code>{{ $server->id }}</code></td>
-                             </tr>
-                             <tr>
--                                <td>External Identifier</td>
-+                                <td>Externe Kennung</td>
-                                 @if(is_null($server->external_id))
--                                    <td><span class="label label-default">Not Set</span></td>
-+                                    <td><span class="label label-default">Nicht festgelegt</span></td>
-                                 @else
-                                     <td><code>{{ $server->external_id }}</code></td>
-                                 @endif
-@@ -42,7 +42,7 @@
-                                 <td><code>{{ $server->uuid }}</code></td>
-                             </tr>
-                             <tr>
--                                <td>Current Egg</td>
-+                                <td>Aktuell genutztes Egg</td>
-                                 <td>
-                                     <a href="{{ route('admin.nests.view', $server->nest_id) }}">{{ $server->nest->name }}</a> ::
-                                     <a href="{{ route('admin.nests.egg.view', $server->egg_id) }}">{{ $server->egg->name }}</a>
-@@ -56,65 +56,65 @@
-                                 <td>CPU Limit</td>
-                                 <td>
-                                     @if($server->cpu === 0)
--                                        <code>Unlimited</code>
-+                                        <code>Unbegrenzt</code>
-                                     @else
-                                         <code>{{ $server->cpu }}%</code>
-                                     @endif
-                                 </td>
-                             </tr>
-                             <tr>
--                                <td>CPU Pinning</td>
-+                                <td>CPU-Kernreservierung</td>
-                                 <td>
-                                     @if($server->threads != null)
-                                         <code>{{ $server->threads }}</code>
-                                     @else
--                                        <span class="label label-default">Not Set</span>
-+                                        <span class="label label-default">Nicht festgelegt</span>
-                                     @endif
-                                 </td>
-                             </tr>
-                             <tr>
--                                <td>Memory</td>
-+                                <td>Arbeitsspeicher</td>
-                                 <td>
-                                     @if($server->memory === 0)
--                                        <code>Unlimited</code>
-+                                        <code>Kein Limit festgelegt</code>
-                                     @else
--                                        <code>{{ $server->memory }}MiB</code>
-+                                        <code>{{ $server->memory }}MB</code>
-                                     @endif
-                                     /
-                                     @if($server->swap === 0)
--                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Not Set</code>
-+                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Nicht festgelegt</code>
-                                     @elseif($server->swap === -1)
--                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Unlimited</code>
-+                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Kein Limit</code>
-                                     @else
--                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space"> {{ $server->swap }}MiB</code>
-+                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space"> {{ $server->swap }}MB</code>
-                                     @endif
-                                 </td>
-                             </tr>
-                             <tr>
--                                <td>Disk Space</td>
-+                                <td>Festplattenspeicher</td>
-                                 <td>
-                                     @if($server->disk === 0)
--                                        <code>Unlimited</code>
-+                                        <code>Kein Limit festgelegt</code>
-                                     @else
--                                        <code>{{ $server->disk }}MiB</code>
-+                                        <code>{{ $server->disk }}MB</code>
-                                     @endif
-                                 </td>
-                             </tr>
-                             <tr>
--                                <td>Block IO Weight</td>
-+                                <td>Block IO-Gewicht</td>
-                                 <td><code>{{ $server->io }}</code></td>
-                             </tr>
-                             <tr>
--                                <td>Default Connection</td>
-+                                <td>Primärer Port</td>
-                                 <td><code>{{ $server->allocation->ip }}:{{ $server->allocation->port }}</code></td>
-                             </tr>
-                             <tr>
--                                <td>Connection Alias</td>
-+                                <td>Alias des Ports</td>
-                                 <td>
-                                     @if($server->allocation->alias !== $server->allocation->ip)
-                                         <code>{{ $server->allocation->alias }}:{{ $server->allocation->port }}</code>
-                                     @else
--                                        <span class="label label-default">No Alias Assigned</span>
-+                                        <span class="label label-default">Nicht definiert</span>
-                                     @endif
-                                 </td>
-                             </tr>
-@@ -132,7 +132,7 @@
-                         <div class="col-sm-12">
-                             <div class="small-box bg-yellow">
-                                 <div class="inner">
--                                    <h3 class="no-margin">Suspended</h3>
-+                                    <h3 class="no-margin">Gesperrt</h3>
-                                 </div>
-                             </div>
-                         </div>
-@@ -141,7 +141,7 @@
-                         <div class="col-sm-12">
-                             <div class="small-box {{ (! $server->isInstalled()) ? 'bg-blue' : 'bg-maroon' }}">
-                                 <div class="inner">
--                                    <h3 class="no-margin">{{ (! $server->isInstalled()) ? 'Installing' : 'Install Failed' }}</h3>
-+                                    <h3 class="no-margin">{{ (! $server->isInstalled()) ? 'Wird installiert...' : 'Installation fehlgeschlagen' }}</h3>
-                                 </div>
-                             </div>
-                         </div>
-@@ -150,11 +150,11 @@
-                         <div class="small-box bg-gray">
-                             <div class="inner">
-                                 <h3>{{ str_limit($server->user->username, 16) }}</h3>
--                                <p>Server Owner</p>
-+                                <p>Server Inhaber</p>
-                             </div>
-                             <div class="icon"><i class="fa fa-user"></i></div>
-                             <a href="{{ route('admin.users.view', $server->user->id) }}" class="small-box-footer">
--                                More info <i class="fa fa-arrow-circle-right"></i>
-+                                Mehr Infos <i class="fa fa-arrow-circle-right"></i>
-                             </a>
-                         </div>
-                     </div>
-@@ -166,7 +166,7 @@
-                             </div>
-                             <div class="icon"><i class="fa fa-codepen"></i></div>
-                             <a href="{{ route('admin.nodes.view', $server->node->id) }}" class="small-box-footer">
--                                More info <i class="fa fa-arrow-circle-right"></i>
-+                                Mehr Infos <i class="fa fa-arrow-circle-right"></i>
-                             </a>
-                         </div>
-                     </div>
-diff --git a/resources/views/admin/servers/view/manage.blade.php b/resources/views/admin/servers/view/manage.blade.php
-index e6177a4..a9b3741 100644
---- a/resources/views/admin/servers/view/manage.blade.php
-+++ b/resources/views/admin/servers/view/manage.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Server — {{ $server->name }}: Manage
-+    Server — {{ $server->name }}: Verwaltung
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Additional actions to control this server.</small></h1>
-+    <h1>{{ $server->name }}<small>Kontrollcenter für diesen Server.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
--        <li class="active">Manage</li>
-+        <li class="active">Verwaltung</li>
-     </ol>
- @endsection
- 
-@@ -20,19 +20,19 @@
-         <div class="col-sm-4">
-             <div class="box box-danger">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Reinstall Server</h3>
-+                    <h3 class="box-title">Server neu aufsetzen</h3>
-                 </div>
-                 <div class="box-body">
--                    <p>This will reinstall the server with the assigned service scripts. <strong>Danger!</strong> This could overwrite server data.</p>
-+                    <p>Das wird den Server neu aufsetzen <strong>ACHTUNG!</strong> Dies kann zum Datenverlust führen, mach bitte vorher ein Backup.</p>
-                 </div>
-                 <div class="box-footer">
-                     @if($server->isInstalled())
-                         <form action="{{ route('admin.servers.view.manage.reinstall', $server->id) }}" method="POST">
-                             {!! csrf_field() !!}
--                            <button type="submit" class="btn btn-danger">Reinstall Server</button>
-+                            <button type="submit" class="btn btn-danger">Jetzt neu aufsetzen</button>
-                         </form>
-                     @else
--                        <button class="btn btn-danger disabled">Server Must Install Properly to Reinstall</button>
-+                        <button class="btn btn-danger disabled">Aktuell nicht möglich</button>
-                     @endif
-                 </div>
-             </div>
-@@ -40,15 +40,15 @@
-         <div class="col-sm-4">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Install Status</h3>
-+                    <h3 class="box-title">Installations-Status</h3>
-                 </div>
-                 <div class="box-body">
--                    <p>If you need to change the install status from uninstalled to installed, or vice versa, you may do so with the button below.</p>
-+                    <p>Wenn du gerade etwas am Server veränderst (Plugins updaten oder Ressourcen verändern etc.), kannst du den Installations-Status nutzen. Damit wird der Zugriff zum Server für die Benutzer des Servers gesperrt.</p>
-                 </div>
-                 <div class="box-footer">
-                     <form action="{{ route('admin.servers.view.manage.toggle', $server->id) }}" method="POST">
-                         {!! csrf_field() !!}
--                        <button type="submit" class="btn btn-primary">Toggle Install Status</button>
-+                        <button type="submit" class="btn btn-primary">Status umschalten</button>
-                     </form>
-                 </div>
-             </div>
-@@ -58,16 +58,16 @@
-             <div class="col-sm-4">
-                 <div class="box box-warning">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Suspend Server</h3>
-+                        <h3 class="box-title">Server sperren</h3>
-                     </div>
-                     <div class="box-body">
--                        <p>This will suspend the server, stop any running processes, and immediately block the user from being able to access their files or otherwise manage the server through the panel or API.</p>
-+                        <p>Das sperrt den Server vollständig und fährt ihn sicher herunter. Die Benutzer haben in der Zeit dann keinen Zugang mehr</p>
-                     </div>
-                     <div class="box-footer">
-                         <form action="{{ route('admin.servers.view.manage.suspension', $server->id) }}" method="POST">
-                             {!! csrf_field() !!}
-                             <input type="hidden" name="action" value="suspend" />
--                            <button type="submit" class="btn btn-warning @if(! is_null($server->transfer)) disabled @endif">Suspend Server</button>
-+                            <button type="submit" class="btn btn-warning @if(! is_null($server->transfer)) disabled @endif">Server sperren</button>
-                         </form>
-                     </div>
-                 </div>
-@@ -76,16 +76,16 @@
-             <div class="col-sm-4">
-                 <div class="box box-success">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Unsuspend Server</h3>
-+                        <h3 class="box-title">Sperre aufheben</h3>
-                     </div>
-                     <div class="box-body">
--                        <p>This will unsuspend the server and restore normal user access.</p>
-+                        <p>Hebt die aktive Sperre auf und gibt den Zugang für die Benutzer wieder frei.</p>
-                     </div>
-                     <div class="box-footer">
-                         <form action="{{ route('admin.servers.view.manage.suspension', $server->id) }}" method="POST">
-                             {!! csrf_field() !!}
-                             <input type="hidden" name="action" value="unsuspend" />
--                            <button type="submit" class="btn btn-success">Unsuspend Server</button>
-+                            <button type="submit" class="btn btn-success">Server freigeben</button>
-                         </form>
-                     </div>
-                 </div>
-@@ -96,21 +96,21 @@
-             <div class="col-sm-4">
-                 <div class="box box-success">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Transfer Server</h3>
-+                        <h3 class="box-title">Server übertragen</h3>
-                     </div>
-                     <div class="box-body">
-                         <p>
--                            Transfer this server to another node connected to this panel.
--                            <strong>Warning!</strong> This feature has not been fully tested and may have bugs.
-+                            Überträgt den Server zu einem andern Node, der mit diesem Panel verbunden ist
-+                            <strong>ACHTUNG!</strong> Diese Funktion ist nicht vollständig ausgereift, mache lieber vorher ein Backup.
-                         </p>
-                     </div>
- 
-                     <div class="box-footer">
-                         @if($canTransfer)
--                            <button class="btn btn-success" data-toggle="modal" data-target="#transferServerModal">Transfer Server</button>
-+                            <button class="btn btn-success" data-toggle="modal" data-target="#transferServerModal">Server übertragen</button>
-                         @else
--                            <button class="btn btn-success disabled">Transfer Server</button>
--                            <p style="padding-top: 1rem;">Transferring a server requires more than one node to be configured on your panel.</p>
-+                            <button class="btn btn-success disabled">Server übertragen</button>
-+                            <p style="padding-top: 1rem;">Ein Transfer ist erst ab mindestens 2 Nodes möglich, die über demselben Panel laufen.</p>
-                         @endif
-                     </div>
-                 </div>
-@@ -119,17 +119,17 @@
-             <div class="col-sm-4">
-                 <div class="box box-success">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Transfer Server</h3>
-+                        <h3 class="box-title">Server übertragen</h3>
-                     </div>
-                     <div class="box-body">
-                         <p>
--                            This server is currently being transferred to another node.
--                            Transfer was initiated at <strong>{{ $server->transfer->created_at }}</strong>
-+                            Dieser Server wird gerade auf einen anderen Node übertragen
-+                            Transfer wurde gestartet am <strong>{{ $server->transfer->created_at }}</strong>
-                         </p>
-                     </div>
- 
-                     <div class="box-footer">
--                        <button class="btn btn-success disabled">Transfer Server</button>
-+                        <button class="btn btn-success disabled">Transfer läuft...</button>
-                     </div>
-                 </div>
-             </div>
-@@ -142,7 +142,7 @@
-                 <form action="{{ route('admin.servers.view.manage.transfer', $server->id) }}" method="POST">
-                     <div class="modal-header">
-                         <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
--                        <h4 class="modal-title">Transfer Server</h4>
-+                        <h4 class="modal-title">Server übertragen</h4>
-                     </div>
- 
-                     <div class="modal-body">
-@@ -164,27 +164,27 @@
-                                         </optgroup>
-                                     @endforeach
-                                 </select>
--                                <p class="small text-muted no-margin">The node which this server will be transferred to.</p>
-+                                <p class="small text-muted no-margin">Der Node, auf dem du diesen Server übertragen möchtest.</p>
-                             </div>
- 
-                             <div class="form-group col-md-12">
--                                <label for="pAllocation">Default Allocation</label>
-+                                <label for="pAllocation">Standard Port</label>
-                                 <select name="allocation_id" id="pAllocation" class="form-control"></select>
--                                <p class="small text-muted no-margin">The main allocation that will be assigned to this server.</p>
-+                                <p class="small text-muted no-margin">Der primäre Port für diesen Server.</p>
-                             </div>
- 
-                             <div class="form-group col-md-12">
--                                <label for="pAllocationAdditional">Additional Allocation(s)</label>
-+                                <label for="pAllocationAdditional">Weitere Ports</label>
-                                 <select name="allocation_additional[]" id="pAllocationAdditional" class="form-control" multiple></select>
--                                <p class="small text-muted no-margin">Additional allocations to assign to this server on creation.</p>
-+                                <p class="small text-muted no-margin">Ports, die ebenfalls reserviert werden sollen.</p>
-                             </div>
-                         </div>
-                     </div>
- 
-                     <div class="modal-footer">
-                         {!! csrf_field() !!}
--                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
--                        <button type="submit" class="btn btn-success btn-sm">Confirm</button>
-+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
-+                        <button type="submit" class="btn btn-success btn-sm">Starten</button>
-                     </div>
-                 </form>
-             </div>
-diff --git a/resources/views/admin/servers/view/mounts.blade.php b/resources/views/admin/servers/view/mounts.blade.php
-index 36ca98d..b5d6e53 100644
---- a/resources/views/admin/servers/view/mounts.blade.php
-+++ b/resources/views/admin/servers/view/mounts.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Server — {{ $server->name }}: Mounts
-+    Server — {{ $server->name }}: Integrationen
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Manage server mounts.</small></h1>
-+    <h1>{{ $server->name }}<small>Verwalte die Integrationen des Servers.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
--        <li class="active">Mounts</li>
-+        <li class="active">Integrationen</li>
-     </ol>
- @endsection
- 
-@@ -21,7 +21,7 @@
-         <div class="col-sm-12">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Available Mounts</h3>
-+                    <h3 class="box-title">Verfügbare Integrationen</h3>
-                 </div>
- 
-                 <div class="box-body table-responsible no-padding">
-@@ -29,8 +29,8 @@
-                         <tr>
-                             <th>ID</th>
-                             <th>Name</th>
--                            <th>Source</th>
--                            <th>Target</th>
-+                            <th>Quelle</th>
-+                            <th>Ziel</th>
-                             <th>Status</th>
-                             <th></th>
-                         </tr>
-@@ -44,7 +44,7 @@
- 
-                                 @if (! in_array($mount->id, $server->mounts->pluck('id')->toArray()))
-                                     <td class="col-sm-2 middle">
--                                        <span class="label label-primary">Unmounted</span>
-+                                        <span class="label label-primary">Nicht integriert</span>
-                                     </td>
- 
-                                     <td class="col-sm-1 middle">
-@@ -56,7 +56,7 @@
-                                     </td>
-                                 @else
-                                     <td class="col-sm-2 middle">
--                                        <span class="label label-success">Mounted</span>
-+                                        <span class="label label-success">Integriert</span>
-                                     </td>
- 
-                                     <td class="col-sm-1 middle">
-diff --git a/resources/views/admin/servers/view/startup.blade.php b/resources/views/admin/servers/view/startup.blade.php
-index 0533029..95ec53a 100644
---- a/resources/views/admin/servers/view/startup.blade.php
-+++ b/resources/views/admin/servers/view/startup.blade.php
-@@ -1,16 +1,16 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Server — {{ $server->name }}: Startup
-+    Server — {{ $server->name }}: Startbefehl
- @endsection
- 
- @section('content-header')
--    <h1>{{ $server->name }}<small>Control startup command as well as variables.</small></h1>
-+    <h1>{{ $server->name }}<small>Verwalte hier den Startbefehl des Servers.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
-         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
--        <li class="active">Startup</li>
-+        <li class="active">Startbefehl</li>
-     </ol>
- @endsection
- 
-@@ -21,20 +21,20 @@
-         <div class="col-xs-12">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Startup Command Modification</h3>
-+                    <h3 class="box-title">Startbefehl des Servers</h3>
-                 </div>
-                 <div class="box-body">
--                    <label for="pStartup" class="form-label">Startup Command</label>
-+                    <label for="pStartup" class="form-label">Aktueller Befehl</label>
-                     <input id="pStartup" name="startup" class="form-control" type="text" value="{{ old('startup', $server->startup) }}" />
--                    <p class="small text-muted">Edit your server's startup command here. The following variables are available by default: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, and <code>@{{SERVER_PORT}}</code>.</p>
-+                    <p class="small text-muted">Hier kannst du den Startbefehl des Servers bearbeiten. Du kannst folgende Variablen nutzen, die Angaben direkt korrigieren, wenn du etwas davon veränderst: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, and <code>@{{SERVER_PORT}}</code>.</p>
-                 </div>
-                 <div class="box-body">
--                    <label for="pDefaultStartupCommand" class="form-label">Default Service Start Command</label>
-+                    <label for="pDefaultStartupCommand" class="form-label">Vordefinierter Startbefehl</label>
-                     <input id="pDefaultStartupCommand" class="form-control" type="text" readonly />
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <button type="submit" class="btn btn-primary btn-sm pull-right">Save Modifications</button>
-+                    <button type="submit" class="btn btn-primary btn-sm pull-right">Änderungen speichern</button>
-                 </div>
-             </div>
-         </div>
-@@ -43,16 +43,16 @@
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Service Configuration</h3>
-+                    <h3 class="box-title">Konfiguration</h3>
-                 </div>
-                 <div class="box-body row">
-                     <div class="col-xs-12">
-                         <p class="small text-danger">
--                            Changing any of the below values will result in the server processing a re-install command. The server will be stopped and will then proceed.
--                            If you would like the service scripts to not run, ensure the box is checked at the bottom.
-+                            Das Ändern eines der folgenden Werte führt dazu, dass der Server einen Neuinstallationsbefehl ausführt. Der Server wird gestoppt und fährt dann fort.
-+                            Wenn du möchtest, dass die Dienstskripte nicht ausgeführt werden, stelle sicher, dass das Kontrollkästchen unten aktiviert ist.
-                         </p>
-                         <p class="small text-danger">
--                            <strong>This is a destructive operation in many cases. This server will be stopped immediately in order for this action to proceed.</strong>
-+                            <strong>Änderungen werden hier nicht empfohlen, wenn bereits ein aktives Projekt auf diesem Server läuft.</strong>
-                         </p>
-                     </div>
-                     <div class="form-group col-xs-12">
-@@ -66,32 +66,32 @@
-                                 >{{ $nest->name }}</option>
-                             @endforeach
-                         </select>
--                        <p class="small text-muted no-margin">Select the Nest that this server will be grouped into.</p>
-+                        <p class="small text-muted no-margin">Wähle den Nest aus, der sich mit deinem Projekt identifiziert.</p>
-                     </div>
-                     <div class="form-group col-xs-12">
-                         <label for="pEggId">Egg</label>
-                         <select name="egg_id" id="pEggId" class="form-control"></select>
--                        <p class="small text-muted no-margin">Select the Egg that will provide processing data for this server.</p>
-+                        <p class="small text-muted no-margin">Wähle den passenden Egg für deinen Server aus.</p>
-                     </div>
-                     <div class="form-group col-xs-12">
-                         <div class="checkbox checkbox-primary no-margin-bottom">
-                             <input id="pSkipScripting" name="skip_scripts" type="checkbox" value="1" @if($server->skip_scripts) checked @endif />
--                            <label for="pSkipScripting" class="strong">Skip Egg Install Script</label>
-+                            <label for="pSkipScripting" class="strong">Egg Installationsskript überspringen</label>
-                         </div>
--                        <p class="small text-muted no-margin">If the selected Egg has an install script attached to it, the script will run during install. If you would like to skip this step, check this box.</p>
-+                        <p class="small text-muted no-margin">Wenn an das ausgewählte Egg ein Installationsskript angehängt ist, wird das Skript während der Installation ausgeführt. Wenn du diesen Schritt überspringen möchtest, aktiviere dieses Kontrollkästchen.</p>
-                     </div>
-                 </div>
-             </div>
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Docker Image Configuration</h3>
-+                    <h3 class="box-title">Docker Image Optionen</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
-                         <label for="pDockerImage">Image</label>
-                         <select id="pDockerImage" name="docker_image" class="form-control"></select>
--                        <input id="pDockerImageCustom" name="custom_docker_image" value="{{ old('custom_docker_image') }}" class="form-control" placeholder="Or enter a custom image..." style="margin-top:1rem"/>
--                        <p class="small text-muted no-margin">This is the Docker image that will be used to run this server. Select an image from the dropdown or enter a custom image in the text field above.</p>
-+                        <input id="pDockerImageCustom" name="custom_docker_image" value="{{ old('custom_docker_image') }}" class="form-control" placeholder="Oder wähle ein anderes Image..." style="margin-top:1rem"/>
-+                        <p class="small text-muted no-margin">Dies ist das aktuelle Docker Image, was für diesen Server verwendet wird. Du kannst im Menü auch ein anderes Image verwenden.</p>
-                     </div>
-                 </div>
-             </div>
-@@ -144,7 +144,7 @@
-             $('#appendVariablesTo').html('');
-             $.each(_.get(objectChain, 'variables', []), function (i, item) {
-                 var setValue = _.get(Pterodactyl.server_variables, item.env_variable, item.default_value);
--                var isRequired = (item.required === 1) ? '<span class="label label-danger">Required</span> ' : '';
-+                var isRequired = (item.required === 1) ? '<span class="label label-danger">Benötigt</span> ' : '';
-                 var dataAppend = ' \
-                     <div class="col-xs-12"> \
-                         <div class="box"> \
-@@ -156,8 +156,8 @@
-                                 <p class="no-margin small text-muted">' + item.description + '</p> \
-                             </div> \
-                             <div class="box-footer"> \
--                                <p class="no-margin text-muted small"><strong>Startup Command Variable:</strong> <code>' + item.env_variable + '</code></p> \
--                                <p class="no-margin text-muted small"><strong>Input Rules:</strong> <code>' + item.rules + '</code></p> \
-+                                <p class="no-margin text-muted small"><strong>Startbefehl Variable:</strong> <code>' + item.env_variable + '</code></p> \
-+                                <p class="no-margin text-muted small"><strong>Regeln:</strong> <code>' + item.rules + '</code></p> \
-                             </div> \
-                         </div> \
-                     </div>';
-diff --git a/resources/views/admin/settings/advanced.blade.php b/resources/views/admin/settings/advanced.blade.php
-index 0f2056c..9f3e4c3 100644
---- a/resources/views/admin/settings/advanced.blade.php
-+++ b/resources/views/admin/settings/advanced.blade.php
-@@ -2,14 +2,14 @@
- @include('partials/admin.settings.nav', ['activeTab' => 'advanced'])
- 
- @section('title')
--    Advanced Settings
-+    Erweitert
- @endsection
- 
- @section('content-header')
--    <h1>Advanced Settings<small>Configure advanced settings for Pterodactyl.</small></h1>
-+    <h1>Erweiterte Einstellungen<small>Stelle hier erweiterte Optionen für Pterodactyl ein.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Settings</li>
-+        <li class="active">Einstellungen</li>
-     </ol>
- @endsection
- 
-@@ -28,10 +28,10 @@
-                                 <label class="control-label">Status</label>
-                                 <div>
-                                     <select class="form-control" name="recaptcha:enabled">
--                                        <option value="true">Enabled</option>
--                                        <option value="false" @if(old('recaptcha:enabled', config('recaptcha.enabled')) == '0') selected @endif>Disabled</option>
-+                                        <option value="true">Aktiviert</option>
-+                                        <option value="false" @if(old('recaptcha:enabled', config('recaptcha.enabled')) == '0') selected @endif>Deaktiviert</option>
-                                     </select>
--                                    <p class="text-muted small">If enabled, login forms and password reset forms will do a silent captcha check and display a visible captcha if needed.</p>
-+                                    <p class="text-muted small">Wenn es aktiv ist, dann wird man beim Anmelden nach einem reCAPTCHA gefragt.</p>
-                                 </div>
-                             </div>
-                             <div class="form-group col-md-4">
-@@ -44,7 +44,7 @@
-                                 <label class="control-label">Secret Key</label>
-                                 <div>
-                                     <input type="text" required class="form-control" name="recaptcha:secret_key" value="{{ old('recaptcha:secret_key', config('recaptcha.secret_key')) }}">
--                                    <p class="text-muted small">Used for communication between your site and Google. Be sure to keep it a secret.</p>
-+                                    <p class="text-muted small">Wird für die Kommunikation zwischen den Server und Google verwendet.</p>
-                                 </div>
-                             </div>
-                         </div>
-@@ -52,7 +52,7 @@
-                             <div class="row">
-                                 <div class="col-xs-12">
-                                     <div class="alert alert-warning no-margin">
--                                        You are currently using reCAPTCHA keys that were shipped with this Panel. For improved security it is recommended to <a href="https://www.google.com/recaptcha/admin">generate new invisible reCAPTCHA keys</a> that tied specifically to your website.
-+                                        Du nutzt aktuell einen Key, der bei der Installation hinzugefügt wurde. Erstelle bitte <a href="https://www.google.com/recaptcha/admin">hier</a> einen eigenen Key, damit du die volle Kontrolle dazu bekommst.
-                                     </div>
-                                 </div>
-                             </div>
-@@ -61,22 +61,22 @@
-                 </div>
-                 <div class="box">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">HTTP Connections</h3>
-+                        <h3 class="box-title">HTTP Verbindungen</h3>
-                     </div>
-                     <div class="box-body">
-                         <div class="row">
-                             <div class="form-group col-md-6">
--                                <label class="control-label">Connection Timeout</label>
-+                                <label class="control-label">Timeout bei Verbindung</label>
-                                 <div>
-                                     <input type="number" required class="form-control" name="pterodactyl:guzzle:connect_timeout" value="{{ old('pterodactyl:guzzle:connect_timeout', config('pterodactyl.guzzle.connect_timeout')) }}">
--                                    <p class="text-muted small">The amount of time in seconds to wait for a connection to be opened before throwing an error.</p>
-+                                    <p class="text-muted small">Die Anzahl in Sekunden, wie lange versucht werden soll eine Verbindung herzustellen, bis es erfolglos ist.</p>
-                                 </div>
-                             </div>
-                             <div class="form-group col-md-6">
--                                <label class="control-label">Request Timeout</label>
-+                                <label class="control-label">Timeout bei Anfragen</label>
-                                 <div>
-                                     <input type="number" required class="form-control" name="pterodactyl:guzzle:timeout" value="{{ old('pterodactyl:guzzle:timeout', config('pterodactyl.guzzle.timeout')) }}">
--                                    <p class="text-muted small">The amount of time in seconds to wait for a request to be completed before throwing an error.</p>
-+                                    <p class="text-muted small">Die Anzahl in Sekunden, wie lange versucht werden soll eine Anfrage zu senden, bis es erfolglos ist..</p>
-                                 </div>
-                             </div>
-                         </div>
-@@ -84,7 +84,7 @@
-                 </div>
-                 <div class="box">
-                     <div class="box-header with-border">
--                        <h3 class="box-title">Automatic Allocation Creation</h3>
-+                        <h3 class="box-title">Automatische Portfreigabe</h3>
-                     </div>
-                     <div class="box-body">
-                         <div class="row">
-@@ -92,24 +92,24 @@
-                                 <label class="control-label">Status</label>
-                                 <div>
-                                     <select class="form-control" name="pterodactyl:client_features:allocations:enabled">
--                                        <option value="false">Disabled</option>
--                                        <option value="true" @if(old('pterodactyl:client_features:allocations:enabled', config('pterodactyl.client_features.allocations.enabled'))) selected @endif>Enabled</option>
-+                                        <option value="false">Deaktiviert</option>
-+                                        <option value="true" @if(old('pterodactyl:client_features:allocations:enabled', config('pterodactyl.client_features.allocations.enabled'))) selected @endif>Aktiviert</option>
-                                     </select>
--                                    <p class="text-muted small">If enabled users will have the option to automatically create new allocations for their server via the frontend.</p>
-+                                    <p class="text-muted small">Wenn es aktiv ist, dann können Benutzer eigene Portfreigaben machen. Jeder Port kann nur einmal zugeteilt werden.</p>
-                                 </div>
-                             </div>
-                             <div class="form-group col-md-4">
--                                <label class="control-label">Starting Port</label>
-+                                <label class="control-label">Startport</label>
-                                 <div>
-                                     <input type="number" class="form-control" name="pterodactyl:client_features:allocations:range_start" value="{{ old('pterodactyl:client_features:allocations:range_start', config('pterodactyl.client_features.allocations.range_start')) }}">
--                                    <p class="text-muted small">The starting port in the range that can be automatically allocated.</p>
-+                                    <p class="text-muted small">Der Startport kann automatisch festgelegt werden.</p>
-                                 </div>
-                             </div>
-                             <div class="form-group col-md-4">
--                                <label class="control-label">Ending Port</label>
-+                                <label class="control-label">Endport</label>
-                                 <div>
-                                     <input type="number" class="form-control" name="pterodactyl:client_features:allocations:range_end" value="{{ old('pterodactyl:client_features:allocations:range_end', config('pterodactyl.client_features.allocations.range_end')) }}">
--                                    <p class="text-muted small">The ending port in the range that can be automatically allocated.</p>
-+                                    <p class="text-muted small">Der Endport kann automatisch festgelegt werden.</p>
-                                 </div>
-                             </div>
-                         </div>
-@@ -118,7 +118,7 @@
-                 <div class="box box-primary">
-                     <div class="box-footer">
-                         {{ csrf_field() }}
--                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Save</button>
-+                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Speichern</button>
-                     </div>
-                 </div>
-             </form>
-diff --git a/resources/views/admin/settings/index.blade.php b/resources/views/admin/settings/index.blade.php
-index 489646d..71663d0 100644
---- a/resources/views/admin/settings/index.blade.php
-+++ b/resources/views/admin/settings/index.blade.php
-@@ -2,14 +2,14 @@
- @include('partials/admin.settings.nav', ['activeTab' => 'basic'])
- 
- @section('title')
--    Settings
-+    Einstellungen
- @endsection
- 
- @section('content-header')
--    <h1>Panel Settings<small>Configure Pterodactyl to your liking.</small></h1>
-+    <h1>Panel Einstellungen<small>Stelle hier das Panel von Pterodactyl ein</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Settings</li>
-+        <li class="active">Einstellungen</li>
-     </ol>
- @endsection
- 
-@@ -19,54 +19,54 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Panel Settings</h3>
-+                    <h3 class="box-title">Panel-Einstellungen</h3>
-                 </div>
-                 <form action="{{ route('admin.settings') }}" method="POST">
-                     <div class="box-body">
-                         <div class="row">
-                             <div class="form-group col-md-4">
--                                <label class="control-label">Company Name</label>
-+                                <label class="control-label">Name deines Unternehmens</label>
-                                 <div>
-                                     <input type="text" class="form-control" name="app:name" value="{{ old('app:name', config('app.name')) }}" />
--                                    <p class="text-muted"><small>This is the name that is used throughout the panel and in emails sent to clients.</small></p>
-+                                    <p class="text-muted"><small>Dieser Name wird überall im Panel angezeigt. Das spiegelt den Betreiber dieses Systems.</small></p>
-                                 </div>
-                             </div>
-                             <div class="form-group col-md-4">
--                                <label class="control-label">Require 2-Factor Authentication</label>
-+                                <label class="control-label">2-Faktor Authentifizierung erzwingen</label>
-                                 <div>
-                                     <div class="btn-group" data-toggle="buttons">
-                                         @php
-                                             $level = old('pterodactyl:auth:2fa_required', config('pterodactyl.auth.2fa_required'));
-                                         @endphp
-                                         <label class="btn btn-primary @if ($level == 0) active @endif">
--                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="0" @if ($level == 0) checked @endif> Not Required
-+                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="0" @if ($level == 0) checked @endif> Nicht notwendig
-                                         </label>
-                                         <label class="btn btn-primary @if ($level == 1) active @endif">
--                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="1" @if ($level == 1) checked @endif> Admin Only
-+                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="1" @if ($level == 1) checked @endif> Nur Admins
-                                         </label>
-                                         <label class="btn btn-primary @if ($level == 2) active @endif">
--                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="2" @if ($level == 2) checked @endif> All Users
-+                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="2" @if ($level == 2) checked @endif> Für alle Pflicht
-                                         </label>
-                                     </div>
--                                    <p class="text-muted"><small>If enabled, any account falling into the selected grouping will be required to have 2-Factor authentication enabled to use the Panel.</small></p>
-+                                    <p class="text-muted"><small>Wenn diese Option aktiviert ist, muss für jedes Konto, das in die ausgewählte Gruppierung fällt, die 2-Faktor-Authentifizierung aktiviert sein, um das Panel verwenden zu können.</small></p>
-                                 </div>
-                             </div>
-                             <div class="form-group col-md-4">
--                                <label class="control-label">Default Language</label>
-+                                <label class="control-label">Sprache</label>
-                                 <div>
-                                     <select name="app:locale" class="form-control">
-                                         @foreach($languages as $key => $value)
--                                            <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>{{ $value }}</option>
-+                                            <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>Deutsch</option>
-                                         @endforeach
-                                     </select>
--                                    <p class="text-muted"><small>The default language to use when rendering UI components.</small></p>
-+                                    <p class="text-muted"><small>Die beim Rendern von UI-Komponenten zu verwendende Standardsprache.</small></p>
-                                 </div>
-                             </div>
-                         </div>
-                     </div>
-                     <div class="box-footer">
-                         {!! csrf_field() !!}
--                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Save</button>
-+                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Änderungen speichern</button>
-                     </div>
-                 </form>
-             </div>
-diff --git a/resources/views/admin/settings/mail.blade.php b/resources/views/admin/settings/mail.blade.php
-index 9e99acd..f2cf2b6 100644
---- a/resources/views/admin/settings/mail.blade.php
-+++ b/resources/views/admin/settings/mail.blade.php
-@@ -2,14 +2,14 @@
- @include('partials/admin.settings.nav', ['activeTab' => 'mail'])
- 
- @section('title')
--    Mail Settings
-+    Mail-Versand
- @endsection
- 
- @section('content-header')
--    <h1>Mail Settings<small>Configure how Pterodactyl should handle sending emails.</small></h1>
-+    <h1>Mail-Versand<small>Richte hier dein Mail-System ein</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Settings</li>
-+        <li class="active">Einstellungen</li>
-     </ol>
- @endsection
- 
-@@ -19,14 +19,14 @@
-         <div class="col-xs-12">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Email Settings</h3>
-+                    <h3 class="box-title">Mail-Versand</h3>
-                 </div>
-                 @if($disabled)
-                     <div class="box-body">
-                         <div class="row">
-                             <div class="col-xs-12">
-                                 <div class="alert alert-info no-margin-bottom">
--                                    This interface is limited to instances using SMTP as the mail driver. Please either use <code>php artisan p:environment:mail</code> command to update your email settings, or set <code>MAIL_DRIVER=smtp</code> in your environment file.
-+                                    Diese Schnittstelle ist auf Instanzen beschränkt, die SMTP als E-Mail-Treiber verwenden. Bitte verwende entweder den Befehl <code>php artisan p:environment:mail</code>, um Ihre E-Mail-Einstellungen zu aktualisieren, oder setze diese Zeile: <code>MAIL_DRIVER=smtp</code> in der Umgebungsdatei ein.
-                                 </div>
-                             </div>
-                         </div>
-@@ -39,59 +39,59 @@
-                                     <label class="control-label">SMTP Host</label>
-                                     <div>
-                                         <input required type="text" class="form-control" name="mail:mailers:smtp:host" value="{{ old('mail:mailers:smtp:host', config('mail.mailers.smtp.host')) }}" />
--                                        <p class="text-muted small">Enter the SMTP server address that mail should be sent through.</p>
-+                                        <p class="text-muted small">Gebe die Adresse des SMTP-Servers ein, über den E-Mails gesendet werden sollen.</p>
-                                     </div>
-                                 </div>
-                                 <div class="form-group col-md-2">
-                                     <label class="control-label">SMTP Port</label>
-                                     <div>
-                                         <input required type="number" class="form-control" name="mail:mailers:smtp:port" value="{{ old('mail:mailers:smtp:port', config('mail.mailers.smtp.port')) }}" />
--                                        <p class="text-muted small">Enter the SMTP server port that mail should be sent through.</p>
-+                                        <p class="text-muted small">Gebe den Port des SMTP-Servers ein, über den E-Mails gesendet werden sollen.</p>
-                                     </div>
-                                 </div>
-                                 <div class="form-group col-md-4">
--                                    <label class="control-label">Encryption</label>
-+                                    <label class="control-label">Verschlüsselung</label>
-                                     <div>
-                                         @php
-                                             $encryption = old('mail:mailers:smtp:encryption', config('mail.mailers.smtp.encryption'));
-                                         @endphp
-                                         <select name="mail:mailers:smtp:encryption" class="form-control">
--                                            <option value="" @if($encryption === '') selected @endif>None</option>
-+                                            <option value="" @if($encryption === '') selected @endif>Keine</option>
-                                             <option value="tls" @if($encryption === 'tls') selected @endif>Transport Layer Security (TLS)</option>
-                                             <option value="ssl" @if($encryption === 'ssl') selected @endif>Secure Sockets Layer (SSL)</option>
-                                         </select>
--                                        <p class="text-muted small">Select the type of encryption to use when sending mail.</p>
-+                                        <p class="text-muted small">Wähle eine der Verschlüsselungen aus, mit der E-Mails versendet werden sollen.</p>
-                                     </div>
-                                 </div>
-                                 <div class="form-group col-md-6">
--                                    <label class="control-label">Username <span class="field-optional"></span></label>
-+                                    <label class="control-label">Benutzername <span class="field-optional"></span></label>
-                                     <div>
-                                         <input type="text" class="form-control" name="mail:mailers:smtp:username" value="{{ old('mail:mailers:smtp:username', config('mail.mailers.smtp.username')) }}" />
--                                        <p class="text-muted small">The username to use when connecting to the SMTP server.</p>
-+                                        <p class="text-muted small">Der Benutzername, womit du dich mit dem SMTP-Server verbindest.</p>
-                                     </div>
-                                 </div>
-                                 <div class="form-group col-md-6">
--                                    <label class="control-label">Password <span class="field-optional"></span></label>
-+                                    <label class="control-label">Passwort <span class="field-optional"></span></label>
-                                     <div>
-                                         <input type="password" class="form-control" name="mail:mailers:smtp:password"/>
--                                        <p class="text-muted small">The password to use in conjunction with the SMTP username. Leave blank to continue using the existing password. To set the password to an empty value enter <code>!e</code> into the field.</p>
-+                                        <p class="text-muted small">Das Kennwort, das zusammen mit dem SMTP-Benutzernamen verwendet werden soll. Lasse das Feld leer, um das vorhandene Passwort weiter zu verwenden. Um das Passwort auf einen leeren Wert zu setzen, gebe <code>!e</code> in das Feld ein.</p>
-                                     </div>
-                                 </div>
-                             </div>
-                             <div class="row">
-                                 <hr />
-                                 <div class="form-group col-md-6">
--                                    <label class="control-label">Mail From</label>
-+                                    <label class="control-label">Absender</label>
-                                     <div>
-                                         <input required type="email" class="form-control" name="mail:from:address" value="{{ old('mail:from:address', config('mail.from.address')) }}" />
--                                        <p class="text-muted small">Enter an email address that all outgoing emails will originate from.</p>
-+                                        <p class="text-muted small">Gib eine E-Mail Adresse ein, über die alle E-Mails versendet werden sollen..</p>
-                                     </div>
-                                 </div>
-                                 <div class="form-group col-md-6">
--                                    <label class="control-label">Mail From Name <span class="field-optional"></span></label>
-+                                    <label class="control-label">Name des Absenders <span class="field-optional"></span></label>
-                                     <div>
-                                         <input type="text" class="form-control" name="mail:from:name" value="{{ old('mail:from:name', config('mail.from.name')) }}" />
--                                        <p class="text-muted small">The name that emails should appear to come from.</p>
-+                                        <p class="text-muted small">Gib hier deine Firma ein, damit der Empfänger weiß, von wem die E-Mail kommt.</p>
-                                     </div>
-                                 </div>
-                             </div>
-@@ -99,8 +99,8 @@
-                         <div class="box-footer">
-                             {{ csrf_field() }}
-                             <div class="pull-right">
--                                <button type="button" id="testButton" class="btn btn-sm btn-success">Test</button>
--                                <button type="button" id="saveButton" class="btn btn-sm btn-primary">Save</button>
-+                                <button type="button" id="testButton" class="btn btn-sm btn-success">Testen</button>
-+                                <button type="button" id="saveButton" class="btn btn-sm btn-primary">Speichern</button>
-                             </div>
-                         </div>
-                     </form>
-@@ -137,10 +137,11 @@
-         function testSettings() {
-             swal({
-                 type: 'info',
--                title: 'Test Mail Settings',
--                text: 'Click "Test" to begin the test.',
-+                title: 'Mail-System testen',
-+                text: 'Klicke auf START, um den Test zu starten.',
-                 showCancelButton: true,
--                confirmButtonText: 'Test',
-+                cancelButtonText: 'Abbrechen',
-+                confirmButtonText: 'START',
-                 closeOnConfirm: false,
-                 showLoaderOnConfirm: true
-             }, function () {
-@@ -152,8 +153,8 @@
-                     showErrorDialog(jqXHR, 'test');
-                 }).done(function () {
-                     swal({
--                        title: 'Success',
--                        text: 'The test message was sent successfully.',
-+                        title: 'Es hat funktioniert! :)',
-+                        text: 'Die Test E-Mail wurde erfolgreich versendet.',
-                         type: 'success'
-                     });
-                 });
-@@ -180,8 +181,8 @@
-             }
- 
-             swal({
--                title: 'Whoops!',
--                text: 'An error occurred while attempting to ' + verb + ' mail settings: ' + errorText,
-+                title: 'Ohje. Noch nicht ganz!',
-+                text: 'Es trat ein Fehler bei ' + verb + ' auf.\n\n Mail-Einstellungen: ' + errorText,
-                 type: 'error'
-             });
-         }
-@@ -191,8 +192,8 @@
-             $('#saveButton').on('click', function () {
-                 saveSettings().done(function () {
-                     swal({
--                        title: 'Success',
--                        text: 'Mail settings have been updated successfully and the queue worker was restarted to apply these changes.',
-+                        title: 'Gespeichert und Online!',
-+                        text: 'Die Einstellungen des Mail-Systems wurden übernommen und stehen jetzt allen Benutzern zur Verfügung, um E-Mails erhalten zu können.',
-                         type: 'success'
-                     });
-                 });
-diff --git a/resources/views/admin/users/index.blade.php b/resources/views/admin/users/index.blade.php
-index 0c8e906..4404005 100644
---- a/resources/views/admin/users/index.blade.php
-+++ b/resources/views/admin/users/index.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    List Users
-+    Alle Benutzer
- @endsection
- 
- @section('content-header')
--    <h1>Users<small>All registered users on the system.</small></h1>
-+    <h1>Alle Benutzer<small>Alle registrierten Benutzer im System.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li class="active">Users</li>
-+        <li class="active">Benutzer</li>
-     </ol>
- @endsection
- 
-@@ -17,14 +17,14 @@
-     <div class="col-xs-12">
-         <div class="box box-primary">
-             <div class="box-header with-border">
--                <h3 class="box-title">User List</h3>
-+                <h3 class="box-title">Benutzer-Übersicht</h3>
-                 <div class="box-tools search01">
-                     <form action="{{ route('admin.users') }}" method="GET">
-                         <div class="input-group input-group-sm">
--                            <input type="text" name="filter[email]" class="form-control pull-right" value="{{ request()->input('filter.email') }}" placeholder="Search">
-+                            <input type="text" name="filter[email]" class="form-control pull-right" value="{{ request()->input('filter.email') }}" placeholder="Benutzer suchen">
-                             <div class="input-group-btn">
-                                 <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
--                                <a href="{{ route('admin.users.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Create New</button></a>
-+                                <a href="{{ route('admin.users.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Hinzufügen</button></a>
-                             </div>
-                         </div>
-                     </form>
-@@ -35,12 +35,12 @@
-                     <thead>
-                         <tr>
-                             <th>ID</th>
--                            <th>Email</th>
--                            <th>Client Name</th>
--                            <th>Username</th>
--                            <th class="text-center">2FA</th>
--                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user is marked as the owner of.">Servers Owned</span></th>
--                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user can access because they are marked as a subuser.">Can Access</span></th>
-+                            <th>E-Mail</th>
-+                            <th>Name</th>
-+                            <th>Benutzername</th>
-+                            <th class="text-center">2-FA</th>
-+                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user is marked as the owner of.">Server im Besitz</span></th>
-+                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user can access because they are marked as a subuser.">Aktiver Zugang</span></th>
-                             <th></th>
-                         </tr>
-                     </thead>
-diff --git a/resources/views/admin/users/new.blade.php b/resources/views/admin/users/new.blade.php
-index 2ff5016..1621a25 100644
---- a/resources/views/admin/users/new.blade.php
-+++ b/resources/views/admin/users/new.blade.php
-@@ -1,15 +1,15 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Create User
-+    Benutzer erstellen
- @endsection
- 
- @section('content-header')
--    <h1>Create User<small>Add a new user to the system.</small></h1>
-+    <h1>Benutzer erstellen<small>Füge einen Benutzer ins System hinzu.</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.users') }}">Users</a></li>
--        <li class="active">Create</li>
-+        <li><a href="{{ route('admin.users') }}">Alle Benutzer</a></li>
-+        <li class="active">Erstellen</li>
-     </ol>
- @endsection
- 
-@@ -19,55 +19,55 @@
-         <div class="col-md-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Identity</h3>
-+                    <h3 class="box-title">Identität</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
--                        <label for="email" class="control-label">Email</label>
-+                        <label for="email" class="control-label">E-Mail</label>
-                         <div>
-                             <input type="text" autocomplete="off" name="email" value="{{ old('email') }}" class="form-control" />
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="username" class="control-label">Username</label>
-+                        <label for="username" class="control-label">Benutzername</label>
-                         <div>
-                             <input type="text" autocomplete="off" name="username" value="{{ old('username') }}" class="form-control" />
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="name_first" class="control-label">Client First Name</label>
-+                        <label for="name_first" class="control-label">Vorname</label>
-                         <div>
-                             <input type="text" autocomplete="off" name="name_first" value="{{ old('name_first') }}" class="form-control" />
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="name_last" class="control-label">Client Last Name</label>
-+                        <label for="name_last" class="control-label">Nachname</label>
-                         <div>
-                             <input type="text" autocomplete="off" name="name_last" value="{{ old('name_last') }}" class="form-control" />
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Default Language</label>
-+                        <label class="control-label">Sprache</label>
-                         <div>
-                             <select name="language" class="form-control">
-                                 @foreach($languages as $key => $value)
--                                    <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>{{ $value }}</option>
-+                                    <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>Deutsch</option>
-                                 @endforeach
-                             </select>
--                            <p class="text-muted"><small>The default language to use when rendering the Panel for this user.</small></p>
-+                            <p class="text-muted"><small>Die Sprache, mit der dieser Benutzer arbeiten wird.</small></p>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
--                    <input type="submit" value="Create User" class="btn btn-success btn-sm">
-+                    <input type="submit" value="Benutzer erstellen" class="btn btn-success btn-sm">
-                 </div>
-             </div>
-         </div>
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Permissions</h3>
-+                    <h3 class="box-title">Berechtigungen</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group col-md-12">
-@@ -77,7 +77,7 @@
-                                 <option value="0">@lang('strings.no')</option>
-                                 <option value="1">@lang('strings.yes')</option>
-                             </select>
--                            <p class="text-muted"><small>Setting this to 'Yes' gives a user full administrative access.</small></p>
-+                            <p class="text-muted"><small>Hier kannst du entscheiden, ob der Benutzer einen Zugang zu diesem Admin-Panel bekommen darf. WENN DU DIR UNSICHER BIST, DANN LASS ES SO WIE ES IST!</small></p>
-                         </div>
-                     </div>
-                 </div>
-@@ -86,15 +86,15 @@
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Password</h3>
-+                    <h3 class="box-title">Passwort festlegen</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="alert alert-info">
--                        <p>Providing a user password is optional. New user emails prompt users to create a password the first time they login. If a password is provided here you will need to find a different method of providing it to the user.</p>
-+                        <p>Wenn du kein Passwort angibst, dann wird eins automatisch generiert. Die Voraussetzung hierbei ist, dass das E-Mail System betriebsbereit ist.</p>
-                     </div>
-                     <div id="gen_pass" class=" alert alert-success" style="display:none;margin-bottom: 10px;"></div>
-                     <div class="form-group">
--                        <label for="pass" class="control-label">Password</label>
-+                        <label for="pass" class="control-label">Passwort</label>
-                         <div>
-                             <input type="password" name="password" class="form-control" />
-                         </div>
-@@ -117,7 +117,7 @@
-                     'X-CSRF-TOKEN': '{{ csrf_token() }}'
-                },
-                 success: function(data) {
--                    $("#gen_pass").html('<strong>Generated Password:</strong> ' + data).slideDown();
-+                    $("#gen_pass").html('<strong>Generiertes Passwort:</strong> ' + data).slideDown();
-                     $('input[name="password"], input[name="password_confirmation"]').val(data);
-                     return false;
-                 }
-diff --git a/resources/views/admin/users/view.blade.php b/resources/views/admin/users/view.blade.php
-index f042d89..922d5c8 100644
---- a/resources/views/admin/users/view.blade.php
-+++ b/resources/views/admin/users/view.blade.php
-@@ -1,14 +1,14 @@
- @extends('layouts.admin')
- 
- @section('title')
--    Manager User: {{ $user->username }}
-+    Nutzer verwalten: {{ $user->username }}
- @endsection
- 
- @section('content-header')
-     <h1>{{ $user->name_first }} {{ $user->name_last}}<small>{{ $user->username }}</small></h1>
-     <ol class="breadcrumb">
-         <li><a href="{{ route('admin.index') }}">Admin</a></li>
--        <li><a href="{{ route('admin.users') }}">Users</a></li>
-+        <li><a href="{{ route('admin.users') }}">Alle Benutzer</a></li>
-         <li class="active">{{ $user->username }}</li>
-     </ol>
- @endsection
-@@ -19,64 +19,64 @@
-         <div class="col-md-6">
-             <div class="box box-primary">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Identity</h3>
-+                    <h3 class="box-title">Identität</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
--                        <label for="email" class="control-label">Email</label>
-+                        <label for="email" class="control-label">E-Mail</label>
-                         <div>
-                             <input type="email" name="email" value="{{ $user->email }}" class="form-control form-autocomplete-stop">
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="registered" class="control-label">Username</label>
-+                        <label for="registered" class="control-label">Benutzername</label>
-                         <div>
-                             <input type="text" name="username" value="{{ $user->username }}" class="form-control form-autocomplete-stop">
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="registered" class="control-label">Client First Name</label>
-+                        <label for="registered" class="control-label">Vorname</label>
-                         <div>
-                             <input type="text" name="name_first" value="{{ $user->name_first }}" class="form-control form-autocomplete-stop">
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label for="registered" class="control-label">Client Last Name</label>
-+                        <label for="registered" class="control-label">Nachname</label>
-                         <div>
-                             <input type="text" name="name_last" value="{{ $user->name_last }}" class="form-control form-autocomplete-stop">
-                         </div>
-                     </div>
-                     <div class="form-group">
--                        <label class="control-label">Default Language</label>
-+                        <label class="control-label">Sprache</label>
-                         <div>
-                             <select name="language" class="form-control">
-                                 @foreach($languages as $key => $value)
--                                    <option value="{{ $key }}" @if($user->language === $key) selected @endif>{{ $value }}</option>
-+                                    <option value="{{ $key }}" @if($user->language === $key) selected @endif>Deutsch</option>
-                                 @endforeach
-                             </select>
--                            <p class="text-muted"><small>The default language to use when rendering the Panel for this user.</small></p>
-+                            <p class="text-muted"><small>Die Sprache, womit der Benutzer auf dem Panel arbeitet.</small></p>
-                         </div>
-                     </div>
-                 </div>
-                 <div class="box-footer">
-                     {!! csrf_field() !!}
-                     {!! method_field('PATCH') !!}
--                    <input type="submit" value="Update User" class="btn btn-primary btn-sm">
-+                    <input type="submit" value="Änderungen speichern" class="btn btn-primary btn-sm">
-                 </div>
-             </div>
-         </div>
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Password</h3>
-+                    <h3 class="box-title">Passwort-Verwaltung</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="alert alert-success" style="display:none;margin-bottom:10px;" id="gen_pass"></div>
-                     <div class="form-group no-margin-bottom">
--                        <label for="password" class="control-label">Password <span class="field-optional"></span></label>
-+                        <label for="password" class="control-label">Passwort <span class="field-optional"></span></label>
-                         <div>
-                             <input type="password" id="password" name="password" class="form-control form-autocomplete-stop">
--                            <p class="text-muted small">Leave blank to keep this user's password the same. User will not receive any notification if password is changed.</p>
-+                            <p class="text-muted small">Lass es leer, wenn es nicht geändert werden soll.</p>
-                         </div>
-                     </div>
-                 </div>
-@@ -85,7 +85,7 @@
-         <div class="col-md-6">
-             <div class="box">
-                 <div class="box-header with-border">
--                    <h3 class="box-title">Permissions</h3>
-+                    <h3 class="box-title">Berechtigungen</h3>
-                 </div>
-                 <div class="box-body">
-                     <div class="form-group">
-@@ -95,7 +95,7 @@
-                                 <option value="0">@lang('strings.no')</option>
-                                 <option value="1" {{ $user->root_admin ? 'selected="selected"' : '' }}>@lang('strings.yes')</option>
-                             </select>
--                            <p class="text-muted"><small>Setting this to 'Yes' gives a user full administrative access.</small></p>
-+                            <p class="text-muted"><small>Achte bitte darauf, wem du Zugriff auf dieses Panel gibst. Mit dieser Berechtigung kann dann der Benutzer dasselbe tun, wie du.</small></p>
-                         </div>
-                     </div>
-                 </div>
-@@ -105,16 +105,16 @@
-     <div class="col-xs-12">
-         <div class="box box-danger">
-             <div class="box-header with-border">
--                <h3 class="box-title">Delete User</h3>
-+                <h3 class="box-title">Benutzer entfernen</h3>
-             </div>
-             <div class="box-body">
--                <p class="no-margin">There must be no servers associated with this account in order for it to be deleted.</p>
-+                <p class="no-margin">Es dürfen keine Server mit deren Account verbunden sein, wenn du den Account entfernen möchtest.</p>
-             </div>
-             <div class="box-footer">
-                 <form action="{{ route('admin.users.view', $user->id) }}" method="POST">
-                     {!! csrf_field() !!}
-                     {!! method_field('DELETE') !!}
--                    <input id="delete" type="submit" class="btn btn-sm btn-danger pull-right" {{ $user->servers->count() < 1 ?: 'disabled' }} value="Delete User" />
-+                    <input id="delete" type="submit" class="btn btn-sm btn-danger pull-right" {{ $user->servers->count() < 1 ?: 'disabled' }} value="Benutzer entfernen" />
-                 </form>
-             </div>
-         </div>
-diff --git a/resources/views/layouts/admin.blade.php b/resources/views/layouts/admin.blade.php
-index 1543ddf..d5a2545 100644
---- a/resources/views/layouts/admin.blade.php
-+++ b/resources/views/layouts/admin.blade.php
-@@ -43,7 +43,7 @@
-                 </a>
-                 <nav class="navbar navbar-static-top">
-                     <a href="#" class="sidebar-toggle" data-toggle="push-menu" role="button">
--                        <span class="sr-only">Toggle navigation</span>
-+                        <span class="sr-only">Navigation umschalten</span>
-                         <span class="icon-bar"></span>
-                         <span class="icon-bar"></span>
-                         <span class="icon-bar"></span>
-@@ -57,10 +57,10 @@
-                                 </a>
-                             </li>
-                             <li>
--                                <li><a href="{{ route('index') }}" data-toggle="tooltip" data-placement="bottom" title="Exit Admin Control"><i class="fa fa-server"></i></a></li>
-+                                <li><a href="{{ route('index') }}" data-toggle="tooltip" data-placement="bottom" title="Admin-Panel verlassen"><i class="fa fa-server"></i></a></li>
-                             </li>
-                             <li>
--                                <li><a href="{{ route('auth.logout') }}" id="logoutButton" data-toggle="tooltip" data-placement="bottom" title="Logout"><i class="fa fa-sign-out"></i></a></li>
-+                                <li><a href="{{ route('auth.logout') }}" id="logoutButton" data-toggle="tooltip" data-placement="bottom" title="Abmelden"><i class="fa fa-sign-out"></i></a></li>
-                             </li>
-                         </ul>
-                     </div>
-@@ -69,52 +69,53 @@
-             <aside class="main-sidebar">
-                 <section class="sidebar">
-                     <ul class="sidebar-menu">
--                        <li class="header">BASIC ADMINISTRATION</li>
-+
-+                        <li class="header">ALLGEMEIN</li>
-                         <li class="{{ Route::currentRouteName() !== 'admin.index' ?: 'active' }}">
-                             <a href="{{ route('admin.index') }}">
--                                <i class="fa fa-home"></i> <span>Overview</span>
-+                                <i class="fa fa-home"></i> <span>Übersicht</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.settings') ?: 'active' }}">
-                             <a href="{{ route('admin.settings')}}">
--                                <i class="fa fa-wrench"></i> <span>Settings</span>
-+                                <i class="fa fa-wrench"></i> <span>Einstellungen</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.api') ?: 'active' }}">
-                             <a href="{{ route('admin.api.index')}}">
--                                <i class="fa fa-gamepad"></i> <span>Application API</span>
-+                                <i class="fa fa-gamepad"></i> <span>Anwendungs-API</span>
-                             </a>
-                         </li>
-                         <li class="header">MANAGEMENT</li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.databases') ?: 'active' }}">
-                             <a href="{{ route('admin.databases') }}">
--                                <i class="fa fa-database"></i> <span>Databases</span>
-+                                <i class="fa fa-database"></i> <span>Alle Datenbanken</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.locations') ?: 'active' }}">
-                             <a href="{{ route('admin.locations') }}">
--                                <i class="fa fa-globe"></i> <span>Locations</span>
-+                                <i class="fa fa-globe"></i> <span>Alle Standorte</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.nodes') ?: 'active' }}">
-                             <a href="{{ route('admin.nodes') }}">
--                                <i class="fa fa-sitemap"></i> <span>Nodes</span>
-+                                <i class="fa fa-sitemap"></i> <span>Alle Nodes</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.servers') ?: 'active' }}">
-                             <a href="{{ route('admin.servers') }}">
--                                <i class="fa fa-server"></i> <span>Servers</span>
-+                                <i class="fa fa-server"></i> <span>Alle Server</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.users') ?: 'active' }}">
-                             <a href="{{ route('admin.users') }}">
--                                <i class="fa fa-users"></i> <span>Users</span>
-+                                <i class="fa fa-users"></i> <span>Alle Benutzer</span>
-                             </a>
-                         </li>
-                         <li class="header">SERVICE MANAGEMENT</li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.mounts') ?: 'active' }}">
-                             <a href="{{ route('admin.mounts') }}">
--                                <i class="fa fa-magic"></i> <span>Mounts</span>
-+                                <i class="fa fa-magic"></i> <span>Verknüpfungen</span>
-                             </a>
-                         </li>
-                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.nests') ?: 'active' }}">
-@@ -134,7 +135,7 @@
-                         <div class="col-xs-12">
-                             @if (count($errors) > 0)
-                                 <div class="alert alert-danger">
--                                    There was an error validating the data provided.<br><br>
-+                                    Beim Validieren der bereitgestellten Daten ist ein Fehler aufgetreten.<br><br>
-                                     <ul>
-                                         @foreach ($errors->all() as $error)
-                                             <li>{{ $error }}</li>
-@@ -159,7 +160,7 @@
-                     <strong><i class="fa fa-fw {{ $appIsGit ? 'fa-git-square' : 'fa-code-fork' }}"></i></strong> {{ $appVersion }}<br />
-                     <strong><i class="fa fa-fw fa-clock-o"></i></strong> {{ round(microtime(true) - LARAVEL_START, 3) }}s
-                 </div>
--                Copyright &copy; 2015 - {{ date('Y') }} <a href="https://pterodactyl.io/">Pterodactyl Software</a>.
-+                Copyright &copy; 2015 - {{ date('Y') }} <a href="https://pterodactyl.io/">Pterodactyl Software</a> | Übersetzt von <a href="https://germandactyl.de/">Pavl21</a>.
-             </footer>
-         </div>
-         @section('footer-scripts')
-@@ -183,12 +184,13 @@
- 
-                         var that = this;
-                         swal({
--                            title: 'Do you want to log out?',
-+                            title: 'Möchtest du dich wirklich abmelden?',
-                             type: 'warning',
-                             showCancelButton: true,
-+                            cancelButtonText: 'Abbrechen',
-                             confirmButtonColor: '#d9534f',
-                             cancelButtonColor: '#d33',
--                            confirmButtonText: 'Log out'
-+                            confirmButtonText: 'Jetzt abmelden'
-                         }, function () {
-                              $.ajax({
-                                 type: 'POST',
-diff --git a/resources/views/partials/admin/settings/nav.blade.php b/resources/views/partials/admin/settings/nav.blade.php
-index 9f1ace7..2f4093d 100644
---- a/resources/views/partials/admin/settings/nav.blade.php
-+++ b/resources/views/partials/admin/settings/nav.blade.php
-@@ -6,9 +6,9 @@
-         <div class="col-xs-12">
-             <div class="nav-tabs-custom nav-tabs-floating">
-                 <ul class="nav nav-tabs">
--                    <li @if($activeTab === 'basic')class="active"@endif><a href="{{ route('admin.settings') }}">General</a></li>
--                    <li @if($activeTab === 'mail')class="active"@endif><a href="{{ route('admin.settings.mail') }}">Mail</a></li>
--                    <li @if($activeTab === 'advanced')class="active"@endif><a href="{{ route('admin.settings.advanced') }}">Advanced</a></li>
-+                    <li @if($activeTab === 'basic')class="active"@endif><a href="{{ route('admin.settings') }}">Allgemein</a></li>
-+                    <li @if($activeTab === 'mail')class="active"@endif><a href="{{ route('admin.settings.mail') }}">Mail-Versand</a></li>
-+                    <li @if($activeTab === 'advanced')class="active"@endif><a href="{{ route('admin.settings.advanced') }}">Erweitert</a></li>
-                 </ul>
-             </div>
-         </div>
-diff --git a/resources/views/partials/admin/settings/notice.blade.php b/resources/views/partials/admin/settings/notice.blade.php
-index 2dc0bc1..16f8890 100644
---- a/resources/views/partials/admin/settings/notice.blade.php
-+++ b/resources/views/partials/admin/settings/notice.blade.php
-@@ -3,7 +3,7 @@
-         <div class="row">
-             <div class="col-xs-12">
-                 <div class="alert alert-danger">
--                    Your Panel is currently configured to read settings from the environment only. You will need to set <code>APP_ENVIRONMENT_ONLY=false</code> in your environment file in order to load settings dynamically.
-+                    Dein Panel ist derzeit so konfiguriert, dass es nur Einstellungen aus der Umgebung liest. Du musst <code>APP_ENVIRONMENT_ONLY=false</code> in Ihrer Umgebungsdatei angeben, um Einstellungen dynamisch zu laden.
-                 </div>
-             </div>
-         </div>
--- 
-2.39.1
-
diff --git a/public/themes/pterodactyl/css/pterodactyl.css b/public/themes/pterodactyl/css/pterodactyl.css
index 8e3580f..c18f538 100644
--- a/public/themes/pterodactyl/css/pterodactyl.css
+++ b/public/themes/pterodactyl/css/pterodactyl.css
@@ -462,7 +462,7 @@ label.control-label > span {
 }
 
 label.control-label > span.field-required:before {
-    content: "required";
+    content: "Pflichtfeld";
     color: #dd4b39;
 }
 
diff --git a/public/themes/pterodactyl/js/admin/new-server.js b/public/themes/pterodactyl/js/admin/new-server.js
index db138cb..3e72f36 100644
--- a/public/themes/pterodactyl/js/admin/new-server.js
+++ b/public/themes/pterodactyl/js/admin/new-server.js
@@ -19,27 +19,27 @@
 // SOFTWARE.
 $(document).ready(function() {
     $('#pNestId').select2({
-        placeholder: 'Select a Nest',
+        placeholder: 'Wähle ein Nest aus',
     }).change();
 
     $('#pEggId').select2({
-        placeholder: 'Select a Nest Egg',
+        placeholder: 'Wähle ein Nest Egg aus',
     });
 
     $('#pPackId').select2({
-        placeholder: 'Select a Service Pack',
+        placeholder: 'Wähle ein Service Pack aus',
     });
 
     $('#pNodeId').select2({
-        placeholder: 'Select a Node',
+        placeholder: 'Wähle eine Node aus',
     }).change();
 
     $('#pAllocation').select2({
-        placeholder: 'Select a Default Allocation',
+        placeholder: 'Wähle eine Standardzuordnung aus',
     });
 
     $('#pAllocationAdditional').select2({
-        placeholder: 'Select Additional Allocations',
+        placeholder: 'Füge weitere Ports hinzu',
     });
 });
 
@@ -59,7 +59,7 @@ $('#pNodeId').on('change', function () {
         if (v.id == currentNode) {
             $('#pAllocation').html('').select2({
                 data: v.allocations,
-                placeholder: 'Select a Default Allocation',
+                placeholder: 'Wähle eine Standardzuordnung aus',
             });
 
             updateAdditionalAllocations();
@@ -114,7 +114,7 @@ $('#pEggId').on('change', function (event) {
     $.each(_.get(objectChain, 'variables', []), function (i, item) {
         variableIds[item.env_variable] = 'var_ref_' + item.id;
 
-        let isRequired = (item.required === 1) ? '<span class="label label-danger">Required</span> ' : '';
+        let isRequired = (item.required === 1) ? '<span class="label label-danger">Pflichtfeld</span> ' : '';
         let dataAppend = ' \
             <div class="form-group col-sm-6"> \
                 <label for="var_ref_' + item.id + '" class="control-label">' + isRequired + item.name + '</label> \
@@ -154,7 +154,7 @@ function updateAdditionalAllocations() {
 
             $('#pAllocationAdditional').html('').select2({
                 data: allocations,
-                placeholder: 'Select Additional Allocations',
+                placeholder: 'Füge weitere Ports hinzu',
             });
         }
     });
diff --git a/public/themes/pterodactyl/js/admin/server/transfer.js b/public/themes/pterodactyl/js/admin/server/transfer.js
index 5c2664a..2145670 100644
--- a/public/themes/pterodactyl/js/admin/server/transfer.js
+++ b/public/themes/pterodactyl/js/admin/server/transfer.js
@@ -1,14 +1,14 @@
 $(document).ready(function () {
     $('#pNodeId').select2({
-        placeholder: 'Select a Node',
+        placeholder: 'Node auswählen',
     }).change();
 
     $('#pAllocation').select2({
-        placeholder: 'Select a Default Allocation',
+        placeholder: 'Wähle eine Standardzuordnung aus',
     });
 
     $('#pAllocationAdditional').select2({
-        placeholder: 'Select Additional Allocations',
+        placeholder: 'Füge weitere Ports hinzu',
     });
 });
 
@@ -19,7 +19,7 @@ $('#pNodeId').on('change', function () {
         if (v.id == currentNode) {
             $('#pAllocation').html('').select2({
                 data: v.allocations,
-                placeholder: 'Select a Default Allocation',
+                placeholder: 'Wähle eine Standardzuordnung aus',
             });
 
             updateAdditionalAllocations();
@@ -49,7 +49,7 @@ function updateAdditionalAllocations() {
 
             $('#pAllocationAdditional').html('').select2({
                 data: allocations,
-                placeholder: 'Select Additional Allocations',
+                placeholder: 'Füge weitere Ports hinzu',
             });
         }
     });
diff --git a/resources/lang/en/activity.php b/resources/lang/en/activity.php
index 501a1dc..fcf75b2 100644
--- a/resources/lang/en/activity.php
+++ b/resources/lang/en/activity.php
@@ -8,123 +8,123 @@
  */
 return [
     'auth' => [
-        'fail' => 'Failed log in',
-        'success' => 'Logged in',
-        'password-reset' => 'Password reset',
-        'reset-password' => 'Requested password reset',
-        'checkpoint' => 'Two-factor authentication requested',
-        'recovery-token' => 'Used two-factor recovery token',
-        'token' => 'Solved two-factor challenge',
-        'ip-blocked' => 'Blocked request from unlisted IP address for :identifier',
+        'fail' => 'Hat das Passwort falsch eingegeben (Möglicherweise unbefugt)',
+        'success' => 'Hat sich erfolgreich angemeldet',
+        'password-reset' => 'Passwort zurückgesetzt',
+        'reset-password' => 'Passwort-Anfrage gesendet',
+        'checkpoint' => '2-FA Anfrage gesendet',
+        'recovery-token' => 'Hat ein Backup-Code für 2-FA verwendet',
+        'token' => '2-FA wurde genehmigt',
+        'ip-blocked' => 'Anfrage von nicht gelisteter IP-Adresse geblockt: :identifier',
         'sftp' => [
-            'fail' => 'Failed SFTP log in',
+            'fail' => 'SFTP Login fehlgeschlagen',
         ],
     ],
     'user' => [
         'account' => [
-            'email-changed' => 'Changed email from :old to :new',
-            'password-changed' => 'Changed password',
+            'email-changed' => 'E-Mail von :old zu :new geändert',
+            'password-changed' => 'Passwort geändert',
         ],
         'api-key' => [
-            'create' => 'Created new API key :identifier',
-            'delete' => 'Deleted API key :identifier',
+            'create' => 'Neuen API-Schlüssel :identifier erstellt',
+            'delete' => 'Hat API-Schlüssel :identifier gelöscht',
         ],
         'ssh-key' => [
-            'create' => 'Added SSH key :fingerprint to account',
-            'delete' => 'Removed SSH key :fingerprint from account',
+            'create' => 'Hat SSH-Schlüssel :fingerprint hinzugefügt',
+            'delete' => 'Hat SSH-Schlüssel :fingerprint entfernt',
         ],
         'two-factor' => [
-            'create' => 'Enabled two-factor auth',
-            'delete' => 'Disabled two-factor auth',
+            'create' => 'Hat 2-FA aktiviert',
+            'delete' => 'Hat 2-FA deaktiviert',
         ],
     ],
     'server' => [
-        'reinstall' => 'Reinstalled server',
+        'reinstall' => 'Hat den Server neu aufgesetzt',
         'console' => [
-            'command' => 'Executed ":command" on the server',
+            'command' => 'Hat den Befehl ":command" ausgeführt',
         ],
         'power' => [
-            'start' => 'Started the server',
-            'stop' => 'Stopped the server',
-            'restart' => 'Restarted the server',
-            'kill' => 'Killed the server process',
+            'start' => 'Hat den Server gestartet',
+            'stop' => 'Hat den Server gestoppt',
+            'restart' => 'Hat den Server neu gestartet',
+            'kill' => 'Hat den Server gekillt',
         ],
         'backup' => [
-            'download' => 'Downloaded the :name backup',
-            'delete' => 'Deleted the :name backup',
-            'restore' => 'Restored the :name backup (deleted files: :truncate)',
-            'restore-complete' => 'Completed restoration of the :name backup',
-            'restore-failed' => 'Failed to complete restoration of the :name backup',
-            'start' => 'Started a new backup :name',
-            'complete' => 'Marked the :name backup as complete',
-            'fail' => 'Marked the :name backup as failed',
-            'lock' => 'Locked the :name backup',
-            'unlock' => 'Unlocked the :name backup',
+            'download' => 'Hat das Backup :name heruntergeladen',
+            'delete' => 'Hat das Backup :name gelöscht',
+            'restore' => 'Hat das Backup :name wiederhergestellt (Gelöschte Dateien: :truncate)',
+            'restore-complete' => 'Komplette Wiederherstellung des Backups :name',
+            'restore-failed' => 'Fehlgeschlagene Wiederherstellung des Backups :name',
+            'start' => 'Erstellt das Backup :name',
+            'complete' => 'Markiert das Backup :name als erfolgreich',
+            'fail' => 'Markiert das Backup :name als fehlgeschlagen',
+            'lock' => 'Hat das Backup :name gesperrt',
+            'unlock' => 'Hat das Backup :name entsperrt',
         ],
         'database' => [
-            'create' => 'Created new database :name',
-            'rotate-password' => 'Password rotated for database :name',
-            'delete' => 'Deleted database :name',
+            'create' => 'Hat die Datenbank :name erstellt',
+            'rotate-password' => 'Hat ein neues Passwort für die Datenbank :name erstellt',
+            'delete' => 'Hat die Datenbank :name gelöscht',
         ],
         'file' => [
-            'compress_one' => 'Compressed :directory:file',
-            'compress_other' => 'Compressed :count files in :directory',
-            'read' => 'Viewed the contents of :file',
-            'copy' => 'Created a copy of :file',
-            'create-directory' => 'Created directory :directory:name',
-            'decompress' => 'Decompressed :files in :directory',
-            'delete_one' => 'Deleted :directory:files.0',
-            'delete_other' => 'Deleted :count files in :directory',
-            'download' => 'Downloaded :file',
-            'pull' => 'Downloaded a remote file from :url to :directory',
-            'rename_one' => 'Renamed :directory:files.0.from to :directory:files.0.to',
-            'rename_other' => 'Renamed :count files in :directory',
-            'write' => 'Wrote new content to :file',
-            'upload' => 'Began a file upload',
-            'uploaded' => 'Uploaded :directory:file',
+            'compress_one' => 'Hat :directory:file komprimiert',
+            'compress_other' => 'Hat :count Dateien in :directory komprimiert',
+            'read' => 'Hat sich :file angeschaut',
+            'copy' => 'Hat eine Kopie von :file erstellt',
+            'create-directory' => 'Hat den Ordner :directory:name erstellt',
+            'decompress' => 'Hat :files Dateien in :directory entpackt',
+            'delete_one' => 'Hat die Datei :directory:files.0 gelöscht',
+            'delete_other' => 'Hat :count Dateien in :directory gelöscht',
+            'download' => 'Hat :file heruntergeladen',
+            'pull' => 'Hat die Remote-Datei :url in :directory abgelegt',
+            'rename_one' => 'Hat den Namen :directory:files.0.from in :directory:files.0.to geändert',
+            'rename_other' => 'Hat :count Dateien in :directory geändert',
+            'write' => 'Hat etwas in :file verändert',
+            'upload' => 'Hat einen Upload gestartet',
+            'uploaded' => 'Hat :directory:file hochgeladen',
         ],
         'sftp' => [
-            'denied' => 'Blocked SFTP access due to permissions',
-            'create_one' => 'Created :files.0',
-            'create_other' => 'Created :count new files',
-            'write_one' => 'Modified the contents of :files.0',
-            'write_other' => 'Modified the contents of :count files',
-            'delete_one' => 'Deleted :files.0',
-            'delete_other' => 'Deleted :count files',
-            'create-directory_one' => 'Created the :files.0 directory',
-            'create-directory_other' => 'Created :count directories',
-            'rename_one' => 'Renamed :files.0.from to :files.0.to',
-            'rename_other' => 'Renamed or moved :count files',
+            'denied' => 'Hat den SFTP-Zugang aufgrund ungenügender Rechte verweigert',
+            'create_one' => 'Hat die Datei :files.0 erstellt',
+            'create_other' => 'Hat :count neue Dateien erstellt',
+            'write_one' => 'Hat etwas in :files.0 verändert',
+            'write_other' => 'Hat etwas in :count Dateien verändert',
+            'delete_one' => 'Hat :files.0 gelöscht',
+            'delete_other' => 'Hat :count Dateien gelöscht',
+            'create-directory_one' => 'Hat den Ordner :files.0 erstellt',
+            'create-directory_other' => 'Hat :count Ordner erstellt',
+            'rename_one' => 'Hat die Datei :files.0.from in :files.0.to unbenannt',
+            'rename_other' => 'Hat :count Dateien verschoben oder umbenannt',
         ],
         'allocation' => [
-            'create' => 'Added :allocation to the server',
-            'notes' => 'Updated the notes for :allocation from ":old" to ":new"',
-            'primary' => 'Set :allocation as the primary server allocation',
-            'delete' => 'Deleted the :allocation allocation',
+            'create' => 'Hat den Port :allocation zu diesem Server zugeteilt',
+            'notes' => 'Hat den Vermerk vom Port :allocation von ":old" zu ":new" geändert',
+            'primary' => 'Hat den Port :allocation als primären Server-Zugang festgelegt',
+            'delete' => 'Hat den Port :allocation entfernt',
         ],
         'schedule' => [
-            'create' => 'Created the :name schedule',
-            'update' => 'Updated the :name schedule',
-            'execute' => 'Manually executed the :name schedule',
-            'delete' => 'Deleted the :name schedule',
+            'create' => 'Hat den Zeitplan :name erstellt',
+            'update' => 'Hat den Zeitplan :name verändert',
+            'execute' => 'Hat den Zeitplan :name manuell gestartet',
+            'delete' => 'Hat den Zeitplan :name gelöscht',
         ],
         'task' => [
-            'create' => 'Created a new ":action" task for the :name schedule',
-            'update' => 'Updated the ":action" task for the :name schedule',
-            'delete' => 'Deleted a task for the :name schedule',
+            'create' => 'Hat die Aufgabe ":action" in :name erstellt',
+            'update' => 'Hat die Aufgabe ":action" in :name verändert',
+            'delete' => 'Hat eine Aufgabe in im Zeitplan :name gelöscht',
         ],
         'settings' => [
-            'rename' => 'Renamed the server from :old to :new',
-            'description' => 'Changed the server description from :old to :new',
+            'rename' => 'Hat den Server von :old zu :new umbenannt',
+            'description' => 'Hat die Beschreibung des Servers von :old zu :new unbenannt',
         ],
         'startup' => [
-            'edit' => 'Changed the :variable variable from ":old" to ":new"',
-            'image' => 'Updated the Docker Image for the server from :old to :new',
+            'edit' => 'Hat die Variable :variable von ":old" zu ":new" verändert',
+            'image' => 'Hat das Docker Image von :old zu :new aktualisiert',
         ],
         'subuser' => [
-            'create' => 'Added :email as a subuser',
-            'update' => 'Updated the subuser permissions for :email',
-            'delete' => 'Removed :email as a subuser',
+            'create' => 'Hat :email als Benutzer hinzugefügt',
+            'update' => 'Hat die Berechtigungen von :email verändert',
+            'delete' => 'Hat :email vom Server entfernt',
         ],
     ],
 ];
diff --git a/resources/lang/en/admin/nests.php b/resources/lang/en/admin/nests.php
index 2a6f377..88d4e29 100644
--- a/resources/lang/en/admin/nests.php
+++ b/resources/lang/en/admin/nests.php
@@ -2,25 +2,25 @@
 
 return [
     'notices' => [
-        'created' => 'A new nest, :name, has been successfully created.',
-        'deleted' => 'Successfully deleted the requested nest from the Panel.',
-        'updated' => 'Successfully updated the nest configuration options.',
+        'created' => 'Das neue Nest :name wurde erfolgreich erstellt.',
+        'deleted' => 'Das Nest wurde erfolgreich vom Panel entfernt.',
+        'updated' => 'Die Konfigurationsoptionen des Nests wurden erfolgreich aktualisiert.',
     ],
     'eggs' => [
         'notices' => [
-            'imported' => 'Successfully imported this Egg and its associated variables.',
-            'updated_via_import' => 'This Egg has been updated using the file provided.',
-            'deleted' => 'Successfully deleted the requested egg from the Panel.',
-            'updated' => 'Egg configuration has been updated successfully.',
-            'script_updated' => 'Egg install script has been updated and will run whenever servers are installed.',
-            'egg_created' => 'A new egg was laid successfully. You will need to restart any running daemons to apply this new egg.',
+            'imported' => 'Dieses Egg wurde erfolgreich wurde erfolgreich mit seinen zugehörigen Variablen importiert.',
+            'updated_via_import' => 'Dieses Egg wurde erfolgreich mit der angegebenen Datei aktualisiert.',
+            'deleted' => 'Das angefragte Egg wurde Erfolgreich vom Panel entfernt.',
+            'updated' => 'Die Egg-Konfiguration wurde erfolgreich aktualisiert.',
+            'script_updated' => 'Das Egg-Installationsskript wurde aktualisiert und wird nun bei jeder Serverinstallation ausgeführt.',
+            'egg_created' => 'Das Egg wurde erfolgreich erstellt. Du musst alle laufenden Daemons neu starten, um dieses Egg anzuwenden.',
         ],
     ],
     'variables' => [
         'notices' => [
-            'variable_deleted' => 'The variable ":variable" has been deleted and will no longer be available to servers once rebuilt.',
-            'variable_updated' => 'The variable ":variable" has been updated. You will need to rebuild any servers using this variable in order to apply changes.',
-            'variable_created' => 'New variable has successfully been created and assigned to this egg.',
+            'variable_deleted' => 'Die Variable ":variable" wurde gelöscht und wird nach Rebuild nicht länger für Server verfügbar sein.',
+            'variable_updated' => 'Die Variable ":variable" wurde updated. Du musst alle Server, die diese Variable verwenden, neu erstellen, um die Änderungen zu übernehmen.',
+            'variable_created' => 'Die neue Variable wurde erfolgreich erstellt und diesem Egg zugeteilt.',
         ],
     ],
 ];
diff --git a/resources/lang/en/admin/node.php b/resources/lang/en/admin/node.php
index cffd4d7..fdd1cb9 100644
--- a/resources/lang/en/admin/node.php
+++ b/resources/lang/en/admin/node.php
@@ -2,15 +2,15 @@
 
 return [
     'validation' => [
-        'fqdn_not_resolvable' => 'The FQDN or IP address provided does not resolve to a valid IP address.',
-        'fqdn_required_for_ssl' => 'A fully qualified domain name that resolves to a public IP address is required in order to use SSL for this node.',
+        'fqdn_not_resolvable' => 'Der bereitgestellte FQDN oder die angegebene IP-Adresse wird nicht in eine gültige IP-Adresse aufgelöst.',
+        'fqdn_required_for_ssl' => 'Ein vollständig qualifizierter Domänenname, der in eine öffentliche IP-Adresse aufgelöst wird, ist erforderlich, um SSL für diesen Node zu verwenden.',
     ],
     'notices' => [
-        'allocations_added' => 'Allocations have successfully been added to this node.',
-        'node_deleted' => 'Node has been successfully removed from the panel.',
-        'location_required' => 'You must have at least one location configured before you can add a node to this panel.',
-        'node_created' => 'Successfully created new node. You can automatically configure the daemon on this machine by visiting the \'Configuration\' tab. <strong>Before you can add any servers you must first allocate at least one IP address and port.</strong>',
-        'node_updated' => 'Node information has been updated. If any daemon settings were changed you will need to reboot it for those changes to take effect.',
-        'unallocated_deleted' => 'Deleted all un-allocated ports for <code>:ip</code>.',
+        'allocations_added' => 'Die Ports wurden erfolgreich freigegeben.',
+        'node_deleted' => 'Node wurde erfolgreich entfernt.',
+        'location_required' => 'Du musst zuerst ein Standort hinzufügen, bevor du ein Node erstellen kannst.',
+        'node_created' => 'Neuer Node erfolgreich erstellt. Du kannst den Daemon auf diesem Computer automatisch konfigurieren, indem du die Registerkarte \'Wings-Integration\' öffnest. <strong>Bevor du Server hinzufügen kannst, musst du zuerst mindestens eine IP-Adresse und einen Port zuweisen.</strong>',
+        'node_updated' => 'Die Informationen zum Node wurden gespeichert. Möglicherweise ist ein Neustart notwendig.',
+        'unallocated_deleted' => 'Alle nicht zugewiesene Ports für <code>:ip</code> wurden entfernt.',
     ],
 ];
diff --git a/resources/lang/en/admin/server.php b/resources/lang/en/admin/server.php
index 29cb72d..6e1bd2f 100644
--- a/resources/lang/en/admin/server.php
+++ b/resources/lang/en/admin/server.php
@@ -2,26 +2,26 @@
 
 return [
     'exceptions' => [
-        'no_new_default_allocation' => 'You are attempting to delete the default allocation for this server but there is no fallback allocation to use.',
-        'marked_as_failed' => 'This server was marked as having failed a previous installation. Current status cannot be toggled in this state.',
-        'bad_variable' => 'There was a validation error with the :name variable.',
-        'daemon_exception' => 'There was an exception while attempting to communicate with the daemon resulting in a HTTP/:code response code. This exception has been logged. (request id: :request_id)',
-        'default_allocation_not_found' => 'The requested default allocation was not found in this server\'s allocations.',
+        'no_new_default_allocation' => 'Du versuchst, die Standardzuweisung für diesen Server zu löschen, aber es gibt keine Ersatzzuweisung, die du verwenden kannst.',
+        'marked_as_failed' => 'Dieser Server wurde bei einer früheren Installation als fehlgeschlagen markiert. Der aktuelle Status kann in diesem Zustand nicht umgeschaltet werden.',
+        'bad_variable' => 'Es gab einen Validierungsfehler mit der Variable :name.',
+        'daemon_exception' => 'Beim Versuch, mit dem Daemon zu kommunizieren, ist eine Ausnahme aufgetreten, die zu einem HTTP/:code Antwortcode führte. Diese Ausnahme wurde protokolliert (Anfrage-ID: :request_id).',
+        'default_allocation_not_found' => 'Die angeforderte Standardzuweisung wurde in den Zuweisungen dieses Servers nicht gefunden.',
     ],
     'alerts' => [
-        'startup_changed' => 'The startup configuration for this server has been updated. If this server\'s nest or egg was changed a reinstall will be occurring now.',
-        'server_deleted' => 'Server has successfully been deleted from the system.',
-        'server_created' => 'Server was successfully created on the panel. Please allow the daemon a few minutes to completely install this server.',
-        'build_updated' => 'The build details for this server have been updated. Some changes may require a restart to take effect.',
-        'suspension_toggled' => 'Server suspension status has been changed to :status.',
-        'rebuild_on_boot' => 'This server has been marked as requiring a Docker Container rebuild. This will happen the next time the server is started.',
-        'install_toggled' => 'The installation status for this server has been toggled.',
-        'server_reinstalled' => 'This server has been queued for a reinstallation beginning now.',
-        'details_updated' => 'Server details have been successfully updated.',
-        'docker_image_updated' => 'Successfully changed the default Docker image to use for this server. A reboot is required to apply this change.',
-        'node_required' => 'You must have at least one node configured before you can add a server to this panel.',
-        'transfer_nodes_required' => 'You must have at least two nodes configured before you can transfer servers.',
-        'transfer_started' => 'Server transfer has been started.',
-        'transfer_not_viable' => 'The node you selected does not have the required disk space or memory available to accommodate this server.',
+        'startup_changed' => 'Die Startkonfiguration für diesen Server wurde aktualisiert. Wenn das Nest oder Egg dieses Servers geändert wurde, wird nun eine Neuinstallation durchgeführt.',
+        'server_deleted' => 'Der Server wurde erfolgreich aus dem System gelöscht.',
+        'server_created' => 'Der Server wurde erfolgreich im Panel erstellt. Bitte gib dem Daemon ein paar Minuten Zeit, um diesen Server vollständig zu installieren.',
+        'build_updated' => 'Die Build-Details für diesen Server wurden aktualisiert. Einige Änderungen erfordern möglicherweise einen Neustart, um wirksam zu werden.',
+        'suspension_toggled' => 'Der Status der Serversperre wurde in :status geändert.',
+        'rebuild_on_boot' => 'Dieser Server wurde so markiert, dass ein Docker-Container-Rebuild erforderlich ist. Dies geschieht, wenn der Server das nächste Mal gestartet wird.',
+        'install_toggled' => 'Der Installationsstatus für diesen Server wurde umgeschaltet.',
+        'server_reinstalled' => 'Dieser Server steht ab sofort für eine Neuinstallation in der Warteschlange.',
+        'details_updated' => 'Die Serverdetails wurden erfolgreich aktualisiert.',
+        'docker_image_updated' => 'Das für diesen Server zu verwendende Standard-Docker-Image wurde erfolgreich geändert. Ein Neustart ist erforderlich, um diese Änderung anzuwenden.',
+        'node_required' => 'Du musst mindestens eine Node konfiguriert haben, bevor du einen Server zu diesem Panel hinzufügen kannst.',
+        'transfer_nodes_required' => 'Du musst mindestens 2 Nodes konfiguriert haben bevor du Server transferieren kannst.',
+        'transfer_started' => 'Servertransfer wurde gestartet',
+        'transfer_not_viable' => 'Die von dir ausgewählte Node verfügt nicht über den erforderlichen Festplatten- oder Arbeitsspeicher, um diesen Server aufzunehmen.',
     ],
 ];
diff --git a/resources/lang/en/admin/user.php b/resources/lang/en/admin/user.php
index 65e2278..0fa646d 100644
--- a/resources/lang/en/admin/user.php
+++ b/resources/lang/en/admin/user.php
@@ -2,10 +2,10 @@
 
 return [
     'exceptions' => [
-        'user_has_servers' => 'Cannot delete a user with active servers attached to their account. Please delete their servers before continuing.',
+        'user_has_servers' => 'Ein Benutzer mit aktiven Servern in seinem Konto kann nicht gelöscht werden. Bitte lösche seine Server, bevor du fortfährst.',
     ],
     'notices' => [
-        'account_created' => 'Account has been created successfully.',
-        'account_updated' => 'Account has been successfully updated.',
+        'account_created' => 'Das Konto wurde erfolgreich angelegt. ',
+        'account_updated' => 'Das Konto wurde erfolgreich aktualisiert.',
     ],
 ];
diff --git a/resources/lang/en/auth.php b/resources/lang/en/auth.php
index 2a3a452..6510364 100644
--- a/resources/lang/en/auth.php
+++ b/resources/lang/en/auth.php
@@ -3,7 +3,7 @@
 return [
     'sign_in' => 'Sign In',
     'go_to_login' => 'Go to Login',
-    'failed' => 'No account matching those credentials could be found.',
+    'failed' => 'Dieser Account existiert nicht. Bitte überprüfe deine Zugangsdaten.',
 
     'forgot_password' => [
         'label' => 'Forgot Password?',
diff --git a/resources/lang/en/command/messages.php b/resources/lang/en/command/messages.php
index a4a3aaf..bfe0073 100644
--- a/resources/lang/en/command/messages.php
+++ b/resources/lang/en/command/messages.php
@@ -2,65 +2,65 @@
 
 return [
     'location' => [
-        'no_location_found' => 'Could not locate a record matching the provided short code.',
-        'ask_short' => 'Location Short Code',
-        'ask_long' => 'Location Description',
-        'created' => 'Successfully created a new location (:name) with an ID of :id.',
-        'deleted' => 'Successfully deleted the requested location.',
+        'no_location_found' => 'Es konnte kein Datensatz gefunden werden, der mit dem angegebenen Kurzcode übereinstimmt.',
+        'ask_short' => 'Standort Kurzcode',
+        'ask_long' => 'Standortbeschreibung',
+        'created' => 'Erfolgreich den neuen Ort :name mit der ID :id erstellt.',
+        'deleted' => 'Der angeforderte Speicherort wurde erfolgreich gelöscht.',
     ],
     'user' => [
-        'search_users' => 'Enter a Username, User ID, or Email Address',
-        'select_search_user' => 'ID of user to delete (Enter \'0\' to re-search)',
-        'deleted' => 'User successfully deleted from the Panel.',
-        'confirm_delete' => 'Are you sure you want to delete this user from the Panel?',
-        'no_users_found' => 'No users were found for the search term provided.',
-        'multiple_found' => 'Multiple accounts were found for the user provided, unable to delete a user because of the --no-interaction flag.',
-        'ask_admin' => 'Is this user an administrator?',
-        'ask_email' => 'Email Address',
-        'ask_username' => 'Username',
-        'ask_name_first' => 'First Name',
-        'ask_name_last' => 'Last Name',
-        'ask_password' => 'Password',
-        'ask_password_tip' => 'If you would like to create an account with a random password emailed to the user, re-run this command (CTRL+C) and pass the `--no-password` flag.',
-        'ask_password_help' => 'Passwords must be at least 8 characters in length and contain at least one capital letter and number.',
+        'search_users' => 'Gib einen Benutzernamen, eine Benutzer-ID oder eine E-Mail-Adresse ein.',
+        'select_search_user' => 'ID des zu löschenden Benutzers (Gib \'0\' ein, um erneut zu suchen)',
+        'deleted' => 'Benutzer erfolgreich vom Panel entfernt.',
+        'confirm_delete' => 'Bist du dir sicher, dass du diesen Nutzer vom Panel entfernen möchtest?',
+        'no_users_found' => 'Mit den eingegebenen Suchbegriff wurden keine Benutzer gefunden.',
+        'multiple_found' => 'Es wurden mehrere Konten für den angegebenen Benutzer gefunden, der aufgrund des Flags --no-interaction nicht gelöscht werden kann.',
+        'ask_admin' => 'Ist dieser Nutzer ein Administrator?',
+        'ask_email' => 'Email-Adresse',
+        'ask_username' => 'Benutzername',
+        'ask_name_first' => 'Vorname',
+        'ask_name_last' => 'Nachname',
+        'ask_password' => 'Passwort',
+        'ask_password_tip' => 'Wenn du ein Konto mit einem zufälligen Kennwort erstellen möchtest, das dem Benutzer per E-Mail zugesandt wird, führe diesen Befehl erneut aus (STRG+C) und übergeben das Flag "no-password".',
+        'ask_password_help' => 'Die Passwörter müssen mindestens 8 Zeichen lang sein und mindestens einen Großbuchstaben und eine Zahl enthalten.',
         '2fa_help_text' => [
-            'This command will disable 2-factor authentication for a user\'s account if it is enabled. This should only be used as an account recovery command if the user is locked out of their account.',
-            'If this is not what you wanted to do, press CTRL+C to exit this process.',
+            'Mit diesem Befehl wird die 2-Faktor-Authentifizierung für das Konto eines Benutzers deaktiviert, wenn sie aktiviert ist. Er sollte nur als Befehl zur Wiederherstellung des Kontos verwendet werden, wenn der Benutzer aus seinem Konto ausgesperrt wurde.',
+            'Wenn du das nicht willst, drückst du STRG+C, um diesen Vorgang zu beenden.',
         ],
-        '2fa_disabled' => '2-Factor authentication has been disabled for :email.',
+        '2fa_disabled' => '2-Faktor Authentifizierung wurde für :email. deaktiviert.',
     ],
     'schedule' => [
-        'output_line' => 'Dispatching job for first task in `:schedule` (:hash).',
+        'output_line' => 'Versenden des Auftrags für die erste Aufgabe in `:schedule` (:hash).',
     ],
     'maintenance' => [
-        'deleting_service_backup' => 'Deleting service backup file :file.',
+        'deleting_service_backup' => 'Löschen der Service-Backup-Datei :file.',
     ],
     'server' => [
-        'rebuild_failed' => 'Rebuild request for ":name" (#:id) on node ":node" failed with error: :message',
+        'rebuild_failed' => 'Rebuild-Anfrage für ":name" (#:id) auf Node ":node" fehlgeschlagen mit Fehler: :message',
         'reinstall' => [
-            'failed' => 'Reinstall request for ":name" (#:id) on node ":node" failed with error: :message',
-            'confirm' => 'You are about to reinstall against a group of servers. Do you wish to continue?',
+            'failed' => 'Neuinstallationsanforderung für ":name" (#:id) auf Knoten ":node" fehlgeschlagen mit Fehler: :message',
+            'confirm' => 'Du bist dabei, eine Neuinstallation für eine Gruppe von Servern durchzuführen. Willst du fortfahren?',
         ],
         'power' => [
-            'confirm' => 'You are about to perform a :action against :count servers. Do you wish to continue?',
-            'action_failed' => 'Power action request for ":name" (#:id) on node ":node" failed with error: :message',
+            'confirm' => 'Du bist dabei, eine :Aktion gegen :count-Server durchzuführen. Willst du fortfahren?',
+            'action_failed' => 'Power Action-Anfrage für ":name" (#:id) auf Knoten ":node" fehlgeschlagen mit Fehler: :message',
         ],
     ],
     'environment' => [
         'mail' => [
-            'ask_smtp_host' => 'SMTP Host (e.g. smtp.gmail.com)',
+            'ask_smtp_host' => 'SMTP Host (z.B. smtp.gmail.com)',
             'ask_smtp_port' => 'SMTP Port',
-            'ask_smtp_username' => 'SMTP Username',
-            'ask_smtp_password' => 'SMTP Password',
+            'ask_smtp_username' => 'SMTP Nutzername',
+            'ask_smtp_password' => 'SMTP Passwort',
             'ask_mailgun_domain' => 'Mailgun Domain',
-            'ask_mailgun_endpoint' => 'Mailgun Endpoint',
-            'ask_mailgun_secret' => 'Mailgun Secret',
-            'ask_mandrill_secret' => 'Mandrill Secret',
-            'ask_postmark_username' => 'Postmark API Key',
-            'ask_driver' => 'Which driver should be used for sending emails?',
-            'ask_mail_from' => 'Email address emails should originate from',
-            'ask_mail_name' => 'Name that emails should appear from',
-            'ask_encryption' => 'Encryption method to use',
+            'ask_mailgun_endpoint' => 'Mailgun Endpunkt',
+            'ask_mailgun_secret' => 'Mailgun Geheimschlüssel',
+            'ask_mandrill_secret' => 'Mandrill Geheimschlüssel',
+            'ask_postmark_username' => 'Postmark API-Schlüssel',
+            'ask_driver' => 'Welcher Treiber sollte für den Versand von E-Mails verwendet werden?',
+            'ask_mail_from' => 'E-Mail-Adresse, von der die E-Mails stammen sollten',
+            'ask_mail_name' => 'Name, unter dem die E-Mails erscheinen sollen',
+            'ask_encryption' => 'Zu verwendende Verschlüsselungsmethode',
         ],
     ],
 ];
diff --git a/resources/lang/en/exceptions.php b/resources/lang/en/exceptions.php
index 4d44c4f..0919d9f 100644
--- a/resources/lang/en/exceptions.php
+++ b/resources/lang/en/exceptions.php
@@ -1,59 +1,59 @@
 <?php
 
 return [
-    'daemon_connection_failed' => 'There was an exception while attempting to communicate with the daemon resulting in a HTTP/:code response code. This exception has been logged.',
+    'daemon_connection_failed' => 'Beim Versuch, mit dem Daemon zu kommunizieren, ist eine Ausnahme aufgetreten, die zu einem HTTP/:code-Antwortcode führte. Diese Ausnahme wurde protokolliert.',
     'node' => [
-        'servers_attached' => 'A node must have no servers linked to it in order to be deleted.',
-        'daemon_off_config_updated' => 'The daemon configuration <strong>has been updated</strong>, however there was an error encountered while attempting to automatically update the configuration file on the Daemon. You will need to manually update the configuration file (config.yml) for the daemon to apply these changes.',
+        'servers_attached' => 'Dieser Node darf keine aktiven Server beinhalten, die darüber laufen.',
+        'daemon_off_config_updated' => 'Die Daemon-Config <strong>wurde aktualisiert</strong>, aber stehen noch nicht bereit! Du musst die Config.yml auf den neusten bringen und Wings neu starten.',
     ],
     'allocations' => [
-        'server_using' => 'A server is currently assigned to this allocation. An allocation can only be deleted if no server is currently assigned.',
-        'too_many_ports' => 'Adding more than 1000 ports in a single range at once is not supported.',
-        'invalid_mapping' => 'The mapping provided for :port was invalid and could not be processed.',
-        'cidr_out_of_range' => 'CIDR notation only allows masks between /25 and /32.',
-        'port_out_of_range' => 'Ports in an allocation must be greater than 1024 and less than or equal to 65535.',
+        'server_using' => 'Der Port wurde bereits einem Server zugewiesen. Entferne erst den zugewiesenen Port vom jeweiligen Server.',
+        'too_many_ports' => 'Ein Server kann nicht mehr als 1000 Ports zugeteilt bekommen (Wofür braucht man so viele?).',
+        'invalid_mapping' => 'Die Kartierung für :port war ungültig und kann nicht übernommen werden.',
+        'cidr_out_of_range' => 'Die CIDR-Notation erlaubt nur Masken zwischen /25 und /32.',
+        'port_out_of_range' => 'Die freigegebenen Ports müssen größer als 1024 und kleiner als 65535 sein.',
     ],
     'nest' => [
-        'delete_has_servers' => 'A Nest with active servers attached to it cannot be deleted from the Panel.',
+        'delete_has_servers' => 'Ein Nest mit aktiven Servern kann nicht entfernt werden.',
         'egg' => [
-            'delete_has_servers' => 'An Egg with active servers attached to it cannot be deleted from the Panel.',
-            'invalid_copy_id' => 'The Egg selected for copying a script from either does not exist, or is copying a script itself.',
-            'must_be_child' => 'The "Copy Settings From" directive for this Egg must be a child option for the selected Nest.',
-            'has_children' => 'This Egg is a parent to one or more other Eggs. Please delete those Eggs before deleting this Egg.',
+            'delete_has_servers' => 'Ein Nest mit aktiven Servern kann nicht entfernt werden',
+            'invalid_copy_id' => 'Das zum Kopieren eines Skripts ausgewählte Ei existiert entweder nicht oder kopiert selbst ein Skript.',
+            'must_be_child' => 'TDie Anweisung "Einstellungen kopieren von" für dieses Egg muss eine untergeordnete Option für das ausgewählte Nest sein.',
+            'has_children' => 'Dieses Egg ist mit anderen Eggs verbunden. Entferne erst die anderen Eggs, die dazu gehören.',
         ],
         'variables' => [
-            'env_not_unique' => 'The environment variable :name must be unique to this Egg.',
-            'reserved_name' => 'The environment variable :name is protected and cannot be assigned to a variable.',
-            'bad_validation_rule' => 'The validation rule ":rule" is not a valid rule for this application.',
+            'env_not_unique' => 'Die Umgebungsvariable :name muss für dieses Ei eindeutig sein.',
+            'reserved_name' => 'Die Umgebungsvariable :name ist geschützt und kann keiner Variablen zugewiesen werden.',
+            'bad_validation_rule' => 'Die Validierungsregel ":rule" ist keine gültige Regel für diese Anwendung.',
         ],
         'importer' => [
-            'json_error' => 'There was an error while attempting to parse the JSON file: :error.',
-            'file_error' => 'The JSON file provided was not valid.',
-            'invalid_json_provided' => 'The JSON file provided is not in a format that can be recognized.',
+            'json_error' => 'Beim Analysieren der JSON-Datei ist ein Fehler aufgetreten: :error.',
+            'file_error' => 'Die bereitgestellte JSON-Datei war ungültig.',
+            'invalid_json_provided' => 'Die bereitgestellte JSON-Datei hat kein erkennbares Format.',
         ],
     ],
     'subusers' => [
-        'editing_self' => 'Editing your own subuser account is not permitted.',
-        'user_is_owner' => 'You cannot add the server owner as a subuser for this server.',
-        'subuser_exists' => 'A user with that email address is already assigned as a subuser for this server.',
+        'editing_self' => 'Du kannst deine eigenen Berechtigungen nicht verändern.',
+        'user_is_owner' => 'Der Benutzer ist bereits der Admin des Servers',
+        'subuser_exists' => 'Dieser Benutzer ist bereits ein Teil des Servers.',
     ],
     'databases' => [
-        'delete_has_databases' => 'Cannot delete a database host server that has active databases linked to it.',
+        'delete_has_databases' => 'Ein Datenbankhostserver, mit dem aktive Datenbanken verknüpft sind, kann nicht gelöscht werden.',
     ],
     'tasks' => [
-        'chain_interval_too_long' => 'The maximum interval time for a chained task is 15 minutes.',
+        'chain_interval_too_long' => 'Die maximale Intervallzeit für eine verkettete Aufgabe beträgt 15 Minuten.',
     ],
     'locations' => [
-        'has_nodes' => 'Cannot delete a location that has active nodes attached to it.',
+        'has_nodes' => 'Der Standort kann bei einem aktiven zugeteilten Node nicht entfernt werden.',
     ],
     'users' => [
-        'node_revocation_failed' => 'Failed to revoke keys on <a href=":link">Node #:node</a>. :error',
+        'node_revocation_failed' => 'Schlüssel konnten nicht widerrufen werden in <a href=":link">Node #:node</a>. :error',
     ],
     'deployment' => [
-        'no_viable_nodes' => 'No nodes satisfying the requirements specified for automatic deployment could be found.',
-        'no_viable_allocations' => 'No allocations satisfying the requirements for automatic deployment were found.',
+        'no_viable_nodes' => 'Es konnten keine Nodes gefunden werden, die die für die automatische Bereitstellung angegebenen Anforderungen erfüllen.',
+        'no_viable_allocations' => 'Es wurden keine Portfreigaben gefunden, die die Anforderungen für die automatische Bereitstellung erfüllen.',
     ],
     'api' => [
-        'resource_not_found' => 'The requested resource does not exist on this server.',
+        'resource_not_found' => 'Die angeforderte Ressource ist auf diesem Server nicht vorhanden.',
     ],
 ];
diff --git a/resources/lang/en/pagination.php b/resources/lang/en/pagination.php
index ecac3aa..25253b1 100644
--- a/resources/lang/en/pagination.php
+++ b/resources/lang/en/pagination.php
@@ -12,6 +12,6 @@ return [
     |
     */
 
-    'previous' => '&laquo; Previous',
-    'next' => 'Next &raquo;',
+    'previous' => '&laquo; Zurück',
+    'next' => 'Weiter &raquo;',
 ];
diff --git a/resources/lang/en/passwords.php b/resources/lang/en/passwords.php
index 76a6959..a743f3e 100644
--- a/resources/lang/en/passwords.php
+++ b/resources/lang/en/passwords.php
@@ -11,9 +11,9 @@ return [
     | has failed, such as for an invalid token or invalid new password.
     |
     */
-    'password' => 'Passwords must be at least six characters and match the confirmation.',
-    'reset' => 'Your password has been reset!',
-    'sent' => 'We have e-mailed your password reset link!',
-    'token' => 'This password reset token is invalid.',
-    'user' => "We can't find a user with that e-mail address.",
+    'password' => 'Passwörter müssen mindestens sechs Zeichen lang sein und der Bestätigung entsprechen.',
+    'reset' => 'Dein Passwort wurde zurückgesetzt!',
+    'sent' => 'Wir haben dir einen Link zum Zurücksetzen deines Passworts per E-Mail geschickt!',
+    'token' => 'Der Token zum Zurücksetzen des Passworts ist ungültig.',
+    'user' => "Wir können keinen Benutzer mit dieser E-Mail-Adresse finden.",
 ];
diff --git a/resources/lang/en/strings.php b/resources/lang/en/strings.php
index ddade2c..98532bd 100644
--- a/resources/lang/en/strings.php
+++ b/resources/lang/en/strings.php
@@ -1,95 +1,95 @@
 <?php
 
 return [
-    'email' => 'Email',
-    'email_address' => 'Email address',
-    'user_identifier' => 'Username or Email',
-    'password' => 'Password',
-    'new_password' => 'New password',
-    'confirm_password' => 'Confirm new password',
-    'login' => 'Login',
-    'home' => 'Home',
-    'servers' => 'Servers',
+    'email' => 'E-Mail',
+    'email_address' => 'Email-Addresse',
+    'user_identifier' => 'Benutzername oder E-Mail',
+    'password' => 'Passwort',
+    'new_password' => 'Neues Passwort',
+    'confirm_password' => 'Neues Passwort bestätigen',
+    'login' => 'Anmelden',
+    'home' => 'Zuhause',
+    'servers' => 'Server',
     'id' => 'ID',
     'name' => 'Name',
     'node' => 'Node',
-    'connection' => 'Connection',
-    'memory' => 'Memory',
+    'connection' => 'Verbindung',
+    'memory' => 'Arbeitsspeicher',
     'cpu' => 'CPU',
-    'disk' => 'Disk',
+    'disk' => 'Speicher',
     'status' => 'Status',
-    'search' => 'Search',
-    'suspended' => 'Suspended',
+    'search' => 'Suche',
+    'suspended' => 'Gesperrt',
     'account' => 'Account',
-    'security' => 'Security',
-    'ip' => 'IP Address',
-    'last_activity' => 'Last Activity',
-    'revoke' => 'Revoke',
-    '2fa_token' => 'Authentication Token',
-    'submit' => 'Submit',
-    'close' => 'Close',
-    'settings' => 'Settings',
-    'configuration' => 'Configuration',
+    'security' => 'Sicherheit',
+    'ip' => 'IP-Adresse',
+    'last_activity' => 'Letzte Aktivität',
+    'revoke' => 'Entziehen',
+    '2fa_token' => 'Authentifizierungstoken',
+    'submit' => 'Absenden',
+    'close' => 'Schließen',
+    'settings' => 'Einstellungen',
+    'configuration' => 'Konfiguration',
     'sftp' => 'SFTP',
-    'databases' => 'Databases',
+    'databases' => 'Datenbanken',
     'memo' => 'Memo',
-    'created' => 'Created',
-    'expires' => 'Expires',
+    'created' => 'Erstellt',
+    'expires' => 'Läuft ab',
     'public_key' => 'Token',
-    'api_access' => 'Api Access',
-    'never' => 'never',
-    'sign_out' => 'Sign out',
-    'admin_control' => 'Admin Control',
-    'required' => 'Required',
+    'api_access' => 'API-Zugriff',
+    'never' => 'Niemals',
+    'sign_out' => 'Ausloggen',
+    'admin_control' => 'Admin-Steuerung',
+    'required' => 'Erforderlich',
     'port' => 'Port',
-    'username' => 'Username',
-    'database' => 'Database',
-    'new' => 'New',
-    'danger' => 'Danger',
-    'create' => 'Create',
-    'select_all' => 'Select All',
-    'select_none' => 'Select None',
+    'username' => 'Benutzername',
+    'database' => 'Datenbank',
+    'new' => 'Neu',
+    'danger' => 'Gefahr',
+    'create' => 'Erstellen',
+    'select_all' => 'Alle wählen',
+    'select_none' => 'Nichts auswählen',
     'alias' => 'Alias',
-    'primary' => 'Primary',
-    'make_primary' => 'Make Primary',
-    'none' => 'None',
-    'cancel' => 'Cancel',
-    'created_at' => 'Created At',
-    'action' => 'Action',
-    'data' => 'Data',
-    'queued' => 'Queued',
-    'last_run' => 'Last Run',
-    'next_run' => 'Next Run',
-    'not_run_yet' => 'Not Run Yet',
-    'yes' => 'Yes',
-    'no' => 'No',
-    'delete' => 'Delete',
+    'primary' => 'Primär',
+    'make_primary' => 'Primär machen',
+    'none' => 'Keiner',
+    'cancel' => 'Abbrechen',
+    'created_at' => 'Erstellt am',
+    'action' => 'Aktion',
+    'data' => 'Daten',
+    'queued' => 'In Warteschlange',
+    'last_run' => 'Letzter Lauf',
+    'next_run' => 'Nächster Lauf',
+    'not_run_yet' => 'Noch nicht ausgeführt',
+    'yes' => 'Ja',
+    'no' => 'Nein',
+    'delete' => 'Löschen',
     '2fa' => '2FA',
-    'logout' => 'Logout',
-    'admin_cp' => 'Admin Control Panel',
+    'logout' => 'Ausloggen',
+    'admin_cp' => 'Admin-Systemsteuerung',
     'optional' => 'Optional',
-    'read_only' => 'Read Only',
+    'read_only' => 'Nur lesen',
     'relation' => 'Relation',
-    'owner' => 'Owner',
+    'owner' => 'Inhaber',
     'admin' => 'Admin',
-    'subuser' => 'Subuser',
-    'captcha_invalid' => 'The provided captcha is invalid.',
-    'tasks' => 'Tasks',
-    'seconds' => 'Seconds',
-    'minutes' => 'Minutes',
-    'under_maintenance' => 'Under Maintenance',
+    'subuser' => 'Unterbenutzer',
+    'captcha_invalid' => 'Das bereitgestellte Captcha ist ungültig.',
+    'tasks' => 'Aufgaben',
+    'seconds' => 'Sekunden',
+    'minutes' => 'Minuten',
+    'under_maintenance' => 'Wird gewartet',
     'days' => [
-        'sun' => 'Sunday',
-        'mon' => 'Monday',
-        'tues' => 'Tuesday',
-        'wed' => 'Wednesday',
-        'thurs' => 'Thursday',
-        'fri' => 'Friday',
-        'sat' => 'Saturday',
+        'sun' => 'Sonntag',
+        'mon' => 'Montag',
+        'tues' => 'Dienstag',
+        'wed' => 'Mittwoch',
+        'thurs' => 'Donnerstag',
+        'fri' => 'Freitag',
+        'sat' => 'Samstag',
     ],
-    'last_used' => 'Last Used',
-    'enable' => 'Enable',
-    'disable' => 'Disable',
-    'save' => 'Save',
+    'last_used' => 'Zuletzt verwendet',
+    'enable' => 'Aktivieren',
+    'disable' => 'Deaktivieren',
+    'save' => 'Speichern',
     'copyright' => '&copy; 2015 - :year Pterodactyl Software',
 ];
diff --git a/resources/lang/en/validation.php b/resources/lang/en/validation.php
index a82aaa1..6b7b426 100644
--- a/resources/lang/en/validation.php
+++ b/resources/lang/en/validation.php
@@ -12,78 +12,78 @@ return [
     |
     */
 
-    'accepted' => 'The :attribute must be accepted.',
-    'active_url' => 'The :attribute is not a valid URL.',
-    'after' => 'The :attribute must be a date after :date.',
-    'after_or_equal' => 'The :attribute must be a date after or equal to :date.',
-    'alpha' => 'The :attribute may only contain letters.',
-    'alpha_dash' => 'The :attribute may only contain letters, numbers, and dashes.',
-    'alpha_num' => 'The :attribute may only contain letters and numbers.',
-    'array' => 'The :attribute must be an array.',
-    'before' => 'The :attribute must be a date before :date.',
-    'before_or_equal' => 'The :attribute must be a date before or equal to :date.',
+    'accepted' => ':attribute muss akzeptiert werden',
+    'active_url' => ':attribute ist keine validate URL.',
+    'after' => ':attribute muss ein Datum nach :date sein.',
+    'after_or_equal' => ':attribute muss ein Datum nach oder gleich :date sein.',
+    'alpha' => ':attribute darf nur Buchstaben enthalten.',
+    'alpha_dash' => ':attribute darf nur Buchstaben, Zahlen und Bindestriche enthalten.',
+    'alpha_num' => ':attribute darf nur Buchstaben und Zahlen enthalten.',
+    'array' => ':attribute muss ein Array sein.',
+    'before' => ':attribute muss ein Datum vor :date sein.',
+    'before_or_equal' => ':attribute muss ein Datum vor oder gleich :date sein.',
     'between' => [
-        'numeric' => 'The :attribute must be between :min and :max.',
-        'file' => 'The :attribute must be between :min and :max kilobytes.',
-        'string' => 'The :attribute must be between :min and :max characters.',
-        'array' => 'The :attribute must have between :min and :max items.',
+        'numeric' => ':attribute muss zwischen :min und :max liegen.',
+        'file' => ':attribute muss zwischen :min und :max Kilobyte liegen.',
+        'string' => ':attribute muss zwischen :min und :max Zeichen liegen.',
+        'array' => ':attribute muss zwischen :min und :max Elemente haben.',
     ],
-    'boolean' => 'The :attribute field must be true or false.',
-    'confirmed' => 'The :attribute confirmation does not match.',
-    'date' => 'The :attribute is not a valid date.',
-    'date_format' => 'The :attribute does not match the format :format.',
-    'different' => 'The :attribute and :other must be different.',
-    'digits' => 'The :attribute must be :digits digits.',
-    'digits_between' => 'The :attribute must be between :min and :max digits.',
-    'dimensions' => 'The :attribute has invalid image dimensions.',
-    'distinct' => 'The :attribute field has a duplicate value.',
-    'email' => 'The :attribute must be a valid email address.',
-    'exists' => 'The selected :attribute is invalid.',
-    'file' => 'The :attribute must be a file.',
-    'filled' => 'The :attribute field is required.',
-    'image' => 'The :attribute must be an image.',
-    'in' => 'The selected :attribute is invalid.',
-    'in_array' => 'The :attribute field does not exist in :other.',
-    'integer' => 'The :attribute must be an integer.',
-    'ip' => 'The :attribute must be a valid IP address.',
-    'json' => 'The :attribute must be a valid JSON string.',
+    'boolean' => ':attribute muss wahr oder falsch sein.',
+    'confirmed' => ':attribute Bestätigung stimmt nicht überein.',
+    'date' => ':attribute ist kein gültiges Datum.',
+    'date_format' => ':attribute passt nicht zum Format :format.',
+    'different' => ':attribute und :other müssen unterschiedlich sein.',
+    'digits' => ':attribute muss :digits Ziffern sein.',
+    'digits_between' => ':attribute muss zwischen :min und :max Ziffern liegen.',
+    'dimensions' => ':attribute hat ungültige Bildabmessungen.',
+    'distinct' => 'Das :attribute Feld hat einen doppelten Wert.',
+    'email' => ':attribute muss eine gültige E-Mail-Adresse sein.',
+    'exists' => 'Das ausgewählte Attribut :attribute ist ungültig.',
+    'file' => ':attribute muss eine Datei sein.',
+    'filled' => 'Das :attribute Feld wird für die Fortsetzung benötigt.',
+    'image' => ':attribute muss ein Bild sein.',
+    'in' => 'Das ausgewählte Attribut :attribute ist ungültig.',
+    'in_array' => 'Das :attribute Feld existiert nicht in :other.',
+    'integer' => ':attribute muss eine ganze Zahl sein.',
+    'ip' => ':attribute muss eine gültige IP-Adresse sein.',
+    'json' => ':attribute muss eine gültige JSON-Zeichenfolge sein.',
     'max' => [
-        'numeric' => 'The :attribute may not be greater than :max.',
-        'file' => 'The :attribute may not be greater than :max kilobytes.',
-        'string' => 'The :attribute may not be greater than :max characters.',
-        'array' => 'The :attribute may not have more than :max items.',
+        'numeric' => ':attribute darf nicht größer als :max sein.',
+        'file' => ':attribute darf nicht größer als :max kilobytes sein.',
+        'string' => ':attribute darf nicht größer als :max Zeichen sein.',
+        'array' => ':attribute darf nicht mehr als :max Elemente haben.',
     ],
-    'mimes' => 'The :attribute must be a file of type: :values.',
-    'mimetypes' => 'The :attribute must be a file of type: :values.',
+    'mimes' => ':attribute muss eine Datei des Typs :values sein.',
+    'mimetypes' => ':attribute muss eine Datei des Typs :values sein.',
     'min' => [
-        'numeric' => 'The :attribute must be at least :min.',
-        'file' => 'The :attribute must be at least :min kilobytes.',
-        'string' => 'The :attribute must be at least :min characters.',
-        'array' => 'The :attribute must have at least :min items.',
+        'numeric' => ':attribute muss mindestens :min sein.',
+        'file' => ':attribute muss mindestens :min Kilobyte groß sein.',
+        'string' => ':attribute muss mindestens :min Zeichen lang sein.',
+        'array' => ':attribute muss mindestens :min Elemente haben.',
     ],
-    'not_in' => 'The selected :attribute is invalid.',
-    'numeric' => 'The :attribute must be a number.',
-    'present' => 'The :attribute field must be present.',
-    'regex' => 'The :attribute format is invalid.',
-    'required' => 'The :attribute field is required.',
-    'required_if' => 'The :attribute field is required when :other is :value.',
-    'required_unless' => 'The :attribute field is required unless :other is in :values.',
-    'required_with' => 'The :attribute field is required when :values is present.',
-    'required_with_all' => 'The :attribute field is required when :values is present.',
-    'required_without' => 'The :attribute field is required when :values is not present.',
-    'required_without_all' => 'The :attribute field is required when none of :values are present.',
-    'same' => 'The :attribute and :other must match.',
+    'not_in' => 'Das ausgewählte Attribut :attribute ist ungültig.',
+    'numeric' => ':attribute muss eine Nummer sein.',
+    'present' => 'Das :attribute Feld muss vorhanden sein.',
+    'regex' => 'Das :attribute Format ist ungültig.',
+    'required' => 'Das :attribute Feld ist erforderlich.',
+    'required_if' => 'Das :attribute Feld ist erforderlich, wenn :other :value ist.',
+    'required_unless' => 'Das :attribute Feld ist erforderlich, es sei denn, :other ist :values.',
+    'required_with' => 'Das :attribute Feld ist erforderlich, wenn :values vorhanden ist.',
+    'required_with_all' => 'Das :attribute Feld ist erforderlich, wenn :values vorhanden ist.',
+    'required_without' => 'Das Feld :attribute ist erforderlich, wenn :values nicht vorhanden ist.',
+    'required_without_all' => 'Das :attribute Feld ist erforderlich, wenn keiner der :values vorhanden ist.',
+    'same' => ':attribute und :other müssen übereinstimmen.',
     'size' => [
-        'numeric' => 'The :attribute must be :size.',
-        'file' => 'The :attribute must be :size kilobytes.',
-        'string' => 'The :attribute must be :size characters.',
-        'array' => 'The :attribute must contain :size items.',
+        'numeric' => ':attribute muss :size sein.',
+        'file' => ':attribute muss :size Kilobyte sein.',
+        'string' => ':attribute muss :size Zeichen sein.',
+        'array' => ':attribute muss :size Elemente enthalten.',
     ],
-    'string' => 'The :attribute must be a string.',
-    'timezone' => 'The :attribute must be a valid zone.',
-    'unique' => 'The :attribute has already been taken.',
-    'uploaded' => 'The :attribute failed to upload.',
-    'url' => 'The :attribute format is invalid.',
+    'string' => ':attribute muss eine Zeichenfolge sein.',
+    'timezone' => ':attribute muss eine gültige Zone sein.',
+    'unique' => ':attribute wurde bereits vergeben.',
+    'uploaded' => ':attribute konnte nicht hochgeladen werden.',
+    'url' => 'Das Format von :attribute ist ungültig.',
 
     /*
     |--------------------------------------------------------------------------
@@ -100,7 +100,7 @@ return [
 
     // Internal validation logic for Pterodactyl
     'internal' => [
-        'variable_value' => ':env variable',
-        'invalid_password' => 'The password provided was invalid for this account.',
+        'variable_value' => ':env Variable',
+        'invalid_password' => 'Das angegebene Passwort war für dieses Konto ungültig.',
     ],
 ];
diff --git a/resources/scripts/api/server/files/compressFiles.ts b/resources/scripts/api/server/files/compressFiles.ts
index b4c0a25..6eef1df 100644
--- a/resources/scripts/api/server/files/compressFiles.ts
+++ b/resources/scripts/api/server/files/compressFiles.ts
@@ -7,9 +7,9 @@ export default async (uuid: string, directory: string, files: string[]): Promise
         `/api/client/servers/${uuid}/files/compress`,
         { root: directory, files },
         {
-            timeout: 60000,
+            timeout: 30000,
             timeoutErrorMessage:
-                'It looks like this archive is taking a long time to generate. It will appear once completed.',
+                'Die Archivierung hat zu lange gedauert. Der Prozess wurde abgebrochen.',
         }
     );
 
diff --git a/resources/scripts/api/server/files/decompressFiles.ts b/resources/scripts/api/server/files/decompressFiles.ts
index 37557a6..b058238 100644
--- a/resources/scripts/api/server/files/decompressFiles.ts
+++ b/resources/scripts/api/server/files/decompressFiles.ts
@@ -7,7 +7,7 @@ export default async (uuid: string, directory: string, file: string): Promise<vo
         {
             timeout: 300000,
             timeoutErrorMessage:
-                'It looks like this archive is taking a long time to be unarchived. Once completed the unarchived files will appear.',
+                'Das Entpacken dauert länger als erwartet. Bitte diese Seite nicht schließen, das Popup schließt sich nach fertigstellung selbst. ',
         }
     );
 };
diff --git a/resources/scripts/components/NavigationBar.tsx b/resources/scripts/components/NavigationBar.tsx
index 89180b1..055bc2b 100644
--- a/resources/scripts/components/NavigationBar.tsx
+++ b/resources/scripts/components/NavigationBar.tsx
@@ -67,20 +67,20 @@ export default () => {
                         </NavLink>
                     </Tooltip>
                     {rootAdmin && (
-                        <Tooltip placement={'bottom'} content={'Admin'}>
+                        <Tooltip placement={'bottom'} content={'Admin-Panel'}>
                             <a href={'/admin'} rel={'noreferrer'}>
                                 <FontAwesomeIcon icon={faCogs} />
                             </a>
                         </Tooltip>
                     )}
-                    <Tooltip placement={'bottom'} content={'Account Settings'}>
+                    <Tooltip placement={'bottom'} content={'Mein Account'}>
                         <NavLink to={'/account'}>
                             <span className={'flex items-center w-5 h-5'}>
                                 <Avatar.User />
                             </span>
                         </NavLink>
                     </Tooltip>
-                    <Tooltip placement={'bottom'} content={'Sign Out'}>
+                    <Tooltip placement={'bottom'} content={'Abmelden'}>
                         <button onClick={onTriggerLogout}>
                             <FontAwesomeIcon icon={faSignOutAlt} />
                         </button>
diff --git a/resources/scripts/components/auth/ForgotPasswordContainer.tsx b/resources/scripts/components/auth/ForgotPasswordContainer.tsx
index 76ddf19..fbe0ebc 100644
--- a/resources/scripts/components/auth/ForgotPasswordContainer.tsx
+++ b/resources/scripts/components/auth/ForgotPasswordContainer.tsx
@@ -38,7 +38,7 @@ export default () => {
                 console.error(error);
 
                 setSubmitting(false);
-                addFlash({ type: 'error', title: 'Error', message: httpErrorToHuman(error) });
+                addFlash({ type: 'error', title: 'Fehler', message: httpErrorToHuman(error) });
             });
 
             return;
@@ -47,11 +47,11 @@ export default () => {
         requestPasswordResetEmail(email, token)
             .then((response) => {
                 resetForm();
-                addFlash({ type: 'success', title: 'Success', message: response });
+                addFlash({ type: 'success', title: 'Erfolgreich', message: response });
             })
             .catch((error) => {
                 console.error(error);
-                addFlash({ type: 'error', title: 'Error', message: httpErrorToHuman(error) });
+                addFlash({ type: 'error', title: 'Fehler', message: httpErrorToHuman(error) });
             })
             .then(() => {
                 setToken('');
@@ -67,24 +67,24 @@ export default () => {
             initialValues={{ email: '' }}
             validationSchema={object().shape({
                 email: string()
-                    .email('A valid email address must be provided to continue.')
-                    .required('A valid email address must be provided to continue.'),
+                    .email('Gib eine gültige E-Mail Adresse an')
+                    .required('Gib eine gültige E-Mail Adresse an'),
             })}
         >
             {({ isSubmitting, setSubmitting, submitForm }) => (
-                <LoginFormContainer title={'Request Password Reset'} css={tw`w-full flex`}>
+                <LoginFormContainer title={'Passwort zurücksetzen'} css={tw`w-full flex`}>
                     <Field
                         light
-                        label={'Email'}
+                        label={'E-Mail'}
                         description={
-                            'Enter your account email address to receive instructions on resetting your password.'
+                            'Gib deine E-Mail Adresse an, um dein Passwort zurückzusetzen. Du bekommst dann eine E-Mail mit einer Anleitung.'
                         }
                         name={'email'}
                         type={'email'}
                     />
                     <div css={tw`mt-6`}>
                         <Button type={'submit'} size={'xlarge'} disabled={isSubmitting} isLoading={isSubmitting}>
-                            Send Email
+                            E-Mail senden
                         </Button>
                     </div>
                     {recaptchaEnabled && (
@@ -107,7 +107,7 @@ export default () => {
                             to={'/auth/login'}
                             css={tw`text-xs text-neutral-500 tracking-wide uppercase no-underline hover:text-neutral-700`}
                         >
-                            Return to Login
+                            Zum Login
                         </Link>
                     </div>
                 </LoginFormContainer>
diff --git a/resources/scripts/components/auth/LoginCheckpointContainer.tsx b/resources/scripts/components/auth/LoginCheckpointContainer.tsx
index 66a6fb8..d3d8b6b 100644
--- a/resources/scripts/components/auth/LoginCheckpointContainer.tsx
+++ b/resources/scripts/components/auth/LoginCheckpointContainer.tsx
@@ -27,16 +27,16 @@ const LoginCheckpointContainer = () => {
     const [isMissingDevice, setIsMissingDevice] = useState(false);
 
     return (
-        <LoginFormContainer title={'Device Checkpoint'} css={tw`w-full flex`}>
+        <LoginFormContainer title={'2-FA Authentifizierung'} css={tw`w-full flex`}>
             <div css={tw`mt-6`}>
                 <Field
                     light
                     name={isMissingDevice ? 'recoveryCode' : 'code'}
-                    title={isMissingDevice ? 'Recovery Code' : 'Authentication Code'}
+                    title={isMissingDevice ? 'Wiederherstellungscode' : '2-FA Authentifizierung'}
                     description={
                         isMissingDevice
-                            ? 'Enter one of the recovery codes generated when you setup 2-Factor authentication on this account in order to continue.'
-                            : 'Enter the two-factor token generated by your device.'
+                            ? 'Beim Einrichten der 2-Faktor Authentifizierung wurden dir Backup-Codes bereitgestellt. Nutze eine dieser Codes, um fortzufahren.'
+                            : 'Gib hier den 6-stelligen Code ein'
                     }
                     type={'text'}
                     autoComplete={'one-time-code'}
@@ -45,7 +45,7 @@ const LoginCheckpointContainer = () => {
             </div>
             <div css={tw`mt-6`}>
                 <Button size={'xlarge'} type={'submit'} disabled={isSubmitting} isLoading={isSubmitting}>
-                    Continue
+                    Weiter
                 </Button>
             </div>
             <div css={tw`mt-6 text-center`}>
@@ -57,7 +57,7 @@ const LoginCheckpointContainer = () => {
                     }}
                     css={tw`cursor-pointer text-xs text-neutral-500 tracking-wide uppercase no-underline hover:text-neutral-700`}
                 >
-                    {!isMissingDevice ? "I've Lost My Device" : 'I Have My Device'}
+                    {!isMissingDevice ? "Ich habe mein Gerät verloren" : 'Ich hab mein Gerät vor mir'}
                 </span>
             </div>
             <div css={tw`mt-6 text-center`}>
@@ -65,7 +65,7 @@ const LoginCheckpointContainer = () => {
                     to={'/auth/login'}
                     css={tw`text-xs text-neutral-500 tracking-wide uppercase no-underline hover:text-neutral-700`}
                 >
-                    Return to Login
+                    Zum Login zurück
                 </Link>
             </div>
         </LoginFormContainer>
diff --git a/resources/scripts/components/auth/LoginContainer.tsx b/resources/scripts/components/auth/LoginContainer.tsx
index 453a27e..29a9311 100644
--- a/resources/scripts/components/auth/LoginContainer.tsx
+++ b/resources/scripts/components/auth/LoginContainer.tsx
@@ -69,19 +69,19 @@ const LoginContainer = ({ history }: RouteComponentProps) => {
             onSubmit={onSubmit}
             initialValues={{ username: '', password: '' }}
             validationSchema={object().shape({
-                username: string().required('A username or email must be provided.'),
-                password: string().required('Please enter your account password.'),
+                username: string().required('Gib deinen Benutzernamen oder deine E-Mail ein'),
+                password: string().required('Gib dein Passwort ein'),
             })}
         >
             {({ isSubmitting, setSubmitting, submitForm }) => (
-                <LoginFormContainer title={'Login to Continue'} css={tw`w-full flex`}>
-                    <Field light type={'text'} label={'Username or Email'} name={'username'} disabled={isSubmitting} />
+                <LoginFormContainer title={'Anmelden'} css={tw`w-full flex`}>
+                    <Field light type={'text'} label={'Benutzername oder E-Mail'} name={'username'} disabled={isSubmitting} />
                     <div css={tw`mt-6`}>
-                        <Field light type={'password'} label={'Password'} name={'password'} disabled={isSubmitting} />
+                        <Field light type={'password'} label={'Passwort'} name={'password'} disabled={isSubmitting} />
                     </div>
                     <div css={tw`mt-6`}>
                         <Button type={'submit'} size={'xlarge'} isLoading={isSubmitting} disabled={isSubmitting}>
-                            Login
+                            Anmelden
                         </Button>
                     </div>
                     {recaptchaEnabled && (
@@ -104,7 +104,7 @@ const LoginContainer = ({ history }: RouteComponentProps) => {
                             to={'/auth/password'}
                             css={tw`text-xs text-neutral-500 tracking-wide no-underline uppercase hover:text-neutral-600`}
                         >
-                            Forgot password?
+                            Passwort vergessen?
                         </Link>
                     </div>
                 </LoginFormContainer>
diff --git a/resources/scripts/components/auth/LoginFormContainer.tsx b/resources/scripts/components/auth/LoginFormContainer.tsx
index 6274981..6b8f1d0 100644
--- a/resources/scripts/components/auth/LoginFormContainer.tsx
+++ b/resources/scripts/components/auth/LoginFormContainer.tsx
@@ -48,7 +48,16 @@ export default forwardRef<HTMLFormElement, Props>(({ title, ...props }, ref) =>
                 target={'_blank'}
                 css={tw`no-underline text-neutral-500 hover:text-neutral-300`}
             >
-                Pterodactyl Software
+                Pterodactyl Software&nbsp;
+            </a>
+            | Übersetzt von&nbsp;
+            <a
+                rel={'noopener nofollow noreferrer'}
+                href={'https://germandactyl.de'}
+                target={'_blank'}
+                css={tw`no-underline text-neutral-500 hover:text-neutral-300`}
+            >
+                Pavl21
             </a>
         </p>
     </Container>
diff --git a/resources/scripts/components/auth/ResetPasswordContainer.tsx b/resources/scripts/components/auth/ResetPasswordContainer.tsx
index 86f95ab..4bdd0e6 100644
--- a/resources/scripts/components/auth/ResetPasswordContainer.tsx
+++ b/resources/scripts/components/auth/ResetPasswordContainer.tsx
@@ -39,7 +39,7 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
                 console.error(error);
 
                 setSubmitting(false);
-                addFlash({ type: 'error', title: 'Error', message: httpErrorToHuman(error) });
+                addFlash({ type: 'error', title: 'Fehler', message: httpErrorToHuman(error) });
             });
     };
 
@@ -52,16 +52,16 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
             }}
             validationSchema={object().shape({
                 password: string()
-                    .required('A new password is required.')
-                    .min(8, 'Your new password should be at least 8 characters in length.'),
+                    .required('Gib dein neues Passwort ein')
+                    .min(8, 'Dein Passwort sollte mehr als 8 Zeichen haben.'),
                 passwordConfirmation: string()
-                    .required('Your new password does not match.')
+                    .required('Beide Passwörter stimmen nicht überein.')
                     // @ts-expect-error this is valid
-                    .oneOf([ref('password'), null], 'Your new password does not match.'),
+                    .oneOf([ref('password'), null], 'Beide Passwörter stimmen nicht überein.'),
             })}
         >
             {({ isSubmitting }) => (
-                <LoginFormContainer title={'Reset Password'} css={tw`w-full flex`}>
+                <LoginFormContainer title={'Passwort zurücksetzen'} css={tw`w-full flex`}>
                     <div>
                         <label>Email</label>
                         <Input value={email} isLight disabled />
@@ -69,18 +69,18 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
                     <div css={tw`mt-6`}>
                         <Field
                             light
-                            label={'New Password'}
+                            label={'Neues Passwort'}
                             name={'password'}
                             type={'password'}
-                            description={'Passwords must be at least 8 characters in length.'}
+                            description={'Das Passwort muss länger als 8 Zeichen sein.'}
                         />
                     </div>
                     <div css={tw`mt-6`}>
-                        <Field light label={'Confirm New Password'} name={'passwordConfirmation'} type={'password'} />
+                        <Field light label={'Jetzt zurücksetzen'} name={'passwordConfirmation'} type={'password'} />
                     </div>
                     <div css={tw`mt-6`}>
                         <Button size={'xlarge'} type={'submit'} disabled={isSubmitting} isLoading={isSubmitting}>
-                            Reset Password
+                            Jetzt zurücksetzen
                         </Button>
                     </div>
                     <div css={tw`mt-6 text-center`}>
@@ -88,7 +88,7 @@ export default ({ match, location }: RouteComponentProps<{ token: string }>) =>
                             to={'/auth/login'}
                             css={tw`text-xs text-neutral-500 tracking-wide no-underline uppercase hover:text-neutral-600`}
                         >
-                            Return to Login
+                            Zum Login zurück
                         </Link>
                     </div>
                 </LoginFormContainer>
diff --git a/resources/scripts/components/dashboard/AccountApiContainer.tsx b/resources/scripts/components/dashboard/AccountApiContainer.tsx
index 282f190..710dc11 100644
--- a/resources/scripts/components/dashboard/AccountApiContainer.tsx
+++ b/resources/scripts/components/dashboard/AccountApiContainer.tsx
@@ -14,6 +14,7 @@ import GreyRowBox from '@/components/elements/GreyRowBox';
 import { Dialog } from '@/components/elements/dialog';
 import { useFlashKey } from '@/plugins/useFlash';
 import Code from '@/components/elements/Code';
+import { de } from "date-fns/locale";
 
 export default () => {
     const [deleteIdentifier, setDeleteIdentifier] = useState('');
@@ -42,26 +43,26 @@ export default () => {
     };
 
     return (
-        <PageContentBlock title={'Account API'}>
+        <PageContentBlock title={'API-Zugangsdaten'}>
             <FlashMessageRender byKey={'account'} />
             <div css={tw`md:flex flex-nowrap my-10`}>
-                <ContentBox title={'Create API Key'} css={tw`flex-none w-full md:w-1/2`}>
+                <ContentBox title={'API-Schlüssel erstellen'} css={tw`flex-none w-full md:w-1/2`}>
                     <CreateApiKeyForm onKeyCreated={(key) => setKeys((s) => [...s!, key])} />
                 </ContentBox>
-                <ContentBox title={'API Keys'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
+                <ContentBox title={'Alle API-Schlüssel'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
                     <SpinnerOverlay visible={loading} />
                     <Dialog.Confirm
-                        title={'Delete API Key'}
-                        confirm={'Delete Key'}
+                        title={'API-Schlüssel löschen'}
+                        confirm={'Schlüssel löschen'}
                         open={!!deleteIdentifier}
                         onClose={() => setDeleteIdentifier('')}
                         onConfirmed={() => doDeletion(deleteIdentifier)}
                     >
-                        All requests using the <Code>{deleteIdentifier}</Code> key will be invalidated.
+                        Alle Anfragen, die an <Code>{deleteIdentifier}</Code> gesendet werden, ignoriert dann dein Server.
                     </Dialog.Confirm>
                     {keys.length === 0 ? (
                         <p css={tw`text-center text-sm`}>
-                            {loading ? 'Loading...' : 'No API keys exist for this account.'}
+                            {loading ? 'Einen Moment bitte...' : 'Du hast keine API-Schlüssel aktiv.'}
                         </p>
                     ) : (
                         keys.map((key, index) => (
@@ -73,8 +74,8 @@ export default () => {
                                 <div css={tw`ml-4 flex-1 overflow-hidden`}>
                                     <p css={tw`text-sm break-words`}>{key.description}</p>
                                     <p css={tw`text-2xs text-neutral-300 uppercase`}>
-                                        Last used:&nbsp;
-                                        {key.lastUsedAt ? format(key.lastUsedAt, 'MMM do, yyyy HH:mm') : 'Never'}
+                                        Zuletzt angefragt:&nbsp;
+                                        {key.lastUsedAt ? format(key.lastUsedAt, 'do MMM yyyy, HH:mm', {locale: de}) : 'Noch nie'}
                                     </p>
                                 </div>
                                 <p css={tw`text-sm ml-4 hidden md:block`}>
diff --git a/resources/scripts/components/dashboard/AccountOverviewContainer.tsx b/resources/scripts/components/dashboard/AccountOverviewContainer.tsx
index 4f64aec..2b83276 100644
--- a/resources/scripts/components/dashboard/AccountOverviewContainer.tsx
+++ b/resources/scripts/components/dashboard/AccountOverviewContainer.tsx
@@ -30,21 +30,21 @@ export default () => {
     const { state } = useLocation<undefined | { twoFactorRedirect?: boolean }>();
 
     return (
-        <PageContentBlock title={'Account Overview'}>
+        <PageContentBlock title={'Mein Account'}>
             {state?.twoFactorRedirect && (
-                <MessageBox title={'2-Factor Required'} type={'error'}>
-                    Your account must have two-factor authentication enabled in order to continue.
+                <MessageBox title={'2-Faktor benötigt'} type={'error'}>
+                    Die 2-Faktor Authentifizierung wird benötigt. Richte es erst ein.
                 </MessageBox>
             )}
 
             <Container css={[tw`lg:grid lg:grid-cols-3 mb-10`, state?.twoFactorRedirect ? tw`mt-4` : tw`mt-10`]}>
-                <ContentBox title={'Update Password'} showFlashes={'account:password'}>
+                <ContentBox title={'Passwort ändern'} showFlashes={'account:password'}>
                     <UpdatePasswordForm />
                 </ContentBox>
-                <ContentBox css={tw`mt-8 sm:mt-0 sm:ml-8`} title={'Update Email Address'} showFlashes={'account:email'}>
+                <ContentBox css={tw`mt-8 sm:mt-0 sm:ml-8`} title={'E-Mail Adresse ändern'} showFlashes={'account:email'}>
                     <UpdateEmailAddressForm />
                 </ContentBox>
-                <ContentBox css={tw`md:ml-8 mt-8 md:mt-0`} title={'Two-Step Verification'}>
+                <ContentBox css={tw`md:ml-8 mt-8 md:mt-0`} title={'2-Faktor Authentifizierung'}>
                     <ConfigureTwoFactorForm />
                 </ContentBox>
             </Container>
diff --git a/resources/scripts/components/dashboard/ApiKeyModal.tsx b/resources/scripts/components/dashboard/ApiKeyModal.tsx
index 6d01029..047b398 100644
--- a/resources/scripts/components/dashboard/ApiKeyModal.tsx
+++ b/resources/scripts/components/dashboard/ApiKeyModal.tsx
@@ -14,10 +14,9 @@ const ApiKeyModal = ({ apiKey }: Props) => {
 
     return (
         <>
-            <h3 css={tw`mb-6 text-2xl`}>Your API Key</h3>
+            <h3 css={tw`mb-6 text-2xl`}>Dein API-Schlüssel</h3>
             <p css={tw`text-sm mb-6`}>
-                The API key you have requested is shown below. Please store this in a safe location, it will not be
-                shown again.
+                Dein API-Schlüssel steht jetzt bereit. Gib es nur an Clients oder Personen weiter, denen du vertraust. Sie haben dieselben Berechtigungen wie du!
             </p>
             <pre css={tw`text-sm bg-neutral-900 rounded py-2 px-4 font-mono`}>
                 <CopyOnClick text={apiKey}>
@@ -26,7 +25,7 @@ const ApiKeyModal = ({ apiKey }: Props) => {
             </pre>
             <div css={tw`flex justify-end mt-6`}>
                 <Button type={'button'} onClick={() => dismiss()}>
-                    Close
+                    Schliessen
                 </Button>
             </div>
         </>
diff --git a/resources/scripts/components/dashboard/DashboardContainer.tsx b/resources/scripts/components/dashboard/DashboardContainer.tsx
index 25e0e4d..8895906 100644
--- a/resources/scripts/components/dashboard/DashboardContainer.tsx
+++ b/resources/scripts/components/dashboard/DashboardContainer.tsx
@@ -53,7 +53,7 @@ export default () => {
             {rootAdmin && (
                 <div css={tw`mb-2 flex justify-end items-center`}>
                     <p css={tw`uppercase text-xs text-neutral-400 mr-2`}>
-                        {showOnlyAdmin ? "Showing others' servers" : 'Showing your servers'}
+                        {showOnlyAdmin ? "Andere Server" : 'Eigene Server'}
                     </p>
                     <Switch
                         name={'show_all_servers'}
@@ -74,8 +74,8 @@ export default () => {
                         ) : (
                             <p css={tw`text-center text-sm text-neutral-400`}>
                                 {showOnlyAdmin
-                                    ? 'There are no other servers to display.'
-                                    : 'There are no servers associated with your account.'}
+                                    ? 'Derzeit gibt es keine Server.'
+                                    : 'Aktuell ist kein Server mit deinem Account verbunden.'}
                             </p>
                         )
                     }
diff --git a/resources/scripts/components/dashboard/ServerRow.tsx b/resources/scripts/components/dashboard/ServerRow.tsx
index 08637bf..8290dfc 100644
--- a/resources/scripts/components/dashboard/ServerRow.tsx
+++ b/resources/scripts/components/dashboard/ServerRow.tsx
@@ -84,9 +84,9 @@ export default ({ server, className }: { server: Server; className?: string }) =
         alarms.disk = server.limits.disk === 0 ? false : isAlarmState(stats.diskUsageInBytes, server.limits.disk);
     }
 
-    const diskLimit = server.limits.disk !== 0 ? bytesToString(mbToBytes(server.limits.disk)) : 'Unlimited';
-    const memoryLimit = server.limits.memory !== 0 ? bytesToString(mbToBytes(server.limits.memory)) : 'Unlimited';
-    const cpuLimit = server.limits.cpu !== 0 ? server.limits.cpu + ' %' : 'Unlimited';
+    const diskLimit = server.limits.disk !== 0 ? bytesToString(mbToBytes(server.limits.disk)) : 'Kein Limit';
+    const memoryLimit = server.limits.memory !== 0 ? bytesToString(mbToBytes(server.limits.memory)) : 'Kein Limit';
+    const cpuLimit = server.limits.cpu !== 0 ? server.limits.cpu + ' %' : 'Kein Limit';
 
     return (
         <StatusIndicatorBox as={Link} to={`/server/${server.id}`} className={className} $status={stats?.status}>
@@ -120,19 +120,19 @@ export default ({ server, className }: { server: Server; className?: string }) =
                     isSuspended ? (
                         <div css={tw`flex-1 text-center`}>
                             <span css={tw`bg-red-500 rounded px-2 py-1 text-red-100 text-xs`}>
-                                {server.status === 'suspended' ? 'Suspended' : 'Connection Error'}
+                                {server.status === 'suspended' ? 'Gesperrt' : 'Verbindungsfehler'}
                             </span>
                         </div>
                     ) : server.isTransferring || server.status ? (
                         <div css={tw`flex-1 text-center`}>
                             <span css={tw`bg-neutral-500 rounded px-2 py-1 text-neutral-100 text-xs`}>
                                 {server.isTransferring
-                                    ? 'Transferring'
+                                    ? 'Transfer läuft...'
                                     : server.status === 'installing'
-                                    ? 'Installing'
+                                    ? 'Installation läuft...'
                                     : server.status === 'restoring_backup'
-                                    ? 'Restoring Backup'
-                                    : 'Unavailable'}
+                                    ? 'Backup wiederherstellen...'
+                                    : 'Nicht verfügbar'}
                             </span>
                         </div>
                     ) : (
@@ -147,7 +147,7 @@ export default ({ server, className }: { server: Server; className?: string }) =
                                     {stats.cpuUsagePercent.toFixed(2)} %
                                 </IconDescription>
                             </div>
-                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>of {cpuLimit}</p>
+                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>von {cpuLimit}</p>
                         </div>
                         <div css={tw`flex-1 ml-4 sm:block hidden`}>
                             <div css={tw`flex justify-center`}>
@@ -156,7 +156,7 @@ export default ({ server, className }: { server: Server; className?: string }) =
                                     {bytesToString(stats.memoryUsageInBytes)}
                                 </IconDescription>
                             </div>
-                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>of {memoryLimit}</p>
+                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>von {memoryLimit}</p>
                         </div>
                         <div css={tw`flex-1 ml-4 sm:block hidden`}>
                             <div css={tw`flex justify-center`}>
@@ -165,7 +165,7 @@ export default ({ server, className }: { server: Server; className?: string }) =
                                     {bytesToString(stats.diskUsageInBytes)}
                                 </IconDescription>
                             </div>
-                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>of {diskLimit}</p>
+                            <p css={tw`text-xs text-neutral-600 text-center mt-1`}>von {diskLimit}</p>
                         </div>
                     </React.Fragment>
                 )}
diff --git a/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx b/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx
index f10701f..666d5f6 100644
--- a/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx
+++ b/resources/scripts/components/dashboard/activity/ActivityLogContainer.tsx
@@ -31,7 +31,7 @@ export default () => {
     }, [error]);
 
     return (
-        <PageContentBlock title={'Account Activity Log'}>
+        <PageContentBlock title={'Alle Account-Aktivitäten'}>
             <FlashMessageRender byKey={'account'} />
             {(filters.filters?.event || filters.filters?.ip) && (
                 <div className={'flex justify-end mb-2'}>
diff --git a/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx b/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx
index a59977f..eac4b37 100644
--- a/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx
+++ b/resources/scripts/components/dashboard/forms/ConfigureTwoFactorForm.tsx
@@ -32,14 +32,14 @@ export default () => {
             <DisableTOTPDialog open={visible === 'disable'} onClose={() => setVisible(null)} />
             <p css={tw`text-sm`}>
                 {isEnabled
-                    ? 'Two-step verification is currently enabled on your account.'
-                    : 'You do not currently have two-step verification enabled on your account. Click the button below to begin configuring it.'}
+                    ? 'Die 2-Faktor Authentifizierung ist aktuell aktiv.'
+                    : '2-Faktor ist aktuell nicht eingerichtet, du kannst es jederzeit einrichten.'}
             </p>
             <div css={tw`mt-6`}>
                 {isEnabled ? (
-                    <Button.Danger onClick={() => setVisible('disable')}>Disable Two-Step</Button.Danger>
+                    <Button.Danger onClick={() => setVisible('disable')}>2-Faktor deaktivieren</Button.Danger>
                 ) : (
-                    <Button onClick={() => setVisible('enable')}>Enable Two-Step</Button>
+                    <Button onClick={() => setVisible('enable')}>2-Faktor einrichten</Button>
                 )}
             </div>
         </div>
diff --git a/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx b/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx
index 9c27304..9e46ff1 100644
--- a/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx
+++ b/resources/scripts/components/dashboard/forms/CreateApiKeyForm.tsx
@@ -59,24 +59,24 @@ export default ({ onKeyCreated }: { onKeyCreated: (key: ApiKey) => void }) => {
                     <Form>
                         <SpinnerOverlay visible={isSubmitting} />
                         <FormikFieldWrapper
-                            label={'Description'}
+                            label={'Beschreibung'}
                             name={'description'}
-                            description={'A description of this API key.'}
+                            description={'Für was ist der API-Schlüssel gedacht?'}
                             css={tw`mb-6`}
                         >
                             <Field name={'description'} as={Input} />
                         </FormikFieldWrapper>
                         <FormikFieldWrapper
-                            label={'Allowed IPs'}
+                            label={'Erlaubte IPs'}
                             name={'allowedIps'}
                             description={
-                                'Leave blank to allow any IP address to use this API key, otherwise provide each IP address on a new line.'
+                                'Erlaube hier nur Anfragen, die von zugelassenen IPs stammen, lasse es leer wenn es theoretisch alle dürfen.'
                             }
                         >
                             <Field name={'allowedIps'} as={CustomTextarea} />
                         </FormikFieldWrapper>
                         <div css={tw`flex justify-end mt-6`}>
-                            <Button>Create</Button>
+                            <Button>Erstellen</Button>
                         </div>
                     </Form>
                 )}
diff --git a/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx b/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx
index 125376e..f186910 100644
--- a/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx
+++ b/resources/scripts/components/dashboard/forms/DisableTOTPDialog.tsx
@@ -41,7 +41,7 @@ const DisableTOTPDialog = () => {
         <form id={'disable-totp-form'} className={'mt-6'} onSubmit={submit}>
             <FlashMessageRender byKey={'account:two-step'} className={'-mt-2 mb-6'} />
             <label className={'block pb-1'} htmlFor={'totp-password'}>
-                Password
+                Passwort
             </label>
             <Input.Text
                 id={'totp-password'}
@@ -51,14 +51,14 @@ const DisableTOTPDialog = () => {
                 onChange={(e) => setPassword(e.currentTarget.value)}
             />
             <Dialog.Footer>
-                <Button.Text onClick={close}>Cancel</Button.Text>
+                <Button.Text onClick={close}>Abbrechen</Button.Text>
                 <Tooltip
                     delay={100}
                     disabled={password.length > 0}
-                    content={'You must enter your account password to continue.'}
+                    content={'Gib dein Passwort ein, um fortzufahren.'}
                 >
                     <Button.Danger type={'submit'} form={'disable-totp-form'} disabled={submitting || !password.length}>
-                        Disable
+                        Deaktivieren
                     </Button.Danger>
                 </Tooltip>
             </Dialog.Footer>
@@ -67,6 +67,6 @@ const DisableTOTPDialog = () => {
 };
 
 export default asDialog({
-    title: 'Disable Two-Step Verification',
-    description: 'Disabling two-step verification will make your account less secure.',
+    title: '2-Faktor deaktivieren',
+    description: 'Das Deaktivieren verringert die Sicherheit deines Accounts.',
 })(DisableTOTPDialog);
diff --git a/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx b/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx
index 3e2d0ba..7e95f23 100644
--- a/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx
+++ b/resources/scripts/components/dashboard/forms/RecoveryTokensDialog.tsx
@@ -20,9 +20,9 @@ export default ({ tokens, open, onClose }: RecoveryTokenDialogProps) => {
         <Dialog
             open={open}
             onClose={onClose}
-            title={'Two-Step Authentication Enabled'}
+            title={'2-Faktor Authentifizierung ist jetzt aktiv.'}
             description={
-                'Store the codes below somewhere safe. If you lose access to your phone you can use these backup codes to sign in.'
+                'Speicher die Backup-Codes dort ab, wo sie sicher und gut auffindbar sind. Das ist sehr wichtig, damit du bei Verlust deines Gerätes die 2FA neu einrichten kannst.'
             }
             hideCloseIcon
             preventExternalClose
@@ -41,10 +41,10 @@ export default ({ tokens, open, onClose }: RecoveryTokenDialogProps) => {
                 </pre>
             </CopyOnClick>
             <Alert type={'danger'} className={'mt-3'}>
-                These codes will not be shown again.
+                Diese Codes werden nur dieses mal gezeigt.
             </Alert>
             <Dialog.Footer>
-                <Button.Text onClick={onClose}>Done</Button.Text>
+                <Button.Text onClick={onClose}>Ich habs gespeichert</Button.Text>
             </Dialog.Footer>
         </Dialog>
     );
diff --git a/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx b/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx
index 836b2ef..41c2c10 100644
--- a/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx
+++ b/resources/scripts/components/dashboard/forms/SetupTOTPDialog.tsx
@@ -70,12 +70,11 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
             </div>
             <CopyOnClick text={token?.secret}>
                 <p className={'font-mono text-sm text-gray-100 text-center mt-2'}>
-                    {token?.secret.match(/.{1,4}/g)!.join(' ') || 'Loading...'}
+                    {token?.secret.match(/.{1,4}/g)!.join(' ') || 'Wird geladen...'}
                 </p>
             </CopyOnClick>
             <p id={'totp-code-description'} className={'mt-6'}>
-                Scan the QR code above using the two-step authentication app of your choice. Then, enter the 6-digit
-                code generated into the field below.
+                Scanne den QR-Code mit einer Authentificator-App deiner Wahl (z.B: Microsoft Authentificator) und gib dann den 6-stelligen Code ein.
             </p>
             <Input.Text
                 aria-labelledby={'totp-code-description'}
@@ -90,7 +89,7 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
                 pattern={'\\d{6}'}
             />
             <label htmlFor={'totp-password'} className={'block mt-3'}>
-                Account Password
+                Passwort deines Accounts
             </label>
             <Input.Text
                 variant={Input.Text.Variants.Loose}
@@ -100,13 +99,13 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
                 onChange={(e) => setPassword(e.currentTarget.value)}
             />
             <Dialog.Footer>
-                <Button.Text onClick={close}>Cancel</Button.Text>
+                <Button.Text onClick={close}>Abbrechen</Button.Text>
                 <Tooltip
                     disabled={password.length > 0 && value.length === 6}
                     content={
                         !token
-                            ? 'Waiting for QR code to load...'
-                            : 'You must enter the 6-digit code and your password to continue.'
+                            ? 'Dein QR-Code wird geladen...'
+                            : 'Gib den 6-stelligen Code und dein Passwort ein, um fortzufahren.'
                     }
                     delay={100}
                 >
@@ -115,7 +114,7 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
                         type={'submit'}
                         form={'enable-totp-form'}
                     >
-                        Enable
+                        Aktivieren
                     </Button>
                 </Tooltip>
             </Dialog.Footer>
@@ -124,7 +123,7 @@ const ConfigureTwoFactorForm = ({ onTokens }: Props) => {
 };
 
 export default asDialog({
-    title: 'Enable Two-Step Verification',
+    title: '2-Faktor Authentifizierung einrichten',
     description:
-        "Help protect your account from unauthorized access. You'll be prompted for a verification code each time you sign in.",
+        'Schütze dich vor ungebetenen Benutzern in deinem eigenen Account und richte hier dein Schutzschild ein.',
 })(ConfigureTwoFactorForm);
diff --git a/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx b/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx
index 462a37f..b721880 100644
--- a/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx
+++ b/resources/scripts/components/dashboard/forms/UpdateEmailAddressForm.tsx
@@ -16,7 +16,7 @@ interface Values {
 
 const schema = Yup.object().shape({
     email: Yup.string().email().required(),
-    password: Yup.string().required('You must provide your current account password.'),
+    password: Yup.string().required('Gib dein aktuelles Passwort für deinen Account ein.'),
 });
 
 export default () => {
@@ -33,14 +33,14 @@ export default () => {
                 addFlash({
                     type: 'success',
                     key: 'account:email',
-                    message: 'Your primary email has been updated.',
+                    message: 'Deine aktuelle E-Mail Adresse wurde durch die neue ersetzt.',
                 })
             )
             .catch((error) =>
                 addFlash({
                     type: 'error',
                     key: 'account:email',
-                    title: 'Error',
+                    title: 'Fehler',
                     message: httpErrorToHuman(error),
                 })
             )
@@ -62,11 +62,11 @@ export default () => {
                                 id={'confirm_password'}
                                 type={'password'}
                                 name={'password'}
-                                label={'Confirm Password'}
+                                label={'Passwort bestäigen'}
                             />
                         </div>
                         <div css={tw`mt-6`}>
-                            <Button disabled={isSubmitting || !isValid}>Update Email</Button>
+                            <Button disabled={isSubmitting || !isValid}>E-Mail speichern</Button>
                         </div>
                     </Form>
                 </React.Fragment>
diff --git a/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx b/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx
index 707ee72..fcc38e9 100644
--- a/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx
+++ b/resources/scripts/components/dashboard/forms/UpdatePasswordForm.tsx
@@ -17,11 +17,11 @@ interface Values {
 }
 
 const schema = Yup.object().shape({
-    current: Yup.string().min(1).required('You must provide your current password.'),
+    current: Yup.string().min(1).required('Gib dein aktuelles Passwort ein'),
     password: Yup.string().min(8).required(),
     confirmPassword: Yup.string().test(
         'password',
-        'Password confirmation does not match the password you entered.',
+        'Die Passwortbestätigung stimmt nicht mit dem eingegebenen Passwort überein.',
         function (value) {
             return value === this.parent.password;
         }
@@ -47,7 +47,7 @@ export default () => {
                 addFlash({
                     key: 'account:password',
                     type: 'error',
-                    title: 'Error',
+                    title: 'Fehler',
                     message: httpErrorToHuman(error),
                 })
             )
@@ -69,16 +69,16 @@ export default () => {
                                 id={'current_password'}
                                 type={'password'}
                                 name={'current'}
-                                label={'Current Password'}
+                                label={'Aktuelles Passwort'}
                             />
                             <div css={tw`mt-6`}>
                                 <Field
                                     id={'new_password'}
                                     type={'password'}
                                     name={'password'}
-                                    label={'New Password'}
+                                    label={'Neues Passwort'}
                                     description={
-                                        'Your new password should be at least 8 characters in length and unique to this website.'
+                                        'Dein neues Passwort sollte mindestens 8 Zeichen lang sein, das verbessert die Sicherheit deines Accounts.'
                                     }
                                 />
                             </div>
@@ -87,11 +87,11 @@ export default () => {
                                     id={'confirm_new_password'}
                                     type={'password'}
                                     name={'confirmPassword'}
-                                    label={'Confirm New Password'}
+                                    label={'Bestätige das neue Passwort'}
                                 />
                             </div>
                             <div css={tw`mt-6`}>
-                                <Button disabled={isSubmitting || !isValid}>Update Password</Button>
+                                <Button disabled={isSubmitting || !isValid}>Neues Passwort speichern</Button>
                             </div>
                         </Form>
                     </React.Fragment>
diff --git a/resources/scripts/components/dashboard/search/SearchContainer.tsx b/resources/scripts/components/dashboard/search/SearchContainer.tsx
index 4da0f3a..94ed25d 100644
--- a/resources/scripts/components/dashboard/search/SearchContainer.tsx
+++ b/resources/scripts/components/dashboard/search/SearchContainer.tsx
@@ -19,7 +19,7 @@ export default () => {
     return (
         <>
             {visible && <SearchModal appear visible={visible} onDismissed={() => setVisible(false)} />}
-            <Tooltip placement={'bottom'} content={'Search'}>
+            <Tooltip placement={'bottom'} content={'Suchen'}>
                 <div className={'navigation-link'} onClick={() => setVisible(true)}>
                     <FontAwesomeIcon icon={faSearch} />
                 </div>
diff --git a/resources/scripts/components/dashboard/search/SearchModal.tsx b/resources/scripts/components/dashboard/search/SearchModal.tsx
index 1335cff..e45596c 100644
--- a/resources/scripts/components/dashboard/search/SearchModal.tsx
+++ b/resources/scripts/components/dashboard/search/SearchModal.tsx
@@ -80,7 +80,7 @@ export default ({ ...props }: Props) => {
         <Formik
             onSubmit={search}
             validationSchema={object().shape({
-                term: string().min(3, 'Please enter at least three characters to begin searching.'),
+                term: string().min(3, 'Gib mindetens 3 Zeichen ein, um die Suche zu starten'),
             })}
             initialValues={{ term: '' } as Values}
         >
@@ -89,8 +89,8 @@ export default ({ ...props }: Props) => {
                     <Form>
                         <FormikFieldWrapper
                             name={'term'}
-                            label={'Search term'}
-                            description={'Enter a server name, uuid, or allocation to begin searching.'}
+                            label={'Serversuche'}
+                            description={'Gib den Servernamen, die UUID oder den zugewiesenen Port ein.'}
                         >
                             <SearchWatcher />
                             <InputSpinner visible={isSubmitting}>
diff --git a/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx b/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx
index 0308b39..df22f9a 100644
--- a/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx
+++ b/resources/scripts/components/dashboard/ssh/AccountSSHContainer.tsx
@@ -12,6 +12,7 @@ import { faKey } from '@fortawesome/free-solid-svg-icons';
 import { format } from 'date-fns';
 import CreateSSHKeyForm from '@/components/dashboard/ssh/CreateSSHKeyForm';
 import DeleteSSHKeyButton from '@/components/dashboard/ssh/DeleteSSHKeyButton';
+import { de } from "date-fns/locale";
 
 export default () => {
     const { clearAndAddHttpError } = useFlashKey('account');
@@ -25,17 +26,17 @@ export default () => {
     }, [error]);
 
     return (
-        <PageContentBlock title={'SSH Keys'}>
+        <PageContentBlock title={'SSH-Schlüssel'}>
             <FlashMessageRender byKey={'account'} />
             <div css={tw`md:flex flex-nowrap my-10`}>
-                <ContentBox title={'Add SSH Key'} css={tw`flex-none w-full md:w-1/2`}>
+                <ContentBox title={'SSH-Schlüssel hinzufügen'} css={tw`flex-none w-full md:w-1/2`}>
                     <CreateSSHKeyForm />
                 </ContentBox>
-                <ContentBox title={'SSH Keys'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
+                <ContentBox title={'SSH Schlüssel'} css={tw`flex-1 overflow-hidden mt-8 md:mt-0 md:ml-8`}>
                     <SpinnerOverlay visible={!data && isValidating} />
                     {!data || !data.length ? (
                         <p css={tw`text-center text-sm`}>
-                            {!data ? 'Loading...' : 'No SSH Keys exist for this account.'}
+                            {!data ? 'Einen Moment...' : 'Du hast keine SSH-Schlüssel eingerichtet.'}
                         </p>
                     ) : (
                         data.map((key, index) => (
@@ -48,8 +49,8 @@ export default () => {
                                     <p css={tw`text-sm break-words font-medium`}>{key.name}</p>
                                     <p css={tw`text-xs mt-1 font-mono truncate`}>SHA256:{key.fingerprint}</p>
                                     <p css={tw`text-xs mt-1 text-neutral-300 uppercase`}>
-                                        Added on:&nbsp;
-                                        {format(key.createdAt, 'MMM do, yyyy HH:mm')}
+                                        Erstellt am:&nbsp;
+                                        {format(key.createdAt, 'do MMM yyyy, HH:mm', { locale: de })}
                                     </p>
                                 </div>
                                 <DeleteSSHKeyButton name={key.name} fingerprint={key.fingerprint} />
diff --git a/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx b/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx
index 4b4f39c..f0094e2 100644
--- a/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx
+++ b/resources/scripts/components/dashboard/ssh/CreateSSHKeyForm.tsx
@@ -48,18 +48,18 @@ export default () => {
                 {({ isSubmitting }) => (
                     <Form>
                         <SpinnerOverlay visible={isSubmitting} />
-                        <FormikFieldWrapper label={'SSH Key Name'} name={'name'} css={tw`mb-6`}>
+                        <FormikFieldWrapper label={'SSH-Schlüssel Name'} name={'name'} css={tw`mb-6`}>
                             <Field name={'name'} as={Input} />
                         </FormikFieldWrapper>
                         <FormikFieldWrapper
-                            label={'Public Key'}
+                            label={'Öffentlicher Schlüssel'}
                             name={'publicKey'}
-                            description={'Enter your public SSH key.'}
+                            description={'Gib deinen SSH-Key ein'}
                         >
                             <Field name={'publicKey'} as={CustomTextarea} />
                         </FormikFieldWrapper>
                         <div css={tw`flex justify-end mt-6`}>
-                            <Button>Save</Button>
+                            <Button>Speichern</Button>
                         </div>
                     </Form>
                 )}
diff --git a/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx b/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx
index bd9aaf0..1f2610a 100644
--- a/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx
+++ b/resources/scripts/components/dashboard/ssh/DeleteSSHKeyButton.tsx
@@ -28,12 +28,12 @@ export default ({ name, fingerprint }: { name: string; fingerprint: string }) =>
         <>
             <Dialog.Confirm
                 open={visible}
-                title={'Delete SSH Key'}
-                confirm={'Delete Key'}
+                title={'SSH-Schlüssel löschen'}
+                confirm={'Schlüssel löschen'}
                 onConfirmed={onClick}
                 onClose={() => setVisible(false)}
             >
-                Removing the <Code>{name}</Code> SSH key will invalidate its usage across the Panel.
+                Entfernt <Code>{name}</Code> vom Panel, der Schlüssel wird dann ungültig.
             </Dialog.Confirm>
             <button css={tw`ml-4 p-2 text-sm`} onClick={() => setVisible(true)}>
                 <FontAwesomeIcon
diff --git a/resources/scripts/components/elements/ConfirmationModal.tsx b/resources/scripts/components/elements/ConfirmationModal.tsx
index 52f9f9e..1a3a6df 100644
--- a/resources/scripts/components/elements/ConfirmationModal.tsx
+++ b/resources/scripts/components/elements/ConfirmationModal.tsx
@@ -20,7 +20,7 @@ const ConfirmationModal: React.FC<Props> = ({ title, children, buttonText, onCon
             <div css={tw`text-neutral-300`}>{children}</div>
             <div css={tw`flex flex-wrap items-center justify-end mt-8`}>
                 <Button isSecondary onClick={() => dismiss()} css={tw`w-full sm:w-auto border-transparent`}>
-                    Cancel
+                    Abbrechen
                 </Button>
                 <Button color={'red'} css={tw`w-full sm:w-auto mt-4 sm:mt-0 sm:ml-4`} onClick={() => onConfirmed()}>
                     {buttonText}
diff --git a/resources/scripts/components/elements/CopyOnClick.tsx b/resources/scripts/components/elements/CopyOnClick.tsx
index 431e9c8..0fecf91 100644
--- a/resources/scripts/components/elements/CopyOnClick.tsx
+++ b/resources/scripts/components/elements/CopyOnClick.tsx
@@ -51,8 +51,8 @@ const CopyOnClick = ({ text, showInNotification = true, children }: CopyOnClickP
                             <div className={'rounded-md py-3 px-4 text-gray-200 bg-neutral-600/95 shadow'}>
                                 <p>
                                     {showInNotification
-                                        ? `Copied "${String(text)}" to clipboard.`
-                                        : 'Copied text to clipboard.'}
+                                        ? `"${String(text)}" wurde in die Zwischenablage kopiert.`
+                                        : 'Der Text wurde in die Zwischenablage kopiert.'}
                                 </p>
                             </div>
                         </div>
diff --git a/resources/scripts/components/elements/ErrorBoundary.tsx b/resources/scripts/components/elements/ErrorBoundary.tsx
index c92d952..1da8ac7 100644
--- a/resources/scripts/components/elements/ErrorBoundary.tsx
+++ b/resources/scripts/components/elements/ErrorBoundary.tsx
@@ -27,7 +27,7 @@ class ErrorBoundary extends React.Component<{}, State> {
                 <div css={tw`flex items-center bg-neutral-900 rounded p-3 text-red-500`}>
                     <Icon icon={faExclamationTriangle} css={tw`h-4 w-auto mr-2`} />
                     <p css={tw`text-sm text-neutral-100`}>
-                        An error was encountered by the application while rendering this view. Try refreshing the page.
+                        Beim verarbeiten dieser Seite trat ein Fehler auf. Bitte lade die Seite neu.
                     </p>
                 </div>
             </div>
diff --git a/resources/scripts/components/elements/PageContentBlock.tsx b/resources/scripts/components/elements/PageContentBlock.tsx
index a4df195..cfa78f9 100644
--- a/resources/scripts/components/elements/PageContentBlock.tsx
+++ b/resources/scripts/components/elements/PageContentBlock.tsx
@@ -34,7 +34,16 @@ const PageContentBlock: React.FC<PageContentBlockProps> = ({ title, showFlashKey
                         >
                             Pterodactyl&reg;
                         </a>
-                        &nbsp;&copy; 2015 - {new Date().getFullYear()}
+                        &nbsp;&copy; 2015 - {new Date().getFullYear()} | Übersetzt von&nbsp;
+                        <a
+                            rel={'noopener nofollow noreferrer'}
+                            href={'https://germandactyl.de'}
+                            target={'_blank'}
+                            css={tw`no-underline text-neutral-500 hover:text-neutral-300`}
+                        >
+                            Pavl21
+                        </a>
+
                     </p>
                 </ContentContainer>
             </>
diff --git a/resources/scripts/components/elements/PermissionRoute.tsx b/resources/scripts/components/elements/PermissionRoute.tsx
index e7a46e5..82aede9 100644
--- a/resources/scripts/components/elements/PermissionRoute.tsx
+++ b/resources/scripts/components/elements/PermissionRoute.tsx
@@ -18,7 +18,7 @@ export default ({ permission, children, ...props }: Props) => (
                 matchAny
                 action={permission}
                 renderOnError={
-                    <ServerError title={'Access Denied'} message={'You do not have permission to access this page.'} />
+                    <ServerError title={'Ungenügende Rechte'} message={'Der Zugang zu dieser Seite wurde bei dir gesperrt.'} />
                 }
             >
                 {children}
diff --git a/resources/scripts/components/elements/ScreenBlock.tsx b/resources/scripts/components/elements/ScreenBlock.tsx
index 0b57e92..5c10ce3 100644
--- a/resources/scripts/components/elements/ScreenBlock.tsx
+++ b/resources/scripts/components/elements/ScreenBlock.tsx
@@ -69,14 +69,14 @@ type ServerErrorProps = (Omit<PropsWithBack, 'image' | 'title'> | Omit<PropsWith
 };
 
 const ServerError = ({ title, ...props }: ServerErrorProps) => (
-    <ScreenBlock title={title || 'Something went wrong'} image={ServerErrorSvg} {...props} />
+    <ScreenBlock title={title || 'Etwas ist schief gelaufen'} image={ServerErrorSvg} {...props} />
 );
 
 const NotFound = ({ title, message, onBack }: Partial<Pick<ScreenBlockProps, 'title' | 'message' | 'onBack'>>) => (
     <ScreenBlock
         title={title || '404'}
         image={NotFoundSvg}
-        message={message || 'The requested resource was not found.'}
+        message={message || 'Die angeforderte Seite wurde nicht gefunden.'}
         onBack={onBack}
     />
 );
diff --git a/resources/scripts/components/elements/activity/ActivityLogEntry.tsx b/resources/scripts/components/elements/activity/ActivityLogEntry.tsx
index 183b2ab..58e015b 100644
--- a/resources/scripts/components/elements/activity/ActivityLogEntry.tsx
+++ b/resources/scripts/components/elements/activity/ActivityLogEntry.tsx
@@ -11,6 +11,7 @@ import style from './style.module.css';
 import Avatar from '@/components/Avatar';
 import useLocationHash from '@/plugins/useLocationHash';
 import { getObjectKeys, isObject } from '@/lib/objects';
+import { de } from 'date-fns/locale';
 
 interface Props {
     activity: ActivityLog;
@@ -53,7 +54,7 @@ export default ({ activity, children }: Props) => {
             <div className={'col-span-10 sm:col-span-9 flex'}>
                 <div className={'flex-1 px-4 sm:px-0'}>
                     <div className={'flex items-center text-gray-50'}>
-                        <Tooltip placement={'top'} content={actor?.email || 'System User'}>
+                        <Tooltip placement={'top'} content={actor?.email || 'System Nutzer'}>
                             <span>{actor?.username || 'System'}</span>
                         </Tooltip>
                         <span className={'text-gray-400'}>&nbsp;&mdash;&nbsp;</span>
@@ -65,12 +66,12 @@ export default ({ activity, children }: Props) => {
                         </Link>
                         <div className={classNames(style.icons, 'group-hover:text-gray-300')}>
                             {activity.isApi && (
-                                <Tooltip placement={'top'} content={'Using API Key'}>
+                                <Tooltip placement={'top'} content={'API-Schlüssel verwendet'}>
                                     <TerminalIcon />
                                 </Tooltip>
                             )}
                             {activity.event.startsWith('server:sftp.') && (
-                                <Tooltip placement={'top'} content={'Using SFTP'}>
+                                <Tooltip placement={'top'} content={'SFTP verwendet'}>
                                     <FolderOpenIcon />
                                 </Tooltip>
                             )}
@@ -87,8 +88,8 @@ export default ({ activity, children }: Props) => {
                                 <span className={'text-gray-400'}>&nbsp;|&nbsp;</span>
                             </span>
                         )}
-                        <Tooltip placement={'right'} content={format(activity.timestamp, 'MMM do, yyyy H:mm:ss')}>
-                            <span>{formatDistanceToNowStrict(activity.timestamp, { addSuffix: true })}</span>
+                        <Tooltip placement={'right'} content={format(activity.timestamp, 'do MMM yyyy, H:mm:ss', {locale: de})}>
+                            <span>{formatDistanceToNowStrict(activity.timestamp, { locale: de, addSuffix: true })}</span>
                         </Tooltip>
                     </div>
                 </div>
diff --git a/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx b/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx
index f1460ac..1c15e53 100644
--- a/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx
+++ b/resources/scripts/components/elements/activity/ActivityLogMetaButton.tsx
@@ -8,7 +8,7 @@ export default ({ meta }: { meta: Record<string, unknown> }) => {
 
     return (
         <div className={'self-center md:px-4'}>
-            <Dialog open={open} onClose={() => setOpen(false)} hideCloseIcon title={'Metadata'}>
+            <Dialog open={open} onClose={() => setOpen(false)} hideCloseIcon title={'Metadaten'}>
                 <pre
                     className={
                         'bg-gray-900 rounded p-2 font-mono text-sm leading-relaxed overflow-x-scroll whitespace-pre-wrap'
@@ -17,11 +17,11 @@ export default ({ meta }: { meta: Record<string, unknown> }) => {
                     {JSON.stringify(meta, null, 2)}
                 </pre>
                 <Dialog.Footer>
-                    <Button.Text onClick={() => setOpen(false)}>Close</Button.Text>
+                    <Button.Text onClick={() => setOpen(false)}>Schliessen</Button.Text>
                 </Dialog.Footer>
             </Dialog>
             <button
-                aria-describedby={'View additional event metadata'}
+                aria-describedby={'Weitere Daten anzeigen'}
                 className={
                     'p-2 transition-colors duration-100 text-gray-400 group-hover:text-gray-300 group-hover:hover:text-gray-50'
                 }
diff --git a/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx b/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx
index bedfe4e..c7bd28b 100644
--- a/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx
+++ b/resources/scripts/components/elements/dialog/ConfirmationDialog.tsx
@@ -13,7 +13,7 @@ export default ({ confirm = 'Okay', children, onConfirmed, ...props }: Confirmat
         <Dialog {...props} description={typeof children === 'string' ? children : undefined}>
             {typeof children !== 'string' && children}
             <Dialog.Footer>
-                <Button.Text onClick={props.onClose}>Cancel</Button.Text>
+                <Button.Text onClick={props.onClose}>Abbrechen</Button.Text>
                 <Button.Danger onClick={onConfirmed}>{confirm}</Button.Danger>
             </Dialog.Footer>
         </Dialog>
diff --git a/resources/scripts/components/elements/table/PaginationFooter.tsx b/resources/scripts/components/elements/table/PaginationFooter.tsx
index 0484065..6defb0a 100644
--- a/resources/scripts/components/elements/table/PaginationFooter.tsx
+++ b/resources/scripts/components/elements/table/PaginationFooter.tsx
@@ -40,13 +40,13 @@ const PaginationFooter = ({ pagination, className, onPageSelect }: Props) => {
     return (
         <div className={classNames('flex items-center justify-between my-2', className)}>
             <p className={'text-sm text-neutral-500'}>
-                Showing&nbsp;
+                Zeige&nbsp;
                 <span className={'font-semibold text-neutral-400'}>
                     {Math.max(start, Math.min(pagination.total, 1))}
                 </span>
-                &nbsp;to&nbsp;
-                <span className={'font-semibold text-neutral-400'}>{end}</span> of&nbsp;
-                <span className={'font-semibold text-neutral-400'}>{pagination.total}</span> results.
+                &nbsp;von&nbsp;
+                <span className={'font-semibold text-neutral-400'}>{end}</span> mit insgesamt&nbsp;
+                <span className={'font-semibold text-neutral-400'}>{pagination.total}</span> Ergebnissen.
             </p>
             {pagination.totalPages > 1 && (
                 <div className={'flex space-x-1'}>
diff --git a/resources/scripts/components/server/ConflictStateRenderer.tsx b/resources/scripts/components/server/ConflictStateRenderer.tsx
index d090b73..b5aeb0d 100644
--- a/resources/scripts/components/server/ConflictStateRenderer.tsx
+++ b/resources/scripts/components/server/ConflictStateRenderer.tsx
@@ -14,30 +14,30 @@ export default () => {
 
     return status === 'installing' || status === 'install_failed' || status === 'reinstall_failed' ? (
         <ScreenBlock
-            title={'Running Installer'}
+            title={'Installation läuft'}
             image={ServerInstallSvg}
-            message={'Your server should be ready soon, please try again in a few minutes.'}
+            message={'Dein Server hat aktuell ein Installationsprozess. Das kann einige Minuten dauern.'}
         />
     ) : status === 'suspended' ? (
         <ScreenBlock
-            title={'Server Suspended'}
+            title={'Dein Server ist gesperrt'}
             image={ServerErrorSvg}
-            message={'This server is suspended and cannot be accessed.'}
+            message={'Der Zugang zum Server ist aktuell gesperrt. Melde dich beim Betreiber, um weitere Informationen zu erhalten.'}
         />
     ) : isNodeUnderMaintenance ? (
         <ScreenBlock
-            title={'Node under Maintenance'}
+            title={'Node ist offline'}
             image={ServerErrorSvg}
-            message={'The node of this server is currently under maintenance.'}
+            message={'Die Node, auf den dein Server läuft, ist aktuell in Wartungsarbeiten, dein Server steht daher gerade nicht zur Verfügung.'}
         />
     ) : (
         <ScreenBlock
-            title={isTransferring ? 'Transferring' : 'Restoring from Backup'}
+            title={isTransferring ? 'Transfer läuft...' : 'Backup wiederherstellen...'}
             image={ServerRestoreSvg}
             message={
                 isTransferring
-                    ? 'Your server is being transferred to a new node, please check back later.'
-                    : 'Your server is currently being restored from a backup, please check back in a few minutes.'
+                    ? 'Dein Server wird aktuell auf den neuen Node übertragen, das kann etwas dauern.'
+                    : 'Dein Server wird aktuell mit einem Backup wiederhergestellt. Das kann je nach Größe des Servers etwas dauern.'
             }
         />
     );
diff --git a/resources/scripts/components/server/ServerActivityLogContainer.tsx b/resources/scripts/components/server/ServerActivityLogContainer.tsx
index 5319454..f7eaac1 100644
--- a/resources/scripts/components/server/ServerActivityLogContainer.tsx
+++ b/resources/scripts/components/server/ServerActivityLogContainer.tsx
@@ -32,7 +32,7 @@ export default () => {
     }, [error]);
 
     return (
-        <ServerContentBlock title={'Activity Log'}>
+        <ServerContentBlock title={'Alle Aktivitäten'}>
             <FlashMessageRender byKey={'server:activity'} />
             {(filters.filters?.event || filters.filters?.ip) && (
                 <div className={'flex justify-end mb-2'}>
@@ -41,14 +41,14 @@ export default () => {
                         className={classNames(btnStyles.button, btnStyles.text, 'w-full sm:w-auto')}
                         onClick={() => setFilters((value) => ({ ...value, filters: {} }))}
                     >
-                        Clear Filters <XCircleIcon className={'w-4 h-4 ml-2'} />
+                        Filter lösen <XCircleIcon className={'w-4 h-4 ml-2'} />
                     </Link>
                 </div>
             )}
             {!data && isValidating ? (
                 <Spinner centered />
             ) : !data?.items.length ? (
-                <p className={'text-sm text-center text-gray-400'}>No activity logs available for this server.</p>
+                <p className={'text-sm text-center text-gray-400'}>Hier werden alle Aktivitäten angezeigt.</p>
             ) : (
                 <div className={'bg-gray-700'}>
                     {data?.items.map((activity) => (
diff --git a/resources/scripts/components/server/UptimeDuration.tsx b/resources/scripts/components/server/UptimeDuration.tsx
index 6623de0..d54717e 100644
--- a/resources/scripts/components/server/UptimeDuration.tsx
+++ b/resources/scripts/components/server/UptimeDuration.tsx
@@ -10,14 +10,14 @@ export default ({ uptime }: { uptime: number }) => {
     if (days > 0) {
         return (
             <>
-                {days}d {hours}h {minutes}m
+                {days}T {hours}Std. {minutes}min.
             </>
         );
     }
 
     return (
         <>
-            {hours}h {minutes}m {seconds}s
+            {hours}Std. {minutes}min. {seconds}sek.
         </>
     );
 };
diff --git a/resources/scripts/components/server/WebsocketHandler.tsx b/resources/scripts/components/server/WebsocketHandler.tsx
index 2772013..47a14f7 100644
--- a/resources/scripts/components/server/WebsocketHandler.tsx
+++ b/resources/scripts/components/server/WebsocketHandler.tsx
@@ -54,7 +54,7 @@ export default () => {
                 updateToken(uuid, socket);
             } else {
                 setError(
-                    'There was an error validating the credentials provided for the websocket. Please refresh the page.'
+                    'Beim Anmelden ist ein Fehler aufgetreten. Bitte lade die Seite neu.'
                 );
             }
         });
@@ -112,7 +112,7 @@ export default () => {
                         <>
                             <Spinner size={'small'} />
                             <p css={tw`ml-2 text-sm text-red-100`}>
-                                We&apos;re having some trouble connecting to your server, please wait...
+                                Aktuell gibt es Probleme, den Node-Server zu erreichen. Wir versuchen es weiter...
                             </p>
                         </>
                     ) : (
diff --git a/resources/scripts/components/server/backups/BackupContainer.tsx b/resources/scripts/components/server/backups/BackupContainer.tsx
index 0134142..8d74854 100644
--- a/resources/scripts/components/server/backups/BackupContainer.tsx
+++ b/resources/scripts/components/server/backups/BackupContainer.tsx
@@ -43,8 +43,8 @@ const BackupContainer = () => {
                         !backupLimit ? null : (
                             <p css={tw`text-center text-sm text-neutral-300`}>
                                 {page > 1
-                                    ? "Looks like we've run out of backups to show you, try going back a page."
-                                    : 'It looks like there are no backups currently stored for this server.'}
+                                    ? "Anscheinend gibt es gerade keine Backups, die verfügbar sind. Versuch es später nochmal."
+                                    : 'Aktuell gibt es keine Backups für diesen Server'}
                             </p>
                         )
                     ) : (
@@ -56,14 +56,14 @@ const BackupContainer = () => {
             </Pagination>
             {backupLimit === 0 && (
                 <p css={tw`text-center text-sm text-neutral-300`}>
-                    Backups cannot be created for this server because the backup limit is set to 0.
+                    Aktuell wird nicht erlaubt, das Backups für diesen Server erstellt werden dürfen.
                 </p>
             )}
             <Can action={'backup.create'}>
                 <div css={tw`mt-6 sm:flex items-center justify-end`}>
                     {backupLimit > 0 && backups.backupCount > 0 && (
                         <p css={tw`text-sm text-neutral-300 mb-4 sm:mr-6 sm:mb-0`}>
-                            {backups.backupCount} of {backupLimit} backups have been created for this server.
+                            {backups.backupCount} Backups sind verfügbar ({backupLimit} ist Maximum)
                         </p>
                     )}
                     {backupLimit > 0 && backupLimit > backups.backupCount && (
diff --git a/resources/scripts/components/server/backups/BackupContextMenu.tsx b/resources/scripts/components/server/backups/BackupContextMenu.tsx
index 43c8b35..374cc85 100644
--- a/resources/scripts/components/server/backups/BackupContextMenu.tsx
+++ b/resources/scripts/components/server/backups/BackupContextMenu.tsx
@@ -122,21 +122,22 @@ export default ({ backup }: Props) => {
             <Dialog.Confirm
                 open={modal === 'unlock'}
                 onClose={() => setModal('')}
-                title={`Unlock "${backup.name}"`}
+                title={`Wirklich "${backup.name}" entsperren?`}
                 onConfirmed={onLockToggle}
             >
-                This backup will no longer be protected from automated or accidental deletions.
+                Wenn du dies freigibst, wird das Backup nicht mehr vor versehntlicher Löschung geschützt.
             </Dialog.Confirm>
             <Dialog.Confirm
                 open={modal === 'restore'}
                 onClose={() => setModal('')}
-                confirm={'Restore'}
-                title={`Restore "${backup.name}"`}
+                confirm={'Wiederherstellen'}
+                title={`Backup "${backup.name}" anwenden?`}
                 onConfirmed={() => doRestorationAction()}
             >
                 <p>
-                    Your server will be stopped. You will not be able to control the power state, access the file
-                    manager, or create additional backups until completed.
+                    Dein Server wird dabei gestoppt! Im laufenden Prozess hast du keine Rechte, 
+					um etwas am Server zu tun. Wenn du auf Nummer sicher gehen möchtest, erstelle vorher ein Backup, 
+					falls diese Wiederherstellung schief geht.
                 </p>
                 <p css={tw`mt-4 -mb-2 bg-gray-700 p-3 rounded`}>
                     <label htmlFor={'restore_truncate'} css={tw`text-base flex items-center cursor-pointer`}>
@@ -148,18 +149,18 @@ export default ({ backup }: Props) => {
                             checked={truncate}
                             onChange={() => setTruncate((s) => !s)}
                         />
-                        Delete all files before restoring backup.
+                        Alle Daten vor dem Backup löschen
                     </label>
                 </p>
             </Dialog.Confirm>
             <Dialog.Confirm
-                title={`Delete "${backup.name}"`}
-                confirm={'Continue'}
+                title={`Backup "${backup.name}" löschen?`}
+                confirm={'Bestätigen'}
                 open={modal === 'delete'}
                 onClose={() => setModal('')}
                 onConfirmed={doDeletion}
             >
-                This is a permanent operation. The backup cannot be recovered once deleted.
+                Das Backup kann danach nicht wiederhergestellt werden. Tue dies mit bedacht!
             </Dialog.Confirm>
             <SpinnerOverlay visible={loading} fixed />
             {backup.isSuccessful ? (
@@ -177,13 +178,13 @@ export default ({ backup }: Props) => {
                         <Can action={'backup.download'}>
                             <DropdownButtonRow onClick={doDownload}>
                                 <FontAwesomeIcon fixedWidth icon={faCloudDownloadAlt} css={tw`text-xs`} />
-                                <span css={tw`ml-2`}>Download</span>
+                                <span css={tw`ml-2`}>Herunterladen</span>
                             </DropdownButtonRow>
                         </Can>
                         <Can action={'backup.restore'}>
                             <DropdownButtonRow onClick={() => setModal('restore')}>
                                 <FontAwesomeIcon fixedWidth icon={faBoxOpen} css={tw`text-xs`} />
-                                <span css={tw`ml-2`}>Restore</span>
+                                <span css={tw`ml-2`}>Wiederherstellen</span>
                             </DropdownButtonRow>
                         </Can>
                         <Can action={'backup.delete'}>
@@ -194,12 +195,12 @@ export default ({ backup }: Props) => {
                                         icon={backup.isLocked ? faUnlock : faLock}
                                         css={tw`text-xs mr-2`}
                                     />
-                                    {backup.isLocked ? 'Unlock' : 'Lock'}
+                                    {backup.isLocked ? 'Entsperren' : 'Sperren'}
                                 </DropdownButtonRow>
                                 {!backup.isLocked && (
                                     <DropdownButtonRow danger onClick={() => setModal('delete')}>
                                         <FontAwesomeIcon fixedWidth icon={faTrashAlt} css={tw`text-xs`} />
-                                        <span css={tw`ml-2`}>Delete</span>
+                                        <span css={tw`ml-2`}>Löschen</span>
                                     </DropdownButtonRow>
                                 )}
                             </>
diff --git a/resources/scripts/components/server/backups/BackupRow.tsx b/resources/scripts/components/server/backups/BackupRow.tsx
index f59f472..12aea65 100644
--- a/resources/scripts/components/server/backups/BackupRow.tsx
+++ b/resources/scripts/components/server/backups/BackupRow.tsx
@@ -12,6 +12,7 @@ import GreyRowBox from '@/components/elements/GreyRowBox';
 import getServerBackups from '@/api/swr/getServerBackups';
 import { ServerBackup } from '@/api/server/types';
 import { SocketEvent } from '@/components/server/events';
+import { de } from "date-fns/locale";
 
 interface Props {
     backup: ServerBackup;
@@ -67,7 +68,7 @@ export default ({ backup, className }: Props) => {
                             <span
                                 css={tw`bg-red-500 py-px px-2 rounded-full text-white text-xs uppercase border border-red-600 mr-2`}
                             >
-                                Failed
+                                FEHLER
                             </span>
                         )}
                         <p css={tw`break-words truncate`}>{backup.name}</p>
@@ -81,10 +82,10 @@ export default ({ backup, className }: Props) => {
                 </div>
             </div>
             <div css={tw`flex-1 md:flex-none md:w-48 mt-4 md:mt-0 md:ml-8 md:text-center`}>
-                <p title={format(backup.createdAt, 'ddd, MMMM do, yyyy HH:mm:ss')} css={tw`text-sm`}>
-                    {formatDistanceToNow(backup.createdAt, { includeSeconds: true, addSuffix: true })}
+                <p title={format(backup.createdAt, 'do MMMM yyyy ,HH:mm:ss', { locale: de })} css={tw`text-sm`}>
+                    {formatDistanceToNow(backup.createdAt, { locale: de, includeSeconds: true, addSuffix: true })}
                 </p>
-                <p css={tw`text-2xs text-neutral-500 uppercase mt-1`}>Created</p>
+                <p css={tw`text-2xs text-neutral-500 uppercase mt-1`}>Erstellt</p>
             </div>
             <Can action={['backup.download', 'backup.restore', 'backup.delete']} matchAny>
                 <div css={tw`mt-4 md:mt-0 ml-6`} style={{ marginRight: '-0.5rem' }}>
diff --git a/resources/scripts/components/server/backups/CreateBackupButton.tsx b/resources/scripts/components/server/backups/CreateBackupButton.tsx
index fef0150..6884837 100644
--- a/resources/scripts/components/server/backups/CreateBackupButton.tsx
+++ b/resources/scripts/components/server/backups/CreateBackupButton.tsx
@@ -28,21 +28,21 @@ const ModalContent = ({ ...props }: RequiredModalProps) => {
         <Modal {...props} showSpinnerOverlay={isSubmitting}>
             <Form>
                 <FlashMessageRender byKey={'backups:create'} css={tw`mb-4`} />
-                <h2 css={tw`text-2xl mb-6`}>Create server backup</h2>
+                <h2 css={tw`text-2xl mb-6`}>Backup erstellen</h2>
                 <Field
                     name={'name'}
-                    label={'Backup name'}
-                    description={'If provided, the name that should be used to reference this backup.'}
+                    label={'Name des Backups'}
+                    description={'Wenn du es leer lässt, wird ein Zeitstempel als Name genutzt'}
                 />
                 <div css={tw`mt-6`}>
                     <FormikFieldWrapper
                         name={'ignored'}
-                        label={'Ignored Files & Directories'}
+                        label={'Dateien & Ordner ingorieren'}
                         description={`
-                            Enter the files or folders to ignore while generating this backup. Leave blank to use
-                            the contents of the .pteroignore file in the root of the server directory if present.
-                            Wildcard matching of files and folders is supported in addition to negating a rule by
-                            prefixing the path with an exclamation point.
+                            Gib die Dateien oder Ordner ein, die beim Erstellen dieser Sicherung ignoriert werden sollen. Kein Pflichtfeld.
+                            den Inhalt der Datei .pteroignore im Stammverzeichnis des Serververzeichnisses, falls vorhanden.
+                            Der Wildcard-Abgleich von Dateien und Ordnern wird zusätzlich zum Negieren einer Regel durch unterstützt
+                            dem Pfad ein Ausrufezeichen voranstellen.
                         `}
                     >
                         <FormikField as={Textarea} name={'ignored'} rows={6} />
@@ -52,14 +52,14 @@ const ModalContent = ({ ...props }: RequiredModalProps) => {
                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
                         <FormikSwitch
                             name={'isLocked'}
-                            label={'Locked'}
-                            description={'Prevents this backup from being deleted until explicitly unlocked.'}
+                            label={'Backup schützen'}
+                            description={'Schützt das Backup vor versehentlichen Löschen, dies kann später deaktiviert werden.'}
                         />
                     </div>
                 </Can>
                 <div css={tw`flex justify-end mt-6`}>
                     <Button type={'submit'} disabled={isSubmitting}>
-                        Start backup
+                        Backup erstellen
                     </Button>
                 </div>
             </Form>
@@ -109,7 +109,7 @@ export default () => {
                 </Formik>
             )}
             <Button css={tw`w-full sm:w-auto`} onClick={() => setVisible(true)}>
-                Create backup
+                Backup erstellen
             </Button>
         </>
     );
diff --git a/resources/scripts/components/server/console/Console.tsx b/resources/scripts/components/server/console/Console.tsx
index 25f0743..ef815ba 100644
--- a/resources/scripts/components/server/console/Console.tsx
+++ b/resources/scripts/components/server/console/Console.tsx
@@ -52,7 +52,7 @@ const terminalProps: ITerminalOptions = {
 };
 
 export default () => {
-    const TERMINAL_PRELUDE = '\u001b[1m\u001b[33mcontainer@pterodactyl~ \u001b[0m';
+    const TERMINAL_PRELUDE = '\u001b[1m\u001b[33mcontainer@server-verwaltung~ \u001b[0m';
     const ref = useRef<HTMLDivElement>(null);
     const terminal = useMemo(() => new Terminal({ ...terminalProps }), []);
     const fitAddon = new FitAddon();
@@ -79,7 +79,7 @@ export default () => {
         switch (status) {
             // Sent by either the source or target node if a failure occurs.
             case 'failure':
-                terminal.writeln(TERMINAL_PRELUDE + 'Transfer has failed.\u001b[0m');
+                terminal.writeln(TERMINAL_PRELUDE + 'Transfer ist fehlgeschlagen.\u001b[0m');
                 return;
         }
     };
@@ -90,7 +90,7 @@ export default () => {
         );
 
     const handlePowerChangeEvent = (state: string) =>
-        terminal.writeln(TERMINAL_PRELUDE + 'Server marked as ' + state + '...\u001b[0m');
+        terminal.writeln(TERMINAL_PRELUDE + 'Server meldet den Status ' + state + '...\u001b[0m');
 
     const handleCommandKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
         if (e.key === 'ArrowUp') {
@@ -206,8 +206,8 @@ export default () => {
                     <input
                         className={classNames('peer', styles.command_input)}
                         type={'text'}
-                        placeholder={'Type a command...'}
-                        aria-label={'Console command input.'}
+                        placeholder={'Befehl eingeben...'}
+                        aria-label={'Die Macht ist so fern, und doch so nah...'}
                         disabled={!instance || !connected}
                         onKeyDown={handleCommandKeyDown}
                         autoCorrect={'off'}
diff --git a/resources/scripts/components/server/console/PowerButtons.tsx b/resources/scripts/components/server/console/PowerButtons.tsx
index f5f8189..bed4591 100644
--- a/resources/scripts/components/server/console/PowerButtons.tsx
+++ b/resources/scripts/components/server/console/PowerButtons.tsx
@@ -42,11 +42,11 @@ export default ({ className }: PowerButtonProps) => {
                 open={open}
                 hideCloseIcon
                 onClose={() => setOpen(false)}
-                title={'Forcibly Stop Process'}
-                confirm={'Continue'}
+                title={'WARNUNG'}
+                confirm={'Fortfahren'}
                 onConfirmed={onButtonClick.bind(this, 'kill-confirmed')}
             >
-                Forcibly stopping a server can lead to data corruption.
+                Das erzwingen kann Daten zerstören, da in diesem Vorgang im Grunde der Stecker gezogen wird, wo der Server überhaupt nicht mit rechnet! Es können Daten verloren gehen.
             </Dialog.Confirm>
             <Can action={'control.start'}>
                 <Button
@@ -54,12 +54,12 @@ export default ({ className }: PowerButtonProps) => {
                     disabled={status !== 'offline'}
                     onClick={onButtonClick.bind(this, 'start')}
                 >
-                    Start
+                    Starten
                 </Button>
             </Can>
             <Can action={'control.restart'}>
                 <Button.Text className={'flex-1'} disabled={!status} onClick={onButtonClick.bind(this, 'restart')}>
-                    Restart
+                    Neustart
                 </Button.Text>
             </Can>
             <Can action={'control.stop'}>
@@ -68,7 +68,7 @@ export default ({ className }: PowerButtonProps) => {
                     disabled={status === 'offline'}
                     onClick={onButtonClick.bind(this, killable ? 'kill' : 'stop')}
                 >
-                    {killable ? 'Kill' : 'Stop'}
+                    {killable ? 'Stop erzwingen' : 'Stoppen'}
                 </Button.Danger>
             </Can>
         </div>
diff --git a/resources/scripts/components/server/console/ServerConsoleContainer.tsx b/resources/scripts/components/server/console/ServerConsoleContainer.tsx
index c3af78f..ea82c34 100644
--- a/resources/scripts/components/server/console/ServerConsoleContainer.tsx
+++ b/resources/scripts/components/server/console/ServerConsoleContainer.tsx
@@ -22,14 +22,14 @@ const ServerConsoleContainer = () => {
     const isNodeUnderMaintenance = ServerContext.useStoreState((state) => state.server.data!.isNodeUnderMaintenance);
 
     return (
-        <ServerContentBlock title={'Console'}>
+        <ServerContentBlock title={'Konsole'}>
             {(isNodeUnderMaintenance || isInstalling || isTransferring) && (
                 <Alert type={'warning'} className={'mb-4'}>
                     {isNodeUnderMaintenance
-                        ? 'The node of this server is currently under maintenance and all actions are unavailable.'
+                        ? 'Der Node, mit dem dein Server verbunden ist, befindet sich aktuell in Wartung. Dein Server bleibt solange offline.'
                         : isInstalling
-                        ? 'This server is currently running its installation process and most actions are unavailable.'
-                        : 'This server is currently being transferred to another node and all actions are unavailable.'}
+                        ? 'Dein Server hat gerade einen aktiven Installationsprozess. In der Zeit sind einige Aktionen nicht verfügbar.'
+                        : 'Dein Server wird gerade auf einen anderen Node übertragen, das kann etwas dauern. In der Zeit kannst du nichts tun.'}
                 </Alert>
             )}
             <div className={'grid grid-cols-4 gap-4 mb-4'}>
diff --git a/resources/scripts/components/server/console/ServerDetailsBlock.tsx b/resources/scripts/components/server/console/ServerDetailsBlock.tsx
index a3a0096..872208e 100644
--- a/resources/scripts/components/server/console/ServerDetailsBlock.tsx
+++ b/resources/scripts/components/server/console/ServerDetailsBlock.tsx
@@ -90,23 +90,25 @@ const ServerDetailsBlock = ({ className }: { className?: string }) => {
 
     return (
         <div className={classNames('grid grid-cols-6 gap-2 md:gap-4', className)}>
-            <StatBlock icon={faWifi} title={'Address'} copyOnClick={allocation}>
+            <StatBlock icon={faWifi} title={'Server-Zugang'} copyOnClick={allocation}>
                 {allocation}
             </StatBlock>
             <StatBlock
                 icon={faClock}
-                title={'Uptime'}
+                title={'Laufzeit'}
                 color={getBackgroundColor(status === 'running' ? 0 : status !== 'offline' ? 9 : 10, 10)}
             >
                 {status === null ? (
                     'Offline'
                 ) : stats.uptime > 0 ? (
                     <UptimeDuration uptime={stats.uptime / 1000} />
+                ) : status === 'starting' ? (
+                    'Startet'
                 ) : (
                     capitalize(status)
                 )}
             </StatBlock>
-            <StatBlock icon={faMicrochip} title={'CPU Load'} color={getBackgroundColor(stats.cpu, limits.cpu)}>
+            <StatBlock icon={faMicrochip} title={'CPU-Auslastung'} color={getBackgroundColor(stats.cpu, limits.cpu)}>
                 {status === 'offline' ? (
                     <span className={'text-gray-400'}>Offline</span>
                 ) : (
@@ -115,7 +117,7 @@ const ServerDetailsBlock = ({ className }: { className?: string }) => {
             </StatBlock>
             <StatBlock
                 icon={faMemory}
-                title={'Memory'}
+                title={'Arbeitsspeicher'}
                 color={getBackgroundColor(stats.memory / 1024, limits.memory * 1024)}
             >
                 {status === 'offline' ? (
@@ -124,13 +126,13 @@ const ServerDetailsBlock = ({ className }: { className?: string }) => {
                     <Limit limit={textLimits.memory}>{bytesToString(stats.memory)}</Limit>
                 )}
             </StatBlock>
-            <StatBlock icon={faHdd} title={'Disk'} color={getBackgroundColor(stats.disk / 1024, limits.disk * 1024)}>
+            <StatBlock icon={faHdd} title={'Speicher'} color={getBackgroundColor(stats.disk / 1024, limits.disk * 1024)}>
                 <Limit limit={textLimits.disk}>{bytesToString(stats.disk)}</Limit>
             </StatBlock>
-            <StatBlock icon={faCloudDownloadAlt} title={'Network (Inbound)'}>
+            <StatBlock icon={faCloudDownloadAlt} title={'Traffic (Eingehend)'}>
                 {status === 'offline' ? <span className={'text-gray-400'}>Offline</span> : bytesToString(stats.rx)}
             </StatBlock>
-            <StatBlock icon={faCloudUploadAlt} title={'Network (Outbound)'}>
+            <StatBlock icon={faCloudUploadAlt} title={'Traffic (Ausgehend)'}>
                 {status === 'offline' ? <span className={'text-gray-400'}>Offline</span> : bytesToString(stats.tx)}
             </StatBlock>
         </div>
diff --git a/resources/scripts/components/server/console/StatGraphs.tsx b/resources/scripts/components/server/console/StatGraphs.tsx
index 9af405b..237fc6f 100644
--- a/resources/scripts/components/server/console/StatGraphs.tsx
+++ b/resources/scripts/components/server/console/StatGraphs.tsx
@@ -17,8 +17,8 @@ export default () => {
     const previous = useRef<Record<'tx' | 'rx', number>>({ tx: -1, rx: -1 });
 
     const cpu = useChartTickLabel('CPU', limits.cpu, '%', 2);
-    const memory = useChartTickLabel('Memory', limits.memory, 'MiB');
-    const network = useChart('Network', {
+    const memory = useChartTickLabel('Arbeitsspeicher', limits.memory, 'MiB');
+    const network = useChart('Traffic', {
         sets: 2,
         options: {
             scales: {
@@ -34,7 +34,7 @@ export default () => {
         callback(opts, index) {
             return {
                 ...opts,
-                label: !index ? 'Network In' : 'Network Out',
+                label: !index ? 'Traffic (Eingehend)' : 'Traffic (Ausgehend)',
                 borderColor: !index ? theme('colors.cyan.400') : theme('colors.yellow.400'),
                 backgroundColor: hexToRgba(!index ? theme('colors.cyan.700') : theme('colors.yellow.700'), 0.5),
             };
@@ -68,20 +68,20 @@ export default () => {
 
     return (
         <>
-            <ChartBlock title={'CPU Load'}>
+            <ChartBlock title={'CPU-Auslastung'}>
                 <Line {...cpu.props} />
             </ChartBlock>
-            <ChartBlock title={'Memory'}>
+            <ChartBlock title={'Arbeitsspeicher'}>
                 <Line {...memory.props} />
             </ChartBlock>
             <ChartBlock
-                title={'Network'}
+                title={'Traffic'}
                 legend={
                     <>
-                        <Tooltip arrow content={'Inbound'}>
+                        <Tooltip arrow content={'Eingehend'}>
                             <CloudDownloadIcon className={'mr-2 w-4 h-4 text-yellow-400'} />
                         </Tooltip>
-                        <Tooltip arrow content={'Outbound'}>
+                        <Tooltip arrow content={'Ausgehend'}>
                             <CloudUploadIcon className={'w-4 h-4 text-cyan-400'} />
                         </Tooltip>
                     </>
diff --git a/resources/scripts/components/server/databases/CreateDatabaseButton.tsx b/resources/scripts/components/server/databases/CreateDatabaseButton.tsx
index 24c19e0..02f59a7 100644
--- a/resources/scripts/components/server/databases/CreateDatabaseButton.tsx
+++ b/resources/scripts/components/server/databases/CreateDatabaseButton.tsx
@@ -18,14 +18,14 @@ interface Values {
 
 const schema = object().shape({
     databaseName: string()
-        .required('A database name must be provided.')
-        .min(3, 'Database name must be at least 3 characters.')
-        .max(48, 'Database name must not exceed 48 characters.')
+        .required('Die Erstellung der Datenbank erfordert einen Namen.')
+        .min(3, 'Es werden mindestens 3 Zeichen benötigt')
+        .max(48, 'Es sind maximal 48 Zeichen erlaubt.')
         .matches(
             /^[\w\-.]{3,48}$/,
-            'Database name should only contain alphanumeric characters, underscores, dashes, and/or periods.'
+            'Der Name darf nur alphanummerische Zeichen, Unterstriche, Bindestriche oder Punkte enthalten.'
         ),
-    connectionsFrom: string().matches(/^[\w\-/.%:]+$/, 'A valid host address must be provided.'),
+    connectionsFrom: string().matches(/^[\w\-/.%:]+$/, 'Es muss eine gültige Hostadresse angegeben werden.'),
 });
 
 export default () => {
@@ -69,23 +69,23 @@ export default () => {
                         }}
                     >
                         <FlashMessageRender byKey={'database:create'} css={tw`mb-6`} />
-                        <h2 css={tw`text-2xl mb-6`}>Create new database</h2>
+                        <h2 css={tw`text-2xl mb-6`}>Datenbank erstellen</h2>
                         <Form css={tw`m-0`}>
                             <Field
                                 type={'string'}
                                 id={'database_name'}
                                 name={'databaseName'}
-                                label={'Database Name'}
-                                description={'A descriptive name for your database instance.'}
+                                label={'Name der Datenbank'}
+                                description={'Eine kurze Beschreibung in ein bis zwei Wörtern (am besten), für was die Datenbank gedacht ist.'}
                             />
                             <div css={tw`mt-6`}>
                                 <Field
                                     type={'string'}
                                     id={'connections_from'}
                                     name={'connectionsFrom'}
-                                    label={'Connections From'}
+                                    label={'Verbindungen beschränkt erlauben'}
                                     description={
-                                        'Where connections should be allowed from. Leave blank to allow connections from anywhere.'
+                                        'Gibt vor, von wo Verbindungen zugelassen werden dürfen. Lasse das Feld leer, um alle Verbindungen zuzulassen.'
                                     }
                                 />
                             </div>
@@ -96,17 +96,17 @@ export default () => {
                                     css={tw`w-full sm:w-auto sm:mr-2`}
                                     onClick={() => setVisible(false)}
                                 >
-                                    Cancel
+                                    Abbrechen
                                 </Button>
                                 <Button css={tw`w-full mt-4 sm:w-auto sm:mt-0`} type={'submit'}>
-                                    Create Database
+                                    Datenbank erstellen
                                 </Button>
                             </div>
                         </Form>
                     </Modal>
                 )}
             </Formik>
-            <Button onClick={() => setVisible(true)}>New Database</Button>
+            <Button onClick={() => setVisible(true)}>Neue Datenbank</Button>
         </>
     );
 };
diff --git a/resources/scripts/components/server/databases/DatabaseRow.tsx b/resources/scripts/components/server/databases/DatabaseRow.tsx
index 9d2c05a..807eccb 100644
--- a/resources/scripts/components/server/databases/DatabaseRow.tsx
+++ b/resources/scripts/components/server/databases/DatabaseRow.tsx
@@ -40,8 +40,8 @@ export default ({ database, className }: Props) => {
 
     const schema = object().shape({
         confirm: string()
-            .required('The database name must be provided.')
-            .oneOf([database.name.split('_', 2)[1], database.name], 'The database name must be provided.'),
+            .required('Gib einen Namen für die Datenbank ein.')
+            .oneOf([database.name.split('_', 2)[1], database.name], 'Name der Datenbank fehlt.'),
     });
 
     const submit = (values: { confirm: string }, { setSubmitting }: FormikHelpers<{ confirm: string }>) => {
@@ -72,25 +72,25 @@ export default ({ database, className }: Props) => {
                         }}
                     >
                         <FlashMessageRender byKey={'database:delete'} css={tw`mb-6`} />
-                        <h2 css={tw`text-2xl mb-6`}>Confirm database deletion</h2>
+                        <h2 css={tw`text-2xl mb-6`}>Datenbank löschen?</h2>
                         <p css={tw`text-sm`}>
-                            Deleting a database is a permanent action, it cannot be undone. This will permanently delete
-                            the <strong>{database.name}</strong> database and remove all associated data.
+                            Dadurch wird die Datenbank  <strong>{database.name}</strong>  unwiderruflich gelöscht. Bitte beachte, welche Plugins etc. mit der Datenbank verbunden sind!
+							Trenne vor dem Löschen der Datenbank alle Verknüpfungen.
                         </p>
                         <Form css={tw`m-0 mt-6`}>
                             <Field
                                 type={'text'}
                                 id={'confirm_name'}
                                 name={'confirm'}
-                                label={'Confirm Database Name'}
-                                description={'Enter the database name to confirm deletion.'}
+                                label={'Bestätige den Namen'}
+                                description={'Gib den Namen der Datenbank ein, um die Löschung zu bestätigen'}
                             />
                             <div css={tw`mt-6 text-right`}>
                                 <Button type={'button'} isSecondary css={tw`mr-2`} onClick={() => setVisible(false)}>
-                                    Cancel
+                                    Abbrechen
                                 </Button>
                                 <Button type={'submit'} color={'red'} disabled={!isValid}>
-                                    Delete Database
+                                    Datenbank löschen
                                 </Button>
                             </div>
                         </Form>
@@ -99,33 +99,33 @@ export default ({ database, className }: Props) => {
             </Formik>
             <Modal visible={connectionVisible} onDismissed={() => setConnectionVisible(false)}>
                 <FlashMessageRender byKey={'database-connection-modal'} css={tw`mb-6`} />
-                <h3 css={tw`mb-6 text-2xl`}>Database connection details</h3>
+                <h3 css={tw`mb-6 text-2xl`}>Details der Datenbank</h3>
                 <div>
-                    <Label>Endpoint</Label>
+                    <Label>Zugang</Label>
                     <CopyOnClick text={database.connectionString}>
                         <Input type={'text'} readOnly value={database.connectionString} />
                     </CopyOnClick>
                 </div>
                 <div css={tw`mt-6`}>
-                    <Label>Connections from</Label>
+                    <Label>Verbindungen von</Label>
                     <Input type={'text'} readOnly value={database.allowConnectionsFrom} />
                 </div>
                 <div css={tw`mt-6`}>
-                    <Label>Username</Label>
+                    <Label>Benutzername</Label>
                     <CopyOnClick text={database.username}>
                         <Input type={'text'} readOnly value={database.username} />
                     </CopyOnClick>
                 </div>
                 <Can action={'database.view_password'}>
                     <div css={tw`mt-6`}>
-                        <Label>Password</Label>
+                        <Label>Passwort</Label>
                         <CopyOnClick text={database.password}>
                             <Input type={'text'} readOnly value={database.password} />
                         </CopyOnClick>
                     </div>
                 </Can>
                 <div css={tw`mt-6`}>
-                    <Label>JDBC Connection String</Label>
+                    <Label>JDBC Verbindungen</Label>
                     <CopyOnClick text={jdbcConnectionString}>
                         <Input type={'text'} readOnly value={jdbcConnectionString} />
                     </CopyOnClick>
@@ -135,7 +135,7 @@ export default ({ database, className }: Props) => {
                         <RotatePasswordButton databaseId={database.id} onUpdate={appendDatabase} />
                     </Can>
                     <Button isSecondary onClick={() => setConnectionVisible(false)}>
-                        Close
+                        Schliessen
                     </Button>
                 </div>
             </Modal>
@@ -152,17 +152,17 @@ export default ({ database, className }: Props) => {
                     <CopyOnClick text={database.connectionString}>
                         <p css={tw`text-sm`}>{database.connectionString}</p>
                     </CopyOnClick>
-                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Endpoint</p>
+                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Endpunkt</p>
                 </div>
                 <div css={tw`ml-8 text-center hidden md:block`}>
                     <p css={tw`text-sm`}>{database.allowConnectionsFrom}</p>
-                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Connections from</p>
+                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Verbindungen von</p>
                 </div>
                 <div css={tw`ml-8 text-center hidden md:block`}>
                     <CopyOnClick text={database.username}>
                         <p css={tw`text-sm`}>{database.username}</p>
                     </CopyOnClick>
-                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Username</p>
+                    <p css={tw`mt-1 text-2xs text-neutral-500 uppercase select-none`}>Benutzername</p>
                 </div>
                 <div css={tw`ml-8`}>
                     <Button isSecondary css={tw`mr-2`} onClick={() => setConnectionVisible(true)}>
diff --git a/resources/scripts/components/server/databases/DatabasesContainer.tsx b/resources/scripts/components/server/databases/DatabasesContainer.tsx
index 16b6900..dfddf2f 100644
--- a/resources/scripts/components/server/databases/DatabasesContainer.tsx
+++ b/resources/scripts/components/server/databases/DatabasesContainer.tsx
@@ -37,7 +37,7 @@ export default () => {
     }, []);
 
     return (
-        <ServerContentBlock title={'Databases'}>
+        <ServerContentBlock title={'Datenbanken'}>
             <FlashMessageRender byKey={'databases'} css={tw`mb-4`} />
             {!databases.length && loading ? (
                 <Spinner size={'large'} centered />
@@ -55,16 +55,15 @@ export default () => {
                         ) : (
                             <p css={tw`text-center text-sm text-neutral-300`}>
                                 {databaseLimit > 0
-                                    ? 'It looks like you have no databases.'
-                                    : 'Databases cannot be created for this server.'}
+                                    ? 'Aktuell gibt es keine Datenbanken für deinen Server.'
+                                    : 'Aktuell ist es auf deinem Server nicht erlaubt, Datenbanken zu erstellen.'}
                             </p>
                         )}
                         <Can action={'database.create'}>
                             <div css={tw`mt-6 flex items-center justify-end`}>
                                 {databaseLimit > 0 && databases.length > 0 && (
                                     <p css={tw`text-sm text-neutral-300 mb-4 sm:mr-6 sm:mb-0`}>
-                                        {databases.length} of {databaseLimit} databases have been allocated to this
-                                        server.
+                                        {databases.length} Datenbanken sind aktiv ({databaseLimit} ist Maximum)
                                     </p>
                                 )}
                                 {databaseLimit > 0 && databaseLimit !== databases.length && (
diff --git a/resources/scripts/components/server/databases/RotatePasswordButton.tsx b/resources/scripts/components/server/databases/RotatePasswordButton.tsx
index 0c687d7..28572fd 100644
--- a/resources/scripts/components/server/databases/RotatePasswordButton.tsx
+++ b/resources/scripts/components/server/databases/RotatePasswordButton.tsx
@@ -37,7 +37,7 @@ export default ({ databaseId, onUpdate }: { databaseId: string; onUpdate: (datab
 
     return (
         <Button isSecondary color={'primary'} css={tw`mr-2`} onClick={rotate} isLoading={loading}>
-            Rotate Password
+            Neues Passwort erstellen
         </Button>
     );
 };
diff --git a/resources/scripts/components/server/features/GSLTokenModalFeature.tsx b/resources/scripts/components/server/features/GSLTokenModalFeature.tsx
index f56ae7c..9017053 100644
--- a/resources/scripts/components/server/features/GSLTokenModalFeature.tsx
+++ b/resources/scripts/components/server/features/GSLTokenModalFeature.tsx
@@ -75,25 +75,25 @@ const GSLTokenModalFeature = () => {
             >
                 <FlashMessageRender key={'feature:gslToken'} css={tw`mb-4`} />
                 <Form>
-                    <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Invalid GSL token!</h2>
+                    <h2 css={tw`text-2xl mb-4 text-neutral-100`}>GSL Token ist ungültig!</h2>
                     <p css={tw`mt-4`}>
-                        It seems like your Gameserver Login Token (GSL token) is invalid or has expired.
+                        Es schaut so aus, als wäre dein GSL-Token entweder nicht gültig oder abgelaufen.
                     </p>
                     <p css={tw`mt-4`}>
-                        You can either generate a new one and enter it below or leave the field blank to remove it
-                        completely.
+                        Du hast einen neuen generieren lassen, oder du lässt das Feld leer.
+                        Demnach wird es dann entfernt.
                     </p>
                     <div css={tw`sm:flex items-center mt-4`}>
                         <Field
                             name={'gslToken'}
                             label={'GSL Token'}
-                            description={'Visit https://steamcommunity.com/dev/managegameservers to generate a token.'}
+                            description={'Besuche https://steamcommunity.com/dev/managegameservers um einen Token zu generieren.'}
                             autoFocus
                         />
                     </div>
                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
                         <Button type={'submit'} css={tw`mt-4 sm:mt-0 sm:ml-4 w-full sm:w-auto`}>
-                            Update GSL Token
+                            GSL-Token speichern
                         </Button>
                     </div>
                 </Form>
diff --git a/resources/scripts/components/server/features/JavaVersionModalFeature.tsx b/resources/scripts/components/server/features/JavaVersionModalFeature.tsx
index 91902f0..e811355 100644
--- a/resources/scripts/components/server/features/JavaVersionModalFeature.tsx
+++ b/resources/scripts/components/server/features/JavaVersionModalFeature.tsx
@@ -76,11 +76,11 @@ const JavaVersionModalFeature = () => {
             showSpinnerOverlay={loading}
         >
             <FlashMessageRender key={'feature:javaVersion'} css={tw`mb-4`} />
-            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Unsupported Java Version</h2>
+            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Java-Version wird nicht unterstützt</h2>
             <p css={tw`mt-4`}>
-                This server is currently running an unsupported version of Java and cannot be started.
+                Dein Server läuft aktuell mit einer Java-Version, die nicht kompatibel ist.
                 <Can action={'startup.docker-image'}>
-                    &nbsp;Please select a supported version from the list below to continue starting the server.
+                    &nbsp;Wähle eine vefügbare Version, und starte dann erneut den Server.
                 </Can>
             </p>
             <Can action={'startup.docker-image'}>
diff --git a/resources/scripts/components/server/features/PIDLimitModalFeature.tsx b/resources/scripts/components/server/features/PIDLimitModalFeature.tsx
index f4061bc..b3baf8e 100644
--- a/resources/scripts/components/server/features/PIDLimitModalFeature.tsx
+++ b/resources/scripts/components/server/features/PIDLimitModalFeature.tsx
@@ -60,20 +60,19 @@ const PIDLimitModalFeature = () => {
                 <>
                     <div css={tw`mt-4 sm:flex items-center`}>
                         <FontAwesomeIcon css={tw`pr-4`} icon={faExclamationTriangle} color={'orange'} size={'4x'} />
-                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>Memory or process limit reached...</h2>
+                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>RAM oder Prozess-Limit erreicht!</h2>
                     </div>
-                    <p css={tw`mt-4`}>This server has reached the maximum process or memory limit.</p>
+                    <p css={tw`mt-4`}>Dein Server hat den Limit des Servers erreicht, prüfe die Nutzung der Ressourcen oder führe ein Upgrade durch</p>
                     <p css={tw`mt-4`}>
-                        Increasing <code css={tw`font-mono bg-neutral-900`}>container_pid_limit</code> in the wings
-                        configuration, <code css={tw`font-mono bg-neutral-900`}>config.yml</code>, might help resolve
-                        this issue.
+                        Füge <code css={tw`font-mono bg-neutral-900`}>container_pid_limit</code> in die 
+						in die Wings-Config (<code css={tw`font-mono bg-neutral-900`}>config.yml</code>), das kann das Problem lösen
                     </p>
                     <p css={tw`mt-4`}>
-                        <b>Note: Wings must be restarted for the configuration file changes to take effect</b>
+                        <b>Info: Wings muss neu gestartet werden. Dies ist mit <code css={tw`font-mono bg-neutral-900`}>systemctl restart wings</code> möglich.</b>
                     </p>
                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
-                            Close
+                            Schliessen
                         </Button>
                     </div>
                 </>
@@ -81,20 +80,19 @@ const PIDLimitModalFeature = () => {
                 <>
                     <div css={tw`mt-4 sm:flex items-center`}>
                         <FontAwesomeIcon css={tw`pr-4`} icon={faExclamationTriangle} color={'orange'} size={'4x'} />
-                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Possible resource limit reached...</h2>
+                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Limit erreicht!</h2>
                     </div>
                     <p css={tw`mt-4`}>
-                        This server is attempting to use more resources than allocated. Please contact the administrator
-                        and give them the error below.
+                        Dieser Server hat mehr Ressourcen zugeteilt bekommen als verfügbar. Bitte kontaktiere den Administrator.
                     </p>
                     <p css={tw`mt-4`}>
                         <code css={tw`font-mono bg-neutral-900`}>
-                            pthread_create failed, Possibly out of memory or process/resource limits reached
+                            pthread_create failed, wahrscheinlich ist das Limit des RAMs oder der Prozesse erreicht worden.
                         </code>
                     </p>
                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
-                            Close
+                            Schliessen
                         </Button>
                     </div>
                 </>
diff --git a/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx b/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx
index 81acba5..869c3e2 100644
--- a/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx
+++ b/resources/scripts/components/server/features/SteamDiskSpaceFeature.tsx
@@ -50,35 +50,33 @@ const SteamDiskSpaceFeature = () => {
             {isAdmin ? (
                 <>
                     <div css={tw`mt-4 sm:flex items-center`}>
-                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>Out of available disk space...</h2>
+                        <h2 css={tw`text-2xl mb-4 text-neutral-100 `}>Der Speicher ist voll!</h2>
                     </div>
                     <p css={tw`mt-4`}>
-                        This server has run out of available disk space and cannot complete the install or update
-                        process.
+                        Der Server hat den Speicherlimit erreicht. Erweitere deinen Speicherplatz oder entferne unnötige Dateien.
                     </p>
                     <p css={tw`mt-4`}>
-                        Ensure the machine has enough disk space by typing{' '}
-                        <code css={tw`font-mono bg-neutral-900 rounded py-1 px-2`}>df -h</code> on the machine hosting
-                        this server. Delete files or increase the available disk space to resolve the issue.
+                        Sei versichert, das auf dem Server genug Speicherplatz beim ausführen verfügbar ist, das kannst du mit dem Befehl {' '}
+                        <code css={tw`font-mono bg-neutral-900 rounded py-1 px-2`}>df -h</code> prüfen. Entferne unnötige Dateien oder Ordner, um Speicherplatz zu schaffen und das Problem zu beheben.
                     </p>
                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
-                            Close
+                            Okay
                         </Button>
                     </div>
                 </>
             ) : (
                 <>
                     <div css={tw`mt-4 sm:flex items-center`}>
-                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Out of available disk space...</h2>
+                        <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Der Speicher ist voll!</h2>
                     </div>
                     <p css={tw`mt-4`}>
-                        This server has run out of available disk space and cannot complete the install or update
-                        process. Please get in touch with the administrator(s) and inform them of disk space issues.
+                        Dein Server hat den Speicherlimit erreicht,  es kann somit keine Installation oder Upgrade durchgeführt werden.
+                        Bitte kontaktiere den Support des Systems, um das Problem zu beheben.
                     </p>
                     <div css={tw`mt-8 sm:flex items-center justify-end`}>
                         <Button onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
-                            Close
+                            Okay
                         </Button>
                     </div>
                 </>
diff --git a/resources/scripts/components/server/features/eula/EulaModalFeature.tsx b/resources/scripts/components/server/features/eula/EulaModalFeature.tsx
index fd7afe1..fb9fe19 100644
--- a/resources/scripts/components/server/features/eula/EulaModalFeature.tsx
+++ b/resources/scripts/components/server/features/eula/EulaModalFeature.tsx
@@ -65,9 +65,9 @@ const EulaModalFeature = () => {
             showSpinnerOverlay={loading}
         >
             <FlashMessageRender key={'feature:eula'} css={tw`mb-4`} />
-            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Accept Minecraft&reg; EULA</h2>
+            <h2 css={tw`text-2xl mb-4 text-neutral-100`}>Akzeptiere die Minecraft&reg; EULA</h2>
             <p css={tw`text-neutral-200`}>
-                By pressing {'"I Accept"'} below you are indicating your agreement to the&nbsp;
+                Wenn du {'"Ja, ich will"'} anklickst, akzeptierst du die &nbsp;
                 <a
                     target={'_blank'}
                     css={tw`text-primary-300 underline transition-colors duration-150 hover:text-primary-400`}
@@ -80,10 +80,10 @@ const EulaModalFeature = () => {
             </p>
             <div css={tw`mt-8 sm:flex items-center justify-end`}>
                 <Button isSecondary onClick={() => setVisible(false)} css={tw`w-full sm:w-auto border-transparent`}>
-                    Cancel
+                    Abbrechen
                 </Button>
                 <Button onClick={onAcceptEULA} css={tw`mt-4 sm:mt-0 sm:ml-4 w-full sm:w-auto`}>
-                    I Accept
+                    Ja, ich will
                 </Button>
             </div>
         </Modal>
diff --git a/resources/scripts/components/server/files/ChmodFileModal.tsx b/resources/scripts/components/server/files/ChmodFileModal.tsx
index 27a474d..e8f49f4 100644
--- a/resources/scripts/components/server/files/ChmodFileModal.tsx
+++ b/resources/scripts/components/server/files/ChmodFileModal.tsx
@@ -59,10 +59,10 @@ const ChmodFileModal = ({ files, ...props }: OwnProps) => {
                     <Form css={tw`m-0`}>
                         <div css={tw`flex flex-wrap items-end`}>
                             <div css={tw`w-full sm:flex-1 sm:mr-4`}>
-                                <Field type={'string'} id={'file_mode'} name={'mode'} label={'File Mode'} autoFocus />
+                                <Field type={'string'} id={'file_mode'} name={'mode'} label={'Rechte-Modus'} autoFocus />
                             </div>
                             <div css={tw`w-full sm:w-auto mt-4 sm:mt-0`}>
-                                <Button css={tw`w-full`}>Update</Button>
+                                <Button css={tw`w-full`}>Speichern</Button>
                             </div>
                         </div>
                     </Form>
diff --git a/resources/scripts/components/server/files/FileDropdownMenu.tsx b/resources/scripts/components/server/files/FileDropdownMenu.tsx
index fd89295..ff86a49 100644
--- a/resources/scripts/components/server/files/FileDropdownMenu.tsx
+++ b/resources/scripts/components/server/files/FileDropdownMenu.tsx
@@ -132,12 +132,12 @@ const FileDropdownMenu = ({ file }: { file: FileObject }) => {
             <Dialog.Confirm
                 open={showConfirmation}
                 onClose={() => setShowConfirmation(false)}
-                title={`Delete ${file.isFile ? 'File' : 'Directory'}`}
-                confirm={'Delete'}
+                title={`${file.isFile ? 'Datei' : 'Ordner'} löschen?`}
+                confirm={'Löschen'}
                 onConfirmed={doDeletion}
             >
-                You will not be able to recover the contents of&nbsp;
-                <span className={'font-semibold text-gray-50'}>{file.name}</span> once deleted.
+                Dadurch löschst du unwiderruflich of&nbsp;
+                <span className={'font-semibold text-gray-50'}>{file.name}</span>. Bist du dir sicher?
             </Dialog.Confirm>
             <DropdownMenu
                 ref={onClickRef}
@@ -167,27 +167,27 @@ const FileDropdownMenu = ({ file }: { file: FileObject }) => {
                 )}
             >
                 <Can action={'file.update'}>
-                    <Row onClick={() => setModal('rename')} icon={faPencilAlt} title={'Rename'} />
-                    <Row onClick={() => setModal('move')} icon={faLevelUpAlt} title={'Move'} />
-                    <Row onClick={() => setModal('chmod')} icon={faFileCode} title={'Permissions'} />
+                    <Row onClick={() => setModal('rename')} icon={faPencilAlt} title={'Umbenennen'} />
+                    <Row onClick={() => setModal('move')} icon={faLevelUpAlt} title={'Verschieben'} />
+                    <Row onClick={() => setModal('chmod')} icon={faFileCode} title={'Berechtigungen'} />
                 </Can>
                 {file.isFile && (
                     <Can action={'file.create'}>
-                        <Row onClick={doCopy} icon={faCopy} title={'Copy'} />
+                        <Row onClick={doCopy} icon={faCopy} title={'Kopieren'} />
                     </Can>
                 )}
                 {file.isArchiveType() ? (
                     <Can action={'file.create'}>
-                        <Row onClick={doUnarchive} icon={faBoxOpen} title={'Unarchive'} />
+                        <Row onClick={doUnarchive} icon={faBoxOpen} title={'Entpacken'} />
                     </Can>
                 ) : (
                     <Can action={'file.archive'}>
-                        <Row onClick={doArchive} icon={faFileArchive} title={'Archive'} />
+                        <Row onClick={doArchive} icon={faFileArchive} title={'Archivieren'} />
                     </Can>
                 )}
-                {file.isFile && <Row onClick={doDownload} icon={faFileDownload} title={'Download'} />}
+                {file.isFile && <Row onClick={doDownload} icon={faFileDownload} title={'Herunterladen'} />}
                 <Can action={'file.delete'}>
-                    <Row onClick={() => setShowConfirmation(true)} icon={faTrashAlt} title={'Delete'} $danger />
+                    <Row onClick={() => setShowConfirmation(true)} icon={faTrashAlt} title={'Löschen'} $danger />
                 </Can>
             </DropdownMenu>
         </>
diff --git a/resources/scripts/components/server/files/FileEditContainer.tsx b/resources/scripts/components/server/files/FileEditContainer.tsx
index 28ba91d..e2e94e4 100644
--- a/resources/scripts/components/server/files/FileEditContainer.tsx
+++ b/resources/scripts/components/server/files/FileEditContainer.tsx
@@ -94,10 +94,10 @@ export default () => {
             {hash.replace(/^#/, '').endsWith('.pteroignore') && (
                 <div css={tw`mb-4 p-4 border-l-4 bg-neutral-900 rounded border-cyan-400`}>
                     <p css={tw`text-neutral-300 text-sm`}>
-                        You&apos;re editing a <code css={tw`font-mono bg-black rounded py-px px-1`}>.pteroignore</code>{' '}
-                        file. Any files or directories listed in here will be excluded from backups. Wildcards are
-                        supported by using an asterisk (<code css={tw`font-mono bg-black rounded py-px px-1`}>*</code>).
-                        You can negate a prior rule by prepending an exclamation point (
+                        Du bearbeitest eine <code css={tw`font-mono bg-black rounded py-px px-1`}>.pteroignore</code>{' '}
+                        Datei. Diese Datei zeigt dem Backup-System, das dieses Verzeichnis ausgenmommen werden soll. Wildcards sind
+                        unterstützt durch die Verwendung eines Sternchens (<code css={tw`font-mono bg-black rounded py-px px-1`}>*</code>).
+                        Du kannst eine vorherige Regel negieren, indem Sie ein Ausrufezeichen voranstellen (
                         <code css={tw`font-mono bg-black rounded py-px px-1`}>!</code>).
                     </p>
                 </div>
@@ -142,13 +142,13 @@ export default () => {
                 {action === 'edit' ? (
                     <Can action={'file.update'}>
                         <Button css={tw`flex-1 sm:flex-none`} onClick={() => save()}>
-                            Save Content
+                            Änderungen speichern
                         </Button>
                     </Can>
                 ) : (
                     <Can action={'file.create'}>
                         <Button css={tw`flex-1 sm:flex-none`} onClick={() => setModalVisible(true)}>
-                            Create File
+                            Datei erstellen
                         </Button>
                     </Can>
                 )}
diff --git a/resources/scripts/components/server/files/FileManagerContainer.tsx b/resources/scripts/components/server/files/FileManagerContainer.tsx
index 3bebd20..d77e76e 100644
--- a/resources/scripts/components/server/files/FileManagerContainer.tsx
+++ b/resources/scripts/components/server/files/FileManagerContainer.tsx
@@ -79,7 +79,7 @@ export default () => {
                             <NewDirectoryButton />
                             <UploadButton />
                             <NavLink to={`/server/${id}/files/new${window.location.hash}`}>
-                                <Button>New File</Button>
+                                <Button>Neue Datei</Button>
                             </NavLink>
                         </div>
                     </Can>
@@ -90,15 +90,14 @@ export default () => {
             ) : (
                 <>
                     {!files.length ? (
-                        <p css={tw`text-sm text-neutral-400 text-center`}>This directory seems to be empty.</p>
+                        <p css={tw`text-sm text-neutral-400 text-center`}>Dieser Ordner ist leer.</p>
                     ) : (
                         <CSSTransition classNames={'fade'} timeout={150} appear in>
                             <div>
                                 {files.length > 250 && (
                                     <div css={tw`rounded bg-yellow-400 mb-px p-3`}>
                                         <p css={tw`text-yellow-900 text-sm text-center`}>
-                                            This directory is too large to display in the browser, limiting the output
-                                            to the first 250 files.
+                                            Dieser Ordner ist zu groß, um alles anzuzeigen. Die Ansicht wurde auf 250 Dateien begrenzt.
                                         </p>
                                     </div>
                                 )}
diff --git a/resources/scripts/components/server/files/FileManagerStatus.tsx b/resources/scripts/components/server/files/FileManagerStatus.tsx
index 3e1c2df..ad14075 100644
--- a/resources/scripts/components/server/files/FileManagerStatus.tsx
+++ b/resources/scripts/components/server/files/FileManagerStatus.tsx
@@ -58,17 +58,17 @@ const FileUploadList = () => {
             ))}
             <Dialog.Footer>
                 <Button.Danger variant={Button.Variants.Secondary} onClick={() => clearFileUploads()}>
-                    Cancel Uploads
+                    Upload abbrechen
                 </Button.Danger>
-                <Button.Text onClick={close}>Close</Button.Text>
+                <Button.Text onClick={close}>Schliessen</Button.Text>
             </Dialog.Footer>
         </div>
     );
 };
 
 const FileUploadListDialog = asDialog({
-    title: 'File Uploads',
-    description: 'The following files are being uploaded to your server.',
+    title: 'Wird hochgeladen',
+    description: 'Folgende Dateien werden gerade hochgeladen...',
 })(FileUploadList);
 
 export default () => {
@@ -89,7 +89,7 @@ export default () => {
     return (
         <>
             {count > 0 && (
-                <Tooltip content={`${count} files are uploading, click to view`}>
+                <Tooltip content={`${count} Dateien werden hochgeladen, zum Anzeigen klicken`}>
                     <button
                         className={'flex items-center justify-center w-10 h-10'}
                         onClick={() => (open.value = true)}
diff --git a/resources/scripts/components/server/files/FileNameModal.tsx b/resources/scripts/components/server/files/FileNameModal.tsx
index 04781ba..2cfbc72 100644
--- a/resources/scripts/components/server/files/FileNameModal.tsx
+++ b/resources/scripts/components/server/files/FileNameModal.tsx
@@ -44,12 +44,12 @@ export default ({ onFileNamed, onDismissed, ...props }: Props) => {
                         <Field
                             id={'fileName'}
                             name={'fileName'}
-                            label={'File Name'}
-                            description={'Enter the name that this file should be saved as.'}
+                            label={'Datei erstellen'}
+                            description={'Gib ein, wie die Datei heißen soll.'}
                             autoFocus
                         />
                         <div css={tw`mt-6 text-right`}>
-                            <Button>Create File</Button>
+                            <Button>Erstellen</Button>
                         </div>
                     </Form>
                 </Modal>
diff --git a/resources/scripts/components/server/files/FileObjectRow.tsx b/resources/scripts/components/server/files/FileObjectRow.tsx
index 8032dab..882af81 100644
--- a/resources/scripts/components/server/files/FileObjectRow.tsx
+++ b/resources/scripts/components/server/files/FileObjectRow.tsx
@@ -14,6 +14,7 @@ import { usePermissions } from '@/plugins/usePermissions';
 import { join } from 'path';
 import { bytesToString } from '@/lib/formatters';
 import styles from './style.module.css';
+import { de } from "date-fns/locale";
 
 const Clickable: React.FC<{ file: FileObject }> = memo(({ file, children }) => {
     const [canReadContents] = usePermissions(['file.read-content']);
@@ -57,8 +58,8 @@ const FileObjectRow = ({ file }: { file: FileObject }) => (
             {file.isFile && <div css={tw`w-1/6 text-right mr-4 hidden sm:block`}>{bytesToString(file.size)}</div>}
             <div css={tw`w-1/5 text-right mr-4 hidden md:block`} title={file.modifiedAt.toString()}>
                 {Math.abs(differenceInHours(file.modifiedAt, new Date())) > 48
-                    ? format(file.modifiedAt, 'MMM do, yyyy h:mma')
-                    : formatDistanceToNow(file.modifiedAt, { addSuffix: true })}
+                    ? format(file.modifiedAt, 'do MMM yyyy hh:mm', { locale: de })
+                    : formatDistanceToNow(file.modifiedAt, { locale: de, addSuffix: true })}
             </div>
         </Clickable>
         <FileDropdownMenu file={file} />
diff --git a/resources/scripts/components/server/files/MassActionsBar.tsx b/resources/scripts/components/server/files/MassActionsBar.tsx
index 44230f2..9271d55 100644
--- a/resources/scripts/components/server/files/MassActionsBar.tsx
+++ b/resources/scripts/components/server/files/MassActionsBar.tsx
@@ -33,7 +33,7 @@ const MassActionsBar = () => {
     const onClickCompress = () => {
         setLoading(true);
         clearFlashes('files');
-        setLoadingMessage('Archiving files...');
+        setLoadingMessage('Dateien archivieren...');
 
         compressFiles(uuid, directory, selectedFiles)
             .then(() => mutate())
@@ -46,7 +46,7 @@ const MassActionsBar = () => {
         setLoading(true);
         setShowConfirm(false);
         clearFlashes('files');
-        setLoadingMessage('Deleting files...');
+        setLoadingMessage('Löschvorgang läuft...');
 
         deleteFiles(uuid, directory, selectedFiles)
             .then(() => {
@@ -67,21 +67,21 @@ const MassActionsBar = () => {
                     {loadingMessage}
                 </SpinnerOverlay>
                 <Dialog.Confirm
-                    title={'Delete Files'}
+                    title={'Dateien löschen'}
                     open={showConfirm}
-                    confirm={'Delete'}
+                    confirm={'Löschen'}
                     onClose={() => setShowConfirm(false)}
                     onConfirmed={onClickConfirmDeletion}
                 >
                     <p className={'mb-2'}>
-                        Are you sure you want to delete&nbsp;
-                        <span className={'font-semibold text-gray-50'}>{selectedFiles.length} files</span>? This is a
-                        permanent action and the files cannot be recovered.
+                        Bist du sicher, das du&nbsp;
+                        <span className={'font-semibold text-gray-50'}>{selectedFiles.length} Dateien löschen möchtest</span>? Das ist eine
+                        Aktion, die man nicht rückgängig machen kann!
                     </p>
                     {selectedFiles.slice(0, 15).map((file) => (
                         <li key={file}>{file}</li>
                     ))}
-                    {selectedFiles.length > 15 && <li>and {selectedFiles.length - 15} others</li>}
+                    {selectedFiles.length > 15 && <li>und {selectedFiles.length - 15} weitere</li>}
                 </Dialog.Confirm>
                 {showMove && (
                     <RenameFileModal
@@ -96,10 +96,10 @@ const MassActionsBar = () => {
                     <div className={'fixed bottom-0 mb-6 flex justify-center w-full z-50'}>
                         <Fade timeout={75} in={selectedFiles.length > 0} unmountOnExit>
                             <div css={tw`flex items-center space-x-4 pointer-events-auto rounded p-4 bg-black/50`}>
-                                <Button onClick={() => setShowMove(true)}>Move</Button>
-                                <Button onClick={onClickCompress}>Archive</Button>
+                                <Button onClick={() => setShowMove(true)}>Verschieben</Button>
+                                <Button onClick={onClickCompress}>Archivieren</Button>
                                 <Button.Danger variant={Button.Variants.Secondary} onClick={() => setShowConfirm(true)}>
-                                    Delete
+                                    Löschen
                                 </Button.Danger>
                             </div>
                         </Fade>
diff --git a/resources/scripts/components/server/files/NewDirectoryButton.tsx b/resources/scripts/components/server/files/NewDirectoryButton.tsx
index ff74357..fd8737e 100644
--- a/resources/scripts/components/server/files/NewDirectoryButton.tsx
+++ b/resources/scripts/components/server/files/NewDirectoryButton.tsx
@@ -21,7 +21,7 @@ interface Values {
 }
 
 const schema = object().shape({
-    directoryName: string().required('A valid directory name must be provided.'),
+    directoryName: string().required('Gib einen gültigen Namen ein.'),
 });
 
 const generateDirectoryData = (name: string): FileObject => ({
@@ -40,7 +40,7 @@ const generateDirectoryData = (name: string): FileObject => ({
 });
 
 const NewDirectoryDialog = asDialog({
-    title: 'Create Directory',
+    title: 'Ordner erstellen',
 })(() => {
     const uuid = ServerContext.useStoreState((state) => state.server.data!.uuid);
     const directory = ServerContext.useStoreState((state) => state.files.directory);
@@ -73,7 +73,7 @@ const NewDirectoryDialog = asDialog({
                     <Form css={tw`m-0`}>
                         <Field autoFocus id={'directoryName'} name={'directoryName'} label={'Name'} />
                         <p css={tw`mt-2 text-sm md:text-base break-all`}>
-                            <span css={tw`text-neutral-200`}>This directory will be created as&nbsp;</span>
+                            <span css={tw`text-neutral-200`}>Der Ordner wird erstellt in &nbsp;</span>
                             <Code>
                                 /home/container/
                                 <span css={tw`text-cyan-200`}>
@@ -84,10 +84,10 @@ const NewDirectoryDialog = asDialog({
                     </Form>
                     <Dialog.Footer>
                         <Button.Text className={'w-full sm:w-auto'} onClick={close}>
-                            Cancel
+                            Abbrechen
                         </Button.Text>
                         <Button className={'w-full sm:w-auto'} onClick={submitForm}>
-                            Create
+                            Erstellen
                         </Button>
                     </Dialog.Footer>
                 </>
@@ -103,7 +103,7 @@ export default ({ className }: WithClassname) => {
         <>
             <NewDirectoryDialog open={open} onClose={setOpen.bind(this, false)} />
             <Button.Text onClick={setOpen.bind(this, true)} className={className}>
-                Create Directory
+                Ordner erstellen
             </Button.Text>
         </>
     );
diff --git a/resources/scripts/components/server/files/RenameFileModal.tsx b/resources/scripts/components/server/files/RenameFileModal.tsx
index 4f67533..8a15287 100644
--- a/resources/scripts/components/server/files/RenameFileModal.tsx
+++ b/resources/scripts/components/server/files/RenameFileModal.tsx
@@ -66,22 +66,22 @@ const RenameFileModal = ({ files, useMoveTerminology, ...props }: OwnProps) => {
                                     type={'string'}
                                     id={'file_name'}
                                     name={'name'}
-                                    label={'File Name'}
+                                    label={'Umbenennen'}
                                     description={
                                         useMoveTerminology
-                                            ? 'Enter the new name and directory of this file or folder, relative to the current directory.'
+                                            ? 'Vergebe einen neuen Namen für diese Datei (oder Ordner).'
                                             : undefined
                                     }
                                     autoFocus
                                 />
                             </div>
                             <div css={tw`w-full sm:w-auto mt-4 sm:mt-0`}>
-                                <Button css={tw`w-full`}>{useMoveTerminology ? 'Move' : 'Rename'}</Button>
+                                <Button css={tw`w-full`}>{useMoveTerminology ? 'Verschieben' : 'Unbenennen'}</Button>
                             </div>
                         </div>
                         {useMoveTerminology && (
                             <p css={tw`text-xs mt-2 text-neutral-400`}>
-                                <strong css={tw`text-neutral-200`}>New location:</strong>
+                                <strong css={tw`text-neutral-200`}>Neuer Ort:</strong>
                                 &nbsp;/home/container/{join(directory, values.name).replace(/^(\.\.\/|\/)+/, '')}
                             </p>
                         )}
diff --git a/resources/scripts/components/server/files/UploadButton.tsx b/resources/scripts/components/server/files/UploadButton.tsx
index 9ab889b..1cfc5e0 100644
--- a/resources/scripts/components/server/files/UploadButton.tsx
+++ b/resources/scripts/components/server/files/UploadButton.tsx
@@ -65,7 +65,7 @@ export default ({ className }: WithClassname) => {
         clearAndAddHttpError();
         const list = Array.from(files);
         if (list.some((file) => !file.size || (!file.type && file.size === 4096))) {
-            return addError('Folder uploads are not supported at this time.', 'Error');
+            return addError('Ordner können nicht hochgeladen werden.', 'Fehler');
         }
 
         const uploads = list.map((file) => {
@@ -125,7 +125,7 @@ export default ({ className }: WithClassname) => {
                             >
                                 <CloudUploadIcon className={'w-10 h-10 flex-shrink-0'} />
                                 <p className={'font-header flex-1 text-lg text-neutral-100 text-center'}>
-                                    Drag and drop files to upload.
+                                    Drag&Drop um etwas hochzuladen
                                 </p>
                             </div>
                         </div>
@@ -147,7 +147,7 @@ export default ({ className }: WithClassname) => {
                 multiple
             />
             <Button className={className} onClick={() => fileUploadInput.current && fileUploadInput.current.click()}>
-                Upload
+                Hochladen
             </Button>
         </>
     );
diff --git a/resources/scripts/components/server/network/AllocationRow.tsx b/resources/scripts/components/server/network/AllocationRow.tsx
index e68bc33..ca0a5d6 100644
--- a/resources/scripts/components/server/network/AllocationRow.tsx
+++ b/resources/scripts/components/server/network/AllocationRow.tsx
@@ -88,7 +88,7 @@ const AllocationRow = ({ allocation }: Props) => {
                 <InputSpinner visible={loading}>
                     <Textarea
                         className={'bg-neutral-800 hover:border-neutral-600 border-transparent'}
-                        placeholder={'Notes'}
+                        placeholder={'Vermerk zum Port'}
                         defaultValue={allocation.notes || undefined}
                         onChange={(e) => setAllocationNotes(e.currentTarget.value)}
                     />
@@ -97,7 +97,7 @@ const AllocationRow = ({ allocation }: Props) => {
             <div className={'flex justify-end space-x-4 mt-4 w-full md:mt-0 md:w-48'}>
                 {allocation.isDefault ? (
                     <Button size={Button.Sizes.Small} className={'!text-gray-50 !bg-blue-600'} disabled>
-                        Primary
+                        Primär
                     </Button>
                 ) : (
                     <>
@@ -106,7 +106,7 @@ const AllocationRow = ({ allocation }: Props) => {
                         </Can>
                         <Can action={'allocation.update'}>
                             <Button.Text size={Button.Sizes.Small} onClick={setPrimaryAllocation}>
-                                Make Primary
+                                Primär machen
                             </Button.Text>
                         </Can>
                     </>
diff --git a/resources/scripts/components/server/network/DeleteAllocationButton.tsx b/resources/scripts/components/server/network/DeleteAllocationButton.tsx
index c680448..f3ee3f4 100644
--- a/resources/scripts/components/server/network/DeleteAllocationButton.tsx
+++ b/resources/scripts/components/server/network/DeleteAllocationButton.tsx
@@ -39,11 +39,11 @@ const DeleteAllocationButton = ({ allocation }: Props) => {
             <Dialog.Confirm
                 open={confirm}
                 onClose={() => setConfirm(false)}
-                title={'Remove Allocation'}
-                confirm={'Delete'}
+                title={'Port rauswerfen'}
+                confirm={'Entfernen'}
                 onConfirmed={deleteAllocation}
             >
-                This allocation will be immediately removed from your server.
+                Dadurch wird der Port nicht mehr für deinen Server reserviert und für andere freigegeben.
             </Dialog.Confirm>
             <Button.Danger
                 variant={Button.Variants.Secondary}
diff --git a/resources/scripts/components/server/network/NetworkContainer.tsx b/resources/scripts/components/server/network/NetworkContainer.tsx
index 182beaa..d59c661 100644
--- a/resources/scripts/components/server/network/NetworkContainer.tsx
+++ b/resources/scripts/components/server/network/NetworkContainer.tsx
@@ -51,7 +51,7 @@ const NetworkContainer = () => {
     };
 
     return (
-        <ServerContentBlock showFlashKey={'server:network'} title={'Network'}>
+        <ServerContentBlock showFlashKey={'server:network'} title={'Netzwerk'}>
             {!data ? (
                 <Spinner size={'large'} centered />
             ) : (
@@ -64,12 +64,11 @@ const NetworkContainer = () => {
                             <SpinnerOverlay visible={loading} />
                             <div css={tw`mt-6 sm:flex items-center justify-end`}>
                                 <p css={tw`text-sm text-neutral-300 mb-4 sm:mr-6 sm:mb-0`}>
-                                    You are currently using {data.length} of {allocationLimit} allowed allocations for
-                                    this server.
+                                    {data.length} Ports stehen für diesen Server bereit ({allocationLimit} dürfen es Maximal sein)
                                 </p>
                                 {allocationLimit > data.length && (
                                     <Button css={tw`w-full sm:w-auto`} color={'primary'} onClick={onCreateAllocation}>
-                                        Create Allocation
+                                        Port zuteilen
                                     </Button>
                                 )}
                             </div>
diff --git a/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx b/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx
index 1710ad0..aa29c1d 100644
--- a/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx
+++ b/resources/scripts/components/server/schedules/DeleteScheduleButton.tsx
@@ -41,19 +41,19 @@ export default ({ scheduleId, onDeleted }: Props) => {
             <Dialog.Confirm
                 open={visible}
                 onClose={() => setVisible(false)}
-                title={'Delete Schedule'}
-                confirm={'Delete'}
+                title={'Zeitplan löschen'}
+                confirm={'Bestätigen'}
                 onConfirmed={onDelete}
             >
                 <SpinnerOverlay visible={isLoading} />
-                All tasks will be removed and any running processes will be terminated.
+                Alle geplanten Aufgaben werden hierbei gelöscht und bereits laufende werden abgebrochen.
             </Dialog.Confirm>
             <Button.Danger
                 variant={Button.Variants.Secondary}
                 className={'flex-1 sm:flex-none mr-4 border-transparent'}
                 onClick={() => setVisible(true)}
             >
-                Delete
+                Löschen
             </Button.Danger>
         </>
     );
diff --git a/resources/scripts/components/server/schedules/EditScheduleModal.tsx b/resources/scripts/components/server/schedules/EditScheduleModal.tsx
index 6a89cb3..47f705e 100644
--- a/resources/scripts/components/server/schedules/EditScheduleModal.tsx
+++ b/resources/scripts/components/server/schedules/EditScheduleModal.tsx
@@ -90,29 +90,29 @@ const EditScheduleModal = ({ schedule }: Props) => {
         >
             {({ isSubmitting }) => (
                 <Form>
-                    <h3 css={tw`text-2xl mb-6`}>{schedule ? 'Edit schedule' : 'Create new schedule'}</h3>
+                    <h3 css={tw`text-2xl mb-6`}>{schedule ? 'Zeitplan bearbeiten' : 'Zeitplan erstellen'}</h3>
                     <FlashMessageRender byKey={'schedule:edit'} css={tw`mb-6`} />
                     <Field
                         name={'name'}
-                        label={'Schedule name'}
-                        description={'A human readable identifier for this schedule.'}
+                        label={'Name des Zeitplans'}
+                        description={'Gib dem Zeitplan einen guten Namen, damit du weißt, wofür dies gedacht ist.'}
                     />
                     <div css={tw`grid grid-cols-2 sm:grid-cols-5 gap-4 mt-6`}>
                         <Field name={'minute'} label={'Minute'} />
-                        <Field name={'hour'} label={'Hour'} />
-                        <Field name={'dayOfMonth'} label={'Day of month'} />
-                        <Field name={'month'} label={'Month'} />
-                        <Field name={'dayOfWeek'} label={'Day of week'} />
+                        <Field name={'hour'} label={'Stunde'} />
+                        <Field name={'dayOfMonth'} label={'Tag des Monats'} />
+                        <Field name={'month'} label={'Monat'} />
+                        <Field name={'dayOfWeek'} label={'Tag der Woche'} />
                     </div>
                     <p css={tw`text-neutral-400 text-xs mt-2`}>
-                        The schedule system supports the use of Cronjob syntax when defining when tasks should begin
-                        running. Use the fields above to specify when these tasks should begin running.
+                        Das Zeitplansystem unterstützt die Verwendung der Cronjob-Syntax beim Definieren, wann Aufgaben ausgeführt werden sollen.
+                        Verwende die Felder oben, um anzugeben, wann diese Tasks ausgeführt werden sollen.
                     </p>
                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
                         <Switch
                             name={'show_cheatsheet'}
-                            description={'Show the cron cheatsheet for some examples.'}
-                            label={'Show Cheatsheet'}
+                            description={'Wenn du Hilfe beim definieren brauchst, kann das nützlich sein.'}
+                            label={'Beispiele anzeigen'}
                             defaultChecked={showCheatsheet}
                             onChange={() => setShowCheetsheet((s) => !s)}
                         />
@@ -125,20 +125,20 @@ const EditScheduleModal = ({ schedule }: Props) => {
                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
                         <FormikSwitch
                             name={'onlyWhenOnline'}
-                            description={'Only execute this schedule when the server is in a running state.'}
-                            label={'Only When Server Is Online'}
+                            description={'Führe den Zeitplan nur aus, wenn der Server in dem Zeitpunkt online ist.'}
+                            label={'Nur wenn der Server online ist'}
                         />
                     </div>
                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
                         <FormikSwitch
                             name={'enabled'}
-                            description={'This schedule will be executed automatically if enabled.'}
-                            label={'Schedule Enabled'}
+                            description={'Der Zeitplan wird automatisch gestartet, wenn der Zeitpunkt erreicht wird.'}
+                            label={'Zeitplan aktiv'}
                         />
                     </div>
                     <div css={tw`mt-6 text-right`}>
                         <Button className={'w-full sm:w-auto'} type={'submit'} disabled={isSubmitting}>
-                            {schedule ? 'Save changes' : 'Create schedule'}
+                            {schedule ? 'Änderungen speichern' : 'Zeitplan erstellen'}
                         </Button>
                     </div>
                 </Form>
diff --git a/resources/scripts/components/server/schedules/NewTaskButton.tsx b/resources/scripts/components/server/schedules/NewTaskButton.tsx
index 7b48016..5f2a815 100644
--- a/resources/scripts/components/server/schedules/NewTaskButton.tsx
+++ b/resources/scripts/components/server/schedules/NewTaskButton.tsx
@@ -14,7 +14,7 @@ export default ({ schedule }: Props) => {
         <>
             <TaskDetailsModal schedule={schedule} visible={visible} onModalDismissed={() => setVisible(false)} />
             <Button onClick={() => setVisible(true)} className={'flex-1'}>
-                New Task
+                Neue Aufgabe
             </Button>
         </>
     );
diff --git a/resources/scripts/components/server/schedules/RunScheduleButton.tsx b/resources/scripts/components/server/schedules/RunScheduleButton.tsx
index 750b544..5c56255 100644
--- a/resources/scripts/components/server/schedules/RunScheduleButton.tsx
+++ b/resources/scripts/components/server/schedules/RunScheduleButton.tsx
@@ -37,7 +37,7 @@ const RunScheduleButton = ({ schedule }: { schedule: Schedule }) => {
                 disabled={schedule.isProcessing}
                 onClick={onTriggerExecute}
             >
-                Run Now
+                Jetzt starten
             </Button>
         </>
     );
diff --git a/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx b/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx
index 0eeed15..321f2f0 100644
--- a/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx
+++ b/resources/scripts/components/server/schedules/ScheduleCheatsheetCards.tsx
@@ -6,47 +6,47 @@ export default () => {
         <>
             <div css={tw`md:w-1/2 h-full bg-neutral-600`}>
                 <div css={tw`flex flex-col`}>
-                    <h2 css={tw`py-4 px-6 font-bold`}>Examples</h2>
+                    <h2 css={tw`py-4 px-6 font-bold`}>Beispiele</h2>
                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
                         <div css={tw`w-1/2`}>*/5 * * * *</div>
-                        <div css={tw`w-1/2`}>every 5 minutes</div>
+                        <div css={tw`w-1/2`}>Alle 5 Minuten</div>
                     </div>
                     <div css={tw`flex py-4 px-6`}>
                         <div css={tw`w-1/2`}>0 */1 * * *</div>
-                        <div css={tw`w-1/2`}>every hour</div>
+                        <div css={tw`w-1/2`}>Stündlich</div>
                     </div>
                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
                         <div css={tw`w-1/2`}>0 8-12 * * *</div>
-                        <div css={tw`w-1/2`}>hour range</div>
+                        <div css={tw`w-1/2`}>Stundenbereich</div>
                     </div>
                     <div css={tw`flex py-4 px-6`}>
                         <div css={tw`w-1/2`}>0 0 * * *</div>
-                        <div css={tw`w-1/2`}>once a day</div>
+                        <div css={tw`w-1/2`}>Einmal am Tag</div>
                     </div>
                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
                         <div css={tw`w-1/2`}>0 0 * * MON</div>
-                        <div css={tw`w-1/2`}>every Monday</div>
+                        <div css={tw`w-1/2`}>Immer Montags</div>
                     </div>
                 </div>
             </div>
             <div css={tw`md:w-1/2 h-full bg-neutral-600`}>
-                <h2 css={tw`py-4 px-6 font-bold`}>Special Characters</h2>
+                <h2 css={tw`py-4 px-6 font-bold`}>Spezielle Aktionen</h2>
                 <div css={tw`flex flex-col`}>
                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
                         <div css={tw`w-1/2`}>*</div>
-                        <div css={tw`w-1/2`}>any value</div>
+                        <div css={tw`w-1/2`}>Jeder Wert</div>
                     </div>
                     <div css={tw`flex py-4 px-6`}>
                         <div css={tw`w-1/2`}>,</div>
-                        <div css={tw`w-1/2`}>value list separator</div>
+                        <div css={tw`w-1/2`}>Wertelisten-Trennzeichen</div>
                     </div>
                     <div css={tw`flex py-4 px-6 bg-neutral-500`}>
                         <div css={tw`w-1/2`}>-</div>
-                        <div css={tw`w-1/2`}>range values</div>
+                        <div css={tw`w-1/2`}>Bereichswerte</div>
                     </div>
                     <div css={tw`flex py-4 px-6`}>
                         <div css={tw`w-1/2`}>/</div>
-                        <div css={tw`w-1/2`}>step values</div>
+                        <div css={tw`w-1/2`}>Stufenwerte</div>
                     </div>
                 </div>
             </div>
diff --git a/resources/scripts/components/server/schedules/ScheduleContainer.tsx b/resources/scripts/components/server/schedules/ScheduleContainer.tsx
index 1722314..1ac89a4 100644
--- a/resources/scripts/components/server/schedules/ScheduleContainer.tsx
+++ b/resources/scripts/components/server/schedules/ScheduleContainer.tsx
@@ -38,7 +38,7 @@ export default () => {
     }, []);
 
     return (
-        <ServerContentBlock title={'Schedules'}>
+        <ServerContentBlock title={'Zeitpläne'}>
             <FlashMessageRender byKey={'schedules'} css={tw`mb-4`} />
             {!schedules.length && loading ? (
                 <Spinner size={'large'} centered />
@@ -46,7 +46,7 @@ export default () => {
                 <>
                     {schedules.length === 0 ? (
                         <p css={tw`text-sm text-center text-neutral-300`}>
-                            There are no schedules configured for this server.
+                            Es gibt aktuell keine Zeitpläne für diesen Server
                         </p>
                     ) : (
                         schedules.map((schedule) => (
@@ -68,7 +68,7 @@ export default () => {
                         <div css={tw`mt-8 flex justify-end`}>
                             <EditScheduleModal visible={visible} onModalDismissed={() => setVisible(false)} />
                             <Button type={'button'} onClick={() => setVisible(true)}>
-                                Create schedule
+                                Zeitplan erstellen
                             </Button>
                         </div>
                     </Can>
diff --git a/resources/scripts/components/server/schedules/ScheduleCronRow.tsx b/resources/scripts/components/server/schedules/ScheduleCronRow.tsx
index 187478f..4b372e2 100644
--- a/resources/scripts/components/server/schedules/ScheduleCronRow.tsx
+++ b/resources/scripts/components/server/schedules/ScheduleCronRow.tsx
@@ -15,19 +15,19 @@ const ScheduleCronRow = ({ cron, className }: Props) => (
         </div>
         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
             <p className={'font-medium'}>{cron.hour}</p>
-            <p className={'text-2xs text-neutral-500 uppercase'}>Hour</p>
+            <p className={'text-2xs text-neutral-500 uppercase'}>Stunde</p>
         </div>
         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
             <p className={'font-medium'}>{cron.dayOfMonth}</p>
-            <p className={'text-2xs text-neutral-500 uppercase'}>Day (Month)</p>
+            <p className={'text-2xs text-neutral-500 uppercase'}>Tag des Monats</p>
         </div>
         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
             <p className={'font-medium'}>{cron.month}</p>
-            <p className={'text-2xs text-neutral-500 uppercase'}>Month</p>
+            <p className={'text-2xs text-neutral-500 uppercase'}>Monat</p>
         </div>
         <div className={'w-1/5 sm:w-auto text-center ml-4'}>
             <p className={'font-medium'}>{cron.dayOfWeek}</p>
-            <p className={'text-2xs text-neutral-500 uppercase'}>Day (Week)</p>
+            <p className={'text-2xs text-neutral-500 uppercase'}>Tag der Woche</p>
         </div>
     </div>
 );
diff --git a/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx b/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx
index bacef24..b85da88 100644
--- a/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx
+++ b/resources/scripts/components/server/schedules/ScheduleEditContainer.tsx
@@ -17,6 +17,7 @@ import isEqual from 'react-fast-compare';
 import { format } from 'date-fns';
 import ScheduleCronRow from '@/components/server/schedules/ScheduleCronRow';
 import RunScheduleButton from '@/components/server/schedules/RunScheduleButton';
+import { de } from "date-fns/locale";
 
 interface Params {
     id: string;
@@ -36,7 +37,7 @@ const ActivePill = ({ active }: { active: boolean }) => (
             active ? tw`bg-green-600 text-green-100` : tw`bg-red-600 text-red-100`,
         ]}
     >
-        {active ? 'Active' : 'Inactive'}
+        {active ? 'Aktiv' : 'Inaktiv'}
     </span>
 );
 
@@ -78,7 +79,7 @@ export default () => {
     }, []);
 
     return (
-        <PageContentBlock title={'Schedules'}>
+        <PageContentBlock title={'Zeitpläne'}>
             <FlashMessageRender byKey={'schedules'} css={tw`mb-4`} />
             {!schedule || isLoading ? (
                 <Spinner size={'large'} centered />
@@ -97,23 +98,23 @@ export default () => {
                                             css={tw`flex items-center rounded-full px-2 py-px text-xs ml-4 uppercase bg-neutral-600 text-white`}
                                         >
                                             <Spinner css={tw`w-3! h-3! mr-2`} />
-                                            Processing
+                                            Wird ausgeführt...
                                         </span>
                                     ) : (
                                         <ActivePill active={schedule.isActive} />
                                     )}
                                 </h3>
                                 <p css={tw`mt-1 text-sm text-neutral-200`}>
-                                    Last run at:&nbsp;
+                                    Zuletzt ausgeführt:&nbsp;
                                     {schedule.lastRunAt ? (
-                                        format(schedule.lastRunAt, "MMM do 'at' h:mma")
+                                        format(schedule.lastRunAt, "do MMM 'um' hh:mm", { locale: de })
                                     ) : (
                                         <span css={tw`text-neutral-300`}>n/a</span>
                                     )}
                                     <span css={tw`ml-4 pl-4 border-l-4 border-neutral-600 py-px`}>
-                                        Next run at:&nbsp;
+                                        Nächster Start:&nbsp;
                                         {schedule.nextRunAt ? (
-                                            format(schedule.nextRunAt, "MMM do 'at' h:mma")
+                                            format(schedule.nextRunAt, "do MMM 'um' hh:mm", { locale: de })
                                         ) : (
                                             <span css={tw`text-neutral-300`}>n/a</span>
                                         )}
@@ -123,7 +124,7 @@ export default () => {
                             <div css={tw`flex sm:block mt-3 sm:mt-0`}>
                                 <Can action={'schedule.update'}>
                                     <Button.Text className={'flex-1 mr-4'} onClick={toggleEditModal}>
-                                        Edit
+                                        Bearbeiten
                                     </Button.Text>
                                     <NewTaskButton schedule={schedule} />
                                 </Can>
@@ -131,10 +132,10 @@ export default () => {
                         </div>
                         <div css={tw`hidden sm:grid grid-cols-5 md:grid-cols-5 gap-4 mb-4 mt-4`}>
                             <CronBox title={'Minute'} value={schedule.cron.minute} />
-                            <CronBox title={'Hour'} value={schedule.cron.hour} />
-                            <CronBox title={'Day (Month)'} value={schedule.cron.dayOfMonth} />
-                            <CronBox title={'Month'} value={schedule.cron.month} />
-                            <CronBox title={'Day (Week)'} value={schedule.cron.dayOfWeek} />
+                            <CronBox title={'Stunde'} value={schedule.cron.hour} />
+                            <CronBox title={'Tag des Monats'} value={schedule.cron.dayOfMonth} />
+                            <CronBox title={'Monat'} value={schedule.cron.month} />
+                            <CronBox title={'Tag der Woche'} value={schedule.cron.dayOfWeek} />
                         </div>
                         <div css={tw`bg-neutral-700 rounded-b`}>
                             {schedule.tasks.length > 0
diff --git a/resources/scripts/components/server/schedules/ScheduleRow.tsx b/resources/scripts/components/server/schedules/ScheduleRow.tsx
index 1a7da64..5a3d4c6 100644
--- a/resources/scripts/components/server/schedules/ScheduleRow.tsx
+++ b/resources/scripts/components/server/schedules/ScheduleRow.tsx
@@ -5,6 +5,7 @@ import { faCalendarAlt } from '@fortawesome/free-solid-svg-icons';
 import { format } from 'date-fns';
 import tw from 'twin.macro';
 import ScheduleCronRow from '@/components/server/schedules/ScheduleCronRow';
+import { de } from "date-fns/locale";
 
 export default ({ schedule }: { schedule: Schedule }) => (
     <>
@@ -14,7 +15,7 @@ export default ({ schedule }: { schedule: Schedule }) => (
         <div css={tw`flex-1 md:ml-4`}>
             <p>{schedule.name}</p>
             <p css={tw`text-xs text-neutral-400`}>
-                Last run at: {schedule.lastRunAt ? format(schedule.lastRunAt, "MMM do 'at' h:mma") : 'never'}
+                Letzter Start: {schedule.lastRunAt ? format(schedule.lastRunAt, "do MMM 'um' hh:mm", {locale: de}) : 'Noch nie'}
             </p>
         </div>
         <div>
@@ -24,7 +25,7 @@ export default ({ schedule }: { schedule: Schedule }) => (
                     schedule.isActive ? tw`bg-green-600` : tw`bg-neutral-400`,
                 ]}
             >
-                {schedule.isActive ? 'Active' : 'Inactive'}
+                {schedule.isActive ? 'Aktiv' : 'Inaktiv'}
             </p>
         </div>
         <ScheduleCronRow cron={schedule.cron} css={tw`mx-auto sm:mx-8 w-full sm:w-auto mt-4 sm:mt-0`} />
@@ -35,7 +36,7 @@ export default ({ schedule }: { schedule: Schedule }) => (
                     schedule.isActive && !schedule.isProcessing ? tw`bg-green-600` : tw`bg-neutral-400`,
                 ]}
             >
-                {schedule.isProcessing ? 'Processing' : schedule.isActive ? 'Active' : 'Inactive'}
+                {schedule.isProcessing ? 'Wird ausgeführt' : schedule.isActive ? 'Aktiv' : 'Inaktiv'}
             </p>
         </div>
     </>
diff --git a/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx b/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx
index f950b94..dc0c44d 100644
--- a/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx
+++ b/resources/scripts/components/server/schedules/ScheduleTaskRow.tsx
@@ -29,13 +29,13 @@ interface Props {
 const getActionDetails = (action: string): [string, any] => {
     switch (action) {
         case 'command':
-            return ['Send Command', faCode];
+            return ['Befehl senden', faCode];
         case 'power':
-            return ['Send Power Action', faToggleOn];
+            return ['Status des Servers ändern', faToggleOn];
         case 'backup':
-            return ['Create Backup', faFileArchive];
+            return ['Backup erstellen', faFileArchive];
         default:
-            return ['Unknown Action', faCode];
+            return ['Unbekannte Aufgabe', faCode];
     }
 };
 
@@ -76,13 +76,13 @@ export default ({ schedule, task }: Props) => {
                 onModalDismissed={() => setIsEditing(false)}
             />
             <ConfirmationModal
-                title={'Confirm task deletion'}
-                buttonText={'Delete Task'}
+                title={'Löschen bestätigen'}
+                buttonText={'Aufgabe löschen'}
                 onConfirmed={onConfirmDeletion}
                 visible={visible}
                 onModalDismissed={() => setVisible(false)}
             >
-                Are you sure you want to delete this task? This action cannot be undone.
+                Möchtest du wirklich diese Aufgabe löschen, dies kannst du nicht rückgängig machen.
             </ConfirmationModal>
             <FontAwesomeIcon icon={icon} css={tw`text-lg text-white hidden md:block`} />
             <div css={tw`flex-none sm:flex-1 w-full sm:w-auto overflow-x-auto`}>
@@ -90,7 +90,7 @@ export default ({ schedule, task }: Props) => {
                 {task.payload && (
                     <div css={tw`md:ml-6 mt-2`}>
                         {task.action === 'backup' && (
-                            <p css={tw`text-xs uppercase text-neutral-400 mb-1`}>Ignoring files & folders:</p>
+                            <p css={tw`text-xs uppercase text-neutral-400 mb-1`}>Dateien und Ordner ignorieren:</p>
                         )}
                         <div
                             css={tw`font-mono bg-neutral-800 rounded py-1 px-2 text-sm w-auto inline-block whitespace-pre-wrap break-all`}
@@ -105,7 +105,7 @@ export default ({ schedule, task }: Props) => {
                     <div css={tw`mr-6`}>
                         <div css={tw`flex items-center px-2 py-1 bg-yellow-500 text-yellow-800 text-sm rounded-full`}>
                             <Icon icon={faArrowCircleDown} css={tw`w-3 h-3 mr-2`} />
-                            Continues on Failure
+                            Fortsetzen, wenn es fehlschlägt
                         </div>
                     </div>
                 )}
@@ -113,14 +113,14 @@ export default ({ schedule, task }: Props) => {
                     <div css={tw`mr-6`}>
                         <div css={tw`flex items-center px-2 py-1 bg-neutral-500 text-sm rounded-full`}>
                             <Icon icon={faClock} css={tw`w-3 h-3 mr-2`} />
-                            {task.timeOffset}s later
+                            {task.timeOffset}s später
                         </div>
                     </div>
                 )}
                 <Can action={'schedule.update'}>
                     <button
                         type={'button'}
-                        aria-label={'Edit scheduled task'}
+                        aria-label={'Geplante Aufgabe bearbeiten'}
                         css={tw`block text-sm p-2 text-neutral-500 hover:text-neutral-100 transition-colors duration-150 mr-4 ml-auto sm:ml-0`}
                         onClick={() => setIsEditing(true)}
                     >
@@ -130,7 +130,7 @@ export default ({ schedule, task }: Props) => {
                 <Can action={'schedule.update'}>
                     <button
                         type={'button'}
-                        aria-label={'Delete scheduled task'}
+                        aria-label={'Geplante Aufgabe löschen'}
                         css={tw`block text-sm p-2 text-neutral-500 hover:text-red-600 transition-colors duration-150`}
                         onClick={() => setVisible(true)}
                     >
diff --git a/resources/scripts/components/server/schedules/TaskDetailsModal.tsx b/resources/scripts/components/server/schedules/TaskDetailsModal.tsx
index f54c0a2..7fd15fb 100644
--- a/resources/scripts/components/server/schedules/TaskDetailsModal.tsx
+++ b/resources/scripts/components/server/schedules/TaskDetailsModal.tsx
@@ -36,15 +36,15 @@ const schema = object().shape({
     action: string().required().oneOf(['command', 'power', 'backup']),
     payload: string().when('action', {
         is: (v) => v !== 'backup',
-        then: string().required('A task payload must be provided.'),
+        then: string().required('Eine Aufgabe muss bereitgestellt werden.'),
         otherwise: string(),
     }),
     continueOnFailure: boolean(),
     timeOffset: number()
-        .typeError('The time offset must be a valid number between 0 and 900.')
-        .required('A time offset value must be provided.')
-        .min(0, 'The time offset must be at least 0 seconds.')
-        .max(900, 'The time offset must be less than 900 seconds.'),
+        .typeError('Die Zeit muss zwischen 0 und 900 Sekunden sein')
+        .required('Es wurde keine Zeit festgelegt.')
+        .min(0, 'Der Zeitversatz muss mindestens 0 Sekunden betragen.')
+        .max(900, 'Die Zeit muss unter 900 Sekunden sein.'),
 });
 
 const ActionListener = () => {
@@ -83,7 +83,7 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
         if (backupLimit === 0 && values.action === 'backup') {
             setSubmitting(false);
             addError({
-                message: "A backup task cannot be created when the server's backup limit is set to 0.",
+                message: "Aktuell darf für diesen Server kein Backup erstellt werden.",
                 key: 'schedule:task',
             });
         } else {
@@ -119,25 +119,25 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
             {({ isSubmitting, values }) => (
                 <Form css={tw`m-0`}>
                     <FlashMessageRender byKey={'schedule:task'} css={tw`mb-4`} />
-                    <h2 css={tw`text-2xl mb-6`}>{task ? 'Edit Task' : 'Create Task'}</h2>
+                    <h2 css={tw`text-2xl mb-6`}>{task ? 'Aufgabe bearbeiten' : 'Aufgabe erstellen'}</h2>
                     <div css={tw`flex`}>
                         <div css={tw`mr-2 w-1/3`}>
-                            <Label>Action</Label>
+                            <Label>Kernaufgabe</Label>
                             <ActionListener />
                             <FormikFieldWrapper name={'action'}>
                                 <FormikField as={Select} name={'action'}>
-                                    <option value={'command'}>Send command</option>
-                                    <option value={'power'}>Send power action</option>
-                                    <option value={'backup'}>Create backup</option>
+                                    <option value={'command'}>Befehl senden</option>
+                                    <option value={'power'}>Serverstatus ändern</option>
+                                    <option value={'backup'}>Backup erstellen</option>
                                 </FormikField>
                             </FormikFieldWrapper>
                         </div>
                         <div css={tw`flex-1 ml-6`}>
                             <Field
                                 name={'timeOffset'}
-                                label={'Time offset (in seconds)'}
+                                label={'Wartezeit bis zur Fortsetzung (in sek.)'}
                                 description={
-                                    'The amount of time to wait after the previous task executes before running this one. If this is the first task on a schedule this will not be applied.'
+                                    'Zwischen den Aufgaben kannst du festlegen, wie lange gewartet werden soll. Dies gilt für alle Aufgaben in diesem Zeitplan.'
                                 }
                             />
                         </div>
@@ -145,30 +145,30 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
                     <div css={tw`mt-6`}>
                         {values.action === 'command' ? (
                             <div>
-                                <Label>Payload</Label>
+                                <Label>Aufgabe</Label>
                                 <FormikFieldWrapper name={'payload'}>
                                     <FormikField as={Textarea} name={'payload'} rows={6} />
                                 </FormikFieldWrapper>
                             </div>
                         ) : values.action === 'power' ? (
                             <div>
-                                <Label>Payload</Label>
+                                <Label>Aufgabe</Label>
                                 <FormikFieldWrapper name={'payload'}>
                                     <FormikField as={Select} name={'payload'}>
-                                        <option value={'start'}>Start the server</option>
-                                        <option value={'restart'}>Restart the server</option>
-                                        <option value={'stop'}>Stop the server</option>
-                                        <option value={'kill'}>Terminate the server</option>
+                                        <option value={'start'}>Server starten</option>
+                                        <option value={'restart'}>Server neustarten</option>
+                                        <option value={'stop'}>Server stoppen</option>
+                                        <option value={'kill'}>Server sofort stoppen (kann zu Datenverlust führen)</option>
                                     </FormikField>
                                 </FormikFieldWrapper>
                             </div>
                         ) : (
                             <div>
-                                <Label>Ignored Files</Label>
+                                <Label>Dateien ignorieren</Label>
                                 <FormikFieldWrapper
                                     name={'payload'}
                                     description={
-                                        'Optional. Include the files and folders to be excluded in this backup. By default, the contents of your .pteroignore file will be used. If you have reached your backup limit, the oldest backup will be rotated.'
+                                        'Optional. Du musst das nicht machen, wenn du bereits in den Verzeichnissen die Datei .ptignore angelegt hast.'
                                     }
                                 >
                                     <FormikField as={Textarea} name={'payload'} rows={6} />
@@ -179,13 +179,13 @@ const TaskDetailsModal = ({ schedule, task }: Props) => {
                     <div css={tw`mt-6 bg-neutral-700 border border-neutral-800 shadow-inner p-4 rounded`}>
                         <FormikSwitch
                             name={'continueOnFailure'}
-                            description={'Future tasks will be run when this task fails.'}
-                            label={'Continue on Failure'}
+                            description={'Andere Aufgaben werden fortgesetzt, wenn der davor fehlschlägt.'}
+                            label={'Bei Fehler fortsetzen'}
                         />
                     </div>
                     <div css={tw`flex justify-end mt-6`}>
                         <Button type={'submit'} disabled={isSubmitting}>
-                            {task ? 'Save Changes' : 'Create Task'}
+                            {task ? 'Speichern' : 'Aufgabe erstellen'}
                         </Button>
                     </div>
                 </Form>
diff --git a/resources/scripts/components/server/settings/ReinstallServerBox.tsx b/resources/scripts/components/server/settings/ReinstallServerBox.tsx
index 3a5a8da..8a0c96d 100644
--- a/resources/scripts/components/server/settings/ReinstallServerBox.tsx
+++ b/resources/scripts/components/server/settings/ReinstallServerBox.tsx
@@ -21,7 +21,7 @@ export default () => {
                 addFlash({
                     key: 'settings',
                     type: 'success',
-                    message: 'Your server has begun the reinstallation process.',
+                    message: 'Dein Server wird jetzt neu aufgesetzt...',
                 });
             })
             .catch((error) => {
@@ -37,28 +37,26 @@ export default () => {
     }, []);
 
     return (
-        <TitledGreyBox title={'Reinstall Server'} css={tw`relative`}>
+        <TitledGreyBox title={'Server neu aufsetzen'} css={tw`relative`}>
             <Dialog.Confirm
                 open={modalVisible}
-                title={'Confirm server reinstallation'}
-                confirm={'Yes, reinstall server'}
+                title={'WARNUNG'}
+                confirm={'Server jetzt neu aufsetzen'}
                 onClose={() => setModalVisible(false)}
                 onConfirmed={reinstall}
             >
-                Your server will be stopped and some files may be deleted or modified during this process, are you sure
-                you wish to continue?
+                Dein Server wird hierbei heruntergefahren und mit dem neuesten Update auf den aktuellen Stand gebracht.
+                Einige Dateien werden auch auf die Standard-Optionen zurückgesetzt. Mach dir am besten einmal ein Backup.
             </Dialog.Confirm>
             <p css={tw`text-sm`}>
-                Reinstalling your server will stop it, and then re-run the installation script that initially set it
-                up.&nbsp;
+                Das neu Aufsetzen des Servers wird den Server stoppen und einige Dateien modifizieren oder löschen.&nbsp;
                 <strong css={tw`font-medium`}>
-                    Some files may be deleted or modified during this process, please back up your data before
-                    continuing.
+                    Mache vorher ein Backup, bevor du etwas bereust.
                 </strong>
             </p>
             <div css={tw`mt-6 text-right`}>
                 <Button.Danger variant={Button.Variants.Secondary} onClick={() => setModalVisible(true)}>
-                    Reinstall Server
+                    Prozess starten
                 </Button.Danger>
             </div>
         </TitledGreyBox>
diff --git a/resources/scripts/components/server/settings/RenameServerBox.tsx b/resources/scripts/components/server/settings/RenameServerBox.tsx
index 9cb290a..8ec58ae 100644
--- a/resources/scripts/components/server/settings/RenameServerBox.tsx
+++ b/resources/scripts/components/server/settings/RenameServerBox.tsx
@@ -24,18 +24,18 @@ const RenameServerBox = () => {
     const { isSubmitting } = useFormikContext<Values>();
 
     return (
-        <TitledGreyBox title={'Change Server Details'} css={tw`relative`}>
+        <TitledGreyBox title={'Details des Servers'} css={tw`relative`}>
             <SpinnerOverlay visible={isSubmitting} />
             <Form css={tw`mb-0`}>
                 <Field id={'name'} name={'name'} label={'Server Name'} type={'text'} />
                 <div css={tw`mt-6`}>
-                    <Label>Server Description</Label>
+                    <Label>Beschreibung des Servers</Label>
                     <FormikFieldWrapper name={'description'}>
                         <FormikField as={Textarea} name={'description'} rows={3} />
                     </FormikFieldWrapper>
                 </div>
                 <div css={tw`mt-6 text-right`}>
-                    <Button type={'submit'}>Save</Button>
+                    <Button type={'submit'}>Speichern</Button>
                 </div>
             </Form>
         </TitledGreyBox>
diff --git a/resources/scripts/components/server/settings/SettingsContainer.tsx b/resources/scripts/components/server/settings/SettingsContainer.tsx
index bcdc80b..c3d2314 100644
--- a/resources/scripts/components/server/settings/SettingsContainer.tsx
+++ b/resources/scripts/components/server/settings/SettingsContainer.tsx
@@ -23,20 +23,20 @@ export default () => {
     const sftp = ServerContext.useStoreState((state) => state.server.data!.sftpDetails, isEqual);
 
     return (
-        <ServerContentBlock title={'Settings'}>
+        <ServerContentBlock title={'Einstellungen'}>
             <FlashMessageRender byKey={'settings'} css={tw`mb-4`} />
             <div css={tw`md:flex`}>
                 <div css={tw`w-full md:flex-1 md:mr-10`}>
                     <Can action={'file.sftp'}>
-                        <TitledGreyBox title={'SFTP Details'} css={tw`mb-6 md:mb-10`}>
+                        <TitledGreyBox title={'SFTP Zugangsdaten'} css={tw`mb-6 md:mb-10`}>
                             <div>
-                                <Label>Server Address</Label>
+                                <Label>Adresse</Label>
                                 <CopyOnClick text={`sftp://${ip(sftp.ip)}:${sftp.port}`}>
                                     <Input type={'text'} value={`sftp://${ip(sftp.ip)}:${sftp.port}`} readOnly />
                                 </CopyOnClick>
                             </div>
                             <div css={tw`mt-6`}>
-                                <Label>Username</Label>
+                                <Label>Benutzer</Label>
                                 <CopyOnClick text={`${username}.${id}`}>
                                     <Input type={'text'} value={`${username}.${id}`} readOnly />
                                 </CopyOnClick>
@@ -45,19 +45,19 @@ export default () => {
                                 <div css={tw`flex-1`}>
                                     <div css={tw`border-l-4 border-cyan-500 p-3`}>
                                         <p css={tw`text-xs text-neutral-200`}>
-                                            Your SFTP password is the same as the password you use to access this panel.
+                                            Das SFTP Passwort ist dasselbe, wie von deinem Login.
                                         </p>
                                     </div>
                                 </div>
                                 <div css={tw`ml-4`}>
                                     <a href={`sftp://${username}.${id}@${ip(sftp.ip)}:${sftp.port}`}>
-                                        <Button.Text variant={Button.Variants.Secondary}>Launch SFTP</Button.Text>
+                                        <Button.Text variant={Button.Variants.Secondary}>In Client öffnen</Button.Text>
                                     </a>
                                 </div>
                             </div>
                         </TitledGreyBox>
                     </Can>
-                    <TitledGreyBox title={'Debug Information'} css={tw`mb-6 md:mb-10`}>
+                    <TitledGreyBox title={'Standort des Servers'} css={tw`mb-6 md:mb-10`}>
                         <div css={tw`flex items-center justify-between text-sm`}>
                             <p>Node</p>
                             <code css={tw`font-mono bg-neutral-900 rounded py-1 px-2`}>{node}</code>
diff --git a/resources/scripts/components/server/startup/StartupContainer.tsx b/resources/scripts/components/server/startup/StartupContainer.tsx
index 35bb9a6..8cfbd1a 100644
--- a/resources/scripts/components/server/startup/StartupContainer.tsx
+++ b/resources/scripts/components/server/startup/StartupContainer.tsx
@@ -80,12 +80,12 @@ const StartupContainer = () => {
         !error || (error && isValidating) ? (
             <Spinner centered size={Spinner.Size.LARGE} />
         ) : (
-            <ServerError title={'Oops!'} message={httpErrorToHuman(error)} onRetry={() => mutate()} />
+            <ServerError title={'Hups!'} message={httpErrorToHuman(error)} onRetry={() => mutate()} />
         )
     ) : (
-        <ServerContentBlock title={'Startup Settings'} showFlashKey={'startup:image'}>
+        <ServerContentBlock title={'Einstellungen'} showFlashKey={'startup:image'}>
             <div css={tw`md:flex`}>
-                <TitledGreyBox title={'Startup Command'} css={tw`flex-1`}>
+                <TitledGreyBox title={'Startbefehl'} css={tw`flex-1`}>
                     <div css={tw`px-1 py-2`}>
                         <p css={tw`font-mono bg-neutral-900 rounded py-2 px-4`}>{data.invocation}</p>
                     </div>
@@ -107,8 +107,8 @@ const StartupContainer = () => {
                                 </Select>
                             </InputSpinner>
                             <p css={tw`text-xs text-neutral-300 mt-2`}>
-                                This is an advanced feature allowing you to select a Docker image to use when running
-                                this server instance.
+                                Das ist eine erweiterte Funktion, mit der man die Docker Version auswählen kann.
+                                Hier ist aber normalerweise kein Handlungsbedarf notwendig.
                             </p>
                         </>
                     ) : (
@@ -116,15 +116,15 @@ const StartupContainer = () => {
                             <Input disabled readOnly value={variables.dockerImage} />
                             {isCustomImage && (
                                 <p css={tw`text-xs text-neutral-300 mt-2`}>
-                                    This {"server's"} Docker image has been manually set by an administrator and cannot
-                                    be changed through this UI.
+                                    Dieser {"server's"} Docker Image wurde vom Betreiber fest eingestellt und
+                                    kann nicht verändert werden.
                                 </p>
                             )}
                         </>
                     )}
                 </TitledGreyBox>
             </div>
-            <h3 css={tw`mt-8 mb-2 text-2xl`}>Variables</h3>
+            <h3 css={tw`mt-8 mb-2 text-2xl`}>Variablen</h3>
             <div css={tw`grid gap-8 md:grid-cols-2`}>
                 {data.variables.map((variable) => (
                     <VariableBox key={variable.envVariable} variable={variable} />
diff --git a/resources/scripts/components/server/startup/VariableBox.tsx b/resources/scripts/components/server/startup/VariableBox.tsx
index ad73615..f2ed381 100644
--- a/resources/scripts/components/server/startup/VariableBox.tsx
+++ b/resources/scripts/components/server/startup/VariableBox.tsx
@@ -63,7 +63,7 @@ const VariableBox = ({ variable }: Props) => {
             title={
                 <p css={tw`text-sm uppercase`}>
                     {!variable.isEditable && (
-                        <span css={tw`bg-neutral-700 text-xs py-1 px-2 rounded-full mr-2 mb-1`}>Read Only</span>
+                        <span css={tw`bg-neutral-700 text-xs py-1 px-2 rounded-full mr-2 mb-1`}>Nur lesen</span>
                     )}
                     {variable.name}
                 </p>
diff --git a/resources/scripts/components/server/users/AddSubuserButton.tsx b/resources/scripts/components/server/users/AddSubuserButton.tsx
index d4847ce..cf9c34a 100644
--- a/resources/scripts/components/server/users/AddSubuserButton.tsx
+++ b/resources/scripts/components/server/users/AddSubuserButton.tsx
@@ -8,7 +8,7 @@ export default () => {
     return (
         <>
             <EditSubuserModal visible={visible} onModalDismissed={() => setVisible(false)} />
-            <Button onClick={() => setVisible(true)}>New User</Button>
+            <Button onClick={() => setVisible(true)}>Neuer Benutzer</Button>
         </>
     );
 };
diff --git a/resources/scripts/components/server/users/EditSubuserModal.tsx b/resources/scripts/components/server/users/EditSubuserModal.tsx
index c28d56f..51dabaf 100644
--- a/resources/scripts/components/server/users/EditSubuserModal.tsx
+++ b/resources/scripts/components/server/users/EditSubuserModal.tsx
@@ -96,9 +96,9 @@ const EditSubuserModal = ({ subuser }: Props) => {
             }
             validationSchema={object().shape({
                 email: string()
-                    .max(191, 'Email addresses must not exceed 191 characters.')
-                    .email('A valid email address must be provided.')
-                    .required('A valid email address must be provided.'),
+                    .max(191, 'Die E-Mail Adresse darf 191 Zeichen nicht überschreiten.')
+                    .email('Gib eine gültige E-Mail Adresse ein.')
+                    .required('Schau bitte nochmal drauf, die E-Mail stimmt nicht ganz.'),
                 permissions: array().of(string()),
             })}
         >
@@ -106,12 +106,12 @@ const EditSubuserModal = ({ subuser }: Props) => {
                 <div css={tw`flex justify-between`}>
                     <h2 css={tw`text-2xl`} ref={ref}>
                         {subuser
-                            ? `${canEditUser ? 'Modify' : 'View'} permissions for ${subuser.email}`
-                            : 'Create new subuser'}
+                            ? `${canEditUser ? 'Bearbeite' : 'Zeige'} Berechtigungen von ${subuser.email}`
+                            : 'Neuen Benutzer erstellen'}
                     </h2>
                     <div>
                         <Button type={'submit'} css={tw`w-full sm:w-auto`}>
-                            {subuser ? 'Save' : 'Invite User'}
+                            {subuser ? 'Speichern' : 'Benutzer einladen'}
                         </Button>
                     </div>
                 </div>
@@ -119,8 +119,7 @@ const EditSubuserModal = ({ subuser }: Props) => {
                 {!isRootAdmin && loggedInPermissions[0] !== '*' && (
                     <div css={tw`mt-4 pl-4 py-2 border-l-4 border-cyan-400`}>
                         <p css={tw`text-sm text-neutral-300`}>
-                            Only permissions which your account is currently assigned may be selected when creating or
-                            modifying other users.
+                            Der Benutzer kann nur die Berechtigungen nutzen, die hier genehmigt werden.
                         </p>
                     </div>
                 )}
@@ -128,9 +127,10 @@ const EditSubuserModal = ({ subuser }: Props) => {
                     <div css={tw`mt-6`}>
                         <Field
                             name={'email'}
-                            label={'User Email'}
+                            label={'E-Mail des Benutzers'}
                             description={
-                                'Enter the email address of the user you wish to invite as a subuser for this server.'
+                                'Gib die E-Mail Adresse ein, die dem Benutzer gehört.' +
+                                'Diese E-Mail Adresse muss einen Account besitzen.'
                             }
                         />
                     </div>
@@ -160,7 +160,7 @@ const EditSubuserModal = ({ subuser }: Props) => {
                 <Can action={subuser ? 'user.update' : 'user.create'}>
                     <div css={tw`pb-6 flex justify-end`}>
                         <Button type={'submit'} css={tw`w-full sm:w-auto`}>
-                            {subuser ? 'Save' : 'Invite User'}
+                            {subuser ? 'Speichern' : 'Einladen'}
                         </Button>
                     </div>
                 </Can>
diff --git a/resources/scripts/components/server/users/RemoveSubuserButton.tsx b/resources/scripts/components/server/users/RemoveSubuserButton.tsx
index defc6b4..6ceab1f 100644
--- a/resources/scripts/components/server/users/RemoveSubuserButton.tsx
+++ b/resources/scripts/components/server/users/RemoveSubuserButton.tsx
@@ -36,19 +36,19 @@ export default ({ subuser }: { subuser: Subuser }) => {
     return (
         <>
             <ConfirmationModal
-                title={'Delete this subuser?'}
-                buttonText={'Yes, remove subuser'}
+                title={'Benutzer entfernen?'}
+                buttonText={'Rechte entfernen'}
                 visible={showConfirmation}
                 showSpinnerOverlay={loading}
                 onConfirmed={() => doDeletion()}
                 onModalDismissed={() => setShowConfirmation(false)}
             >
-                Are you sure you wish to remove this subuser? They will have all access to this server revoked
-                immediately.
+                Bist du dir sicher? Hierbei entfernst du alle Rechte, die der Benutzer zu diesem Server bekommen hat.
+                Alternativ kannst du auch die Rechte einschränken.
             </ConfirmationModal>
             <button
                 type={'button'}
-                aria-label={'Delete subuser'}
+                aria-label={'Benutzer entfernen'}
                 css={tw`block text-sm p-2 text-neutral-500 hover:text-red-600 transition-colors duration-150`}
                 onClick={() => setShowConfirmation(true)}
             >
diff --git a/resources/scripts/components/server/users/UserRow.tsx b/resources/scripts/components/server/users/UserRow.tsx
index 45693c5..99c60a3 100644
--- a/resources/scripts/components/server/users/UserRow.tsx
+++ b/resources/scripts/components/server/users/UserRow.tsx
@@ -36,13 +36,13 @@ export default ({ subuser }: Props) => {
                     />
                     &nbsp;
                 </p>
-                <p css={tw`text-2xs text-neutral-500 uppercase hidden md:block`}>2FA Enabled</p>
+                <p css={tw`text-2xs text-neutral-500 uppercase hidden md:block`}>2-FA aktiviert</p>
             </div>
             <div css={tw`ml-4 hidden md:block`}>
                 <p css={tw`font-medium text-center`}>
                     {subuser.permissions.filter((permission) => permission !== 'websocket.connect').length}
                 </p>
-                <p css={tw`text-2xs text-neutral-500 uppercase`}>Permissions</p>
+                <p css={tw`text-2xs text-neutral-500 uppercase`}>Berechtigungen</p>
             </div>
             {subuser.uuid !== uuid && (
                 <>
diff --git a/resources/scripts/components/server/users/UsersContainer.tsx b/resources/scripts/components/server/users/UsersContainer.tsx
index a5d505f..c2e5828 100644
--- a/resources/scripts/components/server/users/UsersContainer.tsx
+++ b/resources/scripts/components/server/users/UsersContainer.tsx
@@ -48,10 +48,10 @@ export default () => {
     }
 
     return (
-        <ServerContentBlock title={'Users'}>
+        <ServerContentBlock title={'Benutzer'}>
             <FlashMessageRender byKey={'users'} css={tw`mb-4`} />
             {!subusers.length ? (
-                <p css={tw`text-center text-sm text-neutral-300`}>It looks like you don&apos;t have any subusers.</p>
+                <p css={tw`text-center text-sm text-neutral-300`}>Aktuell arbeitest du alleine am Server</p>
             ) : (
                 subusers.map((subuser) => <UserRow key={subuser.uuid} subuser={subuser} />)
             )}
diff --git a/resources/scripts/hoc/RequireServerPermission.tsx b/resources/scripts/hoc/RequireServerPermission.tsx
index cb7da2b..1ff586f 100644
--- a/resources/scripts/hoc/RequireServerPermission.tsx
+++ b/resources/scripts/hoc/RequireServerPermission.tsx
@@ -11,7 +11,7 @@ const RequireServerPermission: React.FC<RequireServerPermissionProps> = ({ child
         <Can
             action={permissions}
             renderOnError={
-                <ServerError title={'Access Denied'} message={'You do not have permission to access this page.'} />
+                <ServerError title={'Ungenügende Rechte'} message={'Der Zugang zu dieser Seite wurde für dich gesperrt.'} />
             }
         >
             {children}
diff --git a/resources/scripts/routers/routes.ts b/resources/scripts/routers/routes.ts
index 8fba642..af71083 100644
--- a/resources/scripts/routers/routes.ts
+++ b/resources/scripts/routers/routes.ts
@@ -46,23 +46,23 @@ export default {
     account: [
         {
             path: '/',
-            name: 'Account',
+            name: 'Mein Account',
             component: AccountOverviewContainer,
             exact: true,
         },
         {
             path: '/api',
-            name: 'API Credentials',
+            name: 'API-Zugangsdaten',
             component: AccountApiContainer,
         },
         {
             path: '/ssh',
-            name: 'SSH Keys',
+            name: 'SSH-Schlüssel',
             component: AccountSSHContainer,
         },
         {
             path: '/activity',
-            name: 'Activity',
+            name: 'Aktivitäten',
             component: ActivityLogContainer,
         },
     ],
@@ -70,14 +70,14 @@ export default {
         {
             path: '/',
             permission: null,
-            name: 'Console',
+            name: 'Konsole',
             component: ServerConsole,
             exact: true,
         },
         {
             path: '/files',
             permission: 'file.*',
-            name: 'Files',
+            name: 'Dateien',
             component: FileManagerContainer,
         },
         {
@@ -89,13 +89,13 @@ export default {
         {
             path: '/databases',
             permission: 'database.*',
-            name: 'Databases',
+            name: 'Datenbanken',
             component: DatabasesContainer,
         },
         {
             path: '/schedules',
             permission: 'schedule.*',
-            name: 'Schedules',
+            name: 'Zeitpläne',
             component: ScheduleContainer,
         },
         {
@@ -107,7 +107,7 @@ export default {
         {
             path: '/users',
             permission: 'user.*',
-            name: 'Users',
+            name: 'Benutzer',
             component: UsersContainer,
         },
         {
@@ -119,25 +119,25 @@ export default {
         {
             path: '/network',
             permission: 'allocation.*',
-            name: 'Network',
+            name: 'Netzwerk',
             component: NetworkContainer,
         },
         {
             path: '/startup',
             permission: 'startup.*',
-            name: 'Startup',
+            name: 'Startbefehl',
             component: StartupContainer,
         },
         {
             path: '/settings',
             permission: ['settings.*', 'file.sftp'],
-            name: 'Settings',
+            name: 'Einstellungen',
             component: SettingsContainer,
         },
         {
             path: '/activity',
             permission: 'activity.*',
-            name: 'Activity',
+            name: 'Aktivitäten',
             component: ServerActivityLogContainer,
         },
     ],
diff --git a/resources/scripts/state/flashes.ts b/resources/scripts/state/flashes.ts
index 088aca2..caac16e 100644
--- a/resources/scripts/state/flashes.ts
+++ b/resources/scripts/state/flashes.ts
@@ -26,7 +26,7 @@ const flashes: FlashStore = {
     }),
 
     addError: action((state, payload) => {
-        state.items.push({ type: 'error', title: 'Error', ...payload });
+        state.items.push({ type: 'error', title: 'Fehler', ...payload });
     }),
 
     clearAndAddHttpError: action((state, payload) => {
@@ -38,7 +38,7 @@ const flashes: FlashStore = {
             state.items = [
                 {
                     type: 'error',
-                    title: 'Error',
+                    title: 'Fehler',
                     key: payload.key,
                     message: httpErrorToHuman(payload.error),
                 },
diff --git a/resources/views/admin/api/index.blade.php b/resources/views/admin/api/index.blade.php
index d863c57..e6e3205 100644
--- a/resources/views/admin/api/index.blade.php
+++ b/resources/views/admin/api/index.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Application API
+    Anwendungs-API
 @endsection
 
 @section('content-header')
-    <h1>Application API<small>Control access credentials for managing this Panel via the API.</small></h1>
+    <h1>Anwendungs-API<small>Verwalte das Panel über die Application-API</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Application API</li>
+        <li class="active">Anwendungs-API</li>
     </ol>
 @endsection
 
@@ -17,18 +17,18 @@
         <div class="col-xs-12">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Credentials List</h3>
+                    <h3 class="box-title">Aktive Schlüssel</h3>
                     <div class="box-tools">
-                        <a href="{{ route('admin.api.new') }}" class="btn btn-sm btn-primary">Create New</a>
+                        <a href="{{ route('admin.api.new') }}" class="btn btn-sm btn-primary">Schlüssel erstellen</a>
                     </div>
                 </div>
                 <div class="box-body table-responsive no-padding">
                     <table class="table table-hover">
                         <tr>
-                            <th>Key</th>
-                            <th>Memo</th>
-                            <th>Last Used</th>
-                            <th>Created</th>
+                            <th>API-Schlüssel</th>
+                            <th>Notiz</th>
+                            <th>Zuletzt genutzt</th>
+                            <th>Erstellt</th>
                             <th></th>
                         </tr>
                         @foreach($keys as $key)
@@ -66,12 +66,13 @@
                 event.preventDefault();
                 swal({
                     type: 'error',
-                    title: 'Revoke API Key',
-                    text: 'Once this API key is revoked any applications currently using it will stop working.',
+                    title: 'API-Schlüssel ungültig machen',
+                    text: 'Alle Verknüpfungen mit diesem Schlüssel werden nach Bestätigung unbenutzbar.',
                     showCancelButton: true,
+                    cancelButtonText: 'Abbrechen',
                     allowOutsideClick: true,
                     closeOnConfirm: false,
-                    confirmButtonText: 'Revoke',
+                    confirmButtonText: 'Ungültig machen',
                     confirmButtonColor: '#d9534f',
                     showLoaderOnConfirm: true
                 }, function () {
@@ -85,15 +86,15 @@
                         swal({
                             type: 'success',
                             title: '',
-                            text: 'API Key has been revoked.'
+                            text: 'API-Schlüssel ist jetzt nicht mehr aktiv.'
                         });
                         self.parent().parent().slideUp();
                     }).fail(function (jqXHR) {
                         console.error(jqXHR);
                         swal({
                             type: 'error',
-                            title: 'Whoops!',
-                            text: 'An error occurred while attempting to revoke this key.'
+                            title: 'Hups!',
+                            text: 'Beim verändern der Gültigkeit trat ein Fehler auf.'
                         });
                     });
                 });
diff --git a/resources/views/admin/api/new.blade.php b/resources/views/admin/api/new.blade.php
index b5876ee..be91f38 100644
--- a/resources/views/admin/api/new.blade.php
+++ b/resources/views/admin/api/new.blade.php
@@ -1,15 +1,15 @@
 @extends('layouts.admin')
 
 @section('title')
-    Application API
+    Anwendungs-API
 @endsection
 
 @section('content-header')
-    <h1>Application API<small>Create a new application API key.</small></h1>
+    <h1>Anwendungs-API<small>Erstelle einen neuen API-Schlüssel</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.api.index') }}">Application API</a></li>
-        <li class="active">New Credentials</li>
+        <li><a href="{{ route('admin.api.index') }}">Anwendungs-API</a></li>
+        <li class="active">Neuer API-Schlüssel</li>
     </ol>
 @endsection
 
@@ -19,7 +19,7 @@
             <div class="col-sm-8 col-xs-12">
                 <div class="box box-primary">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Select Permissions</h3>
+                        <h3 class="box-title">Berechtigungen festlegen</h3>
                     </div>
                     <div class="box-body table-responsive no-padding">
                         <table class="table table-hover">
@@ -28,15 +28,15 @@
                                     <td class="col-sm-3 strong">{{ str_replace('_', ' ', title_case($resource)) }}</td>
                                     <td class="col-sm-3 radio radio-primary text-center">
                                         <input type="radio" id="r_{{ $resource }}" name="r_{{ $resource }}" value="{{ $permissions['r'] }}">
-                                        <label for="r_{{ $resource }}">Read</label>
+                                        <label for="r_{{ $resource }}">Lesen</label>
                                     </td>
                                     <td class="col-sm-3 radio radio-primary text-center">
                                         <input type="radio" id="rw_{{ $resource }}" name="r_{{ $resource }}" value="{{ $permissions['rw'] }}">
-                                        <label for="rw_{{ $resource }}">Read &amp; Write</label>
+                                        <label for="rw_{{ $resource }}">Lesen &amp; Schreiben</label>
                                     </td>
                                     <td class="col-sm-3 radio text-center">
                                         <input type="radio" id="n_{{ $resource }}" name="r_{{ $resource }}" value="{{ $permissions['n'] }}" checked>
-                                        <label for="n_{{ $resource }}">None</label>
+                                        <label for="n_{{ $resource }}">Keine Rechte</label>
                                     </td>
                                 </tr>
                             @endforeach
@@ -48,14 +48,14 @@
                 <div class="box box-primary">
                     <div class="box-body">
                         <div class="form-group">
-                            <label class="control-label" for="memoField">Description <span class="field-required"></span></label>
+                            <label class="control-label" for="memoField">Beschreibung <span class="field-required"></span></label>
                             <input id="memoField" type="text" name="memo" class="form-control">
                         </div>
-                        <p class="text-muted">Once you have assigned permissions and created this set of credentials you will be unable to come back and edit it. If you need to make changes down the road you will need to create a new set of credentials.</p>
+                        <p class="text-muted">Nachdem du Berechtigungen zugewiesen und diesen Satz von Anmeldeinformationen erstellt hast, kannst du ihn nicht mehr bearbeiten. Wenn Sie später Änderungen vornehmen müssen, musst du einen neuen Satz von Anmeldeinformationen erstellen.</p>
                     </div>
                     <div class="box-footer">
                         {{ csrf_field() }}
-                        <button type="submit" class="btn btn-success btn-sm pull-right">Create Credentials</button>
+                        <button type="submit" class="btn btn-success btn-sm pull-right">API-Schlüssel aktivieren</button>
                     </div>
                 </div>
             </div>
diff --git a/resources/views/admin/databases/index.blade.php b/resources/views/admin/databases/index.blade.php
index e4c69c5..40d026c 100644
--- a/resources/views/admin/databases/index.blade.php
+++ b/resources/views/admin/databases/index.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Database Hosts
+    Datenbank Hosts
 @endsection
 
 @section('content-header')
-    <h1>Database Hosts<small>Database hosts that servers can have databases created on.</small></h1>
+    <h1>Datenbanken Hosts<small>Erstelle hier einen zentralen Host-Punkt für weitere Datenbanken.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Database Hosts</li>
+        <li class="active">Datenbank-Hosts</li>
     </ol>
 @endsection
 
@@ -17,9 +17,9 @@
     <div class="col-xs-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Host List</h3>
+                <h3 class="box-title">Host Liste</h3>
                 <div class="box-tools">
-                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newHostModal">Create New</button>
+                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newHostModal">Erstellen</button>
                 </div>
             </div>
             <div class="box-body table-responsive no-padding">
@@ -30,8 +30,8 @@
                             <th>Name</th>
                             <th>Host</th>
                             <th>Port</th>
-                            <th>Username</th>
-                            <th class="text-center">Databases</th>
+                            <th>Benutzername</th>
+                            <th class="text-center">Datenbanken</th>
                             <th class="text-center">Node</th>
                         </tr>
                         @foreach ($hosts as $host)
@@ -46,7 +46,7 @@
                                     @if(! is_null($host->node))
                                         <a href="{{ route('admin.nodes.view', $host->node->id) }}">{{ $host->node->name }}</a>
                                     @else
-                                        <span class="label label-default">None</span>
+                                        <span class="label label-default">Keine</span>
                                     @endif
                                 </td>
                             </tr>
@@ -63,42 +63,42 @@
             <form action="{{ route('admin.databases') }}" method="POST">
                 <div class="modal-header">
                     <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
-                    <h4 class="modal-title">Create New Database Host</h4>
+                    <h4 class="modal-title">Database Host hinzufügen</h4>
                 </div>
                 <div class="modal-body">
                     <div class="form-group">
                         <label for="pName" class="form-label">Name</label>
                         <input type="text" name="name" id="pName" class="form-control" />
-                        <p class="text-muted small">A short identifier used to distinguish this location from others. Must be between 1 and 60 characters, for example, <code>us.nyc.lvl3</code>.</p>
+                        <p class="text-muted small">Eine kurze Beschreibung zum Standort, wo der Server steht. Zum Beispiel:<code>de.srv1.hl4</code>.</p>
                     </div>
                     <div class="row">
                         <div class="col-md-6">
                             <label for="pHost" class="form-label">Host</label>
                             <input type="text" name="host" id="pHost" class="form-control" />
-                            <p class="text-muted small">The IP address or FQDN that should be used when attempting to connect to this MySQL host <em>from the panel</em> to add new databases.</p>
+                            <p class="text-muted small">Die IP-Adresse oder Domain, worüber die Datenbank und <em>das Panel läuft.</em></p>
                         </div>
                         <div class="col-md-6">
                             <label for="pPort" class="form-label">Port</label>
                             <input type="text" name="port" id="pPort" class="form-control" value="3306"/>
-                            <p class="text-muted small">The port that MySQL is running on for this host.</p>
+                            <p class="text-muted small">Der Port, worüber alle Datenbanken laufen.</p>
                         </div>
                     </div>
                     <div class="row">
                         <div class="col-md-6">
-                            <label for="pUsername" class="form-label">Username</label>
+                            <label for="pUsername" class="form-label">Benutzername</label>
                             <input type="text" name="username" id="pUsername" class="form-control" />
-                            <p class="text-muted small">The username of an account that has enough permissions to create new users and databases on the system.</p>
+                            <p class="text-muted small">Der Benutzername eines Kontos, das über ausreichende Berechtigungen verfügt, um neue Benutzer und Datenbanken auf dem System zu erstellen.</p>
                         </div>
                         <div class="col-md-6">
-                            <label for="pPassword" class="form-label">Password</label>
+                            <label for="pPassword" class="form-label">Passwort</label>
                             <input type="password" name="password" id="pPassword" class="form-control" />
-                            <p class="text-muted small">The password to the account defined.</p>
+                            <p class="text-muted small">Das Passwort für das definierte Konto.</p>
                         </div>
                     </div>
                     <div class="form-group">
                         <label for="pNodeId" class="form-label">Linked Node</label>
                         <select name="node_id" id="pNodeId" class="form-control">
-                            <option value="">None</option>
+                            <option value="">Keine</option>
                             @foreach($locations as $location)
                                 <optgroup label="{{ $location->short }}">
                                     @foreach($location->nodes as $node)
@@ -107,14 +107,14 @@
                                 </optgroup>
                             @endforeach
                         </select>
-                        <p class="text-muted small">This setting does nothing other than default to this database host when adding a database to a server on the selected node.</p>
+                        <p class="text-muted small">Diese Einstellung bewirkt nichts anderes als standardmäßig diesen Datenbankhost, wenn eine Datenbank zu einem Server auf dem ausgewählten Knoten hinzugefügt wird.</p>
                     </div>
                 </div>
                 <div class="modal-footer">
-                    <p class="text-danger small text-left">The account defined for this database host <strong>must</strong> have the <code>WITH GRANT OPTION</code> permission. If the defined account does not have this permission requests to create databases <em>will</em> fail. <strong>Do not use the same account details for MySQL that you have defined for this panel.</strong></p>
+                    <p class="text-danger small text-left">Der Account der die Datenbank verwaltet <strong>muss</strong> die <code>WITH GRANT OPTION</code> Berechtigung haben. Wenn das nicht der Fall ist, dann wird das Erstellen einer Datenbank<em>fehlschlagen</em><strong>. Nutze nicht denselben Account für das Panel und MySQL. Zwei verschiedene sind wichtig!</strong></p>
                     {!! csrf_field() !!}
-                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
-                    <button type="submit" class="btn btn-success btn-sm">Create</button>
+                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
+                    <button type="submit" class="btn btn-success btn-sm">Erstellen</button>
                 </div>
             </form>
         </div>
diff --git a/resources/views/admin/databases/view.blade.php b/resources/views/admin/databases/view.blade.php
index e105751..e4b6ee1 100644
--- a/resources/views/admin/databases/view.blade.php
+++ b/resources/views/admin/databases/view.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Database Hosts &rarr; View &rarr; {{ $host->name }}
+    Datenbank-Hosts &rarr; Betrachte &rarr; {{ $host->name }}
 @endsection
 
 @section('content-header')
-    <h1>{{ $host->name }}<small>Viewing associated databases and details for this database host.</small></h1>
+    <h1>{{ $host->name }}<small>Anzeigen der zugehörigen Datenbanken und Details für diesen Datenbankhost.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.databases') }}">Database Hosts</a></li>
+        <li><a href="{{ route('admin.databases') }}">Datenbank-Hosts</a></li>
         <li class="active">{{ $host->name }}</li>
     </ol>
 @endsection
@@ -19,7 +19,7 @@
         <div class="col-sm-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Host Details</h3>
+                    <h3 class="box-title">Host-Details</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
@@ -29,17 +29,17 @@
                     <div class="form-group">
                         <label for="pHost" class="form-label">Host</label>
                         <input type="text" id="pHost" name="host" class="form-control" value="{{ old('host', $host->host) }}" />
-                        <p class="text-muted small">The IP address or FQDN that should be used when attempting to connect to this MySQL host <em>from the panel</em> to add new databases.</p>
+                        <p class="text-muted small">Die IP-Adresse oder der FQDN, der verwendet werden soll, wenn versucht wird, eine Verbindung zu diesem MySQL-Host <em>vom Panel</em> aus herzustellen, um neue Datenbanken hinzuzufügen.</p>
                     </div>
                     <div class="form-group">
                         <label for="pPort" class="form-label">Port</label>
                         <input type="text" id="pPort" name="port" class="form-control" value="{{ old('port', $host->port) }}" />
-                        <p class="text-muted small">The port that MySQL is running on for this host.</p>
+                        <p class="text-muted small">Der Port, auf dem MySQL-Server für diesen Host läuft.</p>
                     </div>
                     <div class="form-group">
-                        <label for="pNodeId" class="form-label">Linked Node</label>
+                        <label for="pNodeId" class="form-label">Verlinkte Node</label>
                         <select name="node_id" id="pNodeId" class="form-control">
-                            <option value="">None</option>
+                            <option value="">Keine</option>
                             @foreach($locations as $location)
                                 <optgroup label="{{ $location->short }}">
                                     @foreach($location->nodes as $node)
@@ -48,7 +48,7 @@
                                 </optgroup>
                             @endforeach
                         </select>
-                        <p class="text-muted small">This setting does nothing other than default to this database host when adding a database to a server on the selected node.</p>
+                        <p class="text-muted small">Diese Einstellung bewirkt nichts anderes, als dass beim Hinzufügen einer Datenbank zu einem Server auf der ausgewählten Node standardmäßig dieser Datenbankhost verwendet wird.</p>
                     </div>
                 </div>
             </div>
@@ -56,25 +56,25 @@
         <div class="col-sm-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">User Details</h3>
+                    <h3 class="box-title">Benutzer-Details</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
-                        <label for="pUsername" class="form-label">Username</label>
+                        <label for="pUsername" class="form-label">Benutzername</label>
                         <input type="text" name="username" id="pUsername" class="form-control" value="{{ old('username', $host->username) }}" />
-                        <p class="text-muted small">The username of an account that has enough permissions to create new users and databases on the system.</p>
+                        <p class="text-muted small">Der Benutzername eines Kontos, das über ausreichende Berechtigungen verfügt, um neue Benutzer und Datenbanken im System zu erstellen.</p>
                     </div>
                     <div class="form-group">
-                        <label for="pPassword" class="form-label">Password</label>
+                        <label for="pPassword" class="form-label">Passwort</label>
                         <input type="password" name="password" id="pPassword" class="form-control" />
-                        <p class="text-muted small">The password to the account defined. Leave blank to continue using the assigned password.</p>
+                        <p class="text-muted small">Das Passwort für das definierte Konto. Leer lassen, um weiterhin das zugewiesene Passwort zu verwenden.</p>
                     </div>
                     <hr />
-                    <p class="text-danger small text-left">The account defined for this database host <strong>must</strong> have the <code>WITH GRANT OPTION</code> permission. If the defined account does not have this permission requests to create databases <em>will</em> fail. <strong>Do not use the same account details for MySQL that you have defined for this panel.</strong></p>
+                    <p class="text-danger small text-left">Der Account der die Datenbank verwaltet <strong>muss</strong> die <code>WITH GRANT OPTION</code> Berechtigung haben. Wenn das nicht der Fall ist, dann wird das Erstellen einer Datenbankfehlschlagen. <strong>Nutze nicht denselben Account für das Panel und MySQL. Zwei verschiedene sind wichtig!</strong></p>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Save</button>
+                    <button name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Speichern</button>
                     <button name="_method" value="DELETE" class="btn btn-sm btn-danger pull-left muted muted-hover"><i class="fa fa-trash-o"></i></button>
                 </div>
             </div>
@@ -85,16 +85,16 @@
     <div class="col-xs-12">
         <div class="box">
             <div class="box-header with-border">
-                <h3 class="box-title">Databases</h3>
+                <h3 class="box-title">Datenbank</h3>
             </div>
             <div class="box-body table-responsive no-padding">
                 <table class="table table-hover">
                     <tr>
                         <th>Server</th>
-                        <th>Database Name</th>
-                        <th>Username</th>
-                        <th>Connections From</th>
-                        <th>Max Connections</th>
+                        <th>Datenbankname</th>
+                        <th>Benutzername</th>
+                        <th>Verbindungen von</th>
+                        <th>Maximale Verbindungen</th>
                         <th></th>
                     </tr>
                     @foreach($databases as $database)
@@ -106,11 +106,11 @@
                             @if($database->max_connections != null)
                                 <td class="middle">{{ $database->max_connections }}</td>
                             @else
-                                <td class="middle">Unlimited</td>
+                                <td class="middle">Kein Limit</td>
                             @endif
                             <td class="text-center">
                                 <a href="{{ route('admin.servers.view.database', $database->getRelation('server')->id) }}">
-                                    <button class="btn btn-xs btn-primary">Manage</button>
+                                    <button class="btn btn-xs btn-primary">Verwalten</button>
                                 </a>
                             </td>
                         </tr>
diff --git a/resources/views/admin/eggs/new.blade.php b/resources/views/admin/eggs/new.blade.php
index 9a0a078..2159965 100644
--- a/resources/views/admin/eggs/new.blade.php
+++ b/resources/views/admin/eggs/new.blade.php
@@ -1,15 +1,15 @@
 @extends('layouts.admin')
 
 @section('title')
-    Nests &rarr; New Egg
+    Nests &rarr; Neues Egg
 @endsection
 
 @section('content-header')
-    <h1>New Egg<small>Create a new Egg to assign to servers.</small></h1>
+    <h1>Neues Egg<small>Erstellen Sie ein neues Egg, um es Servern zuzuweisen.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
-        <li class="active">New Egg</li>
+        <li class="active">Neues Egg</li>
     </ol>
 @endsection
 
@@ -19,43 +19,43 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Configuration</h3>
+                    <h3 class="box-title">Konfiguration</h3>
                 </div>
                 <div class="box-body">
                     <div class="row">
                         <div class="col-sm-6">
                             <div class="form-group">
-                                <label for="pNestId" class="form-label">Associated Nest</label>
+                                <label for="pNestId" class="form-label">Zugehöriges Nest</label>
                                 <div>
                                     <select name="nest_id" id="pNestId">
                                         @foreach($nests as $nest)
                                             <option value="{{ $nest->id }}" {{ old('nest_id') != $nest->id ?: 'selected' }}>{{ $nest->name }} &lt;{{ $nest->author }}&gt;</option>
                                         @endforeach
                                     </select>
-                                    <p class="text-muted small">Think of a Nest as a category. You can put multiple Eggs in a nest, but consider putting only Eggs that are related to each other in each Nest.</p>
+                                    <p class="text-muted small">Stellen Sie sich ein Nest als eine Kategorie vor. Du kannst mehrere Eggs in ein Nest legen, aber überlege dir, nur Eggs in jedes Nest zu legen, die miteinander verwandt sind.</p>
                                 </div>
                             </div>
                             <div class="form-group">
                                 <label for="pName" class="form-label">Name</label>
                                 <input type="text" id="pName" name="name" value="{{ old('name') }}" class="form-control" />
-                                <p class="text-muted small">A simple, human-readable name to use as an identifier for this Egg. This is what users will see as their game server type.</p>
+                                <p class="text-muted small">Ein einfacher, für Menschen lesbarer Name, der als Kennung für dieses Egg verwendet werden kann. Dies wird den Benutzern als ihr Gameserver-Typ angezeigt.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pDescription" class="form-label">Description</label>
+                                <label for="pDescription" class="form-label">Beschreibung</label>
                                 <textarea id="pDescription" name="description" class="form-control" rows="8">{{ old('description') }}</textarea>
-                                <p class="text-muted small">A description of this Egg.</p>
+                                <p class="text-muted small">Eine Beschreibung dieses Eggs.</p>
                             </div>
                             <div class="form-group">
                                 <div class="checkbox checkbox-primary no-margin-bottom">
                                     <input id="pForceOutgoingIp" name="force_outgoing_ip" type="checkbox" value="1" {{ \Pterodactyl\Helpers\Utilities::checked('force_outgoing_ip', 0) }} />
-                                    <label for="pForceOutgoingIp" class="strong">Force Outgoing IP</label>
+                                    <label for="pForceOutgoingIp" class="strong">Ausgehende IP erzwingen</label>
                                     <p class="text-muted small">
-                                        Forces all outgoing network traffic to have its Source IP NATed to the IP of the server's primary allocation IP.
-                                        Required for certain games to work properly when the Node has multiple public IP addresses.
+                                        Erzwingt, dass der gesamte ausgehende Netzwerkverkehr seine Quell-IP mit der IP der primären Zuordnungs-IP des Servers NATiert.
+                                        Erforderlich, damit bestimmte Spiele ordnungsgemäß funktionieren, wenn die Node mehrere öffentliche IP-Adressen hat.
                                         <br>
                                         <strong>
-                                            Enabling this option will disable internal networking for any servers using this egg,
-                                            causing them to be unable to internally access other servers on the same node.
+                                            Das Aktivieren dieser Option deaktiviert das interne Netzwerk für alle Server,
+                                            die dieses Egg verwenden, wodurch sie nicht mehr intern auf andere Server auf dieselbe Node zugreifen können.
                                         </strong>
                                     </p>
                                 </div>
@@ -65,12 +65,12 @@
                             <div class="form-group">
                                 <label for="pDockerImage" class="control-label">Docker Images</label>
                                 <textarea id="pDockerImages" name="docker_images" rows="4" placeholder="quay.io/pterodactyl/service" class="form-control">{{ old('docker_images') }}</textarea>
-                                <p class="text-muted small">The docker images available to servers using this egg. Enter one per line. Users will be able to select from this list of images if more than one value is provided.</p>
+                                <p class="text-muted small">Die Docker-Images, die für Server, die dieses Egg verwenden, verfügbar sind. Gib einen Wert pro Zeile ein. Wenn mehr als ein Wert angegeben wird, können die Nutzer aus dieser Liste von Images auswählen.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pStartup" class="control-label">Startup Command</label>
+                                <label for="pStartup" class="control-label">Startbefehl</label>
                                 <textarea id="pStartup" name="startup" class="form-control" rows="10">{{ old('startup') }}</textarea>
-                                <p class="text-muted small">The default startup command that should be used for new servers created with this Egg. You can change this per-server as needed.</p>
+                                <p class="text-muted small">Der standardmäßige Startbefehl, der für neue Server verwendet werden sollte, die mit diesem Egg erstellt wurden. Du kannst dies für jeden Server nach Bedarf ändern.</p>
                             </div>
                         </div>
                     </div>
@@ -80,51 +80,51 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Process Management</h3>
+                    <h3 class="box-title">Prozessmanagement</h3>
                 </div>
                 <div class="box-body">
                     <div class="row">
                         <div class="col-xs-12">
                             <div class="alert alert-warning">
-                                <p>All fields are required unless you select a separate option from the 'Copy Settings From' dropdown, in which case fields may be left blank to use the values from that option.</p>
+                                <p>Alle Felder müssen ausgefüllt werden, es sei denn, du wählst eine andere Option aus dem Dropdown-Menü "Einstellungen kopieren von". In diesem Fall können die Felder leer gelassen werden, um die Werte aus dieser Option zu verwenden.</p>
                             </div>
                         </div>
                         <div class="col-sm-6">
                             <div class="form-group">
-                                <label for="pConfigFrom" class="form-label">Copy Settings From</label>
+                                <label for="pConfigFrom" class="form-label">Einstellungen kopieren von</label>
                                 <select name="config_from" id="pConfigFrom" class="form-control">
-                                    <option value="">None</option>
+                                    <option value="">Keinem</option>
                                 </select>
-                                <p class="text-muted small">If you would like to default to settings from another Egg select it from the dropdown above.</p>
+                                <p class="text-muted small">Wenn du die Einstellungen eines anderen Eggs verwenden möchtest, wähle es aus dem Dropdown-Menü oben aus.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pConfigStop" class="form-label">Stop Command</label>
+                                <label for="pConfigStop" class="form-label">Stop-Befehl</label>
                                 <input type="text" id="pConfigStop" name="config_stop" class="form-control" value="{{ old('config_stop') }}" />
-                                <p class="text-muted small">The command that should be sent to server processes to stop them gracefully. If you need to send a <code>SIGINT</code> you should enter <code>^C</code> here.</p>
+                                <p class="text-muted small">Der Befehl, der an Serverprozesse gesendet werden sollte, um sie ordnungsgemäß zu stoppen. Wenn du einen <code>SIGINT</code> senden musst, gib hier <code>^C</code> ein.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pConfigLogs" class="form-label">Log Configuration</label>
+                                <label for="pConfigLogs" class="form-label">Log-Konfiguration</label>
                                 <textarea data-action="handle-tabs" id="pConfigLogs" name="config_logs" class="form-control" rows="6">{{ old('config_logs') }}</textarea>
-                                <p class="text-muted small">This should be a JSON representation of where log files are stored, and whether or not the daemon should be creating custom logs.</p>
+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation sein, wo Log-Dateien gespeichert werden und ob der Daemon benutzerdefinierte Protokolle erstellen soll oder nicht.</p>
                             </div>
                         </div>
                         <div class="col-sm-6">
                             <div class="form-group">
-                                <label for="pConfigFiles" class="form-label">Configuration Files</label>
+                                <label for="pConfigFiles" class="form-label">Konfigurationsdateien</label>
                                 <textarea data-action="handle-tabs" id="pConfigFiles" name="config_files" class="form-control" rows="6">{{ old('config_files') }}</textarea>
-                                <p class="text-muted small">This should be a JSON representation of configuration files to modify and what parts should be changed.</p>
+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation der zu ändernden Konfigurationsdateien und der zu ändernden Teile sein.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pConfigStartup" class="form-label">Start Configuration</label>
+                                <label for="pConfigStartup" class="form-label">Startkonfiguration</label>
                                 <textarea data-action="handle-tabs" id="pConfigStartup" name="config_startup" class="form-control" rows="6">{{ old('config_startup') }}</textarea>
-                                <p class="text-muted small">This should be a JSON representation of what values the daemon should be looking for when booting a server to determine completion.</p>
+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation dessen sein, nach welchen Werten der Daemon beim Booten eines Servers suchen sollte, um den Abschluss zu bestimmen.</p>
                             </div>
                         </div>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" class="btn btn-success btn-sm pull-right">Create</button>
+                    <button type="submit" class="btn btn-success btn-sm pull-right">Erstellen</button>
                 </div>
             </div>
         </div>
@@ -141,7 +141,7 @@
         $('#pConfigFrom').select2();
     });
     $('#pNestId').on('change', function (event) {
-        $('#pConfigFrom').html('<option value="">None</option>').select2({
+        $('#pConfigFrom').html('<option value="">Keinem</option>').select2({
             data: $.map(_.get(Pterodactyl.nests, $(this).val() + '.eggs', []), function (item) {
                 return {
                     id: item.id,
diff --git a/resources/views/admin/eggs/scripts.blade.php b/resources/views/admin/eggs/scripts.blade.php
index 5bbc9ee..c78a73c 100644
--- a/resources/views/admin/eggs/scripts.blade.php
+++ b/resources/views/admin/eggs/scripts.blade.php
@@ -1,17 +1,17 @@
 @extends('layouts.admin')
 
 @section('title')
-    Nests &rarr; Egg: {{ $egg->name }} &rarr; Install Script
+    Nests &rarr; Egg: {{ $egg->name }} &rarr; Installationsskript
 @endsection
 
 @section('content-header')
-    <h1>{{ $egg->name }}<small>Manage the install script for this Egg.</small></h1>
+    <h1>{{ $egg->name }}<small>Verwalten des Installationsskripts für dieses Egg.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
         <li><a href="{{ route('admin.nests.view', $egg->nest->id) }}">{{ $egg->nest->name }}</a></li>
         <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">{{ $egg->name }}</a></li>
-        <li class="active">{{ $egg->name }}</li>
+        <li class="active">Installationsskript</li>
     </ol>
 @endsection
 
@@ -20,9 +20,9 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Configuration</a></li>
-                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variables</a></li>
-                <li class="active"><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Install Script</a></li>
+                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Konfiguration</a></li>
+                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variablen</a></li>
+                <li class="active"><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Installationsskript</a></li>
             </ul>
         </div>
     </div>
@@ -32,12 +32,12 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Install Script</h3>
+                    <h3 class="box-title">Installationsskript</h3>
                 </div>
                 @if(! is_null($egg->copyFrom))
                     <div class="box-body">
                         <div class="callout callout-warning no-margin">
-                            This service option is copying installation scripts and container options from <a href="{{ route('admin.nests.egg.view', $egg->copyFrom->id) }}">{{ $egg->copyFrom->name }}</a>. Any changes you make to this script will not apply unless you select "None" from the dropdown box below.
+                            Diese Dienstoption kopiert Installationsskripts und Containeroptionen von <a href="{{ route('admin.nests.egg.view', $egg->copyFrom->id) }}">{{ $egg->copyFrom->name }}</a>. Alle Änderungen, die du an diesem Skript vornimmst, werden nicht übernommen, es sei denn, du wählst in der Dropdown-Box unten "Keinem" aus.
                         </div>
                     </div>
                 @endif
@@ -47,29 +47,29 @@
                 <div class="box-body">
                     <div class="row">
                         <div class="form-group col-sm-4">
-                            <label class="control-label">Copy Script From</label>
+                            <label class="control-label">Skript kopieren von</label>
                             <select id="pCopyScriptFrom" name="copy_script_from">
-                                <option value="">None</option>
+                                <option value="">Keinem</option>
                                 @foreach($copyFromOptions as $opt)
                                     <option value="{{ $opt->id }}" {{ $egg->copy_script_from !== $opt->id ?: 'selected' }}>{{ $opt->name }}</option>
                                 @endforeach
                             </select>
-                            <p class="text-muted small">If selected, script above will be ignored and script from selected option will be used in place.</p>
+                            <p class="text-muted small">Wenn diese Option ausgewählt ist, wird das obige Skript ignoriert und stattdessen das Skript der ausgewählten Option verwendet.</p>
                         </div>
                         <div class="form-group col-sm-4">
-                            <label class="control-label">Script Container</label>
+                            <label class="control-label">Skript-Container</label>
                             <input type="text" name="script_container" class="form-control" value="{{ $egg->script_container }}" />
-                            <p class="text-muted small">Docker container to use when running this script for the server.</p>
+                            <p class="text-muted small">Docker-Container, der beim Ausführen dieses Skripts für den Server verwendet werden soll.</p>
                         </div>
                         <div class="form-group col-sm-4">
-                            <label class="control-label">Script Entrypoint Command</label>
+                            <label class="control-label">Skript-Startbefehl</label>
                             <input type="text" name="script_entry" class="form-control" value="{{ $egg->script_entry }}" />
-                            <p class="text-muted small">The entrypoint command to use for this script.</p>
+                            <p class="text-muted small">Der Startbefehl, der für dieses Skript verwendet werden soll.</p>
                         </div>
                     </div>
                     <div class="row">
                         <div class="col-xs-12 text-muted">
-                            The following service options rely on this script:
+                            Die folgenden Dienstoptionen basieren auf diesem Skript:
                             @if(count($relyOnScript) > 0)
                                 @foreach($relyOnScript as $rely)
                                     <a href="{{ route('admin.nests.egg.view', $rely->id) }}">
@@ -77,7 +77,7 @@
                                     </a>
                                 @endforeach
                             @else
-                                <em>none</em>
+                                <em>Keine</em>
                             @endif
                         </div>
                     </div>
@@ -85,7 +85,7 @@
                 <div class="box-footer">
                     {!! csrf_field() !!}
                     <textarea name="script_install" class="hidden"></textarea>
-                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Save</button>
+                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Speichern</button>
                 </div>
             </div>
         </div>
diff --git a/resources/views/admin/eggs/variables.blade.php b/resources/views/admin/eggs/variables.blade.php
index fbc1478..a7b8dbc 100644
--- a/resources/views/admin/eggs/variables.blade.php
+++ b/resources/views/admin/eggs/variables.blade.php
@@ -1,17 +1,17 @@
 @extends('layouts.admin')
 
 @section('title')
-    Egg &rarr; {{ $egg->name }} &rarr; Variables
+    Egg &rarr; {{ $egg->name }} &rarr; Variablen
 @endsection
 
 @section('content-header')
-    <h1>{{ $egg->name }}<small>Managing variables for this Egg.</small></h1>
+    <h1>{{ $egg->name }}<small>Verwalten von Variablen für dieses Egg.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
         <li><a href="{{ route('admin.nests.view', $egg->nest->id) }}">{{ $egg->nest->name }}</a></li>
         <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">{{ $egg->name }}</a></li>
-        <li class="active">Variables</li>
+        <li class="active">Variablen</li>
     </ol>
 @endsection
 
@@ -20,9 +20,9 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Configuration</a></li>
-                <li class="active"><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variables</a></li>
-                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Install Script</a></li>
+                <li><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Konfiguration</a></li>
+                <li class="active"><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variablen</a></li>
+                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Installationsskript</a></li>
             </ul>
         </div>
     </div>
@@ -31,7 +31,7 @@
     <div class="col-xs-12">
         <div class="box no-border">
             <div class="box-body">
-                <a href="#" class="btn btn-sm btn-success pull-right" data-toggle="modal" data-target="#newVariableModal">Create New Variable</a>
+                <a href="#" class="btn btn-sm btn-success pull-right" data-toggle="modal" data-target="#newVariableModal">Neue Variable erstellen</a>
             </div>
         </div>
     </div>
@@ -50,38 +50,38 @@
                             <input type="text" name="name" value="{{ $variable->name }}" class="form-control" />
                         </div>
                         <div class="form-group">
-                            <label class="form-label">Description</label>
+                            <label class="form-label">Beschreibung</label>
                             <textarea name="description" class="form-control" rows="3">{{ $variable->description }}</textarea>
                         </div>
                         <div class="row">
                             <div class="form-group col-md-6">
-                                <label class="form-label">Environment Variable</label>
+                                <label class="form-label">Umgebungsvariable</label>
                                 <input type="text" name="env_variable" value="{{ $variable->env_variable }}" class="form-control" />
                             </div>
                             <div class="form-group col-md-6">
-                                <label class="form-label">Default Value</label>
+                                <label class="form-label">Standardwert</label>
                                 <input type="text" name="default_value" value="{{ $variable->default_value }}" class="form-control" />
                             </div>
                             <div class="col-xs-12">
-                                <p class="text-muted small">This variable can be accessed in the startup command by using <code>{{ $variable->env_variable }}</code>.</p>
+                                <p class="text-muted small">Auf diese Variable kann im Startbefehl mit <code>{{ $variable->env_variable }}</code> zugegriffen werden.</p>
                             </div>
                         </div>
                         <div class="form-group">
-                            <label class="form-label">Permissions</label>
+                            <label class="form-label">Berechtigungen</label>
                             <select name="options[]" class="pOptions form-control" multiple>
-                                <option value="user_viewable" {{ (! $variable->user_viewable) ?: 'selected' }}>Users Can View</option>
-                                <option value="user_editable" {{ (! $variable->user_editable) ?: 'selected' }}>Users Can Edit</option>
+                                <option value="user_viewable" {{ (! $variable->user_viewable) ?: 'selected' }}>Nutzer können ansehen</option>
+                                <option value="user_editable" {{ (! $variable->user_editable) ?: 'selected' }}>Nutzer können bearbeiten</option>
                             </select>
                         </div>
                         <div class="form-group">
-                            <label class="form-label">Input Rules</label>
+                            <label class="form-label">Eingaberegeln</label>
                             <input type="text" name="rules" class="form-control" value="{{ $variable->rules }}" />
-                            <p class="text-muted small">These rules are defined using standard <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework validation rules</a>.</p>
+                            <p class="text-muted small">Diese Regeln werden mithilfe von standardmäßigen <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework-Validierungsregeln</a> definiert.</p>
                         </div>
                     </div>
                     <div class="box-footer">
                         {!! csrf_field() !!}
-                        <button class="btn btn-sm btn-primary pull-right" name="_method" value="PATCH" type="submit">Save</button>
+                        <button class="btn btn-sm btn-primary pull-right" name="_method" value="PATCH" type="submit">Speichern</button>
                         <button class="btn btn-sm btn-danger pull-left muted muted-hover" data-action="delete" name="_method" value="DELETE" type="submit"><i class="fa fa-trash-o"></i></button>
                     </div>
                 </form>
@@ -94,7 +94,7 @@
         <div class="modal-content">
             <div class="modal-header">
                 <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
-                <h4 class="modal-title">Create New Egg Variable</h4>
+                <h4 class="modal-title">Neue Egg-Variable erstellen</h4>
             </div>
             <form action="{{ route('admin.nests.egg.variables', $egg->id) }}" method="POST">
                 <div class="modal-body">
@@ -103,39 +103,39 @@
                         <input type="text" name="name" class="form-control" value="{{ old('name') }}"/>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Description</label>
+                        <label class="control-label">Beschreibung</label>
                         <textarea name="description" class="form-control" rows="3">{{ old('description') }}</textarea>
                     </div>
                     <div class="row">
                         <div class="form-group col-md-6">
-                            <label class="control-label">Environment Variable <span class="field-required"></span></label>
+                            <label class="control-label">Umgebungsvariable <span class="field-required"></span></label>
                             <input type="text" name="env_variable" class="form-control" value="{{ old('env_variable') }}" />
                         </div>
                         <div class="form-group col-md-6">
-                            <label class="control-label">Default Value</label>
+                            <label class="control-label">Standardwert</label>
                             <input type="text" name="default_value" class="form-control" value="{{ old('default_value') }}" />
                         </div>
                         <div class="col-xs-12">
-                            <p class="text-muted small">This variable can be accessed in the startup command by entering <code>@{{environment variable value}}</code>.</p>
+                            <p class="text-muted small">Auf diese Variable kann im Startbefehl durch Eingabe von <code>@{{environment variable value}}</code> zugegriffen werden.</p>
                         </div>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Permissions</label>
+                        <label class="control-label">Berechtigungen</label>
                         <select name="options[]" class="pOptions form-control" multiple>
-                            <option value="user_viewable">Users Can View</option>
-                            <option value="user_editable">Users Can Edit</option>
+                            <option value="user_viewable">Nutzer können ansehen</option>
+                            <option value="user_editable">Nutzer können bearbeiten</option>
                         </select>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Input Rules <span class="field-required"></span></label>
+                        <label class="control-label">Eingaberegeln <span class="field-required"></span></label>
                         <input type="text" name="rules" class="form-control" value="{{ old('rules', 'required|string|max:20') }}" placeholder="required|string|max:20" />
-                        <p class="text-muted small">These rules are defined using standard <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework validation rules</a>.</p>
+                            <p class="text-muted small">Diese Regeln werden mithilfe von standardmäßigen <a href="https://laravel.com/docs/5.7/validation#available-validation-rules" target="_blank">Laravel Framework-Validierungsregeln</a> definiert.</p>
                     </div>
                 </div>
                 <div class="modal-footer">
                     {!! csrf_field() !!}
-                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Close</button>
-                    <button type="submit" class="btn btn-primary">Create Variable</button>
+                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Schließen</button>
+                    <button type="submit" class="btn btn-primary">Variable erstellen</button>
                 </div>
             </form>
         </div>
@@ -148,7 +148,7 @@
     <script>
         $('.pOptions').select2();
         $('[data-action="delete"]').on('mouseenter', function (event) {
-            $(this).find('i').html(' Delete Variable');
+            $(this).find('i').html(' Variable löschen');
         }).on('mouseleave', function (event) {
             $(this).find('i').html('');
         });
diff --git a/resources/views/admin/eggs/view.blade.php b/resources/views/admin/eggs/view.blade.php
index b999d2a..2106519 100644
--- a/resources/views/admin/eggs/view.blade.php
+++ b/resources/views/admin/eggs/view.blade.php
@@ -19,9 +19,9 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li class="active"><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Configuration</a></li>
-                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variables</a></li>
-                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Install Script</a></li>
+                <li class="active"><a href="{{ route('admin.nests.egg.view', $egg->id) }}">Konfiguration</a></li>
+                <li><a href="{{ route('admin.nests.egg.variables', $egg->id) }}">Variablen</a></li>
+                <li><a href="{{ route('admin.nests.egg.scripts', $egg->id) }}">Installationsskript</a></li>
             </ul>
         </div>
     </div>
@@ -34,16 +34,16 @@
                     <div class="row">
                         <div class="col-xs-8">
                             <div class="form-group no-margin-bottom">
-                                <label for="pName" class="control-label">Egg File</label>
+                                <label for="pName" class="control-label">Egg-Datei</label>
                                 <div>
                                     <input type="file" name="import_file" class="form-control" style="border: 0;margin-left:-10px;" />
-                                    <p class="text-muted small no-margin-bottom">If you would like to replace settings for this Egg by uploading a new JSON file, simply select it here and press "Update Egg". This will not change any existing startup strings or Docker images for existing servers.</p>
+                                    <p class="text-muted small no-margin-bottom">Wenn du die Einstellungen für dieses Egg ersetzen möchtest, indem du eine neue JSON-Datei hochlädst, wähle sie einfach hier aus und klicke auf "Egg aktualisieren". Dadurch werden keine bestehenden Startstrings oder Docker-Images für bestehende Server geändert.</p>
                                 </div>
                             </div>
                         </div>
                         <div class="col-xs-4">
                             {!! csrf_field() !!}
-                            <button type="submit" name="_method" value="PUT" class="btn btn-sm btn-danger pull-right">Update Egg</button>
+                            <button type="submit" name="_method" value="PUT" class="btn btn-sm btn-danger pull-right">Egg aktualisieren</button>
                         </div>
                     </div>
                 </div>
@@ -56,7 +56,7 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Configuration</h3>
+                    <h3 class="box-title">Konfiguration</h3>
                 </div>
                 <div class="box-body">
                     <div class="row">
@@ -64,7 +64,7 @@
                             <div class="form-group">
                                 <label for="pName" class="control-label">Name <span class="field-required"></span></label>
                                 <input type="text" id="pName" name="name" value="{{ $egg->name }}" class="form-control" />
-                                <p class="text-muted small">A simple, human-readable name to use as an identifier for this Egg.</p>
+                                <p class="text-muted small">Ein einfacher, für Menschen lesbarer Name, der als Kennung für dieses Egg verwendet werden kann. Dies wird den Benutzern als ihr Gameserver-Typ angezeigt.</p>
                             </div>
                             <div class="form-group">
                                 <label for="pUuid" class="control-label">UUID</label>
@@ -74,29 +74,29 @@
                             <div class="form-group">
                                 <label for="pAuthor" class="control-label">Author</label>
                                 <input type="text" id="pAuthor" readonly value="{{ $egg->author }}" class="form-control" />
-                                <p class="text-muted small">The author of this version of the Egg. Uploading a new Egg configuration from a different author will change this.</p>
+                                <p class="text-muted small">Der Autor dieser Version des Egg. Das Hochladen einer neuen Egg-Konfiguration von einem anderen Autor ändert dies.</p>
                             </div>
                             <div class="form-group">
                                 <label for="pDockerImage" class="control-label">Docker Images <span class="field-required"></span></label>
                                 <textarea id="pDockerImages" name="docker_images" class="form-control" rows="4">{{ implode(PHP_EOL, $images) }}</textarea>
                                 <p class="text-muted small">
-                                    The docker images available to servers using this egg. Enter one per line. Users
-                                    will be able to select from this list of images if more than one value is provided.
-                                    Optionally, a display name may be provided by prefixing the image with the name
-                                    followed by a pipe character, and then the image URL. Example: <code>Display Name|ghcr.io/my/egg</code>
+                                    Die Docker-Images, die für Server, die dieses Egg verwenden, verfügbar sind. Gib einen Wert pro Zeile ein.
+                                    Wenn mehr als ein Wert angegeben wird, können die Nutzer aus dieser Liste von Images auswählen
+                                    Optional kann ein Anzeigename bereitgestellt werden, indem dem Bild der Name vorangestellt wird,
+                                    gefolgt von einem Pipe-Zeichen und dann der Bild-URL. Beispiel: <code>Anzeigename|ghcr.io/my/egg</code>
                                 </p>
                             </div>
                             <div class="form-group">
                                 <div class="checkbox checkbox-primary no-margin-bottom">
                                     <input id="pForceOutgoingIp" name="force_outgoing_ip" type="checkbox" value="1" @if($egg->force_outgoing_ip) checked @endif />
-                                    <label for="pForceOutgoingIp" class="strong">Force Outgoing IP</label>
+                                    <label for="pForceOutgoingIp" class="strong">Ausgehende IP erzwingen</label>
                                     <p class="text-muted small">
-                                        Forces all outgoing network traffic to have its Source IP NATed to the IP of the server's primary allocation IP.
-                                        Required for certain games to work properly when the Node has multiple public IP addresses.
+                                        Erzwingt, dass der gesamte ausgehende Netzwerkverkehr seine Quell-IP mit der IP der primären Zuordnungs-IP des Servers NATiert.
+                                        Erforderlich, damit bestimmte Spiele ordnungsgemäß funktionieren, wenn die Node mehrere öffentliche IP-Adressen hat.
                                         <br>
                                         <strong>
-                                            Enabling this option will disable internal networking for any servers using this egg,
-                                            causing them to be unable to internally access other servers on the same node.
+                                            Das Aktivieren dieser Option deaktiviert das interne Netzwerk für alle Server,
+                                            die dieses Egg verwenden, wodurch sie nicht mehr intern auf andere Server auf dieselbe Node zugreifen können.
                                         </strong>
                                     </p>
                                 </div>
@@ -105,14 +105,14 @@
                         </div>
                         <div class="col-sm-6">
                             <div class="form-group">
-                                <label for="pDescription" class="control-label">Description</label>
+                                <label for="pDescription" class="control-label">Beschreibung</label>
                                 <textarea id="pDescription" name="description" class="form-control" rows="8">{{ $egg->description }}</textarea>
-                                <p class="text-muted small">A description of this Egg that will be displayed throughout the Panel as needed.</p>
+                                <p class="text-muted small">Eine Beschreibung dieses Eggs, die bei Bedarf im gesamten Panel angezeigt wird.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pStartup" class="control-label">Startup Command <span class="field-required"></span></label>
+                                <label for="pStartup" class="control-label">Startbefehl <span class="field-required"></span></label>
                                 <textarea id="pStartup" name="startup" class="form-control" rows="8">{{ $egg->startup }}</textarea>
-                                <p class="text-muted small">The default startup command that should be used for new servers using this Egg.</p>
+                                <p class="text-muted small">Der standardmäßige Startbefehl, der für neue Server verwendet werden sollte, die dieses Eggs verwenden.</p>
                             </div>
                         </div>
                     </div>
@@ -122,56 +122,56 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Process Management</h3>
+                    <h3 class="box-title">Prozessmanagement</h3>
                 </div>
                 <div class="box-body">
                     <div class="row">
                         <div class="col-xs-12">
                             <div class="alert alert-warning">
-                                <p>The following configuration options should not be edited unless you understand how this system works. If wrongly modified it is possible for the daemon to break.</p>
-                                <p>All fields are required unless you select a separate option from the 'Copy Settings From' dropdown, in which case fields may be left blank to use the values from that Egg.</p>
+                                <p>Die folgenden Konfigurationsoptionen sollten nicht bearbeitet werden, es sei denn, Sie verstehen, wie dieses System funktioniert. Bei falscher Änderung kann der Daemon kaputt gehen.</p>
+                                <p>Alle Felder sind erforderlich, es sei denn, Sie wählen eine separate Option aus der Dropdown-Liste „Einstellungen kopieren von“. In diesem Fall können die Felder leer gelassen werden, um die Werte aus diesem Egg zu verwenden.</p>
                             </div>
                         </div>
                         <div class="col-sm-6">
                             <div class="form-group">
-                                <label for="pConfigFrom" class="form-label">Copy Settings From</label>
+                                <label for="pConfigFrom" class="form-label">Einstellungen kopieren von</label>
                                 <select name="config_from" id="pConfigFrom" class="form-control">
-                                    <option value="">None</option>
+                                    <option value="">Keinem</option>
                                     @foreach($egg->nest->eggs as $o)
                                         <option value="{{ $o->id }}" {{ ($egg->config_from !== $o->id) ?: 'selected' }}>{{ $o->name }} &lt;{{ $o->author }}&gt;</option>
                                     @endforeach
                                 </select>
-                                <p class="text-muted small">If you would like to default to settings from another Egg select it from the menu above.</p>
+                                <p class="text-muted small">Wenn du die Einstellungen eines anderen Eggs verwenden möchtest, wähle es im Menü oben aus.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pConfigStop" class="form-label">Stop Command</label>
+                                <label for="pConfigStop" class="form-label">Stop-Befehl</label>
                                 <input type="text" id="pConfigStop" name="config_stop" class="form-control" value="{{ $egg->config_stop }}" />
-                                <p class="text-muted small">The command that should be sent to server processes to stop them gracefully. If you need to send a <code>SIGINT</code> you should enter <code>^C</code> here.</p>
+                                <p class="text-muted small">Der Befehl, der an Serverprozesse gesendet werden sollte, um sie ordnungsgemäß zu stoppen. Wenn du einen <code>SIGINT</code> senden musst, gib hier <code>^C</code> ein.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pConfigLogs" class="form-label">Log Configuration</label>
+                                <label for="pConfigLogs" class="form-label">Log-Konfiguration</label>
                                 <textarea data-action="handle-tabs" id="pConfigLogs" name="config_logs" class="form-control" rows="6">{{ ! is_null($egg->config_logs) ? json_encode(json_decode($egg->config_logs), JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) : '' }}</textarea>
-                                <p class="text-muted small">This should be a JSON representation of where log files are stored, and whether or not the daemon should be creating custom logs.</p>
+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation sein, wo Log-Dateien gespeichert werden und ob der Daemon benutzerdefinierte Protokolle erstellen soll oder nicht.</p>
                             </div>
                         </div>
                         <div class="col-sm-6">
                             <div class="form-group">
-                                <label for="pConfigFiles" class="form-label">Configuration Files</label>
+                                <label for="pConfigFiles" class="form-label">Konfigurationsdateien</label>
                                 <textarea data-action="handle-tabs" id="pConfigFiles" name="config_files" class="form-control" rows="6">{{ ! is_null($egg->config_files) ? json_encode(json_decode($egg->config_files), JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) : '' }}</textarea>
-                                <p class="text-muted small">This should be a JSON representation of configuration files to modify and what parts should be changed.</p>
+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation der zu ändernden Konfigurationsdateien und der zu ändernden Teile sein.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pConfigStartup" class="form-label">Start Configuration</label>
+                                <label for="pConfigStartup" class="form-label">Startkonfiguration</label>
                                 <textarea data-action="handle-tabs" id="pConfigStartup" name="config_startup" class="form-control" rows="6">{{ ! is_null($egg->config_startup) ? json_encode(json_decode($egg->config_startup), JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) : '' }}</textarea>
-                                <p class="text-muted small">This should be a JSON representation of what values the daemon should be looking for when booting a server to determine completion.</p>
+                                <p class="text-muted small">Dies sollte eine JSON-Repräsentation dessen sein, nach welchen Werten der Daemon beim Booten eines Servers suchen sollte, um den Abschluss zu bestimmen.</p>
                             </div>
                         </div>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Save</button>
-                    <a href="{{ route('admin.nests.egg.export', $egg->id) }}" class="btn btn-sm btn-info pull-right" style="margin-right:10px;">Export</a>
+                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Speichern</button>
+                    <a href="{{ route('admin.nests.egg.export', $egg->id) }}" class="btn btn-sm btn-info pull-right" style="margin-right:10px;">Exportieren</a>
                     <button id="deleteButton" type="submit" name="_method" value="DELETE" class="btn btn-danger btn-sm muted muted-hover">
                         <i class="fa fa-trash-o"></i>
                     </button>
@@ -187,7 +187,7 @@
     <script>
     $('#pConfigFrom').select2();
     $('#deleteButton').on('mouseenter', function (event) {
-        $(this).find('i').html(' Delete Egg');
+        $(this).find('i').html(' Egg löschen');
     }).on('mouseleave', function (event) {
         $(this).find('i').html('');
     });
diff --git a/resources/views/admin/index.blade.php b/resources/views/admin/index.blade.php
index ed26a92..173b10e 100644
--- a/resources/views/admin/index.blade.php
+++ b/resources/views/admin/index.blade.php
@@ -5,10 +5,10 @@
 @endsection
 
 @section('content-header')
-    <h1>Administrative Overview<small>A quick glance at your system.</small></h1>
+    <h1>Admin Dashboard<small>Hier ist deine Übersicht vom System.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Index</li>
+        <li class="active">Infoseite</li>
     </ol>
 @endsection
 
@@ -23,13 +23,13 @@
             @endif
         ">
             <div class="box-header with-border">
-                <h3 class="box-title">System Information</h3>
+                <h3 class="box-title">Status des Systems</h3>
             </div>
             <div class="box-body">
                 @if ($version->isLatestPanel())
-                    You are running Pterodactyl Panel version <code>{{ config('app.version') }}</code>. Your panel is up-to-date!
+                    Auf diesem Server läuft derzeit die Version <code>{{ config('app.version') }}</code>. Du bist auf dem neuesten Stand!
                 @else
-                    Your panel is <strong>not up-to-date!</strong> The latest version is <a href="https://github.com/Pterodactyl/Panel/releases/v{{ $version->getPanel() }}" target="_blank"><code>{{ $version->getPanel() }}</code></a> and you are currently running version <code>{{ config('app.version') }}</code>.
+                    Das Panel ist <strong>nicht aktuell!</strong> Die letzte Version ist <a href="https://github.com/Pterodactyl/Panel/releases/v{{ $version->getPanel() }}" target="_blank"><code>{{ $version->getPanel() }}</code></a> und du nutzt derzeit <code>{{ config('app.version') }}</code> auf diesem Server. <hr>WARNUNG: Du nutzt GermanDactyl für die Übersetzung, es stehen möglicherweise noch keine Updates zur Verfügung!
                 @endif
             </div>
         </div>
@@ -37,17 +37,17 @@
 </div>
 <div class="row">
     <div class="col-xs-6 col-sm-3 text-center">
-        <a href="{{ $version->getDiscord() }}"><button class="btn btn-warning" style="width:100%;"><i class="fa fa-fw fa-support"></i> Get Help <small>(via Discord)</small></button></a>
+        <a href="{{ $version->getDiscord() }}"><button class="btn btn-warning" style="width:100%;"><i class="fa fa-fw fa-support"></i> Hilfe erhalten <small>(Englisch)</small></button></a>
     </div>
     <div class="col-xs-6 col-sm-3 text-center">
-        <a href="https://pterodactyl.io"><button class="btn btn-primary" style="width:100%;"><i class="fa fa-fw fa-link"></i> Documentation</button></a>
+        <a href="https://pterodactyl.io"><button class="btn btn-primary" style="width:100%;"><i class="fa fa-fw fa-link"></i> Wiki-Page</button></a>
     </div>
     <div class="clearfix visible-xs-block">&nbsp;</div>
     <div class="col-xs-6 col-sm-3 text-center">
         <a href="https://github.com/pterodactyl/panel"><button class="btn btn-primary" style="width:100%;"><i class="fa fa-fw fa-support"></i> Github</button></a>
     </div>
     <div class="col-xs-6 col-sm-3 text-center">
-        <a href="{{ $version->getDonations() }}"><button class="btn btn-success" style="width:100%;"><i class="fa fa-fw fa-money"></i> Support the Project</button></a>
+        <a href="{{ $version->getDonations() }}"><button class="btn btn-success" style="width:100%;"><i class="fa fa-fw fa-money"></i> Unterstütze Pterodactyl</button></a>
     </div>
 </div>
 @endsection
diff --git a/resources/views/admin/locations/index.blade.php b/resources/views/admin/locations/index.blade.php
index c54ffe0..e245a76 100644
--- a/resources/views/admin/locations/index.blade.php
+++ b/resources/views/admin/locations/index.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Locations
+    Standorte
 @endsection
 
 @section('content-header')
-    <h1>Locations<small>All locations that nodes can be assigned to for easier categorization.</small></h1>
+    <h1>Standorte<small>Alle Standorte, wo Rechenzentren für deine Server stehen.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Locations</li>
+        <li class="active">Standorte</li>
     </ol>
 @endsection
 
@@ -17,9 +17,9 @@
     <div class="col-xs-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Location List</h3>
+                <h3 class="box-title">Alle Standorte</h3>
                 <div class="box-tools">
-                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newLocationModal">Create New</button>
+                    <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newLocationModal">Hinzufügen</button>
                 </div>
             </div>
             <div class="box-body table-responsive no-padding">
@@ -27,10 +27,10 @@
                     <tbody>
                         <tr>
                             <th>ID</th>
-                            <th>Short Code</th>
-                            <th>Description</th>
-                            <th class="text-center">Nodes</th>
-                            <th class="text-center">Servers</th>
+                            <th>Kürzel</th>
+                            <th>Beschreibung</th>
+                            <th class="text-center">Laufende Nodes</th>
+                            <th class="text-center">Aktive Server</th>
                         </tr>
                         @foreach ($locations as $location)
                             <tr>
@@ -52,27 +52,27 @@
         <div class="modal-content">
             <form action="{{ route('admin.locations') }}" method="POST">
                 <div class="modal-header">
-                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
-                    <h4 class="modal-title">Create Location</h4>
+                    <button type="button" class="close" data-dismiss="modal" aria-label="Schließen"><span aria-hidden="true">&times;</span></button>
+                    <h4 class="modal-title">Standort hinzufügen</h4>
                 </div>
                 <div class="modal-body">
                     <div class="row">
                         <div class="col-md-12">
-                            <label for="pShortModal" class="form-label">Short Code</label>
+                            <label for="pShortModal" class="form-label">Kürzel</label>
                             <input type="text" name="short" id="pShortModal" class="form-control" />
-                            <p class="text-muted small">A short identifier used to distinguish this location from others. Must be between 1 and 60 characters, for example, <code>us.nyc.lvl3</code>.</p>
+                            <p class="text-muted small">Ein Kürzel für den Standort des Rechenzentrums, du kannst deine eigene Struktur nutzen, zum Beispiel <code>rz-de-srv01</code></p>
                         </div>
                         <div class="col-md-12">
-                            <label for="pLongModal" class="form-label">Description</label>
+                            <label for="pLongModal" class="form-label">Beschreibung</label>
                             <textarea name="long" id="pLongModal" class="form-control" rows="4"></textarea>
-                            <p class="text-muted small">A longer description of this location. Must be less than 191 characters.</p>
+                            <p class="text-muted small">Eine Beschreibung des Standortes, zum Beispiel die Anschrift oder Kontaktdaten.</p>
                         </div>
                     </div>
                 </div>
                 <div class="modal-footer">
                     {!! csrf_field() !!}
-                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
-                    <button type="submit" class="btn btn-success btn-sm">Create</button>
+                    <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
+                    <button type="submit" class="btn btn-success btn-sm">Erstellen</button>
                 </div>
             </form>
         </div>
diff --git a/resources/views/admin/locations/view.blade.php b/resources/views/admin/locations/view.blade.php
index f34dcf3..b7d57f0 100644
--- a/resources/views/admin/locations/view.blade.php
+++ b/resources/views/admin/locations/view.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Locations &rarr; View &rarr; {{ $location->short }}
+    Standorte &rarr; Ansehen &rarr; {{ $location->short }}
 @endsection
 
 @section('content-header')
     <h1>{{ $location->short }}<small>{{ str_limit($location->long, 75) }}</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.locations') }}">Locations</a></li>
+        <li><a href="{{ route('admin.locations') }}">Standorte</a></li>
         <li class="active">{{ $location->short }}</li>
     </ol>
 @endsection
@@ -18,23 +18,23 @@
     <div class="col-sm-6">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Location Details</h3>
+                <h3 class="box-title">Details des Standortes</h3>
             </div>
             <form action="{{ route('admin.locations.view', $location->id) }}" method="POST">
                 <div class="box-body">
                     <div class="form-group">
-                        <label for="pShort" class="form-label">Short Code</label>
+                        <label for="pShort" class="form-label">Kürzel</label>
                         <input type="text" id="pShort" name="short" class="form-control" value="{{ $location->short }}" />
                     </div>
                     <div class="form-group">
-                        <label for="pLong" class="form-label">Description</label>
+                        <label for="pLong" class="form-label">Beschreibung</label>
                         <textarea id="pLong" name="long" class="form-control" rows="4">{{ $location->long }}</textarea>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
                     {!! method_field('PATCH') !!}
-                    <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Save</button>
+                    <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Speichern</button>
                     <button name="action" value="delete" class="btn btn-sm btn-danger pull-left muted muted-hover"><i class="fa fa-trash-o"></i></button>
                 </div>
             </form>
@@ -51,7 +51,7 @@
                         <th>ID</th>
                         <th>Name</th>
                         <th>FQDN</th>
-                        <th>Servers</th>
+                        <th>Alle Server</th>
                     </tr>
                     @foreach($location->nodes as $node)
                         <tr>
diff --git a/resources/views/admin/mounts/index.blade.php b/resources/views/admin/mounts/index.blade.php
index a3b9892..491bc4c 100644
--- a/resources/views/admin/mounts/index.blade.php
+++ b/resources/views/admin/mounts/index.blade.php
@@ -2,14 +2,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Mounts
+    Integrationen
 @endsection
 
 @section('content-header')
-    <h1>Mounts<small>Configure and manage additional mount points for servers.</small></h1>
+    <h1>Integrationen<small>Konfiguriere und verwalte die Integrationen.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Mounts</li>
+        <li class="active">Integrationen</li>
     </ol>
 @endsection
 
@@ -18,10 +18,10 @@
         <div class="col-xs-12">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Mount List</h3>
+                    <h3 class="box-title">Alle Integrationen</h3>
 
                     <div class="box-tools">
-                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newMountModal">Create New</button>
+                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#newMountModal">Erstellen</button>
                     </div>
                 </div>
 
@@ -31,11 +31,11 @@
                             <tr>
                                 <th>ID</th>
                                 <th>Name</th>
-                                <th>Source</th>
-                                <th>Target</th>
+                                <th>Quelle</th>
+                                <th>Ziel</th>
                                 <th class="text-center">Eggs</th>
                                 <th class="text-center">Nodes</th>
-                                <th class="text-center">Servers</th>
+                                <th class="text-center">Anzahl Server</th>
                             </tr>
 
                             @foreach ($mounts as $mount)
@@ -65,7 +65,7 @@
                             <span aria-hidden="true" style="color: #FFFFFF">&times;</span>
                         </button>
 
-                        <h4 class="modal-title">Create Mount</h4>
+                        <h4 class="modal-title">Integration erstellen</h4>
                     </div>
 
                     <div class="modal-body">
@@ -73,69 +73,69 @@
                             <div class="col-md-12">
                                 <label for="pName" class="form-label">Name</label>
                                 <input type="text" id="pName" name="name" class="form-control" />
-                                <p class="text-muted small">Unique name used to separate this mount from another.</p>
+                                <p class="text-muted small">Ein Name, womit du den mit anderen unterscheiden kannst.</p>
                             </div>
 
                             <div class="col-md-12">
-                                <label for="pDescription" class="form-label">Description</label>
+                                <label for="pDescription" class="form-label">Beschreibung</label>
                                 <textarea id="pDescription" name="description" class="form-control" rows="4"></textarea>
-                                <p class="text-muted small">A longer description for this mount, must be less than 191 characters.</p>
+                                <p class="text-muted small">Eine Beschreibung, wofür dies gedacht ist (max. 191 Zeichen)</p>
                             </div>
 
                             <div class="col-md-6">
-                                <label for="pSource" class="form-label">Source</label>
+                                <label for="pSource" class="form-label">Quelle</label>
                                 <input type="text" id="pSource" name="source" class="form-control" />
-                                <p class="text-muted small">File path on the host system to mount to a container.</p>
+                                <p class="text-muted small">Die Verzeichnisquelle im Host-System</p>
                             </div>
 
                             <div class="col-md-6">
-                                <label for="pTarget" class="form-label">Target</label>
+                                <label for="pTarget" class="form-label">Ziel</label>
                                 <input type="text" id="pTarget" name="target" class="form-control" />
-                                <p class="text-muted small">Where the mount will be accessible inside a container.</p>
+                                <p class="text-muted small">Wo dies in einem Container zugänglich sein wird.</p>
                             </div>
 
                             <div class="col-md-6">
-                                <label class="form-label">Read Only</label>
+                                <label class="form-label">Nur Lesezugriff</label>
 
                                 <div>
                                     <div class="radio radio-success radio-inline">
                                         <input type="radio" id="pReadOnlyFalse" name="read_only" value="0" checked>
-                                        <label for="pReadOnlyFalse">False</label>
+                                        <label for="pReadOnlyFalse">Nein</label>
                                     </div>
 
                                     <div class="radio radio-warning radio-inline">
                                         <input type="radio" id="pReadOnly" name="read_only" value="1">
-                                        <label for="pReadOnly">True</label>
+                                        <label for="pReadOnly">Ja</label>
                                     </div>
                                 </div>
 
-                                <p class="text-muted small">Is the mount read only inside the container?</p>
+                                <p class="text-muted small">Ist es im Container schreibgeschützt?</p>
                             </div>
 
                             <div class="col-md-6">
-                                <label class="form-label">User Mountable</label>
+                                <label class="form-label">An Benutzer verknüpfbar</label>
 
                                 <div>
                                     <div class="radio radio-success radio-inline">
                                         <input type="radio" id="pUserMountableFalse" name="user_mountable" value="0" checked>
-                                        <label for="pUserMountableFalse">False</label>
+                                        <label for="pUserMountableFalse">Nein</label>
                                     </div>
 
                                     <div class="radio radio-warning radio-inline">
                                         <input type="radio" id="pUserMountable" name="user_mountable" value="1">
-                                        <label for="pUserMountable">True</label>
+                                        <label for="pUserMountable">Ja</label>
                                     </div>
                                 </div>
 
-                                <p class="text-muted small">Should users be able to mount this themselves?</p>
+                                <p class="text-muted small">Sollten Benutzer in der Lage sein, dies selbst zu integrieren?</p>
                             </div>
                         </div>
                     </div>
 
                     <div class="modal-footer">
                         {!! csrf_field() !!}
-                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
-                        <button type="submit" class="btn btn-success btn-sm">Create</button>
+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
+                        <button type="submit" class="btn btn-success btn-sm">Erstellen</button>
                     </div>
                 </form>
             </div>
diff --git a/resources/views/admin/mounts/view.blade.php b/resources/views/admin/mounts/view.blade.php
index 96a156d..409713b 100644
--- a/resources/views/admin/mounts/view.blade.php
+++ b/resources/views/admin/mounts/view.blade.php
@@ -2,7 +2,7 @@
 @extends('layouts.admin')
 
 @section('title')
-    Mounts &rarr; View &rarr; {{ $mount->id }}
+    Mounts &rarr; Ansehen &rarr; {{ $mount->id }}
 @endsection
 
 @section('content-header')
@@ -19,13 +19,13 @@
         <div class="col-sm-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Mount Details</h3>
+                    <h3 class="box-title">Mount-Details</h3>
                 </div>
 
                 <form action="{{ route('admin.mounts.view', $mount->id) }}" method="POST">
                     <div class="box-body">
                         <div class="form-group">
-                            <label for="PUniqueID" class="form-label">Unique ID</label>
+                            <label for="PUniqueID" class="form-label">Eindeutige ID</label>
                             <input type="text" id="PUniqueID" class="form-control" value="{{ $mount->uuid }}" disabled />
                         </div>
 
@@ -35,51 +35,51 @@
                         </div>
 
                         <div class="form-group">
-                            <label for="pDescription" class="form-label">Description</label>
+                            <label for="pDescription" class="form-label">Beschreibung</label>
                             <textarea id="pDescription" name="description" class="form-control" rows="4">{{ $mount->description }}</textarea>
                         </div>
 
                         <div class="row">
                             <div class="form-group col-md-6">
-                                <label for="pSource" class="form-label">Source</label>
+                                <label for="pSource" class="form-label">Quelle</label>
                                 <input type="text" id="pSource" name="source" class="form-control" value="{{ $mount->source }}" />
                             </div>
 
                             <div class="form-group col-md-6">
-                                <label for="pTarget" class="form-label">Target</label>
+                                <label for="pTarget" class="form-label">Ziel</label>
                                 <input type="text" id="pTarget" name="target" class="form-control" value="{{ $mount->target }}" />
                             </div>
                         </div>
 
                         <div class="row">
                             <div class="form-group col-md-6">
-                                <label class="form-label">Read Only</label>
+                                <label class="form-label">Nur lesen</label>
 
                                 <div>
                                     <div class="radio radio-success radio-inline">
                                         <input type="radio" id="pReadOnlyFalse" name="read_only" value="0" @if(!$mount->read_only) checked @endif>
-                                        <label for="pReadOnlyFalse">False</label>
+                                        <label for="pReadOnlyFalse">Nein</label>
                                     </div>
 
                                     <div class="radio radio-warning radio-inline">
                                         <input type="radio" id="pReadOnly" name="read_only" value="1" @if($mount->read_only) checked @endif>
-                                        <label for="pReadOnly">True</label>
+                                        <label for="pReadOnly">Ja</label>
                                     </div>
                                 </div>
                             </div>
 
                             <div class="form-group col-md-6">
-                                <label class="form-label">User Mountable</label>
+                                <label class="form-label">Vom Benutzer montierbar</label>
 
                                 <div>
                                     <div class="radio radio-success radio-inline">
                                         <input type="radio" id="pUserMountableFalse" name="user_mountable" value="0" @if(!$mount->user_mountable) checked @endif>
-                                        <label for="pUserMountableFalse">False</label>
+                                        <label for="pUserMountableFalse">Nein</label>
                                     </div>
 
                                     <div class="radio radio-warning radio-inline">
                                         <input type="radio" id="pUserMountable" name="user_mountable" value="1" @if($mount->user_mountable) checked @endif>
-                                        <label for="pUserMountable">True</label>
+                                        <label for="pUserMountable">Ja</label>
                                     </div>
                                 </div>
                             </div>
@@ -90,7 +90,7 @@
                         {!! csrf_field() !!}
                         {!! method_field('PATCH') !!}
 
-                        <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Save</button>
+                        <button name="action" value="edit" class="btn btn-sm btn-primary pull-right">Speichern</button>
                         <button name="action" value="delete" class="btn btn-sm btn-danger pull-left muted muted-hover"><i class="fa fa-trash-o"></i></button>
                     </div>
                 </form>
@@ -103,7 +103,7 @@
                     <h3 class="box-title">Eggs</h3>
 
                     <div class="box-tools">
-                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addEggsModal">Add Eggs</button>
+                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addEggsModal">Eggs hinzufügen</button>
                     </div>
                 </div>
 
@@ -133,7 +133,7 @@
                     <h3 class="box-title">Nodes</h3>
 
                     <div class="box-tools">
-                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addNodesModal">Add Nodes</button>
+                        <button class="btn btn-sm btn-primary" data-toggle="modal" data-target="#addNodesModal">Nodes hinzufügen</button>
                     </div>
                 </div>
 
@@ -171,7 +171,7 @@
                             <span aria-hidden="true" style="color: #FFFFFF">&times;</span>
                         </button>
 
-                        <h4 class="modal-title">Add Eggs</h4>
+                        <h4 class="modal-title">Eggs hinzufügen</h4>
                     </div>
 
                     <div class="modal-body">
@@ -198,8 +198,8 @@
                     <div class="modal-footer">
                         {!! csrf_field() !!}
 
-                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
-                        <button type="submit" class="btn btn-primary btn-sm">Add</button>
+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
+                        <button type="submit" class="btn btn-primary btn-sm">Hinzufügen</button>
                     </div>
                 </form>
             </div>
@@ -215,7 +215,7 @@
                             <span aria-hidden="true" style="color: #FFFFFF">&times;</span>
                         </button>
 
-                        <h4 class="modal-title">Add Nodes</h4>
+                        <h4 class="modal-title">Nodes hinzufügen</h4>
                     </div>
 
                     <div class="modal-body">
@@ -242,8 +242,8 @@
                     <div class="modal-footer">
                         {!! csrf_field() !!}
 
-                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
-                        <button type="submit" class="btn btn-primary btn-sm">Add</button>
+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
+                        <button type="submit" class="btn btn-primary btn-sm">Hinzufügen</button>
                     </div>
                 </form>
             </div>
@@ -257,11 +257,11 @@
     <script>
         $(document).ready(function() {
             $('#pEggs').select2({
-                placeholder: 'Select eggs..',
+                placeholder: 'Eggs auswählen..',
             });
 
             $('#pNodes').select2({
-                placeholder: 'Select nodes..',
+                placeholder: 'Nodes auswählen..',
             });
 
             $('button[data-action="detach-egg"]').click(function (event) {
@@ -276,11 +276,11 @@
                     headers: { 'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content') },
                 }).done(function () {
                     element.parent().parent().addClass('warning').delay(100).fadeOut();
-                    swal({ type: 'success', title: 'Egg detached.' });
+                    swal({ type: 'success', title: 'Egg getrennt.' });
                 }).fail(function (jqXHR) {
                     console.error(jqXHR);
                     swal({
-                        title: 'Whoops!',
+                        title: 'Hoppla!',
                         text: jqXHR.responseJSON.error,
                         type: 'error'
                     });
@@ -299,11 +299,11 @@
                     headers: { 'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content') },
                 }).done(function () {
                     element.parent().parent().addClass('warning').delay(100).fadeOut();
-                    swal({ type: 'success', title: 'Node detached.' });
+                    swal({ type: 'success', title: 'Node getrennt.' });
                 }).fail(function (jqXHR) {
                     console.error(jqXHR);
                     swal({
-                        title: 'Whoops!',
+                        title: 'Hoppla!',
                         text: jqXHR.responseJSON.error,
                         type: 'error'
                     });
diff --git a/resources/views/admin/nests/index.blade.php b/resources/views/admin/nests/index.blade.php
index 4bb6ae7..406c7a0 100644
--- a/resources/views/admin/nests/index.blade.php
+++ b/resources/views/admin/nests/index.blade.php
@@ -5,7 +5,7 @@
 @endsection
 
 @section('content-header')
-    <h1>Nests<small>All nests currently available on this system.</small></h1>
+    <h1>Nests<small>Alle verfügbaren Nests im System</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li class="active">Nests</li>
@@ -16,7 +16,7 @@
 <div class="row">
     <div class="col-xs-12">
         <div class="alert alert-danger">
-            Eggs are a powerful feature of Pterodactyl Panel that allow for extreme flexibility and configuration. Please note that while powerful, modifying an egg wrongly can very easily brick your servers and cause more problems. Please avoid editing our default eggs — those provided by <code>support@pterodactyl.io</code> — unless you are absolutely sure of what you are doing.
+            Eggs sind ein leistungsstarkes Merkmal von Pterodactyl Panel, das extreme Flexibilität und Konfiguration ermöglicht. Bitte beachte, dass ein falsches Modifizieren eines Eggs zwar leistungsfähig ist, aber sehr leicht deine Server zerstören und mehr Probleme verursachen kann. Bitte vermeide es, unsere Standard-Eggs – die von <code>support@pterodactyl.io</code> zu bearbeiten, es sei denn, du bist absolut sicher, was du tust.
         </div>
     </div>
 </div>
@@ -24,10 +24,10 @@
     <div class="col-xs-12">
         <div class="box">
             <div class="box-header with-border">
-                <h3 class="box-title">Configured Nests</h3>
+                <h3 class="box-title">Verfügbare Nests</h3>
                 <div class="box-tools">
-                    <a href="#" class="btn btn-sm btn-success" data-toggle="modal" data-target="#importServiceOptionModal" role="button"><i class="fa fa-upload"></i> Import Egg</a>
-                    <a href="{{ route('admin.nests.new') }}" class="btn btn-primary btn-sm">Create New</a>
+                    <a href="#" class="btn btn-sm btn-success" data-toggle="modal" data-target="#importServiceOptionModal" role="button"><i class="fa fa-upload"></i> Egg importieren</a>
+                    <a href="{{ route('admin.nests.new') }}" class="btn btn-primary btn-sm">Hinzufügen</a>
                 </div>
             </div>
             <div class="box-body table-responsive no-padding">
@@ -35,9 +35,9 @@
                     <tr>
                         <th>ID</th>
                         <th>Name</th>
-                        <th>Description</th>
+                        <th>Beschreibung</th>
                         <th class="text-center">Eggs</th>
-                        <th class="text-center">Servers</th>
+                        <th class="text-center">Server-Anzahl</th>
                     </tr>
                     @foreach($nests as $nest)
                         <tr>
@@ -58,33 +58,33 @@
         <div class="modal-content">
             <div class="modal-header">
                 <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
-                <h4 class="modal-title">Import an Egg</h4>
+                <h4 class="modal-title">Egg importieren</h4>
             </div>
             <form action="{{ route('admin.nests.egg.import') }}" enctype="multipart/form-data" method="POST">
                 <div class="modal-body">
                     <div class="form-group">
-                        <label class="control-label" for="pImportFile">Egg File <span class="field-required"></span></label>
+                        <label class="control-label" for="pImportFile">Egg-Datei<span class="field-required"></span></label>
                         <div>
                             <input id="pImportFile" type="file" name="import_file" class="form-control" accept="application/json" />
-                            <p class="small text-muted">Select the <code>.json</code> file for the new egg that you wish to import.</p>
+                            <p class="small text-muted">Wähle die <code>.json</code> Datei, welche du als Egg importieren möchtest.</p>
                         </div>
                     </div>
                     <div class="form-group">
-                        <label class="control-label" for="pImportToNest">Associated Nest <span class="field-required"></span></label>
+                        <label class="control-label" for="pImportToNest">Passender Nest <span class="field-required"></span></label>
                         <div>
                             <select id="pImportToNest" name="import_to_nest">
                                 @foreach($nests as $nest)
                                    <option value="{{ $nest->id }}">{{ $nest->name }} &lt;{{ $nest->author }}&gt;</option>
                                 @endforeach
                             </select>
-                            <p class="small text-muted">Select the nest that this egg will be associated with from the dropdown. If you wish to associate it with a new nest you will need to create that nest before continuing.</p>
+                            <p class="small text-muted">Wähle das Nest, dem dieses Egg zugeordnet werden soll, aus der Dropdown-Liste aus. Wenn du es mit einer neuen Verschachtelung verknüpfen möchtest, müssen Sie diese Verschachtelung erstellen, bevor du fortfährst.</p>
                         </div>
                     </div>
                 </div>
                 <div class="modal-footer">
                     {{ csrf_field() }}
-                    <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
-                    <button type="submit" class="btn btn-primary">Import</button>
+                    <button type="button" class="btn btn-default" data-dismiss="modal">Abbrechen</button>
+                    <button type="submit" class="btn btn-primary">Importieren</button>
                 </div>
             </form>
         </div>
diff --git a/resources/views/admin/nests/new.blade.php b/resources/views/admin/nests/new.blade.php
index 9a1cb0e..3d502a4 100644
--- a/resources/views/admin/nests/new.blade.php
+++ b/resources/views/admin/nests/new.blade.php
@@ -1,15 +1,15 @@
 @extends('layouts.admin')
 
 @section('title')
-    New Nest
+    Neues Nest
 @endsection
 
 @section('content-header')
-    <h1>New Nest<small>Configure a new nest to deploy to all nodes.</small></h1>
+    <h1>Neues Nest<small>Richte ein Nest ein, womit du Nodes integrieren kannst.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nests') }}">Nests</a></li>
-        <li class="active">New</li>
+        <li class="active">Erstellen</li>
     </ol>
 @endsection
 
@@ -19,18 +19,18 @@
         <div class="col-md-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">New Nest</h3>
+                    <h3 class="box-title">Neues Nest</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
                         <label class="control-label">Name</label>
                         <div>
                             <input type="text" name="name" class="form-control" value="{{ old('name') }}" />
-                            <p class="text-muted"><small>This should be a descriptive category name that encompasses all of the eggs within the nest.</small></p>
+                            <p class="text-muted"><small>Dies sollte ein Name sein, der nicht leicht zu vertauschen ist.</small></p>
                         </div>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Description</label>
+                        <label class="control-label">Beschreibung</label>
                         <div>
                             <textarea name="description" class="form-control" rows="6">{{ old('description') }}</textarea>
                         </div>
@@ -38,7 +38,7 @@
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" class="btn btn-primary pull-right">Save</button>
+                    <button type="submit" class="btn btn-primary pull-right">Speichern</button>
                 </div>
             </div>
         </div>
diff --git a/resources/views/admin/nests/view.blade.php b/resources/views/admin/nests/view.blade.php
index 9a67307..46f1c88 100644
--- a/resources/views/admin/nests/view.blade.php
+++ b/resources/views/admin/nests/view.blade.php
@@ -23,11 +23,11 @@
                         <label class="control-label">Name <span class="field-required"></span></label>
                         <div>
                             <input type="text" name="name" class="form-control" value="{{ $nest->name }}" />
-                            <p class="text-muted"><small>This should be a descriptive category name that encompasses all of the options within the service.</small></p>
+                            <p class="text-muted"><small>Dies sollte ein beschreibender Kategoriename sein, der alle Optionen innerhalb des Dienstes umfasst.</small></p>
                         </div>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Description</label>
+                        <label class="control-label">Beschreibung</label>
                         <div>
                             <textarea name="description" class="form-control" rows="7">{{ $nest->description }}</textarea>
                         </div>
@@ -35,7 +35,7 @@
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Save</button>
+                    <button type="submit" name="_method" value="PATCH" class="btn btn-primary btn-sm pull-right">Speichern</button>
                     <button id="deleteButton" type="submit" name="_method" value="DELETE" class="btn btn-sm btn-danger muted muted-hover"><i class="fa fa-trash-o"></i></button>
                 </div>
             </div>
@@ -45,24 +45,24 @@
         <div class="box">
             <div class="box-body">
                 <div class="form-group">
-                    <label class="control-label">Nest ID</label>
+                    <label class="control-label">Nest-ID</label>
                     <div>
                         <input type="text" readonly class="form-control" value="{{ $nest->id }}" />
-                        <p class="text-muted small">A unique ID used for identification of this nest internally and through the API.</p>
+                        <p class="text-muted small">Eine eindeutige ID, die zur Identifizierung dieses Nests intern und über die API verwendet wird.</p>
                     </div>
                 </div>
                 <div class="form-group">
-                    <label class="control-label">Author</label>
+                    <label class="control-label">Autor</label>
                     <div>
                         <input type="text" readonly class="form-control" value="{{ $nest->author }}" />
-                        <p class="text-muted small">The author of this service option. Please direct questions and issues to them unless this is an official option authored by <code>support@pterodactyl.io</code>.</p>
+                        <p class="text-muted small">Der Autor dieser Serviceoption. Bitte richten Sie Fragen und Probleme an sie, es sei denn, dies ist eine offizielle Option, die von <code>support@pterodactyl.io</code> verfasst wurde.</p>
                     </div>
                 </div>
                 <div class="form-group">
                     <label class="control-label">UUID</label>
                     <div>
                         <input type="text" readonly class="form-control" value="{{ $nest->uuid }}" />
-                        <p class="text-muted small">A UUID that all servers using this option are assigned for identification purposes.</p>
+                        <p class="text-muted small">Eine UUID, die allen Servern, die diese Option verwenden, zu Identifikationszwecken zugewiesen wird.</p>
                     </div>
                 </div>
             </div>
@@ -80,8 +80,8 @@
                     <tr>
                         <th>ID</th>
                         <th>Name</th>
-                        <th>Description</th>
-                        <th class="text-center">Servers</th>
+                        <th>Beschreibung</th>
+                        <th class="text-center">Server</th>
                         <th class="text-center"></th>
                     </tr>
                     @foreach($nest->eggs as $egg)
@@ -98,7 +98,7 @@
                 </table>
             </div>
             <div class="box-footer">
-                <a href="{{ route('admin.nests.egg.new') }}"><button class="btn btn-success btn-sm pull-right">New Egg</button></a>
+                <a href="{{ route('admin.nests.egg.new') }}"><button class="btn btn-success btn-sm pull-right">Neues Egg</button></a>
             </div>
         </div>
     </div>
@@ -109,7 +109,7 @@
     @parent
     <script>
         $('#deleteButton').on('mouseenter', function (event) {
-            $(this).find('i').html(' Delete Nest');
+            $(this).find('i').html(' Nest löschen');
         }).on('mouseleave', function (event) {
             $(this).find('i').html('');
         });
diff --git a/resources/views/admin/nodes/index.blade.php b/resources/views/admin/nodes/index.blade.php
index 3b3a3fa..fd9434b 100644
--- a/resources/views/admin/nodes/index.blade.php
+++ b/resources/views/admin/nodes/index.blade.php
@@ -1,7 +1,7 @@
 @extends('layouts.admin')
 
 @section('title')
-    List Nodes
+    Alle Nodes
 @endsection
 
 @section('scripts')
@@ -10,7 +10,7 @@
 @endsection
 
 @section('content-header')
-    <h1>Nodes<small>All nodes available on the system.</small></h1>
+    <h1>Nodes<small>Alle Nodes in deinem System.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li class="active">Nodes</li>
@@ -22,14 +22,14 @@
     <div class="col-xs-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Node List</h3>
+                <h3 class="box-title">Node-Übersicht</h3>
                 <div class="box-tools search01">
                     <form action="{{ route('admin.nodes') }}" method="GET">
                         <div class="input-group input-group-sm">
-                            <input type="text" name="filter[name]" class="form-control pull-right" value="{{ request()->input('filter.name') }}" placeholder="Search Nodes">
+                            <input type="text" name="filter[name]" class="form-control pull-right" value="{{ request()->input('filter.name') }}" placeholder="Node suchen...">
                             <div class="input-group-btn">
                                 <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
-                                <a href="{{ route('admin.nodes.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Create New</button></a>
+                                <a href="{{ route('admin.nodes.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Hinzufügen</button></a>
                             </div>
                         </div>
                     </form>
@@ -41,12 +41,12 @@
                         <tr>
                             <th></th>
                             <th>Name</th>
-                            <th>Location</th>
-                            <th>Memory</th>
-                            <th>Disk</th>
-                            <th class="text-center">Servers</th>
+                            <th>Standort</th>
+                            <th>Arbeitsspeicher</th>
+                            <th>Speicherplatz</th>
+                            <th class="text-center">Server</th>
                             <th class="text-center">SSL</th>
-                            <th class="text-center">Public</th>
+                            <th class="text-center">Öffentlich</th>
                         </tr>
                         @foreach ($nodes as $node)
                             <tr>
@@ -87,11 +87,11 @@
                 timeout: 5000
             }).done(function (data) {
                 $(element).find('i').tooltip({
-                    title: 'v' + data.version,
+                    title: 'Läuft auf Version ' + data.version,
                 });
                 $(element).removeClass('text-muted').find('i').removeClass().addClass('fa fa-fw fa-heartbeat faa-pulse animated').css('color', '#50af51');
             }).fail(function (error) {
-                var errorText = 'Error connecting to node! Check browser console for details.';
+                var errorText = 'Diese Node antwortet nicht, bitte überprüfe diesen Server und die Konsole.';
                 try {
                     errorText = error.responseJSON.errors[0].detail || errorText;
                 } catch (ex) {}
diff --git a/resources/views/admin/nodes/new.blade.php b/resources/views/admin/nodes/new.blade.php
index b10d08a..e90a03c 100644
--- a/resources/views/admin/nodes/new.blade.php
+++ b/resources/views/admin/nodes/new.blade.php
@@ -1,15 +1,15 @@
 @extends('layouts.admin')
 
 @section('title')
-    Nodes &rarr; New
+    Node anlegen
 @endsection
 
 @section('content-header')
-    <h1>New Node<small>Create a new local or remote node for servers to be installed to.</small></h1>
+    <h1>Neuer Node<small>Erstelle einen lokalen externen Node-Server</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
-        <li class="active">New</li>
+        <li class="active">Node erstellen</li>
     </ol>
 @endsection
 
@@ -19,20 +19,20 @@
         <div class="col-sm-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Basic Details</h3>
+                    <h3 class="box-title">Infos zur Node</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
                         <label for="pName" class="form-label">Name</label>
                         <input type="text" name="name" id="pName" class="form-control" value="{{ old('name') }}"/>
-                        <p class="text-muted small">Character limits: <code>a-zA-Z0-9_.-</code> and <code>[Space]</code> (min 1, max 100 characters).</p>
+                        <p class="text-muted small">Erlaubte Zeichen: <code>a-zA-Z0-9_.-</code> und <code>[Leerzeichen]</code> (mind. 1, max. 100).</p>
                     </div>
                     <div class="form-group">
-                        <label for="pDescription" class="form-label">Description</label>
+                        <label for="pDescription" class="form-label">Beschreibung</label>
                         <textarea name="description" id="pDescription" rows="4" class="form-control">{{ old('description') }}</textarea>
                     </div>
                     <div class="form-group">
-                        <label for="pLocationId" class="form-label">Location</label>
+                        <label for="pLocationId" class="form-label">Standort</label>
                         <select name="location_id" id="pLocationId">
                             @foreach($locations as $location)
                                 <option value="{{ $location->id }}" {{ $location->id != old('location_id') ?: 'selected' }}>{{ $location->short }}</option>
@@ -40,56 +40,56 @@
                         </select>
                     </div>
                     <div class="form-group">
-                        <label class="form-label">Node Visibility</label>
+                        <label class="form-label">Sichtbarkeit</label>
                         <div>
                             <div class="radio radio-success radio-inline">
 
                                 <input type="radio" id="pPublicTrue" value="1" name="public" checked>
-                                <label for="pPublicTrue"> Public </label>
+                                <label for="pPublicTrue"> Öffentlich </label>
                             </div>
                             <div class="radio radio-danger radio-inline">
                                 <input type="radio" id="pPublicFalse" value="0" name="public">
-                                <label for="pPublicFalse"> Private </label>
+                                <label for="pPublicFalse"> Privat </label>
                             </div>
                         </div>
-                        <p class="text-muted small">By setting a node to <code>private</code> you will be denying the ability to auto-deploy to this node.
+                        <p class="text-muted small">Wenn du dein Node auf <code>Privat</code> einstellst, blockst du die Fähigkeit zur automatischen Bereitstellung auf diesem Node.
                     </div>
                     <div class="form-group">
                         <label for="pFQDN" class="form-label">FQDN</label>
                         <input type="text" name="fqdn" id="pFQDN" class="form-control" value="{{ old('fqdn') }}"/>
-                        <p class="text-muted small">Please enter domain name (e.g <code>node.example.com</code>) to be used for connecting to the daemon. An IP address may be used <em>only</em> if you are not using SSL for this node.</p>
+                        <p class="text-muted small">Gib eine Domain für die Node ein, (Bsp.: <code>node.example.com</code>) um sich mit dem Daemon verbinden zu können. Eine IP-Adresse kann <em>nur genutzt werden</em>, wenn du kein SSL verwendest.</p>
                     </div>
                     <div class="form-group">
-                        <label class="form-label">Communicate Over SSL</label>
+                        <label class="form-label">Über SSL kommunizieren</label>
                         <div>
                             <div class="radio radio-success radio-inline">
                                 <input type="radio" id="pSSLTrue" value="https" name="scheme" checked>
-                                <label for="pSSLTrue"> Use SSL Connection</label>
+                                <label for="pSSLTrue"> SSL verwenden</label>
                             </div>
                             <div class="radio radio-danger radio-inline">
                                 <input type="radio" id="pSSLFalse" value="http" name="scheme" @if(request()->isSecure()) disabled @endif>
-                                <label for="pSSLFalse"> Use HTTP Connection</label>
+                                <label for="pSSLFalse"> HTTP verwenden</label>
                             </div>
                         </div>
                         @if(request()->isSecure())
-                            <p class="text-danger small">Your Panel is currently configured to use a secure connection. In order for browsers to connect to your node it <strong>must</strong> use a SSL connection.</p>
+                            <p class="text-danger small">Dein Panel ist derzeit für die Verwendung einer sicheren Verbindung konfiguriert. Damit sich Browser mit Ihrem Node verbinden können, <strong>muss</strong> eine SSL-Verbindung verwendet werden.</p>
                         @else
-                            <p class="text-muted small">In most cases you should select to use a SSL connection. If using an IP Address or you do not wish to use SSL at all, select a HTTP connection.</p>
+                            <p class="text-muted small">In den meisten Fällen ist eine SSL-Verbindung zu empfehlen. Wenn du eine IP-Adresse verwendest oder überhaupt kein SSL verwenden möchtest, wähle eine HTTP-Verbindung.</p>
                         @endif
                     </div>
                     <div class="form-group">
-                        <label class="form-label">Behind Proxy</label>
+                        <label class="form-label">Proxy nutzen</label>
                         <div>
                             <div class="radio radio-success radio-inline">
                                 <input type="radio" id="pProxyFalse" value="0" name="behind_proxy" checked>
-                                <label for="pProxyFalse"> Not Behind Proxy </label>
+                                <label for="pProxyFalse"> Nein </label>
                             </div>
                             <div class="radio radio-info radio-inline">
                                 <input type="radio" id="pProxyTrue" value="1" name="behind_proxy">
-                                <label for="pProxyTrue"> Behind Proxy </label>
+                                <label for="pProxyTrue"> Ja </label>
                             </div>
                         </div>
-                        <p class="text-muted small">If you are running the daemon behind a proxy such as Cloudflare, select this to have the daemon skip looking for certificates on boot.</p>
+                        <p class="text-muted small">Wenn du den Daemon hinter einem Proxy wie Cloudflare ausführst, wähle diese Option aus, damit der Daemon die Suche nach Zertifikaten beim Booten überspringt.</p>
                     </div>
                 </div>
             </div>
@@ -97,50 +97,50 @@
         <div class="col-sm-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Configuration</h3>
+                    <h3 class="box-title">System-Bereitstellung</h3>
                 </div>
                 <div class="box-body">
                     <div class="row">
                         <div class="form-group col-md-6">
-                            <label for="pDaemonBase" class="form-label">Daemon Server File Directory</label>
+                            <label for="pDaemonBase" class="form-label">Daemon Server - Verzeichnis</label>
                             <input type="text" name="daemonBase" id="pDaemonBase" class="form-control" value="/var/lib/pterodactyl/volumes" />
-                            <p class="text-muted small">Enter the directory where server files should be stored. <strong>If you use OVH you should check your partition scheme. You may need to use <code>/home/daemon-data</code> to have enough space.</strong></p>
+                            <p class="text-muted small">Gebe das Verzeichnis ein, in dem Serverdateien gespeichert werden sollen. <strong>Wenn du OVH verwendest, solltest du das Partitionsschema überprüfen. Möglicherweise musst du <code>/home/daemon-data</code> verwenden, um genügend Speicherplatz zu haben.</strong></p>
                         </div>
                         <div class="form-group col-md-6">
-                            <label for="pMemory" class="form-label">Total Memory</label>
+                            <label for="pMemory" class="form-label">Verfügbarer RAM</label>
                             <div class="input-group">
                                 <input type="text" name="memory" data-multiplicator="true" class="form-control" id="pMemory" value="{{ old('memory') }}"/>
-                                <span class="input-group-addon">MiB</span>
+                                <span class="input-group-addon">MB</span>
                             </div>
                         </div>
                         <div class="form-group col-md-6">
-                            <label for="pMemoryOverallocate" class="form-label">Memory Over-Allocation</label>
+                            <label for="pMemoryOverallocate" class="form-label">RAM-Toleranz über dem Limit</label>
                             <div class="input-group">
                                 <input type="text" name="memory_overallocate" class="form-control" id="pMemoryOverallocate" value="{{ old('memory_overallocate') }}"/>
                                 <span class="input-group-addon">%</span>
                             </div>
                         </div>
                         <div class="col-md-12">
-                            <p class="text-muted small">Enter the total amount of memory available for new servers. If you would like to allow overallocation of memory enter the percentage that you want to allow. To disable checking for overallocation enter <code>-1</code> into the field. Entering <code>0</code> will prevent creating new servers if it would put the node over the limit.</p>
+                            <p class="text-muted small">Gebe die Gesamtmenge an verfügbarem Arbeitsspeicher für neue Server ein. Wenn du eine Überbelegung des Arbeitsspeichers zulassen möchtest, gebe den Prozentsatz ein, den du zulassen möchtest. Um die Überprüfung auf Überbelegung zu deaktivieren, gebe <code>-1</code> in das Feld ein. Die Eingabe von <code>0</code> verhindert das Erstellen neuer Server, wenn der Knoten dadurch das Limit überschreiten würde.</p>
                         </div>
                     </div>
                     <div class="row">
                         <div class="form-group col-md-6">
-                            <label for="pDisk" class="form-label">Total Disk Space</label>
+                            <label for="pDisk" class="form-label">Verfügbarer Speicher</label>
                             <div class="input-group">
                                 <input type="text" name="disk" data-multiplicator="true" class="form-control" id="pDisk" value="{{ old('disk') }}"/>
-                                <span class="input-group-addon">MiB</span>
+                                <span class="input-group-addon">MB</span>
                             </div>
                         </div>
                         <div class="form-group col-md-6">
-                            <label for="pDiskOverallocate" class="form-label">Disk Over-Allocation</label>
+                            <label for="pDiskOverallocate" class="form-label">Speicher-Toleranz über den Limit</label>
                             <div class="input-group">
                                 <input type="text" name="disk_overallocate" class="form-control" id="pDiskOverallocate" value="{{ old('disk_overallocate') }}"/>
                                 <span class="input-group-addon">%</span>
                             </div>
                         </div>
                         <div class="col-md-12">
-                            <p class="text-muted small">Enter the total amount of disk space available for new servers. If you would like to allow overallocation of disk space enter the percentage that you want to allow. To disable checking for overallocation enter <code>-1</code> into the field. Entering <code>0</code> will prevent creating new servers if it would put the node over the limit.</p>
+                            <p class="text-muted small">Gebe die Gesamtmenge an Speicherplatz ein, die für neue Server verfügbar ist. Wenn du eine Überbelegung des Speicherplatzes zulassen möchtest, gebe den Prozentsatz ein, den Sie zulassen möchtest. Um die Überprüfung auf Überbelegung zu deaktivieren, gebe <code>-1</code> in das Feld ein. Die Eingabe von <code>0</code> verhindert das Erstellen neuer Server, wenn der Knoten dadurch das Limit überschreiten würde.</p>
                         </div>
                     </div>
                     <div class="row">
@@ -153,13 +153,13 @@
                             <input type="text" name="daemonSFTP" class="form-control" id="pDaemonSFTP" value="2022" />
                         </div>
                         <div class="col-md-12">
-                            <p class="text-muted small">The daemon runs its own SFTP management container and does not use the SSHd process on the main physical server. <Strong>Do not use the same port that you have assigned for your physical server's SSH process.</strong> If you will be running the daemon behind CloudFlare&reg; you should set the daemon port to <code>8443</code> to allow websocket proxying over SSL.</p>
+                            <p class="text-muted small">Der Daemon führt seinen eigenen SFTP-Verwaltungscontainer aus und verwendet nicht den SSHd-Prozess auf dem physischen Hauptserver. <Strong>Verwende nicht denselben Port, den du für den SSH-Prozess Ihres physischen Servers zugewiesen hast.</strong> Wenn du den Daemon hinter CloudFlare&reg; nutzt, solltest du den Daemon-Port auf <code>8443</code> setzen, um Websocket-Proxy über SSL zuzulassen.</p>
                         </div>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" class="btn btn-success pull-right">Create Node</button>
+                    <button type="submit" class="btn btn-success pull-right">Node erstellen</button>
                 </div>
             </div>
         </div>
diff --git a/resources/views/admin/nodes/view/allocation.blade.php b/resources/views/admin/nodes/view/allocation.blade.php
index 396428b..e96b58a 100644
--- a/resources/views/admin/nodes/view/allocation.blade.php
+++ b/resources/views/admin/nodes/view/allocation.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    {{ $node->name }}: Allocations
+    {{ $node->name }}: Freigegebene Ports
 @endsection
 
 @section('content-header')
-    <h1>{{ $node->name }}<small>Control allocations available for servers on this node.</small></h1>
+    <h1>{{ $node->name }}<small>Verwalte die zugewiesenen Ports</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
-        <li class="active">Allocations</li>
+        <li class="active">Ports</li>
     </ol>
 @endsection
 
@@ -19,11 +19,11 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
-                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
-                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
-                <li class="active"><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
-                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Info</a></li>
+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
+                <li class="active"><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
             </ul>
         </div>
     </div>
@@ -32,7 +32,7 @@
     <div class="col-sm-8">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Existing Allocations</h3>
+                <h3 class="box-title">Zugeteilte Ports</h3>
             </div>
             <div class="box-body table-responsive no-padding" style="overflow-x: visible">
                 <table class="table table-hover" style="margin-bottom:0;">
@@ -40,17 +40,17 @@
                         <th>
                             <input type="checkbox" class="select-all-files hidden-xs" data-action="selectAll">
                         </th>
-                        <th>IP Address <i class="fa fa-fw fa-minus-square" style="font-weight:normal;color:#d9534f;cursor:pointer;" data-toggle="modal" data-target="#allocationModal"></i></th>
-                        <th>IP Alias</th>
+                        <th>IP Addresse<i class="fa fa-fw fa-minus-square" style="font-weight:normal;color:#d9534f;cursor:pointer;" data-toggle="modal" data-target="#allocationModal"></i></th>
+                        <th>Vermerk zur IP</th>
                         <th>Port</th>
-                        <th>Assigned To</th>
+                        <th>Zugeteilt an</th>
                         <th>
                             <div class="btn-group hidden-xs">
                                 <button type="button" id="mass_actions" class="btn btn-sm btn-default dropdown-toggle disabled"
-                                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mass Actions <span class="caret"></span>
+                                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mehrfachauswahl <span class="caret"></span>
                                 </button>
                                 <ul class="dropdown-menu dropdown-massactions">
-                                    <li><a href="#" id="selective-deletion" data-action="selective-deletion">Delete <i class="fa fa-fw fa-trash-o"></i></a></li>
+                                    <li><a href="#" id="selective-deletion" data-action="selective-deletion">Löschen <i class="fa fa-fw fa-trash-o"></i></a></li>
                                 </ul>
                             </div>
                         </th>
@@ -95,38 +95,38 @@
         <form action="{{ route('admin.nodes.view.allocation', $node->id) }}" method="POST">
             <div class="box box-success">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Assign New Allocations</h3>
+                    <h3 class="box-title">Manuelle Portfreigabe</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
-                        <label for="pAllocationIP" class="control-label">IP Address</label>
+                        <label for="pAllocationIP" class="control-label">IP Addresse</label>
                         <div>
                             <select class="form-control" name="allocation_ip" id="pAllocationIP" multiple>
                                 @foreach($allocations as $allocation)
                                     <option value="{{ $allocation->ip }}">{{ $allocation->ip }}</option>
                                 @endforeach
                             </select>
-                            <p class="text-muted small">Enter an IP address to assign ports to here.</p>
+                            <p class="text-muted small">Gib die IP-Adresse ein, die verwendet werden soll.</p>
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="pAllocationIP" class="control-label">IP Alias</label>
+                        <label for="pAllocationIP" class="control-label">Vermerk zur IP</label>
                         <div>
                             <input type="text" id="pAllocationAlias" class="form-control" name="allocation_alias" placeholder="alias" />
-                            <p class="text-muted small">If you would like to assign a default alias to these allocations enter it here.</p>
+                            <p class="text-muted small">Wenn diese IP mit entsprechenden Ports mit einer Info vermerkt werden soll, kannst du das hier tun. Bsp: Minecraft Ports.</p>
                         </div>
                     </div>
                     <div class="form-group">
                         <label for="pAllocationPorts" class="control-label">Ports</label>
                         <div>
                             <select class="form-control" name="allocation_ports[]" id="pAllocationPorts" multiple></select>
-                            <p class="text-muted small">Enter individual ports or port ranges here separated by commas or spaces.</p>
+                            <p class="text-muted small">Gib alle Ports ein, die freigegeben werden sollen. Du kannst mit Kommas oder Bindestrichen mehrere Ports auf einmal freigeben.</p>
                         </div>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" class="btn btn-success btn-sm pull-right">Submit</button>
+                    <button type="submit" class="btn btn-success btn-sm pull-right">Übernehmen</button>
                 </div>
             </div>
         </form>
@@ -137,7 +137,7 @@
         <div class="modal-content">
             <div class="modal-header">
                 <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
-                <h4 class="modal-title">Delete Allocations for IP Block</h4>
+                <h4 class="modal-title">Ports für den IP-Block entfernen</h4>
             </div>
             <form action="{{ route('admin.nodes.view.allocation.removeBlock', $node->id) }}" method="POST">
                 <div class="modal-body">
@@ -153,8 +153,8 @@
                 </div>
                 <div class="modal-footer">
                     {{{ csrf_field() }}}
-                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
-                    <button type="submit" class="btn btn-danger">Delete Allocations</button>
+                    <button type="button" class="btn btn-default" data-dismiss="modal">Schliessen</button>
+                    <button type="submit" class="btn btn-danger">Ports entfernen</button>
                 </div>
             </form>
         </div>
@@ -200,12 +200,13 @@
         var allocation = $(this).data('id');
         swal({
             title: '',
-            text: 'Are you sure you want to delete this allocation?',
+            text: 'Bist du sicher, das du diesen Port entfernen möchtest?',
             type: 'warning',
             showCancelButton: true,
+            cancelButtonText: 'Abbrechen',
             allowOutsideClick: true,
             closeOnConfirm: false,
-            confirmButtonText: 'Delete',
+            confirmButtonText: 'Ja, entfernen',
             confirmButtonColor: '#d9534f',
             showLoaderOnConfirm: true
         }, function () {
@@ -215,11 +216,11 @@
                 headers: { 'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content') },
             }).done(function (data) {
                 element.parent().parent().addClass('warning').delay(100).fadeOut();
-                swal({ type: 'success', title: 'Port Deleted!' });
+                swal({ type: 'success', title: 'Port entfernt' });
             }).fail(function (jqXHR) {
                 console.error(jqXHR);
                 swal({
-                    title: 'Whoops!',
+                    title: 'Hups!',
                     text: jqXHR.responseJSON.error,
                     type: 'error'
                 });
@@ -299,15 +300,16 @@
 
             formattedItems = formattedItems.slice(0, -2);
             if (selectedItems.length > 5) {
-                formattedItems += ', and ' + (selectedItems.length - 5) + ' other(s)';
+                formattedItems += ', und ' + (selectedItems.length - 5) + ' weitere';
             }
 
             swal({
                 type: 'warning',
                 title: '',
-                text: 'Are you sure you want to delete the following allocations: ' + formattedItems + '?',
+                text: 'Möchtest du wirklich folgende Ports entfernen: ' + formattedItems + '?',
                 html: true,
                 showCancelButton: true,
+                cancelButtonText: 'Abbrechen',
                 showConfirmButton: true,
                 closeOnConfirm: false,
                 showLoaderOnConfirm: true
@@ -332,15 +334,15 @@
 
                     swal({
                         type: 'success',
-                        title: 'Allocations Deleted'
+                        title: 'Ports entfernt'
                     });
                 }).fail(function (jqXHR) {
                     console.error(jqXHR);
                     swal({
                         type: 'error',
-                        title: 'Whoops!',
+                        title: 'Hups!',
                         html: true,
-                        text: 'An error occurred while attempting to delete these allocations. Please try again.',
+                        text: 'Es ist ein Fehler beim entfernen der Ports aufgetreten. Versuch es später nochmal.',
                     });
                 });
             });
@@ -348,7 +350,7 @@
             swal({
                 type: 'warning',
                 title: '',
-                text: 'Please select allocation(s) to delete.',
+                text: 'Du hast keine Ports ausgewählt, die entfernt werden sollen.',
             });
         }
     }
diff --git a/resources/views/admin/nodes/view/configuration.blade.php b/resources/views/admin/nodes/view/configuration.blade.php
index 1bbf15f..218731c 100644
--- a/resources/views/admin/nodes/view/configuration.blade.php
+++ b/resources/views/admin/nodes/view/configuration.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    {{ $node->name }}: Configuration
+    {{ $node->name }}: Wings-Integration
 @endsection
 
 @section('content-header')
-    <h1>{{ $node->name }}<small>Your daemon configuration file.</small></h1>
+    <h1>{{ $node->name }}<small>Die Konfguationsdatei.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
-        <li class="active">Configuration</li>
+        <li class="active">Wings-Integration</li>
     </ol>
 @endsection
 
@@ -19,11 +19,11 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
-                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
-                <li class="active"><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
-                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
-                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Info</a></li>
+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
+                <li class="active"><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
             </ul>
         </div>
     </div>
@@ -32,29 +32,28 @@
     <div class="col-sm-8">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Configuration File</h3>
+                <h3 class="box-title">Generierte Config.yml</h3>
             </div>
             <div class="box-body">
                 <pre class="no-margin">{{ $node->getYamlConfiguration() }}</pre>
             </div>
             <div class="box-footer">
-                <p class="no-margin">This file should be placed in your daemon's root directory (usually <code>/etc/pterodactyl</code>) in a file called <code>config.yml</code>.</p>
+                <p class="no-margin">Diese Textzeilen müssen in der <code>config.yml</code> eingesetzt werden. Du findest die Datei im Pfad <code>/etc/pterodactyl</code>.</p>
             </div>
         </div>
     </div>
     <div class="col-sm-4">
         <div class="box box-success">
             <div class="box-header with-border">
-                <h3 class="box-title">Auto-Deploy</h3>
+                <h3 class="box-title">Automatisch Einrichten</h3>
             </div>
             <div class="box-body">
                 <p class="text-muted small">
-                    Use the button below to generate a custom deployment command that can be used to configure
-                    wings on the target server with a single command.
+                    Du kannst auch die einfache Variante nutzen, indem du einen Befehl in deinen Linux Server sendest, der generiert wird.
                 </p>
             </div>
             <div class="box-footer">
-                <button type="button" id="configTokenBtn" class="btn btn-sm btn-default" style="width:100%;">Generate Token</button>
+                <button type="button" id="configTokenBtn" class="btn btn-sm btn-default" style="width:100%;">Befehl generieren</button>
             </div>
         </div>
     </div>
@@ -72,14 +71,14 @@
         }).done(function (data) {
             swal({
                 type: 'success',
-                title: 'Token created.',
-                text: '<p>To auto-configure your node run the following command:<br /><small><pre>cd /etc/pterodactyl && sudo wings configure --panel-url {{ config('app.url') }} --token ' + data.token + ' --node ' + data.node + '{{ config('app.debug') ? ' --allow-insecure' : '' }}</pre></small></p>',
+                title: 'Befehl erstellt.',
+                text: '<p>Führe folgenden Befehl aus, um dies automatisch zu übernehmen:<br /><small><pre>cd /etc/pterodactyl && sudo wings configure --panel-url {{ config('app.url') }} --token ' + data.token + ' --node ' + data.node + '{{ config('app.debug') ? ' --allow-insecure' : '' }}</pre></small></p>',
                 html: true
             })
         }).fail(function () {
             swal({
-                title: 'Error',
-                text: 'Something went wrong creating your token.',
+                title: 'Fehler bei der Erstellung',
+                text: 'Irgendwas hat nicht geklappt, versuch es gleich nochmal.',
                 type: 'error'
             });
         });
diff --git a/resources/views/admin/nodes/view/index.blade.php b/resources/views/admin/nodes/view/index.blade.php
index 2d0bb32..c24992f 100644
--- a/resources/views/admin/nodes/view/index.blade.php
+++ b/resources/views/admin/nodes/view/index.blade.php
@@ -5,7 +5,7 @@
 @endsection
 
 @section('content-header')
-    <h1>{{ $node->name }}<small>A quick overview of your node.</small></h1>
+    <h1>{{ $node->name }}<small>Eine kleine Übersicht zum Node</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
@@ -18,11 +18,11 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li class="active"><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
-                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
-                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
-                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
-                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
+                <li class="active"><a href="{{ route('admin.nodes.view', $node->id) }}">Infos</a></li>
+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Verbundene Server</a></li>
             </ul>
         </div>
     </div>
@@ -39,14 +39,14 @@
                         <table class="table table-hover">
                             <tr>
                                 <td>Daemon Version</td>
-                                <td><code data-attr="info-version"><i class="fa fa-refresh fa-fw fa-spin"></i></code> (Latest: <code>{{ $version->getDaemon() }}</code>)</td>
+                                <td><code data-attr="info-version"><i class="fa fa-refresh fa-fw fa-spin"></i></code> (Neuste: <code>{{ $version->getDaemon() }}</code>)</td>
                             </tr>
                             <tr>
-                                <td>System Information</td>
+                                <td>System-Information</td>
                                 <td data-attr="info-system"><i class="fa fa-refresh fa-fw fa-spin"></i></td>
                             </tr>
                             <tr>
-                                <td>Total CPU Threads</td>
+                                <td>Anzahl an CPU-Kerne</td>
                                 <td data-attr="info-cpus"><i class="fa fa-refresh fa-fw fa-spin"></i></td>
                             </tr>
                         </table>
@@ -57,7 +57,7 @@
                 <div class="col-xs-12">
                     <div class="box box-default">
                         <div class="box-header with-border">
-                            Description
+                            Beschreibung
                         </div>
                         <div class="box-body table-responsive">
                             <pre>{{ $node->description }}</pre>
@@ -68,16 +68,16 @@
             <div class="col-xs-12">
                 <div class="box box-danger">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Delete Node</h3>
+                        <h3 class="box-title">Node löschen</h3>
                     </div>
                     <div class="box-body">
-                        <p class="no-margin">Deleting a node is a irreversible action and will immediately remove this node from the panel. There must be no servers associated with this node in order to continue.</p>
+                        <p class="no-margin">Dies kann nicht rückgängig gemacht, aber neu hinzugefügt und eingerichtet werden. Diesem Node dürfen keine Server zugeordnet sein, um fortzufahren.</p>
                     </div>
                     <div class="box-footer">
                         <form action="{{ route('admin.nodes.view.delete', $node->id) }}" method="POST">
                             {!! csrf_field() !!}
                             {!! method_field('DELETE') !!}
-                            <button type="submit" class="btn btn-danger btn-sm pull-right" {{ ($node->servers_count < 1) ?: 'disabled' }}>Yes, Delete This Node</button>
+                            <button type="submit" class="btn btn-danger btn-sm pull-right" {{ ($node->servers_count < 1) ?: 'disabled' }}>Ja, diese Node löschen</button>
                         </form>
                     </div>
                 </div>
@@ -87,7 +87,7 @@
     <div class="col-sm-4">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">At-a-Glance</h3>
+                <h3 class="box-title">Verfügbare Ressourcen</h3>
             </div>
             <div class="box-body">
                 <div class="row">
@@ -96,8 +96,8 @@
                         <div class="info-box bg-orange">
                             <span class="info-box-icon"><i class="ion ion-wrench"></i></span>
                             <div class="info-box-content" style="padding: 23px 10px 0;">
-                                <span class="info-box-text">This node is under</span>
-                                <span class="info-box-number">Maintenance</span>
+                                <span class="info-box-text">Diese Node ist in</span>
+                                <span class="info-box-number">Wartung</span>
                             </div>
                         </div>
                     </div>
@@ -106,8 +106,8 @@
                         <div class="info-box bg-{{ $stats['disk']['css'] }}">
                             <span class="info-box-icon"><i class="ion ion-ios-folder-outline"></i></span>
                             <div class="info-box-content" style="padding: 15px 10px 0;">
-                                <span class="info-box-text">Disk Space Allocated</span>
-                                <span class="info-box-number">{{ $stats['disk']['value'] }} / {{ $stats['disk']['max'] }} MiB</span>
+                                <span class="info-box-text">Speicherplatz</span>
+                                <span class="info-box-number">{{ $stats['disk']['value'] }} / {{ $stats['disk']['max'] }} MB</span>
                                 <div class="progress">
                                     <div class="progress-bar" style="width: {{ $stats['disk']['percent'] }}%"></div>
                                 </div>
@@ -118,7 +118,7 @@
                         <div class="info-box bg-{{ $stats['memory']['css'] }}">
                             <span class="info-box-icon"><i class="ion ion-ios-barcode-outline"></i></span>
                             <div class="info-box-content" style="padding: 15px 10px 0;">
-                                <span class="info-box-text">Memory Allocated</span>
+                                <span class="info-box-text">Arbeitsspeicher</span>
                                 <span class="info-box-number">{{ $stats['memory']['value'] }} / {{ $stats['memory']['max'] }} MiB</span>
                                 <div class="progress">
                                     <div class="progress-bar" style="width: {{ $stats['memory']['percent'] }}%"></div>
@@ -130,7 +130,7 @@
                         <div class="info-box bg-blue">
                             <span class="info-box-icon"><i class="ion ion-social-buffer-outline"></i></span>
                             <div class="info-box-content" style="padding: 23px 10px 0;">
-                                <span class="info-box-text">Total Servers</span>
+                                <span class="info-box-text">Server verbunden</span>
                                 <span class="info-box-number">{{ $node->servers_count }}</span>
                             </div>
                         </div>
diff --git a/resources/views/admin/nodes/view/servers.blade.php b/resources/views/admin/nodes/view/servers.blade.php
index b8b9ee5..5913b2f 100644
--- a/resources/views/admin/nodes/view/servers.blade.php
+++ b/resources/views/admin/nodes/view/servers.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    {{ $node->name }}: Servers
+    {{ $node->name }}: Alle Server
 @endsection
 
 @section('content-header')
-    <h1>{{ $node->name }}<small>All servers currently assigned to this node.</small></h1>
+    <h1>{{ $node->name }}<small>Alle Server, die mit diesem Node verbunden sind.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
-        <li class="active">Servers</li>
+        <li class="active">Alle Server</li>
     </ol>
 @endsection
 
@@ -19,11 +19,11 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
-                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
-                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
-                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
-                <li class="active"><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Infos</a></li>
+                <li><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
+                <li class="active"><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
             </ul>
         </div>
     </div>
@@ -32,14 +32,14 @@
     <div class="col-sm-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Process Manager</h3>
+                <h3 class="box-title">Laufende Server</h3>
             </div>
             <div class="box-body table-responsive no-padding">
                 <table class="table table-hover">
                     <tr>
                         <th>ID</th>
-                        <th>Server Name</th>
-                        <th>Owner</th>
+                        <th>Projektname</th>
+                        <th>Admin</th>
                         <th>Service</th>
                     </tr>
                     @foreach($servers as $server)
diff --git a/resources/views/admin/nodes/view/settings.blade.php b/resources/views/admin/nodes/view/settings.blade.php
index bfc9d8c..50e4899 100644
--- a/resources/views/admin/nodes/view/settings.blade.php
+++ b/resources/views/admin/nodes/view/settings.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    {{ $node->name }}: Settings
+    {{ $node->name }}: Einstellungen
 @endsection
 
 @section('content-header')
-    <h1>{{ $node->name }}<small>Configure your node settings.</small></h1>
+    <h1>{{ $node->name }}<small>Node-Einstellungen ändern.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
         <li><a href="{{ route('admin.nodes') }}">Nodes</a></li>
         <li><a href="{{ route('admin.nodes.view', $node->id) }}">{{ $node->name }}</a></li>
-        <li class="active">Settings</li>
+        <li class="active">Einstellungen</li>
     </ol>
 @endsection
 
@@ -19,11 +19,11 @@
     <div class="col-xs-12">
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
-                <li><a href="{{ route('admin.nodes.view', $node->id) }}">About</a></li>
-                <li class="active"><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Settings</a></li>
-                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Configuration</a></li>
-                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Allocation</a></li>
-                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Servers</a></li>
+                <li><a href="{{ route('admin.nodes.view', $node->id) }}">Info</a></li>
+                <li class="active"><a href="{{ route('admin.nodes.view.settings', $node->id) }}">Einstellungen</a></li>
+                <li><a href="{{ route('admin.nodes.view.configuration', $node->id) }}">Wings-Integration</a></li>
+                <li><a href="{{ route('admin.nodes.view.allocation', $node->id) }}">Freigegebene Ports</a></li>
+                <li><a href="{{ route('admin.nodes.view.servers', $node->id) }}">Alle Server</a></li>
             </ul>
         </div>
     </div>
@@ -33,24 +33,24 @@
         <div class="col-sm-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Settings</h3>
+                    <h3 class="box-title">Einstellungen</h3>
                 </div>
                 <div class="box-body row">
                     <div class="form-group col-xs-12">
                         <label for="name" class="control-label">Node Name</label>
                         <div>
                             <input type="text" autocomplete="off" name="name" class="form-control" value="{{ old('name', $node->name) }}" />
-                            <p class="text-muted"><small>Character limits: <code>a-zA-Z0-9_.-</code> and <code>[Space]</code> (min 1, max 100 characters).</small></p>
+                            <p class="text-muted"><small>Mögliche Zeichen: <code>a-zA-Z0-9_.-</code> and <code>[Leerzeichen]</code> (min 1, max 100 Zeichen).</small></p>
                         </div>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label for="description" class="control-label">Description</label>
+                        <label for="description" class="control-label">Beschreibung</label>
                         <div>
                             <textarea name="description" id="description" rows="4" class="form-control">{{ $node->description }}</textarea>
                         </div>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label for="name" class="control-label">Location</label>
+                        <label for="name" class="control-label">Standort</label>
                         <div>
                             <select name="location_id" class="form-control">
                                 @foreach($locations as $location)
@@ -60,62 +60,62 @@
                         </div>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label for="public" class="control-label">Allow Automatic Allocation <sup><a data-toggle="tooltip" data-placement="top" title="Allow automatic allocation to this Node?">?</a></sup></label>
+                        <label for="public" class="control-label">Automatische Portzuweisung erlauben <sup><a data-toggle="tooltip" data-placement="top" title="Die Ports dienen zur öffentlichen Freigabe des Servers. Wenn ein neuer Server erstellt wird, sucht das System sich den nächsten freien Port aus.">?</a></sup></label>
                         <div>
-                            <input type="radio" name="public" value="1" {{ (old('public', $node->public)) ? 'checked' : '' }} id="public_1" checked> <label for="public_1" style="padding-left:5px;">Yes</label><br />
-                            <input type="radio" name="public" value="0" {{ (old('public', $node->public)) ? '' : 'checked' }} id="public_0"> <label for="public_0" style="padding-left:5px;">No</label>
+                            <input type="radio" name="public" value="1" {{ (old('public', $node->public)) ? 'checked' : '' }} id="public_1" checked> <label for="public_1" style="padding-left:5px;">Ja</label><br />
+                            <input type="radio" name="public" value="0" {{ (old('public', $node->public)) ? '' : 'checked' }} id="public_0"> <label for="public_0" style="padding-left:5px;">Nein</label>
                         </div>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label for="fqdn" class="control-label">Fully Qualified Domain Name</label>
+                        <label for="fqdn" class="control-label">Domain für diesen Node</label>
                         <div>
                             <input type="text" autocomplete="off" name="fqdn" class="form-control" value="{{ old('fqdn', $node->fqdn) }}" />
                         </div>
-                        <p class="text-muted"><small>Please enter domain name (e.g <code>node.example.com</code>) to be used for connecting to the daemon. An IP address may only be used if you are not using SSL for this node.
-                                <a tabindex="0" data-toggle="popover" data-trigger="focus" title="Why do I need a FQDN?" data-content="In order to secure communications between your server and this node we use SSL. We cannot generate a SSL certificate for IP Addresses, and as such you will need to provide a FQDN.">Why?</a>
+                        <p class="text-muted"><small>Bitte gib den Domainnamen (z. B. <code>node.example.com</code>) ein, der für die Verbindung zum Daemon verwendet werden soll. Eine IP-Adresse darf nur verwendet werden, wenn du für diesen Node kein SSL verwendest.
+                                <a tabindex="0" data-toggle="popover" data-trigger="focus" title="Warum brauche ich eine extra Domain?" data-content="Um die Kommunikation zwischen deinem Server und diesem Node zu sichern, wird SSL verwendet. SSL-Zertifikate können nicht auf IP-Adressen angewendet werden, daher musst du eine eigene Domain dafür angeben.">Warum?</a>
                             </small></p>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Communicate Over SSL</label>
+                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Über SSL kommunizieren</label>
                         <div>
                             <div class="radio radio-success radio-inline">
                                 <input type="radio" id="pSSLTrue" value="https" name="scheme" {{ (old('scheme', $node->scheme) === 'https') ? 'checked' : '' }}>
-                                <label for="pSSLTrue"> Use SSL Connection</label>
+                                <label for="pSSLTrue"> SSL verwenden</label>
                             </div>
                             <div class="radio radio-danger radio-inline">
                                 <input type="radio" id="pSSLFalse" value="http" name="scheme" {{ (old('scheme', $node->scheme) !== 'https') ? 'checked' : '' }}>
-                                <label for="pSSLFalse"> Use HTTP Connection</label>
+                                <label for="pSSLFalse"> HTTP verwenden</label>
                             </div>
                         </div>
-                        <p class="text-muted small">In most cases you should select to use a SSL connection. If using an IP Address or you do not wish to use SSL at all, select a HTTP connection.</p>
+                        <p class="text-muted small">In den meisten Fällen solltest du dich für eine SSL-Verbindung entscheiden. Wenn du eine IP-Adresse verwendest oder überhaupt kein SSL verwenden möchtest, wähle eine HTTP-Verbindung.</p>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Behind Proxy</label>
+                        <label class="form-label"><span class="label label-warning"><i class="fa fa-power-off"></i></span> Proxy verwenden</label>
                         <div>
                             <div class="radio radio-success radio-inline">
                                 <input type="radio" id="pProxyFalse" value="0" name="behind_proxy" {{ (old('behind_proxy', $node->behind_proxy) == false) ? 'checked' : '' }}>
-                                <label for="pProxyFalse"> Not Behind Proxy </label>
+                                <label for="pProxyFalse"> Ja </label>
                             </div>
                             <div class="radio radio-info radio-inline">
                                 <input type="radio" id="pProxyTrue" value="1" name="behind_proxy" {{ (old('behind_proxy', $node->behind_proxy) == true) ? 'checked' : '' }}>
-                                <label for="pProxyTrue"> Behind Proxy </label>
+                                <label for="pProxyTrue"> Nein </label>
                             </div>
                         </div>
-                        <p class="text-muted small">If you are running the daemon behind a proxy such as Cloudflare, select this to have the daemon skip looking for certificates on boot.</p>
+                        <p class="text-muted small">Wenn du den Daemon hinter einem Proxy wie Cloudflare ausführst, wähle diese Option aus, damit der Daemon die Suche nach Zertifikaten beim Booten überspringt.</p>
                     </div>
                     <div class="form-group col-xs-12">
-                        <label class="form-label"><span class="label label-warning"><i class="fa fa-wrench"></i></span> Maintenance Mode</label>
+                        <label class="form-label"><span class="label label-warning"><i class="fa fa-wrench"></i></span> Wartungsmodus</label>
                         <div>
                             <div class="radio radio-success radio-inline">
                                 <input type="radio" id="pMaintenanceFalse" value="0" name="maintenance_mode" {{ (old('behind_proxy', $node->maintenance_mode) == false) ? 'checked' : '' }}>
-                                <label for="pMaintenanceFalse"> Disabled</label>
+                                <label for="pMaintenanceFalse"> Deaktiviert</label>
                             </div>
                             <div class="radio radio-warning radio-inline">
                                 <input type="radio" id="pMaintenanceTrue" value="1" name="maintenance_mode" {{ (old('behind_proxy', $node->maintenance_mode) == true) ? 'checked' : '' }}>
-                                <label for="pMaintenanceTrue"> Enabled</label>
+                                <label for="pMaintenanceTrue"> Aktiviert</label>
                             </div>
                         </div>
-                        <p class="text-muted small">If the node is marked as 'Under Maintenance' users won't be able to access servers that are on this node.</p>
+                        <p class="text-muted small">Wenn der Wartungsmodus an ist, können Benutzer, die unter diesem Node einen Server haben, währenddessen nicht den Server starten.</p>
                     </div>
                 </div>
             </div>
@@ -123,46 +123,46 @@
         <div class="col-sm-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Allocation Limits</h3>
+                    <h3 class="box-title">Ressoucen-Verwaltung</h3>
                 </div>
                 <div class="box-body row">
                     <div class="col-xs-12">
                         <div class="row">
                             <div class="form-group col-xs-6">
-                                <label for="memory" class="control-label">Total Memory</label>
+                                <label for="memory" class="control-label">Verfügbarer RAM</label>
                                 <div class="input-group">
                                     <input type="text" name="memory" class="form-control" data-multiplicator="true" value="{{ old('memory', $node->memory) }}"/>
-                                    <span class="input-group-addon">MiB</span>
+                                    <span class="input-group-addon">MB</span>
                                 </div>
                             </div>
                             <div class="form-group col-xs-6">
-                                <label for="memory_overallocate" class="control-label">Overallocate</label>
+                                <label for="memory_overallocate" class="control-label">RAM-Toleranz über dem Limit</label>
                                 <div class="input-group">
                                     <input type="text" name="memory_overallocate" class="form-control" value="{{ old('memory_overallocate', $node->memory_overallocate) }}"/>
                                     <span class="input-group-addon">%</span>
                                 </div>
                             </div>
                         </div>
-                        <p class="text-muted small">Enter the total amount of memory available on this node for allocation to servers. You may also provide a percentage that can allow allocation of more than the defined memory.</p>
+                        <p class="text-muted small">Gib die Gesamtmenge an Arbeitsspeicher ein, die auf diesem Node für die Zuordnung zu Servern verfügbar ist. Du kannst auch einen Prozentsatz angeben, der die Zuweisung von mehr als dem definierten Speicher zulassen kann.</p>
                     </div>
                     <div class="col-xs-12">
                         <div class="row">
                             <div class="form-group col-xs-6">
-                                <label for="disk" class="control-label">Disk Space</label>
+                                <label for="disk" class="control-label">Speicherplatz</label>
                                 <div class="input-group">
                                     <input type="text" name="disk" class="form-control" data-multiplicator="true" value="{{ old('disk', $node->disk) }}"/>
-                                    <span class="input-group-addon">MiB</span>
+                                    <span class="input-group-addon">MB</span>
                                 </div>
                             </div>
                             <div class="form-group col-xs-6">
-                                <label for="disk_overallocate" class="control-label">Overallocate</label>
+                                <label for="disk_overallocate" class="control-label">Speicher-Toleranz über dem Limit</label>
                                 <div class="input-group">
                                     <input type="text" name="disk_overallocate" class="form-control" value="{{ old('disk_overallocate', $node->disk_overallocate) }}"/>
                                     <span class="input-group-addon">%</span>
                                 </div>
                             </div>
                         </div>
-                        <p class="text-muted small">Enter the total amount of disk space available on this node for server allocation. You may also provide a percentage that will determine the amount of disk space over the set limit to allow.</p>
+                        <p class="text-muted small">Gib den Gesamtspeicherplatz ein, der auf diesem Node für die Serverzuweisung verfügbar ist. Du kannst auch einen Prozentsatz angeben, der bestimmt, wie viel Speicherplatz über dem festgelegten Limit zulässig ist.</p>
                     </div>
                 </div>
             </div>
@@ -170,16 +170,16 @@
         <div class="col-sm-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">General Configuration</h3>
+                    <h3 class="box-title">Allgemeine Einstellungen</h3>
                 </div>
                 <div class="box-body row">
                     <div class="form-group col-xs-12">
-                        <label for="disk_overallocate" class="control-label">Maximum Web Upload Filesize</label>
+                        <label for="disk_overallocate" class="control-label">Maximale Upload-Größe</label>
                         <div class="input-group">
                             <input type="text" name="upload_size" class="form-control" value="{{ old('upload_size', $node->upload_size) }}"/>
-                            <span class="input-group-addon">MiB</span>
+                            <span class="input-group-addon">MB</span>
                         </div>
-                        <p class="text-muted"><small>Enter the maximum size of files that can be uploaded through the web-based file manager.</small></p>
+                        <p class="text-muted"><small>Gib das Maximum an, wie groß eine Datei sein darf, den man hochladen kann.</small></p>
                     </div>
                     <div class="col-xs-12">
                         <div class="row">
@@ -198,7 +198,7 @@
                         </div>
                         <div class="row">
                             <div class="col-md-12">
-                                <p class="text-muted"><small>The daemon runs its own SFTP management container and does not use the SSHd process on the main physical server. <Strong>Do not use the same port that you have assigned for your physical server's SSH process.</strong></small></p>
+                                <p class="text-muted"><small>Der Daemon führt seinen eigenen SFTP-Verwaltungscontainer aus und verwendet nicht den SSHd-Prozess auf dem physischen Hauptserver. <Strong>Verwende nicht denselben Port, den du für den SSH-Prozess Ihres physischen Servers zugewiesen hast.</strong></small></p>
                             </div>
                         </div>
                     </div>
@@ -208,20 +208,20 @@
         <div class="col-xs-12">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Save Settings</h3>
+                    <h3 class="box-title">Änderungen speichern</h3>
                 </div>
                 <div class="box-body row">
                     <div class="form-group col-sm-6">
                         <div>
-                            <input type="checkbox" name="reset_secret" id="reset_secret" /> <label for="reset_secret" class="control-label">Reset Daemon Master Key</label>
+                            <input type="checkbox" name="reset_secret" id="reset_secret" /> <label for="reset_secret" class="control-label">Daemon Master Schlüssel zurücksetzen</label>
                         </div>
-                        <p class="text-muted"><small>Resetting the daemon master key will void any request coming from the old key. This key is used for all sensitive operations on the daemon including server creation and deletion. We suggest changing this key regularly for security.</small></p>
+                        <p class="text-muted"><small>Das Zurücksetzen des Daemon-Hauptschlüssels macht alle Anfragen ungültig, die vom alten Schlüssel stammen. Dieser Schlüssel wird für alle sensiblen Vorgänge auf dem Daemon verwendet, einschließlich Servererstellung und -löschung. Wir empfehlen, diesen Schlüssel aus Sicherheitsgründen regelmäßig zu ändern.</small></p>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! method_field('PATCH') !!}
                     {!! csrf_field() !!}
-                    <button type="submit" class="btn btn-primary pull-right">Save Changes</button>
+                    <button type="submit" class="btn btn-primary pull-right">Änderungen speichern</button>
                 </div>
             </div>
         </div>
diff --git a/resources/views/admin/servers/index.blade.php b/resources/views/admin/servers/index.blade.php
index 4d5ebcd..5c1e381 100644
--- a/resources/views/admin/servers/index.blade.php
+++ b/resources/views/admin/servers/index.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    List Servers
+    Alle Server
 @endsection
 
 @section('content-header')
-    <h1>Servers<small>All servers available on the system.</small></h1>
+    <h1>Server-Übersicht<small>Hier werden alle Server vom System angezeigt.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Servers</li>
+        <li class="active">Alle Server</li>
     </ol>
 @endsection
 
@@ -17,14 +17,14 @@
     <div class="col-xs-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Server List</h3>
+                <h3 class="box-title">Server-Liste</h3>
                 <div class="box-tools search01">
                     <form action="{{ route('admin.servers') }}" method="GET">
                         <div class="input-group input-group-sm">
-                            <input type="text" name="filter[*]" class="form-control pull-right" value="{{ request()->input()['filter']['*'] ?? '' }}" placeholder="Search Servers">
+                            <input type="text" name="filter[*]" class="form-control pull-right" value="{{ request()->input()['filter']['*'] ?? '' }}" placeholder="Server suchen">
                             <div class="input-group-btn">
                                 <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
-                                <a href="{{ route('admin.servers.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Create New</button></a>
+                                <a href="{{ route('admin.servers.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Server erstellen</button></a>
                             </div>
                         </div>
                     </form>
@@ -34,11 +34,11 @@
                 <table class="table table-hover">
                     <tbody>
                         <tr>
-                            <th>Server Name</th>
+                            <th>Projektname</th>
                             <th>UUID</th>
-                            <th>Owner</th>
+                            <th>Admin</th>
                             <th>Node</th>
-                            <th>Connection</th>
+                            <th>Zugang</th>
                             <th></th>
                             <th></th>
                         </tr>
@@ -53,11 +53,11 @@
                                 </td>
                                 <td class="text-center">
                                     @if($server->isSuspended())
-                                        <span class="label bg-maroon">Suspended</span>
+                                        <span class="label bg-maroon">Gesperrt</span>
                                     @elseif(! $server->isInstalled())
-                                        <span class="label label-warning">Installing</span>
+                                        <span class="label label-warning">Installiere...</span>
                                     @else
-                                        <span class="label label-success">Active</span>
+                                        <span class="label label-success">Aktiv</span>
                                     @endif
                                 </td>
                                 <td class="text-center">
diff --git a/resources/views/admin/servers/new.blade.php b/resources/views/admin/servers/new.blade.php
index 4198634..4e69add 100644
--- a/resources/views/admin/servers/new.blade.php
+++ b/resources/views/admin/servers/new.blade.php
@@ -1,15 +1,15 @@
 @extends('layouts.admin')
 
 @section('title')
-    New Server
+    Server erstellen
 @endsection
 
 @section('content-header')
-    <h1>Create Server<small>Add a new server to the panel.</small></h1>
+    <h1>Server erstellen<small>Erstelle einen neuen Server in Pterodactyl</small></h1>
     <ol class="breadcrumb">
-        <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
-        <li class="active">Create Server</li>
+        <li><a href="{{ route('admin.index') }}">Server-Übersicht</a></li>
+        <li><a href="{{ route('admin.servers') }}">Server-Verwaltung</a></li>
+        <li class="active">Server erstellen</li>
     </ol>
 @endsection
 
@@ -19,35 +19,35 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Core Details</h3>
+                    <h3 class="box-title">Kern-Details</h3>
                 </div>
 
                 <div class="box-body row">
                     <div class="col-md-6">
                         <div class="form-group">
-                            <label for="pName">Server Name</label>
+                            <label for="pName">Name des Servers</label>
                             <input type="text" class="form-control" id="pName" name="name" value="{{ old('name') }}" placeholder="Server Name">
-                            <p class="small text-muted no-margin">Character limits: <code>a-z A-Z 0-9 _ - .</code> and <code>[Space]</code>.</p>
+                            <p class="small text-muted no-margin">Darf genutzt werden: <code>a-z A-Z 0-9 _ - .</code> und <code>[Leerzeichen]</code>.</p>
                         </div>
 
                         <div class="form-group">
-                            <label for="pUserId">Server Owner</label>
+                            <label for="pUserId">Admin des Servers</label>
                             <select id="pUserId" name="owner_id" class="form-control" style="padding-left:0;"></select>
-                            <p class="small text-muted no-margin">Email address of the Server Owner.</p>
+                            <p class="small text-muted no-margin">E-Mail Adresse des Owners (Muss als Benutzer registriert sein).</p>
                         </div>
                     </div>
 
                     <div class="col-md-6">
                         <div class="form-group">
-                            <label for="pDescription" class="control-label">Server Description</label>
+                            <label for="pDescription" class="control-label">Kurzbeschreibung</label>
                             <textarea id="pDescription" name="description" rows="3" class="form-control">{{ old('description') }}</textarea>
-                            <p class="text-muted small">A brief description of this server.</p>
+                            <p class="text-muted small">Eine Beschreibung, was der Server genau ist.</p>
                         </div>
 
                         <div class="form-group">
                             <div class="checkbox checkbox-primary no-margin-bottom">
                                 <input id="pStartOnCreation" name="start_on_completion" type="checkbox" {{ \Pterodactyl\Helpers\Utilities::checked('start_on_completion', 1) }} />
-                                <label for="pStartOnCreation" class="strong">Start Server when Installed</label>
+                                <label for="pStartOnCreation" class="strong">Hochfahren, wenn der Server bereit ist</label>
                             </div>
                         </div>
                     </div>
@@ -61,7 +61,7 @@
             <div class="box">
                 <div class="overlay" id="allocationLoader" style="display:none;"><i class="fa fa-refresh fa-spin"></i></div>
                 <div class="box-header with-border">
-                    <h3 class="box-title">Allocation Management</h3>
+                    <h3 class="box-title">Portzuweisung</h3>
                 </div>
 
                 <div class="box-body row">
@@ -81,19 +81,19 @@
                             @endforeach
                         </select>
 
-                        <p class="small text-muted no-margin">The node which this server will be deployed to.</p>
+                        <p class="small text-muted no-margin">Der Node, worüber der Server betrieben wird.</p>
                     </div>
 
                     <div class="form-group col-sm-4">
-                        <label for="pAllocation">Default Allocation</label>
+                        <label for="pAllocation">Primärer Port</label>
                         <select id="pAllocation" name="allocation_id" class="form-control"></select>
-                        <p class="small text-muted no-margin">The main allocation that will be assigned to this server.</p>
+                        <p class="small text-muted no-margin">Der Port, der primär für diesen Server gelten soll.</p>
                     </div>
 
                     <div class="form-group col-sm-4">
-                        <label for="pAllocationAdditional">Additional Allocation(s)</label>
+                        <label for="pAllocationAdditional">Weitere Portreservierungen</label>
                         <select id="pAllocationAdditional" name="allocation_additional[]" class="form-control" multiple></select>
-                        <p class="small text-muted no-margin">Additional allocations to assign to this server on creation.</p>
+                        <p class="small text-muted no-margin">Alle Ports, die für diesen Server direkt mit reserviert werden sollen.</p>
                     </div>
                 </div>
             </div>
@@ -105,30 +105,30 @@
             <div class="box">
                 <div class="overlay" id="allocationLoader" style="display:none;"><i class="fa fa-refresh fa-spin"></i></div>
                 <div class="box-header with-border">
-                    <h3 class="box-title">Application Feature Limits</h3>
+                    <h3 class="box-title">Limits des Servers</h3>
                 </div>
 
                 <div class="box-body row">
                     <div class="form-group col-xs-6">
-                        <label for="pDatabaseLimit" class="control-label">Database Limit</label>
+                        <label for="pDatabaseLimit" class="control-label">Anzahl an Datenbanken</label>
                         <div>
                             <input type="text" id="pDatabaseLimit" name="database_limit" class="form-control" value="{{ old('database_limit', 0) }}"/>
                         </div>
-                        <p class="text-muted small">The total number of databases a user is allowed to create for this server.</p>
+                        <p class="text-muted small">Das Maximum an Datenbanken, was dieser Server betreiben darf.</p>
                     </div>
                     <div class="form-group col-xs-6">
-                        <label for="pAllocationLimit" class="control-label">Allocation Limit</label>
+                        <label for="pAllocationLimit" class="control-label">Anzahl an Portfreigaben</label>
                         <div>
                             <input type="text" id="pAllocationLimit" name="allocation_limit" class="form-control" value="{{ old('allocation_limit', 0) }}"/>
                         </div>
-                        <p class="text-muted small">The total number of allocations a user is allowed to create for this server.</p>
+                        <p class="text-muted small">Das Maximum an Ports, die zugewiesen werden dürfen.</p>
                     </div>
                     <div class="form-group col-xs-6">
-                        <label for="pBackupLimit" class="control-label">Backup Limit</label>
+                        <label for="pBackupLimit" class="control-label">Anzahl an Backups</label>
                         <div>
                             <input type="text" id="pBackupLimit" name="backup_limit" class="form-control" value="{{ old('backup_limit', 0) }}"/>
                         </div>
-                        <p class="text-muted small">The total number of backups that can be created for this server.</p>
+                        <p class="text-muted small">Das Maximum an Backups, die gleichzeitig bereit stehen düfen.</p>
                     </div>
                 </div>
             </div>
@@ -138,7 +138,7 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Resource Management</h3>
+                    <h3 class="box-title">Ressourcen für diesen Server</h3>
                 </div>
 
                 <div class="box-body row">
@@ -150,72 +150,72 @@
                             <span class="input-group-addon">%</span>
                         </div>
 
-                        <p class="text-muted small">If you do not want to limit CPU usage, set the value to <code>0</code>. To determine a value, take the number of threads and multiply it by 100. For example, on a quad core system without hyperthreading <code>(4 * 100 = 400)</code> there is <code>400%</code> available. To limit a server to using half of a single thread, you would set the value to <code>50</code>. To allow a server to use up to two threads, set the value to <code>200</code>.<p>
+                        <p class="text-muted small">Wenn du das Limit auf <code>0</code> einstellst, wird kein Limit festgelegt. 100% bedeutet 1 Kern, wenn du also 4 Kerne zuweisen möchtest, dann musst du <code>400%</code> angeben: <code>(4 * 100 = 400)</code>. Wenn du nur einen halben Kern zuweisen möchtest, dann gib <code>50</code> an. Du kannst auch ganze und halbe Kerne gleichzeitig angeben, 2 1/2 Kerne wären <code>250</code>.<p>
                     </div>
 
                     <div class="form-group col-xs-6">
-                        <label for="pThreads">CPU Pinning</label>
+                        <label for="pThreads">Direkte CPU-Kern Reservierung</label>
 
                         <div>
                             <input type="text" id="pThreads" name="threads" class="form-control" value="{{ old('threads') }}" />
                         </div>
 
-                        <p class="text-muted small"><strong>Advanced:</strong> Enter the specific CPU threads that this process can run on, or leave blank to allow all threads. This can be a single number, or a comma separated list. Example: <code>0</code>, <code>0-1,3</code>, or <code>0,1,3,4</code>.</p>
+                        <p class="text-muted small"><strong>Erweitert:</strong> Gib die spezifischen CPU-Threads ein, auf denen dieser Prozess ausgeführt werden kann, oder lasse das Feld leer, um alle Threads zuzulassen. Dies kann eine einzelne Zahl oder eine durch Kommas getrennte Liste sein. Beispiel: <code>0</code>, <code>0-1,3</code> oder <code>0,1,3,4</code>.</p>
                     </div>
                 </div>
 
                 <div class="box-body row">
                     <div class="form-group col-xs-6">
-                        <label for="pMemory">Memory</label>
+                        <label for="pMemory">Arbeitsspeicher</label>
 
                         <div class="input-group">
                             <input type="text" id="pMemory" name="memory" class="form-control" value="{{ old('memory') }}" />
                             <span class="input-group-addon">MiB</span>
                         </div>
 
-                        <p class="text-muted small">The maximum amount of memory allowed for this container. Setting this to <code>0</code> will allow unlimited memory in a container.</p>
+                        <p class="text-muted small">Die maximal zulässige RAM-Menge für diesen Container. Wenn du es auf <code>0</code> setzt, wird kein Limit festgelegt.</p>
                     </div>
 
                     <div class="form-group col-xs-6">
-                        <label for="pSwap">Swap</label>
+                        <label for="pSwap">Auslagerungsbereich / Swap</label>
 
                         <div class="input-group">
                             <input type="text" id="pSwap" name="swap" class="form-control" value="{{ old('swap', 0) }}" />
                             <span class="input-group-addon">MiB</span>
                         </div>
 
-                        <p class="text-muted small">Setting this to <code>0</code> will disable swap space on this server. Setting to <code>-1</code> will allow unlimited swap.</p>
+                        <p class="text-muted small">Wenn es auf <code>0</code> festgelegt ist, wird der Auslagerungsbereich auf diesem Server deaktiviert. Die Einstellung auf <code>-1</code> erlaubt unbegrenzten Austausch.</p>
                     </div>
                 </div>
 
                 <div class="box-body row">
                     <div class="form-group col-xs-6">
-                        <label for="pDisk">Disk Space</label>
+                        <label for="pDisk">Festplattenspeicher</label>
 
                         <div class="input-group">
                             <input type="text" id="pDisk" name="disk" class="form-control" value="{{ old('disk') }}" />
                             <span class="input-group-addon">MiB</span>
                         </div>
 
-                        <p class="text-muted small">This server will not be allowed to boot if it is using more than this amount of space. If a server goes over this limit while running it will be safely stopped and locked until enough space is available. Set to <code>0</code> to allow unlimited disk usage.</p>
+                        <p class="text-muted small">Dieser Server darf nicht gestartet werden, wenn er mehr als diese Menge an Speicherplatz verwendet. Wenn ein Server während des Betriebs dieses Limit überschreitet, wird er sicher gestoppt und gesperrt, bis genügend Speicherplatz verfügbar ist. Auf <code>0</code> setzen, um unbegrenzte Festplattennutzung zuzulassen.</p>
                     </div>
 
                     <div class="form-group col-xs-6">
-                        <label for="pIO">Block IO Weight</label>
+                        <label for="pIO">Block-IO Gewicht</label>
 
                         <div>
                             <input type="text" id="pIO" name="io" class="form-control" value="{{ old('io', 500) }}" />
                         </div>
 
-                        <p class="text-muted small"><strong>Advanced</strong>: The IO performance of this server relative to other <em>running</em> containers on the system. Value should be between <code>10</code> and <code>1000</code>. Please see <a href="https://docs.docker.com/engine/reference/run/#block-io-bandwidth-blkio-constraint" target="_blank">this documentation</a> for more information about it.</p>
+                        <p class="text-muted small"><strong>Erweitert</strong>: Die IO-Leistung dieses Servers im Vergleich zu anderen <em>ausgeführten</em> Containern auf dem System. Der Wert sollte zwischen <code>10</code> und <code>1000</code> liegen. Weitere Informationen findest du in <a href="https://docs.docker.com/engine/reference/run/#block-io-bandwidth-blkio-constraint" target="_blank">dieser Wiki-Page</a>.</p>
                     </div>
                     <div class="form-group col-xs-12">
                         <div class="checkbox checkbox-primary no-margin-bottom">
                             <input type="checkbox" id="pOomDisabled" name="oom_disabled" value="0" {{ \Pterodactyl\Helpers\Utilities::checked('oom_disabled', 0) }} />
-                            <label for="pOomDisabled" class="strong">Enable OOM Killer</label>
+                            <label for="pOomDisabled" class="strong">RAM-Killer</label>
                         </div>
 
-                        <p class="small text-muted no-margin">Terminates the server if it breaches the memory limits. Enabling OOM killer may cause server processes to exit unexpectedly.</p>
+                        <p class="small text-muted no-margin">Beendet den Server, wenn er die Speichergrenzen überschreitet. Das Aktivieren von OOM Killer kann dazu führen, dass Serverprozesse unerwartet beendet werden, Datenverlust ist hierbei möglich!</p>
                     </div>
                 </div>
             </div>
@@ -226,7 +226,7 @@
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Nest Configuration</h3>
+                    <h3 class="box-title">Nest - Einstellungen</h3>
                 </div>
 
                 <div class="box-body row">
@@ -243,21 +243,21 @@
                             @endforeach
                         </select>
 
-                        <p class="small text-muted no-margin">Select the Nest that this server will be grouped under.</p>
+                        <p class="small text-muted no-margin">Wähle den Nest, der die Grundlagen des Servers festlegen soll.</p>
                     </div>
 
                     <div class="form-group col-xs-12">
                         <label for="pEggId">Egg</label>
                         <select id="pEggId" name="egg_id" class="form-control"></select>
-                        <p class="small text-muted no-margin">Select the Egg that will define how this server should operate.</p>
+                        <p class="small text-muted no-margin">Wähle den Egg aus, den du nutzen möchtest. Bitte beachte, das wir die Erweiterungen vom Egg in der Startbefehl-Kategorie nicht übersetzen können. Es sei denn, du nutzt unsere Eggs.</p>
                     </div>
                     <div class="form-group col-xs-12">
                         <div class="checkbox checkbox-primary no-margin-bottom">
                             <input type="checkbox" id="pSkipScripting" name="skip_scripts" value="1" {{ \Pterodactyl\Helpers\Utilities::checked('skip_scripts', 0) }} />
-                            <label for="pSkipScripting" class="strong">Skip Egg Install Script</label>
+                            <label for="pSkipScripting" class="strong">Egg-Installation überpringen</label>
                         </div>
 
-                        <p class="small text-muted no-margin">If the selected Egg has an install script attached to it, the script will run during the install. If you would like to skip this step, check this box.</p>
+                        <p class="small text-muted no-margin">Wenn du diesen Abschnitt überspringen und/oder selbst machen möchtest, dann setze einen Häkchen.</p>
                     </div>
                 </div>
             </div>
@@ -266,15 +266,15 @@
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Docker Configuration</h3>
+                    <h3 class="box-title">Docker - Einstellungen</h3>
                 </div>
 
                 <div class="box-body row">
                     <div class="form-group col-xs-12">
                         <label for="pDefaultContainer">Docker Image</label>
                         <select id="pDefaultContainer" name="image" class="form-control"></select>
-                        <input id="pDefaultContainerCustom" name="custom_image" value="{{ old('custom_image') }}" class="form-control" placeholder="Or enter a custom image..." style="margin-top:1rem"/>
-                        <p class="small text-muted no-margin">This is the default Docker image that will be used to run this server. Select an image from the dropdown above, or enter a custom image in the text field above.</p>
+                        <input id="pDefaultContainerCustom" name="custom_image" value="{{ old('custom_image') }}" class="form-control" placeholder="Oder füge ein eigenes Image hinzu..." style="margin-top:1rem"/>
+                        <p class="small text-muted no-margin">Dies ist das Standard-Docker-Image, das zum Ausführen dieses Servers verwendet wird. Wähle ein Bild aus der Dropdown-Liste oben aus oder gebe ein benutzerdefiniertes Bild in das Textfeld oben ein.</p>
                     </div>
                 </div>
             </div>
@@ -285,26 +285,26 @@
         <div class="col-md-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Startup Configuration</h3>
+                    <h3 class="box-title">Startbefehl - Einstellungen</h3>
                 </div>
 
                 <div class="box-body row">
                     <div class="form-group col-xs-12">
-                        <label for="pStartup">Startup Command</label>
+                        <label for="pStartup">Der Befehl zum starten des Servers</label>
                         <input type="text" id="pStartup" name="startup" value="{{ old('startup') }}" class="form-control" />
-                        <p class="small text-muted no-margin">The following data substitutes are available for the startup command: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, and <code>@{{SERVER_PORT}}</code>. They will be replaced with the allocated memory, server IP, and server port respectively.</p>
+                        <p class="small text-muted no-margin">Die folgenden Variablen können in diesen Befehl integriert werden: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, und <code>@{{SERVER_PORT}}</code>. Sie werden automatisch mit den Einstellungen befüllt, die festgelegt wurden. Zukünfigte Änderungen an den Limits oder den Ports werden bei Nutzung automatisch verändert.</p>
                     </div>
                 </div>
 
                 <div class="box-header with-border" style="margin-top:-10px;">
-                    <h3 class="box-title">Service Variables</h3>
+                    <h3 class="box-title">Serversoftware - Einstellungen</h3>
                 </div>
 
                 <div class="box-body row" id="appendVariablesTo"></div>
 
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <input type="submit" class="btn btn-success pull-right" value="Create Server" />
+                    <input type="submit" class="btn btn-success pull-right" value="Server erstellen" />
                 </div>
             </div>
         </div>
diff --git a/resources/views/admin/servers/partials/navigation.blade.php b/resources/views/admin/servers/partials/navigation.blade.php
index 964eac8..1117b05 100644
--- a/resources/views/admin/servers/partials/navigation.blade.php
+++ b/resources/views/admin/servers/partials/navigation.blade.php
@@ -7,29 +7,29 @@
         <div class="nav-tabs-custom nav-tabs-floating">
             <ul class="nav nav-tabs">
                 <li class="{{ $router->currentRouteNamed('admin.servers.view') ? 'active' : '' }}">
-                    <a href="{{ route('admin.servers.view', $server->id) }}">About</a></li>
+                    <a href="{{ route('admin.servers.view', $server->id) }}">Über den Server</a></li>
                 @if($server->isInstalled())
                     <li class="{{ $router->currentRouteNamed('admin.servers.view.details') ? 'active' : '' }}">
-                        <a href="{{ route('admin.servers.view.details', $server->id) }}">Details</a>
+                        <a href="{{ route('admin.servers.view.details', $server->id) }}">Infos</a>
                     </li>
                     <li class="{{ $router->currentRouteNamed('admin.servers.view.build') ? 'active' : '' }}">
-                        <a href="{{ route('admin.servers.view.build', $server->id) }}">Build Configuration</a>
+                        <a href="{{ route('admin.servers.view.build', $server->id) }}">Einstellungen</a>
                     </li>
                     <li class="{{ $router->currentRouteNamed('admin.servers.view.startup') ? 'active' : '' }}">
-                        <a href="{{ route('admin.servers.view.startup', $server->id) }}">Startup</a>
+                        <a href="{{ route('admin.servers.view.startup', $server->id) }}">Startbefehl</a>
                     </li>
                     <li class="{{ $router->currentRouteNamed('admin.servers.view.database') ? 'active' : '' }}">
-                        <a href="{{ route('admin.servers.view.database', $server->id) }}">Database</a>
+                        <a href="{{ route('admin.servers.view.database', $server->id) }}">Datenbanken</a>
                     </li>
                     <li class="{{ $router->currentRouteNamed('admin.servers.view.mounts') ? 'active' : '' }}">
-                        <a href="{{ route('admin.servers.view.mounts', $server->id) }}">Mounts</a>
+                        <a href="{{ route('admin.servers.view.mounts', $server->id) }}">Integrationen</a>
                     </li>
                 @endif
                 <li class="{{ $router->currentRouteNamed('admin.servers.view.manage') ? 'active' : '' }}">
-                    <a href="{{ route('admin.servers.view.manage', $server->id) }}">Manage</a>
+                    <a href="{{ route('admin.servers.view.manage', $server->id) }}">Verwaltung</a>
                 </li>
                 <li class="tab-danger {{ $router->currentRouteNamed('admin.servers.view.delete') ? 'active' : '' }}">
-                    <a href="{{ route('admin.servers.view.delete', $server->id) }}">Delete</a>
+                    <a href="{{ route('admin.servers.view.delete', $server->id) }}">Löschen</a>
                 </li>
                 <li class="tab-success">
                     <a href="/server/{{ $server->uuidShort }}" target="_blank"><i class="fa fa-external-link"></i></a>
diff --git a/resources/views/admin/servers/view/build.blade.php b/resources/views/admin/servers/view/build.blade.php
index 655ea36..987d39a 100644
--- a/resources/views/admin/servers/view/build.blade.php
+++ b/resources/views/admin/servers/view/build.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    Server — {{ $server->name }}: Build Details
+    Server — {{ $server->name }}: Kontrollzentrum
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Control allocations and system resources for this server.</small></h1>
+    <h1>{{ $server->name }}<small>Verwalte hier die Ressourcen des Servers.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-        <li class="active">Build Configuration</li>
+        <li class="active">Kontrollzentrum</li>
     </ol>
 @endsection
 
@@ -21,7 +21,7 @@
         <div class="col-sm-5">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Resource Management</h3>
+                    <h3 class="box-title">Zugewiesene Ressourcen</h3>
                 </div>
                 <div class="box-body">
                 <div class="form-group">
@@ -30,59 +30,59 @@
                             <input type="text" name="cpu" class="form-control" value="{{ old('cpu', $server->cpu) }}"/>
                             <span class="input-group-addon">%</span>
                         </div>
-                        <p class="text-muted small">Each <em>virtual</em> core (thread) on the system is considered to be <code>100%</code>. Setting this value to <code>0</code> will allow a server to use CPU time without restrictions.</p>
+                        <p class="text-muted small">Jeder <em>virtuelle</em> ganze Kern bedeutet hier: <code>100%</code>. Setze es auf <code>0</code>, wenn du kein Limit festlegen möchtest.</p>
                     </div>
                     <div class="form-group">
-                        <label for="threads" class="control-label">CPU Pinning</label>
+                        <label for="threads" class="control-label">Direkte CPU-Kern Reservierung</label>
                         <div>
                             <input type="text" name="threads" class="form-control" value="{{ old('threads', $server->threads) }}"/>
                         </div>
-                        <p class="text-muted small"><strong>Advanced:</strong> Enter the specific CPU cores that this process can run on, or leave blank to allow all cores. This can be a single number, or a comma seperated list. Example: <code>0</code>, <code>0-1,3</code>, or <code>0,1,3,4</code>.</p>
+                        <p class="text-muted small"><strong>Erweitert:</strong> Gib die spezifischen CPU-Threads ein, auf denen dieser Prozess ausgeführt werden kann, oder lasse das Feld leer, um alle Threads zuzulassen. Dies kann eine einzelne Zahl oder eine durch Kommas getrennte Liste sein. Beispiel: <code>0</code>, <code>0-1,3</code>, or <code>0,1,3,4</code>.</p>
                     </div>
                     <div class="form-group">
-                        <label for="memory" class="control-label">Allocated Memory</label>
+                        <label for="memory" class="control-label">Arbeitsspeicher</label>
                         <div class="input-group">
                             <input type="text" name="memory" data-multiplicator="true" class="form-control" value="{{ old('memory', $server->memory) }}"/>
-                            <span class="input-group-addon">MiB</span>
+                            <span class="input-group-addon">MB</span>
                         </div>
-                        <p class="text-muted small">The maximum amount of memory allowed for this container. Setting this to <code>0</code> will allow unlimited memory in a container.</p>
+                        <p class="text-muted small">Die maximal zulässige RAM-Menge für diesen Container. Wenn du es auf <code>0</code> setzt, wird kein Limit festgelegt.</p>
                     </div>
                     <div class="form-group">
-                        <label for="swap" class="control-label">Allocated Swap</label>
+                        <label for="swap" class="control-label">Auslagerungsbereich / Swap</label>
                         <div class="input-group">
                             <input type="text" name="swap" data-multiplicator="true" class="form-control" value="{{ old('swap', $server->swap) }}"/>
-                            <span class="input-group-addon">MiB</span>
+                            <span class="input-group-addon">MB</span>
                         </div>
-                        <p class="text-muted small">Setting this to <code>0</code> will disable swap space on this server. Setting to <code>-1</code> will allow unlimited swap.</p>
+                        <p class="text-muted small">Setze es auf <code>0</code>, wenn du es nicht nutzen möchtest. Du kannst auch mit <code>-1</code> einen unbegrenzten Bereich festlegen.</p>
                     </div>
                     <div class="form-group">
-                        <label for="cpu" class="control-label">Disk Space Limit</label>
+                        <label for="cpu" class="control-label">Festplattenspeicher</label>
                         <div class="input-group">
                             <input type="text" name="disk" class="form-control" value="{{ old('disk', $server->disk) }}"/>
-                            <span class="input-group-addon">MiB</span>
+                            <span class="input-group-addon">MB</span>
                         </div>
-                        <p class="text-muted small">This server will not be allowed to boot if it is using more than this amount of space. If a server goes over this limit while running it will be safely stopped and locked until enough space is available. Set to <code>0</code> to allow unlimited disk usage.</p>
+                        <p class="text-muted small">Dieser Server darf nicht gestartet werden, wenn er mehr als diese Menge an Speicherplatz verwendet. Wenn ein Server während des Betriebs dieses Limit überschreitet, wird er sicher gestoppt und gesperrt, bis genügend Speicherplatz verfügbar ist. Auf <code>0</code> setzen, um unbegrenzte Festplattennutzung zuzulassen.</p>
                     </div>
                     <div class="form-group">
-                        <label for="io" class="control-label">Block IO Proportion</label>
+                        <label for="io" class="control-label">Block IO Gewicht</label>
                         <div>
                             <input type="text" name="io" class="form-control" value="{{ old('io', $server->io) }}"/>
                         </div>
-                        <p class="text-muted small"><strong>Advanced</strong>: The IO performance of this server relative to other <em>running</em> containers on the system. Value should be between <code>10</code> and <code>1000</code>.</code></p>
+                        <p class="text-muted small"><strong>Erweitert</strong>: Die IO-Leistung dieses Servers im Vergleich zu anderen <em>ausgeführten</em> Containern auf dem System. Der Wert sollte zwischen <code>10</code> und <code>1000</code> liegen.</code></p>
                     </div>
                     <div class="form-group">
-                        <label for="cpu" class="control-label">OOM Killer</label>
+                        <label for="cpu" class="control-label">RAM-Killer</label>
                         <div>
                             <div class="radio radio-danger radio-inline">
                                 <input type="radio" id="pOomKillerEnabled" value="0" name="oom_disabled" @if(!$server->oom_disabled)checked @endif>
-                                <label for="pOomKillerEnabled">Enabled</label>
+                                <label for="pOomKillerEnabled">An</label>
                             </div>
                             <div class="radio radio-success radio-inline">
                                 <input type="radio" id="pOomKillerDisabled" value="1" name="oom_disabled" @if($server->oom_disabled)checked @endif>
-                                <label for="pOomKillerDisabled">Disabled</label>
+                                <label for="pOomKillerDisabled">Aus</label>
                             </div>
                             <p class="text-muted small">
-                                Enabling OOM killer may cause server processes to exit unexpectedly.
+                                Beendet den Server, wenn er die Speichergrenzen überschreitet. Das Aktivieren von OOM Killer kann dazu führen, dass Serverprozesse unerwartet beendet werden, Datenverlust ist hierbei möglich!
                             </p>
                         </div>
                     </div>
@@ -94,30 +94,30 @@
                 <div class="col-xs-12">
                     <div class="box">
                         <div class="box-header with-border">
-                            <h3 class="box-title">Application Feature Limits</h3>
+                            <h3 class="box-title">Weitere Limits</h3>
                         </div>
                         <div class="box-body">
                             <div class="row">
                                 <div class="form-group col-xs-6">
-                                    <label for="database_limit" class="control-label">Database Limit</label>
+                                    <label for="database_limit" class="control-label">Maximum an Datenbanken</label>
                                     <div>
                                         <input type="text" name="database_limit" class="form-control" value="{{ old('database_limit', $server->database_limit) }}"/>
                                     </div>
-                                    <p class="text-muted small">The total number of databases a user is allowed to create for this server.</p>
+                                    <p class="text-muted small">Die maximale Anzahl an Datenbanken, die der Server gleichzeitig betreiben darf.</p>
                                 </div>
                                 <div class="form-group col-xs-6">
-                                    <label for="allocation_limit" class="control-label">Allocation Limit</label>
+                                    <label for="allocation_limit" class="control-label">Maximum an Portfreigaben</label>
                                     <div>
                                         <input type="text" name="allocation_limit" class="form-control" value="{{ old('allocation_limit', $server->allocation_limit) }}"/>
                                     </div>
-                                    <p class="text-muted small">The total number of allocations a user is allowed to create for this server.</p>
+                                    <p class="text-muted small">Die maximale Anzahl an Portfreigaben, die der Server maximal haben darf.</p>
                                 </div>
                                 <div class="form-group col-xs-6">
-                                    <label for="backup_limit" class="control-label">Backup Limit</label>
+                                    <label for="backup_limit" class="control-label">Maximum an Backups</label>
                                     <div>
                                         <input type="text" name="backup_limit" class="form-control" value="{{ old('backup_limit', $server->backup_limit) }}"/>
                                     </div>
-                                    <p class="text-muted small">The total number of backups that can be created for this server.</p>
+                                    <p class="text-muted small">Die maximale Anzahl an Backups, die gleichzeitig zur Verfügung stehen dürfen.</p>
                                 </div>
                             </div>
                         </div>
@@ -126,11 +126,11 @@
                 <div class="col-xs-12">
                     <div class="box">
                         <div class="box-header with-border">
-                            <h3 class="box-title">Allocation Management</h3>
+                            <h3 class="box-title">Portzuteilung - Einstellungen</h3>
                         </div>
                         <div class="box-body">
                             <div class="form-group">
-                                <label for="pAllocation" class="control-label">Game Port</label>
+                                <label for="pAllocation" class="control-label">Primärer Port</label>
                                 <select id="pAllocation" name="allocation_id" class="form-control">
                                     @foreach ($assigned as $assignment)
                                         <option value="{{ $assignment->id }}"
@@ -140,10 +140,10 @@
                                         >{{ $assignment->alias }}:{{ $assignment->port }}</option>
                                     @endforeach
                                 </select>
-                                <p class="text-muted small">The default connection address that will be used for this game server.</p>
+                                <p class="text-muted small">Das ist der primäre Port für diesen Server.</p>
                             </div>
                             <div class="form-group">
-                                <label for="pAddAllocations" class="control-label">Assign Additional Ports</label>
+                                <label for="pAddAllocations" class="control-label">Ports zuweisen</label>
                                 <div>
                                     <select name="add_allocations[]" class="form-control" multiple id="pAddAllocations">
                                         @foreach ($unassigned as $assignment)
@@ -151,10 +151,10 @@
                                         @endforeach
                                     </select>
                                 </div>
-                                <p class="text-muted small">Please note that due to software limitations you cannot assign identical ports on different IPs to the same server.</p>
+                                <p class="text-muted small">Du kannst jeden Port nur einmal verwenden!</p>
                             </div>
                             <div class="form-group">
-                                <label for="pRemoveAllocations" class="control-label">Remove Additional Ports</label>
+                                <label for="pRemoveAllocations" class="control-label">Ports entfernen</label>
                                 <div>
                                     <select name="remove_allocations[]" class="form-control" multiple id="pRemoveAllocations">
                                         @foreach ($assigned as $assignment)
@@ -162,12 +162,12 @@
                                         @endforeach
                                     </select>
                                 </div>
-                                <p class="text-muted small">Simply select which ports you would like to remove from the list above. If you want to assign a port on a different IP that is already in use you can select it from the left and delete it here.</p>
+                                <p class="text-muted small">Hier kannst du Ports wieder für andere Server freigeben.</p>
                             </div>
                         </div>
                         <div class="box-footer">
                             {!! csrf_field() !!}
-                            <button type="submit" class="btn btn-primary pull-right">Update Build Configuration</button>
+                            <button type="submit" class="btn btn-primary pull-right">Änderungen speichern</button>
                         </div>
                     </div>
                 </div>
diff --git a/resources/views/admin/servers/view/database.blade.php b/resources/views/admin/servers/view/database.blade.php
index 385ce0f..b3e9c09 100644
--- a/resources/views/admin/servers/view/database.blade.php
+++ b/resources/views/admin/servers/view/database.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    Server — {{ $server->name }}: Databases
+    Server — {{ $server->name }}: Datenbanken
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Manage server databases.</small></h1>
+    <h1>{{ $server->name }}<small>Verwalte die Datenbanken des Servers.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-        <li class="active">Databases</li>
+        <li class="active">Datenbanken</li>
     </ol>
 @endsection
 
@@ -19,20 +19,20 @@
 <div class="row">
     <div class="col-sm-7">
         <div class="alert alert-info">
-            Database passwords can be viewed when <a href="/server/{{ $server->uuidShort }}/databases">visiting this server</a> on the front-end.
+            Die Passwörter der Datenbanken können <a href="/server/{{ $server->uuidShort }}/databases">im Server-Panel</a> eingesehen werden.
         </div>
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Active Databases</h3>
+                <h3 class="box-title">Aktive Datenbanken</h3>
             </div>
             <div class="box-body table-responsible no-padding">
                 <table class="table table-hover">
                     <tr>
-                        <th>Database</th>
-                        <th>Username</th>
-                        <th>Connections From</th>
+                        <th>Datenbank</th>
+                        <th>Benutzername</th>
+                        <th>Verbindungen von</th>
                         <th>Host</th>
-                        <th>Max Connections</th>
+                        <th>Max. Verbindungen</th>
                         <th></th>
                     </tr>
                     @foreach($server->databases as $database)
@@ -44,7 +44,7 @@
                             @if($database->max_connections != null)
                                 <td>{{ $database->max_connections }}</td>
                             @else
-                                <td>Unlimited</td>
+                                <td>Kein Limit</td>
                             @endif
                             <td class="text-center">
                                 <button data-action="reset-password" data-id="{{ $database->id }}" class="btn btn-xs btn-primary"><i class="fa fa-refresh"></i></button>
@@ -59,41 +59,41 @@
     <div class="col-sm-5">
         <div class="box box-success">
             <div class="box-header with-border">
-                <h3 class="box-title">Create New Database</h3>
+                <h3 class="box-title">Datenbank erstellen</h3>
             </div>
             <form action="{{ route('admin.servers.view.database', $server->id) }}" method="POST">
                 <div class="box-body">
                     <div class="form-group">
-                        <label for="pDatabaseHostId" class="control-label">Database Host</label>
+                        <label for="pDatabaseHostId" class="control-label">Datenbank Host</label>
                         <select id="pDatabaseHostId" name="database_host_id" class="form-control">
                             @foreach($hosts as $host)
                                 <option value="{{ $host->id }}">{{ $host->name }}</option>
                             @endforeach
                         </select>
-                        <p class="text-muted small">Select the host database server that this database should be created on.</p>
+                        <p class="text-muted small">Wähle den Datenbank Host aus, der für das erstellen von Datenbanken für diesen Server verwendet werden soll.</p>
                     </div>
                     <div class="form-group">
-                        <label for="pDatabaseName" class="control-label">Database</label>
+                        <label for="pDatabaseName" class="control-label">Datenbank</label>
                         <div class="input-group">
                             <span class="input-group-addon">s{{ $server->id }}_</span>
-                            <input id="pDatabaseName" type="text" name="database" class="form-control" placeholder="database" />
+                            <input id="pDatabaseName" type="text" name="database" class="form-control" placeholder="user_database" />
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="pRemote" class="control-label">Connections</label>
+                        <label for="pRemote" class="control-label">Verbindungen</label>
                         <input id="pRemote" type="text" name="remote" class="form-control" value="%" />
-                        <p class="text-muted small">This should reflect the IP address that connections are allowed from. Uses standard MySQL notation. If unsure leave as <code>%</code>.</p>
+                        <p class="text-muted small">Dies sollte die IP-Adresse widerspiegeln, von der aus Verbindungen erlaubt sind. Verwendet die Standard-MySQL-Notation. Wenn du dir nicht sicher bist, lasse es auf <code>%</code>.</p>
                     </div>
                     <div class="form-group">
-                        <label for="pmax_connections" class="control-label">Concurrent Connections</label>
+                        <label for="pmax_connections" class="control-label">Gleichzeitige Verbindungen</label>
                         <input id="pmax_connections" type="text" name="max_connections" class="form-control"/>
-                        <p class="text-muted small">This should reflect the max number of concurrent connections from this user to the database. Leave empty for unlimited.</p>
+                        <p class="text-muted small">Hier kannst du entscheiden, wie viele aktuelle Sitzungen gleichzeitig in dieser Datenbank laufen dürfen. Wenn du kein Limit setzen möchtest, lasse es leer.</p>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <p class="text-muted small no-margin">A username and password for this database will be randomly generated after form submission.</p>
-                    <input type="submit" class="btn btn-sm btn-success pull-right" value="Create Database" />
+                    <p class="text-muted small no-margin">Die Zugangsdaten werden zufällig erstellt und orientiert sich nicht an Merkmalen des Servers.</p>
+                    <input type="submit" class="btn btn-sm btn-success pull-right" value="Datenbank erstellen" />
                 </div>
             </form>
         </div>
@@ -111,9 +111,10 @@
         swal({
             title: '',
             type: 'warning',
-            text: 'Are you sure that you want to delete this database? There is no going back, all data will immediately be removed.',
+            text: 'Möchtest du wirklich die Datenbank entfernen? Dieser Vorgang kann nicht rückgängig gemacht werden.',
             showCancelButton: true,
-            confirmButtonText: 'Delete',
+            cancelButtonText: 'Abbrechen',
+            confirmButtonText: 'Ja, entfernen',
             confirmButtonColor: '#d9534f',
             closeOnConfirm: false,
             showLoaderOnConfirm: true,
@@ -129,8 +130,8 @@
                 console.error(jqXHR);
                 swal({
                     type: 'error',
-                    title: 'Whoops!',
-                    text: (typeof jqXHR.responseJSON.error !== 'undefined') ? jqXHR.responseJSON.error : 'An error occurred while processing this request.'
+                    title: 'Hups!',
+                    text: (typeof jqXHR.responseJSON.error !== 'undefined') ? jqXHR.responseJSON.error : 'Beim verarbeiten der Aufgabe ist ein Fehler aufgetreten.'
                 });
             });
         });
@@ -148,17 +149,17 @@
             swal({
                 type: 'success',
                 title: '',
-                text: 'The password for this database has been reset.',
+                text: 'Das Passwort für die Datenbank wurde ersetzt.',
             });
         }).fail(function(jqXHR, textStatus, errorThrown) {
             console.error(jqXHR);
-            var error = 'An error occurred while trying to process this request.';
+            var error = 'Beim verarbeiten der Aufgabe ist ein Fehler aufgetreten.';
             if (typeof jqXHR.responseJSON !== 'undefined' && typeof jqXHR.responseJSON.error !== 'undefined') {
                 error = jqXHR.responseJSON.error;
             }
             swal({
                 type: 'error',
-                title: 'Whoops!',
+                title: 'Hups!',
                 text: error
             });
         }).always(function () {
diff --git a/resources/views/admin/servers/view/delete.blade.php b/resources/views/admin/servers/view/delete.blade.php
index c237096..ac05373 100644
--- a/resources/views/admin/servers/view/delete.blade.php
+++ b/resources/views/admin/servers/view/delete.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    Server — {{ $server->name }}: Delete
+    Server — {{ $server->name }}: Löschen
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Delete this server from the panel.</small></h1>
+    <h1>{{ $server->name }}<small>Lösche diesen Server vom System.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-        <li class="active">Delete</li>
+        <li class="active">Server löschen</li>
     </ol>
 @endsection
 
@@ -20,16 +20,16 @@
     <div class="col-md-6">
         <div class="box">
             <div class="box-header with-border">
-                <h3 class="box-title">Safely Delete Server</h3>
+                <h3 class="box-title">Server sicher löschen</h3>
             </div>
             <div class="box-body">
-                <p>This action will attempt to delete the server from both the panel and daemon. If either one reports an error the action will be cancelled.</p>
-                <p class="text-danger small">Deleting a server is an irreversible action. <strong>All server data</strong> (including files and users) will be removed from the system.</p>
+                <p>Dies löscht den Server vollständig vom System, auch Backups werden entfernt. Sollten Probleme auftreten, wird der Vorgang abgebrochen.</p>
+                <p class="text-danger small">Das löschen des Servers kann nicht rückgängig gemacht werden. Bedenke, das <strong>Alle Serverdaten</strong> gelöscht werden.</p>
             </div>
             <div class="box-footer">
                 <form id="deleteform" action="{{ route('admin.servers.view.delete', $server->id) }}" method="POST">
                     {!! csrf_field() !!}
-                    <button id="deletebtn" class="btn btn-danger">Safely Delete This Server</button>
+                    <button id="deletebtn" class="btn btn-danger">Server sicher löschen</button>
                 </form>
             </div>
         </div>
@@ -37,17 +37,17 @@
     <div class="col-md-6">
         <div class="box box-danger">
             <div class="box-header with-border">
-                <h3 class="box-title">Force Delete Server</h3>
+                <h3 class="box-title">Server unsicher löschen</h3>
             </div>
             <div class="box-body">
-                <p>This action will attempt to delete the server from both the panel and daemon. If the daemon does not respond, or reports an error the deletion will continue.</p>
-                <p class="text-danger small">Deleting a server is an irreversible action. <strong>All server data</strong> (including files and users) will be removed from the system. This method may leave dangling files on your daemon if it reports an error.</p>
+                <p>Dies löscht den Server vollständig vom System, auch Backups werden entfernt. Sollten Probleme auftreten, wird der Vorgang nicht abgebrochen.</p>
+                <p class="text-danger small">Das löschen des Servers kann nicht rückgängig gemacht werden. Bedenke, das <strong>Alle Serverdaten</strong> gelöscht werden. Diese Methode kann das System schädigen, wenn viele Verknüpfungen mit dem Server vorhanden und nicht entfernt worden sind.</p>
             </div>
             <div class="box-footer">
                 <form id="forcedeleteform" action="{{ route('admin.servers.view.delete', $server->id) }}" method="POST">
                     {!! csrf_field() !!}
                     <input type="hidden" name="force_delete" value="1" />
-                    <button id="forcedeletebtn"" class="btn btn-danger">Forcibly Delete This Server</button>
+                    <button id="forcedeletebtn" class="btn btn-danger">Server unsicher löschen</button>
                 </form>
             </div>
         </div>
@@ -63,9 +63,10 @@
         swal({
             title: '',
             type: 'warning',
-            text: 'Are you sure that you want to delete this server? There is no going back, all data will immediately be removed.',
+            text: 'Sind Sie sicher, dass Sie diesen Server löschen wollen? Es gibt kein Zurück mehr, alle Daten werden sofort gelöscht.',
             showCancelButton: true,
-            confirmButtonText: 'Delete',
+            cancelButtonText: 'Abbrechen',
+            confirmButtonText: 'Löschen',
             confirmButtonColor: '#d9534f',
             closeOnConfirm: false
         }, function () {
@@ -78,9 +79,10 @@
         swal({
             title: '',
             type: 'warning',
-            text: 'Are you sure that you want to delete this server? There is no going back, all data will immediately be removed.',
+            text: 'Sind Sie sicher, dass Sie diesen Server löschen wollen? Es gibt kein Zurück mehr, alle Daten werden sofort gelöscht.',
             showCancelButton: true,
-            confirmButtonText: 'Delete',
+            cancelButtonText: 'Abbrechen',
+            confirmButtonText: 'Löschen',
             confirmButtonColor: '#d9534f',
             closeOnConfirm: false
         }, function () {
diff --git a/resources/views/admin/servers/view/details.blade.php b/resources/views/admin/servers/view/details.blade.php
index 9b4464b..6aef80d 100644
--- a/resources/views/admin/servers/view/details.blade.php
+++ b/resources/views/admin/servers/view/details.blade.php
@@ -5,10 +5,10 @@
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Edit details for this server including owner and container.</small></h1>
+    <h1>{{ $server->name }}<small>Details zu diesem Server bearbeiten, einschließlich Eigentümer und Container.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}"> Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
         <li class="active">Details</li>
     </ol>
@@ -20,37 +20,37 @@
     <div class="col-xs-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">Base Information</h3>
+                <h3 class="box-title">Allgemeines zum Server</h3>
             </div>
             <form action="{{ route('admin.servers.view.details', $server->id) }}" method="POST">
                 <div class="box-body">
                     <div class="form-group">
                         <label for="name" class="control-label">Server Name <span class="field-required"></span></label>
                         <input type="text" name="name" value="{{ old('name', $server->name) }}" class="form-control" />
-                        <p class="text-muted small">Character limits: <code>a-zA-Z0-9_-</code> and <code>[Space]</code>.</p>
+                        <p class="text-muted small">Darf verwendet werden: <code>a-zA-Z0-9_-</code> und <code>[Leerzeichen]</code>.</p>
                     </div>
                     <div class="form-group">
-                        <label for="external_id" class="control-label">External Identifier</label>
+                        <label for="external_id" class="control-label">Externe Kennung</label>
                         <input type="text" name="external_id" value="{{ old('external_id', $server->external_id) }}" class="form-control" />
-                        <p class="text-muted small">Leave empty to not assign an external identifier for this server. The external ID should be unique to this server and not be in use by any other servers.</p>
+                        <p class="text-muted small">Lasse dieses Feld leer, um diesem Server keine externe Kennung zuzuweisen. Die externe ID sollte für diesen Server eindeutig sein und nicht von anderen Servern verwendet werden.</p>
                     </div>
                     <div class="form-group">
-                        <label for="pUserId" class="control-label">Server Owner <span class="field-required"></span></label>
+                        <label for="pUserId" class="control-label">Inhaber des Servers <span class="field-required"></span></label>
                         <select name="owner_id" class="form-control" id="pUserId">
                             <option value="{{ $server->owner_id }}" selected>{{ $server->user->email }}</option>
                         </select>
-                        <p class="text-muted small">You can change the owner of this server by changing this field to an email matching another use on this system. If you do this a new daemon security token will be generated automatically.</p>
+                        <p class="text-muted small">Du kannst hier den Inhaber des Servers ändern Die Voraussetung ist, das der neue Benutzer bereits registriert ist.</p>
                     </div>
                     <div class="form-group">
-                        <label for="description" class="control-label">Server Description</label>
+                        <label for="description" class="control-label">Beschreibung des Servers</label>
                         <textarea name="description" rows="3" class="form-control">{{ old('description', $server->description) }}</textarea>
-                        <p class="text-muted small">A brief description of this server.</p>
+                        <p class="text-muted small">Eine Beschreibung, was genau dieser Server ist.</p>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
                     {!! method_field('PATCH') !!}
-                    <input type="submit" class="btn btn-sm btn-primary" value="Update Details" />
+                    <input type="submit" class="btn btn-sm btn-primary" value="Änderungen speichern" />
                 </div>
             </form>
         </div>
diff --git a/resources/views/admin/servers/view/index.blade.php b/resources/views/admin/servers/view/index.blade.php
index f94c6d4..a13deb4 100644
--- a/resources/views/admin/servers/view/index.blade.php
+++ b/resources/views/admin/servers/view/index.blade.php
@@ -8,7 +8,7 @@
     <h1>{{ $server->name }}<small>{{ str_limit($server->description) }}</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li class="active">{{ $server->name }}</li>
     </ol>
 @endsection
@@ -21,18 +21,18 @@
             <div class="col-xs-12">
                 <div class="box box-primary">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Information</h3>
+                        <h3 class="box-title">Details des Servers</h3>
                     </div>
                     <div class="box-body table-responsive no-padding">
                         <table class="table table-hover">
                             <tr>
-                                <td>Internal Identifier</td>
+                                <td>Interne Kennung</td>
                                 <td><code>{{ $server->id }}</code></td>
                             </tr>
                             <tr>
-                                <td>External Identifier</td>
+                                <td>Externe Kennung</td>
                                 @if(is_null($server->external_id))
-                                    <td><span class="label label-default">Not Set</span></td>
+                                    <td><span class="label label-default">Nicht festgelegt</span></td>
                                 @else
                                     <td><code>{{ $server->external_id }}</code></td>
                                 @endif
@@ -42,7 +42,7 @@
                                 <td><code>{{ $server->uuid }}</code></td>
                             </tr>
                             <tr>
-                                <td>Current Egg</td>
+                                <td>Aktuell genutztes Egg</td>
                                 <td>
                                     <a href="{{ route('admin.nests.view', $server->nest_id) }}">{{ $server->nest->name }}</a> ::
                                     <a href="{{ route('admin.nests.egg.view', $server->egg_id) }}">{{ $server->egg->name }}</a>
@@ -56,65 +56,65 @@
                                 <td>CPU Limit</td>
                                 <td>
                                     @if($server->cpu === 0)
-                                        <code>Unlimited</code>
+                                        <code>Unbegrenzt</code>
                                     @else
                                         <code>{{ $server->cpu }}%</code>
                                     @endif
                                 </td>
                             </tr>
                             <tr>
-                                <td>CPU Pinning</td>
+                                <td>CPU-Kernreservierung</td>
                                 <td>
                                     @if($server->threads != null)
                                         <code>{{ $server->threads }}</code>
                                     @else
-                                        <span class="label label-default">Not Set</span>
+                                        <span class="label label-default">Nicht festgelegt</span>
                                     @endif
                                 </td>
                             </tr>
                             <tr>
-                                <td>Memory</td>
+                                <td>Arbeitsspeicher</td>
                                 <td>
                                     @if($server->memory === 0)
-                                        <code>Unlimited</code>
+                                        <code>Kein Limit festgelegt</code>
                                     @else
-                                        <code>{{ $server->memory }}MiB</code>
+                                        <code>{{ $server->memory }}MB</code>
                                     @endif
                                     /
                                     @if($server->swap === 0)
-                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Not Set</code>
+                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Nicht festgelegt</code>
                                     @elseif($server->swap === -1)
-                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Unlimited</code>
+                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space">Kein Limit</code>
                                     @else
-                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space"> {{ $server->swap }}MiB</code>
+                                        <code data-toggle="tooltip" data-placement="top" title="Swap Space"> {{ $server->swap }}MB</code>
                                     @endif
                                 </td>
                             </tr>
                             <tr>
-                                <td>Disk Space</td>
+                                <td>Festplattenspeicher</td>
                                 <td>
                                     @if($server->disk === 0)
-                                        <code>Unlimited</code>
+                                        <code>Kein Limit festgelegt</code>
                                     @else
-                                        <code>{{ $server->disk }}MiB</code>
+                                        <code>{{ $server->disk }}MB</code>
                                     @endif
                                 </td>
                             </tr>
                             <tr>
-                                <td>Block IO Weight</td>
+                                <td>Block IO-Gewicht</td>
                                 <td><code>{{ $server->io }}</code></td>
                             </tr>
                             <tr>
-                                <td>Default Connection</td>
+                                <td>Primärer Port</td>
                                 <td><code>{{ $server->allocation->ip }}:{{ $server->allocation->port }}</code></td>
                             </tr>
                             <tr>
-                                <td>Connection Alias</td>
+                                <td>Alias des Ports</td>
                                 <td>
                                     @if($server->allocation->alias !== $server->allocation->ip)
                                         <code>{{ $server->allocation->alias }}:{{ $server->allocation->port }}</code>
                                     @else
-                                        <span class="label label-default">No Alias Assigned</span>
+                                        <span class="label label-default">Nicht definiert</span>
                                     @endif
                                 </td>
                             </tr>
@@ -132,7 +132,7 @@
                         <div class="col-sm-12">
                             <div class="small-box bg-yellow">
                                 <div class="inner">
-                                    <h3 class="no-margin">Suspended</h3>
+                                    <h3 class="no-margin">Gesperrt</h3>
                                 </div>
                             </div>
                         </div>
@@ -141,7 +141,7 @@
                         <div class="col-sm-12">
                             <div class="small-box {{ (! $server->isInstalled()) ? 'bg-blue' : 'bg-maroon' }}">
                                 <div class="inner">
-                                    <h3 class="no-margin">{{ (! $server->isInstalled()) ? 'Installing' : 'Install Failed' }}</h3>
+                                    <h3 class="no-margin">{{ (! $server->isInstalled()) ? 'Wird installiert...' : 'Installation fehlgeschlagen' }}</h3>
                                 </div>
                             </div>
                         </div>
@@ -150,11 +150,11 @@
                         <div class="small-box bg-gray">
                             <div class="inner">
                                 <h3>{{ str_limit($server->user->username, 16) }}</h3>
-                                <p>Server Owner</p>
+                                <p>Inhaber des Servers</p>
                             </div>
                             <div class="icon"><i class="fa fa-user"></i></div>
                             <a href="{{ route('admin.users.view', $server->user->id) }}" class="small-box-footer">
-                                More info <i class="fa fa-arrow-circle-right"></i>
+                                Zum Profil <i class="fa fa-arrow-circle-right"></i>
                             </a>
                         </div>
                     </div>
@@ -162,11 +162,11 @@
                         <div class="small-box bg-gray">
                             <div class="inner">
                                 <h3>{{ str_limit($server->node->name, 16) }}</h3>
-                                <p>Server Node</p>
+                                <p>Zugeteilte Node</p>
                             </div>
                             <div class="icon"><i class="fa fa-codepen"></i></div>
                             <a href="{{ route('admin.nodes.view', $server->node->id) }}" class="small-box-footer">
-                                More info <i class="fa fa-arrow-circle-right"></i>
+                                Node-Einstellungen <i class="fa fa-arrow-circle-right"></i>
                             </a>
                         </div>
                     </div>
diff --git a/resources/views/admin/servers/view/manage.blade.php b/resources/views/admin/servers/view/manage.blade.php
index e6177a4..a9b3741 100644
--- a/resources/views/admin/servers/view/manage.blade.php
+++ b/resources/views/admin/servers/view/manage.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    Server — {{ $server->name }}: Manage
+    Server — {{ $server->name }}: Verwaltung
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Additional actions to control this server.</small></h1>
+    <h1>{{ $server->name }}<small>Kontrollcenter für diesen Server.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-        <li class="active">Manage</li>
+        <li class="active">Verwaltung</li>
     </ol>
 @endsection
 
@@ -20,19 +20,19 @@
         <div class="col-sm-4">
             <div class="box box-danger">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Reinstall Server</h3>
+                    <h3 class="box-title">Server neu aufsetzen</h3>
                 </div>
                 <div class="box-body">
-                    <p>This will reinstall the server with the assigned service scripts. <strong>Danger!</strong> This could overwrite server data.</p>
+                    <p>Das wird den Server neu aufsetzen <strong>ACHTUNG!</strong> Dies kann zum Datenverlust führen, mach bitte vorher ein Backup.</p>
                 </div>
                 <div class="box-footer">
                     @if($server->isInstalled())
                         <form action="{{ route('admin.servers.view.manage.reinstall', $server->id) }}" method="POST">
                             {!! csrf_field() !!}
-                            <button type="submit" class="btn btn-danger">Reinstall Server</button>
+                            <button type="submit" class="btn btn-danger">Jetzt neu aufsetzen</button>
                         </form>
                     @else
-                        <button class="btn btn-danger disabled">Server Must Install Properly to Reinstall</button>
+                        <button class="btn btn-danger disabled">Aktuell nicht möglich</button>
                     @endif
                 </div>
             </div>
@@ -40,15 +40,15 @@
         <div class="col-sm-4">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Install Status</h3>
+                    <h3 class="box-title">Installations-Status</h3>
                 </div>
                 <div class="box-body">
-                    <p>If you need to change the install status from uninstalled to installed, or vice versa, you may do so with the button below.</p>
+                    <p>Wenn du gerade etwas am Server veränderst (Plugins updaten oder Ressourcen verändern etc.), kannst du den Installations-Status nutzen. Damit wird der Zugriff zum Server für die Benutzer des Servers gesperrt.</p>
                 </div>
                 <div class="box-footer">
                     <form action="{{ route('admin.servers.view.manage.toggle', $server->id) }}" method="POST">
                         {!! csrf_field() !!}
-                        <button type="submit" class="btn btn-primary">Toggle Install Status</button>
+                        <button type="submit" class="btn btn-primary">Status umschalten</button>
                     </form>
                 </div>
             </div>
@@ -58,16 +58,16 @@
             <div class="col-sm-4">
                 <div class="box box-warning">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Suspend Server</h3>
+                        <h3 class="box-title">Server sperren</h3>
                     </div>
                     <div class="box-body">
-                        <p>This will suspend the server, stop any running processes, and immediately block the user from being able to access their files or otherwise manage the server through the panel or API.</p>
+                        <p>Das sperrt den Server vollständig und fährt ihn sicher herunter. Die Benutzer haben in der Zeit dann keinen Zugang mehr</p>
                     </div>
                     <div class="box-footer">
                         <form action="{{ route('admin.servers.view.manage.suspension', $server->id) }}" method="POST">
                             {!! csrf_field() !!}
                             <input type="hidden" name="action" value="suspend" />
-                            <button type="submit" class="btn btn-warning @if(! is_null($server->transfer)) disabled @endif">Suspend Server</button>
+                            <button type="submit" class="btn btn-warning @if(! is_null($server->transfer)) disabled @endif">Server sperren</button>
                         </form>
                     </div>
                 </div>
@@ -76,16 +76,16 @@
             <div class="col-sm-4">
                 <div class="box box-success">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Unsuspend Server</h3>
+                        <h3 class="box-title">Sperre aufheben</h3>
                     </div>
                     <div class="box-body">
-                        <p>This will unsuspend the server and restore normal user access.</p>
+                        <p>Hebt die aktive Sperre auf und gibt den Zugang für die Benutzer wieder frei.</p>
                     </div>
                     <div class="box-footer">
                         <form action="{{ route('admin.servers.view.manage.suspension', $server->id) }}" method="POST">
                             {!! csrf_field() !!}
                             <input type="hidden" name="action" value="unsuspend" />
-                            <button type="submit" class="btn btn-success">Unsuspend Server</button>
+                            <button type="submit" class="btn btn-success">Server freigeben</button>
                         </form>
                     </div>
                 </div>
@@ -96,21 +96,21 @@
             <div class="col-sm-4">
                 <div class="box box-success">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Transfer Server</h3>
+                        <h3 class="box-title">Server übertragen</h3>
                     </div>
                     <div class="box-body">
                         <p>
-                            Transfer this server to another node connected to this panel.
-                            <strong>Warning!</strong> This feature has not been fully tested and may have bugs.
+                            Überträgt den Server zu einem andern Node, der mit diesem Panel verbunden ist
+                            <strong>ACHTUNG!</strong> Diese Funktion ist nicht vollständig ausgereift, mache lieber vorher ein Backup.
                         </p>
                     </div>
 
                     <div class="box-footer">
                         @if($canTransfer)
-                            <button class="btn btn-success" data-toggle="modal" data-target="#transferServerModal">Transfer Server</button>
+                            <button class="btn btn-success" data-toggle="modal" data-target="#transferServerModal">Server übertragen</button>
                         @else
-                            <button class="btn btn-success disabled">Transfer Server</button>
-                            <p style="padding-top: 1rem;">Transferring a server requires more than one node to be configured on your panel.</p>
+                            <button class="btn btn-success disabled">Server übertragen</button>
+                            <p style="padding-top: 1rem;">Ein Transfer ist erst ab mindestens 2 Nodes möglich, die über demselben Panel laufen.</p>
                         @endif
                     </div>
                 </div>
@@ -119,17 +119,17 @@
             <div class="col-sm-4">
                 <div class="box box-success">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Transfer Server</h3>
+                        <h3 class="box-title">Server übertragen</h3>
                     </div>
                     <div class="box-body">
                         <p>
-                            This server is currently being transferred to another node.
-                            Transfer was initiated at <strong>{{ $server->transfer->created_at }}</strong>
+                            Dieser Server wird gerade auf einen anderen Node übertragen
+                            Transfer wurde gestartet am <strong>{{ $server->transfer->created_at }}</strong>
                         </p>
                     </div>
 
                     <div class="box-footer">
-                        <button class="btn btn-success disabled">Transfer Server</button>
+                        <button class="btn btn-success disabled">Transfer läuft...</button>
                     </div>
                 </div>
             </div>
@@ -142,7 +142,7 @@
                 <form action="{{ route('admin.servers.view.manage.transfer', $server->id) }}" method="POST">
                     <div class="modal-header">
                         <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
-                        <h4 class="modal-title">Transfer Server</h4>
+                        <h4 class="modal-title">Server übertragen</h4>
                     </div>
 
                     <div class="modal-body">
@@ -164,27 +164,27 @@
                                         </optgroup>
                                     @endforeach
                                 </select>
-                                <p class="small text-muted no-margin">The node which this server will be transferred to.</p>
+                                <p class="small text-muted no-margin">Der Node, auf dem du diesen Server übertragen möchtest.</p>
                             </div>
 
                             <div class="form-group col-md-12">
-                                <label for="pAllocation">Default Allocation</label>
+                                <label for="pAllocation">Standard Port</label>
                                 <select name="allocation_id" id="pAllocation" class="form-control"></select>
-                                <p class="small text-muted no-margin">The main allocation that will be assigned to this server.</p>
+                                <p class="small text-muted no-margin">Der primäre Port für diesen Server.</p>
                             </div>
 
                             <div class="form-group col-md-12">
-                                <label for="pAllocationAdditional">Additional Allocation(s)</label>
+                                <label for="pAllocationAdditional">Weitere Ports</label>
                                 <select name="allocation_additional[]" id="pAllocationAdditional" class="form-control" multiple></select>
-                                <p class="small text-muted no-margin">Additional allocations to assign to this server on creation.</p>
+                                <p class="small text-muted no-margin">Ports, die ebenfalls reserviert werden sollen.</p>
                             </div>
                         </div>
                     </div>
 
                     <div class="modal-footer">
                         {!! csrf_field() !!}
-                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Cancel</button>
-                        <button type="submit" class="btn btn-success btn-sm">Confirm</button>
+                        <button type="button" class="btn btn-default btn-sm pull-left" data-dismiss="modal">Abbrechen</button>
+                        <button type="submit" class="btn btn-success btn-sm">Starten</button>
                     </div>
                 </form>
             </div>
diff --git a/resources/views/admin/servers/view/mounts.blade.php b/resources/views/admin/servers/view/mounts.blade.php
index 36ca98d..b5d6e53 100644
--- a/resources/views/admin/servers/view/mounts.blade.php
+++ b/resources/views/admin/servers/view/mounts.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    Server — {{ $server->name }}: Mounts
+    Server — {{ $server->name }}: Integrationen
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Manage server mounts.</small></h1>
+    <h1>{{ $server->name }}<small>Verwalte die Integrationen des Servers.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-        <li class="active">Mounts</li>
+        <li class="active">Integrationen</li>
     </ol>
 @endsection
 
@@ -21,7 +21,7 @@
         <div class="col-sm-12">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Available Mounts</h3>
+                    <h3 class="box-title">Verfügbare Integrationen</h3>
                 </div>
 
                 <div class="box-body table-responsible no-padding">
@@ -29,8 +29,8 @@
                         <tr>
                             <th>ID</th>
                             <th>Name</th>
-                            <th>Source</th>
-                            <th>Target</th>
+                            <th>Quelle</th>
+                            <th>Ziel</th>
                             <th>Status</th>
                             <th></th>
                         </tr>
@@ -44,7 +44,7 @@
 
                                 @if (! in_array($mount->id, $server->mounts->pluck('id')->toArray()))
                                     <td class="col-sm-2 middle">
-                                        <span class="label label-primary">Unmounted</span>
+                                        <span class="label label-primary">Nicht integriert</span>
                                     </td>
 
                                     <td class="col-sm-1 middle">
@@ -56,7 +56,7 @@
                                     </td>
                                 @else
                                     <td class="col-sm-2 middle">
-                                        <span class="label label-success">Mounted</span>
+                                        <span class="label label-success">Integriert</span>
                                     </td>
 
                                     <td class="col-sm-1 middle">
diff --git a/resources/views/admin/servers/view/startup.blade.php b/resources/views/admin/servers/view/startup.blade.php
index 0533029..dd10dd2 100644
--- a/resources/views/admin/servers/view/startup.blade.php
+++ b/resources/views/admin/servers/view/startup.blade.php
@@ -1,16 +1,16 @@
 @extends('layouts.admin')
 
 @section('title')
-    Server — {{ $server->name }}: Startup
+    Server — {{ $server->name }}: Startbefehl
 @endsection
 
 @section('content-header')
-    <h1>{{ $server->name }}<small>Control startup command as well as variables.</small></h1>
+    <h1>{{ $server->name }}<small>Verwalte hier den Startbefehl des Servers.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.servers') }}">Servers</a></li>
+        <li><a href="{{ route('admin.servers') }}">Alle Server</a></li>
         <li><a href="{{ route('admin.servers.view', $server->id) }}">{{ $server->name }}</a></li>
-        <li class="active">Startup</li>
+        <li class="active">Startbefehl</li>
     </ol>
 @endsection
 
@@ -21,20 +21,20 @@
         <div class="col-xs-12">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Startup Command Modification</h3>
+                    <h3 class="box-title">Startbefehl des Servers</h3>
                 </div>
                 <div class="box-body">
-                    <label for="pStartup" class="form-label">Startup Command</label>
+                    <label for="pStartup" class="form-label">Aktueller Befehl</label>
                     <input id="pStartup" name="startup" class="form-control" type="text" value="{{ old('startup', $server->startup) }}" />
-                    <p class="small text-muted">Edit your server's startup command here. The following variables are available by default: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, and <code>@{{SERVER_PORT}}</code>.</p>
+                    <p class="small text-muted">Hier kannst du den Startbefehl des Servers bearbeiten. Du kannst folgende Variablen nutzen, die Angaben direkt korrigieren, wenn du etwas davon veränderst: <code>@{{SERVER_MEMORY}}</code>, <code>@{{SERVER_IP}}</code>, und <code>@{{SERVER_PORT}}</code>.</p>
                 </div>
                 <div class="box-body">
-                    <label for="pDefaultStartupCommand" class="form-label">Default Service Start Command</label>
+                    <label for="pDefaultStartupCommand" class="form-label">Vordefinierter Startbefehl</label>
                     <input id="pDefaultStartupCommand" class="form-control" type="text" readonly />
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <button type="submit" class="btn btn-primary btn-sm pull-right">Save Modifications</button>
+                    <button type="submit" class="btn btn-primary btn-sm pull-right">Änderungen speichern</button>
                 </div>
             </div>
         </div>
@@ -43,16 +43,16 @@
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Service Configuration</h3>
+                    <h3 class="box-title">Konfiguration</h3>
                 </div>
                 <div class="box-body row">
                     <div class="col-xs-12">
                         <p class="small text-danger">
-                            Changing any of the below values will result in the server processing a re-install command. The server will be stopped and will then proceed.
-                            If you would like the service scripts to not run, ensure the box is checked at the bottom.
+                            Das Ändern eines der folgenden Werte führt dazu, dass der Server einen Neuinstallationsbefehl ausführt. Der Server wird gestoppt und fährt dann fort.
+                            Wenn du möchtest, dass die Dienstskripte nicht ausgeführt werden, stelle sicher, dass das Kontrollkästchen unten aktiviert ist.
                         </p>
                         <p class="small text-danger">
-                            <strong>This is a destructive operation in many cases. This server will be stopped immediately in order for this action to proceed.</strong>
+                            <strong>Änderungen werden hier nicht empfohlen, wenn bereits ein aktives Projekt auf diesem Server läuft.</strong>
                         </p>
                     </div>
                     <div class="form-group col-xs-12">
@@ -66,32 +66,32 @@
                                 >{{ $nest->name }}</option>
                             @endforeach
                         </select>
-                        <p class="small text-muted no-margin">Select the Nest that this server will be grouped into.</p>
+                        <p class="small text-muted no-margin">Wähle den Nest aus, der sich mit deinem Projekt identifiziert.</p>
                     </div>
                     <div class="form-group col-xs-12">
                         <label for="pEggId">Egg</label>
                         <select name="egg_id" id="pEggId" class="form-control"></select>
-                        <p class="small text-muted no-margin">Select the Egg that will provide processing data for this server.</p>
+                        <p class="small text-muted no-margin">Wähle den passenden Egg für deinen Server aus.</p>
                     </div>
                     <div class="form-group col-xs-12">
                         <div class="checkbox checkbox-primary no-margin-bottom">
                             <input id="pSkipScripting" name="skip_scripts" type="checkbox" value="1" @if($server->skip_scripts) checked @endif />
-                            <label for="pSkipScripting" class="strong">Skip Egg Install Script</label>
+                            <label for="pSkipScripting" class="strong">Egg Installationsskript überspringen</label>
                         </div>
-                        <p class="small text-muted no-margin">If the selected Egg has an install script attached to it, the script will run during install. If you would like to skip this step, check this box.</p>
+                        <p class="small text-muted no-margin">Wenn an das ausgewählte Egg ein Installationsskript angehängt ist, wird das Skript während der Installation ausgeführt. Wenn du diesen Schritt überspringen möchtest, aktiviere dieses Kontrollkästchen.</p>
                     </div>
                 </div>
             </div>
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Docker Image Configuration</h3>
+                    <h3 class="box-title">Docker Image Optionen</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
                         <label for="pDockerImage">Image</label>
                         <select id="pDockerImage" name="docker_image" class="form-control"></select>
-                        <input id="pDockerImageCustom" name="custom_docker_image" value="{{ old('custom_docker_image') }}" class="form-control" placeholder="Or enter a custom image..." style="margin-top:1rem"/>
-                        <p class="small text-muted no-margin">This is the Docker image that will be used to run this server. Select an image from the dropdown or enter a custom image in the text field above.</p>
+                        <input id="pDockerImageCustom" name="custom_docker_image" value="{{ old('custom_docker_image') }}" class="form-control" placeholder="Oder wähle ein anderes Image..." style="margin-top:1rem"/>
+                        <p class="small text-muted no-margin">Dies ist das aktuelle Docker Image, was für diesen Server verwendet wird. Du kannst im Menü auch ein anderes Image verwenden.</p>
                     </div>
                 </div>
             </div>
@@ -144,7 +144,7 @@
             $('#appendVariablesTo').html('');
             $.each(_.get(objectChain, 'variables', []), function (i, item) {
                 var setValue = _.get(Pterodactyl.server_variables, item.env_variable, item.default_value);
-                var isRequired = (item.required === 1) ? '<span class="label label-danger">Required</span> ' : '';
+                var isRequired = (item.required === 1) ? '<span class="label label-danger">Benötigt</span> ' : '';
                 var dataAppend = ' \
                     <div class="col-xs-12"> \
                         <div class="box"> \
@@ -156,8 +156,8 @@
                                 <p class="no-margin small text-muted">' + item.description + '</p> \
                             </div> \
                             <div class="box-footer"> \
-                                <p class="no-margin text-muted small"><strong>Startup Command Variable:</strong> <code>' + item.env_variable + '</code></p> \
-                                <p class="no-margin text-muted small"><strong>Input Rules:</strong> <code>' + item.rules + '</code></p> \
+                                <p class="no-margin text-muted small"><strong>Startbefehl Variable:</strong> <code>' + item.env_variable + '</code></p> \
+                                <p class="no-margin text-muted small"><strong>Regeln:</strong> <code>' + item.rules + '</code></p> \
                             </div> \
                         </div> \
                     </div>';
diff --git a/resources/views/admin/settings/advanced.blade.php b/resources/views/admin/settings/advanced.blade.php
index 0f2056c..9f3e4c3 100644
--- a/resources/views/admin/settings/advanced.blade.php
+++ b/resources/views/admin/settings/advanced.blade.php
@@ -2,14 +2,14 @@
 @include('partials/admin.settings.nav', ['activeTab' => 'advanced'])
 
 @section('title')
-    Advanced Settings
+    Erweitert
 @endsection
 
 @section('content-header')
-    <h1>Advanced Settings<small>Configure advanced settings for Pterodactyl.</small></h1>
+    <h1>Erweiterte Einstellungen<small>Stelle hier erweiterte Optionen für Pterodactyl ein.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Settings</li>
+        <li class="active">Einstellungen</li>
     </ol>
 @endsection
 
@@ -28,10 +28,10 @@
                                 <label class="control-label">Status</label>
                                 <div>
                                     <select class="form-control" name="recaptcha:enabled">
-                                        <option value="true">Enabled</option>
-                                        <option value="false" @if(old('recaptcha:enabled', config('recaptcha.enabled')) == '0') selected @endif>Disabled</option>
+                                        <option value="true">Aktiviert</option>
+                                        <option value="false" @if(old('recaptcha:enabled', config('recaptcha.enabled')) == '0') selected @endif>Deaktiviert</option>
                                     </select>
-                                    <p class="text-muted small">If enabled, login forms and password reset forms will do a silent captcha check and display a visible captcha if needed.</p>
+                                    <p class="text-muted small">Wenn es aktiv ist, dann wird man beim Anmelden nach einem reCAPTCHA gefragt.</p>
                                 </div>
                             </div>
                             <div class="form-group col-md-4">
@@ -44,7 +44,7 @@
                                 <label class="control-label">Secret Key</label>
                                 <div>
                                     <input type="text" required class="form-control" name="recaptcha:secret_key" value="{{ old('recaptcha:secret_key', config('recaptcha.secret_key')) }}">
-                                    <p class="text-muted small">Used for communication between your site and Google. Be sure to keep it a secret.</p>
+                                    <p class="text-muted small">Wird für die Kommunikation zwischen den Server und Google verwendet.</p>
                                 </div>
                             </div>
                         </div>
@@ -52,7 +52,7 @@
                             <div class="row">
                                 <div class="col-xs-12">
                                     <div class="alert alert-warning no-margin">
-                                        You are currently using reCAPTCHA keys that were shipped with this Panel. For improved security it is recommended to <a href="https://www.google.com/recaptcha/admin">generate new invisible reCAPTCHA keys</a> that tied specifically to your website.
+                                        Du nutzt aktuell einen Key, der bei der Installation hinzugefügt wurde. Erstelle bitte <a href="https://www.google.com/recaptcha/admin">hier</a> einen eigenen Key, damit du die volle Kontrolle dazu bekommst.
                                     </div>
                                 </div>
                             </div>
@@ -61,22 +61,22 @@
                 </div>
                 <div class="box">
                     <div class="box-header with-border">
-                        <h3 class="box-title">HTTP Connections</h3>
+                        <h3 class="box-title">HTTP Verbindungen</h3>
                     </div>
                     <div class="box-body">
                         <div class="row">
                             <div class="form-group col-md-6">
-                                <label class="control-label">Connection Timeout</label>
+                                <label class="control-label">Timeout bei Verbindung</label>
                                 <div>
                                     <input type="number" required class="form-control" name="pterodactyl:guzzle:connect_timeout" value="{{ old('pterodactyl:guzzle:connect_timeout', config('pterodactyl.guzzle.connect_timeout')) }}">
-                                    <p class="text-muted small">The amount of time in seconds to wait for a connection to be opened before throwing an error.</p>
+                                    <p class="text-muted small">Die Anzahl in Sekunden, wie lange versucht werden soll eine Verbindung herzustellen, bis es erfolglos ist.</p>
                                 </div>
                             </div>
                             <div class="form-group col-md-6">
-                                <label class="control-label">Request Timeout</label>
+                                <label class="control-label">Timeout bei Anfragen</label>
                                 <div>
                                     <input type="number" required class="form-control" name="pterodactyl:guzzle:timeout" value="{{ old('pterodactyl:guzzle:timeout', config('pterodactyl.guzzle.timeout')) }}">
-                                    <p class="text-muted small">The amount of time in seconds to wait for a request to be completed before throwing an error.</p>
+                                    <p class="text-muted small">Die Anzahl in Sekunden, wie lange versucht werden soll eine Anfrage zu senden, bis es erfolglos ist..</p>
                                 </div>
                             </div>
                         </div>
@@ -84,7 +84,7 @@
                 </div>
                 <div class="box">
                     <div class="box-header with-border">
-                        <h3 class="box-title">Automatic Allocation Creation</h3>
+                        <h3 class="box-title">Automatische Portfreigabe</h3>
                     </div>
                     <div class="box-body">
                         <div class="row">
@@ -92,24 +92,24 @@
                                 <label class="control-label">Status</label>
                                 <div>
                                     <select class="form-control" name="pterodactyl:client_features:allocations:enabled">
-                                        <option value="false">Disabled</option>
-                                        <option value="true" @if(old('pterodactyl:client_features:allocations:enabled', config('pterodactyl.client_features.allocations.enabled'))) selected @endif>Enabled</option>
+                                        <option value="false">Deaktiviert</option>
+                                        <option value="true" @if(old('pterodactyl:client_features:allocations:enabled', config('pterodactyl.client_features.allocations.enabled'))) selected @endif>Aktiviert</option>
                                     </select>
-                                    <p class="text-muted small">If enabled users will have the option to automatically create new allocations for their server via the frontend.</p>
+                                    <p class="text-muted small">Wenn es aktiv ist, dann können Benutzer eigene Portfreigaben machen. Jeder Port kann nur einmal zugeteilt werden.</p>
                                 </div>
                             </div>
                             <div class="form-group col-md-4">
-                                <label class="control-label">Starting Port</label>
+                                <label class="control-label">Startport</label>
                                 <div>
                                     <input type="number" class="form-control" name="pterodactyl:client_features:allocations:range_start" value="{{ old('pterodactyl:client_features:allocations:range_start', config('pterodactyl.client_features.allocations.range_start')) }}">
-                                    <p class="text-muted small">The starting port in the range that can be automatically allocated.</p>
+                                    <p class="text-muted small">Der Startport kann automatisch festgelegt werden.</p>
                                 </div>
                             </div>
                             <div class="form-group col-md-4">
-                                <label class="control-label">Ending Port</label>
+                                <label class="control-label">Endport</label>
                                 <div>
                                     <input type="number" class="form-control" name="pterodactyl:client_features:allocations:range_end" value="{{ old('pterodactyl:client_features:allocations:range_end', config('pterodactyl.client_features.allocations.range_end')) }}">
-                                    <p class="text-muted small">The ending port in the range that can be automatically allocated.</p>
+                                    <p class="text-muted small">Der Endport kann automatisch festgelegt werden.</p>
                                 </div>
                             </div>
                         </div>
@@ -118,7 +118,7 @@
                 <div class="box box-primary">
                     <div class="box-footer">
                         {{ csrf_field() }}
-                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Save</button>
+                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Speichern</button>
                     </div>
                 </div>
             </form>
diff --git a/resources/views/admin/settings/index.blade.php b/resources/views/admin/settings/index.blade.php
index 489646d..bd8de9b 100644
--- a/resources/views/admin/settings/index.blade.php
+++ b/resources/views/admin/settings/index.blade.php
@@ -2,14 +2,14 @@
 @include('partials/admin.settings.nav', ['activeTab' => 'basic'])
 
 @section('title')
-    Settings
+    Einstellungen
 @endsection
 
 @section('content-header')
-    <h1>Panel Settings<small>Configure Pterodactyl to your liking.</small></h1>
+    <h1>Panel Einstellungen<small>Stelle hier das Panel von Pterodactyl ein</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Settings</li>
+        <li class="active">Einstellungen</li>
     </ol>
 @endsection
 
@@ -19,54 +19,54 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Panel Settings</h3>
+                    <h3 class="box-title">Panel-Einstellungen</h3>
                 </div>
                 <form action="{{ route('admin.settings') }}" method="POST">
                     <div class="box-body">
                         <div class="row">
                             <div class="form-group col-md-4">
-                                <label class="control-label">Company Name</label>
+                                <label class="control-label">Name deines Systems</label>
                                 <div>
                                     <input type="text" class="form-control" name="app:name" value="{{ old('app:name', config('app.name')) }}" />
-                                    <p class="text-muted"><small>This is the name that is used throughout the panel and in emails sent to clients.</small></p>
+                                    <p class="text-muted"><small>Dieser Name wird überall im Panel angezeigt. Das spiegelt den Betreiber dieses Systems.</small></p>
                                 </div>
                             </div>
                             <div class="form-group col-md-4">
-                                <label class="control-label">Require 2-Factor Authentication</label>
+                                <label class="control-label">2-Faktor Authentifizierung erzwingen</label>
                                 <div>
                                     <div class="btn-group" data-toggle="buttons">
                                         @php
                                             $level = old('pterodactyl:auth:2fa_required', config('pterodactyl.auth.2fa_required'));
                                         @endphp
                                         <label class="btn btn-primary @if ($level == 0) active @endif">
-                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="0" @if ($level == 0) checked @endif> Not Required
+                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="0" @if ($level == 0) checked @endif> Nicht notwendig
                                         </label>
                                         <label class="btn btn-primary @if ($level == 1) active @endif">
-                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="1" @if ($level == 1) checked @endif> Admin Only
+                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="1" @if ($level == 1) checked @endif> Nur Admins
                                         </label>
                                         <label class="btn btn-primary @if ($level == 2) active @endif">
-                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="2" @if ($level == 2) checked @endif> All Users
+                                            <input type="radio" name="pterodactyl:auth:2fa_required" autocomplete="off" value="2" @if ($level == 2) checked @endif> Für alle Pflicht
                                         </label>
                                     </div>
-                                    <p class="text-muted"><small>If enabled, any account falling into the selected grouping will be required to have 2-Factor authentication enabled to use the Panel.</small></p>
+                                    <p class="text-muted"><small>Wenn diese Option aktiviert ist, muss für jedes Konto, das in die ausgewählte Gruppierung fällt, die 2-Faktor-Authentifizierung aktiviert sein, um das Panel verwenden zu können.</small></p>
                                 </div>
                             </div>
                             <div class="form-group col-md-4">
-                                <label class="control-label">Default Language</label>
+                                <label class="control-label">Sprache</label>
                                 <div>
                                     <select name="app:locale" class="form-control">
                                         @foreach($languages as $key => $value)
-                                            <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>{{ $value }}</option>
+                                            <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>Deutsch (GermanDactyl)</option>
                                         @endforeach
                                     </select>
-                                    <p class="text-muted"><small>The default language to use when rendering UI components.</small></p>
+                                    <p class="text-muted"><small>Die Standardsprache des Panels. Wenn du die englische Version nutzen möchtest, musst du GermanDactyl entfernen.</small></p>
                                 </div>
                             </div>
                         </div>
                     </div>
                     <div class="box-footer">
                         {!! csrf_field() !!}
-                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Save</button>
+                        <button type="submit" name="_method" value="PATCH" class="btn btn-sm btn-primary pull-right">Änderungen speichern</button>
                     </div>
                 </form>
             </div>
diff --git a/resources/views/admin/settings/mail.blade.php b/resources/views/admin/settings/mail.blade.php
index 9e99acd..f2cf2b6 100644
--- a/resources/views/admin/settings/mail.blade.php
+++ b/resources/views/admin/settings/mail.blade.php
@@ -2,14 +2,14 @@
 @include('partials/admin.settings.nav', ['activeTab' => 'mail'])
 
 @section('title')
-    Mail Settings
+    Mail-Versand
 @endsection
 
 @section('content-header')
-    <h1>Mail Settings<small>Configure how Pterodactyl should handle sending emails.</small></h1>
+    <h1>Mail-Versand<small>Richte hier dein Mail-System ein</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Settings</li>
+        <li class="active">Einstellungen</li>
     </ol>
 @endsection
 
@@ -19,14 +19,14 @@
         <div class="col-xs-12">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Email Settings</h3>
+                    <h3 class="box-title">Mail-Versand</h3>
                 </div>
                 @if($disabled)
                     <div class="box-body">
                         <div class="row">
                             <div class="col-xs-12">
                                 <div class="alert alert-info no-margin-bottom">
-                                    This interface is limited to instances using SMTP as the mail driver. Please either use <code>php artisan p:environment:mail</code> command to update your email settings, or set <code>MAIL_DRIVER=smtp</code> in your environment file.
+                                    Diese Schnittstelle ist auf Instanzen beschränkt, die SMTP als E-Mail-Treiber verwenden. Bitte verwende entweder den Befehl <code>php artisan p:environment:mail</code>, um Ihre E-Mail-Einstellungen zu aktualisieren, oder setze diese Zeile: <code>MAIL_DRIVER=smtp</code> in der Umgebungsdatei ein.
                                 </div>
                             </div>
                         </div>
@@ -39,59 +39,59 @@
                                     <label class="control-label">SMTP Host</label>
                                     <div>
                                         <input required type="text" class="form-control" name="mail:mailers:smtp:host" value="{{ old('mail:mailers:smtp:host', config('mail.mailers.smtp.host')) }}" />
-                                        <p class="text-muted small">Enter the SMTP server address that mail should be sent through.</p>
+                                        <p class="text-muted small">Gebe die Adresse des SMTP-Servers ein, über den E-Mails gesendet werden sollen.</p>
                                     </div>
                                 </div>
                                 <div class="form-group col-md-2">
                                     <label class="control-label">SMTP Port</label>
                                     <div>
                                         <input required type="number" class="form-control" name="mail:mailers:smtp:port" value="{{ old('mail:mailers:smtp:port', config('mail.mailers.smtp.port')) }}" />
-                                        <p class="text-muted small">Enter the SMTP server port that mail should be sent through.</p>
+                                        <p class="text-muted small">Gebe den Port des SMTP-Servers ein, über den E-Mails gesendet werden sollen.</p>
                                     </div>
                                 </div>
                                 <div class="form-group col-md-4">
-                                    <label class="control-label">Encryption</label>
+                                    <label class="control-label">Verschlüsselung</label>
                                     <div>
                                         @php
                                             $encryption = old('mail:mailers:smtp:encryption', config('mail.mailers.smtp.encryption'));
                                         @endphp
                                         <select name="mail:mailers:smtp:encryption" class="form-control">
-                                            <option value="" @if($encryption === '') selected @endif>None</option>
+                                            <option value="" @if($encryption === '') selected @endif>Keine</option>
                                             <option value="tls" @if($encryption === 'tls') selected @endif>Transport Layer Security (TLS)</option>
                                             <option value="ssl" @if($encryption === 'ssl') selected @endif>Secure Sockets Layer (SSL)</option>
                                         </select>
-                                        <p class="text-muted small">Select the type of encryption to use when sending mail.</p>
+                                        <p class="text-muted small">Wähle eine der Verschlüsselungen aus, mit der E-Mails versendet werden sollen.</p>
                                     </div>
                                 </div>
                                 <div class="form-group col-md-6">
-                                    <label class="control-label">Username <span class="field-optional"></span></label>
+                                    <label class="control-label">Benutzername <span class="field-optional"></span></label>
                                     <div>
                                         <input type="text" class="form-control" name="mail:mailers:smtp:username" value="{{ old('mail:mailers:smtp:username', config('mail.mailers.smtp.username')) }}" />
-                                        <p class="text-muted small">The username to use when connecting to the SMTP server.</p>
+                                        <p class="text-muted small">Der Benutzername, womit du dich mit dem SMTP-Server verbindest.</p>
                                     </div>
                                 </div>
                                 <div class="form-group col-md-6">
-                                    <label class="control-label">Password <span class="field-optional"></span></label>
+                                    <label class="control-label">Passwort <span class="field-optional"></span></label>
                                     <div>
                                         <input type="password" class="form-control" name="mail:mailers:smtp:password"/>
-                                        <p class="text-muted small">The password to use in conjunction with the SMTP username. Leave blank to continue using the existing password. To set the password to an empty value enter <code>!e</code> into the field.</p>
+                                        <p class="text-muted small">Das Kennwort, das zusammen mit dem SMTP-Benutzernamen verwendet werden soll. Lasse das Feld leer, um das vorhandene Passwort weiter zu verwenden. Um das Passwort auf einen leeren Wert zu setzen, gebe <code>!e</code> in das Feld ein.</p>
                                     </div>
                                 </div>
                             </div>
                             <div class="row">
                                 <hr />
                                 <div class="form-group col-md-6">
-                                    <label class="control-label">Mail From</label>
+                                    <label class="control-label">Absender</label>
                                     <div>
                                         <input required type="email" class="form-control" name="mail:from:address" value="{{ old('mail:from:address', config('mail.from.address')) }}" />
-                                        <p class="text-muted small">Enter an email address that all outgoing emails will originate from.</p>
+                                        <p class="text-muted small">Gib eine E-Mail Adresse ein, über die alle E-Mails versendet werden sollen..</p>
                                     </div>
                                 </div>
                                 <div class="form-group col-md-6">
-                                    <label class="control-label">Mail From Name <span class="field-optional"></span></label>
+                                    <label class="control-label">Name des Absenders <span class="field-optional"></span></label>
                                     <div>
                                         <input type="text" class="form-control" name="mail:from:name" value="{{ old('mail:from:name', config('mail.from.name')) }}" />
-                                        <p class="text-muted small">The name that emails should appear to come from.</p>
+                                        <p class="text-muted small">Gib hier deine Firma ein, damit der Empfänger weiß, von wem die E-Mail kommt.</p>
                                     </div>
                                 </div>
                             </div>
@@ -99,8 +99,8 @@
                         <div class="box-footer">
                             {{ csrf_field() }}
                             <div class="pull-right">
-                                <button type="button" id="testButton" class="btn btn-sm btn-success">Test</button>
-                                <button type="button" id="saveButton" class="btn btn-sm btn-primary">Save</button>
+                                <button type="button" id="testButton" class="btn btn-sm btn-success">Testen</button>
+                                <button type="button" id="saveButton" class="btn btn-sm btn-primary">Speichern</button>
                             </div>
                         </div>
                     </form>
@@ -137,10 +137,11 @@
         function testSettings() {
             swal({
                 type: 'info',
-                title: 'Test Mail Settings',
-                text: 'Click "Test" to begin the test.',
+                title: 'Mail-System testen',
+                text: 'Klicke auf START, um den Test zu starten.',
                 showCancelButton: true,
-                confirmButtonText: 'Test',
+                cancelButtonText: 'Abbrechen',
+                confirmButtonText: 'START',
                 closeOnConfirm: false,
                 showLoaderOnConfirm: true
             }, function () {
@@ -152,8 +153,8 @@
                     showErrorDialog(jqXHR, 'test');
                 }).done(function () {
                     swal({
-                        title: 'Success',
-                        text: 'The test message was sent successfully.',
+                        title: 'Es hat funktioniert! :)',
+                        text: 'Die Test E-Mail wurde erfolgreich versendet.',
                         type: 'success'
                     });
                 });
@@ -180,8 +181,8 @@
             }
 
             swal({
-                title: 'Whoops!',
-                text: 'An error occurred while attempting to ' + verb + ' mail settings: ' + errorText,
+                title: 'Ohje. Noch nicht ganz!',
+                text: 'Es trat ein Fehler bei ' + verb + ' auf.\n\n Mail-Einstellungen: ' + errorText,
                 type: 'error'
             });
         }
@@ -191,8 +192,8 @@
             $('#saveButton').on('click', function () {
                 saveSettings().done(function () {
                     swal({
-                        title: 'Success',
-                        text: 'Mail settings have been updated successfully and the queue worker was restarted to apply these changes.',
+                        title: 'Gespeichert und Online!',
+                        text: 'Die Einstellungen des Mail-Systems wurden übernommen und stehen jetzt allen Benutzern zur Verfügung, um E-Mails erhalten zu können.',
                         type: 'success'
                     });
                 });
diff --git a/resources/views/admin/users/index.blade.php b/resources/views/admin/users/index.blade.php
index 0c8e906..4404005 100644
--- a/resources/views/admin/users/index.blade.php
+++ b/resources/views/admin/users/index.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    List Users
+    Alle Benutzer
 @endsection
 
 @section('content-header')
-    <h1>Users<small>All registered users on the system.</small></h1>
+    <h1>Alle Benutzer<small>Alle registrierten Benutzer im System.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li class="active">Users</li>
+        <li class="active">Benutzer</li>
     </ol>
 @endsection
 
@@ -17,14 +17,14 @@
     <div class="col-xs-12">
         <div class="box box-primary">
             <div class="box-header with-border">
-                <h3 class="box-title">User List</h3>
+                <h3 class="box-title">Benutzer-Übersicht</h3>
                 <div class="box-tools search01">
                     <form action="{{ route('admin.users') }}" method="GET">
                         <div class="input-group input-group-sm">
-                            <input type="text" name="filter[email]" class="form-control pull-right" value="{{ request()->input('filter.email') }}" placeholder="Search">
+                            <input type="text" name="filter[email]" class="form-control pull-right" value="{{ request()->input('filter.email') }}" placeholder="Benutzer suchen">
                             <div class="input-group-btn">
                                 <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
-                                <a href="{{ route('admin.users.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Create New</button></a>
+                                <a href="{{ route('admin.users.new') }}"><button type="button" class="btn btn-sm btn-primary" style="border-radius: 0 3px 3px 0;margin-left:-1px;">Hinzufügen</button></a>
                             </div>
                         </div>
                     </form>
@@ -35,12 +35,12 @@
                     <thead>
                         <tr>
                             <th>ID</th>
-                            <th>Email</th>
-                            <th>Client Name</th>
-                            <th>Username</th>
-                            <th class="text-center">2FA</th>
-                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user is marked as the owner of.">Servers Owned</span></th>
-                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user can access because they are marked as a subuser.">Can Access</span></th>
+                            <th>E-Mail</th>
+                            <th>Name</th>
+                            <th>Benutzername</th>
+                            <th class="text-center">2-FA</th>
+                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user is marked as the owner of.">Server im Besitz</span></th>
+                            <th class="text-center"><span data-toggle="tooltip" data-placement="top" title="Servers that this user can access because they are marked as a subuser.">Aktiver Zugang</span></th>
                             <th></th>
                         </tr>
                     </thead>
diff --git a/resources/views/admin/users/new.blade.php b/resources/views/admin/users/new.blade.php
index 2ff5016..1621a25 100644
--- a/resources/views/admin/users/new.blade.php
+++ b/resources/views/admin/users/new.blade.php
@@ -1,15 +1,15 @@
 @extends('layouts.admin')
 
 @section('title')
-    Create User
+    Benutzer erstellen
 @endsection
 
 @section('content-header')
-    <h1>Create User<small>Add a new user to the system.</small></h1>
+    <h1>Benutzer erstellen<small>Füge einen Benutzer ins System hinzu.</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.users') }}">Users</a></li>
-        <li class="active">Create</li>
+        <li><a href="{{ route('admin.users') }}">Alle Benutzer</a></li>
+        <li class="active">Erstellen</li>
     </ol>
 @endsection
 
@@ -19,55 +19,55 @@
         <div class="col-md-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Identity</h3>
+                    <h3 class="box-title">Identität</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
-                        <label for="email" class="control-label">Email</label>
+                        <label for="email" class="control-label">E-Mail</label>
                         <div>
                             <input type="text" autocomplete="off" name="email" value="{{ old('email') }}" class="form-control" />
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="username" class="control-label">Username</label>
+                        <label for="username" class="control-label">Benutzername</label>
                         <div>
                             <input type="text" autocomplete="off" name="username" value="{{ old('username') }}" class="form-control" />
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="name_first" class="control-label">Client First Name</label>
+                        <label for="name_first" class="control-label">Vorname</label>
                         <div>
                             <input type="text" autocomplete="off" name="name_first" value="{{ old('name_first') }}" class="form-control" />
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="name_last" class="control-label">Client Last Name</label>
+                        <label for="name_last" class="control-label">Nachname</label>
                         <div>
                             <input type="text" autocomplete="off" name="name_last" value="{{ old('name_last') }}" class="form-control" />
                         </div>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Default Language</label>
+                        <label class="control-label">Sprache</label>
                         <div>
                             <select name="language" class="form-control">
                                 @foreach($languages as $key => $value)
-                                    <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>{{ $value }}</option>
+                                    <option value="{{ $key }}" @if(config('app.locale') === $key) selected @endif>Deutsch</option>
                                 @endforeach
                             </select>
-                            <p class="text-muted"><small>The default language to use when rendering the Panel for this user.</small></p>
+                            <p class="text-muted"><small>Die Sprache, mit der dieser Benutzer arbeiten wird.</small></p>
                         </div>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
-                    <input type="submit" value="Create User" class="btn btn-success btn-sm">
+                    <input type="submit" value="Benutzer erstellen" class="btn btn-success btn-sm">
                 </div>
             </div>
         </div>
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Permissions</h3>
+                    <h3 class="box-title">Berechtigungen</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group col-md-12">
@@ -77,7 +77,7 @@
                                 <option value="0">@lang('strings.no')</option>
                                 <option value="1">@lang('strings.yes')</option>
                             </select>
-                            <p class="text-muted"><small>Setting this to 'Yes' gives a user full administrative access.</small></p>
+                            <p class="text-muted"><small>Hier kannst du entscheiden, ob der Benutzer einen Zugang zu diesem Admin-Panel bekommen darf. WENN DU DIR UNSICHER BIST, DANN LASS ES SO WIE ES IST!</small></p>
                         </div>
                     </div>
                 </div>
@@ -86,15 +86,15 @@
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Password</h3>
+                    <h3 class="box-title">Passwort festlegen</h3>
                 </div>
                 <div class="box-body">
                     <div class="alert alert-info">
-                        <p>Providing a user password is optional. New user emails prompt users to create a password the first time they login. If a password is provided here you will need to find a different method of providing it to the user.</p>
+                        <p>Wenn du kein Passwort angibst, dann wird eins automatisch generiert. Die Voraussetzung hierbei ist, dass das E-Mail System betriebsbereit ist.</p>
                     </div>
                     <div id="gen_pass" class=" alert alert-success" style="display:none;margin-bottom: 10px;"></div>
                     <div class="form-group">
-                        <label for="pass" class="control-label">Password</label>
+                        <label for="pass" class="control-label">Passwort</label>
                         <div>
                             <input type="password" name="password" class="form-control" />
                         </div>
@@ -117,7 +117,7 @@
                     'X-CSRF-TOKEN': '{{ csrf_token() }}'
                },
                 success: function(data) {
-                    $("#gen_pass").html('<strong>Generated Password:</strong> ' + data).slideDown();
+                    $("#gen_pass").html('<strong>Generiertes Passwort:</strong> ' + data).slideDown();
                     $('input[name="password"], input[name="password_confirmation"]').val(data);
                     return false;
                 }
diff --git a/resources/views/admin/users/view.blade.php b/resources/views/admin/users/view.blade.php
index f042d89..a57d257 100644
--- a/resources/views/admin/users/view.blade.php
+++ b/resources/views/admin/users/view.blade.php
@@ -1,14 +1,14 @@
 @extends('layouts.admin')
 
 @section('title')
-    Manager User: {{ $user->username }}
+    Nutzer verwalten: {{ $user->username }}
 @endsection
 
 @section('content-header')
     <h1>{{ $user->name_first }} {{ $user->name_last}}<small>{{ $user->username }}</small></h1>
     <ol class="breadcrumb">
         <li><a href="{{ route('admin.index') }}">Admin</a></li>
-        <li><a href="{{ route('admin.users') }}">Users</a></li>
+        <li><a href="{{ route('admin.users') }}">Alle Benutzer</a></li>
         <li class="active">{{ $user->username }}</li>
     </ol>
 @endsection
@@ -19,64 +19,64 @@
         <div class="col-md-6">
             <div class="box box-primary">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Identity</h3>
+                    <h3 class="box-title">Identität</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
-                        <label for="email" class="control-label">Email</label>
+                        <label for="email" class="control-label">E-Mail</label>
                         <div>
                             <input type="email" name="email" value="{{ $user->email }}" class="form-control form-autocomplete-stop">
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="registered" class="control-label">Username</label>
+                        <label for="registered" class="control-label">Benutzername</label>
                         <div>
                             <input type="text" name="username" value="{{ $user->username }}" class="form-control form-autocomplete-stop">
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="registered" class="control-label">Client First Name</label>
+                        <label for="registered" class="control-label">Vorname</label>
                         <div>
                             <input type="text" name="name_first" value="{{ $user->name_first }}" class="form-control form-autocomplete-stop">
                         </div>
                     </div>
                     <div class="form-group">
-                        <label for="registered" class="control-label">Client Last Name</label>
+                        <label for="registered" class="control-label">Nachname</label>
                         <div>
                             <input type="text" name="name_last" value="{{ $user->name_last }}" class="form-control form-autocomplete-stop">
                         </div>
                     </div>
                     <div class="form-group">
-                        <label class="control-label">Default Language</label>
+                        <label class="control-label">Sprache</label>
                         <div>
                             <select name="language" class="form-control">
                                 @foreach($languages as $key => $value)
-                                    <option value="{{ $key }}" @if($user->language === $key) selected @endif>{{ $value }}</option>
+                                    <option value="{{ $key }}" @if($user->language === $key) selected @endif>Deutsch (GermanDactyl)</option>
                                 @endforeach
                             </select>
-                            <p class="text-muted"><small>The default language to use when rendering the Panel for this user.</small></p>
+                            <p class="text-muted"><small>Die Sprache, womit der Benutzer auf dem Panel arbeitet. Wenn du es suf englisch anbieten möchtest, dann musst du GermanDactyl entfernen.</small></p>
                         </div>
                     </div>
                 </div>
                 <div class="box-footer">
                     {!! csrf_field() !!}
                     {!! method_field('PATCH') !!}
-                    <input type="submit" value="Update User" class="btn btn-primary btn-sm">
+                    <input type="submit" value="Änderungen speichern" class="btn btn-primary btn-sm">
                 </div>
             </div>
         </div>
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Password</h3>
+                    <h3 class="box-title">Passwort-Verwaltung</h3>
                 </div>
                 <div class="box-body">
                     <div class="alert alert-success" style="display:none;margin-bottom:10px;" id="gen_pass"></div>
                     <div class="form-group no-margin-bottom">
-                        <label for="password" class="control-label">Password <span class="field-optional"></span></label>
+                        <label for="password" class="control-label">Passwort <span class="field-optional"></span></label>
                         <div>
                             <input type="password" id="password" name="password" class="form-control form-autocomplete-stop">
-                            <p class="text-muted small">Leave blank to keep this user's password the same. User will not receive any notification if password is changed.</p>
+                            <p class="text-muted small">Lass es leer, wenn es nicht geändert werden soll.</p>
                         </div>
                     </div>
                 </div>
@@ -85,7 +85,7 @@
         <div class="col-md-6">
             <div class="box">
                 <div class="box-header with-border">
-                    <h3 class="box-title">Permissions</h3>
+                    <h3 class="box-title">Berechtigungen</h3>
                 </div>
                 <div class="box-body">
                     <div class="form-group">
@@ -95,7 +95,7 @@
                                 <option value="0">@lang('strings.no')</option>
                                 <option value="1" {{ $user->root_admin ? 'selected="selected"' : '' }}>@lang('strings.yes')</option>
                             </select>
-                            <p class="text-muted"><small>Setting this to 'Yes' gives a user full administrative access.</small></p>
+                            <p class="text-muted"><small>Achte bitte darauf, wem du Zugriff auf dieses Panel gibst. Mit dieser Berechtigung kann dann der Benutzer dasselbe tun, wie du.</small></p>
                         </div>
                     </div>
                 </div>
@@ -105,16 +105,16 @@
     <div class="col-xs-12">
         <div class="box box-danger">
             <div class="box-header with-border">
-                <h3 class="box-title">Delete User</h3>
+                <h3 class="box-title">Benutzer entfernen</h3>
             </div>
             <div class="box-body">
-                <p class="no-margin">There must be no servers associated with this account in order for it to be deleted.</p>
+                <p class="no-margin">Es dürfen keine Server mit deren Account verbunden sein, wenn du den Account entfernen möchtest.</p>
             </div>
             <div class="box-footer">
                 <form action="{{ route('admin.users.view', $user->id) }}" method="POST">
                     {!! csrf_field() !!}
                     {!! method_field('DELETE') !!}
-                    <input id="delete" type="submit" class="btn btn-sm btn-danger pull-right" {{ $user->servers->count() < 1 ?: 'disabled' }} value="Delete User" />
+                    <input id="delete" type="submit" class="btn btn-sm btn-danger pull-right" {{ $user->servers->count() < 1 ?: 'disabled' }} value="Benutzer entfernen" />
                 </form>
             </div>
         </div>
diff --git a/resources/views/layouts/admin.blade.php b/resources/views/layouts/admin.blade.php
index 1543ddf..d5a2545 100644
--- a/resources/views/layouts/admin.blade.php
+++ b/resources/views/layouts/admin.blade.php
@@ -43,7 +43,7 @@
                 </a>
                 <nav class="navbar navbar-static-top">
                     <a href="#" class="sidebar-toggle" data-toggle="push-menu" role="button">
-                        <span class="sr-only">Toggle navigation</span>
+                        <span class="sr-only">Navigation umschalten</span>
                         <span class="icon-bar"></span>
                         <span class="icon-bar"></span>
                         <span class="icon-bar"></span>
@@ -57,10 +57,10 @@
                                 </a>
                             </li>
                             <li>
-                                <li><a href="{{ route('index') }}" data-toggle="tooltip" data-placement="bottom" title="Exit Admin Control"><i class="fa fa-server"></i></a></li>
+                                <li><a href="{{ route('index') }}" data-toggle="tooltip" data-placement="bottom" title="Admin-Panel verlassen"><i class="fa fa-server"></i></a></li>
                             </li>
                             <li>
-                                <li><a href="{{ route('auth.logout') }}" id="logoutButton" data-toggle="tooltip" data-placement="bottom" title="Logout"><i class="fa fa-sign-out"></i></a></li>
+                                <li><a href="{{ route('auth.logout') }}" id="logoutButton" data-toggle="tooltip" data-placement="bottom" title="Abmelden"><i class="fa fa-sign-out"></i></a></li>
                             </li>
                         </ul>
                     </div>
@@ -69,52 +69,53 @@
             <aside class="main-sidebar">
                 <section class="sidebar">
                     <ul class="sidebar-menu">
-                        <li class="header">BASIC ADMINISTRATION</li>
+
+                        <li class="header">ALLGEMEIN</li>
                         <li class="{{ Route::currentRouteName() !== 'admin.index' ?: 'active' }}">
                             <a href="{{ route('admin.index') }}">
-                                <i class="fa fa-home"></i> <span>Overview</span>
+                                <i class="fa fa-home"></i> <span>Übersicht</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.settings') ?: 'active' }}">
                             <a href="{{ route('admin.settings')}}">
-                                <i class="fa fa-wrench"></i> <span>Settings</span>
+                                <i class="fa fa-wrench"></i> <span>Einstellungen</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.api') ?: 'active' }}">
                             <a href="{{ route('admin.api.index')}}">
-                                <i class="fa fa-gamepad"></i> <span>Application API</span>
+                                <i class="fa fa-gamepad"></i> <span>Anwendungs-API</span>
                             </a>
                         </li>
                         <li class="header">MANAGEMENT</li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.databases') ?: 'active' }}">
                             <a href="{{ route('admin.databases') }}">
-                                <i class="fa fa-database"></i> <span>Databases</span>
+                                <i class="fa fa-database"></i> <span>Alle Datenbanken</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.locations') ?: 'active' }}">
                             <a href="{{ route('admin.locations') }}">
-                                <i class="fa fa-globe"></i> <span>Locations</span>
+                                <i class="fa fa-globe"></i> <span>Alle Standorte</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.nodes') ?: 'active' }}">
                             <a href="{{ route('admin.nodes') }}">
-                                <i class="fa fa-sitemap"></i> <span>Nodes</span>
+                                <i class="fa fa-sitemap"></i> <span>Alle Nodes</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.servers') ?: 'active' }}">
                             <a href="{{ route('admin.servers') }}">
-                                <i class="fa fa-server"></i> <span>Servers</span>
+                                <i class="fa fa-server"></i> <span>Alle Server</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.users') ?: 'active' }}">
                             <a href="{{ route('admin.users') }}">
-                                <i class="fa fa-users"></i> <span>Users</span>
+                                <i class="fa fa-users"></i> <span>Alle Benutzer</span>
                             </a>
                         </li>
                         <li class="header">SERVICE MANAGEMENT</li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.mounts') ?: 'active' }}">
                             <a href="{{ route('admin.mounts') }}">
-                                <i class="fa fa-magic"></i> <span>Mounts</span>
+                                <i class="fa fa-magic"></i> <span>Verknüpfungen</span>
                             </a>
                         </li>
                         <li class="{{ ! starts_with(Route::currentRouteName(), 'admin.nests') ?: 'active' }}">
@@ -134,7 +135,7 @@
                         <div class="col-xs-12">
                             @if (count($errors) > 0)
                                 <div class="alert alert-danger">
-                                    There was an error validating the data provided.<br><br>
+                                    Beim Validieren der bereitgestellten Daten ist ein Fehler aufgetreten.<br><br>
                                     <ul>
                                         @foreach ($errors->all() as $error)
                                             <li>{{ $error }}</li>
@@ -159,7 +160,7 @@
                     <strong><i class="fa fa-fw {{ $appIsGit ? 'fa-git-square' : 'fa-code-fork' }}"></i></strong> {{ $appVersion }}<br />
                     <strong><i class="fa fa-fw fa-clock-o"></i></strong> {{ round(microtime(true) - LARAVEL_START, 3) }}s
                 </div>
-                Copyright &copy; 2015 - {{ date('Y') }} <a href="https://pterodactyl.io/">Pterodactyl Software</a>.
+                Copyright &copy; 2015 - {{ date('Y') }} <a href="https://pterodactyl.io/">Pterodactyl Software</a> | Übersetzt von <a href="https://germandactyl.de/">Pavl21</a>.
             </footer>
         </div>
         @section('footer-scripts')
@@ -183,12 +184,13 @@
 
                         var that = this;
                         swal({
-                            title: 'Do you want to log out?',
+                            title: 'Möchtest du dich wirklich abmelden?',
                             type: 'warning',
                             showCancelButton: true,
+                            cancelButtonText: 'Abbrechen',
                             confirmButtonColor: '#d9534f',
                             cancelButtonColor: '#d33',
-                            confirmButtonText: 'Log out'
+                            confirmButtonText: 'Jetzt abmelden'
                         }, function () {
                              $.ajax({
                                 type: 'POST',
diff --git a/resources/views/partials/admin/settings/nav.blade.php b/resources/views/partials/admin/settings/nav.blade.php
index 9f1ace7..2f4093d 100644
--- a/resources/views/partials/admin/settings/nav.blade.php
+++ b/resources/views/partials/admin/settings/nav.blade.php
@@ -6,9 +6,9 @@
         <div class="col-xs-12">
             <div class="nav-tabs-custom nav-tabs-floating">
                 <ul class="nav nav-tabs">
-                    <li @if($activeTab === 'basic')class="active"@endif><a href="{{ route('admin.settings') }}">General</a></li>
-                    <li @if($activeTab === 'mail')class="active"@endif><a href="{{ route('admin.settings.mail') }}">Mail</a></li>
-                    <li @if($activeTab === 'advanced')class="active"@endif><a href="{{ route('admin.settings.advanced') }}">Advanced</a></li>
+                    <li @if($activeTab === 'basic')class="active"@endif><a href="{{ route('admin.settings') }}">Allgemein</a></li>
+                    <li @if($activeTab === 'mail')class="active"@endif><a href="{{ route('admin.settings.mail') }}">Mail-Versand</a></li>
+                    <li @if($activeTab === 'advanced')class="active"@endif><a href="{{ route('admin.settings.advanced') }}">Erweitert</a></li>
                 </ul>
             </div>
         </div>
diff --git a/resources/views/partials/admin/settings/notice.blade.php b/resources/views/partials/admin/settings/notice.blade.php
index 2dc0bc1..16f8890 100644
--- a/resources/views/partials/admin/settings/notice.blade.php
+++ b/resources/views/partials/admin/settings/notice.blade.php
@@ -3,7 +3,7 @@
         <div class="row">
             <div class="col-xs-12">
                 <div class="alert alert-danger">
-                    Your Panel is currently configured to read settings from the environment only. You will need to set <code>APP_ENVIRONMENT_ONLY=false</code> in your environment file in order to load settings dynamically.
+                    Dein Panel ist derzeit so konfiguriert, dass es nur Einstellungen aus der Umgebung liest. Du musst <code>APP_ENVIRONMENT_ONLY=false</code> in Ihrer Umgebungsdatei angeben, um Einstellungen dynamisch zu laden.
                 </div>
             </div>
         </div>
diff --git a/resources/views/vendor/notifications/email-plain.blade.php b/resources/views/vendor/notifications/email-plain.blade.php
index acefa65..ffbc0b0 100644
--- a/resources/views/vendor/notifications/email-plain.blade.php
+++ b/resources/views/vendor/notifications/email-plain.blade.php
@@ -3,7 +3,7 @@
 if (! empty($greeting)) {
     echo $greeting, "\n\n";
 } else {
-    echo $level == 'error' ? 'Whoops!' : 'Hello!', "\n\n";
+    echo $level == 'error' ? 'Ups!' : 'Hallo!', "\n\n";
 }
 
 if (! empty($introLines)) {
@@ -18,5 +18,5 @@ if (! empty($outroLines)) {
     echo implode("\n", $outroLines), "\n\n";
 }
 
-echo 'Regards,', "\n";
+echo 'Viele Grüße,', "\n";
 echo config('app.name'), "\n";
diff --git a/resources/views/vendor/notifications/email.blade.php b/resources/views/vendor/notifications/email.blade.php
index 94f86a9..3f6079c 100644
--- a/resources/views/vendor/notifications/email.blade.php
+++ b/resources/views/vendor/notifications/email.blade.php
@@ -88,9 +88,9 @@ $style = [
                                                 {{ $greeting }}
                                             @else
                                                 @if ($level == 'error')
-                                                    Whoops!
+                                                    Ups!
                                                 @else
-                                                    Hello!
+                                                    Hallo!
                                                 @endif
                                             @endif
                                         </h1>
@@ -140,7 +140,7 @@ $style = [
 
                                         <!-- Salutation -->
                                         <p style="{{ $style['paragraph'] }}">
-                                            Regards,<br>{{ config('app.name') }}
+                                            Viele Grüße, das Team von<br>{{ config('app.name') }}
                                         </p>
 
                                         <!-- Sub Copy -->
@@ -149,8 +149,8 @@ $style = [
                                                 <tr>
                                                     <td style="{{ $fontFamily }}">
                                                         <p style="{{ $style['paragraph-sub'] }}">
-                                                            If you’re having trouble clicking the "{{ $actionText }}" button,
-                                                            copy and paste the URL below into your web browser:
+                                                            Wenn der Button nicht funktioniert,
+                                                            dann nutze diesen Link in deinem Browser:
                                                         </p>
 
                                                         <p style="{{ $style['paragraph-sub'] }}">
@@ -177,7 +177,7 @@ $style = [
                                         <p style="{{ $style['paragraph-sub'] }}">
                                             &copy; {{ date('Y') }}
                                             <a style="{{ $style['anchor'] }}" href="{{ url('/') }}" target="_blank">{{ config('app.name') }}</a>.
-                                            All rights reserved.
+                                            Alle Rechte vorbehalten.
                                         </p>
                                     </td>
                                 </tr>
-- 
2.39.2

